<?xml version="1.0" encoding="utf-8"?>
<search>
  
  
  
  <entry>
    <title>go语言</title>
    <link href="/2026/05/10/go%E8%AF%AD%E8%A8%80/"/>
    <url>/2026/05/10/go%E8%AF%AD%E8%A8%80/</url>
    
    <content type="html"><![CDATA[<h1 id="Go-语言教程"><a href="#Go-语言教程" class="headerlink" title="Go 语言教程"></a>Go 语言教程</h1><blockquote><p>涵盖 Go 语言至今几乎所有在生产中使用的特性，从语法基础到运行时原理、并发模型、泛型、工具链等。每个主题均附深度解释、代码示例和常见陷阱。</p></blockquote><hr><h2 id="1-环境与工具链"><a href="#1-环境与工具链" class="headerlink" title="1. 环境与工具链"></a>1. 环境与工具链</h2><h3 id="安装与多版本管理"><a href="#安装与多版本管理" class="headerlink" title="安装与多版本管理"></a>安装与多版本管理</h3><p>从 <a href="https://go.dev/dl/">go.dev&#x2F;dl</a>下载安装。Linux 下可将压缩包解压到 <code>/usr/local/go</code>，并将 <code>/usr/local/go/bin</code>加入 <code>PATH</code>。</p><p><strong>管理多个 Go 版本</strong>：</p><p>- </p><pre><code class="hljs">**传统方式**：使用版本管理工具如 `goenv`或手动切换 `GOROOT`。</code></pre><p>- </p><pre><code class="hljs">**Go 1.21+ 推荐方式**：利用 `GOTOOLCHAIN`和 `go install`。<figure class="highlight apache"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs apache"><span class="hljs-comment"># 安装一个特定的 Go 版本到 $HOME/sdk</span><br><span class="hljs-attribute">go</span> install golang.org/dl/go1.<span class="hljs-number">22</span>.<span class="hljs-number">0</span>@latest<br><span class="hljs-attribute">go1</span>.<span class="hljs-number">22</span>.<span class="hljs-number">0</span> download<br><span class="hljs-comment"># 使用该版本</span><br><span class="hljs-attribute">go1</span>.<span class="hljs-number">22</span>.<span class="hljs-number">0</span> version<br></code></pre></td></tr></table></figure></code></pre><p>- </p><pre><code class="hljs">**工具链管理**：在 `go.mod`中通过 `toolchain`指令声明项目所需最低工具链版本。当本地工具链版本低于此要求时，Go 命令会自动下载并切换到符合要求的版本，此行为可由 `GOTOOLCHAIN`环境变量（如 `GOTOOLCHAIN=go1.22.0`或 `GOTOOLCHAIN=auto`）控制。这是实现可重复构建的重要一环。</code></pre><h3 id="项目初始化与模块"><a href="#项目初始化与模块" class="headerlink" title="项目初始化与模块"></a>项目初始化与模块</h3><figure class="highlight maxima"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs maxima"><span class="hljs-built_in">mkdir</span> hello &amp;&amp; cd hello<br><span class="hljs-built_in">go</span> <span class="hljs-built_in">mod</span> init <span class="hljs-built_in">example</span>/hello<br></code></pre></td></tr></table></figure><p>- </p><pre><code class="hljs">**`go.mod`文件**：定义了模块的**模块路径**（依赖导入的根路径）、**Go 版本**和**直接依赖**。它是 Go 模块的基石。</code></pre><p>- </p><pre><code class="hljs">**`go.sum`文件**：记录了所有直接和传递依赖的加密校验和（哈希值），用于保证构建的一致性。**切勿手动编辑此文件**。</code></pre><p>- </p><pre><code class="hljs">**模块路径**：通常采用**仓库地址**的格式，如 `github.com/username/project`。这解决了“依赖地狱”和 vendor 问题。</code></pre><h3 id="基础命令"><a href="#基础命令" class="headerlink" title="基础命令"></a>基础命令</h3><table><thead><tr><th>命令</th><th>作用</th></tr></thead><tbody><tr><td><code>go run .</code>或 <code>go run main.go</code></td><td>编译当前包&#x2F;文件并立即执行，适用于快速测试。</td></tr><tr><td><code>go build</code></td><td>编译当前包。<code>-o</code>指定输出路径和文件名。<code>-ldflags</code>可传递链接器标志，如 <code>-ldflags=&quot;-s -w&quot;</code>用于缩小二进制体积。</td></tr><tr><td><code>GOOS=linux GOARCH=arm64 go build</code></td><td>交叉编译。<code>GOOS</code>指定目标操作系统，<code>GOARCH</code>指定目标架构。</td></tr><tr><td><code>go install</code></td><td>编译并将二进制安装到 <code>$GOBIN</code>（默认为 <code>$GOPATH/bin</code>）。常用于安装全局工具。</td></tr><tr><td><code>go fmt ./...</code></td><td>格式化当前模块下所有 Go 文件。<code>gofmt</code>的风格是强制的，无争议。</td></tr><tr><td><code>go vet ./...</code></td><td>运行静态分析工具，报告可疑但可能编译通过的代码问题（如 <code>printf</code>格式错误）。</td></tr><tr><td><code>go test ./...</code></td><td>运行当前模块下所有包的测试。<code>-v</code>显示详情，<code>-run TestFuncName</code>运行特定测试。</td></tr><tr><td><code>go mod tidy</code></td><td><strong>核心命令</strong>。根据源码中的 <code>import</code>自动增删 <code>go.mod</code>中的依赖，并下载缺失的模块。每次提交前建议运行。</td></tr><tr><td><code>go get pkg@version</code></td><td>添加、升级或降级特定依赖。<code>@latest</code>获取最新版本。</td></tr></tbody></table><h3 id="工作区模式（Go-1-18-）"><a href="#工作区模式（Go-1-18-）" class="headerlink" title="工作区模式（Go 1.18+）"></a>工作区模式（Go 1.18+）</h3><p>用于同时开发多个互相依赖的本地模块，无需频繁修改 <code>go.mod</code>中的 <code>replace</code>指令。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-comment"># 在项目根目录（包含多个模块目录的地方）执行</span><br>go work init<br>go work use ./moduleA ./moduleB<br></code></pre></td></tr></table></figure><p>- </p><pre><code class="hljs">这会生成一个 `go.work`文件。`go`命令在工作区模式下会优先使用工作区内模块的本地版本，而非 `go.mod`中声明的版本。</code></pre><p>- </p><pre><code class="hljs">适用于微服务、前后端分离仓库等需要联调的场景。</code></pre><h3 id="关键环境变量"><a href="#关键环境变量" class="headerlink" title="关键环境变量"></a>关键环境变量</h3><p>- </p><pre><code class="hljs">`GOPROXY`：模块代理服务器。国内建议设置 `https://goproxy.cn,direct`以加速下载。`direct`表示代理失败时直连。</code></pre><p>- </p><pre><code class="hljs">`GOPRIVATE`：逗号分隔的模块路径前缀，匹配的模块请求将不经过代理，直接访问版本控制系统（如 Git）。用于私有仓库。</code></pre><p>- </p><pre><code class="hljs">`GONOSUMDB`：逗号分隔的模块路径前缀，匹配的模块将不经过校验和数据库检查。</code></pre><p>- </p><pre><code class="hljs">`CGO_ENABLED`：是否启用 cgo（C语言调用Go）。交叉编译时，如果目标平台不支持C库，通常需要设为 `0`。</code></pre><p>- </p><pre><code class="hljs">`GO111MODULE`：Go 1.16 后默认为 `on`，可忽略。历史遗留变量，用于切换模块模式。</code></pre><hr><h2 id="2-词法结构"><a href="#2-词法结构" class="headerlink" title="2. 词法结构"></a>2. 词法结构</h2><h3 id="注释"><a href="#注释" class="headerlink" title="注释"></a>注释</h3><p>- </p><pre><code class="hljs">**单行注释** `//`：会被 `go doc`工具提取，生成文档。紧贴在声明前的注释会被视为该声明的文档。</code></pre><p>- </p><pre><code class="hljs">**多行注释** `/* */`：通常用于包文档（`package`语句前）或临时注释掉大段代码。不支持嵌套。</code></pre><h3 id="标识符与关键字"><a href="#标识符与关键字" class="headerlink" title="标识符与关键字"></a>标识符与关键字</h3><p>- </p><pre><code class="hljs">**标识符**：由 Unicode 字母或下划线 `_`开头，后跟任意数量的 Unicode 字母、数字或下划线。区分大小写。`αβ`和 `Δx`是合法的。</code></pre><p>- </p><pre><code class="hljs">**25 个关键字**（不能用作标识符）：<figure class="highlight angelscript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs angelscript"><span class="hljs-keyword">break</span>    <span class="hljs-keyword">default</span>     func   <span class="hljs-keyword">interface</span> <span class="hljs-symbol">select</span><br><span class="hljs-symbol">case</span>     <span class="hljs-symbol">defer</span>       <span class="hljs-symbol">go</span>     <span class="hljs-symbol">map</span>       <span class="hljs-symbol">struct</span><br><span class="hljs-symbol">chan</span>     <span class="hljs-symbol">else</span>        <span class="hljs-symbol">goto</span>   <span class="hljs-symbol">package</span>   <span class="hljs-symbol">switch</span><br><span class="hljs-symbol">const</span>    <span class="hljs-symbol">fallthrough</span> <span class="hljs-symbol">if</span>     <span class="hljs-symbol">range</span>     <span class="hljs-symbol">type</span><br><span class="hljs-symbol">continue</span> <span class="hljs-symbol">for</span>         <span class="hljs-symbol">import</span> <span class="hljs-symbol">return</span>    <span class="hljs-symbol">var</span><br></code></pre></td></tr></table></figure></code></pre><p>- </p><pre><code class="hljs">**预声明标识符**：包括常量（`true`, `false`, `iota`, `nil`）、所有基本类型名、以及 `append`, `cap`, `close`, `complex`, `copy`, `delete`, `imag`, `len`, `make`, `new`, `panic`, `print`, `println`, `real`, `recover`等内置函数。**可以覆盖它们，但强烈不建议**，这会引发极大的混淆。</code></pre><h3 id="分号自动插入"><a href="#分号自动插入" class="headerlink" title="分号自动插入"></a>分号自动插入</h3><p>编译器会在行末的特定 token（如标识符、字面量、<code>break</code>, <code>continue</code>, <code>fallthrough</code>, <code>return</code>, <code>++</code>, <code>--</code>, <code>)</code>, <code>]</code>, <code>&#125;</code>）后自动插入分号。</p><p>- </p><pre><code class="hljs">**重要规则**：因此，左大括号 `&#123;`**不能** 另起一行，必须放在语句的同一行末尾，否则编译器会在行尾插入分号，导致语法错误。<figure class="highlight gml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs gml"><span class="hljs-comment">// 正确</span><br><span class="hljs-keyword">if</span> <span class="hljs-variable language_">x</span> &gt; <span class="hljs-number">0</span> &#123;<br>&#125;<br><span class="hljs-comment">// 错误</span><br><span class="hljs-keyword">if</span> <span class="hljs-variable language_">x</span> &gt; <span class="hljs-number">0</span><br>&#123; <span class="hljs-comment">// 编译错误: unexpected semicolon or newline before &#123;</span><br>&#125;<br></code></pre></td></tr></table></figure></code></pre><h3 id="字面量"><a href="#字面量" class="headerlink" title="字面量"></a>字面量</h3><p>- </p><pre><code class="hljs">**整数**：`42`（十进制），`0b1010`（二进制，Go 1.13+），`0o755`（八进制，Go 1.13+），`0xFF`（十六进制）。可用下划线 `_`增强可读性，如 `1_000_000`。</code></pre><p>- </p><pre><code class="hljs">**浮点数**：`3.14`，`1.5e-10`（科学计数法）。</code></pre><p>- </p><pre><code class="hljs">**复数**：`2+3i`，`1.2e3+4.5i`。`complex64`实虚部为 `float32`，`complex128`为 `float64`。</code></pre><p>- </p><pre><code class="hljs">**字符 (rune)**：用单引号，表示一个 Unicode 码点（`int32`）。`&#39;a&#39;`，`&#39;\n&#39;`（转义），`&#39;\x41&#39;`（十六进制），`&#39;\u4e2d&#39;`（Unicode），`&#39;\U0001F600&#39;`（扩展 Unicode）。</code></pre><p>- </p><pre><code class="hljs">**字符串**：双引号 `&quot;Hello\n&quot;`支持转义；反引号 ``raw \n string``原样保留，包括换行，常用于正则、JSON、SQL等多行文本。</code></pre><hr><h2 id="3-类型系统"><a href="#3-类型系统" class="headerlink" title="3. 类型系统"></a>3. 类型系统</h2><h3 id="基本类型一览"><a href="#基本类型一览" class="headerlink" title="基本类型一览"></a>基本类型一览</h3><table><thead><tr><th>类型</th><th>说明</th><th>零值</th></tr></thead><tbody><tr><td><code>bool</code></td><td>布尔</td><td><code>false</code></td></tr><tr><td><code>string</code></td><td>不可变 UTF-8 字符串</td><td><code>&quot;&quot;</code></td></tr><tr><td><code>int</code>, <code>int8</code>, <code>int16</code>, <code>int32</code>, <code>int64</code></td><td>有符号整数</td><td><code>0</code></td></tr><tr><td><code>uint</code>, <code>uint8</code>, <code>uint16</code>, <code>uint32</code>, <code>uint64</code>, <code>uintptr</code></td><td>无符号整数</td><td><code>0</code></td></tr><tr><td><code>byte</code></td><td><code>uint8</code>的别名，强调原始数据</td><td><code>0</code></td></tr><tr><td><code>rune</code></td><td><code>int32</code>的别名，表示 Unicode 码点</td><td><code>0</code></td></tr><tr><td><code>float32</code>, <code>float64</code></td><td>IEEE-754 浮点数</td><td><code>0</code></td></tr><tr><td><code>complex64</code>, <code>complex128</code></td><td>复数</td><td><code>0+0i</code></td></tr></tbody></table><p>- </p><pre><code class="hljs">`int`和 `uint`的长度取决于目标平台（32 位系统为 32 位，64 位系统为 64 位）。编写有确定范围的整数时，应使用明确长度的类型（如 `int32`）。</code></pre><p>- </p><pre><code class="hljs">`uintptr`大小足以存储一个指针的位模式，仅用于底层编程（如 `unsafe`包、CGo）。</code></pre><p>- </p><pre><code class="hljs">**零值机制**：每个类型都有默认零值，变量声明后即拥有此值。这消除了未初始化变量的不确定性，是 Go 安全性的重要基石。</code></pre><h3 id="类型定义-vs-类型别名"><a href="#类型定义-vs-类型别名" class="headerlink" title="类型定义 vs 类型别名"></a>类型定义 vs 类型别名</h3><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-comment">// 类型定义：创建全新的命名类型</span><br><span class="hljs-keyword">type</span> Celsius <span class="hljs-type">float64</span><br><span class="hljs-keyword">var</span> c Celsius = <span class="hljs-number">100.0</span><br><span class="hljs-keyword">var</span> f <span class="hljs-type">float64</span> = <span class="hljs-number">37.0</span><br><span class="hljs-comment">// c = f          // 编译错误：类型不匹配</span><br>c = Celsius(f)    <span class="hljs-comment">// 需要显式类型转换</span><br><span class="hljs-comment">// 可以为 Celsius 定义方法</span><br><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-params">(c Celsius)</span></span> String() <span class="hljs-type">string</span> &#123; <span class="hljs-keyword">return</span> fmt.Sprintf(<span class="hljs-string">&quot;%.2f°C&quot;</span>, c) &#125;<br><br><span class="hljs-comment">// 类型别名：只是同一类型的另一个名字</span><br><span class="hljs-keyword">type</span> MyInt = <span class="hljs-type">int</span><br><span class="hljs-keyword">var</span> a <span class="hljs-type">int</span> = <span class="hljs-number">10</span><br><span class="hljs-keyword">var</span> b MyInt = a <span class="hljs-comment">// 无需转换，完全等价</span><br><span class="hljs-comment">// 不能为 MyInt 定义 int 不存在的方法</span><br></code></pre></td></tr></table></figure><p>- </p><pre><code class="hljs">类型定义是**强类型**的基石，提供了类型安全和方法绑定的能力。</code></pre><p>- </p><pre><code class="hljs">类型别名主要用于**代码重构**（平滑迁移）或为复杂类型提供更短的名称。</code></pre><h3 id="可比较性与排序"><a href="#可比较性与排序" class="headerlink" title="可比较性与排序"></a>可比较性与排序</h3><p>- </p><pre><code class="hljs">**可比较**（`==`, `!=`）：- 基本类型、指针、通道、接口、结构体（当所有字段可比较时）、数组（当元素可比较时）。- 切片、映射、函数**不可比较**，只能与 `nil`比较。</code></pre><p>- </p><pre><code class="hljs">**接口比较**：比较两个接口值时，会比较它们的动态类型和动态值。**如果动态类型不可比较（例如切片、映射、函数），则运行时会 panic**。</code></pre><p>- </p><pre><code class="hljs">**可排序**（`&lt;`, `&lt;=`, `&gt;`, `&gt;=`）：整数、浮点数、字符串（按字典序）。</code></pre><p>- </p><pre><code class="hljs">结构体和数组的比较是**逐字段/逐元素**进行的，且要求它们的类型必须完全一致（或底层类型一致且至少一个是未命名类型）。</code></pre><hr><h2 id="4-变量与常量"><a href="#4-变量与常量" class="headerlink" title="4. 变量与常量"></a>4. 变量与常量</h2><h3 id="变量声明"><a href="#变量声明" class="headerlink" title="变量声明"></a>变量声明</h3><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-keyword">var</span> a <span class="hljs-type">int</span> = <span class="hljs-number">10</span>       <span class="hljs-comment">// 完整声明，类型在变量名之后</span><br><span class="hljs-keyword">var</span> b = <span class="hljs-number">20</span>           <span class="hljs-comment">// 类型推断</span><br>c := <span class="hljs-number">30</span>              <span class="hljs-comment">// 短变量声明（只能在函数内部使用）</span><br><span class="hljs-keyword">var</span> x, y <span class="hljs-type">int</span> = <span class="hljs-number">1</span>, <span class="hljs-number">2</span>  <span class="hljs-comment">// 多变量声明</span><br><span class="hljs-keyword">var</span> (<br>    name = <span class="hljs-string">&quot;Alice&quot;</span><br>    age  = <span class="hljs-number">30</span><br>)<br></code></pre></td></tr></table></figure><p>- </p><pre><code class="hljs">**短变量声明 `:=`**：- 它是声明**并初始化**的快捷方式。- 要求 `:=`左侧**至少有一个**变量是**新声明的**。- 允许与已声明的变量混用，此时对旧变量是**赋值**，对新变量是**声明**。<figure class="highlight stata"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><code class="hljs stata"><span class="hljs-keyword">file</span>, <span class="hljs-keyword">err</span> := os.<span class="hljs-keyword">Open</span>(<span class="hljs-string">&quot;a.txt&quot;</span>) <span class="hljs-comment">// 声明 file 和 err</span><br><span class="hljs-comment">// ... 使用 file</span><br><span class="hljs-keyword">file</span>, <span class="hljs-keyword">err</span> := os.Create(<span class="hljs-string">&quot;b.txt&quot;</span>) <span class="hljs-comment">// 错误！file 已声明，但 err 可能已声明（如果在上一步中未出错）。在 Go 1.20 前，如果 err 已在同一作用域声明，此句会编译错误。更清晰的写法是：</span><br><span class="hljs-keyword">file</span>, <span class="hljs-keyword">err</span> = os.Create(<span class="hljs-string">&quot;b.txt&quot;</span>) <span class="hljs-comment">// 使用赋值</span><br><span class="hljs-comment">// 或者</span><br>newFile, <span class="hljs-keyword">err</span> := os.Create(<span class="hljs-string">&quot;b.txt&quot;</span>) <span class="hljs-comment">// 声明一个新变量</span><br><br>如果短变量声明位于一个新的代码块内（如 <span class="hljs-keyword">if</span>、<span class="hljs-keyword">for</span> 中），它会创建新的局部变量，而不是赋值给外部变量：<br><span class="hljs-keyword">var</span> <span class="hljs-keyword">err</span> <span class="hljs-keyword">error</span><br><span class="hljs-keyword">if</span> true &#123;<br><span class="hljs-keyword">file</span>, <span class="hljs-keyword">err</span> := os.Create(<span class="hljs-string">&quot;b.txt&quot;</span>) <span class="hljs-comment">// 这里 err 是新局部变量，不会影响外部的 err</span><br>&#125;<br></code></pre></td></tr></table></figure></code></pre><h3 id="常量与无类型常量"><a href="#常量与无类型常量" class="headerlink" title="常量与无类型常量"></a>常量与无类型常量</h3><p>- </p><pre><code class="hljs">常量在编译时求值，只能是布尔、数字（整数、浮点数、复数）或字符串。</code></pre><p>- </p><pre><code class="hljs">**无类型常量**：像 `42`, `3.14`, `&quot;hello&quot;`这样的字面量常量，在 Go 中具有“无类型”的属性。它们拥有比基本类型更高的精度（如高精度整数、任意精度浮点数），并且可以根据上下文**隐式转换**为特定类型，只要不溢出或损失精度。<figure class="highlight arcade"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs arcade">const <span class="hljs-literal">Pi</span> = <span class="hljs-number">3.14159265358979323846264338327950288419716939937510582097494459</span><br><span class="hljs-keyword">var</span> f32 float32 = <span class="hljs-literal">Pi</span> <span class="hljs-comment">// 隐式转换为 float32，精度损失</span><br><span class="hljs-keyword">var</span> f64 float64 = <span class="hljs-literal">Pi</span> <span class="hljs-comment">// 隐式转换为 float64</span><br><span class="hljs-keyword">var</span> i int = <span class="hljs-literal">Pi</span>       <span class="hljs-comment">// 错误：常量 3.14159... 被截断为整数</span><br></code></pre></td></tr></table></figure></code></pre><p>- </p><pre><code class="hljs">无类型常量有一个**默认类型**（在需要类型明确的上下文中使用）：整数是 `int`，浮点数是 `float64`，复数是 `complex128`，字符是 `rune`，字符串是 `string`。</code></pre><p>在 Go 语言中，<strong>无类型常量</strong>（untyped constant）是指声明时不指定具体类型的常量。</p><p><strong>没有固定类型</strong><br>例如 <code>const a = 100</code>，<code>a</code> 没有一个确定的类型（不像 <code>const b int = 100</code> 有 <code>int</code> 类型）。</p><ol><li><p><strong>更高的数值精度</strong><br> 无类型数值常量可以有非常高的精度（至少 256 位），不会溢出或截断，直到被赋值给一个具体类型的变量。</p></li><li><p><strong>隐式转换灵活</strong><br> 可以赋值给任何能容纳其值的变量，而无需显式转换：</p> <figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-keyword">const</span> untyped = <span class="hljs-number">100</span><br><span class="hljs-keyword">var</span> i <span class="hljs-type">int8</span> = untyped      <span class="hljs-comment">// 可以，100 在 int8 范围内</span><br><span class="hljs-keyword">var</span> f <span class="hljs-type">float64</span> = untyped   <span class="hljs-comment">// 变成 100.0</span><br><span class="hljs-keyword">var</span> b <span class="hljs-type">byte</span> = untyped      <span class="hljs-comment">// 可以</span><br></code></pre></td></tr></table></figure></li><li><p><strong>参与表达式仍保持无类型</strong><br> 多个无类型常量运算，结果仍是无类型。如果混合了类型，才可能产生类型。</p></li></ol><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-keyword">const</span> x = <span class="hljs-number">1</span> &lt;&lt; <span class="hljs-number">100</span>      <span class="hljs-comment">// 无类型，很大</span><br><span class="hljs-comment">// var y int = x        // 错误：x 太大，无法放入 int（编译会报错）</span><br><span class="hljs-keyword">var</span> z = x &gt;&gt; <span class="hljs-number">99</span>         <span class="hljs-comment">// z 变成无类型常量 2</span><br></code></pre></td></tr></table></figure><table><thead><tr><th>类型化常量</th><th>无类型常量</th></tr></thead><tbody><tr><td><code>const typed int = 100</code></td><td><code>const untyped = 100</code></td></tr><tr><td>有固定类型，赋值需显式转换</td><td>无固定类型，赋值自动适应</td></tr></tbody></table><h3 id="iota-进阶"><a href="#iota-进阶" class="headerlink" title="iota 进阶"></a>iota 进阶</h3><p><code>iota</code>是一个预声明的标识符，在 <code>const</code>声明块中，它表示连续的、无类型的整数常量，从 0 开始，每遇到一个 <code>const</code>关键字重置为 0，在同一个 <code>const</code>块中每新增一行声明，<code>iota</code>递增 1。</p><figure class="highlight maxima"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br></pre></td><td class="code"><pre><code class="hljs maxima">// <span class="hljs-number">1</span>. 基本枚举<br>const (<br>    Unknown = iota // <span class="hljs-number">0</span><br>    Running        // <span class="hljs-number">1</span><br>    Stopped        // <span class="hljs-number">2</span><br>)<br><br>// <span class="hljs-number">2</span>. 带表达式的枚举<br>const (<br>    <span class="hljs-symbol">_</span> = iota // 跳过 <span class="hljs-number">0</span><br>    KB ByteSize = <span class="hljs-number">1</span> &lt;&lt; (<span class="hljs-number">10</span> * iota) // <span class="hljs-number">1</span> &lt;&lt; <span class="hljs-number">10</span><br>    MB                             // <span class="hljs-number">1</span> &lt;&lt; <span class="hljs-number">20</span><br>    GB                             // <span class="hljs-number">1</span> &lt;&lt; <span class="hljs-number">30</span><br>)<br><br>// <span class="hljs-number">3</span>. 位掩码<br>const (<br>    FlagRead = <span class="hljs-number">1</span> &lt;&lt; iota // <span class="hljs-number">1</span> (<span class="hljs-number">0b001</span>)<br>    FlagWrite            // <span class="hljs-number">2</span> (<span class="hljs-number">0b010</span>)<br>    FlagExec             // <span class="hljs-number">4</span> (<span class="hljs-number">0b100</span>)<br>)<br>// 使用：perm := FlagRead | FlagWrite<br><br>// <span class="hljs-number">4</span>. 跳值<br>const (<br>    a = iota // <span class="hljs-number">0</span><br>    b        // <span class="hljs-number">1</span><br>    <span class="hljs-symbol">_</span>        // <span class="hljs-number">2</span> (被跳过，但 iota 仍递增)<br>    c        // <span class="hljs-number">3</span><br>)<br></code></pre></td></tr></table></figure><p><code>iota</code> 是 Go 语言中一个<strong>预声明的标识符</strong>，专用于 <code>const</code> 声明块中，用来生成<strong>连续的整数常量</strong>。它提供了一种简洁的方式定义枚举、位掩码等序列。</p><ul><li>在每个 <code>const</code> 块中，<code>iota</code> 从 <strong>0</strong> 开始。</li><li>每遇到一行常量声明（即每次 <code>const</code> 块内的新行），<code>iota</code> 自动<strong>递增 1</strong>。</li><li><strong>同一行</strong>的多个常量赋值（如 <code>X, Y = iota, iota</code>）<strong>共享同一个</strong> <code>iota</code> 值（不会分别递增）。</li><li>离开 <code>const</code> 块后，<code>iota</code> 重置为 0。</li></ul><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-keyword">const</span> (<br>    A = <span class="hljs-literal">iota</span>  <span class="hljs-comment">// 0</span><br>    B = <span class="hljs-literal">iota</span>  <span class="hljs-comment">// 1</span><br>    C = <span class="hljs-literal">iota</span>  <span class="hljs-comment">// 2</span><br>)<br><br><span class="hljs-comment">// 可以简写为（省略重复的 = iota）：</span><br><span class="hljs-keyword">const</span> (<br>    D = <span class="hljs-literal">iota</span>  <span class="hljs-comment">// 0</span><br>    E         <span class="hljs-comment">// 1</span><br>    F         <span class="hljs-comment">// 2</span><br>)<br></code></pre></td></tr></table></figure><h4 id="1-从非零值开始"><a href="#1-从非零值开始" class="headerlink" title="1. 从非零值开始"></a>1. 从非零值开始</h4><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-keyword">const</span> (<br>    One = <span class="hljs-literal">iota</span> + <span class="hljs-number">1</span>   <span class="hljs-comment">// 1</span><br>    Two              <span class="hljs-comment">// 2</span><br>    Three            <span class="hljs-comment">// 3</span><br>)<br></code></pre></td></tr></table></figure><h4 id="2-使用表达式"><a href="#2-使用表达式" class="headerlink" title="2. 使用表达式"></a>2. 使用表达式</h4><p><code>iota</code> 可以与运算符、函数结合：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-keyword">const</span> (<br>    KB = <span class="hljs-number">1</span> &lt;&lt; (<span class="hljs-number">10</span> * <span class="hljs-literal">iota</span>)   <span class="hljs-comment">// 1 &lt;&lt; (10*0) = 1</span><br>    MB                      <span class="hljs-comment">// 1 &lt;&lt; (10*1) = 1024</span><br>    GB                      <span class="hljs-comment">// 1 &lt;&lt; (10*2) = 1048576</span><br>)<br></code></pre></td></tr></table></figure><h4 id="3-跳过某些值"><a href="#3-跳过某些值" class="headerlink" title="3. 跳过某些值"></a>3. 跳过某些值</h4><p>使用 <code>_</code> 忽略不需要的 <code>iota</code>：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-keyword">const</span> (<br>    _ = <span class="hljs-literal">iota</span>      <span class="hljs-comment">// 0，跳过</span><br>    Red           <span class="hljs-comment">// 1</span><br>    Blue          <span class="hljs-comment">// 2</span><br>    Green         <span class="hljs-comment">// 3</span><br>)<br></code></pre></td></tr></table></figure><h4 id="4-位掩码（标志位）"><a href="#4-位掩码（标志位）" class="headerlink" title="4. 位掩码（标志位）"></a>4. 位掩码（标志位）</h4><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-keyword">const</span> (<br>    FlagUp = <span class="hljs-number">1</span> &lt;&lt; <span class="hljs-literal">iota</span>      <span class="hljs-comment">// 1</span><br>    FlagBroadcast           <span class="hljs-comment">// 2</span><br>    FlagLoopback            <span class="hljs-comment">// 4</span><br>    FlagPointToPoint        <span class="hljs-comment">// 8</span><br>    FlagMulticast           <span class="hljs-comment">// 16</span><br>)<br></code></pre></td></tr></table></figure><h4 id="5-在同一行中使用"><a href="#5-在同一行中使用" class="headerlink" title="5. 在同一行中使用"></a>5. 在同一行中使用</h4><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-keyword">const</span> (<br>    X, Y = <span class="hljs-literal">iota</span>, <span class="hljs-literal">iota</span>   <span class="hljs-comment">// 0, 0</span><br>    X2, Y2              <span class="hljs-comment">// 1, 1</span><br>)<br></code></pre></td></tr></table></figure><ul><li><p><code>iota</code> 仅在 <code>const</code> 块内有效，在函数或其他作用域中使用会编译错误。</p></li><li><p>如果 <code>const</code> 块中有多个常量定义，但某些未显式使用 <code>iota</code>，仍会按行递增吗？<br>  <strong>是的</strong>，因为 <code>iota</code> 是按行递增的，即使该行没有用到它（如只写 <code>A = 5</code> 下一行 <code>B</code> 若不赋值，会沿用上一行的表达式，此时 <code>iota</code> 仍已递增）。<br>  例：</p>  <figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-keyword">const</span> (<br>    A = <span class="hljs-number">5</span>       <span class="hljs-comment">// iota = 0 未使用</span><br>    B           <span class="hljs-comment">// iota = 1，沿用上一行表达式 =&gt; B = 5</span><br>    C = <span class="hljs-literal">iota</span>    <span class="hljs-comment">// iota = 2 =&gt; C = 2</span><br>)<br></code></pre></td></tr></table></figure><p>  虽然 A 没有用 <code>iota</code>，但 <code>iota</code> 的值仍然在递增。</p></li><li><p>不要在同一个 <code>const</code> 块内混合方式过于复杂，否则可读性降低。</p></li></ul><ol><li><strong>枚举常量</strong>：<code>StateRunning = iota</code> 等。</li><li><strong>数据库字段状态</strong>：<code>StatusPending</code>, <code>StatusApproved</code>, <code>StatusRejected</code>。</li><li><strong>选项标志</strong>（<code>io.Reader</code> 等标准库中大量使用位掩码）。</li></ol><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-keyword">package</span> main<br><br><span class="hljs-keyword">import</span> <span class="hljs-string">&quot;fmt&quot;</span><br><br><span class="hljs-keyword">type</span> Weekday <span class="hljs-type">int</span><br><br><span class="hljs-keyword">const</span> (<br>    Sunday Weekday = <span class="hljs-literal">iota</span>   <span class="hljs-comment">// 0</span><br>    Monday                  <span class="hljs-comment">// 1</span><br>    Tuesday                 <span class="hljs-comment">// 2</span><br>    Wednesday<br>    Thursday<br>    Friday<br>    Saturday<br>)<br><br><span class="hljs-keyword">const</span> (<br>    _ = <span class="hljs-literal">iota</span><br>    KB = <span class="hljs-number">1</span> &lt;&lt; (<span class="hljs-number">10</span> * <span class="hljs-literal">iota</span>)   <span class="hljs-comment">// 1 &lt;&lt; (10*1) = 1024</span><br>    MB                      <span class="hljs-comment">// 1048576</span><br>    GB                      <span class="hljs-comment">// 1073741824</span><br>)<br><br><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">main</span><span class="hljs-params">()</span></span> &#123;<br>    fmt.Println(Sunday, Monday, Tuesday) <span class="hljs-comment">// 0 1 2</span><br>    fmt.Println(KB, MB, GB)              <span class="hljs-comment">// 1024 1048576 1073741824</span><br>&#125;<br></code></pre></td></tr></table></figure><p>总之，<code>iota</code> 是 Go 中简化数字常量序列定义的轻量级语法糖，合理使用可以让代码更简洁、语义更清晰。</p><hr><h2 id="5-控制流"><a href="#5-控制流" class="headerlink" title="5. 控制流"></a>5. 控制流</h2><h3 id="if-else-与作用域"><a href="#if-else-与作用域" class="headerlink" title="if-else 与作用域"></a>if-else 与作用域</h3><figure class="highlight stata"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs stata"><span class="hljs-keyword">if</span> v, <span class="hljs-keyword">err</span> := compute(); <span class="hljs-keyword">err</span> != nil &#123;<br>    <span class="hljs-comment">// 处理错误</span><br>    <span class="hljs-keyword">return</span> <span class="hljs-keyword">err</span><br>&#125; <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> v &gt; 10 &#123;<br>    <span class="hljs-comment">// 在这里，v 和 err 仍然可见</span><br>    fmt.Println(<span class="hljs-string">&quot;Large value:&quot;</span>, v)<br>&#125; <span class="hljs-keyword">else</span> &#123;<br>    <span class="hljs-comment">// 在这里，v 和 err 也可见</span><br>    fmt.Println(<span class="hljs-string">&quot;Small value:&quot;</span>, v)<br>&#125;<br><span class="hljs-comment">// 在这里，v 和 err 已超出作用域，不可见</span><br></code></pre></td></tr></table></figure><p>- </p><pre><code class="hljs">**初始化语句**（`v, err := compute()`）中声明的变量，其作用域覆盖整个 `if-else`链（包括所有 `else if`和 `else`块），但在 `if`语句结束后失效。</code></pre><p>- </p><pre><code class="hljs">这种模式是 Go 错误处理的常见范式，将变量作用域限制在需要它的最小范围内。</code></pre><p>这段代码展示了 Go 语言中 <strong><code>if</code> 语句的特殊语法</strong>：可以在条件表达式之前执行一个<strong>简单语句</strong>（通常是变量声明或赋值），并且这些变量会覆盖整个 <code>if-else</code> 结构的作用域。</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-keyword">if</span> v, err := compute(); err != <span class="hljs-literal">nil</span> &#123;<br>    <span class="hljs-comment">// 分支1：error</span><br>&#125; <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> v &gt; <span class="hljs-number">10</span> &#123;<br>    <span class="hljs-comment">// 分支2：v &gt; 10</span><br>&#125; <span class="hljs-keyword">else</span> &#123;<br>    <span class="hljs-comment">// 分支3：v &lt;= 10</span><br>&#125;<br><span class="hljs-comment">// 这里的 v 和 err 已经不存在了</span><br></code></pre></td></tr></table></figure><ul><li><code>v</code> 和 <code>err</code> 是在 <code>if</code> 的<strong>初始化语句</strong>（<code>v, err := compute()</code>）中通过短变量声明创建的。</li><li>这组变量的<strong>作用域</strong>从声明点开始，一直延伸到整个 <code>if-else</code> 结构的<strong>结束花括号</strong>（即最后一个 <code>else</code> 或 <code>else if</code> 的块结束）。</li><li>包括：<ul><li><code>if</code> 后面的条件表达式（<code>err != nil</code>）中可以使用它们。</li><li><code>if</code> 的代码块（第一个 <code>&#123;&#125;</code>）中可以使用它们。</li><li>随后的 <code>else if</code> 的条件及代码块中可以使用它们。</li><li>最后的 <code>else</code> 代码块中也可以使用它们。</li></ul></li><li>一旦离开整个 <code>if-else</code> 结构，<code>v</code> 和 <code>err</code> <strong>就看不到了</strong>，试图在外部引用会编译报错（<code>undefined: v</code>）。</li></ul><ol><li><strong>短变量声明 <code>:=</code></strong> 在初始化语句中会创建新变量，即使外层作用域已有同名变量也会创建<strong>新的局部变量</strong>，并隐藏外层同名变量。</li><li><code>else if</code> 和 <code>else</code> 必须紧跟在 <code>if</code> 块的 <code>&#125;</code> 后面，不能插空行或声明（否则作用域结束）。</li><li>不能在 <code>if</code> 初始化语句中声明一个变量但不使用（Go 坚持“声明必须使用”），否则编译错误。</li></ol><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-keyword">package</span> main<br><br><span class="hljs-keyword">import</span> <span class="hljs-string">&quot;fmt&quot;</span><br><br><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">test</span><span class="hljs-params">(n <span class="hljs-type">int</span>)</span></span> <span class="hljs-type">error</span> &#123;<br>    <span class="hljs-keyword">if</span> v, err := compute(n); err != <span class="hljs-literal">nil</span> &#123;<br>        <span class="hljs-keyword">return</span> err<br>    &#125; <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> v &gt; <span class="hljs-number">10</span> &#123;<br>        fmt.Printf(<span class="hljs-string">&quot;Large: %d\n&quot;</span>, v)<br>    &#125; <span class="hljs-keyword">else</span> &#123;<br>        fmt.Printf(<span class="hljs-string">&quot;Small: %d\n&quot;</span>, v)<br>    &#125;<br>    <span class="hljs-comment">// fmt.Println(v) // 编译错误：undefined: v</span><br>    <span class="hljs-keyword">return</span> <span class="hljs-literal">nil</span><br>&#125;<br><br><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">compute</span><span class="hljs-params">(x <span class="hljs-type">int</span>)</span></span> (<span class="hljs-type">int</span>, <span class="hljs-type">error</span>) &#123;<br>    <span class="hljs-keyword">if</span> x == <span class="hljs-number">0</span> &#123;<br>        <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>, fmt.Errorf(<span class="hljs-string">&quot;zero is invalid&quot;</span>)<br>    &#125;<br>    <span class="hljs-keyword">return</span> x * <span class="hljs-number">2</span>, <span class="hljs-literal">nil</span><br>&#125;<br><br><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">main</span><span class="hljs-params">()</span></span> &#123;<br>    test(<span class="hljs-number">6</span>)   <span class="hljs-comment">// Small: 12</span><br>    test(<span class="hljs-number">10</span>)  <span class="hljs-comment">// Small: 20</span><br>    test(<span class="hljs-number">11</span>)  <span class="hljs-comment">// Large: 22</span><br>    test(<span class="hljs-number">0</span>)   <span class="hljs-comment">// error</span><br>&#125;<br></code></pre></td></tr></table></figure><p>总结：<code>if v, err := compute(); err != nil &#123; ... &#125;</code> 这种写法是 Go 语言中<strong>限制变量作用域</strong>的一道惯用门面，让变量只在真正需要的 <code>if-else</code> 链中存活。</p><h3 id="for-循环的四种形态"><a href="#for-循环的四种形态" class="headerlink" title="for 循环的四种形态"></a>for 循环的四种形态</h3><p>1. </p><pre><code class="hljs">**三段式**：`for i := 0; i &lt; 10; i++ &#123; ... &#125;`</code></pre><p>2. </p><pre><code class="hljs">**while 式**：`for condition &#123; ... &#125;`</code></pre><p>3. </p><pre><code class="hljs">**无限循环**：`for &#123; ... &#125;`(用 `break`或 `return`退出)</code></pre><p>4. </p><pre><code class="hljs">**range 遍历**：<figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-comment">// 数组/切片</span><br><span class="hljs-keyword">for</span> i, v := <span class="hljs-keyword">range</span> arr &#123; <span class="hljs-comment">// i 索引, v 元素值（是副本）</span><br>&#125;<br><span class="hljs-keyword">for</span> i := <span class="hljs-keyword">range</span> arr &#123; <span class="hljs-comment">// 只获取索引</span><br>&#125;<br><span class="hljs-keyword">for</span> _, v := <span class="hljs-keyword">range</span> arr &#123; <span class="hljs-comment">// 只获取值</span><br>&#125;<br><br><span class="hljs-comment">// 映射</span><br><span class="hljs-keyword">for</span> k, v := <span class="hljs-keyword">range</span> m &#123; <span class="hljs-comment">// 顺序随机，每次遍历都可能不同</span><br>&#125;<br><br><span class="hljs-comment">// 字符串</span><br><span class="hljs-keyword">for</span> i, r := <span class="hljs-keyword">range</span> <span class="hljs-string">&quot;世界&quot;</span> &#123; <span class="hljs-comment">// i 是字节偏移量(0, 3)，r 是 rune(&#x27;世&#x27;, &#x27;界&#x27;)</span><br>&#125;<br><br><span class="hljs-comment">// 通道</span><br><span class="hljs-keyword">for</span> v := <span class="hljs-keyword">range</span> ch &#123; <span class="hljs-comment">// 从通道接收，直到通道被关闭</span><br>&#125;<br></code></pre></td></tr></table></figure></code></pre><ol start="5"><li>标准 C 风格 <code>for</code> 循环</li></ol><p>包含初始化语句、条件表达式、后置语句，用分号分隔。</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-keyword">for</span> i := <span class="hljs-number">0</span>; i &lt; <span class="hljs-number">10</span>; i++ &#123;<br>    fmt.Println(i)<br>&#125;<br></code></pre></td></tr></table></figure><ul><li>初始化 <code>i := 0</code> 在循环开始前执行一次。</li><li>每次迭代前检查 <code>i &lt; 10</code>，为 <code>false</code> 则退出。</li><li>每次迭代结束后执行 <code>i++</code>。</li></ul><hr><ol start="2"><li>仅带条件的 <code>for</code>（相当于 <code>while</code>）</li></ol><p>省略初始化和后置语句，只保留条件表达式（分号也可省略）。</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs go">sum := <span class="hljs-number">1</span><br><span class="hljs-keyword">for</span> sum &lt; <span class="hljs-number">100</span> &#123;<br>    sum += sum<br>&#125;<br></code></pre></td></tr></table></figure><ul><li>行为类似其他语言的 <code>while sum &lt; 100 &#123; ... &#125;</code>。</li><li>条件在每次迭代前检查，为 <code>false</code> 时退出。</li></ul><hr><ol start="3"><li>无限 <code>for</code> 循环</li></ol><p>完全省略条件（或写为 <code>true</code>），形成死循环。</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-keyword">for</span> &#123;<br>    <span class="hljs-comment">// 循环体</span><br>    <span class="hljs-comment">// 通常用 break 退出</span><br>&#125;<br></code></pre></td></tr></table></figure><ul><li>常见于服务器监听、事件循环等需要持续运行的场景。</li><li>必须用 <code>break</code>、<code>return</code> 或 <code>panic</code> 等方式跳出，否则永不停止。</li></ul><hr><ol start="4"><li><code>for ... range</code> 循环</li></ol><p>用于迭代数组、切片、字符串、map、通道等集合类型。</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs go">nums := []<span class="hljs-type">int</span>&#123;<span class="hljs-number">2</span>, <span class="hljs-number">4</span>, <span class="hljs-number">6</span>&#125;<br><span class="hljs-keyword">for</span> i, v := <span class="hljs-keyword">range</span> nums &#123;<br>    fmt.Printf(<span class="hljs-string">&quot;索引: %d, 值: %d\n&quot;</span>, i, v)<br>&#125;<br></code></pre></td></tr></table></figure><ul><li>每次迭代返回 <strong>索引</strong> 和 <strong>该索引处的值副本</strong>。</li><li>可忽略不用的值：<code>for _, v := range nums</code> 或 <code>for i := range nums</code>。</li><li>遍历 map 时顺序随机；遍历通道会一直读取直到通道关闭。</li></ul><p>但在多层嵌套循环中可以通过标签配合 <code>break</code>&#x2F;<code>continue</code> 控制外层循环：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs go">outer:<br><span class="hljs-keyword">for</span> i := <span class="hljs-number">0</span>; i &lt; <span class="hljs-number">3</span>; i++ &#123;<br>    <span class="hljs-keyword">for</span> j := <span class="hljs-number">0</span>; j &lt; <span class="hljs-number">3</span>; j++ &#123;<br>        <span class="hljs-keyword">if</span> i == <span class="hljs-number">1</span> &amp;&amp; j == <span class="hljs-number">1</span> &#123;<br>            <span class="hljs-keyword">break</span> outer   <span class="hljs-comment">// 跳出外层循环</span><br>        &#125;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><hr><p><strong>总结</strong>：Go 的四种基本 <code>for</code> 循环形态是：</p><ol><li>传统三语句 <code>for init; condition; post &#123;&#125;</code></li><li>仅条件（类 <code>while</code>）<code>for condition &#123;&#125;</code></li><li>无限循环 <code>for &#123;&#125;</code></li><li>范围迭代 <code>for index, value := range collection &#123;&#125;</code></li></ol><p><strong>重要变更 (Go 1.22+)：循环变量捕获问题已修复</strong></p><p>- </p><pre><code class="hljs">在 **Go 1.22 之前**，`for`循环（包括 `for i:=0; i&lt;N; i++`和 `for range`）的迭代变量 (`i`, `v`) 在每次迭代中**是同一个变量**，只是被重新赋值。在 goroutine 或闭包中捕获这个变量会导致经典的“循环变量捕获”问题。</code></pre><p>- </p><pre><code class="hljs">从 **Go 1.22 开始**，**`for range`循环的每次迭代都会创建新的迭代变量**，解决了闭包捕获问题。但**三段式 `for`循环的迭代变量行为在 Go 1.22 中默认不变**，可通过 `GOEXPERIMENT=loopvar`启用新行为，并在 Go 1.23 中可能成为默认。</code></pre><p>- </p><pre><code class="hljs">**最佳实践**：如果代码需要在旧版本中安全运行，或在涉及三段式循环时，应在循环体内部为迭代变量创建局部副本。<figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-comment">// 安全写法（兼容所有版本和所有 for 循环类型）</span><br><span class="hljs-keyword">for</span> _, v := <span class="hljs-keyword">range</span> values &#123;<br>    v := v <span class="hljs-comment">// 创建局部副本</span><br>    <span class="hljs-keyword">go</span> <span class="hljs-function"><span class="hljs-keyword">func</span><span class="hljs-params">()</span></span> &#123;<br>        fmt.Println(v) <span class="hljs-comment">// 捕获的是局部副本，值是正确的</span><br>    &#125;()<br>&#125;<br></code></pre></td></tr></table></figure></code></pre><h3 id="break-continue-标签"><a href="#break-continue-标签" class="headerlink" title="break &#x2F; continue 标签"></a>break &#x2F; continue 标签</h3><p>标签用于从嵌套循环中跳出到指定层级。</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs go">OuterLoop:<br>    <span class="hljs-keyword">for</span> i := <span class="hljs-number">0</span>; i &lt; <span class="hljs-number">5</span>; i++ &#123;<br>        <span class="hljs-keyword">for</span> j := <span class="hljs-number">0</span>; j &lt; <span class="hljs-number">5</span>; j++ &#123;<br>            <span class="hljs-keyword">if</span> i*j &gt;= <span class="hljs-number">10</span> &#123;<br>                <span class="hljs-keyword">break</span> OuterLoop <span class="hljs-comment">// 直接跳出两层循环</span><br>            &#125;<br>            <span class="hljs-keyword">if</span> j%<span class="hljs-number">2</span> == <span class="hljs-number">0</span> &#123;<br>                <span class="hljs-keyword">continue</span> <span class="hljs-comment">// 继续内层循环的下一次迭代</span><br>            &#125;<br>        &#125;<br>    &#125;<br></code></pre></td></tr></table></figure><h3 id="switch-详解"><a href="#switch-详解" class="headerlink" title="switch 详解"></a>switch 详解</h3><p>1. </p><p>  <strong>表达式 switch</strong>：每个 <code>case</code>自动 <code>break</code>，无需显式写。如需穿透，必须使用 <code>fallthrough</code>关键字，且 <code>fallthrough</code>必须是 case 块中的最后一条语句。</p>  <figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-keyword">switch</span> x := getValue(); x &#123;<span class="hljs-comment">//switch后面有表达式  switch [简初始化语句]; [表达式] &#123;</span><br>    <span class="hljs-comment">//switch 后的表达式会被求值。每个 case 后跟一个或多个值（或表达式），与 switch 表达式的值进行比较。比较时使用严格相等（==），所以类型必须匹配。switch 表达式也可以是一个短声明，</span><br><span class="hljs-keyword">case</span> <span class="hljs-number">1</span>:<br>    fmt.Println(<span class="hljs-string">&quot;one&quot;</span>)<br>    <span class="hljs-comment">// 这里没有 break，自动跳出</span><br><span class="hljs-keyword">case</span> <span class="hljs-number">2</span>, <span class="hljs-number">3</span>: <span class="hljs-comment">// 可以匹配多个值</span><br>    fmt.Println(<span class="hljs-string">&quot;two or three&quot;</span>)<br>    <span class="hljs-keyword">fallthrough</span> <span class="hljs-comment">// 强制执行下一个 case 的代码</span><br><span class="hljs-keyword">case</span> <span class="hljs-number">4</span>:<br>    fmt.Println(<span class="hljs-string">&quot;four (也可能从 case 2,3 fallthrough 过来)&quot;</span>)<br><span class="hljs-keyword">default</span>:<br>    fmt.Println(<span class="hljs-string">&quot;other&quot;</span>)<br>&#125;<br></code></pre></td></tr></table></figure><p>2. </p><p>  <strong>无表达式 switch</strong>：等价于 <code>switch true</code>，是清晰的 if-else-if 链替代品。</p>  <figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-keyword">switch</span> &#123;<br><span class="hljs-keyword">case</span> score &gt;= <span class="hljs-number">90</span>:<br>    grade = <span class="hljs-string">&quot;A&quot;</span><br><span class="hljs-keyword">case</span> score &gt;= <span class="hljs-number">80</span>:<br>    grade = <span class="hljs-string">&quot;B&quot;</span><br><span class="hljs-keyword">case</span> score &gt;= <span class="hljs-number">70</span>:<br>    grade = <span class="hljs-string">&quot;C&quot;</span><br><span class="hljs-keyword">default</span>:<br>    grade = <span class="hljs-string">&quot;F&quot;</span><br>&#125;<span class="hljs-comment">//每个 case 后面必须跟一个布尔表达式（能返回 true/false 的表达式）。</span><br><span class="hljs-comment">//执行时，会从上到下逐一求值每个 case 的表达式，遇到第一个结果为 true 的 case 便执行其代码块。</span><br></code></pre></td></tr></table></figure><p>3. </p><pre><code class="hljs">**类型 switch**：用于判断接口值的动态类型。`v := i.(type)`中的 `v`在 case 分支内是对应类型的变量。<figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">describe</span><span class="hljs-params">(i <span class="hljs-keyword">interface</span>&#123;&#125;)</span></span> &#123;<br>    <span class="hljs-keyword">switch</span> v := i.(<span class="hljs-keyword">type</span>) &#123;<br>    <span class="hljs-keyword">case</span> <span class="hljs-type">string</span>:<br>        fmt.Printf(<span class="hljs-string">&quot;String: %s\n&quot;</span>, v)<br>    <span class="hljs-keyword">case</span> <span class="hljs-type">int</span>:<br>        fmt.Printf(<span class="hljs-string">&quot;Int: %d\n&quot;</span>, v)<br>    <span class="hljs-keyword">case</span> []<span class="hljs-type">int</span>:<br>        fmt.Printf(<span class="hljs-string">&quot;Slice of int with len %d\n&quot;</span>, <span class="hljs-built_in">len</span>(v))<br>    <span class="hljs-keyword">default</span>:<br>        fmt.Printf(<span class="hljs-string">&quot;Unknown type: %T\n&quot;</span>, v)<br>    &#125;<br>&#125;<br><br><br>允许多类型匹配：一个 <span class="hljs-keyword">case</span> 可以匹配多种类型，用逗号分隔。<br><span class="hljs-keyword">case</span> <span class="hljs-type">int</span>, <span class="hljs-type">float64</span>:<br>    fmt.Printf(<span class="hljs-string">&quot;Number: %v\n&quot;</span>, v) <span class="hljs-comment">// v 的类型是 interface&#123;&#125;，因为无法确定确切类型</span><br>这种情况下，v 的类型仍然是 <span class="hljs-keyword">interface</span>&#123;&#125;，因为无法确定是 <span class="hljs-type">int</span> 还是 <span class="hljs-type">float64</span>。如果要操作具体类型，仍需进一步断言。<br><br>可以不声明变量：如果不需要在每个分支中使用具体值，可以省略 v。<br><span class="hljs-keyword">switch</span> i.(<span class="hljs-keyword">type</span>) &#123;<br><span class="hljs-keyword">case</span> <span class="hljs-type">string</span>:<br>    fmt.Println(<span class="hljs-string">&quot;It&#x27;s a string&quot;</span>)  <span class="hljs-comment">// 无法直接访问字符串值</span><br>&#125;<br>此时不能用 v，但可以通过进一步断言获取值。<br></code></pre></td></tr></table></figure></code></pre><h3 id="defer"><a href="#defer" class="headerlink" title="defer"></a>defer</h3><p>Go 中的 <code>defer</code> 语句用于<strong>延迟执行一个函数调用</strong>，使其在外层函数即将返回之前执行。它是 Go 处理资源清理、错误恢复和代码简化的核心机制。</p><ol><li>基本语法</li></ol><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">readFile</span><span class="hljs-params">(path <span class="hljs-type">string</span>)</span></span> <span class="hljs-type">error</span> &#123;<br>    f, err := os.Open(path)<span class="hljs-comment">//os.Open(path) 返回两个值：*os.File 和 error。f 是文件对象，err 是可能发生的错误。:= 短变量声明，自动推导类型并声明变量，作用域是当前函数。</span><br>    <br>    <span class="hljs-keyword">if</span> err != <span class="hljs-literal">nil</span> &#123;  <span class="hljs-comment">//Go 的惯用错误处理：立即检查 err。如果 err 不为 nil，打开文件失败，直接返回错误，函数结束，不再执行后续代码。此时 f.Close() 不会被推迟，因为函数已经返回。</span><br>        <span class="hljs-keyword">return</span> err<br>    &#125;<br>    <span class="hljs-keyword">defer</span> f.Close()   <span class="hljs-comment">// 这行代码告诉编译器：在本函数返回前，执行 f.Close()。f.Close 是文件对象的方法，作用是关闭文件，释放系统资源。在 readFile 返回前一定会执行 f.Close()</span><br><br>    <span class="hljs-comment">// 使用 f 读取内容...</span><br>    <span class="hljs-keyword">return</span> <span class="hljs-literal">nil</span>  <span class="hljs-comment">//一切正常</span><br>&#125;<br><br>获取资源 (os.Open)。<br>立即检查错误，并返回。<br>用 <span class="hljs-keyword">defer</span> 注册资源释放操作。<br>正常使用资源，最后 <span class="hljs-keyword">return</span> <span class="hljs-literal">nil</span>。<br></code></pre></td></tr></table></figure><ul><li><code>defer</code> 后必须跟一个<strong>完整的函数调用</strong>,，不能加括号包裹成表达式，也不能省略 <code>()</code>（除非是方法值）（不能是普通表达式）。</li><li>多个 <code>defer</code> 注册的调用会以**后进先出（LIFO）**的顺序执行。</li></ul><hr><ol start="2"><li>执行时机与顺序</li></ol><p>当一个函数中有多个 <code>defer</code>：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">test</span><span class="hljs-params">()</span></span> &#123;<br>    <span class="hljs-keyword">defer</span> fmt.Println(<span class="hljs-string">&quot;1&quot;</span>)<br>    <span class="hljs-keyword">defer</span> fmt.Println(<span class="hljs-string">&quot;2&quot;</span>)<br>    <span class="hljs-keyword">defer</span> fmt.Println(<span class="hljs-string">&quot;3&quot;</span>)<br>    fmt.Println(<span class="hljs-string">&quot;main&quot;</span>)<br>&#125;<br><span class="hljs-comment">// 输出：main  3  2  1</span><br></code></pre></td></tr></table></figure><p>所有 <code>defer</code> 在函数<strong>即将返回</strong>时执行（包括正常 <code>return</code>、<code>panic</code> 到该函数栈帧时），执行顺序是<strong>最后 <code>defer</code> 的最先执行</strong>。</p><blockquote><p><strong>重要</strong>：<code>defer</code> 注册的是调用，但<strong>调用参数在 <code>defer</code> 声明时就已求值</strong>。</p><p><strong>当 <code>defer</code> 注册一个函数调用时，函数的参数值在 <code>defer</code> 语句执行的那一刻就确定了，而不是等到外层函数返回前才计算</strong></p><p>因为很多人直觉上以为，<code>defer</code> 只是把函数调用“推迟”到返回前执行，参数也会在真正执行时才求值。实际上，Go 会在你写下 <code>defer</code> 这一行的<strong>瞬间</strong>就把参数值算好并保存下来。这会导致行为与直觉不符，特别是被延迟的函数有参数，且该参数是变量时。</p></blockquote><hr><ol start="3"><li>参数求值的时机</li></ol><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">printNum</span><span class="hljs-params">(i <span class="hljs-type">int</span>)</span></span> &#123;<br>    fmt.Println(i)<br>&#125;<br><br><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">test</span><span class="hljs-params">()</span></span> &#123;<br>    i := <span class="hljs-number">1</span><br>    <span class="hljs-keyword">defer</span> printNum(i)  <span class="hljs-comment">// 此时 i=1 被捕获</span><br>    i = <span class="hljs-number">2</span><br>    <span class="hljs-comment">// 返回前执行 printNum(1)，输出 1</span><br>&#125;<br></code></pre></td></tr></table></figure><ul><li><code>defer printNum(i)</code> 中的 <code>i</code> 在运行到 <code>defer</code> 行时立即求值，保存为 <code>1</code>，后续 <code>i</code> 如何变化不影响。</li><li>如果希望延迟执行时获取最新值，应使用闭包（捕获变量引用），让函数体内部直接引用外部变量，而不是通过参数传入：</li></ul><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-keyword">defer</span> <span class="hljs-function"><span class="hljs-keyword">func</span><span class="hljs-params">()</span></span> &#123; fmt.Println(i) &#125;()  <span class="hljs-comment">// 输出 2   这里 defer 后面是一个匿名函数，并且它没有参数。闭包内直接访问变量 i，因此每次读取的都是 i 的最新值。</span><br></code></pre></td></tr></table></figure><hr><ol start="4"><li>与返回值的交互（命名返回值）</li></ol><p><code>defer</code> 可以<strong>修改函数的命名返回值</strong>。这是 <code>defer</code> 非常强大且容易迷惑的特性。</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">example</span><span class="hljs-params">()</span></span> (result <span class="hljs-type">int</span>) &#123;<br>    <span class="hljs-keyword">defer</span> <span class="hljs-function"><span class="hljs-keyword">func</span><span class="hljs-params">()</span></span> &#123;<br>        result++  <span class="hljs-comment">// 函数返回前，result 再被加 1</span><br>    &#125;()<br>    <span class="hljs-keyword">return</span> <span class="hljs-number">5</span><br>&#125;<br><span class="hljs-comment">// 实际返回 6</span><br></code></pre></td></tr></table></figure><p>执行顺序：</p><ol><li><code>return 5</code> 将 <code>5</code> 赋给返回值变量 <code>result</code>。</li><li>执行所有 deferred 函数（这里将 <code>result</code> 从 5 改为 6）。</li><li>函数真正返回，调用者得到 6。</li></ol><p>如果是<strong>非命名返回值</strong>，defer 无法修改已返回的值，因为匿名返回值在 defer 执行前已经拷贝出去了。</p><p><code>defer</code> 能够修改函数的<strong>命名返回值</strong>，这是因为 defer 的执行顺序与返回值赋值在 Go 中的精确流程共同作用的结果。</p><ol><li>命名返回值是什么</li></ol><p>当一个函数给返回值起了名字：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">example</span><span class="hljs-params">()</span></span> (result <span class="hljs-type">int</span>) &#123;<br>    <span class="hljs-comment">// 这里可以直接使用 result 变量</span><br>    ...<br>&#125;<br></code></pre></td></tr></table></figure><p><code>result</code> 就像函数体内一个<strong>局部变量</strong>，从函数入口就存在，并初始化为零值（这里是 <code>0</code>）。</p><p>而匿名返回值不会创建这样的变量：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">example</span><span class="hljs-params">()</span></span> <span class="hljs-type">int</span> &#123;  <span class="hljs-comment">// 返回值没名字</span><br>    <span class="hljs-keyword">return</span> <span class="hljs-number">5</span><br>&#125;<br></code></pre></td></tr></table></figure><p>这种形式下，返回值只是一个隐式的临时值，在函数体内不可直接引用。</p><hr><ol start="2"><li><code>return</code> 与 <code>defer</code> 的执行顺序</li></ol><p>对于带命名返回值的函数，执行流程是：</p><ol><li>执行 <code>return</code> 语句，<strong>将返回值赋给命名返回值变量</strong>。</li><li>按后进先出的顺序执行所有已注册的 deferred 函数。</li><li>函数真正返回，调用者拿到命名返回值变量的最终值。</li></ol><blockquote><p>关键在第 2 步：<strong>deferred 函数在 return 赋值之后、函数真正离开之前运行</strong>，因此可以访问并修改命名返回值变量。</p></blockquote><hr><ol start="3"><li>修改命名返回值的例子</li></ol><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">modify</span><span class="hljs-params">()</span></span> (num <span class="hljs-type">int</span>) &#123;<br>    <span class="hljs-keyword">defer</span> <span class="hljs-function"><span class="hljs-keyword">func</span><span class="hljs-params">()</span></span> &#123;<br>        num++       <span class="hljs-comment">// 修改命名返回值</span><br>    &#125;()<br>    <span class="hljs-keyword">return</span> <span class="hljs-number">7</span>        <span class="hljs-comment">// 先把 7 赋给 num</span><br>&#125;<br><span class="hljs-comment">// 调用 modify() 得到 8</span><br></code></pre></td></tr></table></figure><p>流程：</p><ul><li><code>return 7</code> → <code>num</code> 变成 <code>7</code>。</li><li>运行 <code>defer</code> → <code>num</code> 变成 <code>8</code>。</li><li>函数返回，返回值为 <code>8</code>。</li></ul><p>如果是<strong>匿名返回值</strong>：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">modify</span><span class="hljs-params">()</span></span> <span class="hljs-type">int</span> &#123;<br>    <span class="hljs-keyword">defer</span> <span class="hljs-function"><span class="hljs-keyword">func</span><span class="hljs-params">()</span></span> &#123;<br>        <span class="hljs-comment">// 无法访问返回值变量，函数体内没有名字</span><br>    &#125;()<br>    <span class="hljs-keyword">return</span> <span class="hljs-number">7</span><br>&#125;<br><span class="hljs-comment">// 只能返回 7，defer 无法干涉</span><br></code></pre></td></tr></table></figure><p>匿名返回值在 <code>return</code> 时直接把值拷贝到栈上留给调用者，deferred 函数拿不到那个值。</p><ul><li><p><strong>记录指标</strong>：在返回前修改结果（如包装错误、添加耗时数据）。</p></li><li><p><strong>错误恢复</strong>：当函数发生 <code>panic</code> 时，在 defer 中恢复，并把命名返回值设为安全值。</p>  <figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">safeDivide</span><span class="hljs-params">(a, b <span class="hljs-type">int</span>)</span></span> (ret <span class="hljs-type">int</span>) &#123;<br>    <span class="hljs-keyword">defer</span> <span class="hljs-function"><span class="hljs-keyword">func</span><span class="hljs-params">()</span></span> &#123;<br>        <span class="hljs-keyword">if</span> r := <span class="hljs-built_in">recover</span>(); r != <span class="hljs-literal">nil</span> &#123;<br>            ret = <span class="hljs-number">0</span>   <span class="hljs-comment">// 发生除零错误时返回 0</span><br>        &#125;<br>    &#125;()<br>    <span class="hljs-keyword">return</span> a / b<br>&#125;<br></code></pre></td></tr></table></figure><p>  如果发生 panic，<code>return a / b</code> 没有正常完成，但命名返回值 <code>ret</code> 仍被设置为零值，<code>defer</code> 再将其改为 <code>0</code> 后返回给调用者。</p></li><li><p><strong>简化资源清理与状态返回</strong>：比如在关闭文件后，根据关闭操作是否成功，修改返回值。</p>  <figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">processFile</span><span class="hljs-params">(path <span class="hljs-type">string</span>)</span></span> (err <span class="hljs-type">error</span>) &#123;<br>    f, e := os.Open(path)<br>    <span class="hljs-keyword">if</span> e != <span class="hljs-literal">nil</span> &#123;<br>        <span class="hljs-keyword">return</span> e<br>    &#125;<br>    <span class="hljs-keyword">defer</span> <span class="hljs-function"><span class="hljs-keyword">func</span><span class="hljs-params">()</span></span> &#123;<br>        <span class="hljs-keyword">if</span> closeErr := f.Close(); closeErr != <span class="hljs-literal">nil</span> &#123;<br>            err = closeErr  <span class="hljs-comment">// 将关闭错误赋值给命名返回值 err</span><br>        &#125;<br>    &#125;()<br>    <span class="hljs-comment">// ... 处理文件内容</span><br>    <span class="hljs-keyword">return</span> <span class="hljs-literal">nil</span><br>&#125;<br></code></pre></td></tr></table></figure><p>  这样即便处理成功，如果关闭文件失败，调用者也能感知到错误。</p></li></ul><hr><ol start="5"><li>关键点总结</li></ol><ul><li>命名返回值是函数内部的局部变量，从函数开始就存在。</li><li><code>return</code> 执行 → 赋值给命名返回值 → <code>defer</code> 运行 → 真正返回。</li><li><code>defer</code> 中可以直接读写命名返回值，从而改变最终返回给调用者的值。</li><li>匿名返回值无法被 <code>defer</code> 修改，因为它在函数体内没有变量名。</li></ul><p>这种机制让 <code>defer</code> 成为清理资源和处理错误时非常灵活的工具。但也要小心，过度使用或者修改返回值会让代码逻辑变复杂，最好在有明确需要时才用。</p><hr><ol start="5"><li>常见使用场景</li></ol><ul><li><p><strong>资源释放</strong>：关闭文件、网络连接、数据库连接、释放锁等。</p>  <figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs go">mu.Lock()<br><span class="hljs-keyword">defer</span> mu.Unlock()<br></code></pre></td></tr></table></figure></li><li><p><strong>错误处理与打扫</strong>：记录日志、恢复 panic（见第 7 点）。</p></li><li><p><strong>计量与追踪</strong>：记录耗时、进入&#x2F;退出日志。</p>  <figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">trace</span><span class="hljs-params">(name <span class="hljs-type">string</span>)</span></span> <span class="hljs-function"><span class="hljs-keyword">func</span><span class="hljs-params">()</span></span> &#123;<br>    start := time.Now()<br>    fmt.Println(<span class="hljs-string">&quot;Enter&quot;</span>, name)<br>    <span class="hljs-keyword">return</span> <span class="hljs-function"><span class="hljs-keyword">func</span><span class="hljs-params">()</span></span> &#123;<br>        fmt.Println(<span class="hljs-string">&quot;Exit&quot;</span>, name, time.Since(start))<br>    &#125;<br>&#125;<br><br><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">doWork</span><span class="hljs-params">()</span></span> &#123;<br>    <span class="hljs-keyword">defer</span> trace(<span class="hljs-string">&quot;doWork&quot;</span>)()<br>    <span class="hljs-comment">// 实际工作</span><br>&#125;<br></code></pre></td></tr></table></figure></li></ul><hr><ol start="6"><li><code>defer</code> 与 <code>panic</code> &#x2F; <code>recover</code></li></ol><ul><li>当发生 <code>panic</code> 时，当前函数会立即停止，但<strong>仍会执行该函数中已注册的 <code>defer</code></strong>，然后向外传播给调用者。</li><li>在 <code>defer</code> 中调用 <code>recover()</code> 可以捕获 <code>panic</code>，阻止程序崩溃。</li></ul><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">safeDivide</span><span class="hljs-params">(a, b <span class="hljs-type">int</span>)</span></span> (ret <span class="hljs-type">int</span>) &#123;<br>    <span class="hljs-keyword">defer</span> <span class="hljs-function"><span class="hljs-keyword">func</span><span class="hljs-params">()</span></span> &#123;<br>        <span class="hljs-keyword">if</span> r := <span class="hljs-built_in">recover</span>(); r != <span class="hljs-literal">nil</span> &#123;<br>            fmt.Println(<span class="hljs-string">&quot;Recovered:&quot;</span>, r)<br>            ret = <span class="hljs-number">0</span>   <span class="hljs-comment">// 恢复后返回 0</span><br>        &#125;<br>    &#125;()<br>    <span class="hljs-keyword">return</span> a / b<br>&#125;<br></code></pre></td></tr></table></figure><ul><li><p><code>recover</code> 只有在 <code>defer</code> 直接调用的函数中才有效，嵌套调用无效。</p></li><li><p>在 Go 1.13 之前，<code>defer</code> 有一定性能开销；1.13 及以后，多数简单场景的性能已与直接调用接近，但大量循环中使用仍然需考虑。</p></li><li><p>不要在循环中注册大量 <code>defer</code>，因为那样会累积直到函数结束才执行，可能耗尽内存或导致延迟。最好在循环内单独封装函数，或者不使用 <code>defer</code> 直接手动清理。</p></li><li><p><code>defer</code> 只推迟函数调用，不能推迟代码块。如需执行多步骤清理，须包装为一个函数。</p></li></ul><p>Go 中的 <code>defer</code>、<code>panic</code> 和 <code>recover</code> 搭配使用，构成了一套<strong>用于处理不可恢复错误（或异常）的机制</strong>。它们三者之间的关系可以总结为：</p><ul><li><code>panic</code>：引发运行时的严重错误，默认会导致程序崩溃。</li><li><code>defer</code>：在函数退出前一定会执行的延迟调用。</li><li><code>recover</code>：<strong>仅在 <code>defer</code> 内部调用时</strong>能捕获 <code>panic</code>，从而允许程序从恐慌中恢复。</li></ul><hr><ol><li><code>panic</code> 是什么</li></ol><ul><li><code>panic</code> 是一个内置函数，用来停止当前函数的正常执行。</li><li>当 <code>panic</code> 被调用时：<ol><li>函数立即停止执行。</li><li>执行该函数中<strong>所有已经注册的 <code>defer</code></strong>。</li><li>将 panic 向上传播给调用者（对调用者来说，就像它调用的函数发生了 panic）。</li><li>这个过程一直持续到当前 goroutine 的顶层函数，如果没有任何 <code>recover</code> 捕获，程序崩溃并打印堆栈跟踪。</li></ol></li></ul><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">example</span><span class="hljs-params">()</span></span> &#123;<br>    fmt.Println(<span class="hljs-string">&quot;开始&quot;</span>)<br>    <span class="hljs-built_in">panic</span>(<span class="hljs-string">&quot;出错了&quot;</span>)        <span class="hljs-comment">// 引发恐慌</span><br>    fmt.Println(<span class="hljs-string">&quot;结束&quot;</span>)    <span class="hljs-comment">// 不会执行</span><br>&#125;<br></code></pre></td></tr></table></figure><hr><ol start="2"><li><code>defer</code> 在 <code>panic</code> 时的行为</li></ol><ul><li><strong>即使函数发生 panic，已注册的 <code>defer</code> 仍然会被执行</strong>（按后进先出顺序）。</li><li>这些 <code>defer</code> 完成之后，panic 继续向上传播。</li><li>如果在 <code>defer</code> 中调用了 <code>recover()</code> 且成功捕获了 panic，则 panic 停止传播，程序继续从引发 panic 的函数返回处恢复正常流程。</li></ul><hr><ol start="3"><li><code>recover</code> 怎么用</li></ol><ul><li><p>捕获当前 goroutine 中<strong>正在传播的 <code>panic</code></strong>。</p></li><li><p>返回传递给 <code>panic</code> 的那个值（任意类型，通常是一个 <code>error</code> 或字符串）。</p></li><li><p>如果当前 goroutine 没有发生 <code>panic</code>，<code>recover()</code> 返回 <code>nil</code>。</p></li><li><p><code>recover()</code> 返回当前 panic 传递的值，如果没有 panic，返回 <code>nil</code>。</p></li><li><p><strong><code>recover</code> 只有在 <code>defer</code> 直接调用的函数内部才有效</strong>，否则返回值总是 <code>nil</code>。</p></li><li><p>典型模式：在 <code>defer</code> 函数中使用 <code>recover</code> 检查是否有 panic，并处理。</p></li></ul><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">safeCall</span><span class="hljs-params">()</span></span> (err <span class="hljs-type">error</span>) &#123;<br>    <span class="hljs-keyword">defer</span> <span class="hljs-function"><span class="hljs-keyword">func</span><span class="hljs-params">()</span></span> &#123;<br>        <span class="hljs-keyword">if</span> r := <span class="hljs-built_in">recover</span>(); r != <span class="hljs-literal">nil</span> &#123;<br>            fmt.Println(<span class="hljs-string">&quot;Recovered from:&quot;</span>, r)<br>            err = fmt.Errorf(<span class="hljs-string">&quot;panic occurred: %v&quot;</span>, r)<br>        &#125;<br>    &#125;()<br>    mayPanic()   <span class="hljs-comment">// 如果这里 panic，会由上面的 defer 捕获</span><br>    <span class="hljs-keyword">return</span> <span class="hljs-literal">nil</span><br>&#125;<br></code></pre></td></tr></table></figure><ul><li><code>mayPanic()</code> 若引发 panic，safeCall 的 defer 中的 <code>recover()</code> 会收到值，然后函数安全返回，不会崩溃。</li></ul><hr><ol start="4"><li>执行流程详解（带序列）</li></ol><p>假设函数调用链：</p><figure class="highlight scss"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs scss"><span class="hljs-selector-tag">main</span> → <span class="hljs-selector-tag">A</span>() → <span class="hljs-selector-tag">B</span>() → <span class="hljs-built_in">C</span>()   (C 里 panic)<br></code></pre></td></tr></table></figure><p>当 <code>C</code> 中发生 panic：</p><ol><li>停止 <code>C</code> 的正常代码。</li><li>运行 <code>C</code> 中注册的所有 defer（后进先出）。</li><li>如果 <code>C</code> 的某个 defer 中有 <code>recover()</code> 且捕获了 panic，则 panic 停止，<code>C</code> 返回到 <code>B</code>（正常返回）。</li><li>如果 <code>C</code> 中没有 recover，则 panic 传播到 <code>B</code>，在 <code>B</code> 看来，<code>C</code> 的调用处发生了 panic。然后重复步骤 2：运行 <code>B</code> 的 defer，检查 recover，以此类推。</li><li>直到某个 defer 恢复，或整个 goroutine 崩溃。</li></ol><p>为了更好地理解 <code>panic</code> 的传播机制，我们沿用调用链 <code>main → A → B → C</code>，并给每个函数加入 <code>defer</code> 和可选 <code>recover</code>，通过具体代码模拟并分析每一步的执行细节。</p><hr><p>场景设定：调用关系与基本框架</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-keyword">package</span> main<br><br><span class="hljs-keyword">import</span> <span class="hljs-string">&quot;fmt&quot;</span><br><br><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">A</span><span class="hljs-params">()</span></span> &#123;<br>    <span class="hljs-keyword">defer</span> fmt.Println(<span class="hljs-string">&quot;A 的 defer 1&quot;</span>)<br>    <span class="hljs-keyword">defer</span> fmt.Println(<span class="hljs-string">&quot;A 的 defer 2&quot;</span>)<br>    fmt.Println(<span class="hljs-string">&quot;A 开始&quot;</span>)<br>    B()                     <span class="hljs-comment">// 调用 B</span><br>    fmt.Println(<span class="hljs-string">&quot;A 结束&quot;</span>)   <span class="hljs-comment">// 如果 panic 被恢复，此行才会执行</span><br>&#125;<br><br><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">B</span><span class="hljs-params">()</span></span> &#123;<br>    <span class="hljs-keyword">defer</span> fmt.Println(<span class="hljs-string">&quot;B 的 defer 1&quot;</span>)<br>    <span class="hljs-keyword">defer</span> fmt.Println(<span class="hljs-string">&quot;B 的 defer 2&quot;</span>)<br>    fmt.Println(<span class="hljs-string">&quot;B 开始&quot;</span>)<br>    C()                     <span class="hljs-comment">// 调用 C</span><br>    fmt.Println(<span class="hljs-string">&quot;B 结束&quot;</span>)   <span class="hljs-comment">// 如果 C 中恢复，此行执行；否则不执行</span><br>&#125;<br><br><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">C</span><span class="hljs-params">()</span></span> &#123;<br>    <span class="hljs-keyword">defer</span> fmt.Println(<span class="hljs-string">&quot;C 的 defer 1&quot;</span>)<br>    <span class="hljs-keyword">defer</span> fmt.Println(<span class="hljs-string">&quot;C 的 defer 2&quot;</span>)<br>    fmt.Println(<span class="hljs-string">&quot;C 开始&quot;</span>)<br>    <span class="hljs-built_in">panic</span>(<span class="hljs-string">&quot;C 中触发的 panic&quot;</span>)<br>    fmt.Println(<span class="hljs-string">&quot;C 结束&quot;</span>)   <span class="hljs-comment">// 永远不会执行</span><br>&#125;<br></code></pre></td></tr></table></figure><p>我们将对这个基础版本加入不同的 <code>recover</code> 位置，观察传播链。</p><hr><p>情况一：C 中没有 recover（panic 一路传播到 main，最终崩溃）</p><ol><li><p><strong>C 正常执行到 <code>panic</code></strong>  </p><ul><li>打印 <code>&quot;C 开始&quot;</code>。  </li><li>执行 <code>panic(&quot;C 中触发的 panic&quot;)</code>。  </li><li><code>C</code> 的普通代码立即停止，<code>&quot;C 结束&quot;</code> 不会打印。</li></ul></li><li><p><strong>运行 C 中已注册的 defer</strong>（后进先出）  </p><ul><li>先执行 <code>&quot;C 的 defer 2&quot;</code>。  </li><li>再执行 <code>&quot;C 的 defer 1&quot;</code>。  </li><li>它们都没有 <code>recover</code>，所以 panic 继续传播。</li></ul></li><li><p><strong>C 返回到 B，B 的调用点等价于发生了 panic</strong>  </p><ul><li><code>B</code> 中 <code>C()</code> 之后的代码不会执行（<code>&quot;B 结束&quot;</code> 不会打印）。  </li><li>立即进入 B 的 defer 执行：  <ul><li>先 <code>&quot;B 的 defer 2&quot;</code>。  </li><li>再 <code>&quot;B 的 defer 1&quot;</code>。</li></ul></li><li>B 也无 <code>recover</code>，panic 继续传播。</li></ul></li><li><p><strong>传播到 A</strong>  </p><ul><li>A 中 <code>B()</code> 之后的 <code>&quot;A 结束&quot;</code> 不执行。  </li><li>运行 A 的 defer：  <ul><li>先 <code>&quot;A 的 defer 2&quot;</code>。  </li><li>再 <code>&quot;A 的 defer 1&quot;</code>。</li></ul></li><li>无 <code>recover</code>，panic 继续上传给 <code>main</code>。</li></ul></li><li><p><strong>在 main 中同样无恢复</strong>  </p><ul><li><p>程序崩溃，打印类似以下输出：</p>  <figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs go">C 开始<br>C 的 <span class="hljs-keyword">defer</span> <span class="hljs-number">2</span><br>C 的 <span class="hljs-keyword">defer</span> <span class="hljs-number">1</span><br>B 的 <span class="hljs-keyword">defer</span> <span class="hljs-number">2</span><br>B 的 <span class="hljs-keyword">defer</span> <span class="hljs-number">1</span><br>A 的 <span class="hljs-keyword">defer</span> <span class="hljs-number">2</span><br>A 的 <span class="hljs-keyword">defer</span> <span class="hljs-number">1</span><br><span class="hljs-built_in">panic</span>: C 中触发的 <span class="hljs-built_in">panic</span><br></code></pre></td></tr></table></figure></li><li><p>所有 goroutine 停止，进程退出。</p></li></ul></li></ol><hr><p>情况二：在 C 中加入 recover（panic 在发生处被捕获）</p><p>修改 C：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">C</span><span class="hljs-params">()</span></span> &#123;<br>    <span class="hljs-keyword">defer</span> <span class="hljs-function"><span class="hljs-keyword">func</span><span class="hljs-params">()</span></span> &#123;<br>        <span class="hljs-keyword">if</span> r := <span class="hljs-built_in">recover</span>(); r != <span class="hljs-literal">nil</span> &#123;<br>            fmt.Println(<span class="hljs-string">&quot;C 捕获 panic:&quot;</span>, r)<br>        &#125;<br>    &#125;()<br>    <span class="hljs-keyword">defer</span> fmt.Println(<span class="hljs-string">&quot;C 的 defer（普通）&quot;</span>)<br>    fmt.Println(<span class="hljs-string">&quot;C 开始&quot;</span>)<br>    <span class="hljs-built_in">panic</span>(<span class="hljs-string">&quot;C 中触发的 panic&quot;</span>)<br>    fmt.Println(<span class="hljs-string">&quot;C 结束&quot;</span>)<br>&#125;<br></code></pre></td></tr></table></figure><p>执行流程：</p><ol><li><p><strong>C 中</strong>  </p><ul><li>打印 <code>&quot;C 开始&quot;</code>。  </li><li>遇到 <code>panic</code>。  </li><li>普通代码停止。</li></ul></li><li><p><strong>运行 C 的 defer</strong>  </p><ul><li>先执行普通 defer <code>&quot;C 的 defer（普通）&quot;</code>。  </li><li>然后执行匿名 defer，其中 <code>recover()</code> 返回 <code>&quot;C 中触发的 panic&quot;</code>。  <ul><li>打印 <code>&quot;C 捕获 panic: C 中触发的 panic&quot;</code>。  </li><li><code>panic</code> 被恢复，停止传播。</li></ul></li><li>C 的所有 defer 运行完毕。</li></ul></li><li><p><strong>C 正常返回到 B</strong>  </p><ul><li><code>B</code> 中的 <code>C()</code> 返回，<strong>没有异常</strong>。  </li><li>继续执行 <code>&quot;B 结束&quot;</code>（之前不会执行的代码现在得以运行）。  </li><li>然后运行 B 自己的 defer（<code>&quot;B 的 defer 2&quot;</code>，<code>&quot;B 的 defer 1&quot;</code>）。  </li><li>B 顺利结束。</li></ul></li><li><p><strong>返回到 A</strong>  </p><ul><li>A 继续执行 <code>&quot;A 结束&quot;</code>，然后是 A 的 defer。  </li><li>最终 <code>main</code> 正常继续，程序不会崩溃。</li></ul></li></ol><p>输出示意：</p><figure class="highlight less"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs less"><span class="hljs-selector-tag">A</span> 开始<br><span class="hljs-selector-tag">B</span> 开始<br><span class="hljs-selector-tag">C</span> 开始<br><span class="hljs-selector-tag">C</span> 的 <span class="hljs-selector-tag">defer</span>（普通）<br><span class="hljs-selector-tag">C</span> 捕获 <span class="hljs-selector-tag">panic</span>: <span class="hljs-selector-tag">C</span> 中触发的 <span class="hljs-selector-tag">panic</span><br><span class="hljs-selector-tag">B</span> 结束<br><span class="hljs-selector-tag">B</span> 的 <span class="hljs-selector-tag">defer</span> <span class="hljs-number">2</span><br><span class="hljs-selector-tag">B</span> 的 <span class="hljs-selector-tag">defer</span> <span class="hljs-number">1</span><br><span class="hljs-selector-tag">A</span> 结束<br><span class="hljs-selector-tag">A</span> 的 <span class="hljs-selector-tag">defer</span> <span class="hljs-number">2</span><br><span class="hljs-selector-tag">A</span> 的 <span class="hljs-selector-tag">defer</span> <span class="hljs-number">1</span><br></code></pre></td></tr></table></figure><p>所有 <code>结束</code> 标记都打印了，说明 panic 被成功圈定在 C 内部。</p><hr><p>情况三：在 B 中 recover（C 中无 recover，panic 由 B 捕获）</p><p>修改 B，添加恢复逻辑；C 保持无 recover 版本。</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">B</span><span class="hljs-params">()</span></span> &#123;<br>    <span class="hljs-keyword">defer</span> <span class="hljs-function"><span class="hljs-keyword">func</span><span class="hljs-params">()</span></span> &#123;<br>        <span class="hljs-keyword">if</span> r := <span class="hljs-built_in">recover</span>(); r != <span class="hljs-literal">nil</span> &#123;<br>            fmt.Println(<span class="hljs-string">&quot;B 捕获 panic:&quot;</span>, r)<br>        &#125;<br>    &#125;()<br>    <span class="hljs-keyword">defer</span> fmt.Println(<span class="hljs-string">&quot;B 的 defer 2&quot;</span>)<br>    <span class="hljs-comment">// ... 其余不变</span><br>&#125;<br></code></pre></td></tr></table></figure><p>流程：</p><ol><li><strong>C panic</strong>，运行完 C 的所有 defer 后，向 B 传播。</li><li><strong>B 中</strong>  <ul><li><code>C()</code> 之后的 <code>&quot;B 结束&quot;</code> 跳过。  </li><li>立即执行 B 的 defer：  <ul><li>先 <code>&quot;B 的 defer 2&quot;</code>。  </li><li>再执行恢复用的匿名 defer，<code>recover()</code> 非 nil，打印 <code>&quot;B 捕获 panic: C 中触发的 panic&quot;</code>。  </li><li>panic 被恢复，不再向上传播。</li></ul></li></ul></li><li><strong>B 返回</strong>  <ul><li>B 的返回值按命名返回值规则处理（这里无返回值，则直接返回）。  </li><li>A 继续，执行 <code>&quot;A 结束&quot;</code> 及 A 的 defer。  </li><li>程序正常运行。</li></ul></li></ol><p>输出：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs go">A 开始<br>B 开始<br>C 开始<br>C 的 <span class="hljs-keyword">defer</span> <span class="hljs-number">2</span><br>C 的 <span class="hljs-keyword">defer</span> <span class="hljs-number">1</span><br>B 的 <span class="hljs-keyword">defer</span> <span class="hljs-number">2</span><br>B 捕获 <span class="hljs-built_in">panic</span>: C 中触发的 <span class="hljs-built_in">panic</span><br>A 结束<br>A 的 <span class="hljs-keyword">defer</span> <span class="hljs-number">2</span><br>A 的 <span class="hljs-keyword">defer</span> <span class="hljs-number">1</span><br></code></pre></td></tr></table></figure><p>此处 <code>&quot;B 结束&quot;</code> 未打印，因为 panic 发生在 B 调用 C 的语句上，恢复后 B 的剩余逻辑跳过，直接返回。</p><hr><p>关键点提炼</p><ol><li><p><strong>panic 执行顺序</strong><br> 发生 panic 后，当前函数立即停止，而后<strong>逆序执行已注册的 defer</strong>，再向上层传播。</p></li><li><p><strong>recover 的有效位置</strong><br> 只有<strong>在 defer 直接调用的函数内部</strong>，<code>recover()</code> 才能捕获到 panic。</p></li><li><p><strong>恢复后的代码流向</strong>  </p><ul><li>恢复后，当前函数中 <strong>panic 之后的代码都不会执行</strong>，但上层调用者将感知到一个<strong>正常的返回</strong>。  </li><li>如果函数有命名返回值，可以在 defer 中修改返回值，让调用者得到安全的结果。</li></ul></li><li><p><strong>传播终止与程序崩溃</strong><br> 如果整个调用链的 defer 都未调用 recover，panic 会到达 goroutine 的顶层，届时进程崩溃并打印堆栈信息。</p></li><li><p><strong>设计意图</strong><br> <code>panic/recover</code> 不是常规错误处理工具，而是应对不可恢复错误（如数组越界、除零等运行时错误）或极其严重的业务异常的保护机制，一般只在库的最外层使用。</p></li></ol><p>通过这个逐步追踪的过程，你应该可以直观感受到 panic 如何沿调用链传播，以及 defer + recover 如何灵活地掐断这场“恐慌”。</p><hr><ol start="5"><li>常见用途</li></ol><ul><li><strong>防止程序崩溃</strong>：在 HTTP 服务器、goroutine 中，一个请求&#x2F;任务发生 panic 不应让整个服务挂掉。</li><li><strong>清理资源 + 错误包装</strong>：在库函数中捕获 panic，将其转换为 <code>error</code> 返回，符合 Go 错误处理约定。</li><li><strong>自定义恢复操作</strong>：记录日志、输出当前状态。</li></ul><hr><ol start="6"><li>重要规则和陷阱</li></ol><ul><li><p><code>recover</code> 必须放在 <code>defer</code> 直接调用的函数中：</p>  <figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-keyword">defer</span> <span class="hljs-function"><span class="hljs-keyword">func</span><span class="hljs-params">()</span></span> &#123; <span class="hljs-built_in">recover</span>() &#125;()  <span class="hljs-comment">// 正确</span><br><span class="hljs-keyword">defer</span> <span class="hljs-built_in">recover</span>()                <span class="hljs-comment">// 错误！recover 并非在 defer 直接调用的函数内</span><br></code></pre></td></tr></table></figure></li><li><p><code>recover</code> 只能捕获<strong>同一个 goroutine</strong> 中的 panic，跨 goroutine 无法恢复。</p></li><li><p>即使恢复了，panic 导致的部分代码没有执行（如资源可能未初始化），需谨慎处理恢复后的状态。</p></li><li><p>不要滥用 panic&#x2F;recover 作为常规错误处理（<code>error</code> 更合适），应仅用于真正意外和无法恢复的情况。</p></li></ul><hr><ol start="7"><li>总结关系</li></ol><table><thead><tr><th>机制</th><th>作用</th></tr></thead><tbody><tr><td><code>defer</code></td><td>保证清理代码一定运行，即使 panic 也会执行。</td></tr><tr><td><code>panic</code></td><td>标记严重错误，停止当前控制流，运行 defer，向上传播。</td></tr><tr><td><code>recover</code></td><td>在 defer 内阻止 panic 传播，恢复正常执行。</td></tr></tbody></table><p>三者组合成了 Go 的异常处理模式：<strong>“先延迟清理，出错了通过恢复保护整个程序”</strong>。理解这一机制对编写健壮的 Go 代码至关重要。</p><p><code>defer</code> 核心特性一览：</p><ol><li><strong>延迟执行</strong>：在函数返回前调用，保证清理代码一定运行。</li><li><strong>LIFO 顺序</strong>：后注册先执行。</li><li><strong>参数立即求值</strong>：除非使用闭包捕获变量引用。</li><li><strong>可修改命名返回值</strong>。</li><li><strong>与 panic&#x2F;recover 配合</strong>实现错误恢复。</li></ol><p><strong>参数立即求值</strong>：<code>defer</code>语句中的函数参数会<strong>立即</strong>被求值并捕获，而不是在函数返回时才求值。</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">f</span><span class="hljs-params">()</span></span> &#123;<br>    i := <span class="hljs-number">0</span><br>    <span class="hljs-keyword">defer</span> fmt.Println(i) <span class="hljs-comment">// 输出 0，因为 i 的值(0)在 defer 时已被捕获</span><br>    i++<br>&#125;<br></code></pre></td></tr></table></figure><p>- </p><pre><code class="hljs">**LIFO 执行**：多个 `defer`语句按**后进先出**顺序执行。</code></pre><p>- </p><pre><code class="hljs">**修改具名返回值**：`defer`可以访问并修改函数的**具名返回值**。<figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">f</span><span class="hljs-params">()</span></span> (result <span class="hljs-type">int</span>) &#123;<br>    <span class="hljs-keyword">defer</span> <span class="hljs-function"><span class="hljs-keyword">func</span><span class="hljs-params">()</span></span> &#123; result++ &#125;()<br>    <span class="hljs-keyword">return</span> <span class="hljs-number">1</span> <span class="hljs-comment">// 函数实际返回 2</span><br>&#125;<br></code></pre></td></tr></table></figure></code></pre><p>- </p><pre><code class="hljs">**资源管理与 panic**：`defer`确保资源（如文件、锁）在函数退出（无论是正常返回还是发生 panic）时被释放，是 Go 中资源管理的核心机制。</code></pre><p>- </p><pre><code class="hljs">**性能考量**：`defer`有微小性能开销，在极度热点的循环中，如果可能，手动管理资源（如直接调用 `mu.Unlock()`）可能更高效，但牺牲了安全性。可读性和正确性优先。</code></pre><h3 id="goto"><a href="#goto" class="headerlink" title="goto"></a>goto</h3><p><code>goto</code>可以跳转到当前函数内的标签处。<strong>限制</strong>：不能跳过变量声明（即不能从作用域外跳转到作用域内）。由于其可能破坏代码结构，应<strong>极其谨慎</strong>使用，通常可以用 <code>break</code>、<code>continue</code>、<code>return</code>或函数抽离来替代。一个可被接受的用例是在复杂的错误处理中集中清理资源，但 <code>defer</code>通常是更好的选择。</p><p>Go 语言中的 goto</p><p>Go 语言<strong>支持 <code>goto</code> 语句</strong>，但对其使用做了严格限制，以避免传统 goto 导致的可读性问题。与 C 语言相比，Go 的 goto 更加安全，<strong>不能跳转到其他函数，也不能跳过变量声明</strong>。</p><p>一、语法与基本示例</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-keyword">package</span> main<br><br><span class="hljs-keyword">import</span> <span class="hljs-string">&quot;fmt&quot;</span><br><br><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">main</span><span class="hljs-params">()</span></span> &#123;<br>    x := <span class="hljs-number">1</span><br>    <span class="hljs-keyword">if</span> x == <span class="hljs-number">1</span> &#123;<br>        <span class="hljs-keyword">goto</span> label<br>    &#125;<br>    fmt.Println(<span class="hljs-string">&quot;这行不会执行&quot;</span>)<br>label:<br>    fmt.Println(<span class="hljs-string">&quot;跳转到了这里&quot;</span>)<br>&#125;<br></code></pre></td></tr></table></figure><p>输出：<code>跳转到了这里</code></p><p>二、Go 中 goto 的限制</p><table><thead><tr><th>限制</th><th>说明</th></tr></thead><tbody><tr><td><strong>仅限函数内部</strong></td><td>不能跨函数跳转。</td></tr><tr><td><strong>不能跳过变量声明</strong></td><td>如果跳转的目标在某个变量的声明之前，且该变量在跳转后还被使用，会导致编译错误。</td></tr><tr><td><strong>不能跳入更深的代码块</strong></td><td>可以跳出代码块，但不可以跳入内层代码块（如 <code>if</code>、<code>for</code> 内部）。</td></tr></tbody></table><p>编译错误示例（跳过变量声明）</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">demo</span><span class="hljs-params">()</span></span> &#123;<br>    <span class="hljs-keyword">goto</span> label<br>    <span class="hljs-keyword">var</span> a <span class="hljs-type">int</span> = <span class="hljs-number">10</span>  <span class="hljs-comment">// 被跳过的声明</span><br>label:<br>    a = <span class="hljs-number">20</span>         <span class="hljs-comment">// 错误：跳过了变量 a 的声明</span><br>    fmt.Println(a)<br>&#125;<br></code></pre></td></tr></table></figure><p>编译报错：<code>goto label jumps over declaration of a</code></p><p>合法示例（跳出代码块）</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">demo</span><span class="hljs-params">()</span></span> &#123;<br>    <span class="hljs-keyword">for</span> i := <span class="hljs-number">0</span>; i &lt; <span class="hljs-number">10</span>; i++ &#123;<br>        <span class="hljs-keyword">for</span> j := <span class="hljs-number">0</span>; j &lt; <span class="hljs-number">10</span>; j++ &#123;<br>            <span class="hljs-keyword">if</span> i*j &gt; <span class="hljs-number">50</span> &#123;<br>                <span class="hljs-keyword">goto</span> exit  <span class="hljs-comment">// 跳出双重循环</span><br>            &#125;<br>        &#125;<br>    &#125;<br>exit:<br>    fmt.Println(<span class="hljs-string">&quot;退出循环&quot;</span>)<br>&#125;<br></code></pre></td></tr></table></figure><p>三、常见使用场景</p><ol><li>错误处理与资源清理（类似 C 语言风格）</li></ol><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">readFile</span><span class="hljs-params">(name <span class="hljs-type">string</span>)</span></span> <span class="hljs-type">error</span> &#123;<br>    f, err := os.Open(name)<br>    <span class="hljs-keyword">if</span> err != <span class="hljs-literal">nil</span> &#123;<br>        <span class="hljs-keyword">goto</span> errorOpen<br>    &#125;<br>    <span class="hljs-keyword">defer</span> f.Close()  <span class="hljs-comment">// 不过有 defer 通常不需要 goto</span><br><br>    buf, err := io.ReadAll(f)<br>    <span class="hljs-keyword">if</span> err != <span class="hljs-literal">nil</span> &#123;<br>        <span class="hljs-keyword">goto</span> errorRead<br>    &#125;<br>    <span class="hljs-comment">// 处理 buf...</span><br>    <span class="hljs-keyword">return</span> <span class="hljs-literal">nil</span><br><br>errorOpen:<br>    <span class="hljs-keyword">return</span> fmt.Errorf(<span class="hljs-string">&quot;open error: %v&quot;</span>, err)<br>errorRead:<br>    <span class="hljs-keyword">return</span> fmt.Errorf(<span class="hljs-string">&quot;read error: %v&quot;</span>, err)<br>&#125;<br></code></pre></td></tr></table></figure><blockquote><p><strong>注意</strong>：Go 语言更推荐使用 <strong>defer + 错误返回值</strong>，而非 goto 做清理。上述例子仅为演示，实际上可以用更简洁的方式。</p></blockquote><ol start="2"><li>跳出多重循环</li></ol><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs go">outer:<br>    <span class="hljs-keyword">for</span> i := <span class="hljs-number">0</span>; i &lt; <span class="hljs-number">10</span>; i++ &#123;<br>        <span class="hljs-keyword">for</span> j := <span class="hljs-number">0</span>; j &lt; <span class="hljs-number">10</span>; j++ &#123;<br>            <span class="hljs-keyword">if</span> condition &#123;<br>                <span class="hljs-keyword">break</span> outer  <span class="hljs-comment">// 跳出多层循环，无需 goto</span><br>            &#125;<br>        &#125;<br>    &#125;<br></code></pre></td></tr></table></figure><p>Go 的 <strong><code>break label</code></strong> 可以优雅地跳出多重循环，因此大多数情况下并不需要 <code>goto</code>。</p><ol start="3"><li>状态机实现</li></ol><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">stateMachine</span><span class="hljs-params">(state <span class="hljs-type">int</span>)</span></span> &#123;<br>    <span class="hljs-keyword">switch</span> state &#123;<br>    <span class="hljs-keyword">case</span> <span class="hljs-number">0</span>:<br>        fmt.Println(<span class="hljs-string">&quot;state 0&quot;</span>)<br>        <span class="hljs-keyword">goto</span> case1<br>    <span class="hljs-keyword">case</span> <span class="hljs-number">1</span>:<br>        fmt.Println(<span class="hljs-string">&quot;state 1&quot;</span>)<br>        <span class="hljs-keyword">goto</span> case2<br>    <span class="hljs-keyword">case</span> <span class="hljs-number">2</span>:<br>        fmt.Println(<span class="hljs-string">&quot;state 2&quot;</span>)<br>    &#125;<br>case1:<br>    fmt.Println(<span class="hljs-string">&quot;fallback to case1&quot;</span>)<br>    <span class="hljs-keyword">goto</span> case2<br>case2:<br>    fmt.Println(<span class="hljs-string">&quot;fallback to case2&quot;</span>)<br>&#125;<br></code></pre></td></tr></table></figure><p><strong>但不推荐</strong>，状态机可以用循环 + switch 清晰表达。</p><p>四、Go 语言官方建议</p><p>Go 语言的设计者在规范中明确指出：</p><blockquote><p>尽管 <code>goto</code> 可能会使程序难以理解，但在某些有限场景下（例如跳出深层嵌套）仍可使用。程序员应谨慎使用，且不能跳过变量声明。</p></blockquote><p>实际上，Go 社区普遍认为：</p><ul><li>优先使用结构化控制流（<code>if</code>、<code>for</code>、<code>switch</code>、<code>break label</code>）。</li><li><code>goto</code> 仅适用于极其罕见的优化或特定模式（如手工解析器中的跳转）。</li></ul><p>五、与其他语言的对比</p><table><thead><tr><th>特性</th><th>C &#x2F; C++</th><th>Go</th></tr></thead><tbody><tr><td>跨函数跳转</td><td>不支持</td><td>不支持</td></tr><tr><td>跳过变量声明</td><td>允许（危险）</td><td>禁止编译</td></tr><tr><td>跳入内层代码块</td><td>允许</td><td>禁止</td></tr><tr><td>跳出多重循环</td><td>用 goto</td><td>可用 <code>break label</code></td></tr><tr><td>错误处理中常用</td><td>较常见</td><td>极少（用 defer&#x2F;error）</td></tr></tbody></table><p>六、总结</p><ul><li>Go <strong>支持 <code>goto</code></strong>，但做了严格限制（禁止跳过变量声明、不能跳进内层块）。</li><li><strong>推荐替代方案</strong>：<code>break label</code>、<code>defer</code>、函数返回值。</li><li>除非你编写非常底层的解析器或需要精确控制跳转，否则<strong>尽量不要使用 <code>goto</code></strong>。</li><li>绝大多数 Go 代码中，你几乎见不到 <code>goto</code> — 这是语言设计有意引导的结果。</li></ul><p>如果你正在学习 Go，建议先完全掌握 <code>break label</code>、<code>defer</code> 和错误处理模式，它们足以覆盖几乎所有曾经需要用 <code>goto</code> 的场景。</p><hr><h2 id="6-函数与方法"><a href="#6-函数与方法" class="headerlink" title="6. 函数与方法"></a>6. 函数与方法</h2><h3 id="函数声明"><a href="#函数声明" class="headerlink" title="函数声明"></a>函数声明</h3><p>Go 语言中的函数声明</p><p>函数是 Go 语言中的<strong>一等公民</strong>，可以独立声明和使用。Go 的函数支持<strong>多返回值</strong>、<strong>命名返回值</strong>、<strong>可变参数</strong>，并且函数本身也是一种类型，可以作为参数或返回值。</p><hr><p>一、基本声明格式</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> 函数名<span class="hljs-params">(参数列表)</span></span> (返回值列表) &#123;<br>    函数体<br>&#125;<br></code></pre></td></tr></table></figure><ul><li><code>func</code> 关键字</li><li>参数列表：<code>(参数名 类型, ...)</code>，如果相邻参数类型相同可以合并</li><li>返回值列表：可以没有、有一个、或多个</li><li>函数体用 <code>&#123;&#125;</code> 包裹，左大括号必须与函数名在同一行</li></ul><hr><p>二、参数声明</p><ol><li>普通参数</li></ol><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">add</span><span class="hljs-params">(x <span class="hljs-type">int</span>, y <span class="hljs-type">int</span>)</span></span> <span class="hljs-type">int</span> &#123;<br>    <span class="hljs-keyword">return</span> x + y<br>&#125;<br></code></pre></td></tr></table></figure><ol start="2"><li>类型简写（相同类型可合并）</li></ol><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">add</span><span class="hljs-params">(x, y <span class="hljs-type">int</span>)</span></span> <span class="hljs-type">int</span> &#123;   <span class="hljs-comment">// x 和 y 都是 int</span><br>    <span class="hljs-keyword">return</span> x + y<br>&#125;<br></code></pre></td></tr></table></figure><ol start="3"><li>没有参数</li></ol><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">sayHello</span><span class="hljs-params">()</span></span> <span class="hljs-type">string</span> &#123;<br>    <span class="hljs-keyword">return</span> <span class="hljs-string">&quot;Hello&quot;</span><br>&#125;<br></code></pre></td></tr></table></figure><hr><p>三、返回值</p><ol><li>无返回值（仅执行操作）</li></ol><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">printSum</span><span class="hljs-params">(a, b <span class="hljs-type">int</span>)</span></span> &#123;<br>    fmt.Println(a + b)<br>&#125;<br></code></pre></td></tr></table></figure><ol start="2"><li>单返回值（可省略括号）</li></ol><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">square</span><span class="hljs-params">(x <span class="hljs-type">int</span>)</span></span> <span class="hljs-type">int</span> &#123;<br>    <span class="hljs-keyword">return</span> x * x<br>&#125;<br></code></pre></td></tr></table></figure><ol start="3"><li>多返回值（最典型的 Go 风格）</li></ol><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">div</span><span class="hljs-params">(a, b <span class="hljs-type">int</span>)</span></span> (<span class="hljs-type">int</span>, <span class="hljs-type">error</span>) &#123;<br>    <span class="hljs-keyword">if</span> b == <span class="hljs-number">0</span> &#123;<br>        <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>, errors.New(<span class="hljs-string">&quot;division by zero&quot;</span>)<br>    &#125;<br>    <span class="hljs-keyword">return</span> a / b, <span class="hljs-literal">nil</span><br>&#125;<br></code></pre></td></tr></table></figure><p>调用时常用多重赋值：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs go">result, err := div(<span class="hljs-number">10</span>, <span class="hljs-number">2</span>)<br></code></pre></td></tr></table></figure><ol start="4"><li>命名返回值</li></ol><p>可以在函数签名中给返回值起名字，函数体内可直接 <code>return</code>（裸返回）自动返回这些变量。</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">split</span><span class="hljs-params">(sum <span class="hljs-type">int</span>)</span></span> (x, y <span class="hljs-type">int</span>) &#123;<br>    x = sum * <span class="hljs-number">4</span> / <span class="hljs-number">9</span><br>    y = sum - x<br>    <span class="hljs-keyword">return</span>   <span class="hljs-comment">// 裸返回，返回 x 和 y 的当前值</span><br>&#125;<br></code></pre></td></tr></table></figure><blockquote><p><strong>注意</strong>：命名返回值会初始化为零值，且裸返回适用于短函数，长函数中应显式 <code>return x, y</code> 以提高可读性。</p></blockquote><hr><p>四、可变参数</p><p>使用 <code>...类型</code> 表示接收零个或多个该类型的参数，函数内作为切片使用。</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">sum</span><span class="hljs-params">(nums ...<span class="hljs-type">int</span>)</span></span> <span class="hljs-type">int</span> &#123;<br>    total := <span class="hljs-number">0</span><br>    <span class="hljs-keyword">for</span> _, n := <span class="hljs-keyword">range</span> nums &#123;<br>        total += n<br>    &#125;<br>    <span class="hljs-keyword">return</span> total<br>&#125;<br><br><span class="hljs-comment">// 调用</span><br>fmt.Println(sum(<span class="hljs-number">1</span>, <span class="hljs-number">2</span>, <span class="hljs-number">3</span>))      <span class="hljs-comment">// 6</span><br>fmt.Println(sum())             <span class="hljs-comment">// 0</span><br><br><span class="hljs-comment">// 传入切片</span><br>slice := []<span class="hljs-type">int</span>&#123;<span class="hljs-number">1</span>, <span class="hljs-number">2</span>, <span class="hljs-number">3</span>&#125;<br>fmt.Println(sum(slice...))     <span class="hljs-comment">// 使用 ... 展开切片</span><br></code></pre></td></tr></table></figure><hr><p>五、函数作为值（First-class functions）</p><p>Go 函数可以赋值给变量、作为参数传递、作为返回值。</p><ol><li>赋值给变量</li></ol><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-keyword">var</span> fn<span class="hljs-comment">/*变量名*/</span> <span class="hljs-function"><span class="hljs-keyword">func</span><span class="hljs-params">(<span class="hljs-type">int</span>, <span class="hljs-type">int</span>)</span></span> <span class="hljs-type">int</span><span class="hljs-comment">/*变量类型   该类型表示：一个接收两个 int 参数、返回一个 int 的函数*/</span> = add<br>fmt.Println(fn(<span class="hljs-number">3</span>, <span class="hljs-number">4</span>))  <span class="hljs-comment">// 7</span><br><span class="hljs-comment">/*等同于</span><br><span class="hljs-comment">fn = add</span><br><span class="hljs-comment">func add(x, y int) int &#123;</span><br><span class="hljs-comment">    return x + y</span><br><span class="hljs-comment">&#125;</span><br><span class="hljs-comment">*/</span><br></code></pre></td></tr></table></figure><ol start="2"><li>作为参数（高阶函数）</li></ol><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">apply</span><span class="hljs-params">(f <span class="hljs-keyword">func</span>(<span class="hljs-type">int</span>, <span class="hljs-type">int</span>)</span></span> <span class="hljs-type">int</span>, a, b <span class="hljs-type">int</span>) <span class="hljs-type">int</span> &#123;  <span class="hljs-comment">/* f 是一个函数类型的参数：func(int, int) int，表示它接收两个 int 参数，返回一个 int。*/</span><br>    <span class="hljs-keyword">return</span> f(a, b)<br>&#125;<br>result := apply(add, <span class="hljs-number">3</span>, <span class="hljs-number">4</span>)   <span class="hljs-comment">// 7</span><br></code></pre></td></tr></table></figure><ol start="3"><li>作为返回值</li></ol><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">makeMultiplier</span><span class="hljs-params">(factor <span class="hljs-type">int</span>)</span></span> <span class="hljs-function"><span class="hljs-keyword">func</span><span class="hljs-params">(<span class="hljs-type">int</span>)</span></span> <span class="hljs-type">int</span> &#123;<br>    <span class="hljs-keyword">return</span> <span class="hljs-function"><span class="hljs-keyword">func</span><span class="hljs-params">(x <span class="hljs-type">int</span>)</span></span> <span class="hljs-type">int</span> &#123;  <span class="hljs-comment">/*返回一个匿名函数，该函数接收 x，返回 x * factor。</span><br><span class="hljs-comment">匿名函数捕获了外部变量 factor，形成闭包。*/</span><br>        <span class="hljs-keyword">return</span> x * factor<br>    &#125;<br>&#125;<br>double := makeMultiplier(<span class="hljs-number">2</span>) <span class="hljs-comment">//创建闭包实例  返回的匿名函数“记住”了 factor = 2，将其赋值给变量 double。现在 double 等价于：func(x int) int &#123; return x * 2 &#125;。</span><br>fmt.Println(double(<span class="hljs-number">5</span>))  <span class="hljs-comment">// 10</span><br><span class="hljs-comment">/* 闭包 = 匿名函数 + 它引用的外部变量。</span><br><span class="hljs-comment">makeMultiplier 返回的函数不仅是一个运算逻辑，还携带了创建时 factor 的值。</span><br><span class="hljs-comment">闭包中的 factor 被称为“自由变量”，它会随着闭包一起存活，即使 makeMultiplier 函数已经返回。*/</span><br></code></pre></td></tr></table></figure><hr><p>六、匿名函数与闭包</p><p>没有名字的函数，通常用于就地定义，可以捕获外部变量（形成闭包）。</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">main</span><span class="hljs-params">()</span></span> &#123;<br>    x := <span class="hljs-number">10</span><br>    <span class="hljs-comment">// 定义并立即调用</span><br>    result := <span class="hljs-function"><span class="hljs-keyword">func</span><span class="hljs-params">(y <span class="hljs-type">int</span>)</span></span> <span class="hljs-type">int</span> &#123;<br>        <span class="hljs-keyword">return</span> x + y<br>    &#125;(<span class="hljs-number">5</span>)   <span class="hljs-comment">//紧跟在 &#125; 后面的 (5) 是立即调用这个匿名函数，传入参数 5。</span><br>    fmt.Println(result)  <span class="hljs-comment">// 15</span><br><br>    <span class="hljs-comment">// 赋给变量后调用</span><br>    addX := <span class="hljs-function"><span class="hljs-keyword">func</span><span class="hljs-params">(y <span class="hljs-type">int</span>)</span></span> <span class="hljs-type">int</span> &#123;<br>        <span class="hljs-keyword">return</span> x + y<br>    &#125;<br>    fmt.Println(addX(<span class="hljs-number">20</span>)) <span class="hljs-comment">// 30</span><br>&#125;<br><span class="hljs-comment">/*func(y int) int &#123; return x + y &#125; 定义了一个匿名函数，但没有立即调用。</span><br><span class="hljs-comment">将这个匿名函数赋值给变量 addX。此时 addX 的类型是 func(int) int。</span><br><span class="hljs-comment">这个匿名函数捕获了外层的变量 x（值为 10），形成了一个闭包。</span><br><span class="hljs-comment">之后通过 addX(20) 调用该函数，传入参数 20，内部执行 x + y，即 10 + 20 = 30，打印 30。</span><br><span class="hljs-comment">addX 可以被多次调用，每次都会使用相同的 x 值（除非后续 x 被修改，但这里 x 没有变化）。*/</span><br></code></pre></td></tr></table></figure><hr><p>七、方法与函数的区别</p><ul><li><strong>函数</strong>：独立声明，不属于任何类型。</li><li><strong>方法</strong>：带有接收者（receiver）的函数，属于某个类型。</li></ul><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-comment">// 函数</span><br><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">add</span><span class="hljs-params">(a, b <span class="hljs-type">int</span>)</span></span> <span class="hljs-type">int</span> &#123; <span class="hljs-keyword">return</span> a + b &#125;<br><br><span class="hljs-comment">// 方法（接收者为 Point 类型）</span><br><span class="hljs-keyword">type</span> Point <span class="hljs-keyword">struct</span>&#123; X, Y <span class="hljs-type">int</span> &#125;<br><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-params">(p Point)</span></span> Distance() <span class="hljs-type">float64</span> &#123;<br>    <span class="hljs-keyword">return</span> math.Sqrt(<span class="hljs-type">float64</span>(p.X*p.X + p.Y*p.Y))<br>&#125;<br></code></pre></td></tr></table></figure><hr><p>八、常见模式与注意事项</p><table><thead><tr><th>特性</th><th>说明</th></tr></thead><tbody><tr><td><strong>多返回值</strong></td><td>常用于返回结果 + 错误，取代异常。</td></tr><tr><td><strong>命名返回值</strong></td><td>适合短函数；长函数建议显式 return。</td></tr><tr><td><strong>可变参数</strong></td><td>必须是最后一个参数。</td></tr><tr><td><strong>函数类型</strong></td><td><code>func(参数类型) 返回值类型</code> 可作为类型使用。</td></tr><tr><td><strong>panic&#x2F;recover</strong></td><td>类似异常，但函数一般不主动使用。</td></tr><tr><td><strong>延迟执行</strong></td><td><code>defer</code> 语句在函数返回前执行，用于释放资源。</td></tr></tbody></table><p>示例：defer + 多返回值</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">readFile</span><span class="hljs-params">(name <span class="hljs-type">string</span>)</span></span> (content []<span class="hljs-type">byte</span>, err <span class="hljs-type">error</span>) &#123;<br>    f, err := os.Open(name)<br>    <span class="hljs-keyword">if</span> err != <span class="hljs-literal">nil</span> &#123;<br>        <span class="hljs-keyword">return</span> <span class="hljs-literal">nil</span>, err<br>    &#125;<br>    <span class="hljs-keyword">defer</span> f.Close()   <span class="hljs-comment">// 保证文件关闭</span><br>    <span class="hljs-keyword">return</span> io.ReadAll(f)<br>&#125;<br></code></pre></td></tr></table></figure><hr><p>九、总结</p><p>Go 的函数声明简洁而强大：</p><ul><li>使用 <code>func</code> 关键字，支持多返回值，天然支持错误处理。</li><li>函数是值，可以传递和返回，支持闭包。</li><li>类型简写、可变参数、命名返回值等语法糖提升表达能力。</li><li>方法与函数独立，通过接收者实现面向对象风格。</li></ul><p>掌握函数声明是成为 Go 开发者的基础，也是写出简洁、可维护代码的关键。</p><p>- </p><pre><code class="hljs">**多返回值**：Go 函数的标志性特性，常用于返回结果和错误。</code></pre><p>- </p><pre><code class="hljs">**命名返回值**：在函数签名中为返回值命名。它们被初始化为零值，并可在函数体中像变量一样使用。使用裸 `return`语句时，将返回这些命名变量的当前值。<figure class="highlight axapta"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs axapta">func split(<span class="hljs-keyword">sum</span> <span class="hljs-built_in">int</span>) (x, y <span class="hljs-built_in">int</span>) &#123;<br>    x = <span class="hljs-keyword">sum</span> * <span class="hljs-number">4</span> / <span class="hljs-number">9</span><br>    y = <span class="hljs-keyword">sum</span> - x<br>    <span class="hljs-keyword">return</span> <span class="hljs-comment">// 裸 return，返回 x 和 y</span><br>&#125;<br></code></pre></td></tr></table></figure>**注意**：在长函数中使用裸 `return`会降低可读性，建议仅在短小简单的函数中使用。</code></pre><p>- </p><pre><code class="hljs">**可变参数**：`func sum(nums ...int) int`。在函数内部，`nums`是一个 `[]int`类型的切片。可以将一个切片展开为可变参数：`total := sum(slice...)`。</code></pre><h3 id="闭包与函数值"><a href="#闭包与函数值" class="headerlink" title="闭包与函数值"></a>闭包与函数值</h3><p>函数是可以赋值给变量、作为参数传递、作为返回值。<strong>闭包</strong>是引用了其外部作用域变量的函数值。</p><p>闭包的生命周期可能超过创建它的函数，因此它捕获的变量会被分配到堆上（逃逸分析）。</p><ul><li><strong>栈</strong>：函数执行时使用的内存区域，速度快，但<strong>生命周期与函数绑定</strong>。函数结束后，栈上的局部变量就会被自动销毁。</li><li><strong>堆</strong>：全局内存池，速度稍慢，但<strong>生命周期不受函数限制</strong>，可以长期存在，需要垃圾回收（GC）来清理。</li></ul><p>Go 编译器会将闭包捕获的变量（如 <code>count</code>）<strong>分配到堆上</strong>，而不是栈上。</p><ul><li>堆上的内存不会因为函数退出而释放，而是由垃圾回收器在不再被引用时回收。</li><li>闭包内部会持有一个指向堆上 <code>count</code> 的指针，这样即使 <code>makeCounter</code> 返回了，闭包仍然能找到 <code>count</code></li></ul><p>逃逸分析（Escape Analysis）</p><p>“逃逸分析”是编译器的一种静态分析技术，用于<strong>决定一个变量应该分配在栈上还是堆上</strong>。</p><ul><li><strong>没有逃逸</strong>：变量只在函数内部使用，函数返回后不再被引用 → 分配在栈上（高效）。</li><li><strong>逃逸</strong>：变量在函数返回后可能仍被引用（比如被闭包捕获，或者返回了指向它的指针）→ 分配在堆上（生命周期更长）。</li></ul><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">adder</span><span class="hljs-params">()</span></span> <span class="hljs-function"><span class="hljs-keyword">func</span><span class="hljs-params">(<span class="hljs-type">int</span>)</span></span> <span class="hljs-type">int</span> &#123;<br>    sum := <span class="hljs-number">0</span> <span class="hljs-comment">// sum 被闭包捕获</span><br>    <span class="hljs-keyword">return</span> <span class="hljs-function"><span class="hljs-keyword">func</span><span class="hljs-params">(x <span class="hljs-type">int</span>)</span></span> <span class="hljs-type">int</span> &#123;<br>        sum += x<br>        <span class="hljs-keyword">return</span> sum<br>    &#125;<br>&#125;<br><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">main</span><span class="hljs-params">()</span></span> &#123;<br>    pos, neg := adder(), adder()<br>    <span class="hljs-keyword">for</span> i := <span class="hljs-number">0</span>; i &lt; <span class="hljs-number">10</span>; i++ &#123;<br>        fmt.Println(pos(i),   <span class="hljs-comment">// 输出: 0, 1, 3, 6, 10, 15, 21, 28, 36, 45</span><br>                    neg(<span class="hljs-number">-2</span>*i))<span class="hljs-comment">// 输出: 0, -2, -6, -12, -20, -30, -42, -56, -72, -90</span><br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><h3 id="方法：值接收者-vs-指针接收者"><a href="#方法：值接收者-vs-指针接收者" class="headerlink" title="方法：值接收者 vs 指针接收者"></a>方法：值接收者 vs 指针接收者</h3><p>Go 语言中的方法（Method）</p><p>Go 语言中的<strong>方法</strong>是一种带有<strong>接收者</strong>（receiver）的函数。接收者可以是某个自定义类型（通常是结构体，也可以是任何非内置类型）的实例。方法与普通函数的主要区别在于：<strong>方法属于某个类型，可以像访问属性一样通过 <code>.</code> 运算符调用</strong>。</p><hr><p>一、方法声明语法</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-params">(接收者变量 接收者类型)</span></span> 方法名(参数列表) 返回值列表 &#123;<br>    函数体<br>&#125;<br></code></pre></td></tr></table></figure><ul><li><strong>接收者变量</strong>：方法内部用于访问接收者实例的变量名（类似其他语言的 <code>this</code> 或 <code>self</code>）。</li><li><strong>接收者类型</strong>：可以是结构体类型或自定义类型（<strong>不能是内置类型</strong>，如 <code>int</code>、<code>string</code>，但可以用 <code>type</code> 别名）。</li><li><strong>方法名</strong>：同函数名规则。</li></ul><p>示例：给 <code>Person</code> 结构体添加方法</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-keyword">type</span> Person <span class="hljs-keyword">struct</span> &#123;<br>    Name <span class="hljs-type">string</span><br>    Age  <span class="hljs-type">int</span><br>&#125;<br><br><span class="hljs-comment">// 值接收者：方法内部不会修改原实例</span><br><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-params">(p Person)</span></span> Greet() <span class="hljs-type">string</span> &#123;<br>    <span class="hljs-keyword">return</span> <span class="hljs-string">&quot;Hello, I&#x27;m &quot;</span> + p.Name<br>&#125;<br><br><span class="hljs-comment">// 指针接收者：可以修改原实例</span><br><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-params">(p *Person)</span></span> Birthday() &#123;<br>    p.Age++<br>&#125;<br></code></pre></td></tr></table></figure><hr><p>二、调用方法</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">main</span><span class="hljs-params">()</span></span> &#123;<br>    p := Person&#123;Name: <span class="hljs-string">&quot;Alice&quot;</span>, Age: <span class="hljs-number">30</span>&#125;<br><br>    <span class="hljs-comment">// 调用值接收者方法</span><br>    msg := p.Greet()   <span class="hljs-comment">// &quot;Hello, I&#x27;m Alice&quot;</span><br><br>    <span class="hljs-comment">// 调用指针接收者方法</span><br>    p.Birthday()       <span class="hljs-comment">// p.Age 变为 31</span><br><br>    fmt.Println(msg, p.Age)<br>&#125;<br></code></pre></td></tr></table></figure><hr><p>三、接收者类型的选择：值 vs 指针</p><table><thead><tr><th>接收者类型</th><th>何时使用</th><th>特点</th></tr></thead><tbody><tr><td><strong>值接收者</strong> <code>(p Person)</code></td><td>方法不需要修改接收者；接收者较小（通常 &lt; 128 字节）；接收者类型是 map、slice、channel 等引用类型。</td><td>方法内部修改的是接收者的副本，不影响原实例。调用时不会复制指针（本身是副本）。</td></tr><tr><td><strong>指针接收者</strong> <code>(p *Person)</code></td><td>方法需要修改接收者；接收者较大（避免复制开销）；需要保持一致性（如实现接口时）。</td><td>方法内部修改会影响原实例。调用时不会复制整个接收者。</td></tr></tbody></table><blockquote><p><strong>最佳实践</strong>：如果类型的方法集合中有一个方法使用了指针接收者，最好所有方法都使用指针接收者，保持一致性。</p></blockquote><hr><p>四、方法与普通函数的区别</p><table><thead><tr><th>特性</th><th>方法</th><th>函数</th></tr></thead><tbody><tr><td>接收者</td><td>有</td><td>无</td></tr><tr><td>调用方式</td><td><code>实例.方法名(参数)</code></td><td><code>函数名(参数)</code></td></tr><tr><td>属于</td><td>某个类型</td><td>无归属</td></tr><tr><td>接口实现</td><td>方法用于实现接口</td><td>不能</td></tr></tbody></table><p><strong>函数不能直接当作方法调用</strong></p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">SayHi</span><span class="hljs-params">(p Person)</span></span> <span class="hljs-type">string</span> &#123;<br>    <span class="hljs-keyword">return</span> <span class="hljs-string">&quot;Hi &quot;</span> + p.Name<br>&#125;<br><br><span class="hljs-comment">// 调用：SayHi(p)</span><br><span class="hljs-comment">// 不能 p.SayHi()</span><br></code></pre></td></tr></table></figure><hr><p>五、方法值与方法表达式</p><ol><li>方法值（Method Value）</li></ol><p>将方法绑定到特定实例，生成一个函数值，可以稍后调用。</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs go">p := Person&#123;Name: <span class="hljs-string">&quot;Bob&quot;</span>&#125;<br>greetFunc := p.Greet   <span class="hljs-comment">// 方法值，已经绑定了接收者 p    p.Greet 是一个方法值（method value）。它不是一个普通的方法调用（没有 ()），而是一个将方法 Greet 绑定到特定接收者 p 上形成的函数值。</span><br>greetFunc()            <span class="hljs-comment">// 等价于 p.Greet()</span><br></code></pre></td></tr></table></figure><ol start="2"><li>方法表达式（Method Expression）</li></ol><p>方法表达式是将类型的方法当作普通函数来使用，<strong>显式地将接收者作为第一个参数传递</strong>。与<strong>方法值</strong>（固定接收者）不同，方法表达式不绑定具体实例，而是生成一个函数类型，需要调用者手动提供接收者。</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs go">greetFunc2 := Person.Greet   <span class="hljs-comment">// 方法表达式，类型为 func(Person) string  </span><br><span class="hljs-comment">// 此时 greetFunc2 是函数：func(p Person) string</span><br>msg := greetFunc2(p)         <span class="hljs-comment">// 需要手动传入接收者</span><br></code></pre></td></tr></table></figure><p>与方法值的区别</p><table><thead><tr><th align="left">特性</th><th align="left">方法值 (<code>p.Greet</code>)</th><th align="left">方法表达式 (<code>Person.Greet</code>)</th></tr></thead><tbody><tr><td align="left">形式</td><td align="left">实例.方法名</td><td align="left">类型.方法名</td></tr><tr><td align="left">接收者</td><td align="left">固定绑定到具体实例 <code>p</code></td><td align="left">未绑定，作为第一个参数传入</td></tr><tr><td align="left">生成函数的类型</td><td align="left"><code>func() string</code>（如果原方法无其他参数）</td><td align="left"><code>func(Person) string</code>（第一个参数是接收者）</td></tr><tr><td align="left">调用</td><td align="left"><code>greetFunc()</code> 无需参数</td><td align="left"><code>greetFunc2(p)</code> 必须传入接收者</td></tr><tr><td align="left">适用场景</td><td align="left">延迟调用、回调函数，接收者已确定</td><td align="left">需要灵活指定接收者，例如将方法作为参数传递时</td></tr></tbody></table><hr><p>六、方法与接口</p><p>Go 的接口是<strong>鸭子类型</strong>：只要一个类型实现了接口要求的所有方法，它就自动满足该接口，无需显式声明 <code>implements</code></p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-keyword">type</span> Greeter <span class="hljs-keyword">interface</span> &#123;<br>    Greet() <span class="hljs-type">string</span>  <span class="hljs-comment">//定义了一个接口 Greeter，它要求实现一个 Greet 方法，返回 string。</span><br>&#125;<br><br><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">PrintGreeting</span><span class="hljs-params">(g Greeter)</span></span> &#123;<br>    fmt.Println(g.Greet())<br>&#125;<br><span class="hljs-comment">//PrintGreeting 函数接收一个类型为 Greeter 接口的参数。在函数内部，它调用参数 g 的 Greet() 方法。任何实现了 Greet() string 方法的类型都可以被传入这个函数（因为 Go 的接口是隐式满足的）。</span><br><br><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">main</span><span class="hljs-params">()</span></span> &#123;<br>    p := Person&#123;Name: <span class="hljs-string">&quot;Alice&quot;</span>&#125;<br>    PrintGreeting(p)   <span class="hljs-comment">// Person 实现了 Greeter 接口</span><br>&#125;<br></code></pre></td></tr></table></figure><p>注意：</p><ul><li>值接收者实现的方法既可被值调用，也可被指针调用（编译器自动解引用）。</li><li>指针接收者实现的方法只能被指针调用（因为需要修改原值）。</li></ul><hr><p>七、常见陷阱与注意事项</p><ol><li><p><strong>nil 接收者</strong>：方法内部可以处理 nil 接收者，但需要自行判断。</p> <figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-params">(p *Person)</span></span> IsNil() <span class="hljs-type">bool</span> &#123;<br>    <span class="hljs-keyword">return</span> p == <span class="hljs-literal">nil</span><br>&#125;<br></code></pre></td></tr></table></figure></li><li><p><strong>指针接收者与值接收者的接口一致性</strong>：如果某个方法使用指针接收者，那么只有指针类型的实例才能赋值给该接口变量。</p></li><li><p><strong>小对象尽量用值接收者</strong>：减少堆分配，提高性能。</p></li></ol><hr><p>八、总结</p><ul><li><strong>方法是 Go 实现面向对象风格的途径</strong>：将函数绑定到类型，增强封装性和代码组织性。</li><li><strong>接收者可以是值或指针</strong>：根据是否需要修改原对象及性能需求选择。</li><li><strong>方法与普通函数本质类似，只是多了接收者</strong>。</li><li><strong>接口通过方法集隐式实现</strong>，这是 Go 多态的核心。</li></ul><p>方法是与特定类型关联的函数。接收者可以是值类型或指针类型。</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-keyword">type</span> Counter <span class="hljs-keyword">struct</span> &#123; n <span class="hljs-type">int</span> &#125;<br><span class="hljs-comment">// 值接收者方法</span><br><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-params">(c Counter)</span></span> Value() <span class="hljs-type">int</span> &#123; <span class="hljs-keyword">return</span> c.n &#125; <span class="hljs-comment">// 操作 c 的副本</span><br><span class="hljs-comment">// 指针接收者方法</span><br><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-params">(c *Counter)</span></span> Incr() &#123; c.n++ &#125; <span class="hljs-comment">// 可以修改原值</span><br></code></pre></td></tr></table></figure><p>- </p><pre><code class="hljs">**选择规则**：1. **需要修改接收者**：必须使用指针接收者。2. **接收者是大型结构体**：使用指针接收者避免拷贝开销。3. **一致性**：如果某个方法需要指针接收者，那么该类型的大部分方法都应使用指针接收者，以保持一致性。4. **其他情况**：通常使用值接收者。对于基本类型、小结构体或不需要修改的场景，值接收者更清晰且安全。</code></pre><p>- </p><pre><code class="hljs">**自动转换**：在 Go 语言中，当你通过一个变量调用方法时，编译器会**自动在值和指针之间进行转换**，让你不必显式地写 `&amp;`（取地址）或 `*`（解引用）。| 变量类型 | 方法接收者类型 | 是否允许 | 编译器自动执行的动作           || :------- | :------------- | :------- | :----------------------------- || 值变量   | 值接收者       | ✅ 允许   | 直接将值传给接收者             || 值变量   | 指针接收者     | ✅ 允许   | 自动取地址（`(&amp;变量).方法()`） || 指针变量 | 值接收者       | ✅ 允许   | 自动解引用（`(*指针).方法()`） || 指针变量 | 指针接收者     | ✅ 允许   | 直接传递指针                   |**例外**：不可寻址的值（如 `map`的元素、函数调用的返回值）不能调用指针接收者方法。1. 定义一个类型及方法<figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-keyword">type</span> Counter <span class="hljs-keyword">struct</span> &#123;<br>    n <span class="hljs-type">int</span><br>&#125;<br><span class="hljs-comment">// 值接收者：方法内部修改的是副本，不影响原值</span><br><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-params">(c Counter)</span></span> Value() <span class="hljs-type">int</span> &#123;<br>    <span class="hljs-keyword">return</span> c.n<br>&#125;<br><span class="hljs-comment">// 指针接收者：方法内部可以修改原值</span><br><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-params">(c *Counter)</span></span> Incr() &#123;<br>    c.n++<br>&#125;<br></code></pre></td></tr></table></figure>2. 值变量调用指针接收者方法（自动取地址）<figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs go">c := Counter&#123;n: <span class="hljs-number">0</span>&#125;   <span class="hljs-comment">// c 是一个值变量（不是指针） Counter&#123;n: 0&#125; 表示创建一个 Counter 类型的值，并将字段 n 初始化为 0。这种写法称为字段名初始化，只显式指定了 n 字段，其他未指定的字段（如果有）会被自动设置为该类型的零值（例如 int 为零，string 为空，指针为 nil）。</span><br><span class="hljs-comment">//等价于 声明一个名为 c 的变量，类型为 Counter。将 c 初始化为一个结构体实例，其字段 n 的值为 0。c 是一个值类型变量（不是指针），可以直接使用。</span><br><br><span class="hljs-comment">// 虽然 Incr 的接收者是 *Counter，但 Go 允许这样写：</span><br>c.Incr()   <span class="hljs-comment">// 编译器自动转换成 (&amp;c).Incr()  这个转换发生在编译时，是语法糖，无需程序员手动取地址。</span><br><br>fmt.Println(c.Value()) <span class="hljs-comment">// 输出 1</span><br></code></pre></td></tr></table></figure>**原理**：`c.Incr()` 这种写法在编译时被重写为 `(&amp;c).Incr()`，因为 `Incr` 需要一个指针接收者。这样就避免了手动写 `&amp;c.Incr()`。3. 指针变量调用值接收者方法（自动解引用）<figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs go">pc := &amp;Counter&#123;n: <span class="hljs-number">0</span>&#125;   <span class="hljs-comment">// pc 是一个指针</span><br><br><span class="hljs-comment">// 虽然 Value 的接收者是 Counter（值类型），但 Go 允许这样写：</span><br>val := pc.Value()   <span class="hljs-comment">// 编译器自动转换成 (*pc).Value()</span><br><br>fmt.Println(val)    <span class="hljs-comment">// 输出 0</span><br></code></pre></td></tr></table></figure>**原理**：`pc.Value()` 被重写为 `(*pc).Value()`，因为 `Value` 需要值接收者。---重要限制：接口赋值时不会自动转换**自动转换只在通过具体变量直接调用方法时生效**。当通过接口调用方法时，规则更加严格：- 如果方法是指针接收者，只有**指针类型的值**才能赋值给接口变量（值的变量无法赋值，因为接口需要存储一个指针才能正确调用指针接收者方法，而值的地址可能不能取？等下说明）。举例：<figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-keyword">var</span> g Greeter = Counter&#123;&#125;   <span class="hljs-comment">// 如果 Counter 有指针接收者方法，这行会编译错误</span><br></code></pre></td></tr></table></figure>反过来，如果是值接收者，则值和指针都可以赋值给接口。---为什么 Go 要这样做？1. **方便性**：避免频繁写 `&amp;` 或 `*`，尤其是当你想修改一个值的时候（比如 `c.Incr()` 看起来自然）。2. **安全性**：自动取地址得到的地址是有效的（因为值变量在内存中有地址），不会造成悬空指针。3. **一致性**：无论调用者是指针还是值，方法调用语法统一为 `变量.方法()`。---什么时候需要显式取地址/解引用？- 当你在表达式中需要明确使用地址时（例如，将一个函数赋值给变量，需要确保类型匹配）。- 当接口赋值时，如果方法集不匹配，需要手动取地址：<figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-keyword">var</span> g Greeter = &amp;c   <span class="hljs-comment">// 如果 Greeter 中有指针接收者方法，必须用 &amp;c</span><br></code></pre></td></tr></table></figure>---总结- **值变量 → 指针接收者方法**：Go 自动取地址 `(&amp;v).Method()`- **指针变量 → 值接收者方法**：Go 自动解引用 `(*p).Method()`- 这是编译时完成的语法糖，让代码更加简洁。- 注意：**自动转换只适用于通过具体类型变量直接调用方法**，不适用于接口赋值（那里需要类型严格匹配）。</code></pre><h3 id="方法值与方法表达式"><a href="#方法值与方法表达式" class="headerlink" title="方法值与方法表达式"></a>方法值与方法表达式</h3><p>- </p><pre><code class="hljs">**方法值**：将方法绑定到特定接收者上，形成一个函数值。<figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs go">c := Counter&#123;&#125;<br>incrFunc := c.Incr <span class="hljs-comment">// incrFunc 是一个函数，调用时会对 c 执行 Incr</span><br>incrFunc() <span class="hljs-comment">// c.n 变为 1</span><br><span class="hljs-comment">//Counter 是一个类型，假设它有指针接收者方法 Incr()：func (c *Counter) Incr()</span><br><span class="hljs-comment">//c 是一个 Counter 类型的值变量（或指针，这里为值变量）。</span><br><span class="hljs-comment">//c.Incr 是一个方法值。它把方法 Incr 与具体的接收者 c 绑定在一起，形成一个没有参数的函数值（因为原方法没有除接收者外的其他参数）。</span><br><span class="hljs-comment">//incrFunc 的类型是 func()（无参无返回值）。</span><br><span class="hljs-comment">//调用 incrFunc() 等价于直接调用 c.Incr()，它会修改 c 的 n 字段。</span><br></code></pre></td></tr></table></figure>方法值本质上是一个**闭包**：它捕获了接收者 `c`（对于值接收者会复制一份，对于指针接收者则捕获指针），并记住了要调用的方法。当调用 `incrFunc()` 时，闭包负责将捕获的接收者传递给原始方法。</code></pre><p>- </p><pre><code class="hljs">**方法表达式**：将方法视为一个普通函数，其第一个参数是接收者。<figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-keyword">var</span> incrFunc2 <span class="hljs-function"><span class="hljs-keyword">func</span><span class="hljs-params">(*Counter)</span></span> = (*Counter).Incr<br>incrFunc2(&amp;c) <span class="hljs-comment">// 等价于 c.Incr()</span><br><span class="hljs-comment">//(*Counter).Incr 是一个方法表达式。它将类型 *Counter 上的方法 Incr 提取为一个普通函数。</span><br><span class="hljs-comment">//得到的函数类型为 func(*Counter)，也就是说，这个函数的第一个参数就是接收者（类型 *Counter），后面的参数才是原方法除接收者外的参数（此处 Incr 没有额外参数）。</span><br><span class="hljs-comment">//调用时，需要显式传入接收者：incrFunc2(&amp;c)，这与直接调用 c.Incr() 效果相同。</span><br></code></pre></td></tr></table></figure>方法表达式在需要将方法作为高阶函数参数传递时很有用。与方法值的区别| 特性           | 方法值 `c.Incr`                  | 方法表达式 `(*Counter).Incr`           || :------------- | :------------------------------- | :------------------------------------- || 语法           | `实例.方法名`                    | `类型.方法名`                          || 接收者         | 固定绑定到具体实例 `c`           | 作为第一个参数传入                     || 生成的函数类型 | `func()`（如果原方法无其他参数） | `func(*Counter)`（第一个参数是接收者） || 调用           | `incrFunc()` 无需参数            | `incrFunc2(&amp;c)` 需要传接收者           || 适用场景       | 接收者已确定，需要延迟调用       | 接收者需要在调用时动态决定             |</code></pre><h3 id="init-函数"><a href="#init-函数" class="headerlink" title="init 函数"></a>init 函数</h3><p>调用顺序：<strong>包初始化是按依赖关系进行的</strong>。对于一个包，其所有 <code>init</code>函数会在包内所有变量初始化之后执行。同一个文件内的 <code>init</code>函数按声明顺序执行。<strong>不同文件间的 <code>init</code>执行顺序，按文件名的字典序</strong>（由工具链保证，但业务代码不应依赖此顺序）。</p><p><code>init</code> 是 Go 语言中的一个<strong>特殊函数</strong>，用于在程序启动时自动执行一些初始化操作。它没有参数，没有返回值，不能被调用，只能由 Go 运行时自动执行。</p><p>一、基本特点</p><ol><li><strong>自动执行</strong>：<code>init</code> 函数在 <code>main</code> 函数之前自动调用，无需手动调用。</li><li><strong>无参数无返回值</strong>：<code>func init()</code> 是唯一合法的签名。</li><li><strong>每个包每个源文件可以有多个 <code>init</code></strong>：同一个包（甚至同一个源文件）中可以定义多个 <code>init</code> 函数，它们会按声明顺序执行。</li><li><strong>隐式定义</strong>：<code>init</code> 函数不能被人为调用；如果在代码中显式调用 <code>init()</code>，会编译错误。</li><li><strong>所有依赖包初始化完毕后，才执行当前包的 <code>init</code></strong>：Go 会先递归初始化导入的包，再执行本包的 <code>init</code>，最后才执行 <code>main</code>。</li></ol><p>二、执行顺序</p><p>整个程序的初始化顺序如下：</p><ol><li><strong>导入的包</strong>：递归地初始化所有被导入的包（按依赖图深度优先）。</li><li><strong>包级变量</strong>：当前包中的包级变量按照声明顺序初始化（依赖分析保证）。</li><li><strong><code>init</code> 函数</strong>：按照源文件中出现的顺序执行（如果多个文件，按照文件名字典序执行）。</li><li><strong><code>main</code> 函数</strong>：最后执行。</li></ol><p>示例：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-keyword">package</span> main<br><span class="hljs-keyword">import</span> <span class="hljs-string">&quot;fmt&quot;</span><br><span class="hljs-keyword">var</span> a = <span class="hljs-function"><span class="hljs-keyword">func</span><span class="hljs-params">()</span></span> <span class="hljs-type">int</span> &#123;<br>    fmt.Println(<span class="hljs-string">&quot;initializing a&quot;</span>)<br>    <span class="hljs-keyword">return</span> <span class="hljs-number">1</span><br>&#125;()<br><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">init</span><span class="hljs-params">()</span></span> &#123;<br>    fmt.Println(<span class="hljs-string">&quot;first init&quot;</span>)<br>&#125;<br><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">init</span><span class="hljs-params">()</span></span> &#123;<br>    fmt.Println(<span class="hljs-string">&quot;second init&quot;</span>)<br>&#125;<br><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">main</span><span class="hljs-params">()</span></span> &#123;<br>    fmt.Println(<span class="hljs-string">&quot;main&quot;</span>)<br>&#125;<br></code></pre></td></tr></table></figure><p>输出：</p><figure class="highlight livecodeserver"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs livecodeserver">initializing <span class="hljs-keyword">a</span><br><span class="hljs-keyword">first</span> init<br><span class="hljs-keyword">second</span> init<br>main<br></code></pre></td></tr></table></figure><p>三、用途</p><p><code>init</code> 函数常用于：</p><ul><li><strong>初始化复杂状态</strong>：无法在变量声明中一步完成，如创建数据库连接池、注册驱动。</li><li><strong>注册服务</strong>：例如 <code>database/sql</code> 包的驱动注册：驱动在 <code>init</code> 中调用 <code>sql.Register</code>。</li><li><strong>检查环境或配置</strong>：确保程序运行前满足某些条件（如环境变量存在、配置文件可读）。</li><li><strong>设置包级变量</strong>：基于其他包或逻辑计算初始值。</li></ul><p><strong>注意</strong>：<code>init</code> 应当保持轻量，避免执行过长时间的操作（如网络请求、大量计算），否则会延迟 <code>main</code> 的启动。</p><p>四、多个包的初始化顺序</p><p>Go 会按照导入依赖图<strong>深度优先</strong>初始化包。例如：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-keyword">package</span> main<br><br><span class="hljs-keyword">import</span> _ <span class="hljs-string">&quot;pkgA&quot;</span><br><span class="hljs-keyword">import</span> _ <span class="hljs-string">&quot;pkgB&quot;</span><br></code></pre></td></tr></table></figure><p>如果 <code>pkgA</code> 依赖 <code>pkgC</code>，<code>pkgB</code> 也依赖 <code>pkgC</code>，那么顺序大致为：<br><code>pkgC</code> → <code>pkgA</code> → <code>pkgB</code> → <code>main</code>（每个包先变量后 <code>init</code>）。</p><p>可以使用空白标识符 <code>_</code> 导入包，仅为了执行包的 <code>init</code> 函数（副作用导入）。</p><p>五、常见陷阱</p><ul><li><strong>循环导入</strong>：如果包 A 导入 B，B 又导入 A，并且它们都有 <code>init</code>，会导致循环依赖，编译错误。</li><li><strong>多次初始化</strong>：同一个包的 <code>init</code> 不会重复执行（即使被多个其他包导入）。</li><li><strong>调试困难</strong>：<code>init</code> 中的 <code>panic</code> 会导致程序启动失败，错误信息可能较难定位。建议在 <code>init</code> 中避免复杂逻辑，或在内部使用 <code>defer recover</code>（不推荐）。</li></ul><p>六、替代方案</p><p>对于简单的初始化，优先使用<strong>包级变量初始化表达式</strong>，例如：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-keyword">var</span> db = connectDB() <span class="hljs-comment">// 在 init 之前执行</span><br></code></pre></td></tr></table></figure><p>如果初始化逻辑较复杂，可以编写一个普通的 <code>initDB</code> 函数，并在 <code>main</code> 中显式调用（这样更容易控制顺序和错误处理）。</p><p>总结</p><ul><li><code>init</code> 是 Go 中自动执行的初始化函数，在 <code>main</code> 之前运行。</li><li>每个包可以定义多个 <code>init</code>，按声明顺序执行。</li><li>用于一些无法在变量声明中完成的、且必须在程序早期完成的任务（如注册、单次设置）。</li><li>应保持 <code>init</code> 简单、快速、无副作用或副作用可控。</li></ul><hr><h2 id="7-复合数据类型"><a href="#7-复合数据类型" class="headerlink" title="7. 复合数据类型"></a>7. 复合数据类型</h2><h3 id="数组"><a href="#数组" class="headerlink" title="数组"></a>数组</h3><p>数组是 Go 语言中<strong>固定长度</strong>的、<strong>相同类型元素</strong>的序列。它是<strong>值类型</strong>（赋值或传参会复制整个数组），长度在编译时就确定，不可改变。</p><hr><p>一、声明与初始化</p><ol><li>基本声明</li></ol><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-keyword">var</span> arr [<span class="hljs-number">5</span>]<span class="hljs-type">int</span>               <span class="hljs-comment">// 长度为5，元素自动初始化为零值0</span><br><span class="hljs-keyword">var</span> words [<span class="hljs-number">3</span>]<span class="hljs-type">string</span>          <span class="hljs-comment">// 长度为3，每个元素为 &quot;&quot;</span><br></code></pre></td></tr></table></figure><ol start="2"><li>字面量初始化</li></ol><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs go">a := [<span class="hljs-number">3</span>]<span class="hljs-type">int</span>&#123;<span class="hljs-number">1</span>, <span class="hljs-number">2</span>, <span class="hljs-number">3</span>&#125;        <span class="hljs-comment">// 指定所有元素</span><br>b := [<span class="hljs-number">5</span>]<span class="hljs-type">int</span>&#123;<span class="hljs-number">1</span>, <span class="hljs-number">2</span>&#125;           <span class="hljs-comment">// 前两个为1,2，其余为0</span><br>c := [<span class="hljs-number">4</span>]<span class="hljs-type">int</span>&#123;<span class="hljs-number">2</span>: <span class="hljs-number">100</span>, <span class="hljs-number">3</span>: <span class="hljs-number">200</span>&#125; <span class="hljs-comment">// 指定索引2和3的值，其余为零值</span><br>d := [...]<span class="hljs-type">int</span>&#123;<span class="hljs-number">1</span>, <span class="hljs-number">2</span>, <span class="hljs-number">3</span>&#125;      <span class="hljs-comment">// 使用 ... 让编译器推导长度（长度为3）</span><br></code></pre></td></tr></table></figure><ol start="3"><li>多维数组</li></ol><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-keyword">var</span> matrix [<span class="hljs-number">2</span>][<span class="hljs-number">3</span>]<span class="hljs-type">int</span> = [<span class="hljs-number">2</span>][<span class="hljs-number">3</span>]<span class="hljs-type">int</span>&#123;&#123;<span class="hljs-number">1</span>,<span class="hljs-number">2</span>,<span class="hljs-number">3</span>&#125;, &#123;<span class="hljs-number">4</span>,<span class="hljs-number">5</span>,<span class="hljs-number">6</span>&#125;&#125;<br></code></pre></td></tr></table></figure><hr><p>二、数组的特性</p><ol><li><strong>长度是类型的一部分</strong></li></ol><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs go">[<span class="hljs-number">3</span>]<span class="hljs-type">int</span> 和 [<span class="hljs-number">4</span>]<span class="hljs-type">int</span> 是**不同的类型**，不能直接赋值或比较。<br><span class="hljs-string">``</span><span class="hljs-string">`go</span><br><span class="hljs-string">var a [3]int</span><br><span class="hljs-string">var b [4]int</span><br><span class="hljs-string">// a = b  // 编译错误：类型不匹配</span><br></code></pre></td></tr></table></figure><ol start="2"><li><strong>值类型（重要）</strong></li></ol><ul><li>数组赋值或作为函数参数时，会<strong>完整复制整个数组</strong>（深拷贝）。</li><li>修改副本不会影响原数组。</li></ul><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs go">arr1 := [<span class="hljs-number">3</span>]<span class="hljs-type">int</span>&#123;<span class="hljs-number">1</span>,<span class="hljs-number">2</span>,<span class="hljs-number">3</span>&#125;<br>arr2 := arr1   <span class="hljs-comment">// 复制所有元素</span><br>arr2[<span class="hljs-number">0</span>] = <span class="hljs-number">100</span><br>fmt.Println(arr1[<span class="hljs-number">0</span>]) <span class="hljs-comment">// 1 (原数组未改变)</span><br></code></pre></td></tr></table></figure><ol start="3"><li><strong>可比较性</strong></li></ol><ul><li>两个数组<strong>类型相同</strong>（长度和元素类型相同）时，可以使用 <code>==</code> 或 <code>!=</code> 比较，比较的是每个对应元素是否相等。</li><li>注意：如果元素类型不可比较（如包含 slice、map 等），数组本身也不可比较。</li></ul><hr><p>三、常见操作</p><ol><li>访问与修改</li></ol><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs go">arr := [<span class="hljs-number">5</span>]<span class="hljs-type">int</span>&#123;<span class="hljs-number">10</span>,<span class="hljs-number">20</span>,<span class="hljs-number">30</span>,<span class="hljs-number">40</span>,<span class="hljs-number">50</span>&#125;<br>arr[<span class="hljs-number">2</span>] = <span class="hljs-number">999</span>         <span class="hljs-comment">// 修改第三个元素</span><br>fmt.Println(arr[<span class="hljs-number">2</span>])  <span class="hljs-comment">// 999</span><br></code></pre></td></tr></table></figure><ol start="2"><li>遍历</li></ol><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-comment">// 方式1：通过索引</span><br><span class="hljs-keyword">for</span> i := <span class="hljs-number">0</span>; i &lt; <span class="hljs-built_in">len</span>(arr); i++ &#123;<br>    fmt.Println(arr[i])<br>&#125;<br><br><span class="hljs-comment">// 方式2：range</span><br><span class="hljs-keyword">for</span> index, value := <span class="hljs-keyword">range</span> arr &#123;<br>    fmt.Printf(<span class="hljs-string">&quot;%d: %d\n&quot;</span>, index, value)<br>&#125;<br></code></pre></td></tr></table></figure><h4 id="range"><a href="#range" class="headerlink" title="range"></a>range</h4><p><code>range</code> 是 Go 中用于<strong>遍历各种集合类型</strong>的关键字，只能出现在 <code>for</code> 循环中。它每次迭代返回一或两个值，具体取决于被遍历的类型。</p><hr><p>基本语法</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-keyword">for</span> 索引/键, 值 := <span class="hljs-keyword">range</span> 集合 &#123;<br>    <span class="hljs-comment">// 循环体</span><br>&#125;<br></code></pre></td></tr></table></figure><p>可以省略不需要的值：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-keyword">for</span> _, v := <span class="hljs-keyword">range</span> nums &#123; ... &#125;   <span class="hljs-comment">// 只要值</span><br><span class="hljs-keyword">for</span> i := <span class="hljs-keyword">range</span> nums &#123; ... &#125;      <span class="hljs-comment">// 只要索引/键</span><br></code></pre></td></tr></table></figure><hr><p>对不同类型的行为</p><table><thead><tr><th>类型</th><th>返回的第一个值</th><th>返回的第二个值</th><th>说明</th></tr></thead><tbody><tr><td><strong>数组 &#x2F; 切片</strong></td><td>索引 (int)</td><td>该索引处元素的<strong>副本</strong></td><td>遍历长度固定</td></tr><tr><td><strong>字符串</strong></td><td>字节索引 (int)</td><td>该索引的 <strong>Unicode 码点 (rune)</strong></td><td>按 UTF-8 解码，索引可能不连续</td></tr><tr><td><strong>map</strong></td><td>键</td><td>值</td><td><strong>遍历顺序随机</strong>，同一 map 每次结果可能不同</td></tr><tr><td><strong>通道</strong></td><td>接收到的值</td><td>无</td><td>循环直到通道关闭，关闭后退出</td></tr></tbody></table><hr><ol><li>数组 &#x2F; 切片</li></ol><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs go">arr := [<span class="hljs-number">3</span>]<span class="hljs-type">int</span>&#123;<span class="hljs-number">10</span>, <span class="hljs-number">20</span>, <span class="hljs-number">30</span>&#125;<br><span class="hljs-keyword">for</span> i, v := <span class="hljs-keyword">range</span> arr &#123;<br>    fmt.Printf(<span class="hljs-string">&quot;arr[%d] = %d\n&quot;</span>, i, v)<br>&#125;<br></code></pre></td></tr></table></figure><ul><li><code>v</code> 是元素的一个拷贝，修改 <code>v</code> 不影响原数组&#x2F;切片。</li><li>若想修改原切片元素，直接使用原数组索引：<code>arr[i] = newValue</code>。</li></ul><hr><ol start="2"><li>字符串</li></ol><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs go">s := <span class="hljs-string">&quot;你好&quot;</span><br><span class="hljs-keyword">for</span> i, r := <span class="hljs-keyword">range</span> s &#123;<br>    fmt.Printf(<span class="hljs-string">&quot;位置 %d: %c\n&quot;</span>, i, r)<br>&#125;<br></code></pre></td></tr></table></figure><ul><li><code>i</code> 是该 <code>rune</code> 的起始字节索引（不是字符序号）。</li><li><code>r</code> 是 <code>rune</code> 类型（即 <code>int32</code>），表示一个 Unicode 码点。</li></ul><hr><ol start="3"><li>Map</li></ol><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs go">m := <span class="hljs-keyword">map</span>[<span class="hljs-type">string</span>]<span class="hljs-type">int</span>&#123;<span class="hljs-string">&quot;a&quot;</span>: <span class="hljs-number">1</span>, <span class="hljs-string">&quot;b&quot;</span>: <span class="hljs-number">2</span>&#125;<br><span class="hljs-keyword">for</span> key, value := <span class="hljs-keyword">range</span> m &#123;<br>    fmt.Println(key, value)<br>&#125;<br></code></pre></td></tr></table></figure><ul><li><strong>顺序随机</strong>，不可依赖；若需顺序，先提取键并排序。</li><li>遍历期间如果安全删除或添加元素，行为是确定性的但需谨慎。</li></ul><hr><ol start="4"><li>通道</li></ol><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs go">ch := <span class="hljs-built_in">make</span>(<span class="hljs-keyword">chan</span> <span class="hljs-type">int</span>, <span class="hljs-number">3</span>)<br><span class="hljs-comment">// 写入后 close(ch)</span><br><span class="hljs-keyword">for</span> v := <span class="hljs-keyword">range</span> ch &#123;<br>    fmt.Println(v)<br>&#125;<br></code></pre></td></tr></table></figure><ul><li>只返回一个值（从通道接收到的数据）。</li><li>会一直阻塞读取，直到通道关闭后循环自动退出。</li></ul><hr><p>Go 1.22 的重要变更：循环变量</p><p>在 <strong>Go 1.21 及以前</strong>，<code>range</code> 中的变量（如 <code>i</code>, <code>v</code>）是整个循环中<strong>共享同一个变量</strong>，每次迭代只是修改它的值。这会在使用 goroutine 或闭包时引发捕获问题。</p><p><strong>Go 1.22 起</strong>，<code>for range</code> 的每次迭代都会<strong>创建一组新的变量</strong>，每个闭包&#x2F;goroutine 获取的是独立副本。</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-comment">// Go 1.22+</span><br><span class="hljs-keyword">for</span> _, v := <span class="hljs-keyword">range</span> items &#123;<br>    <span class="hljs-keyword">go</span> <span class="hljs-function"><span class="hljs-keyword">func</span><span class="hljs-params">()</span></span> &#123;<br>        fmt.Println(v) <span class="hljs-comment">// 每个 goroutine 打印自己的 v，不会重复最后一个</span><br>    &#125;()<br>&#125;<br></code></pre></td></tr></table></figure><p>传统三段式 <code>for i:=0; ...</code> 默认仍为旧行为，可通过实验性特性 <code>GOEXPERIMENT=loopvar</code> 提前启用新行为（计划在 Go 1.23 默认生效）。</p><hr><p>总结</p><ul><li><code>range</code> 让遍历集合变得简洁、安全。</li><li>不同类型返回的迭代变量含义不同，特别注意字符串返回的是 <code>rune</code>，map 顺序随机，通道会持续读取直到关闭。</li><li>从 Go 1.22 开始，<code>for range</code> 的迭代变量拥有每次迭代独立的作用域，彻底解决了恼人的闭包&#x2F;goroutine 捕获陷阱。</li></ul><ol start="3"><li>获取长度</li></ol><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-built_in">len</span>(arr)   <span class="hljs-comment">// 常量（编译时确定）</span><br></code></pre></td></tr></table></figure><ol start="4"><li>传递数组（注意性能）</li></ol><p>由于数组是值传递，大数组作为参数时开销较大。通常做法：</p><ul><li>使用<strong>数组指针</strong>：<code>func process(arr *[1024]int)</code></li><li>或者直接使用<strong>切片</strong>（更推荐）。</li></ul><p>在 Go 中，<strong>数组是值类型</strong>。当把一个数组作为参数传递给函数时，Go 会在调用时<strong>复制整个数组</strong>，而不是传递它的引用。这意味着：</p><ul><li>函数内修改数组元素<strong>不影响</strong>原数组（因为是副本）。</li><li>如果数组很大（例如 <code>[1024]int</code>，即 8 KB 或更大），每次调用的复制开销会很高。</li></ul><p>因此，在实际开发中很少直接传递大数组。下面介绍两种惯用替代方案。</p><hr><ol><li>使用数组指针</li></ol><p>将数组的地址传递给函数，这样函数操作的是原始数组，不需要整体复制。</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-keyword">package</span> main<br><br><span class="hljs-keyword">import</span> <span class="hljs-string">&quot;fmt&quot;</span><br><br><span class="hljs-comment">// 接收数组指针，大小固定为 1024</span><br><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">process</span><span class="hljs-params">(arr *[1024]<span class="hljs-type">int</span>)</span></span> &#123;<br>    <span class="hljs-comment">// 通过指针修改原数组</span><br>    arr[<span class="hljs-number">0</span>] = <span class="hljs-number">42</span><br>    fmt.Println(<span class="hljs-string">&quot;处理 arr[0] =&quot;</span>, arr[<span class="hljs-number">0</span>])<br>&#125;<br><br><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">main</span><span class="hljs-params">()</span></span> &#123;<br>    <span class="hljs-keyword">var</span> data [<span class="hljs-number">1024</span>]<span class="hljs-type">int</span><br>    process(&amp;data)          <span class="hljs-comment">// 只传递了 8 字节的指针</span><br>    fmt.Println(<span class="hljs-string">&quot;data[0] =&quot;</span>, data[<span class="hljs-number">0</span>]) <span class="hljs-comment">// 42，原数组被修改</span><br>&#125;<br></code></pre></td></tr></table></figure><p><strong>特点</strong>：</p><ul><li>避免了数组复制，性能好。</li><li>但是指针固定了数组长度：<code>*[1024]int</code> 不能接受不同长度的数组。</li><li>语法上需要显式取地址 <code>&amp;data</code>，且访问元素时指针会自动解引用，写法和普通数组一样。</li></ul><hr><ol start="2"><li>使用切片（更推荐）</li></ol><p>切片在 Go 中本质上是对底层数组的<strong>引用视图</strong>（包含指针、长度、容量），传递给函数时即使值传递，复制成本也非常小（相当于 3 个机器字），并且允许动态长度。</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">process</span><span class="hljs-params">(s []<span class="hljs-type">int</span>)</span></span> &#123;<br>    s[<span class="hljs-number">0</span>] = <span class="hljs-number">99</span><br>    <span class="hljs-comment">// 可以安全地通过 append 扩展，但需注意是否要返回新切片</span><br>&#125;<br><br><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">main</span><span class="hljs-params">()</span></span> &#123;<br>    data := <span class="hljs-built_in">make</span>([]<span class="hljs-type">int</span>, <span class="hljs-number">1024</span>)   <span class="hljs-comment">// 或 data := []int&#123;...&#125;</span><br>    process(data)               <span class="hljs-comment">// 只复制了切片描述符，非常轻量</span><br>    fmt.Println(data[<span class="hljs-number">0</span>])        <span class="hljs-comment">// 99，原底层数组被修改</span><br>&#125;<br></code></pre></td></tr></table></figure><p><strong>为什么切片更好？</strong></p><ul><li><strong>轻量传递</strong>：只复制 24 字节（64 位系统上），与数组长度无关。</li><li><strong>灵活性高</strong>：切片本身支持动态大小，一个函数可以处理任意长度的切片。</li><li><strong>共享底层数组</strong>：在函数中修改切片元素，原切片可见（因为它们底层是同一个数组）。</li><li>无需显式取地址，语法更自然。</li></ul><hr><ol start="3"><li>性能对比示例</li></ol><p>下面演示直接传大数组 vs. 传切片在性能上的差异（概念示意）：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-keyword">var</span> huge [<span class="hljs-number">1_000_000</span>]<span class="hljs-type">int</span><br><br><span class="hljs-comment">// 值传递：每次调用复制 8 MB，非常慢</span><br><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">useArray</span><span class="hljs-params">(arr [1_000_000]<span class="hljs-type">int</span>)</span></span> &#123;<br>    _ = arr[<span class="hljs-number">0</span>]<br>&#125;<br><br><span class="hljs-comment">// 传切片：只复制切片描述符，约 24 字节，极快</span><br><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">useSlice</span><span class="hljs-params">(arr []<span class="hljs-type">int</span>)</span></span> &#123;<br>    _ = arr[<span class="hljs-number">0</span>]<br>&#125;<br><br><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">BenchmarkArray</span><span class="hljs-params">(b *testing.B)</span></span> &#123;<br>    <span class="hljs-keyword">for</span> i := <span class="hljs-number">0</span>; i &lt; b.N; i++ &#123;<br>        useArray(huge) <span class="hljs-comment">// 大量内存复制</span><br>    &#125;<br>&#125;<br><br><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">BenchmarkSlice</span><span class="hljs-params">(b *testing.B)</span></span> &#123;<br>    <span class="hljs-keyword">for</span> i := <span class="hljs-number">0</span>; i &lt; b.N; i++ &#123;<br>        useSlice(huge[:]) <span class="hljs-comment">// 几乎没有复制开销</span><br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>基准测试结果会显示 <code>useSlice</code> 远快于 <code>useArray</code>，且没有因长数组导致的栈内存高占用。</p><hr><p>总结</p><ul><li><strong>数组是值类型</strong>，传参会复制整块内存，大数组性能差。</li><li><strong>解决方案一：数组指针</strong> <code>*[N]T</code> — 避免复制，但长度固定，语法稍繁琐。</li><li><strong>解决方案二：切片</strong> <code>[]T</code> — 更推荐，轻量且灵活，是 Go 中处理序列的首选。</li><li>在实际代码中，应当<strong>始终优先使用切片</strong>，仅在极少数需要在栈上分配固定大小数组时直接使用数组（如存储密钥，避免逃逸到堆等特定场景）。</li></ul><hr><p>四、数组与切片的对比</p><table><thead><tr><th>特性</th><th>数组</th><th>切片</th></tr></thead><tbody><tr><td>长度</td><td>固定，类型的一部分</td><td>可变，动态</td></tr><tr><td>类型表示</td><td><code>[n]T</code></td><td><code>[]T</code></td></tr><tr><td>内存模型</td><td>值类型（整个数组连续存储）</td><td>引用类型（指向底层数组）</td></tr><tr><td>赋值&#x2F;传参</td><td>复制整个数组</td><td>复制切片描述符（指针+长度+容量），效率高</td></tr><tr><td>用途</td><td>底层存储、固定大小序列</td><td>日常绝大部分场景</td></tr></tbody></table><hr><p>五、使用场景</p><ul><li><strong>底层存储</strong>：作为切片的底层数组。</li><li><strong>固定大小的序列</strong>：例如矩阵的维度、星期几（7个元素）、IP地址（4个字节）。</li><li><strong>性能要求极致的场景</strong>：小数组且不希望有 slice 的头开销。</li></ul><p><strong>提示</strong>：在 Go 中，<strong>切片比数组更常用</strong>。只有在明确知道长度固定且不需要动态增减时，才使用数组。</p><hr><p>六、常见误区</p><ol><li><strong>数组长度非常大的值传递</strong>：应使用指针或切片。</li><li><strong>试图修改长度</strong>：数组长度不可变，必须使用切片。</li><li><strong>将数组作为接口传递时的复制</strong>：注意如果数组很大，接口也会包含整个数组的副本。</li></ol><hr><p>总结</p><ul><li>数组是<strong>长度固定、值类型</strong>的序列。</li><li>长度 <code>[n]T</code> 是类型的一部分，<code>[3]int</code> 与 <code>[4]int</code> 不同类型。</li><li>赋值&#x2F;传参会复制整个数组。</li><li>日常开发优先使用<strong>切片</strong>，数组多用于底层或固定大小的场合。</li></ul><h3 id="切片（Slice）"><a href="#切片（Slice）" class="headerlink" title="切片（Slice）"></a>切片（Slice）</h3><p>切片是对数组连续片段的轻量级引用。它是 Go 中动态集合的核心类型。</p><p>切片（slice）是 Go 中<strong>最重要、最常用的数据结构之一</strong>，可以理解为<strong>动态数组</strong>。它在数组之上提供了灵活、高效的操作接口，是处理序列数据的首选。</p><hr><ol><li>什么是切片</li></ol><p>切片是对<strong>底层数组的一个连续片段的引用</strong>（视图）。它本身不存储数据，而是描述底层数组的一部分（或全部）。因此：</p><ul><li>切片是<strong>引用类型</strong>，传递切片本质是复制一个描述符（极低成本），但共享底层数组。</li><li>可以动态增长（通过 <code>append</code>），必要时自动扩容并更换底层数组。</li></ul><hr><ol start="2"><li>底层结构</li></ol><p>在运行时，一个切片由 3 个字段组成：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-keyword">type</span> slice <span class="hljs-keyword">struct</span> &#123;<br>    ptr *T    <span class="hljs-comment">// 指向底层数组起始元素的指针</span><br>    <span class="hljs-built_in">len</span> <span class="hljs-type">int</span>   <span class="hljs-comment">// 切片当前元素个数（长度）</span><br>    <span class="hljs-built_in">cap</span> <span class="hljs-type">int</span>   <span class="hljs-comment">// 从 ptr 开始到底层数组末尾的元素个数（容量）</span><br>&#125;<br></code></pre></td></tr></table></figure><ul><li><code>len</code>：切片内可访问的元素数量。</li><li><code>cap</code>：切片底层数组可供增长的空间，即 <code>ptr</code> 开始到数组末尾的元素数。</li></ul><hr><ol start="3"><li>创建切片的方式</li></ol><p>① 字面量</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs go">s := []<span class="hljs-type">int</span>&#123;<span class="hljs-number">1</span>, <span class="hljs-number">2</span>, <span class="hljs-number">3</span>&#125;  <span class="hljs-comment">// 类型为 []int，自动创建底层数组</span><br></code></pre></td></tr></table></figure><p>② 使用 <code>make</code></p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs go">s := <span class="hljs-built_in">make</span>([]<span class="hljs-type">int</span>, <span class="hljs-number">5</span>, <span class="hljs-number">10</span>) <span class="hljs-comment">// 长度5，容量10，元素为0值</span><br></code></pre></td></tr></table></figure><ul><li><code>make([]T, length, capacity)</code>，capacity 可省略（此时 cap &#x3D;&#x3D; len）。</li></ul><p>③ 从数组或其他切片切割</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><code class="hljs go">arr := [<span class="hljs-number">5</span>]<span class="hljs-type">int</span>&#123;<span class="hljs-number">1</span>,<span class="hljs-number">2</span>,<span class="hljs-number">3</span>,<span class="hljs-number">4</span>,<span class="hljs-number">5</span>&#125;<br>s := arr[<span class="hljs-number">1</span>:<span class="hljs-number">4</span>]   <span class="hljs-comment">// [2,3,4]，len=3，cap=4（从 arr[1] 到数组末尾  </span><br><span class="hljs-comment">//语法 [low:high] 表示从索引 low 开始（包含），到 high-1 结束（不包含） 长度 len(s) = high - low = 4 - 1 = 3。容量 cap(s) 计算方式：从 low 开始到原数组末尾的元素总个数。</span><br>s2 := s[:<span class="hljs-number">2</span>]     <span class="hljs-comment">// [2,3]，共享同一底层数组</span><br><span class="hljs-comment">//在切片 s 上再进行切片，语法 s[:2] 省略 low（默认为 0），high=2 长度 len(s2) = 2 - 0 = 2。容量 cap(s2) 的起点继承自 s，即底层数组的索引 1，然后可以延伸到数组末尾。原 s 的容量为 4，cap(s2) = cap(s) - low偏移 = 4 - 0 = 4（因为 s2 仍在 s 的起始位置）</span><br><br><span class="hljs-comment">//s 和 s2 的 ptr 都指向 arr[1]。</span><br><span class="hljs-comment">//对 s2[0] 的修改会直接影响 s[0] 和 arr[1]，因为它们指向同一块内存。</span><br><br><span class="hljs-comment">//因为 cap(s2) = 4，我们可以在不引发扩容的情况下扩展 s2：</span><br>s2 = s2[:<span class="hljs-number">4</span>]  <span class="hljs-comment">// 扩展到容量允许的最大长度</span><br>fmt.Println(s2) <span class="hljs-comment">// [2 3 4 5]，又看见了原来 s 尾部的 4 和 5</span><br><span class="hljs-comment">//切片只是底层数组上的一个窗口，通过调整窗口大小可以向后“看”到容量范围内的元素。</span><br></code></pre></td></tr></table></figure><ul><li>切片操作 <code>[low:high]</code> 得到新切片，<code>low</code> 默认 0，<code>high</code> 默认 len。</li><li>也可使用 <code>[low:high:max]</code>（三索引切片）限制容量。</li></ul><hr><ol start="4"><li>长度和容量</li></ol><ul><li><code>len(s)</code> 获取切片当前元素个数。</li><li><code>cap(s)</code> 获取切片从当前起始位置到底层数组末尾的最大长度。</li><li>切片截取时，新切片的容量 &#x3D; 原切片的容量 - 截取的起始偏移。</li></ul><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs go">a := [<span class="hljs-number">5</span>]<span class="hljs-type">int</span>&#123;<span class="hljs-number">1</span>,<span class="hljs-number">2</span>,<span class="hljs-number">3</span>,<span class="hljs-number">4</span>,<span class="hljs-number">5</span>&#125;<br>s := a[<span class="hljs-number">1</span>:<span class="hljs-number">3</span>]      <span class="hljs-comment">// [2,3]，len=2，cap=4（从索引1到数组末尾）</span><br>fmt.Println(<span class="hljs-built_in">len</span>(s), <span class="hljs-built_in">cap</span>(s)) <span class="hljs-comment">// 2 4</span><br></code></pre></td></tr></table></figure><hr><ol start="5"><li><code>append</code> 和扩容</li></ol><p>使用内建函数 <code>append</code> 向切片追加元素：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs go">s := []<span class="hljs-type">int</span>&#123;<span class="hljs-number">1</span>,<span class="hljs-number">2</span>&#125;<br>s = <span class="hljs-built_in">append</span>(s, <span class="hljs-number">3</span>, <span class="hljs-number">4</span>)   <span class="hljs-comment">// s 变成 [1,2,3,4]</span><br></code></pre></td></tr></table></figure><ul><li>如果底层数组容量足够，<code>append</code> 会在原数组上追加并更新长度。</li><li>如果容量不足，会自动分配一个更大的底层数组，将原元素<strong>复制</strong>过去，再追加。此时<strong>新切片可能指向全新的底层数组</strong>。</li></ul><p>由于扩容可能更换底层数组，<strong>若多个切片共享同一底层数组，其中一个 append 可能使其脱离共享</strong>，需要小心。</p><p><code>append</code> 在切片容量不足时，会<strong>分配一个全新的底层数组</strong>，将原元素拷贝过去，再追加新元素。这个过程称为扩容（grow）。</p><hr><ol><li>扩容触发条件</li></ol><ul><li>当 <code>len(s) + 新元素个数 &gt; cap(s)</code> 时，<code>append</code> 必须扩容。</li><li>扩容后返回的切片，指向新数组，与原切片不再共享底层内存。</li></ul><hr><ol start="2"><li>新容量的确定规则（Go 1.17 及之前）</li></ol><p>经典规则分两段：</p><ol><li><p>如果<strong>新长度（所需容量，即 old.len + 元素个数）</strong> ≤ <strong>2 × old.cap</strong>：</p><ul><li>旧容量 &lt; 1024 → 新容量 &#x3D; <strong>2 × old.cap</strong></li><li>旧容量 ≥ 1024 → 新容量 &#x3D; <strong>old.cap + old.cap&#x2F;4</strong>（即增长 25%）</li></ul></li><li><p>如果新长度 &gt; 2 × old.cap，则直接使用<strong>新长度</strong>作为新容量（不会无谓地只翻倍再溢出）。</p></li></ol><p>示例：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs go">s := <span class="hljs-built_in">make</span>([]<span class="hljs-type">int</span>, <span class="hljs-number">0</span>, <span class="hljs-number">1</span>)<br><span class="hljs-comment">// 旧 cap=1 &lt; 1024，新长度=1 ≤ 2*1=2，新cap = 2</span><br>s = <span class="hljs-built_in">append</span>(s, <span class="hljs-number">1</span>) <span class="hljs-comment">// cap=2</span><br><br><span class="hljs-comment">// 继续追加3个元素，所需len=4 &gt; 2*2=4? 正好相等，新cap = 2*2=4</span><br>s = <span class="hljs-built_in">append</span>(s, <span class="hljs-number">2</span>,<span class="hljs-number">3</span>,<span class="hljs-number">4</span>) <span class="hljs-comment">// cap=4</span><br><br><span class="hljs-comment">// 再追加5个元素，所需len=9 &gt; 2*4=8，新cap直接 = 9（不会用翻倍规则）</span><br>s = <span class="hljs-built_in">append</span>(s, <span class="hljs-number">5</span>,<span class="hljs-number">6</span>,<span class="hljs-number">7</span>,<span class="hljs-number">8</span>,<span class="hljs-number">9</span>) <span class="hljs-comment">// cap=9</span><br></code></pre></td></tr></table></figure><hr><ol start="3"><li>Go 1.18 之后的平滑扩容规则</li></ol><p>Go 1.18 对扩容策略做了<strong>平滑化</strong>处理，避免跨过 1024 时增长率的突兀变化，让小切片和大切片之间的过渡更自然。</p><p>核心逻辑类似 <code>runtime.growslice</code> 中的实现（简化描述）：</p><ul><li>如果所需容量 <code>newcap</code>（即 <code>old.cap + 增长数</code>，也可能直接取 <code>old.len + 数量</code>）大于 <code>2 * old.cap</code>，则直接使用 <code>newcap</code>。</li><li>否则（即 <code>newcap ≤ 2 * old.cap</code>）：<ul><li>如果 <code>old.cap &lt; 256</code>：新容量 &#x3D; <strong>2 × old.cap</strong></li><li>如果 <code>old.cap ≥ 256</code>：新容量 &#x3D; <code>old.cap + (old.cap + 3*256) / 4</code><br>  这个公式使得增长率从 256 时的约 2 倍平滑下降到约 1.25 倍（当 cap 极大时）。</li></ul></li></ul><blockquote><p>精确实现还要考虑内存对齐，导致最终分配的容量会略微调整（通常向上取整到合适的大小类别），但大趋势如此。</p></blockquote><hr><ol start="4"><li>特殊情况：<code>nil</code> 切片和空切片</li></ol><ul><li><code>var s []int</code> 为 nil，<code>len=0, cap=0</code>。<br>  <code>s = append(s, 1)</code> → 新切片 <code>len=1</code>，<strong>容量由 runtime 给出</strong>，通常最小为 1（可能更大，如 1 或 8，取决于类型大小）。</li><li><code>s := make([]int, 0)</code> 也类似，容量可能为 0，append 同样触发扩容。</li></ul><hr><ol start="5"><li>内存对齐对最终容量的影响</li></ol><p>Go 的 runtime 会考虑<strong>类型的内存大小</strong>，将计算出的理想 <code>newcap</code> 调整为实际分配的大小类别（class），因此最终 <code>cap</code> 可能略大于理论值。例如申请 <code>[]byte</code> 时，可能从 400 调整到 416 或 480 这类值。用户代码不能依赖精确值，只需知道<strong>容量至少满足所需长度，且增长趋势符合上述规则</strong>。</p><hr><ol start="6"><li>一次追加多个元素</li></ol><p><code>append(s, values...)</code> 追加多个元素时，计算所需新长度 &#x3D; <code>len(s) + len(values)</code>，扩容规则基于这个所需长度进行判断。</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs go">s := <span class="hljs-built_in">make</span>([]<span class="hljs-type">int</span>, <span class="hljs-number">0</span>, <span class="hljs-number">2</span>)<br>s = <span class="hljs-built_in">append</span>(s, []<span class="hljs-type">int</span>&#123;<span class="hljs-number">1</span>,<span class="hljs-number">2</span>,<span class="hljs-number">3</span>,<span class="hljs-number">4</span>&#125;...) <span class="hljs-comment">// 所需len=4，old.cap=2，直接取 newcap=4</span><br></code></pre></td></tr></table></figure><hr><ol start="7"><li>总结要点</li></ol><ul><li>触发条件：容量不够。</li><li>规则本质：<strong>保证新切片可以容纳全部元素，并且适度预留空间以减少后续扩容</strong>。</li><li>现代 Go（1.18+）对小切片（&lt;256）翻倍，之后增长率平滑下降，最终趋近 25%。</li><li>实际容量受内存分配器影响，可能略高。</li><li>利用好 <code>make</code> 的容量参数，可以有效减少扩容次数，提升性能。</li></ul><hr><ol start="6"><li>切片的“引用”语义</li></ol><p>切片作为参数传递时，复制的是切片描述符（24 字节），函数内部可以看到原切片的元素，但<strong>追加元素并超过容量后可能指向新数组</strong>，这需要注意是否影响外部。</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">modify</span><span class="hljs-params">(s []<span class="hljs-type">int</span>)</span></span> &#123;<br>    s[<span class="hljs-number">0</span>] = <span class="hljs-number">999</span>          <span class="hljs-comment">// 影响外部，共享底层数组</span><br>    s = <span class="hljs-built_in">append</span>(s, <span class="hljs-number">100</span>)  <span class="hljs-comment">// 可能新分配数组，外部切片不变（len仍为旧值）</span><br>&#125;<br></code></pre></td></tr></table></figure><p>如果希望函数内的 append 反映到外部，通常<strong>返回新切片</strong>：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">add</span><span class="hljs-params">(s []<span class="hljs-type">int</span>, v <span class="hljs-type">int</span>)</span></span> []<span class="hljs-type">int</span> &#123;<br>    <span class="hljs-keyword">return</span> <span class="hljs-built_in">append</span>(s, v)<br>&#125;<br><span class="hljs-comment">// 调用：s = add(s, 10)</span><br></code></pre></td></tr></table></figure><hr><ol start="7"><li><code>copy</code> 函数</li></ol><p><code>copy(dst, src)</code> 将源切片的元素拷贝到目标切片，返回拷贝的元素个数（两者最小长度）。</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs go">src := []<span class="hljs-type">int</span>&#123;<span class="hljs-number">1</span>,<span class="hljs-number">2</span>,<span class="hljs-number">3</span>&#125;<br>dst := <span class="hljs-built_in">make</span>([]<span class="hljs-type">int</span>, <span class="hljs-number">2</span>)<br>n := <span class="hljs-built_in">copy</span>(dst, src)  <span class="hljs-comment">// n=2，dst=[1,2]</span><br></code></pre></td></tr></table></figure><ul><li>必须事先为 <code>dst</code> 分配足够的长度（<code>copy</code> 不会自动扩容）。</li><li>适合需要独立副本的场景。</li></ul><hr><ol start="8"><li>nil 切片与空切片</li></ol><ul><li><strong>nil 切片</strong>：<code>var s []int</code>，此时 <code>s == nil</code>，<code>len=0, cap=0</code>。  </li><li><strong>空切片</strong>：<code>s := make([]int, 0)</code> 或 <code>s := []int&#123;&#125;</code>，不是 nil，<code>len=0, cap 可能不为 0</code>。<br>  两者都可以调用 <code>append</code>，但 JSON 序列化结果不同（nil 变为 <code>null</code>，空切片变为 <code>[]</code>）。</li></ul><hr><ol start="9"><li>与数组的对比</li></ol><table><thead><tr><th>特性</th><th>数组</th><th>切片</th></tr></thead><tbody><tr><td>长度</td><td>固定，编译时确定</td><td>动态，可增长</td></tr><tr><td>类型</td><td><code>[N]T</code> 含长度，不同长度属不同类型</td><td><code>[]T</code>，与长度无关</td></tr><tr><td>传递</td><td>值传递，整体复制</td><td>引用传递（描述符复制），共享底层数组</td></tr><tr><td>用途</td><td>极少数需要固定大小、值语义的场景</td><td><strong>序列处理的默认选择</strong></td></tr><tr><td>字面量</td><td><code>[3]int&#123;1,2,3&#125;</code></td><td><code>[]int&#123;1,2,3&#125;</code></td></tr></tbody></table><hr><ol start="10"><li>常用模式与注意事项</li></ol><ul><li><p><strong>删除元素</strong>：利用切片的截取和 <code>append</code> 拼接。</p>  <figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-comment">// 删除索引 i 的元素</span><br>s = <span class="hljs-built_in">append</span>(s[:i], s[i+<span class="hljs-number">1</span>:]...) <br><br>s[:i] 获取索引 i 之前的所有元素（不含 i），结果是一个切片。<br>s[i+<span class="hljs-number">1</span>:] 获取索引 i 之后的所有元素，结果也是一个切片。<br><span class="hljs-built_in">append</span>(s[:i], s[i+<span class="hljs-number">1</span>:]...) 将后半部分追加到前半部分的末尾，恰好跳过索引 i 的元素。<br>因为这两个切片共享同一个底层数组，追加会导致元素整体左移（将 i+<span class="hljs-number">1</span> 及之后的元素拷贝到 i 位置开始），相当于删除了 i。<br><br>必须确保 i 在有效范围内（<span class="hljs-number">0</span> ≤ i &lt; <span class="hljs-built_in">len</span>(s)）。<br>该操作会修改底层数组，如果其他切片共享该数组，会受到影响。<br>如果有容量，删除后切片的长度减少 <span class="hljs-number">1</span>，容量不变，末尾可能会残留原值（但通过切片看不到）。<br></code></pre></td></tr></table></figure></li><li><p><strong>插入元素</strong>：</p>  <figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs go">s = <span class="hljs-built_in">append</span>(s[:i], <span class="hljs-built_in">append</span>([]T&#123;x&#125;, s[i:]...)...)<br><br>内部 <span class="hljs-built_in">append</span>：<span class="hljs-built_in">append</span>([]T&#123;x&#125;, s[i:]...)<br>创建一个新切片 []T&#123;x&#125;（仅包含待插入的元素 x）。<br>将原切片从 i 开始的后半部分追加到它的后面，得到 [x, s[i], s[i+<span class="hljs-number">1</span>], ...]。<br>这个中间切片可能发生扩容，底层是一个新数组。<br>外部 <span class="hljs-built_in">append</span>：<span class="hljs-built_in">append</span>(s[:i], 中间切片...)<br>将上一步得到的完整后半段（已含 x）追加到 s[:i]（前半部分）之后，形成最终切片。<br><br>如果原切片容量足够，且插入位置靠后，可能不会发生扩容；但内部 <span class="hljs-built_in">append</span>([]T&#123;x&#125;, s[i:]...) 大部分情况都会分配新数组。<br>该写法比较紧凑，但可能产生临时切片，性能敏感场景可以手动分配并拷贝。<br></code></pre></td></tr></table></figure></li><li><p><strong>遍历</strong>：<code>for i, v := range s</code>，<code>v</code> 是值拷贝。</p><p>  <code>i</code> 是当前元素的索引（从 0 开始）。</p><p>  <code>v</code> 是切片中对应元素的<strong>值拷贝</strong>（复制了一份）。修改 <code>v</code> 不会影响原切片 <code>s[i]</code>。</p><p>  若要修改原切片元素，须使用索引：<code>s[i] = newValue</code>。</p></li><li><p><strong>当切片容量很大但长度很小时，为避免内存浪费，可以复制到新切片释放底层大数组</strong>：</p>  <figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs go">s = <span class="hljs-built_in">append</span>([]<span class="hljs-type">int</span>(<span class="hljs-literal">nil</span>), s...) <span class="hljs-comment">// 创建独立副本</span><br><span class="hljs-comment">// 或 small = append([]int&#123;&#125;, small...)</span><br>[]<span class="hljs-type">int</span>(<span class="hljs-literal">nil</span>) 是一个 <span class="hljs-literal">nil</span> 切片（<span class="hljs-built_in">len</span>=<span class="hljs-number">0</span>, <span class="hljs-built_in">cap</span>=<span class="hljs-number">0</span>）。<br><span class="hljs-built_in">append</span> 发现容量不足，会分配一个全新的底层数组，长度刚好为 <span class="hljs-built_in">len</span>(small)（或略大，对齐），然后将 small 的元素复制进去。<br>返回的新切片拥有独立的小容量数组，原大数组不再被引用，可以被 GC 回收。<br><br>明确拷贝后，新切片与原切片不再共享底层数组，彼此修改不影响对方。<br>该方法也可用于复制任何切片，得到一个不会因原始数组扩容而受影响的安全副本。<br></code></pre></td></tr></table></figure></li></ul><p>切片是 Go 中最核心的概念之一，理解其底层数组共享与扩容机制，是写出高效、无 bug 的 Go 程序的关键。</p><h3 id="映射（Map）"><a href="#映射（Map）" class="headerlink" title="映射（Map）"></a>映射（Map）</h3><p>映射是存储键值对的无序集合。</p><p>- </p><pre><code class="hljs">**键的类型 K 必须满足可比较性**（`==`, `!=`），因此切片、映射、函数不能作为键。结构体或数组如果其元素类型可比较，则可作为键。（**一个结构体或数组类型本身是“可比较的”（因此可作为`map`键）的前提，是其所有字段或所有元素类型都是“可比较的”**。如果其包含不可比较的字段（如切片），则该结构体类型本身也不可比较。）</code></pre><p>- </p><pre><code class="hljs">**零值**：`nil`映射。向 `nil`映射写入会 panic，但读取、删除 (`delete`)、`len`操作是安全的（读返回零值，`delete`无操作，`len`返回 0）。通常使用 `make(map[K]V)`或字面量初始化。</code></pre><p>- </p><pre><code class="hljs">**操作**：<figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs go">m := <span class="hljs-built_in">make</span>(<span class="hljs-keyword">map</span>[<span class="hljs-type">string</span>]<span class="hljs-type">int</span>)<br>m[<span class="hljs-string">&quot;apple&quot;</span>] = <span class="hljs-number">5</span> <span class="hljs-comment">// 写入</span><br>v := m[<span class="hljs-string">&quot;apple&quot;</span>] <span class="hljs-comment">// 读取，如果键不存在，v 为值类型的零值</span><br>v, ok := m[<span class="hljs-string">&quot;banana&quot;</span>] <span class="hljs-comment">// ok 为 bool，指示键是否存在</span><br><span class="hljs-built_in">delete</span>(m, <span class="hljs-string">&quot;apple&quot;</span>) <span class="hljs-comment">// 删除键，即使键不存在也不会报错</span><br></code></pre></td></tr></table></figure></code></pre><p>- </p><pre><code class="hljs">**遍历顺序是随机的**：这是 Go 的刻意设计，防止开发者依赖不稳定的遍历顺序。</code></pre><p>- </p><pre><code class="hljs">**非并发安全**：多个 goroutine 并发读写映射会导致未定义行为（竞态）。解决方法：1. **`sync.Mutex`/`sync.RWMutex`**：最通用和灵活。<figure class="highlight stylus"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs stylus"><span class="hljs-selector-tag">var</span> mu sync<span class="hljs-selector-class">.RWMutex</span><br>m := <span class="hljs-built_in">make</span>(map<span class="hljs-selector-attr">[string]</span>int)<br><span class="hljs-comment">// 写</span><br>mu<span class="hljs-selector-class">.Lock</span>()<br>m<span class="hljs-selector-attr">[<span class="hljs-string">&quot;key&quot;</span>]</span> = <span class="hljs-number">1</span><br>mu<span class="hljs-selector-class">.Unlock</span>()<br><span class="hljs-comment">// 读</span><br>mu<span class="hljs-selector-class">.RLock</span>()<br>v := m<span class="hljs-selector-attr">[<span class="hljs-string">&quot;key&quot;</span>]</span><br>mu<span class="hljs-selector-class">.RUnlock</span>()<br></code></pre></td></tr></table></figure>2. **`sync.Map`**：适用于特定场景：a) 键值对只写一次、读多次；b) 各个 goroutine 操作的键集互不相交。在读写比例悬殊（读远大于写）时性能可能优于 `map+Mutex`，但在通用场景下后者更优。</code></pre><h3 id="结构体"><a href="#结构体" class="headerlink" title="结构体"></a>结构体</h3><p>结构体是字段的集合，字段可以是任意类型。</p><p>- </p><pre><code class="hljs">**匿名字段（嵌入）**：通过嵌入，外部结构体可以获取内部类型（匿名字段）的方法和字段，这提供了一种简单的组合机制，类似于继承。<figure class="highlight elm"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs elm"><span class="hljs-keyword">type</span> <span class="hljs-type">Person</span> struct &#123; <span class="hljs-type">Name</span> string; <span class="hljs-type">Age</span> int &#125;<br><span class="hljs-keyword">type</span> <span class="hljs-type">Employee</span> struct &#123;<br>    <span class="hljs-type">Person</span> // <span class="hljs-type">Employee</span> 包含了 <span class="hljs-type">Person</span> 的所有字段<br>    <span class="hljs-type">ID</span>     string<br>&#125;<br><span class="hljs-title">e</span> := <span class="hljs-type">Employee</span>&#123;<span class="hljs-type">Person</span>&#123;<span class="hljs-string">&quot;Alice&quot;</span>, <span class="hljs-number">30</span>&#125;, <span class="hljs-string">&quot;E123&quot;</span>&#125;<br><span class="hljs-title">fmt</span>.<span class="hljs-type">Println</span>(e.<span class="hljs-type">Name</span>) // 可以直接访问嵌入字段的字段<br>// 如果 <span class="hljs-type">Employee</span> 也有 <span class="hljs-type">Name</span> 字段，则 e.<span class="hljs-type">Name</span> 访问的是 <span class="hljs-type">Employee</span>.<span class="hljs-type">Name</span><br>// 可以通过 e.<span class="hljs-type">Person</span>.<span class="hljs-type">Name</span> 显式访问<br></code></pre></td></tr></table></figure></code></pre><p>- </p><pre><code class="hljs">**标签（Tag）**：字段后的字符串字面量，通过反射 (`reflect`包) 读取。常用于序列化（`json`、`xml`）、数据库 ORM（`gorm`）、数据验证（`validate`）等。<figure class="highlight pgsql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs pgsql"><span class="hljs-keyword">type</span> <span class="hljs-keyword">User</span> struct &#123;<br>    <span class="hljs-type">Name</span> string `<span class="hljs-type">json</span>:&quot;name&quot; <span class="hljs-keyword">validate</span>:&quot;required,min=2&quot;`<br>    Age  <span class="hljs-type">int</span>    `<span class="hljs-type">json</span>:&quot;age,omitempty&quot;` // omitempty 表示为零值时忽略<br>&#125;<br></code></pre></td></tr></table></figure></code></pre><p>- </p><pre><code class="hljs">**空结构体 `struct&#123;&#125;`**：不占用任何内存（大小为零）。常用于：- 通道信号：`chan struct&#123;&#125;`，只关心事件发生，不传递数据。- 实现集合：`map[string]struct&#123;&#125;`，只存储键。- 实现“方法集”：为某个类型附加一组方法，而不增加存储开销。</code></pre><p>- </p><pre><code class="hljs">**结构体比较**：当所有字段都是可比较的时，结构体才是可比较的，可以用于 `==`操作或作为 `map`的键。</code></pre><hr><h2 id="8-指针"><a href="#8-指针" class="headerlink" title="8. 指针"></a>8. 指针</h2><p>- </p><pre><code class="hljs">指针保存了值的内存地址。`*T`是指向 `T`类型的指针。零值为 `nil`。</code></pre><p>- </p><pre><code class="hljs">`&amp;`操作符获取变量的地址。`*`操作符解引用指针，获取其指向的值。</code></pre><p>- </p><pre><code class="hljs">**Go 没有指针算术**（不能进行 `p++`这样的操作）。这是为了内存安全。需要低级内存操作时，使用 `unsafe.Pointer`（需格外小心）。</code></pre><p>- </p><pre><code class="hljs">`new(T)`分配一个 `T`类型的零值，并返回其指针 `*T`。等价于 `&amp;T&#123;&#125;`。</code></pre><p>- </p><pre><code class="hljs">**方法与指针的规则**：- 类型 `T`的方法集包含所有**值接收者**方法。- 类型 `*T`的方法集包含所有**值接收者**方法和**指针接收者**方法。- 因此，指针接收者方法只能通过 `*T`类型调用。但当用值变量调用指针接收者方法时，Go 会自动取地址 (`&amp;c`)，前提是该值**可寻址**。</code></pre><p>- </p><pre><code class="hljs">**逃逸分析**：编译器在编译时决定变量分配在栈上还是堆上。基本原则是：如果变量的生命周期超出了声明它的函数（例如，被返回、被全局变量引用、被闭包捕获），它就会“逃逸”到堆上。可以使用 `go build -gcflags=&quot;-m&quot;`来查看逃逸分析结果。理解逃逸有助于编写更高效的代码（减少堆分配和 GC 压力）。</code></pre><hr><h2 id="9-接口"><a href="#9-接口" class="headerlink" title="9. 接口"></a>9. 接口</h2><p>接口是 Go 语言多态和抽象的核心。它是一种类型，定义了一组方法签名。任何实现了这些方法的类型都隐式地满足了该接口，无需显式声明。</p><h3 id="隐式实现"><a href="#隐式实现" class="headerlink" title="隐式实现"></a>隐式实现</h3><p>这种“鸭子类型”的设计极大地降低了耦合，使得接口定义方和实现方可以独立演化。</p><h3 id="空接口与-any"><a href="#空接口与-any" class="headerlink" title="空接口与 any"></a>空接口与 <code>any</code></h3><p><code>interface&#123;&#125;</code>是空接口，它没有定义任何方法，因此任何类型都实现了空接口。Go 1.18 引入了内置别名 <code>any</code>代表 <code>interface&#123;&#125;</code>，推荐使用 <code>any</code>，因为它更简短清晰。</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-keyword">var</span> i any = <span class="hljs-string">&quot;hello&quot;</span><br>s := i.(<span class="hljs-type">string</span>)       <span class="hljs-comment">// 类型断言。如果 i 不是 string，会 panic</span><br>s, ok := i.(<span class="hljs-type">string</span>)  <span class="hljs-comment">// 安全类型断言，ok 为 false 时 s 为零值</span><br></code></pre></td></tr></table></figure><h3 id="接口值的内部结构"><a href="#接口值的内部结构" class="headerlink" title="接口值的内部结构"></a>接口值的内部结构</h3><p>接口值在内存中由两个部分组成：<strong>动态类型</strong>和<strong>动态值</strong>。</p><p>- </p><pre><code class="hljs">**空接口** (`interface&#123;&#125;`/`any`) 在运行时表示为 `eface`，包含一个指向类型信息的 `_type`指针和一个指向数据的 `data`指针。</code></pre><p>- </p><pre><code class="hljs">**非空接口** (例如 `io.Reader`) 在运行时表示为 `iface`，包含一个指向接口表的 `itab`指针和一个指向数据的 `data`指针。`itab`包含了接口的类型信息和方法表。</code></pre><p><strong><code>nil</code>接口陷阱</strong>：</p><figure class="highlight lua"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs lua">var p *<span class="hljs-built_in">os</span>.File = <span class="hljs-literal">nil</span><br>var r <span class="hljs-built_in">io</span>.Reader = p // r 不是 <span class="hljs-literal">nil</span>! 它的动态类型是 *<span class="hljs-built_in">os</span>.File，动态值是 <span class="hljs-literal">nil</span><br><span class="hljs-keyword">if</span> r != <span class="hljs-literal">nil</span> &#123;<br>    // 这里会执行，因为 r 的动态类型不为 <span class="hljs-literal">nil</span><br>&#125;<br></code></pre></td></tr></table></figure><p>一个接口值等于 <code>nil</code><strong>当且仅当</strong>它的动态类型和动态值都为 <code>nil</code>。将具体的 <code>nil</code>指针赋值给接口后，接口值就不再是 <code>nil</code>。在函数返回错误时，应直接 <code>return nil</code>，而不是 <code>return (*MyError)(nil)</code>。</p><h3 id="类型选择（type-switch）"><a href="#类型选择（type-switch）" class="headerlink" title="类型选择（type switch）"></a>类型选择（type switch）</h3><p>这是检查接口值动态类型的最强大工具。</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">do</span><span class="hljs-params">(i <span class="hljs-keyword">interface</span>&#123;&#125;)</span></span> &#123;<br>    <span class="hljs-keyword">switch</span> v := i.(<span class="hljs-keyword">type</span>) &#123; <span class="hljs-comment">// v 的类型在 each case 分支中确定</span><br>    <span class="hljs-keyword">case</span> <span class="hljs-type">int</span>:<br>        fmt.Printf(<span class="hljs-string">&quot;Twice %v is %v\n&quot;</span>, v, v*<span class="hljs-number">2</span>)<br>    <span class="hljs-keyword">case</span> <span class="hljs-type">string</span>:<br>        fmt.Printf(<span class="hljs-string">&quot;%q is %v bytes long\n&quot;</span>, v, <span class="hljs-built_in">len</span>(v))<br>    <span class="hljs-keyword">default</span>:<br>        fmt.Printf(<span class="hljs-string">&quot;I don&#x27;t know about type %T!\n&quot;</span>, v)<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><h3 id="接口组合"><a href="#接口组合" class="headerlink" title="接口组合"></a>接口组合</h3><p>通过嵌入其他接口来创建新接口，这是一种强大的接口复用方式。</p><figure class="highlight elm"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs elm"><span class="hljs-keyword">type</span> <span class="hljs-type">Reader</span> interface &#123; <span class="hljs-type">Read</span>(p []byte) (n int, err error) &#125;<br><span class="hljs-keyword">type</span> <span class="hljs-type">Writer</span> interface &#123; <span class="hljs-type">Write</span>(p []byte) (n int, err error) &#125;<br><span class="hljs-keyword">type</span> <span class="hljs-type">ReadWriter</span> interface &#123;<br>    <span class="hljs-type">Reader</span><br>    <span class="hljs-type">Writer</span><br>&#125;<br>// 任何实现了 <span class="hljs-type">Read</span> 和 <span class="hljs-type">Write</span> 的类型，就自动实现了 <span class="hljs-type">ReadWriter</span><br></code></pre></td></tr></table></figure><hr><h2 id="10-错误处理"><a href="#10-错误处理" class="headerlink" title="10. 错误处理"></a>10. 错误处理</h2><p>Go 采用显式的错误返回值，而非异常。这是一种鼓励程序员立即处理错误的风格。</p><h3 id="error-接口"><a href="#error-接口" class="headerlink" title="error 接口"></a>error 接口</h3><p><code>error</code>是一个内置接口：<code>type error interface &#123; Error() string &#125;</code>。</p><h3 id="创建与包装"><a href="#创建与包装" class="headerlink" title="创建与包装"></a>创建与包装</h3><p>- </p><pre><code class="hljs">`errors.New(&quot;message&quot;)`创建一个简单的错误。</code></pre><p>- </p><pre><code class="hljs">`fmt.Errorf(&quot;context: %w&quot;, err)`使用 `%w`动词包装一个错误，生成一个包含底层错误链的错误。这是 Go 1.13 引入的错误链机制的核心。</code></pre><p>- </p><pre><code class="hljs">`errors.Join(err1, err2)`(Go 1.20+) 可以将多个错误合并为一个，新的 `Error()`方法返回所有错误的文本连接。</code></pre><h3 id="错误检查"><a href="#错误检查" class="headerlink" title="错误检查"></a>错误检查</h3><p>- </p><pre><code class="hljs">`errors.Is(err, target)`：判断错误链 `err`中是否包含某个特定的**错误值**（`target`）。通常用于检查哨兵错误（`io.EOF`）。</code></pre><p>- </p><pre><code class="hljs">`errors.As(err, &amp;target)`：判断错误链 `err`中是否包含某个特定的**错误类型**，如果是，则将错误提取到 `target`（必须是接口或指针类型）中。<figure class="highlight stylus"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs stylus"><span class="hljs-selector-tag">var</span> pathErr *os<span class="hljs-selector-class">.PathError</span><br><span class="hljs-keyword">if</span> errors<span class="hljs-selector-class">.As</span>(err, &amp;pathErr) &#123;<br>    fmt<span class="hljs-selector-class">.Println</span>(<span class="hljs-string">&quot;Failed at path:&quot;</span>, pathErr.Path)<br>&#125;<br></code></pre></td></tr></table></figure></code></pre><h3 id="自定义错误类型"><a href="#自定义错误类型" class="headerlink" title="自定义错误类型"></a>自定义错误类型</h3><p>通过实现 <code>Error()</code>方法，任何类型都可以作为错误。如果需要加入上下文或支持错误链，可以实现 <code>Unwrap() error</code>方法。</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-keyword">type</span> MyError <span class="hljs-keyword">struct</span> &#123;<br>    Msg  <span class="hljs-type">string</span><br>    File <span class="hljs-type">string</span><br>    Line <span class="hljs-type">int</span><br>    Err  <span class="hljs-type">error</span> <span class="hljs-comment">// 底层错误</span><br>&#125;<br><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-params">(e *MyError)</span></span> Error() <span class="hljs-type">string</span> &#123;<br>    <span class="hljs-keyword">return</span> fmt.Sprintf(<span class="hljs-string">&quot;%s:%d: %s: %v&quot;</span>, e.File, e.Line, e.Msg, e.Err)<br>&#125;<br><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-params">(e *MyError)</span></span> Unwrap() <span class="hljs-type">error</span> &#123; <span class="hljs-keyword">return</span> e.Err &#125; <span class="hljs-comment">// 支持错误链</span><br></code></pre></td></tr></table></figure><h3 id="panic-与-recover"><a href="#panic-与-recover" class="headerlink" title="panic 与 recover"></a>panic 与 recover</h3><p>- </p><pre><code class="hljs">`panic(v)`：引发运行时恐慌，导致程序崩溃。它会立即停止当前函数的执行，并开始**逆序执行**当前 goroutine 的所有 `defer`函数。如果某个 `defer`中调用了 `recover`，则 panic 停止传播，程序恢复正常执行。否则，该 goroutine 终止。</code></pre><p>- </p><pre><code class="hljs">`recover()`：用于捕获 panic 传递的值。**`recover`仅在 `defer`函数中调用时才有效**，并且只能捕获同一个 goroutine 中的 panic。它的返回值就是 `panic(v)`中的 `v`。</code></pre><p>- </p><pre><code class="hljs">**使用场景**：`panic`/`recover`应仅用于处理“不可恢复”的程序错误（如程序逻辑错误、关键资源不可用）或用于在包边界转换错误（如 web 框架将 panic 转换为 500 错误）。**不应**用于控制正常的程序流。业务逻辑错误应使用 `error`返回值。</code></pre><hr><h2 id="11-包与模块"><a href="#11-包与模块" class="headerlink" title="11. 包与模块"></a>11. 包与模块</h2><h3 id="包组织"><a href="#包组织" class="headerlink" title="包组织"></a>包组织</h3><p>- </p><pre><code class="hljs">一个目录下的所有 `.go`文件必须属于同一个包。包名建议与目录名一致（`main`包除外）。</code></pre><p>- </p><pre><code class="hljs">**导出**：标识符首字母大写表示导出（公开），小写表示包内私有。</code></pre><p>- </p><pre><code class="hljs">**导入**：<figure class="highlight haxe"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs haxe"><span class="hljs-keyword">import</span> <span class="hljs-string">&quot;fmt&quot;</span><br><span class="hljs-keyword">import</span> m <span class="hljs-string">&quot;math&quot;</span>        <span class="hljs-comment">// 别名导入</span><br><span class="hljs-keyword">import</span> . <span class="hljs-string">&quot;strings&quot;</span>     <span class="hljs-comment">// 点导入（不推荐，易引起命名冲突）</span><br><span class="hljs-keyword">import</span> <span class="hljs-literal">_</span> <span class="hljs-string">&quot;image/png&quot;</span>   <span class="hljs-comment">// 空白导入，仅执行该包的 init 函数</span><br></code></pre></td></tr></table></figure></code></pre><h3 id="模块与依赖管理"><a href="#模块与依赖管理" class="headerlink" title="模块与依赖管理"></a>模块与依赖管理</h3><p>- </p><pre><code class="hljs">**模块**：是相关 Go 包的集合，根目录有 `go.mod`文件。</code></pre><p>- </p><pre><code class="hljs">**版本控制**：模块遵循语义化版本 (SemVer)。主版本 &gt;=2 时，模块路径必须包含 `/vN`后缀（如 `module github.com/user/project/v2`），并且导入路径也需要带上版本（`import &quot;github.com/user/project/v2/mypkg&quot;`）。这解决了依赖冲突。</code></pre><p>- </p><pre><code class="hljs">**`go get`**：添加、升级、降级依赖。`@`符号指定版本、分支或提交。</code></pre><p>- </p><pre><code class="hljs">**`go mod tidy`**：核心命令。根据源码中的 `import`自动同步 `go.mod`和 `go.sum`，移除无用依赖，添加必要依赖。</code></pre><p>- </p><pre><code class="hljs">**`replace`指令**：在 `go.mod`中，可以将一个模块路径替换为本地路径或其他版本，便于本地开发和调试。</code></pre><p>- </p><pre><code class="hljs">**`go work`**：多模块工作区，用于同时开发多个相互依赖的本地模块，避免频繁使用 `replace`。</code></pre><h3 id="internal-包"><a href="#internal-包" class="headerlink" title="internal 包"></a>internal 包</h3><p>如果一个包的导入路径包含 <code>internal</code>路径元素，那么该包只能被位于以 <code>internal</code>目录的父目录为根的目录树中的代码导入。这是 Go 提供的访问控制机制，用于定义内部 API。</p><figure class="highlight nix"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs nix">project<span class="hljs-symbol">/</span><br>├── go.mod<br>├── cmd<span class="hljs-symbol">/</span><br>│   └── myapp<span class="hljs-symbol">/</span><br>│       └── main.go<br>├── internal<span class="hljs-symbol">/</span><br>│   └── mylib<span class="hljs-symbol">/</span>    <span class="hljs-comment"># 只能被 project/ 下的代码导入</span><br>│       └── lib.go<br>└── pkg<span class="hljs-symbol">/</span><br>    └── public<span class="hljs-symbol">/</span>   <span class="hljs-comment"># 可以被任何代码导入</span><br>        └── pub.go<br></code></pre></td></tr></table></figure><hr><h2 id="12-并发编程"><a href="#12-并发编程" class="headerlink" title="12. 并发编程"></a>12. 并发编程</h2><p>Go 的并发模型基于 <strong>CSP (Communicating Sequential Processes)</strong> 理论，核心是 <strong>goroutine</strong> 和 <strong>channel</strong>。</p><h3 id="Goroutine"><a href="#Goroutine" class="headerlink" title="Goroutine"></a>Goroutine</h3><p>- </p><pre><code class="hljs">由 `go`关键字启动，是 Go 运行时管理的轻量级线程。开销极小（初始栈约 2KB，可动态增长/收缩），可轻松创建成千上万个。</code></pre><p>- </p><pre><code class="hljs">**调度模型 (GMP)**：- **G**：goroutine。- **M**：操作系统线程，由操作系统调度。- **P**：逻辑处理器，是 M 执行 G 所需的上下文环境。P 的数量默认为 CPU 核心数，可由 `GOMAXPROCS`环境变量控制。</code></pre><p>- </p><pre><code class="hljs">调度器在 M 上运行 G。当一个 G 阻塞（如系统调用、通道操作）时，运行它的 M 会被解绑，P 会寻找另一个可运行的 G 来执行，或者创建新的 M，以充分利用 CPU。这实现了高效的 I/O 多路复用和并发。</code></pre><h3 id="Channel（通道）"><a href="#Channel（通道）" class="headerlink" title="Channel（通道）"></a>Channel（通道）</h3><p>通道是类型化的管道，用于在 goroutine 之间安全地传递数据和同步。</p><p>- </p><pre><code class="hljs">**内部结构 (`hchan`)**：包含环形缓冲区、发送/接收等待队列、互斥锁等，保证了并发安全。</code></pre><p>- </p><pre><code class="hljs">**创建**：`ch := make(chan int)`（无缓冲），`ch := make(chan int, 10)`（缓冲容量 10）。</code></pre><p>- </p><pre><code class="hljs">**操作**：- `ch &lt;- v`：发送 `v`到通道 `ch`。- `v := &lt;-ch`：从通道 `ch`接收值并赋给 `v`。- `close(ch)`：关闭通道。**只能由发送方关闭**。关闭后不能再发送，但可以继续接收，接收完已发送的数据后，后续接收操作会立即返回元素的零值，并且接收的第二个返回值 `ok`为 `false`。</code></pre><p>- </p><pre><code class="hljs">**无缓冲 vs 缓冲**：- **无缓冲通道**：同步通道。发送操作会阻塞，直到另一个 goroutine 执行对应的接收操作，反之亦然。这保证了通信双方同时就绪。- **缓冲通道**：异步通道。发送操作仅在缓冲区满时阻塞；接收操作仅在缓冲区空时阻塞。</code></pre><p>- </p><pre><code class="hljs">**`for range`通道**：`for v := range ch`会不断从通道接收值，直到通道被关闭。</code></pre><p>- </p><pre><code class="hljs">**单向通道**：`chan&lt;- T`只发送，`&lt;-chan T`只接收。用于函数参数约束，增强类型安全性。</code></pre><p>- </p><pre><code class="hljs">**`select`语句**：多路复用。它会等待多个通道操作中的一个准备就绪。如果多个就绪，则**随机**选择一个执行。`default`子句使得 `select`非阻塞。<figure class="highlight gradle"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><code class="hljs gradle">select &#123;<br><span class="hljs-keyword">case</span> msg1 := &lt;-ch1:<br>    fmt.<span class="hljs-keyword">Println</span>(<span class="hljs-string">&quot;received&quot;</span>, msg1)<br><span class="hljs-keyword">case</span> msg2 := &lt;-ch2:<br>    fmt.<span class="hljs-keyword">Println</span>(<span class="hljs-string">&quot;received&quot;</span>, msg2)<br><span class="hljs-keyword">case</span> ch3 &lt;- <span class="hljs-number">3</span>:<br>    fmt.<span class="hljs-keyword">Println</span>(<span class="hljs-string">&quot;sent 3&quot;</span>)<br><span class="hljs-keyword">case</span> &lt;-time.After(<span class="hljs-number">1</span> * time.Second):<br>    fmt.<span class="hljs-keyword">Println</span>(<span class="hljs-string">&quot;timeout&quot;</span>)<br><span class="hljs-keyword">default</span>:<br>    fmt.<span class="hljs-keyword">Println</span>(<span class="hljs-string">&quot;no channel ready&quot;</span>)<br>&#125;<br></code></pre></td></tr></table></figure>**`nil`通道的特性**：对 `nil`通道的发送和接收操作会**永久阻塞**。这个特性可用于动态地启用或禁用 `select`中的 `case`。</code></pre><h3 id="sync-同步原语"><a href="#sync-同步原语" class="headerlink" title="sync 同步原语"></a>sync 同步原语</h3><p>- </p><pre><code class="hljs">**`sync.Mutex`/ `sync.RWMutex`**：- `Mutex`提供互斥锁，同一时刻只有一个 goroutine 可持有锁。- `RWMutex`读写锁，允许多个读锁并发，但写锁是独占的。适用于读多写少的场景。- **最佳实践**：使用 `defer mu.Unlock()`确保锁被释放。将 `Mutex`和它保护的数据封装在同一个结构体中。</code></pre><p>- </p><pre><code class="hljs">**`sync.WaitGroup`**：等待一组 goroutine 完成。<figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-keyword">var</span> wg sync.WaitGroup<br><span class="hljs-keyword">for</span> i := <span class="hljs-number">0</span>; i &lt; <span class="hljs-number">10</span>; i++ &#123;<br>    wg.Add(<span class="hljs-number">1</span>)<br>    <span class="hljs-keyword">go</span> <span class="hljs-function"><span class="hljs-keyword">func</span><span class="hljs-params">(id <span class="hljs-type">int</span>)</span></span> &#123;<br>        <span class="hljs-keyword">defer</span> wg.Done()<br>        <span class="hljs-comment">// do work</span><br>    &#125;(i)<br>&#125;<br>wg.Wait() <span class="hljs-comment">// 等待所有 goroutine 完成</span><br></code></pre></td></tr></table></figure></code></pre><p>- </p><pre><code class="hljs">**`sync.Once`**：确保某个函数只执行一次，常用于懒加载或单例初始化。<figure class="highlight nim"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs nim"><span class="hljs-keyword">var</span> once sync.<span class="hljs-type">Once</span><br><span class="hljs-keyword">var</span> config *<span class="hljs-type">Config</span><br><span class="hljs-keyword">func</span> loadConfig() *<span class="hljs-type">Config</span> &#123;<br>    once.<span class="hljs-type">Do</span>(<span class="hljs-keyword">func</span>() &#123;<br>        config = &amp;<span class="hljs-type">Config</span><span class="hljs-meta">&#123;...&#125;</span> // 初始化<br>    &#125;)<br>    <span class="hljs-keyword">return</span> config<br>&#125;<br></code></pre></td></tr></table></figure></code></pre><p>- </p><pre><code class="hljs">**`sync.Cond`**：条件变量，用于在特定条件满足时唤醒等待的 goroutine。比 channel 更底层，使用更复杂，谨慎使用。</code></pre><p>- </p><pre><code class="hljs">**`sync.Pool`**：临时对象池，用于缓存和复用临时对象，减轻 GC 压力。适用于创建成本高、生命周期短的对象。**注意**：池中的对象随时可能被 GC 清理，不能假设其存在。</code></pre><p>- </p><pre><code class="hljs">**`sync.Map`**：并发安全的 map。适用于以下场景：a) 键值对只写一次、读多次；b) 多个 goroutine 读写，但键集几乎不相交。否则，`map`+ `sync.Mutex`/`sync.RWMutex`通常性能更好，也更直观。</code></pre><p>- </p><pre><code class="hljs">**`sync/atomic`**：提供对基本类型的原子操作（如 `Add`, `Load`, `Store`, `Swap`, `CompareAndSwap`）。用于实现无锁数据结构或简单的计数器/标志位。比基于锁的操作更高效，但逻辑更复杂。</code></pre><h3 id="并发模式"><a href="#并发模式" class="headerlink" title="并发模式"></a>并发模式</h3><p>- </p><pre><code class="hljs">**退出通知**：通过关闭一个 `chan struct&#123;&#125;`来广播退出信号。<figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs go">done := <span class="hljs-built_in">make</span>(<span class="hljs-keyword">chan</span> <span class="hljs-keyword">struct</span>&#123;&#125;)<br><span class="hljs-keyword">go</span> <span class="hljs-function"><span class="hljs-keyword">func</span><span class="hljs-params">()</span></span> &#123;<br>    <span class="hljs-comment">// 工作...</span><br>    <span class="hljs-keyword">select</span> &#123;<br>    <span class="hljs-keyword">case</span> &lt;-done:<br>        <span class="hljs-keyword">return</span> <span class="hljs-comment">// 收到退出信号</span><br>    <span class="hljs-keyword">default</span>:<br>    &#125;<br>&#125;()<br><span class="hljs-comment">// 需要退出时</span><br><span class="hljs-built_in">close</span>(done)<br></code></pre></td></tr></table></figure></code></pre><p>- </p><pre><code class="hljs">**`for-select`循环**：goroutine 处理多个 channel 的典型模式，通常与 `context`结合。<figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-keyword">for</span> &#123;<br>    <span class="hljs-keyword">select</span> &#123;<br>    <span class="hljs-keyword">case</span> msg := &lt;-msgCh:<br>        handle(msg)<br>    <span class="hljs-keyword">case</span> &lt;-ctx.Done():<br>        <span class="hljs-keyword">return</span> <span class="hljs-comment">// 上下文取消，优雅退出</span><br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure></code></pre><p>- </p><pre><code class="hljs">**错误组 (`golang.org/x/sync/errgroup`)**：管理一组 goroutine，收集第一个错误并取消所有任务。<figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs go">g, ctx := errgroup.WithContext(context.Background())<br>g.Go(<span class="hljs-function"><span class="hljs-keyword">func</span><span class="hljs-params">()</span></span> <span class="hljs-type">error</span> &#123; <span class="hljs-keyword">return</span> doTask1(ctx) &#125;)<br>g.Go(<span class="hljs-function"><span class="hljs-keyword">func</span><span class="hljs-params">()</span></span> <span class="hljs-type">error</span> &#123; <span class="hljs-keyword">return</span> doTask2(ctx) &#125;)<br><span class="hljs-keyword">if</span> err := g.Wait(); err != <span class="hljs-literal">nil</span> &#123;<br>    <span class="hljs-comment">// 处理错误</span><br>&#125;<br></code></pre></td></tr></table></figure></code></pre><hr><h2 id="13-泛型-Generics"><a href="#13-泛型-Generics" class="headerlink" title="13. 泛型 (Generics)"></a>13. 泛型 (Generics)</h2><p>Go 1.18 引入了泛型，允许编写可用于多种类型的代码。</p><h3 id="类型参数与约束"><a href="#类型参数与约束" class="headerlink" title="类型参数与约束"></a>类型参数与约束</h3><p>- </p><pre><code class="hljs">在函数或类型名后的方括号 `[]`中声明类型参数。</code></pre><p>- </p><pre><code class="hljs">约束 (`constraint`) 定义了类型参数必须满足的条件，通常是一个接口，但可以包含类型集。<figure class="highlight stylus"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs stylus"><span class="hljs-comment">// 函数泛型</span><br>func Min<span class="hljs-selector-attr">[T constraints.Ordered]</span>(<span class="hljs-selector-tag">a</span>, <span class="hljs-selector-tag">b</span> T) T &#123;<br>    <span class="hljs-keyword">if</span> <span class="hljs-selector-tag">a</span> &lt; <span class="hljs-selector-tag">b</span> &#123;<br>        return <span class="hljs-selector-tag">a</span><br>    &#125;<br>    return <span class="hljs-selector-tag">b</span><br>&#125;<br><span class="hljs-comment">// 调用</span><br>m := Min<span class="hljs-selector-attr">[int]</span>(<span class="hljs-number">3</span>, <span class="hljs-number">4</span>) <span class="hljs-comment">// 显式指定类型参数</span><br>n := <span class="hljs-built_in">Min</span>(<span class="hljs-number">3.14</span>, <span class="hljs-number">2.71</span>) <span class="hljs-comment">// 类型推断</span><br></code></pre></td></tr></table></figure></code></pre><p>- </p><pre><code class="hljs">**约束**：Go 1.18+ 在 `golang.org/x/exp/constraints`包中提供了一些预定义约束（如 `Ordered`, `Integer`）。Go 1.21 将它们并入了标准库的 `cmp`包（`cmp.Ordered`）。</code></pre><p>- </p><pre><code class="hljs">可以自定义约束，使用 `~`指定底层类型：<figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-keyword">type</span> SignedInt <span class="hljs-keyword">interface</span> &#123;<br>    ~<span class="hljs-type">int</span> | ~<span class="hljs-type">int8</span> | ~<span class="hljs-type">int16</span> | ~<span class="hljs-type">int32</span> | ~<span class="hljs-type">int64</span><br>&#125;<br><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">Abs</span>[<span class="hljs-title">T</span> <span class="hljs-title">SignedInt</span>]<span class="hljs-params">(x T)</span></span> T &#123;<br>    <span class="hljs-keyword">if</span> x &lt; <span class="hljs-number">0</span> &#123;<br>        <span class="hljs-keyword">return</span> -x<br>    &#125;<br>    <span class="hljs-keyword">return</span> x<br>&#125;<br><span class="hljs-keyword">type</span> MyInt <span class="hljs-type">int</span><br><span class="hljs-keyword">var</span> m MyInt = <span class="hljs-number">-5</span><br>fmt.Println(Abs(m)) <span class="hljs-comment">// 合法，因为 MyInt 的底层类型是 int</span><br></code></pre></td></tr></table></figure></code></pre><h3 id="泛型类型"><a href="#泛型类型" class="headerlink" title="泛型类型"></a>泛型类型</h3><p>- </p><pre><code class="hljs">结构体、切片、映射、通道等类型也可以参数化。<figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-keyword">type</span> Stack[T any] <span class="hljs-keyword">struct</span> &#123;<br>    items []T<br>&#125;<br><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-params">(s *Stack[T])</span></span> Push(item T) &#123;<br>    s.items = <span class="hljs-built_in">append</span>(s.items, item)<br>&#125;<br><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-params">(s *Stack[T])</span></span> Pop() (T, <span class="hljs-type">bool</span>) &#123;<br>    <span class="hljs-keyword">if</span> <span class="hljs-built_in">len</span>(s.items) == <span class="hljs-number">0</span> &#123;<br>        <span class="hljs-keyword">var</span> zero T<br>        <span class="hljs-keyword">return</span> zero, <span class="hljs-literal">false</span><br>    &#125;<br>    item := s.items[<span class="hljs-built_in">len</span>(s.items)<span class="hljs-number">-1</span>]<br>    s.items = s.items[:<span class="hljs-built_in">len</span>(s.items)<span class="hljs-number">-1</span>]<br>    <span class="hljs-keyword">return</span> item, <span class="hljs-literal">true</span><br>&#125;<br></code></pre></td></tr></table></figure></code></pre><h3 id="类型集与接口"><a href="#类型集与接口" class="headerlink" title="类型集与接口"></a>类型集与接口</h3><p>- </p><pre><code class="hljs">在 Go 1.18+ 中，接口不仅可以定义方法集，还可以定义类型集（通过 `|`连接多个类型）。这种接口既可以用作约束，也可以用作普通接口类型（如果它只包含方法）。</code></pre><p>- </p><pre><code class="hljs">`comparable`是一个内置接口约束，表示类型支持 `==`和 `!=`操作，可以用作 map 的键。</code></pre><p>- </p><pre><code class="hljs">`any`是 `interface&#123;&#125;`的别名，表示任何类型。</code></pre><h3 id="当前限制"><a href="#当前限制" class="headerlink" title="当前限制"></a>当前限制</h3><p>- </p><pre><code class="hljs">**无泛型方法**：不能在已有类型上定义新的泛型方法（例如 `func (s *Stack[T]) Map[U any](f func(T) U) *Stack[U]`是不允许的）。但可以通过顶层泛型函数或接收者是泛型类型的方法来变通。</code></pre><p>- </p><pre><code class="hljs">**实现原理**：Go 编译器采用“GC Shape Stenciling”策略，为每个不同的 GC 形状（大致由内存布局决定，而不是具体类型）生成一份代码，并通过运行时字典传递类型信息，在代码膨胀和性能之间取得平衡。</code></pre><hr><h2 id="14-反射-Reflection"><a href="#14-反射-Reflection" class="headerlink" title="14. 反射 (Reflection)"></a>14. 反射 (Reflection)</h2><p><code>reflect</code>包提供了在运行时检查类型和操作值的能力。它很强大，但应谨慎使用，因为会损失类型安全、降低性能、使代码复杂。</p><p>- </p><pre><code class="hljs">**获取类型和值**：<figure class="highlight gml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs gml"><span class="hljs-keyword">var</span> <span class="hljs-variable language_">x</span> float64 = <span class="hljs-number">3.4</span><br>t := reflect.TypeOf(<span class="hljs-variable language_">x</span>)  <span class="hljs-comment">// reflect.Type</span><br>v := reflect.ValueOf(<span class="hljs-variable language_">x</span>) <span class="hljs-comment">// reflect.Value</span><br>fmt.Println(t.Kind())    <span class="hljs-comment">// float64</span><br>fmt.Println(v.Float())   <span class="hljs-comment">// 3.4</span><br></code></pre></td></tr></table></figure></code></pre><p>- </p><pre><code class="hljs">**修改变量**：通过反射修改变量，需要获取其值的指针，然后调用 `Elem()`获取指针指向的值，最后调用 `Set`系列方法。<figure class="highlight stylus"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs stylus"><span class="hljs-selector-tag">var</span> f float64 = <span class="hljs-number">3.14</span><br>v := reflect<span class="hljs-selector-class">.ValueOf</span>(&amp;f)<span class="hljs-selector-class">.Elem</span>() <span class="hljs-comment">// 必须传递地址，且 Elem() 解引用</span><br>v<span class="hljs-selector-class">.SetFloat</span>(<span class="hljs-number">2.71</span>)<br>fmt<span class="hljs-selector-class">.Println</span>(f) <span class="hljs-comment">// 2.71</span><br></code></pre></td></tr></table></figure></code></pre><p>- </p><pre><code class="hljs">**调用函数/方法**：<figure class="highlight stylus"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs stylus">func <span class="hljs-built_in">Add</span>(<span class="hljs-selector-tag">a</span>, <span class="hljs-selector-tag">b</span> int) int &#123; return <span class="hljs-selector-tag">a</span> + <span class="hljs-selector-tag">b</span> &#125;<br>fn := reflect<span class="hljs-selector-class">.ValueOf</span>(Add)<br>args := <span class="hljs-selector-attr">[]</span>reflect.Value&#123;reflect<span class="hljs-selector-class">.ValueOf</span>(<span class="hljs-number">3</span>), reflect<span class="hljs-selector-class">.ValueOf</span>(<span class="hljs-number">4</span>)&#125;<br>results := fn<span class="hljs-selector-class">.Call</span>(args)<br>fmt<span class="hljs-selector-class">.Println</span>(results<span class="hljs-selector-attr">[0]</span><span class="hljs-selector-class">.Int</span>()) <span class="hljs-comment">// 7</span><br></code></pre></td></tr></table></figure></code></pre><p>- </p><pre><code class="hljs">**结构体标签**：反射常用于解析结构体标签，如 JSON 编码/解码。<figure class="highlight pgsql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs pgsql"><span class="hljs-keyword">type</span> <span class="hljs-keyword">User</span> struct &#123;<br>    <span class="hljs-type">Name</span> string `<span class="hljs-type">json</span>:&quot;name&quot; <span class="hljs-keyword">validate</span>:&quot;required&quot;`<br>&#125;<br>t := reflect.TypeOf(<span class="hljs-keyword">User</span>&#123;&#125;)<br>field, _ := t.FieldByName(&quot;Name&quot;)<br>fmt.Println(field.Tag.<span class="hljs-keyword">Get</span>(&quot;json&quot;)) // &quot;name&quot;<br></code></pre></td></tr></table></figure></code></pre><p>- </p><pre><code class="hljs">**使用建议**：仅在需要处理未知类型的场景使用反射，如序列化库、ORM 框架、配置文件解析等。在业务逻辑中，优先使用接口和泛型等类型安全的方式。</code></pre><hr><h2 id="15-上下文-Context"><a href="#15-上下文-Context" class="headerlink" title="15. 上下文 Context"></a>15. 上下文 Context</h2><p><code>context.Context</code>是 Go 1.7 引入标准库的，用于在 API 边界和 goroutine 之间传递请求范围的<strong>值</strong>、<strong>取消信号</strong>和<strong>截止时间</strong>。</p><h3 id="核心概念"><a href="#核心概念" class="headerlink" title="核心概念"></a>核心概念</h3><p>- </p><pre><code class="hljs">**树形结构**：Context 构成一棵树。当一个 Context 被取消时，从它派生的所有 Context 都会被取消。</code></pre><p>- </p><pre><code class="hljs">**不可变**：Context 的值是不可变的。要添加值或设置超时，会派生出一个新的 Context。</code></pre><h3 id="创建与派生"><a href="#创建与派生" class="headerlink" title="创建与派生"></a>创建与派生</h3><figure class="highlight stylus"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><code class="hljs stylus"><span class="hljs-comment">// 根 Context，永不取消，没有值</span><br>ctx := context<span class="hljs-selector-class">.Background</span>()<br><span class="hljs-comment">// 或使用 TODO（当不确定用哪个 Context 时）</span><br>ctx = context<span class="hljs-selector-class">.TODO</span>()<br><br><span class="hljs-comment">// 派生一个可取消的 Context</span><br>ctx, cancel := context<span class="hljs-selector-class">.WithCancel</span>(context<span class="hljs-selector-class">.Background</span>())<br>defer <span class="hljs-built_in">cancel</span>() <span class="hljs-comment">// 确保资源被释放，避免泄露</span><br><span class="hljs-comment">// 调用 cancel() 会取消 ctx 及其所有派生 Context</span><br><br><span class="hljs-comment">// 派生一个带截止时间的 Context</span><br>ctx, cancel = context<span class="hljs-selector-class">.WithTimeout</span>(context<span class="hljs-selector-class">.Background</span>(), <span class="hljs-number">2</span>*<span class="hljs-selector-tag">time</span>.Second)<br>defer <span class="hljs-built_in">cancel</span>()<br><br><span class="hljs-comment">// 派生一个带截止时刻的 Context</span><br>ctx, cancel = context<span class="hljs-selector-class">.WithDeadline</span>(context<span class="hljs-selector-class">.Background</span>(), someTime)<br>defer <span class="hljs-built_in">cancel</span>()<br><br><span class="hljs-comment">// 派生一个带键值的 Context</span><br>type ctxKey string<br><span class="hljs-selector-tag">var</span> key ctxKey = <span class="hljs-string">&quot;request-id&quot;</span><br>ctx = context<span class="hljs-selector-class">.WithValue</span>(context<span class="hljs-selector-class">.Background</span>(), key, <span class="hljs-string">&quot;12345&quot;</span>)<br></code></pre></td></tr></table></figure><h3 id="使用-Context"><a href="#使用-Context" class="headerlink" title="使用 Context"></a>使用 Context</h3><p>- </p><pre><code class="hljs">函数应将 `Context`作为第一个参数，通常命名为 `ctx`。</code></pre><p>- </p><pre><code class="hljs">通过 `ctx.Done()`返回的通道接收取消信号。当 Context 被取消或超时，该通道会被关闭。</code></pre><p>- </p><pre><code class="hljs">使用 `select`监听 `ctx.Done()`和业务通道，以实现优雅退出。</code></pre><p>- </p><pre><code class="hljs">`ctx.Err()`返回取消的原因：`context.Canceled`或 `context.DeadlineExceeded`。</code></pre><p>- </p><pre><code class="hljs">`ctx.Value(key)`获取与 Context 关联的值。</code></pre><h3 id="最佳实践"><a href="#最佳实践" class="headerlink" title="最佳实践"></a>最佳实践</h3><p>1. </p><pre><code class="hljs">**传播**：Context 应贯穿整个调用链，从入口（如 HTTP 请求处理）传递到每一个需要它的函数（如数据库查询、RPC 调用）。</code></pre><p>2. </p><pre><code class="hljs">**存储**：**不要将 Context 存储在结构体字段中**（除非该结构体本身的职责就是创建和传递 Context，比如一个 `Server`类型）。应该将 Context 作为参数传递。</code></pre><p>3. </p><pre><code class="hljs">**取消**：**总是 defer cancel()**。即使操作很快完成，调用 `cancel`也是无害的，并且可以释放相关资源。</code></pre><p>4. </p><pre><code class="hljs">**键值**：使用自定义类型作为 `WithValue`的键，避免包间冲突。导出的键应该是常量或不可变量。<figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-comment">// 正确做法</span><br><span class="hljs-keyword">type</span> ctxKey <span class="hljs-type">string</span><br><span class="hljs-keyword">const</span> requestIDKey ctxKey = <span class="hljs-string">&quot;request-id&quot;</span><br><span class="hljs-comment">// 设置值</span><br>ctx = context.WithValue(ctx, requestIDKey, <span class="hljs-string">&quot;abc&quot;</span>)<br><span class="hljs-comment">// 获取值</span><br>id, ok := ctx.Value(requestIDKey).(<span class="hljs-type">string</span>)<br></code></pre></td></tr></table></figure></code></pre><hr><h2 id="16-测试"><a href="#16-测试" class="headerlink" title="16. 测试"></a>16. 测试</h2><p>Go 内置强大的测试支持，鼓励编写简单、可组合的测试。</p><h3 id="单元测试"><a href="#单元测试" class="headerlink" title="单元测试"></a>单元测试</h3><p>- </p><pre><code class="hljs">测试文件以 `_test.go`结尾。</code></pre><p>- </p><pre><code class="hljs">测试函数签名为 `func TestXxx(t *testing.T)`。</code></pre><p>- </p><pre><code class="hljs">`t.Error`/ `t.Errorf`：报告测试失败，但继续执行测试。</code></pre><p>- </p><pre><code class="hljs">`t.Fatal`/ `t.Fatalf`：报告测试失败，并立即终止当前测试函数。</code></pre><p>- </p><pre><code class="hljs">**表驱动测试**：推荐的测试组织方式。<figure class="highlight stylus"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><code class="hljs stylus">func <span class="hljs-built_in">TestAdd</span>(t *testing.T) &#123;<br>    tests := <span class="hljs-selector-attr">[]</span>struct &#123;<br>        name string<br>        <span class="hljs-selector-tag">a</span>, <span class="hljs-selector-tag">b</span> int<br>        want int<br>    &#125;&#123;<br>        &#123;<span class="hljs-string">&quot;positive&quot;</span>, <span class="hljs-number">1</span>, <span class="hljs-number">2</span>, <span class="hljs-number">3</span>&#125;,<br>        &#123;<span class="hljs-string">&quot;zero&quot;</span>, <span class="hljs-number">0</span>, <span class="hljs-number">0</span>, <span class="hljs-number">0</span>&#125;,<br>        &#123;<span class="hljs-string">&quot;negative&quot;</span>, -<span class="hljs-number">1</span>, -<span class="hljs-number">2</span>, -<span class="hljs-number">3</span>&#125;,<br>    &#125;<br>    <span class="hljs-keyword">for</span> _, tt := range tests &#123;<br>        t<span class="hljs-selector-class">.Run</span>(tt<span class="hljs-selector-class">.name</span>, <span class="hljs-built_in">func</span>(t *testing.T) &#123; <span class="hljs-comment">// 子测试</span><br>            got := <span class="hljs-built_in">Add</span>(tt<span class="hljs-selector-class">.a</span>, tt.b)<br>            <span class="hljs-keyword">if</span> got != tt<span class="hljs-selector-class">.want</span> &#123;<br>                t<span class="hljs-selector-class">.Errorf</span>(<span class="hljs-string">&quot;Add(%d, %d) = %d, want %d&quot;</span>, tt<span class="hljs-selector-class">.a</span>, tt<span class="hljs-selector-class">.b</span>, got, tt.want)<br>            &#125;<br>        &#125;)<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure></code></pre><p>- </p><pre><code class="hljs">`t.Helper()`：标记函数为测试辅助函数，调用此函数时，报错信息会指向调用它的代码行，而不是辅助函数内部。</code></pre><p>- </p><pre><code class="hljs">`t.Cleanup(func())`：注册清理函数，在测试（或子测试）结束时执行，是 `defer`的替代品，与 `t.Run`配合更好。</code></pre><p>- </p><pre><code class="hljs">`t.TempDir()`：创建临时目录，测试结束后自动清理。</code></pre><h3 id="模糊测试-Go-1-18"><a href="#模糊测试-Go-1-18" class="headerlink" title="模糊测试 (Go 1.18+)"></a>模糊测试 (Go 1.18+)</h3><p>自动生成随机输入，尝试发现边界条件和未处理的错误。</p><figure class="highlight stylus"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs stylus">func <span class="hljs-built_in">FuzzReverse</span>(f *testing.F) &#123;<br>    <span class="hljs-comment">// 添加种子语料库</span><br>    f<span class="hljs-selector-class">.Add</span>(<span class="hljs-string">&quot;hello&quot;</span>)<br>    f<span class="hljs-selector-class">.Fuzz</span>(<span class="hljs-built_in">func</span>(t *testing<span class="hljs-selector-class">.T</span>, s string) &#123;<br>        rev := <span class="hljs-built_in">Reverse</span>(s)<br>        <span class="hljs-keyword">if</span> <span class="hljs-built_in">Reverse</span>(rev) != s &#123;<br>            t<span class="hljs-selector-class">.Errorf</span>(<span class="hljs-string">&quot;Reverse(Reverse(%q)) != %q&quot;</span>, s, s)<br>        &#125;<br>    &#125;)<br>&#125;<br><span class="hljs-comment">// 运行: go test -fuzz=Fuzz</span><br></code></pre></td></tr></table></figure><h3 id="基准测试"><a href="#基准测试" class="headerlink" title="基准测试"></a>基准测试</h3><figure class="highlight scss"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs scss">func <span class="hljs-built_in">BenchmarkFib</span>(b *testing.B) &#123;<br>    for <span class="hljs-selector-tag">i</span> := <span class="hljs-number">0</span>; <span class="hljs-selector-tag">i</span> &lt; <span class="hljs-selector-tag">b</span><span class="hljs-selector-class">.N</span>; <span class="hljs-selector-tag">i</span>++ &#123;<br>        <span class="hljs-built_in">Fib</span>(<span class="hljs-number">20</span>) <span class="hljs-comment">// 被测试的函数</span><br>    &#125;<br>&#125;<br><span class="hljs-comment">// 运行: go test -bench=. -benchmem</span><br><span class="hljs-comment">// -benchmem 显示内存分配情况</span><br></code></pre></td></tr></table></figure><p>- </p><pre><code class="hljs">`b.N`由框架动态调整，以使测试运行足够长时间得到稳定结果。</code></pre><p>- </p><pre><code class="hljs">使用 `b.ResetTimer()`和 `b.StopTimer()`/ `b.StartTimer()`来排除初始化代码的时间。</code></pre><h3 id="示例测试"><a href="#示例测试" class="headerlink" title="示例测试"></a>示例测试</h3><p>示例函数既是测试也是文档。如果包含 <code>// Output:</code>注释，测试框架会验证输出是否匹配。</p><figure class="highlight csharp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs csharp"><span class="hljs-function">func <span class="hljs-title">ExampleHello</span>()</span> &#123;<br>    fmt.Println(<span class="hljs-string">&quot;hello&quot;</span>)<br>    <span class="hljs-comment">// Output:</span><br>    <span class="hljs-comment">// hello</span><br>&#125;<br></code></pre></td></tr></table></figure><h3 id="TestMain"><a href="#TestMain" class="headerlink" title="TestMain"></a>TestMain</h3><p>如果需要为所有测试设置全局环境或清理，可以定义 <code>TestMain</code>。</p><figure class="highlight stylus"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs stylus">func <span class="hljs-built_in">TestMain</span>(m *testing.M) &#123;<br>    <span class="hljs-built_in">setup</span>() <span class="hljs-comment">// 例如，启动测试数据库</span><br>    <span class="hljs-selector-tag">code</span> := m<span class="hljs-selector-class">.Run</span>() <span class="hljs-comment">// 运行所有测试</span><br>    <span class="hljs-built_in">teardown</span>() <span class="hljs-comment">// 清理</span><br>    os<span class="hljs-selector-class">.Exit</span>(code)<br>&#125;<br></code></pre></td></tr></table></figure><h3 id="覆盖率与竞态检测"><a href="#覆盖率与竞态检测" class="headerlink" title="覆盖率与竞态检测"></a>覆盖率与竞态检测</h3><p>- </p><pre><code class="hljs">**覆盖率**：`go test -coverprofile=cover.out`生成覆盖率文件，`go tool cover -html=cover.out`在浏览器中查看可视化结果。</code></pre><p>- </p><pre><code class="hljs">**竞态检测**：`go test -race`。它会检测数据竞态（多个 goroutine 并发访问同一变量，且至少有一个是写）。**务必在测试中启用竞态检测**，但它有性能开销，不适合生产环境。</code></pre><hr><h2 id="17-标准库精选"><a href="#17-标准库精选" class="headerlink" title="17. 标准库精选"></a>17. 标准库精选</h2><table><thead><tr><th>包名</th><th>核心功能与说明</th></tr></thead><tbody><tr><td><code>fmt</code></td><td>格式化 I&#x2F;O。<code>%v</code>（默认格式）、<code>%+v</code>（结构体包含字段名）、<code>%#v</code>（Go 语法表示）、<code>%T</code>（类型）、<code>%w</code>（包装错误）。</td></tr><tr><td><code>io</code></td><td>定义了 <code>io.Reader</code>和 <code>io.Writer</code>等核心接口。<code>io.Copy</code>, <code>io.ReadAll</code>, <code>io.Pipe</code>等实用函数。</td></tr><tr><td><code>os</code></td><td>操作系统功能：文件操作 (<code>Open</code>, <code>Create</code>)、环境变量、命令行参数、进程管理。</td></tr><tr><td><code>net/http</code></td><td>HTTP 客户端和服务器实现。<code>http.Handler</code>接口是 Web 框架的基础。</td></tr><tr><td><code>encoding/json</code></td><td>JSON 编解码。通过结构体标签 (<code>json:&quot;name,omitempty&quot;</code>) 控制字段映射。流式解码器 (<code>json.Decoder</code>) 适合处理大 JSON。</td></tr><tr><td><code>time</code></td><td>时间处理。<code>time.Time</code>表示时刻，<code>time.Duration</code>表示时长。<strong>重要</strong>：格式化时间必须使用参考时间 <code>Mon Jan 2 15:04:05 MST 2006</code>。</td></tr><tr><td><code>strings</code></td><td>字符串操作：<code>Contains</code>, <code>HasPrefix</code>, <code>Split</code>, <code>Join</code>。<code>strings.Builder</code>用于高效构建字符串。</td></tr><tr><td><code>strconv</code></td><td>字符串与基本类型的转换：<code>Atoi</code>, <code>Itoa</code>, <code>ParseFloat</code>, <code>FormatInt</code>。</td></tr><tr><td><code>sort</code></td><td>排序。<code>sort.Slice</code>可对任意切片排序。<code>sort.Search</code>实现二分查找。</td></tr><tr><td><code>sync</code></td><td>同步原语：<code>Mutex</code>, <code>RWMutex</code>, <code>WaitGroup</code>, <code>Once</code>, <code>Pool</code>, <code>Map</code>。</td></tr><tr><td><code>context</code></td><td>上下文传递与取消。</td></tr><tr><td><code>log</code></td><td>简单日志。<code>log.Printf</code>, <code>log.Fatal</code>（会调用 <code>os.Exit</code>）。生产环境建议使用更强大的日志库（如 <code>slog</code>(Go 1.21+) 或 <code>zap</code>, <code>logrus</code>）。</td></tr><tr><td><code>text/template</code>&#x2F; <code>html/template</code></td><td>文本&#x2F;HTML 模板引擎，用于生成动态内容。</td></tr></tbody></table><hr><h2 id="18-编译器指令与构建"><a href="#18-编译器指令与构建" class="headerlink" title="18. 编译器指令与构建"></a>18. 编译器指令与构建</h2><h3 id="go-generate"><a href="#go-generate" class="headerlink" title="&#x2F;&#x2F;go:generate"></a>&#x2F;&#x2F;go:generate</h3><p>用于在构建前执行代码生成命令。运行 <code>go generate ./...</code>会扫描当前模块下的文件，执行所有 <code>//go:generate</code>指令。</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-comment">//go:generate stringer -type=Pill</span><br><span class="hljs-keyword">type</span> Pill <span class="hljs-type">int</span><br><span class="hljs-keyword">const</span> (<br>    Placebo Pill = <span class="hljs-literal">iota</span><br>    Aspirin<br>    Ibuprofen<br>)<br><span class="hljs-comment">// 运行 go generate 会生成 pill_string.go 文件，实现 String() 方法。</span><br></code></pre></td></tr></table></figure><h3 id="go-embed-Go-1-16"><a href="#go-embed-Go-1-16" class="headerlink" title="&#x2F;&#x2F;go:embed (Go 1.16+)"></a>&#x2F;&#x2F;go:embed (Go 1.16+)</h3><p>在编译时将外部文件（如配置文件、模板、静态资源）嵌入到程序中。</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-keyword">import</span> <span class="hljs-string">&quot;embed&quot;</span><br><span class="hljs-comment">//go:embed hello.txt</span><br><span class="hljs-keyword">var</span> s <span class="hljs-type">string</span> <span class="hljs-comment">// s 会包含 hello.txt 的内容</span><br><span class="hljs-comment">//go:embed images/*</span><br><span class="hljs-keyword">var</span> images embed.FS <span class="hljs-comment">// embed.FS 是一个只读的文件系统</span><br>data, _ := images.ReadFile(<span class="hljs-string">&quot;images/icon.png&quot;</span>)<br></code></pre></td></tr></table></figure><p>- </p><pre><code class="hljs">支持嵌入单个文件、文件树，或使用模式匹配。</code></pre><p>- </p><pre><code class="hljs">变量类型可以是 `string`, `[]byte`, 或 `embed.FS`。</code></pre><h3 id="构建约束"><a href="#构建约束" class="headerlink" title="构建约束"></a>构建约束</h3><p>控制源文件在哪些条件下被编译。</p><p>- </p><pre><code class="hljs">**新式 (Go 1.16+)**：`//go:build linux &amp;&amp; amd64`</code></pre><p>- </p><pre><code class="hljs">**旧式**：`// +build linux,amd64`（已废弃，但工具仍支持）</code></pre><p>- </p><pre><code class="hljs">**文件命名**：`file_linux.go`只在 Linux 系统编译。`file_windows_amd64.go`只在 Windows AMD64 平台编译。</code></pre><hr><h2 id="19-工具链深入"><a href="#19-工具链深入" class="headerlink" title="19. 工具链深入"></a>19. 工具链深入</h2><p>- </p><pre><code class="hljs">`go fmt`：使用 `gofmt`工具格式化代码。风格统一，无需争论。</code></pre><p>- </p><pre><code class="hljs">`go vet`：静态分析工具，检测代码中的常见错误（如格式化字符串不匹配、无用的赋值、锁拷贝等）。应集成到 CI/CD 流程中。</code></pre><p>- </p><pre><code class="hljs">`go doc`/ `pkg.go.dev`：查看文档。`go doc net/http.HandleFunc`。</code></pre><p>- </p><pre><code class="hljs">`go mod why`：解释为什么需要某个依赖。</code></pre><p>- </p><pre><code class="hljs">`go mod graph`：以图的形式显示模块依赖。</code></pre><p>- </p><pre><code class="hljs">**性能剖析 (Profiling)**：1. 导入 `_ &quot;net/http/pprof&quot;`。2. 启动 HTTP 服务器（或在测试中使用 `-cpuprofile`等标志）。3. 访问 `http://localhost/debug/pprof/`获取各类剖析数据。4. 使用 `go tool pprof`进行交互式分析。<figure class="highlight awk"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs awk"><span class="hljs-comment"># 采集 30 秒 CPU 剖析信息</span><br>go tool pprof http:<span class="hljs-regexp">//</span>localhost<span class="hljs-regexp">/debug/</span>pprof/profile?seconds=<span class="hljs-number">30</span><br><span class="hljs-comment"># 查看堆内存分配</span><br>go tool pprof http:<span class="hljs-regexp">//</span>localhost<span class="hljs-regexp">/debug/</span>pprof/heap<br><span class="hljs-comment"># 与上次采样对比</span><br>go tool pprof -base old.pb.gz new.pb.gz<br></code></pre></td></tr></table></figure>- 常用命令 (在 pprof 交互界面)：`top`(查看耗时最多的函数)，`list FuncName`(列出函数具体代码行开销)，`web`(生成调用图，需 `graphviz`)。</code></pre><p>- </p><pre><code class="hljs">**竞态检测 (Race Detector)**：`go test -race`或 `go build -race`。它会在运行时监控内存访问，检测数据竞争。**在集成测试和预发布环境务必启用**，但因其有 5-10 倍性能开销和内存开销，**切勿在生产环境使用**。</code></pre><p>- </p><pre><code class="hljs">**编译器优化报告**：`go build -gcflags=-m=2`可以打印更详细的逃逸分析和内联决策，帮助理解性能优化点。</code></pre><hr><h2 id="20-其他重要特性与细节"><a href="#20-其他重要特性与细节" class="headerlink" title="20. 其他重要特性与细节"></a>20. 其他重要特性与细节</h2><p>- </p><pre><code class="hljs">**字符串不可变**：字符串是只读的字节切片（`[]byte`）。`s[0]`只能读不能写。任何修改操作（如 `+`拼接）都会生成新的字符串。**大量字符串拼接请使用 `strings.Builder`**，它内部使用 `[]byte`并支持高效扩容。</code></pre><p>- </p><pre><code class="hljs">**`for range`字符串**：`for i, r := range &quot;世界&quot;`会迭代每个 **Unicode 码点 (rune)**，`i`是**当前 rune 的起始字节索引**。这是处理多字节 UTF-8 字符的正确方式。`len(&quot;世界&quot;)`返回的是**字节数 (6)**，而非字符数 (2)。</code></pre><p>- </p><pre><code class="hljs">**`select`与 `nil`通道**：对 `nil`通道的发送和接收操作会**永久阻塞**。利用此特性，可以在 `select`中动态地启用或禁用某个 `case`，通过将通道变量设为 `nil`来实现“禁用”。<figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-keyword">var</span> a, b <span class="hljs-keyword">chan</span> <span class="hljs-type">int</span><br><span class="hljs-comment">// 根据条件动态设置 a 或 b</span><br><span class="hljs-keyword">select</span> &#123;<br><span class="hljs-keyword">case</span> v := &lt;-a:<br>    <span class="hljs-comment">// 处理 a</span><br><span class="hljs-keyword">case</span> v := &lt;-b:<br>    <span class="hljs-comment">// 处理 b</span><br><span class="hljs-keyword">default</span>:<br>    <span class="hljs-comment">// a 和 b 都为 nil 或未就绪时执行</span><br>&#125;<br></code></pre></td></tr></table></figure></code></pre><p>- </p><pre><code class="hljs">**`unsafe`包**：允许绕过 Go 的类型安全内存操作，用于底层系统编程、性能关键路径或与 C 代码交互。常用场景：- **`unsafe.Pointer`**：通用指针类型，可与任何类型的指针相互转换。- **`uintptr`**：可存储指针的整数值，用于指针运算。- **零拷贝转换**：`string`与 `[]byte`的高效转换（风险：修改 `[]byte`会破坏字符串不变性）。<figure class="highlight stylus"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs stylus"><span class="hljs-comment">// 危险！转换后修改 b 会引发未定义行为。</span><br>s := <span class="hljs-string">&quot;hello&quot;</span><br><span class="hljs-selector-tag">b</span> := unsafe<span class="hljs-selector-class">.Slice</span>(unsafe<span class="hljs-selector-class">.StringData</span>(s), <span class="hljs-built_in">len</span>(s))<br></code></pre></td></tr></table></figure>**警告**：使用 `unsafe`会使代码不可移植、难以调试，并可能破坏内存安全和垃圾回收。仅在绝对必要且完全理解后果时使用。</code></pre><p>- </p><pre><code class="hljs">**`cgo`**：允许在 Go 中调用 C 代码。通过在 import 前加入特殊的注释块来编写 C 代码。`cgo`会引入构建复杂性、性能开销（Go 与 C 间调用成本）和可移植性问题。仅在需要集成现有 C 库或进行极端系统级优化时使用。</code></pre><p>- </p><pre><code class="hljs">**垃圾回收 (GC)**：Go 使用**并发、三色标记-清除**垃圾回收器。其主要目标是**降低延迟**（STW 停顿时间短）。- **`GOGC`环境变量**：设置触发下一次 GC 的堆内存增长百分比。默认值 100 表示堆增长一倍后触发 GC。增大它（如 200）可减少 GC 频率，提升吞吐量但增加内存占用；减小它则相反。- **`GOMEMLIMIT`(Go 1.19+)**：设置 Go 运行时可以使用的**软内存上限**。当内存使用接近此限制时，运行时会更积极地触发 GC。这有助于在容器环境中更稳定地控制内存使用，避免因 OOM 被系统杀死。**这不是硬限制**，GC 后内存可能短暂超出此值。</code></pre><p>- </p><pre><code class="hljs">**Go 内存模型**：它规定了在多个 goroutine 中，对一个变量的读操作，在什么条件下能保证观察到另一个 goroutine 对该变量的写操作。**Happens-before** 关系是关键。同步原语（通道通信、`sync`包操作、`atomic`操作）会建立 happens-before 关系，从而保证内存可见性。编写无锁并发代码时必须透彻理解内存模型。</code></pre><p>- </p><pre><code class="hljs">**结构体/数组可比较性**：一个结构体或数组类型可用作 `map`键的**前提是其所有字段/元素类型都是可比较的**。即使包含不可比较的字段（如切片），该结构体类型本身依然存在，只是不能用于 `==`比较或作为 `map`键。</code></pre><p>- </p><pre><code class="hljs">**`defer`与 `recover`的细节**：- `recover`必须在一个**被直接 `defer`的函数**中调用才有效。间接调用无效。<figure class="highlight scss"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs scss">defer <span class="hljs-built_in">func</span>() &#123; <span class="hljs-built_in">recover</span>() &#125;() <span class="hljs-comment">// 有效</span><br>defer fmt<span class="hljs-selector-class">.Println</span>(recover()) <span class="hljs-comment">// 无效！recover() 在 defer 参数中求值，此时 panic 尚未发生。</span><br>defer <span class="hljs-built_in">func</span>() &#123; <br>    <span class="hljs-built_in">myRecover</span>() <span class="hljs-comment">// 无效！如果 myRecover 内部调用了 recover()，也无法捕获。</span><br>&#125;()<br></code></pre></td></tr></table></figure>- 正确的模式是：`defer func() &#123; if r := recover(); r != nil &#123; /* 处理 */ &#125; &#125;()`。</code></pre><p>- </p><pre><code class="hljs">**接口与 `nil`的最终辨析**：这是 Go 中最常见的陷阱之一。<figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">returnsError</span><span class="hljs-params">()</span></span> <span class="hljs-type">error</span> &#123;<br>    <span class="hljs-keyword">var</span> p *MyError = <span class="hljs-literal">nil</span> <span class="hljs-comment">// p 是一个 nil 指针</span><br>    <span class="hljs-keyword">if</span> bad() &#123;<br>        p = &amp;MyError&#123;&#125;<br>    &#125;<br>    <span class="hljs-keyword">return</span> p <span class="hljs-comment">// 错误：这里返回的 error 接口值不为 nil！(动态类型是 *MyError, 动态值是 nil)</span><br>&#125;<br><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">main</span><span class="hljs-params">()</span></span> &#123;<br>    <span class="hljs-keyword">if</span> err := returnsError(); err != <span class="hljs-literal">nil</span> &#123;<br>        <span class="hljs-comment">// 即使 bad() 为 false，这里也会进入，因为 err != nil</span><br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>**正确做法**：在返回错误时，如果没有错误，应直接 `return nil`。<figure class="highlight csharp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs csharp"><span class="hljs-function">func <span class="hljs-title">returnsError</span>() error</span> &#123;<br>    <span class="hljs-function"><span class="hljs-keyword">if</span> <span class="hljs-title">bad</span>()</span> &#123;<br>        <span class="hljs-keyword">return</span> &amp;MyError&#123;&#125;<br>    &#125;<br>    <span class="hljs-keyword">return</span> nil <span class="hljs-comment">// 正确：接口的动态类型和动态值均为 nil</span><br>&#125;<br></code></pre></td></tr></table></figure></code></pre><hr><p>这是目前最全面、最详尽的 Go 语言教程，覆盖语言设计、标准库、工具链和工程实践。掌握这些内容，你将具备专业 Go 开发者的视野与能力。</p>]]></content>
    
    
    
    <tags>
      
      <tag>go</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>《Java核心技术卷一》第十二章</title>
    <link href="/2025/12/06/%E3%80%8AJava%E6%A0%B8%E5%BF%83%E6%8A%80%E6%9C%AF%E5%8D%B7%E4%B8%80%E3%80%8B%E7%AC%AC%E5%8D%81%E4%BA%8C%E7%AB%A0/"/>
    <url>/2025/12/06/%E3%80%8AJava%E6%A0%B8%E5%BF%83%E6%8A%80%E6%9C%AF%E5%8D%B7%E4%B8%80%E3%80%8B%E7%AC%AC%E5%8D%81%E4%BA%8C%E7%AB%A0/</url>
    
    <content type="html"><![CDATA[<h2 id="第12章-并发"><a href="#第12章-并发" class="headerlink" title="第12章　并发"></a>第12章　并发</h2><p><strong>多任务（multitasking）</strong>，这是操作系统的一种能力，看起来可以在同一时刻运行多个程序。并发执行的进程数并不受限于 CPU 数。操作系统会为每个进程分配 CPU 时间片，给人并行处理的感觉。</p><p>多线程程序在更低一层扩展了多任务的概念：单个程序看起来在同时完成多个任务。每个任务在一个线程（thread）中执行，线程是控制线程的简称。如果一个程序可以同时运行多个线程，则称这个程序是<strong>多线程程序（multithreaded）</strong>。</p><p>**多进程（process）**与多线程本质的区别在于：每个进程都拥有自己的一整套变量，而线程共享数据。不过，共享变量使线程之间的通信比进程之间的通信更高效、更容易。此外，在某些操作系统中，与进程相比较，线程更“轻量级”，创建、撤销单个线程比启动新进程的开销要小得多。</p><h3 id="12-1-什么是线程"><a href="#12-1-什么是线程" class="headerlink" title="12.1 什么是线程"></a>12.1 什么是线程</h3><p>首先来看一个使用了两个线程的简单程序。这个程序可以在银行账户之间完成资金转账。我们使用了一个 <code>Bank</code> 类，它可以存储给定数量的账户的余额。<code>transfer</code> 方法将一定金额从一个账户转移到另一个账户。</p><p>在第一个线程中，我们将钱从账户 0 转移到账户 1。第二个线程将钱从账户 2 转移到账户 3。</p><p>下面是在一个单独的线程中运行一个任务的简单过程：</p><ol><li>将执行这个任务的代码放在一个类的 <code>run</code> 方法中，这个类要实现 <code>Runnable</code> 接口。</li></ol><p><code>Runnable</code> 接口只有一个方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">interface</span> <span class="hljs-title class_">Runnable</span> &#123;<br>    <span class="hljs-keyword">void</span> <span class="hljs-title function_">run</span><span class="hljs-params">()</span>;<br>&#125;<br></code></pre></td></tr></table></figure><p>由于 <code>Runnable</code> 是一个函数式接口，可以用一个 lambda 表达式创建一个实例：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">Runnable</span> <span class="hljs-variable">r</span> <span class="hljs-operator">=</span> () -&gt; &#123;<br>    <span class="hljs-comment">// task code</span><br>&#125;;<br></code></pre></td></tr></table></figure><ol start="2"><li>从这个 <code>Runnable</code> 构造一个 <code>Thread</code> 对象：</li></ol><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">var</span> <span class="hljs-variable">t</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Thread</span>(r);<br></code></pre></td></tr></table></figure><ol start="3"><li>启动线程：</li></ol><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">t.start();<br></code></pre></td></tr></table></figure><p>为了创建单独的线程来完成转账，我们只需要把转账代码放在一个 <code>Runnable</code> 的 <code>run</code> 方法中，然后启动一个线程：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">Runnable</span> <span class="hljs-variable">r</span> <span class="hljs-operator">=</span> () -&gt; &#123;<br>    <span class="hljs-keyword">try</span> &#123;<br>        <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>; i &lt; STEPS; i++) &#123;<br>            <span class="hljs-type">double</span> <span class="hljs-variable">amount</span> <span class="hljs-operator">=</span> MAX_AMOUNT * Math.random();<br>            bank.transfer(<span class="hljs-number">0</span>, <span class="hljs-number">1</span>, amount);<br>            Thread.sleep((<span class="hljs-type">int</span>) (DELAY * Math.random()));<br>        &#125;<br>    &#125; <span class="hljs-keyword">catch</span> (InterruptedException e) &#123;<br>        <span class="hljs-comment">// handle</span><br>    &#125;<br>&#125;;<br><span class="hljs-type">var</span> <span class="hljs-variable">t</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Thread</span>(r);<br>t.start();<br></code></pre></td></tr></table></figure><p>在一个 <code>for</code> 循环中（对于给定的步数 <code>STEPS</code>），这个线程会转账一个随机金额，然后休眠随机的延迟时间。</p><p>我们要捕获 <code>sleep</code> 方法有可能抛出的 <code>InterruptedException</code> 异常。一般来说，中断用来请求终止一个线程。相应地，出现 <code>InterruptedException</code> 时，<code>run</code> 方法会退出。</p><p>这个程序还会启动第二个线程，它从账户 2 向账户 3 转账。运行这个程序时，可以得到类似这样的输出：</p><figure class="highlight mathematica"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs mathematica"><span class="hljs-built_in">Thread</span><span class="hljs-punctuation">[</span><span class="hljs-built_in">Thread</span><span class="hljs-operator">-</span><span class="hljs-number">1</span><span class="hljs-operator">,</span><span class="hljs-number">5</span><span class="hljs-operator">,</span><span class="hljs-variable">main</span><span class="hljs-punctuation">]</span> <span class="hljs-number">606.77</span> <span class="hljs-variable">from</span> <span class="hljs-number">2</span> <span class="hljs-variable">to</span> <span class="hljs-number">3</span> <span class="hljs-built_in">Total</span> <span class="hljs-variable">Balance</span><span class="hljs-operator">:</span> <span class="hljs-number">400000.00</span><br><span class="hljs-built_in">Thread</span><span class="hljs-punctuation">[</span><span class="hljs-built_in">Thread</span><span class="hljs-operator">-</span><span class="hljs-number">0</span><span class="hljs-operator">,</span><span class="hljs-number">5</span><span class="hljs-operator">,</span><span class="hljs-variable">main</span><span class="hljs-punctuation">]</span> <span class="hljs-number">98.99</span> <span class="hljs-variable">from</span> <span class="hljs-number">0</span> <span class="hljs-variable">to</span> <span class="hljs-number">1</span> <span class="hljs-built_in">Total</span> <span class="hljs-variable">Balance</span><span class="hljs-operator">:</span> <span class="hljs-number">400000.00</span><br><span class="hljs-built_in">Thread</span><span class="hljs-punctuation">[</span><span class="hljs-built_in">Thread</span><span class="hljs-operator">-</span><span class="hljs-number">1</span><span class="hljs-operator">,</span><span class="hljs-number">5</span><span class="hljs-operator">,</span><span class="hljs-variable">main</span><span class="hljs-punctuation">]</span> <span class="hljs-number">476.78</span> <span class="hljs-variable">from</span> <span class="hljs-number">2</span> <span class="hljs-variable">to</span> <span class="hljs-number">3</span> <span class="hljs-built_in">Total</span> <span class="hljs-variable">Balance</span><span class="hljs-operator">:</span> <span class="hljs-number">400000.00</span><br><span class="hljs-built_in">Thread</span><span class="hljs-punctuation">[</span><span class="hljs-built_in">Thread</span><span class="hljs-operator">-</span><span class="hljs-number">0</span><span class="hljs-operator">,</span><span class="hljs-number">5</span><span class="hljs-operator">,</span><span class="hljs-variable">main</span><span class="hljs-punctuation">]</span> <span class="hljs-number">653.64</span> <span class="hljs-variable">from</span> <span class="hljs-number">0</span> <span class="hljs-variable">to</span> <span class="hljs-number">1</span> <span class="hljs-built_in">Total</span> <span class="hljs-variable">Balance</span><span class="hljs-operator">:</span> <span class="hljs-number">400000.00</span><br><span class="hljs-built_in">Thread</span><span class="hljs-punctuation">[</span><span class="hljs-built_in">Thread</span><span class="hljs-operator">-</span><span class="hljs-number">1</span><span class="hljs-operator">,</span><span class="hljs-number">5</span><span class="hljs-operator">,</span><span class="hljs-variable">main</span><span class="hljs-punctuation">]</span> <span class="hljs-number">807.14</span> <span class="hljs-variable">from</span> <span class="hljs-number">2</span> <span class="hljs-variable">to</span> <span class="hljs-number">3</span> <span class="hljs-built_in">Total</span> <span class="hljs-variable">Balance</span><span class="hljs-operator">:</span> <span class="hljs-number">400000.00</span><br><span class="hljs-built_in">Thread</span><span class="hljs-punctuation">[</span><span class="hljs-built_in">Thread</span><span class="hljs-operator">-</span><span class="hljs-number">0</span><span class="hljs-operator">,</span><span class="hljs-number">5</span><span class="hljs-operator">,</span><span class="hljs-variable">main</span><span class="hljs-punctuation">]</span> <span class="hljs-number">481.49</span> <span class="hljs-variable">from</span> <span class="hljs-number">0</span> <span class="hljs-variable">to</span> <span class="hljs-number">1</span> <span class="hljs-built_in">Total</span> <span class="hljs-variable">Balance</span><span class="hljs-operator">:</span> <span class="hljs-number">400000.00</span><br><span class="hljs-built_in">Thread</span><span class="hljs-punctuation">[</span><span class="hljs-built_in">Thread</span><span class="hljs-operator">-</span><span class="hljs-number">0</span><span class="hljs-operator">,</span><span class="hljs-number">5</span><span class="hljs-operator">,</span><span class="hljs-variable">main</span><span class="hljs-punctuation">]</span> <span class="hljs-number">203.73</span> <span class="hljs-variable">from</span> <span class="hljs-number">0</span> <span class="hljs-variable">to</span> <span class="hljs-number">1</span> <span class="hljs-built_in">Total</span> <span class="hljs-variable">Balance</span><span class="hljs-operator">:</span> <span class="hljs-number">400000.00</span><br><span class="hljs-built_in">Thread</span><span class="hljs-punctuation">[</span><span class="hljs-built_in">Thread</span><span class="hljs-operator">-</span><span class="hljs-number">1</span><span class="hljs-operator">,</span><span class="hljs-number">5</span><span class="hljs-operator">,</span><span class="hljs-variable">main</span><span class="hljs-punctuation">]</span> <span class="hljs-number">111.76</span> <span class="hljs-variable">from</span> <span class="hljs-number">2</span> <span class="hljs-variable">to</span> <span class="hljs-number">3</span> <span class="hljs-built_in">Total</span> <span class="hljs-variable">Balance</span><span class="hljs-operator">:</span> <span class="hljs-number">400000.00</span><br><span class="hljs-built_in">Thread</span><span class="hljs-punctuation">[</span><span class="hljs-built_in">Thread</span><span class="hljs-operator">-</span><span class="hljs-number">1</span><span class="hljs-operator">,</span><span class="hljs-number">5</span><span class="hljs-operator">,</span><span class="hljs-variable">main</span><span class="hljs-punctuation">]</span> <span class="hljs-number">794.88</span> <span class="hljs-variable">from</span> <span class="hljs-number">2</span> <span class="hljs-variable">to</span> <span class="hljs-number">3</span> <span class="hljs-built_in">Total</span> <span class="hljs-variable">Balance</span><span class="hljs-operator">:</span> <span class="hljs-number">400000.00</span><br><span class="hljs-operator">...</span><br></code></pre></td></tr></table></figure><p>可以看到，两个线程的输出是交错的，这说明它们在并发运行。实际上，两个输出行交错显示时，输出有时会有些混乱。</p><p>你要了解的就是这些！现在你已经知道了如何并发地运行任务。</p><p>这个程序的完整代码见程序清单 12-1。</p><blockquote><p><strong>注释</strong>：还可以通过建立 <code>Thread</code> 类的一个子类来定义线程，如下所示：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">class</span> <span class="hljs-title class_">MyThread</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Thread</span> &#123;<br> <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">run</span><span class="hljs-params">()</span> &#123;<br>     <span class="hljs-comment">// task code</span><br> &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>然后可以构造这个子类的一个对象，并调用它的 <code>start</code> 方法。不过，现在不再推荐这种方法。应当把要并行运行的任务与运行机制解耦合。如果有多个任务，为每个任务分别创建一个单独的线程开销太大。实际上，可以使用一个线程池，参见 12.6.2 节的介绍。</p></blockquote><blockquote><p> <strong>警告</strong>：不要调用 <code>Thread</code> 类或 <code>Runnable</code> 对象的 <code>run</code> 方法。**直接调用 <code>run</code> 方法只会在同一个线程中执行这个任务，而没有启动新的线程。**实际上，应当调用 <code>Thread.start</code> 方法，这会创建一个新线程来执行 <code>run</code> 方法。</p></blockquote><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br></pre></td><td class="code"><pre><code class="hljs java">程序清单 <span class="hljs-number">12</span>-<span class="hljs-number">1</span>  threads/ThreadTest.java<br><br> <span class="hljs-number">1</span>  <span class="hljs-keyword">package</span> threads;<br> <span class="hljs-number">2</span> <br> <span class="hljs-number">3</span>  <span class="hljs-comment">/**</span><br><span class="hljs-comment"> 4   * <span class="hljs-doctag">@version</span> 1.30 2004-08-01</span><br><span class="hljs-comment"> 5   * <span class="hljs-doctag">@author</span>  Cay Horstmann</span><br><span class="hljs-comment"> 6   */</span><br> <span class="hljs-number">7</span>  <span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">ThreadTest</span> &#123;<br> <span class="hljs-number">8</span>      <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">int</span> <span class="hljs-variable">DELAY</span>  <span class="hljs-operator">=</span> <span class="hljs-number">10</span>;<br> <span class="hljs-number">9</span>      <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">int</span> <span class="hljs-variable">STEPS</span>  <span class="hljs-operator">=</span> <span class="hljs-number">100</span>;<br><span class="hljs-number">10</span>      <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">double</span> <span class="hljs-variable">MAX_AMOUNT</span> <span class="hljs-operator">=</span> <span class="hljs-number">1000</span>;<br><span class="hljs-number">11</span> <br><span class="hljs-number">12</span>      <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> &#123;<br><span class="hljs-number">13</span>          <span class="hljs-type">var</span> <span class="hljs-variable">bank</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Bank</span>(<span class="hljs-number">4</span>, <span class="hljs-number">10000</span>);<br><span class="hljs-number">14</span> <br><span class="hljs-number">15</span>          <span class="hljs-type">Runnable</span> <span class="hljs-variable">task1</span> <span class="hljs-operator">=</span> () -&gt; &#123;<br><span class="hljs-number">16</span>              <span class="hljs-keyword">try</span> &#123;<br><span class="hljs-number">17</span>                  <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>; i &lt; STEPS; i++) &#123;<br><span class="hljs-number">18</span>                      <span class="hljs-type">double</span> <span class="hljs-variable">amount</span> <span class="hljs-operator">=</span> MAX_AMOUNT * Math.random();<br><span class="hljs-number">19</span>                      bank.transfer(<span class="hljs-number">0</span>, <span class="hljs-number">1</span>, amount);<br><span class="hljs-number">20</span>                      Thread.sleep((<span class="hljs-type">int</span>) (DELAY * Math.random()));<br><span class="hljs-number">21</span>                  &#125;<br><span class="hljs-number">22</span>              &#125; <span class="hljs-keyword">catch</span> (InterruptedException e) &#123;<br><span class="hljs-number">23</span>                  <span class="hljs-comment">// do nothing</span><br><span class="hljs-number">24</span>              &#125;<br><span class="hljs-number">25</span>          &#125;;<br><span class="hljs-number">26</span> <br><span class="hljs-number">27</span>          <span class="hljs-type">Runnable</span> <span class="hljs-variable">task2</span> <span class="hljs-operator">=</span> () -&gt; &#123;<br><span class="hljs-number">28</span>              <span class="hljs-keyword">try</span> &#123;<br><span class="hljs-number">29</span>                  <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>; i &lt; STEPS; i++) &#123;<br><span class="hljs-number">30</span>                      <span class="hljs-type">double</span> <span class="hljs-variable">amount</span> <span class="hljs-operator">=</span> MAX_AMOUNT * Math.random();<br><span class="hljs-number">31</span>                      bank.transfer(<span class="hljs-number">2</span>, <span class="hljs-number">3</span>, amount);<br><span class="hljs-number">32</span>                      Thread.sleep((<span class="hljs-type">int</span>) (DELAY * Math.random()));<br><span class="hljs-number">33</span>                  &#125;<br><span class="hljs-number">34</span>              &#125; <span class="hljs-keyword">catch</span> (InterruptedException e) &#123;<br><span class="hljs-number">35</span>                  <span class="hljs-comment">// do nothing</span><br><span class="hljs-number">36</span>              &#125;<br><span class="hljs-number">37</span>          &#125;;<br><span class="hljs-number">38</span> <br><span class="hljs-number">39</span>          <span class="hljs-keyword">new</span> <span class="hljs-title class_">Thread</span>(task1).start();<br><span class="hljs-number">40</span>          <span class="hljs-keyword">new</span> <span class="hljs-title class_">Thread</span>(task2).start();<br><span class="hljs-number">41</span>      &#125;<br><span class="hljs-number">42</span>  &#125;<br></code></pre></td></tr></table></figure><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br></pre></td><td class="code"><pre><code class="hljs java">程序清单 <span class="hljs-number">12</span>-<span class="hljs-number">2</span>  threads/Bank.java<br><br> <span class="hljs-number">1</span>  <span class="hljs-keyword">package</span> threads;<br> <span class="hljs-number">2</span> <br> <span class="hljs-number">3</span>  <span class="hljs-keyword">import</span> java.util.*;<br> <span class="hljs-number">4</span> <br> <span class="hljs-number">5</span>  <span class="hljs-comment">/**</span><br><span class="hljs-comment"> 6   * A bank with a number of bank accounts.</span><br><span class="hljs-comment"> 7   */</span><br> <span class="hljs-number">8</span>  <span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Bank</span> &#123;<br> <span class="hljs-number">9</span>      <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> <span class="hljs-type">double</span>[] accounts;<br><span class="hljs-number">10</span> <br><span class="hljs-number">11</span>      <span class="hljs-comment">/**</span><br><span class="hljs-comment">12       * Constructs the bank.</span><br><span class="hljs-comment">13       * <span class="hljs-doctag">@param</span> n             the number of accounts</span><br><span class="hljs-comment">14       * <span class="hljs-doctag">@param</span> initialBalance the initial balance for each account</span><br><span class="hljs-comment">15       */</span><br><span class="hljs-number">16</span>      <span class="hljs-keyword">public</span> <span class="hljs-title function_">Bank</span><span class="hljs-params">(<span class="hljs-type">int</span> n, <span class="hljs-type">double</span> initialBalance)</span> &#123;<br><span class="hljs-number">17</span>          accounts = <span class="hljs-keyword">new</span> <span class="hljs-title class_">double</span>[n];<br><span class="hljs-number">18</span>          Arrays.fill(accounts, initialBalance);<br><span class="hljs-number">19</span>      &#125;<br><span class="hljs-number">20</span> <br><span class="hljs-number">21</span>      <span class="hljs-comment">/**</span><br><span class="hljs-comment">22       * Transfers money from one account to another.</span><br><span class="hljs-comment">23       * <span class="hljs-doctag">@param</span> from   the account to transfer from</span><br><span class="hljs-comment">24       * <span class="hljs-doctag">@param</span> to     the account to transfer to</span><br><span class="hljs-comment">25       * <span class="hljs-doctag">@param</span> amount the amount to transfer</span><br><span class="hljs-comment">26       */</span><br><span class="hljs-number">27</span>      <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">transfer</span><span class="hljs-params">(<span class="hljs-type">int</span> from, <span class="hljs-type">int</span> to, <span class="hljs-type">double</span> amount)</span> &#123;<br><span class="hljs-number">28</span>          <span class="hljs-keyword">if</span> (accounts[from] &lt; amount) <span class="hljs-keyword">return</span>;<br><span class="hljs-number">29</span>          System.out.print(Thread.currentThread());<br><span class="hljs-number">30</span>          accounts[from] -= amount;<br><span class="hljs-number">31</span>          System.out.printf(<span class="hljs-string">&quot; %10.2f from %d to %d&quot;</span>, amount, from, to);<br><span class="hljs-number">32</span>          accounts[to] += amount;<br><span class="hljs-number">33</span>          System.out.printf(<span class="hljs-string">&quot; Total Balance: %10.2f%n&quot;</span>, getTotalBalance());<br><span class="hljs-number">34</span>      &#125;<br><span class="hljs-number">35</span> <br><span class="hljs-number">36</span>      <span class="hljs-comment">/**</span><br><span class="hljs-comment">37       * Gets the sum of all account balances.</span><br><span class="hljs-comment">38       * <span class="hljs-doctag">@return</span> the total balance</span><br><span class="hljs-comment">39       */</span><br><span class="hljs-number">40</span>      <span class="hljs-keyword">public</span> <span class="hljs-type">double</span> <span class="hljs-title function_">getTotalBalance</span><span class="hljs-params">()</span> &#123;<br><span class="hljs-number">41</span>          <span class="hljs-type">double</span> <span class="hljs-variable">sum</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>;<br><span class="hljs-number">42</span>          <span class="hljs-keyword">for</span> (<span class="hljs-type">double</span> a : accounts)<br><span class="hljs-number">43</span>              sum += a;<br><span class="hljs-number">44</span>          <span class="hljs-keyword">return</span> sum;<br><span class="hljs-number">45</span>      &#125;<br><span class="hljs-number">46</span> <br><span class="hljs-number">47</span>      <span class="hljs-comment">/**</span><br><span class="hljs-comment">48       * Gets the number of accounts in the bank.</span><br><span class="hljs-comment">49       * <span class="hljs-doctag">@return</span> the number of accounts</span><br><span class="hljs-comment">50       */</span><br><span class="hljs-number">51</span>      <span class="hljs-keyword">public</span> <span class="hljs-type">int</span> <span class="hljs-title function_">size</span><span class="hljs-params">()</span> &#123;<br><span class="hljs-number">52</span>          <span class="hljs-keyword">return</span> accounts.length;<br><span class="hljs-number">53</span>      &#125;<br><span class="hljs-number">54</span>  &#125;<br></code></pre></td></tr></table></figure><h3 id="12-2-线程状态"><a href="#12-2-线程状态" class="headerlink" title="12.2 线程状态"></a>12.2 线程状态</h3><p>线程可以有如下 6 种状态：</p><ul><li><strong>New（新建）</strong>  </li><li><strong>Runnable（可运行）</strong>  </li><li><strong>Blocked（阻塞）</strong>  </li><li><strong>Waiting（等待）</strong>  </li><li><strong>Timed waiting（计时等待）</strong>  </li><li><strong>Terminated（终止）</strong></li></ul><p>要确定一个线程的当前状态，只需要调用 <code>getState()</code> 方法。</p><h4 id="12-2-1-新建线程"><a href="#12-2-1-新建线程" class="headerlink" title="12.2.1 新建线程"></a>12.2.1 新建线程</h4><p>当用 <code>new</code> 操作符创建一个新线程时，如 <code>new Thread(r)</code>，这个线程还没有开始运行。这意味着它的状态是新建（New）。当一个线程处于新建状态时，程序还没有开始运行线程中的代码。线程可以运行之前还有一些基础工作要做。</p><h4 id="12-2-2-可运行线程"><a href="#12-2-2-可运行线程" class="headerlink" title="12.2.2 可运行线程"></a>12.2.2 可运行线程</h4><p>一旦调用 <code>start()</code> 方法，线程就处于<strong>可运行（Runnable）状态</strong>。<strong>一个可运行的线程可能正在运行，也可能没有运行</strong>——要由操作系统为线程提供具体的运行时间。（不过，Java 规范没有将“正在运行”作为一个单独的状态；一个正在运行的线程仍然处于可运行状态。）</p><p>**一旦一个线程开始运行，它不一定始终保持运行。**事实上，运行中的线程有时需要暂停，让其他线程有机会运行。线程调度的细节依赖于操作系统提供的服务。抢占式调度系统给每一个可运行线程一个时间片来执行任务。当时间片用完时，操作系统会剥夺该线程的运行权，并给另一个线程一个机会来运行（见图 12-2）。当选择下一个线程时，操作系统会考虑线程的优先级（priority）。</p><p>所有现代桌面和服务器操作系统都使用抢占式调度。但是，像手机这样的小型设备可能使用协作式调度。在这样的设备中，一个线程只有在调用 <code>yield</code> 方法或者被阻塞或等待时才失去控制权。</p><p>在有多个处理器的机器上，每个处理器可以运行一个线程，而且可以有多个线程并行运行。当然，如果线程数多于处理器的数目，调度器还是需要分配时间片。</p><p>一定要记住，在任何给定时刻，一个可运行的线程可能正在运行也可能没有运行（正是出于该原因，这个状态称为“可运行”而不是“正在运行”）。</p><blockquote><p>[!TIP]</p><p>API java.lang.Thread 1.0</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">yield</span><span class="hljs-params">()</span><br></code></pre></td></tr></table></figure><p>使当前正在执行的线程向另一个线程交出运行权。注意这是一个静态方法。</p></blockquote><h4 id="12-2-3-阻塞和等待线程"><a href="#12-2-3-阻塞和等待线程" class="headerlink" title="12.2.3 阻塞和等待线程"></a>12.2.3 阻塞和等待线程</h4><p>**当线程处于阻塞或等待状态时，它暂时是不活动的。它不执行任何代码，并且消耗最少的资源。**要由线程调度器重新激活这个线程。具体细节取决于它是怎样到达非活动状态的。</p><ul><li><p><strong>当一个线程试图获取一个内部的对象锁</strong>（而不是 <code>java.util.concurrent</code> 库中的 <code>Lock</code>），<strong>而这个锁目前被其他线程占有，该线程就会被阻塞</strong>。<strong>当所有其他线程都释放了这个锁，并且线程调度器允许该线程持有这个锁时，它将变成非阻塞状态。</strong></p></li><li><p>**当线程等待另一个线程通知调度器出现某个条件时，这个线程会进入等待状态。**调用 <code>Object.wait</code> 方法或 <code>Thread.join</code> 方法，或者是等待 <code>java.util.concurrent</code> 库中的 <code>Lock</code> 或 <code>Condition</code> 时，就会出现这种情况。实际上，阻塞状态与等待状态并没有太大区别。</p></li><li><p>有几个方法有超时参数，调用这些方法会让线程进入**计时等待（timed waiting）**状态。**这一状态将一直保持到超时期满或者接收到适当的通知。**带有超时参数的方法有 <code>Thread.sleep</code> 和计时版的 <code>Object.wait</code>、<code>Thread.join</code>、<code>Lock.tryLock</code> 以及 <code>Condition.await</code>。</p></li></ul><p>图 12-1 展示了线程可能的状态以及从一个状态到另一个状态可能的转换。当一个线程阻塞或等待时（或者终止时），可以调度另一个线程运行。当一个线程被重新激活（例如，因为超时期满或成功地获得了一个锁），调度器检查它是否具有比当前运行线程更高的优先级。如果是这样，调度器会剥夺某个当前运行线程的运行权，选择运行一个新线程。</p><h4 id="12-2-4-终止线程"><a href="#12-2-4-终止线程" class="headerlink" title="12.2.4 终止线程"></a>12.2.4 终止线程</h4><p>线程会由于以下两个原因之一而终止：</p><ul><li>由于 <code>run</code> 方法正常退出，线程自然终止。</li><li>因为一个没有捕获的异常终止了 <code>run</code> 方法，使线程意外终止。</li></ul><p><img src="/2025/05/04/hello-world/%E7%AC%AC%E5%8D%81%E4%BA%8C%E7%AB%A0/image-20251206102733689.png" alt="image-20251206102733689"></p><p>具体来说，可以调用线程的 <code>stop</code> 方法杀死一个线程。该方法抛出一个 <code>ThreadDeath</code> 错误对象，这会杀死线程。不过，<code>stop</code> 方法已经废弃，不要在你自己的代码中调用这个方法。</p><blockquote><p>[!TIP]</p><p>API java.lang.Thread 1.0  </p><p><code>void join()</code></p><p>​等待指定的线程终止。<br><code>void join(long millis)</code><br>​等待指定的线程终止或者等待经过指定的毫秒数。<br><code>Thread.State getState() 5</code><br>​得到这个线程的状态；取值为 NEW、RUNNABL、BLOCKED、WAITING、TIMED_WAITING 或 TERMINATED。<br><code>void stop()</code><br>​停止该线程。这个方法已经废弃。<br><code>void suspend()</code><br>​暂停这个线程的执行。这个方法已经废弃，将来会删除。<br><code>void resume()</code><br>​恢复线程。这个方法只能在调用 suspend() 之后使用。这个方法已经废弃，将来会删除。</p></blockquote><h3 id="12-3-线程属性"><a href="#12-3-线程属性" class="headerlink" title="12.3 线程属性"></a>12.3 线程属性</h3><p>下面几节将讨论线程的各种属性，包括中断的状态、守护线程、未捕获异常的处理器以及不应使用的一些遗留特性。</p><h4 id="12-3-1-中断线程"><a href="#12-3-1-中断线程" class="headerlink" title="12.3.1 中断线程"></a>12.3.1 中断线程</h4><p>当一个线程的 <code>run</code> 方法返回时（执行了方法体中最后一条语句后，执行 <code>return</code> 语句返回），或者如果出现方法中未捕获的异常，这个线程将终止。在 Java 的早期版本中，还有一个 <code>stop</code> 方法，其他线程可以调用这个方法来终止一个线程。但是，<strong>这个方法现在已经废弃</strong>。</p><p>除了已经废弃的 <code>stop</code> 方法，没有办法强制一个线程终止。不过，<code>interrupt</code> 方法可以用来请求终止一个线程。</p><p>当对一个线程调用 <code>interrupt</code> 方法时，就会设置线程的<strong>中断状态（interrupted status）</strong>。这是每个线程都有的一个 <code>boolean</code> 标志。各个线程都应该不时地检查这个标志，以判断线程是否被中断。</p><p>要确定是否设置了中断状态，首先调用静态方法 <code>Thread.currentThread()</code> 获得当前线程，然后调用 <code>isInterrupted()</code> 方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">while</span> (!Thread.currentThread().isInterrupted() &amp;&amp; more work to <span class="hljs-keyword">do</span>)<br>    <span class="hljs-keyword">do</span> more work<br></code></pre></td></tr></table></figure><p>但是，<strong>如果线程被阻塞，就无法检查中断状态</strong>。这里就要引入 <code>InterruptedException</code> 异常。在一个被 <code>sleep</code> 或 <code>wait</code> 调用阻塞的线程上调用 <code>interrupt</code> 方法时，那个阻塞调用（即 <code>sleep</code> 或 <code>wait</code> 调用）将被一个 <code>InterruptedException</code> 异常中断。（有一些阻塞 I&#x2F;O 调用不能被中断，对此应该考虑选择可中断的调用。）</p><p>Java 语言并没有要求中断的线程应当终止。中断一个线程只是要引起它的注意。被中断的线程可以决定如何响应中断。某些线程非常重要，所以应该处理这个异常，然后再继续执行。但是，更普遍的情况是，线程只希望将中断解释为一个终止请求。这种线程的 <code>run</code> 方法有如下形式：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">Runnable</span> <span class="hljs-variable">r</span> <span class="hljs-operator">=</span> () -&gt; &#123;<br>    <span class="hljs-keyword">try</span> &#123;<br>      <span class="hljs-keyword">while</span> (!Thread.currentThread().isInterrupted() &amp;&amp; more work to <span class="hljs-keyword">do</span>) &#123;<br>        <span class="hljs-comment">// do more work</span><br>    &#125;<br>&#125; <br>    <span class="hljs-keyword">catch</span> (InterruptedException e) &#123;<br>    <span class="hljs-comment">// thread was interrupted during sleep or wait</span><br>&#125; <br>    <span class="hljs-keyword">finally</span> &#123;<br>    <span class="hljs-comment">// cleanup, if required</span><br>&#125;<br><span class="hljs-comment">// exiting the run method terminates the thread</span><br><br>&#125;;<br></code></pre></td></tr></table></figure><p>如果在每次工作迭代之后都调用 <code>sleep</code> 方法（或者其他可中断方法），<code>isInterrupted</code> 检查既没有必要也没有用处。如果设置了中断状态，此时倘若调用 <code>sleep</code> 方法，它不会休眠。实际上，它会清除中断状态（ ！）并抛出 <code>InterruptedException</code>。因此，如果你的循环调用了 <code>sleep</code>，不要检查中断状态，而应当捕获 <code>InterruptedException</code> 异常，如下所示：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">Runnable</span> <span class="hljs-variable">r</span> <span class="hljs-operator">=</span> () -&gt; &#123;<br>    <span class="hljs-keyword">try</span> &#123;<br>        <span class="hljs-keyword">while</span> (more work to <span class="hljs-keyword">do</span>) &#123;<br>            <span class="hljs-comment">// do more work</span><br>            Thread.sleep(delay);<br>        &#125;<br>    &#125; <span class="hljs-keyword">catch</span> (InterruptedException e) &#123;<br>        <span class="hljs-comment">// thread was interrupted during sleep</span><br>    &#125; <span class="hljs-keyword">finally</span> &#123;<br>        <span class="hljs-comment">// cleanup, if required</span><br>    &#125;<br>    <span class="hljs-comment">// exiting the run method terminates the thread</span><br>&#125;;<br></code></pre></td></tr></table></figure><blockquote><p><strong>注释</strong>：有两个非常类似的方法，<code>interrupted</code> 和 <code>isInterrupted</code>。  <code>interrupted</code> 方法是一个<strong>静态方法，它检查当前线程是否被中断</strong>。而且，<strong>调用 <code>interrupted</code> 方法会清除该线程的中断状态</strong>。 另一方面，<code>isInterrupted</code> 方法是一个<strong>实例方法，可以用来检查是否有线程被中断。调用这个方法不会改变中断状态。</strong><br>你可能会发现以前发布的大量代码在底层抑制了 <code>InterruptedException</code> 异常，如下所示：</p></blockquote><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">void</span> <span class="hljs-title function_">mySubTask</span><span class="hljs-params">()</span> &#123;<br>    ...<br>    <span class="hljs-keyword">try</span> &#123;<br>        sleep(delay);<br>    &#125; <span class="hljs-keyword">catch</span> (InterruptedException e) &#123;<br>        <span class="hljs-comment">// don&#x27;t ignore!</span><br>    &#125;<br>    ...<br>&#125;<br></code></pre></td></tr></table></figure><p>不要这样做！如果想不出在 <code>catch</code> 子句中可以做什么有意义的工作，仍然有两个合理的选择：</p><ul><li>在 <code>catch</code> 子句中调用 <code>Thread.currentThread().interrupt()</code> 来设置中断状态。这样一来，调用者就可以检测中断状态。</li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">void</span> <span class="hljs-title function_">mySubTask</span><span class="hljs-params">()</span> &#123;<br>    ...<br>    <span class="hljs-keyword">try</span> &#123;<br>        sleep(delay);<br>    &#125; <span class="hljs-keyword">catch</span> (InterruptedException e) &#123;<br>        Thread.currentThread().interrupt();<br>    &#125;<br>    ...<br>&#125;<br></code></pre></td></tr></table></figure><ul><li>或者，更好的选择是，用 <code>throws InterruptedException</code> 标记你的方法，并去掉 <code>try</code> 语句块。这样一来，调用者（或者最终的 <code>run</code> 方法）就可以捕获这个异常。</li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">void</span> <span class="hljs-title function_">mySubTask</span><span class="hljs-params">()</span> <span class="hljs-keyword">throws</span> InterruptedException &#123;<br>    ...<br>    sleep(delay);<br>    ...<br>&#125;<br></code></pre></td></tr></table></figure><blockquote><p>[!TIP]</p><p>API java.lang.Thread 1.0</p><ul><li><p><code>void interrupt()</code></p><p>  向线程发送中断请求。线程的中断状态将被设置为 <code>true</code>。如果当前该线程被一个 <code>sleep</code> 调用阻塞，则抛出一个 <code>InterruptedException</code> 异常。</p></li><li><p><code>static boolean interrupted()</code></p><p>  测试当前线程（即正在执行这个指令的线程）是否被中断。注意，这是一个静态方法。这个调用有一个副作用——它会将当前线程的中断状态重置为 <code>false</code>。</p></li><li><p><code>boolean isInterrupted()</code></p><p>  测试一个线程是否被中断。与静态 <code>interrupted</code> 方法不同，这个调用不改变线程的中断状态。</p></li><li><p><code>static Thread currentThread()</code></p><p>  返回表示当前正在执行的线程的 <code>Thread</code> 对象。</p></li></ul></blockquote><h4 id="12-3-2-守护线程"><a href="#12-3-2-守护线程" class="headerlink" title="12.3.2 守护线程"></a>12.3.2 守护线程</h4><p>可以通过调用</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">t.setDaemon(<span class="hljs-literal">true</span>);<br></code></pre></td></tr></table></figure><p>将一个线程转换为<strong>守护线程（daemon thread）</strong>。守护线程并没有什么魔力，它的<strong>唯一用途是为其他线程提供服务</strong>。计时器线程就是一个例子，它定时地向其他线程发送“计时器嘀嗒”信号，另外清空过时缓存项的线程也是守护线程。<strong>只剩下守护线程时，虚拟机就会退出</strong>。因为如果只剩下守护线程，就没必要继续运行程序了。</p><blockquote><p>[!TIP]</p><p>API java.lang.Thread 1.0</p><p><code>void setDaemon(boolean isDaemon)</code></p><p><strong>标记该线程为守护线程或用户线程。这一方法必须在线程启动之前调用</strong>。</p></blockquote><h4 id="12-3-3-线程名"><a href="#12-3-3-线程名" class="headerlink" title="12.3.3 线程名"></a>12.3.3 线程名</h4><p>默认情况下，线程有容易记的名字，如 <code>Thread-2</code>。可以用 <code>setName</code> 方法为线程设置任何名字：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">var</span> <span class="hljs-variable">t</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Thread</span>(runnable);<br>t.setName(<span class="hljs-string">&quot;Web crawler&quot;</span>);<br></code></pre></td></tr></table></figure><p>这在线程转储时可能很有用。</p><h4 id="12-3-4-未捕获异常的处理器"><a href="#12-3-4-未捕获异常的处理器" class="headerlink" title="12.3.4 未捕获异常的处理器"></a>12.3.4 未捕获异常的处理器</h4><p><strong>线程的 <code>run</code> 方法不能抛出任何检查型异常</strong>，但是，非检查型异常可能会导致线程终止。在这种情况下，线程会死亡。</p><p>不过，对于可以传播的异常，并没有任何 <code>catch</code> 子句。实际上，在线程死亡之前，异常会传递到一个用于处理未捕获异常的处理器。</p><p>这个处理器必须属于一个实现了 <code>Thread.UncaughtExceptionHandler</code> 接口的类。这个接口只有一个方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">void</span> <span class="hljs-title function_">uncaughtException</span><span class="hljs-params">(Thread t, Throwable e)</span><br></code></pre></td></tr></table></figure><p>可以用 <code>setUncaughtExceptionHandler</code> 方法为任何线程安装一个处理器。也可以用 <code>Thread</code> 类的静态方法 <code>setDefaultUncaughtExceptionHandler</code> 为所有线程安装一个默认的处理器。替代处理器可以使用日志 API 将未捕获异常的报告发送到一个日志文件。</p><p>如果没有安装默认处理器，默认处理器则为 <code>null</code>。但是，如果没有为单个线程安装处理器，那么处理器就是该线程的 <code>ThreadGroup</code> 对象。</p><blockquote><p><strong>注释</strong>：**线程组是可以一起管理的线程的集合。**默认情况下，你创建的所有线程都属于同一个线程组，不过也可以建立其他线程组。由于现在引入了更好的特性来处理线程集合，所以建议不要在你自己的程序中使用线程组。</p></blockquote><p><code>ThreadGroup</code> 类实现了 <code>Thread.UncaughtExceptionHandler</code> 接口。它的 <code>uncaughtException</code> 方法执行以下操作：</p><ol><li>如果该线程组有父线程组，那么调用父线程组的 <code>uncaughtException</code> 方法。  </li><li>否则，如果 <code>Thread.getDefaultUncaughtExceptionHandler()</code> 方法返回一个非 <code>null</code> 的处理器，则调用该处理器。  </li><li>否则，如果 <code>Throwable</code> 是 <code>ThreadDeath</code> 的一个实例，什么都不做。  </li><li>否则，将线程的名字以及 <code>Throwable</code> 的栈轨迹输出到 <code>System.err</code>。</li></ol><p>你在程序中肯定看到过许多这样的栈轨迹。</p><blockquote><p>[!TIP]</p><p>API java.lang.Thread 1.0  </p><ul><li><p><code>static void setDefaultUncaughtExceptionHandler(Thread.UncaughtExceptionHandler handler)</code> 5  </p></li><li><p><code>static Thread.UncaughtExceptionHandler getDefaultUncaughtExceptionHandler()</code> 5</p><p>  设置或获得未捕获异常的默认处理器。  </p></li><li><p><code>void setUncaughtExceptionHandler(Thread.UncaughtExceptionHandler handler)</code> 5  </p></li><li><p><code>Thread.UncaughtExceptionHandler getUncaughtExceptionHandler()</code> 5</p><p>  设置或获得未捕获异常的处理器。如果没有安装处理器，则将线程组对象作为处理器。</p></li></ul><p>API java.lang.Thread.UncaughtExceptionHandler 5  </p><ul><li><p><code>void uncaughtException(Thread t, Throwable e)</code></p><p>  当线程因一个未捕获异常而终止时，要记录一个定制报告。</p></li></ul><p>API java.lang.ThreadGroup 1.0  </p><ul><li><p><code>void uncaughtException(Thread t, Throwable e)</code></p><p>  如果有父线程组，调用父线程组的这个方法；或者，如果有默认处理器，就调用 <code>Thread</code> 类的默认处理器；否则，将栈轨迹打印到标准错误流（不过，如果 <code>e</code> 是一个 <code>ThreadDeath</code> 对象，则会抑制栈轨迹。<code>ThreadDeath</code> 对象由已经废弃的 <code>stop</code> 方法生成）。</p></li></ul></blockquote><h4 id="12-3-5-线程优先级"><a href="#12-3-5-线程优先级" class="headerlink" title="12.3.5 线程优先级"></a>12.3.5 线程优先级</h4><p>在 Java 程序设计语言中，每一个线程有一个优先级。默认情况下，一个线程会继承构造它的那个线程的优先级。可以用 <code>setPriority</code> 方法提高或降低任何一个线程的优先级。可以将优先级设置为 <code>MIN_PRIORITY</code>（在 <code>Thread</code> 类中定义为 1）与 <code>MAX_PRIORITY</code>（定义为 10）之间的任何值。<code>NORM_PRIORITY</code> 定义为 5。</p><p>每当线程调度器有机会选择新线程时，它首先选择有较高优先级的线程。但是，线程优先级高度依赖于系统。当虚拟机依赖于主机平台的线程实现时，Java 线程的优先级会映射到主机平台的优先级，平台的线程优先级可能有更多级别，也可能更少。</p><p>例如，Windows 有 7 个优先级别。Java 的一些优先级会映射到相同的操作系统优先级。</p><p>在面向 Linux 的 Oracle JVM 中，会完全忽略线程优先级，即所有线程都有相同的优先级。</p><p>在没有使用操作系统线程的 Java 早期版本中，线程优先级可能很有用。不过现在不要使用线程优先级了。</p><blockquote><p>[!TIP]</p><p>API java.lang.Thread 1.0  </p><ul><li><p><code>void setPriority(int newPriority)</code></p><p>  设置这个线程的优先级。优先级必须在 <code>Thread.MIN_PRIORITY</code> 与 <code>Thread.MAX_PRIORITY</code> 之间。一般使用 <code>Thread.NORM_PRIORITY</code> 优先级。  </p></li><li><p><code>static int MIN_PRIORITY</code></p><p>  这是 <code>Thread</code> 可以有的最小优先级。最小优先级的值为 1。  </p></li><li><p><code>static int NORM_PRIORITY</code></p><p>  这是 <code>Thread</code> 的默认优先级。默认优先级为 5。  </p></li><li><p><code>static int MAX_PRIORITY</code></p><p>  这是 <code>Thread</code> 可以有的最大优先级。最大优先级的值为 10。</p></li></ul></blockquote><h3 id="12-4-同步"><a href="#12-4-同步" class="headerlink" title="12.4 同步"></a>12.4 同步</h3><p>在大多数实际的多线程应用中，<strong>两个或两个以上的线程需要共享存取相同的数据</strong>。如果两个线程存取同一个对象，并且每个线程分别调用了一个修改该对象状态的方法，会发生什么呢？可以想见，这两个线程会相互覆盖。取决于线程访问数据的次序，可能会导致对象被破坏。这种情况通常称为<strong>竞态条件（race condition）</strong>。</p><h4 id="12-4-1-竞态条件的一个例子"><a href="#12-4-1-竞态条件的一个例子" class="headerlink" title="12.4.1 竞态条件的一个例子"></a>12.4.1 竞态条件的一个例子</h4><p>为了避免多线程破坏共享数据，必须学习如何<strong>同步存取（synchronize the access）</strong>。在本节中，你会看到如果没有使用同步会发生什么。</p><p>在下面的测试程序中，还是考虑我们模拟的银行。与 12.1 节中的例子不同，我们要随机地选择从哪个源账户转账到哪个目标账户。由于这会产生问题，所以下面再来仔细查看 <code>Bank</code> 类 <code>transfer</code> 方法的代码。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">transfer</span><span class="hljs-params">(<span class="hljs-type">int</span> from, <span class="hljs-type">int</span> to, <span class="hljs-type">double</span> amount)</span> &#123;<br>    <span class="hljs-comment">// CAUTION: unsafe when called from multiple threads</span><br>    System.out.print(Thread.currentThread());<br>    accounts[from] -= amount;<br>    System.out.printf(<span class="hljs-string">&quot;%10.2f from %d to %d&quot;</span>, amount, from, to);<br>    accounts[to] += amount;<br>    System.out.printf(<span class="hljs-string">&quot; Total Balance: %10.2f%n&quot;</span>, getTotalBalance());<br>&#125;<br></code></pre></td></tr></table></figure><p>下面是 <code>Runnable</code> 实例的代码。<code>run</code> 方法不断地从一个给定银行账户取钱。在每次迭代中，<code>run</code> 方法选择一个随机的目标账户和一个随机金额，调用 <code>bank</code> 对象的 <code>transfer</code> 方法，然后休眠。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">Runnable</span> <span class="hljs-variable">r</span> <span class="hljs-operator">=</span> () -&gt; &#123;<br>    <span class="hljs-keyword">try</span> &#123;<br>        <span class="hljs-keyword">while</span> (<span class="hljs-literal">true</span>) &#123;<br>            <span class="hljs-type">int</span> <span class="hljs-variable">toAccount</span> <span class="hljs-operator">=</span> (<span class="hljs-type">int</span>) (bank.size() * Math.random());<br>            <span class="hljs-type">double</span> <span class="hljs-variable">amount</span> <span class="hljs-operator">=</span> MAX_AMOUNT * Math.random();<br>            bank.transfer(fromAccount, toAccount, amount);<br>            Thread.sleep((<span class="hljs-type">int</span>) (DELAY * Math.random()));<br>        &#125;<br>    &#125; <span class="hljs-keyword">catch</span> (InterruptedException e) &#123;<br>        <span class="hljs-comment">// 被中断即退出</span><br>    &#125;<br>&#125;;<br></code></pre></td></tr></table></figure><p>这个模拟程序运行时，我们不清楚在某一时刻某个银行账户中有多少钱，但是我们知道所有账户的总金额应该保持不变，因为我们所做的只是把钱从一个账户转移到另一个账户。每一次交易结束时，<code>transfer</code> 方法会重新计算总金额并打印出来。</p><p>这个程序永远不会结束。只能按下组合键 <code>Ctrl+C</code> 来终止这个程序。</p><p>下面是典型的输出：</p><figure class="highlight apache"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><code class="hljs apache"><span class="hljs-attribute">Thread</span>[Thread-<span class="hljs-number">11</span>,<span class="hljs-number">5</span>,main]  <span class="hljs-number">588</span>.<span class="hljs-number">48</span> from <span class="hljs-number">11</span> to <span class="hljs-number">44</span> Total Balance: <span class="hljs-number">100000</span>.<span class="hljs-number">00</span><br><span class="hljs-attribute">Thread</span>[Thread-<span class="hljs-number">12</span>,<span class="hljs-number">5</span>,main]  <span class="hljs-number">976</span>.<span class="hljs-number">11</span> from <span class="hljs-number">12</span> to <span class="hljs-number">22</span> Total Balance: <span class="hljs-number">100000</span>.<span class="hljs-number">00</span><br><span class="hljs-attribute">Thread</span>[Thread-<span class="hljs-number">14</span>,<span class="hljs-number">5</span>,main]  <span class="hljs-number">521</span>.<span class="hljs-number">51</span> from <span class="hljs-number">14</span> to <span class="hljs-number">22</span> Total Balance: <span class="hljs-number">100000</span>.<span class="hljs-number">00</span><br><span class="hljs-attribute">Thread</span>[Thread-<span class="hljs-number">13</span>,<span class="hljs-number">5</span>,main]  <span class="hljs-number">359</span>.<span class="hljs-number">89</span> from <span class="hljs-number">13</span> to <span class="hljs-number">81</span> Total Balance: <span class="hljs-number">100000</span>.<span class="hljs-number">00</span><br><span class="hljs-attribute">Thread</span>[Thread-<span class="hljs-number">36</span>,<span class="hljs-number">5</span>,main]  <span class="hljs-number">401</span>.<span class="hljs-number">71</span> from <span class="hljs-number">36</span> to <span class="hljs-number">73</span> Total Balance:  <span class="hljs-number">99291</span>.<span class="hljs-number">06</span><br><span class="hljs-attribute">Thread</span>[Thread-<span class="hljs-number">35</span>,<span class="hljs-number">5</span>,main]  <span class="hljs-number">691</span>.<span class="hljs-number">46</span> from <span class="hljs-number">35</span> to <span class="hljs-number">77</span> Total Balance:  <span class="hljs-number">99291</span>.<span class="hljs-number">06</span><br><span class="hljs-attribute">Thread</span>[Thread-<span class="hljs-number">37</span>,<span class="hljs-number">5</span>,main]   <span class="hljs-number">78</span>.<span class="hljs-number">64</span> from <span class="hljs-number">37</span> to  <span class="hljs-number">3</span> Total Balance:  <span class="hljs-number">99291</span>.<span class="hljs-number">06</span><br><span class="hljs-attribute">Thread</span>[Thread-<span class="hljs-number">34</span>,<span class="hljs-number">5</span>,main]  <span class="hljs-number">197</span>.<span class="hljs-number">11</span> from <span class="hljs-number">34</span> to <span class="hljs-number">69</span> Total Balance:  <span class="hljs-number">99291</span>.<span class="hljs-number">06</span><br><span class="hljs-attribute">Thread</span>[Thread-<span class="hljs-number">36</span>,<span class="hljs-number">5</span>,main]   <span class="hljs-number">85</span>.<span class="hljs-number">96</span> from <span class="hljs-number">36</span> to  <span class="hljs-number">4</span> Total Balance: -<span class="hljs-number">99291</span>.<span class="hljs-number">06</span><br><span class="hljs-attribute">Thread</span>[Thread- <span class="hljs-number">4</span>,<span class="hljs-number">5</span>,main] Thread[Thread-<span class="hljs-number">33</span>,<span class="hljs-number">5</span>,main]<br>    <span class="hljs-attribute">7</span>.<span class="hljs-number">31</span> from <span class="hljs-number">31</span> to <span class="hljs-number">32</span> Total Balance:  <span class="hljs-number">99979</span>.<span class="hljs-number">24</span><br>  <span class="hljs-attribute">627</span>.<span class="hljs-number">50</span> from  <span class="hljs-number">4</span> to  <span class="hljs-number">5</span> Total Balance:  <span class="hljs-number">99979</span>.<span class="hljs-number">24</span><br></code></pre></td></tr></table></figure><p>可以看到，这里出现了错误。对于最初的几次交易，银行余额保持在 $100 000，这是正确的，因为共 100 个账户，每个账户 $1000。不过，经过一段时间后，余额有细微的变化。运行这个程序的时候，可能很快就能发现出错了，有时则可能需要很长的时间才能发现余额不对。这种情况很影响人们的信任，你可能不希望将辛苦挣来的钱存进这样一个银行。</p><p>看你能不能找出程序清单 12-3 和程序清单 12-2 中 <code>Bank</code> 类的问题。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br></pre></td><td class="code"><pre><code class="hljs java">程序清单 <span class="hljs-number">12</span>-<span class="hljs-number">3</span>  unsynch/UnsynchBankTest.java<br><br> <span class="hljs-number">1</span>  <span class="hljs-keyword">package</span> unsynch;<br> <span class="hljs-number">2</span> <br> <span class="hljs-number">3</span>  <span class="hljs-comment">/**</span><br><span class="hljs-comment"> 4   * This program shows data corruption when multiple threads access a data structure.</span><br><span class="hljs-comment"> 5   * <span class="hljs-doctag">@version</span> 1.32 2018-04-10</span><br><span class="hljs-comment"> 6   * <span class="hljs-doctag">@author</span>  Cay Horstmann</span><br><span class="hljs-comment"> 7   */</span><br> <span class="hljs-number">8</span>  <span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">UnsynchBankTest</span> &#123;<br> <span class="hljs-number">9</span>      <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">int</span> <span class="hljs-variable">NACCOUNTS</span> <span class="hljs-operator">=</span> <span class="hljs-number">100</span>;<br><span class="hljs-number">10</span>      <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">double</span> <span class="hljs-variable">INITIAL_BALANCE</span> <span class="hljs-operator">=</span> <span class="hljs-number">1000</span>;<br><span class="hljs-number">11</span>      <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">double</span> <span class="hljs-variable">MAX_AMOUNT</span> <span class="hljs-operator">=</span> <span class="hljs-number">1000</span>;<br><span class="hljs-number">12</span>      <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">int</span> <span class="hljs-variable">DELAY</span> <span class="hljs-operator">=</span> <span class="hljs-number">10</span>;<br><span class="hljs-number">13</span> <br><span class="hljs-number">14</span>      <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> &#123;<br><span class="hljs-number">15</span>          <span class="hljs-type">var</span> <span class="hljs-variable">bank</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Bank</span>(NACCOUNTS, INITIAL_BALANCE);<br><span class="hljs-number">16</span> <br><span class="hljs-number">17</span>          <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>; i &lt; NACCOUNTS; i++) &#123;<br><span class="hljs-number">18</span>              <span class="hljs-type">int</span> <span class="hljs-variable">fromAccount</span> <span class="hljs-operator">=</span> i;<br><span class="hljs-number">19</span>              <span class="hljs-type">Runnable</span> <span class="hljs-variable">r</span> <span class="hljs-operator">=</span> () -&gt; &#123;<br><span class="hljs-number">20</span>                  <span class="hljs-keyword">try</span> &#123;<br><span class="hljs-number">21</span>                      <span class="hljs-keyword">while</span> (<span class="hljs-literal">true</span>) &#123;<br><span class="hljs-number">22</span>                          <span class="hljs-type">int</span> <span class="hljs-variable">toAccount</span> <span class="hljs-operator">=</span> (<span class="hljs-type">int</span>) (bank.size() * Math.random());<br><span class="hljs-number">23</span>                          <span class="hljs-type">double</span> <span class="hljs-variable">amount</span> <span class="hljs-operator">=</span> MAX_AMOUNT * Math.random();<br><span class="hljs-number">24</span>                          bank.transfer(fromAccount, toAccount, amount);<br><span class="hljs-number">25</span>                          Thread.sleep((<span class="hljs-type">int</span>) (DELAY * Math.random()));<br><span class="hljs-number">26</span>                      &#125;<br><span class="hljs-number">27</span>                  &#125; <span class="hljs-keyword">catch</span> (InterruptedException e) &#123;<br><span class="hljs-number">28</span>                      <span class="hljs-comment">// 被中断即退出</span><br><span class="hljs-number">29</span>                  &#125;<br><span class="hljs-number">30</span>              &#125;;<br><span class="hljs-number">31</span>              <span class="hljs-type">var</span> <span class="hljs-variable">t</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Thread</span>(r);<br><span class="hljs-number">32</span>              t.start();<br><span class="hljs-number">33</span>          &#125;<br><span class="hljs-number">34</span>      &#125;<br><span class="hljs-number">35</span>  &#125;<br></code></pre></td></tr></table></figure><h4 id="12-4-2-竞态条件详解"><a href="#12-4-2-竞态条件详解" class="headerlink" title="12.4.2 竞态条件详解"></a>12.4.2 竞态条件详解</h4><p>12.4.1 节中运行了一个程序，其中有多个线程更新银行账户余额。一段时间之后，不知不觉地出现了错误，可能有些钱会丢失，也可能凭空有钱进账。当两个线程试图同时更新同一个账户时，就会出现这个问题。假设两个线程同时执行指令：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">accounts[to] += amount;<br></code></pre></td></tr></table></figure><p>问题在于这不是原子操作。这个指令可能如下处理：</p><ol><li>将 <code>accounts[to]</code> 加载到寄存器。  </li><li>增加 <code>amount</code>。  </li><li>将结果写回 <code>accounts[to]</code>。</li></ol><p>现在，假定第 1 个线程执行步骤 1 和步骤 2，然后，它的运行权被抢占。再假设第 2 个线程被唤醒，更新 <code>account</code> 数组中的同一个元素。然后，第 1 个线程被唤醒并完成其第 3 步。这个动作会抹去第 2 个线程所做的修改。这样一来，总金额就不再正确了（见图 12-2）。</p><p><img src="/2025/05/04/hello-world/%E7%AC%AC%E5%8D%81%E4%BA%8C%E7%AB%A0/image-20251206155701809.png" alt="image-20251206155701809"></p><p>我们的测试程序可以检测到这种破坏。（当然，如果线程在完成测试时被中断，尽管概率很小，不过确实有可能出现误报！）</p><blockquote><p><strong>注释</strong>：实际上可以查看执行这个类中每一个语句的虚拟机字节码。运行以下命令  </p><figure class="highlight ebnf"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs ebnf"><span class="hljs-attribute">javap -c -v Bank</span><br></code></pre></td></tr></table></figure><p>对 <code>Bank.class</code> 文件进行反编译。例如，以下代码行  </p><figure class="highlight applescript"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs applescript">accounts[<span class="hljs-keyword">to</span>] += amount;<br></code></pre></td></tr></table></figure><p>会转换为下面的字节码： </p><figure class="highlight 1c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs 1c">aload_0<br>getfield <span class="hljs-meta">#2; <span class="hljs-comment">// Field accounts:[D</span></span><br>iload_2<br>dup2<br>daload<br>dload <span class="hljs-number">3</span><br>dadd<br>dastore<br></code></pre></td></tr></table></figure><p>这些代码的含义无关紧要。重要的是这个自增命令是由多条指令组成的，执行这些指令的线程有可能在任何一条指令上被中断。</p></blockquote><p>出现这种破坏的可能性有多大呢？在一个有多个内核的现代处理器上，出问题的风险相当高。我们将交错执行打印语句和更新余额的语句，以提高在单核处理器上观察到这种问题的概率。</p><p>如果删除打印语句，出问题的风险会降低，因为每个线程在再次休眠之前所做的工作很少，调度器不太可能在线程的计算过程中间抢占它的运行权。但是，产生破坏的风险并没有完全消失。如果在负载很重的机器上运行大量线程，那么，即使删除了打印语句，程序依然会出错。这种错误可能几分钟、几小时或几天后才出现。坦白地说，对程序员而言，最糟糕的事情莫过于这种不定期地出现错误。</p><p>真正的问题是 <code>transfer</code> 方法可能会在执行到中间时被中断。如果能够确保线程失去控制权之前方法已经运行完成，那么银行账户对象的状态就不会被破坏。</p><h4 id="12-4-3-锁对象"><a href="#12-4-3-锁对象" class="headerlink" title="12.4.3 锁对象"></a>12.4.3 锁对象</h4><p>有两种机制可防止并发访问同一个代码块。Java 语言为此提供了一个 <code>synchronized</code> 关键字，另外 Java 5 引入了 <code>ReentrantLock</code> 类。<strong><code>synchronized</code> 关键字会自动提供一个锁以及相关的“条件”</strong>，对于大多数需要显式锁的情况，这种机制功能很强大，也很便利。不过，我们相信在分别了解锁和条件的内容之后，能更容易地理解 <code>synchronized</code> 关键字。<code>java.util.concurrent</code> 框架为这些基础机制提供了单独的类。</p><p>用 <code>ReentrantLock</code> 保护代码块的基本结构如下：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs java">myLock.lock(); <span class="hljs-comment">// a ReentrantLock object</span><br><span class="hljs-keyword">try</span> &#123;<br>    <span class="hljs-comment">// critical section</span><br>&#125; <span class="hljs-keyword">finally</span> &#123;<br>    myLock.unlock(); <span class="hljs-comment">// make sure the Lock is unlocked even if an exception is thrown</span><br>&#125;<br></code></pre></td></tr></table></figure><p><strong>这个结构确保任何时刻只有一个线程进入临界区。一旦一个线程锁定了锁对象，任何其他线程都无法通过 <code>lock</code> 语句。当其他线程调用 <code>lock</code> 时，它们会暂停，直到第一个线程释放这个锁对象。</strong></p><blockquote><p><strong>警告</strong>：<strong>要把 <code>unlock</code> 操作包在 <code>finally</code> 子句中</strong>，这一点至关重要。如果临界区中的代码抛出一个异常，必须释放锁。否则，其他线程将永远阻塞。</p></blockquote><blockquote><p><strong>注释</strong>：<strong>使用锁时，就不能使用 <code>try-with-resources</code> 语句</strong>。首先，解锁方法名不是 <code>close</code>。不过，即使将它重命名（例如，重命名为 <code>close</code>），<code>try-with-resources</code> 语句也无法正常工作。它的首部希望声明一个新变量。但是如果使用一个锁，你可能想使用由多个线程共享的同一个变量，而不是使用一个新变量。</p></blockquote><p>下面使用一个锁来保护 <code>Bank</code> 类的 <code>transfer</code> 方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Bank</span> &#123;<br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> <span class="hljs-type">Lock</span> <span class="hljs-variable">bankLock</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">ReentrantLock</span>();<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">transfer</span><span class="hljs-params">(<span class="hljs-type">int</span> from, <span class="hljs-type">int</span> to, <span class="hljs-type">int</span> amount)</span> &#123;<br>        bankLock.lock();<br>        <span class="hljs-keyword">try</span> &#123;<br>            System.out.print(Thread.currentThread());<br>            accounts[from] -= amount;<br>            System.out.printf(<span class="hljs-string">&quot;%10.2f from %d to %d&quot;</span>, amount, from, to);<br>            accounts[to] += amount;<br>            System.out.printf(<span class="hljs-string">&quot; Total Balance: %10.2f%n&quot;</span>, getTotalBalance());<br>        &#125; <span class="hljs-keyword">finally</span> &#123;<br>            bankLock.unlock();<br>        &#125;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p><strong>假设一个线程调用了 <code>transfer</code>，但是在执行结束前被抢占。再假设第二个线程也调用了 <code>transfer</code>，由于第二个线程不能获得锁，将在调用 <code>lock</code> 方法时被阻塞。它会暂停，必须等待第一个线程执行完 <code>transfer</code> 方法。当第一个线程释放锁时，第二个线程才能开始运行</strong>（见图 12-3）。</p><p>尝试一下。把加锁代码增加到 <code>transfer</code> 方法并再次运行程序。这个程序可以一直运行下去，银行余额绝对不会有错误。</p><p>注意每个 <code>Bank</code> 对象都有自己的 <code>ReentrantLock</code> 对象。如果两个线程试图访问同一个 <code>Bank</code> 对象，那么锁可以用来保证串行化访问。不过，**如果两个线程访问不同的 <code>Bank</code> 对象，每个线程会得到不同的锁对象，两个线程都不会阻塞。**本该如此，因为线程在处理不同的 <code>Bank</code> 实例时，线程之间不会相互影响。</p><p>这个锁称为<strong>重入（reentrant）锁</strong>，因为线程可以反复获得已拥有的锁。锁有一个持有计数（hold count）来跟踪对 <code>lock</code> 方法的嵌套调用。线程每一次调用 <code>lock</code> 后都要调用 <code>unlock</code> 来释放锁。由于这个特性，由一个锁保护的代码可以调用另一个同样使用这个锁的方法。</p><p>例如，<code>transfer</code> 方法调用 <code>getTotalBalance</code> 方法，这也会锁定 <code>bankLock</code> 对象，此时 <code>bankLock</code> 对象的持有计数为 2。当 <code>getTotalBalance</code> 方法退出时，持有计数变回 1。当 <code>transfer</code> 方法退出的时候，持有计数变为 0，线程释放锁。</p><p><img src="/2025/05/04/hello-world/%E7%AC%AC%E5%8D%81%E4%BA%8C%E7%AB%A0/image-20251206160211477.png" alt="image-20251206160211477"></p><p>通常我们可能希望保护会更新或检查共享对象的代码块，从而能确信当前操作执行完之后其他线程才能使用同一个对象。</p><blockquote><p><strong>警告</strong>：要注意确保不能由于抛出异常而绕过临界区中的代码。如果在临界区代码结束之前抛出了异常，<code>finally</code> 子句将释放锁，但是对象可能处于被破坏的状态。</p></blockquote><blockquote><p>[!TIP]</p><p>API java.util.concurrent.locks.Lock 5  </p><ul><li><p><code>void lock()</code></p><p>  获得这个锁；如果锁当前被另一个线程占有，则阻塞。  </p></li><li><p><code>void unlock()</code></p><p>  释放这个锁。</p></li></ul><p>API java.util.concurrent.locks.ReentrantLock 5  </p><ul><li><p><code>ReentrantLock()</code></p><p>  构造一个重入锁，可以用来保护一个临界区。</p><ul><li><p><code>ReentrantLock(boolean fair)</code></p><p>  构造一个采用公平策略的锁。一个公平锁倾向于等待时间最长的线程。不过，这种公平保证可能严重影响性能。所以，默认情况下，不要求锁是公平的。</p></li></ul></li></ul></blockquote><blockquote><p><strong>警告</strong>：听起来公平锁很不错，但是公平锁要比常规锁慢得多。只有当你确实了解自己要做什么，而且对于你要解决的问题，有一个特定的理由确实要考虑公平性时，才应使用公平锁。即使使用公平锁，也不能保证线程调度器是公平的。如果线程调度器选择忽略一个已经为锁等待很长时间的线程，它就没有机会得到锁的公平处理。</p></blockquote><h4 id="12-4-4-条件对象"><a href="#12-4-4-条件对象" class="headerlink" title="12.4.4 条件对象"></a>12.4.4 条件对象</h4><p>通常，线程进入临界区后却发现只有满足了某个条件之后它才能执行。可以使用一个<strong>条件对象（condition object）<strong>来</strong>管理那些已经获得了一个锁却不能有效工作的线程。<strong>在这一节中，我们会介绍 Java 库中条件对象的实现。由于历史原因，条件对象经常被称为</strong>条件变量（conditional variable）</strong>。</p><p>现在来优化银行的模拟程序。如果一个账户没有足够的资金用于转账，则我们不希望从这样的账户转出资金。注意不能使用类似下面的代码：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">if</span> (bank.getBalance(from) &gt;= amount)<br>    bank.transfer(from, to, amount);<br></code></pre></td></tr></table></figure><p>在成功地通过这个测试之后，但在调用 <code>transfer</code> 方法之前，当前线程完全有可能被中断：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">if</span> (bank.getBalance(from) &gt;= amount)<br>    <span class="hljs-comment">// thread might be deactivated at this point</span><br>    bank.transfer(from, to, amount);<br></code></pre></td></tr></table></figure><p>在这个线程再次运行时，账户余额可能已经低于提款金额。必须确保在检查余额与转账动作之间没有其他线程修改余额。为此，可以使用一个锁来保护这个测试和转账动作：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">transfer</span><span class="hljs-params">(<span class="hljs-type">int</span> from, <span class="hljs-type">int</span> to, <span class="hljs-type">int</span> amount)</span> &#123;<br>    bankLock.lock();<br>    <span class="hljs-keyword">try</span> &#123;<br>        <span class="hljs-keyword">while</span> (accounts[from] &lt; amount)&#123;<br>            <span class="hljs-comment">// wait</span><br>            ...<br>    &#125;<br>        <span class="hljs-comment">// transfer funds</span><br>        ...<br>    &#125; <span class="hljs-keyword">finally</span> &#123;<br>        bankLock.unlock();<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>现在，当账户中没有足够的资金时，我们会做什么呢？我们会等待，直到另一个线程增加了该账户的资金。但是，这个线程刚刚获得了对 <code>bankLock</code> 的独占访问权，因此别的线程没有存款的机会。这里就要引入条件对象。</p><p>**一个锁对象可以有一个或多个关联的条件对象。**可以用 <code>newCondition</code> 方法获得一个条件对象。习惯上会给每个条件对象一个合适的名字来反映它表示的条件。例如，在这里我们建立了一个条件对象来表示“资金充足”条件：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Bank</span> &#123;<br>    <span class="hljs-keyword">private</span> Condition sufficientFunds;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-title function_">Bank</span><span class="hljs-params">()</span> &#123;<br>        sufficientFunds = bankLock.newCondition();<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>如果 <code>transfer</code> 方法发现资金不足，它会调用：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">sufficientFunds.await();<br></code></pre></td></tr></table></figure><p>当前线程现在暂停，并放弃锁。这允许另一个线程执行，我们希望它能增加账户余额。</p><p>等待获得锁的线程和调用了 <code>await</code> 方法的线程存在本质上的不同。一旦一个线程调用了 <code>await</code> 方法，它就进入这个条件的<strong>等待集（wait set</strong>）。**当锁可用时，该线程并不会变为可运行状态。实际上，它仍保持非活动状态，**直到另一个线程在同一条件上调用 <code>signalAll()</code> 方法。</p><p>当另一个线程完成转账时，它应该调用：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">sufficientFunds.signalAll();<br></code></pre></td></tr></table></figure><p>**这个调用会重新激活等待这个条件的所有线程。当这些线程从等待集中移出时，它们再次变为可运行状态，调度器最终将它们再次激活。**同时，它们会尝试重新进入该对象。一旦锁可用，它们中的某个线程将从 <code>await</code> 调用返回，得到这个锁，并从之前暂停的地方继续执行。</p><p>此时，线程应当再次测试条件。不能保证现在一定满足条件——<code>signalAll()</code> 方法仅仅是通知等待的线程：<strong>现在有可能满足条件，有必要再次检查条件</strong>。</p><blockquote><p><strong>注释</strong>：通常，<code>await</code> 调用应该放在如下形式的一个循环中：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">while</span> (!(OK to proceed))<br> condition.await();<br></code></pre></td></tr></table></figure></blockquote><p>最终需要有某个其他线程调用 <code>signalAll()</code> 方法，这一点至关重要。当一个线程调用 <code>await</code> 时，它没有办法自行重新激活。它寄希望于其他线程。如果没有其他线程来重新激活这个等待的线程，它就再也不能运行了。这将导致令人不快的<strong>死锁（deadlock）</strong> 现象。如果所有其他线程都被阻塞，最后一个活动线程调用了 <code>await</code> 方法但没有先解除另外某个线程的阻塞，现在这个线程也会阻塞。此时没有线程可以解除其他线程的阻塞状态，程序会永远挂起。</p><p>应该什么时候调用 <code>signalAll()</code> 呢？从经验上讲，只要一个对象的状态有变化，而且可能有利于正在等待的线程，就可以调用 <code>signalAll()</code>。例如，当一个账户余额发生改变时，就应该再给等待的线程一个机会来检查余额。在这个例子中，完成转账时，我们就会调用 <code>signalAll()</code> 方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">transfer</span><span class="hljs-params">(<span class="hljs-type">int</span> from, <span class="hljs-type">int</span> to, <span class="hljs-type">int</span> amount)</span> &#123;<br>    bankLock.lock();<br>    <span class="hljs-keyword">try</span> &#123;<br>        <span class="hljs-keyword">while</span> (accounts[from] &lt; amount)<br>            sufficientFunds.await();<br>        <span class="hljs-comment">// transfer funds</span><br>        sufficientFunds.signalAll();<br>    &#125; <span class="hljs-keyword">finally</span> &#123;<br>        bankLock.unlock();<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p><strong>注意</strong>：<strong><code>signalAll()</code> 调用不会立即激活一个等待的线程。它只是解除等待线程的阻塞，使这些线程可以在当前线程释放锁之后竞争访问对象。</strong></p><p><strong>另一个方法 <code>signal()</code> 只是随机选择等待集中的一个线程，并解除这个线程的阻塞状态</strong>。这比解除所有线程的阻塞更高效，但也存在危险。如果随机选择的线程发现自己仍然不能运行，它就会再次阻塞。如果没有其他线程再次调用 <code>signal()</code>，系统就会进入死锁。</p><blockquote><p><strong>警告</strong>：<strong>只有当线程拥有一个条件的锁时，它才能在这个条件上调用 <code>await()</code>、<code>signalAll()</code> 或 <code>signal()</code> 方法。</strong></p></blockquote><p>如果运行程序清单 12-4 中的程序，你会注意到不再有任何错误。总余额永远是 $100000。任何账户都不会出现负的余额（同样地，还是需要按下组合键 <code>Ctrl+C</code> 来终止程序）。你可能还会注意到，这个程序运行起来要慢一些——这是为实现同步机制所涉及的额外工作付出的代价。</p><p>实际上，正确使用条件很有挑战性。开始实现你自己的条件对象之前，应该考虑使用 12.5 节中描述的某个结构。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br></pre></td><td class="code"><pre><code class="hljs java">程序清单 <span class="hljs-number">12</span>-<span class="hljs-number">4</span>  synch/Bank.java<br><br> <span class="hljs-number">1</span>  <span class="hljs-keyword">package</span> synch;<br> <span class="hljs-number">2</span> <br> <span class="hljs-number">3</span>  <span class="hljs-keyword">import</span> java.util.*;<br> <span class="hljs-number">4</span>  <span class="hljs-keyword">import</span> java.util.concurrent.locks.*;<br> <span class="hljs-number">5</span> <br> <span class="hljs-number">6</span>  <span class="hljs-comment">/**</span><br><span class="hljs-comment"> 7   * A bank with a number of bank accounts that uses locks for serializing access.</span><br><span class="hljs-comment"> 8   */</span><br> <span class="hljs-number">9</span>  <span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Bank</span> &#123;<br><span class="hljs-number">10</span>      <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> <span class="hljs-type">double</span>[] accounts;<br><span class="hljs-number">11</span>      <span class="hljs-keyword">private</span> Lock bankLock;<br><span class="hljs-number">12</span>      <span class="hljs-keyword">private</span> Condition sufficientFunds;<br><span class="hljs-number">13</span> <br><span class="hljs-number">14</span>      <span class="hljs-comment">/**</span><br><span class="hljs-comment">15       * Constructs the bank.</span><br><span class="hljs-comment">16       * <span class="hljs-doctag">@param</span> n             the number of accounts</span><br><span class="hljs-comment">17       * <span class="hljs-doctag">@param</span> initialBalance the initial balance for each account</span><br><span class="hljs-comment">18       */</span><br><span class="hljs-number">19</span>      <span class="hljs-keyword">public</span> <span class="hljs-title function_">Bank</span><span class="hljs-params">(<span class="hljs-type">int</span> n, <span class="hljs-type">double</span> initialBalance)</span> &#123;<br><span class="hljs-number">20</span>          accounts = <span class="hljs-keyword">new</span> <span class="hljs-title class_">double</span>[n];<br><span class="hljs-number">21</span>          Arrays.fill(accounts, initialBalance);<br><span class="hljs-number">22</span>          bankLock = <span class="hljs-keyword">new</span> <span class="hljs-title class_">ReentrantLock</span>();<br><span class="hljs-number">23</span>          sufficientFunds = bankLock.newCondition();<br><span class="hljs-number">24</span>      &#125;<br><span class="hljs-number">25</span> <br><span class="hljs-number">26</span>      <span class="hljs-comment">/**</span><br><span class="hljs-comment">27       * Transfers money from one account to another.</span><br><span class="hljs-comment">28       * <span class="hljs-doctag">@param</span> from   the account to transfer from</span><br><span class="hljs-comment">29       * <span class="hljs-doctag">@param</span> to     the account to transfer to</span><br><span class="hljs-comment">30       * <span class="hljs-doctag">@param</span> amount the amount to transfer</span><br><span class="hljs-comment">31       */</span><br><span class="hljs-number">32</span>      <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">transfer</span><span class="hljs-params">(<span class="hljs-type">int</span> from, <span class="hljs-type">int</span> to, <span class="hljs-type">double</span> amount)</span> <span class="hljs-keyword">throws</span> InterruptedException &#123;<br><span class="hljs-number">33</span>          bankLock.lock();<br><span class="hljs-number">34</span>          <span class="hljs-keyword">try</span> &#123;<br><span class="hljs-number">35</span>              <span class="hljs-keyword">while</span> (accounts[from] &lt; amount)<br><span class="hljs-number">36</span>                  sufficientFunds.await();<br><span class="hljs-number">37</span>              System.out.print(Thread.currentThread());<br><span class="hljs-number">38</span>              accounts[from] -= amount;<br><span class="hljs-number">39</span>              System.out.printf(<span class="hljs-string">&quot; %10.2f from %d to %d&quot;</span>, amount, from, to);<br><span class="hljs-number">40</span>              accounts[to] += amount;<br><span class="hljs-number">41</span>              System.out.printf(<span class="hljs-string">&quot; Total Balance: %10.2f%n&quot;</span>, getTotalBalance());<br><span class="hljs-number">42</span>              sufficientFunds.signalAll();<br><span class="hljs-number">43</span>          &#125; <span class="hljs-keyword">finally</span> &#123;<br><span class="hljs-number">44</span>              bankLock.unlock();<br><span class="hljs-number">45</span>          &#125;<br><span class="hljs-number">46</span>      &#125;<br><span class="hljs-number">47</span> <br><span class="hljs-number">48</span>      <span class="hljs-comment">/**</span><br><span class="hljs-comment">49       * Gets the sum of all account balances.</span><br><span class="hljs-comment">50       * <span class="hljs-doctag">@return</span> the total balance</span><br><span class="hljs-comment">51       */</span><br><span class="hljs-number">52</span>      <span class="hljs-keyword">public</span> <span class="hljs-type">double</span> <span class="hljs-title function_">getTotalBalance</span><span class="hljs-params">()</span> &#123;<br><span class="hljs-number">53</span>          bankLock.lock();<br><span class="hljs-number">54</span>          <span class="hljs-keyword">try</span> &#123;<br><span class="hljs-number">55</span>              <span class="hljs-type">double</span> <span class="hljs-variable">sum</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>;<br><span class="hljs-number">56</span>              <span class="hljs-keyword">for</span> (<span class="hljs-type">double</span> a : accounts)<br><span class="hljs-number">57</span>                  sum += a;<br><span class="hljs-number">58</span>              <span class="hljs-keyword">return</span> sum;<br><span class="hljs-number">59</span>          &#125; <span class="hljs-keyword">finally</span> &#123;<br><span class="hljs-number">60</span>              bankLock.unlock();<br><span class="hljs-number">61</span>          &#125;<br><span class="hljs-number">62</span>      &#125;<br><span class="hljs-number">63</span> <br><span class="hljs-number">64</span>      <span class="hljs-comment">/**</span><br><span class="hljs-comment">65       * Gets the number of accounts in the bank.</span><br><span class="hljs-comment">66       * <span class="hljs-doctag">@return</span> the number of accounts</span><br><span class="hljs-comment">67       */</span><br><span class="hljs-number">68</span>      <span class="hljs-keyword">public</span> <span class="hljs-type">int</span> <span class="hljs-title function_">size</span><span class="hljs-params">()</span> &#123;<br><span class="hljs-number">69</span>          <span class="hljs-keyword">return</span> accounts.length;<br><span class="hljs-number">70</span>      &#125;<br><span class="hljs-number">71</span>  &#125;<br></code></pre></td></tr></table></figure><blockquote><p>[!TIP]</p><p>API java.util.concurrent.locks.Lock 5  </p><ul><li><p><code>Condition newCondition()</code></p><p>  返回一个与这个锁相关联的条件对象。</p></li></ul><p>API java.util.concurrent.locks.Condition 5  </p><ul><li><p><code>void await()</code></p><p>  将该线程放在这个条件的等待集中。  </p></li><li><p><code>void signalAll()</code></p><p>  解除该条件等待集中所有线程的阻塞状态。  </p></li><li><p><code>void signal()</code></p><p>  从该条件的等待集中随机选择一个线程，解除其阻塞状态。</p></li></ul></blockquote><h4 id="12-4-5-synchronized-关键字"><a href="#12-4-5-synchronized-关键字" class="headerlink" title="12.4.5 synchronized 关键字"></a>12.4.5 synchronized 关键字</h4><p>在前面的小节中，我们已经了解了如何使用 <code>Lock</code> 和 <code>Condition</code> 对象。在进一步深入之前，先对锁和条件的要点做一个总结：</p><ul><li>锁用来保护代码段，一次只允许一个线程执行被保护的代码。  </li><li>锁可以管理试图进入被保护代码段的线程。  </li><li>一个锁可以有一个或多个关联的条件对象。  </li><li>每个条件对象管理那些已经进入被保护代码段但还不能运行的线程。</li></ul><p><code>Lock</code> 和 <code>Condition</code> 接口允许程序员充分控制锁定。不过，大多数情况下，你并不需要那样控制，完全可以使用 Java 语言内置的一种机制。从 1.0 版开始，Java 中的每个对象都有一个<strong>内部锁（intrinsic lock）</strong>。如果一个方法声明时有 <code>synchronized</code> 关键字，那么对象的锁将保护整个方法。也就是说，要调用这个方法，线程必须获得内部对象锁。</p><p>换句话说，</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">synchronized</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">method</span><span class="hljs-params">()</span> &#123;<br>    method body<br>&#125;<br></code></pre></td></tr></table></figure><p>等价于</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">method</span><span class="hljs-params">()</span> &#123;<br>    <span class="hljs-built_in">this</span>.intrinsicLock.lock();<br>    <span class="hljs-keyword">try</span> &#123;<br>        method body<br>    &#125; <span class="hljs-keyword">finally</span> &#123;<br>        <span class="hljs-built_in">this</span>.intrinsicLock.unlock();<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>例如，可以简单地将 <code>Bank</code> 类的 <code>transfer</code> 方法声明为 <code>synchronized</code>，而不必使用一个显式的锁。</p><p>内部对象锁只有一个关联条件。<code>wait</code> 方法将一个线程增加到等待集中，<code>notifyAll</code> &#x2F; <code>notify</code> 方法可以解除等待线程的阻塞。换句话说，调用 <code>wait</code> 或 <code>notifyAll</code> 等价于：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java">intrinsicCondition.await();<br>intrinsicCondition.signalAll();<br></code></pre></td></tr></table></figure><blockquote><p><strong>注释</strong>：<code>wait</code>、<code>notifyAll</code> 以及 <code>notify</code> 方法是 <code>Object</code> 类的 <code>final</code> 方法。<code>Condition</code> 方法必须命名为 <code>await</code>、<code>signalAll</code> 和 <code>signal</code>，从而不会与那些方法发生冲突。</p></blockquote><p>例如，可以用 Java 如下实现 <code>Bank</code> 类：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Bank</span> &#123;<br>    <span class="hljs-keyword">private</span> <span class="hljs-type">double</span>[] accounts;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">synchronized</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">transfer</span><span class="hljs-params">(<span class="hljs-type">int</span> from, <span class="hljs-type">int</span> to, <span class="hljs-type">int</span> amount)</span><br>            <span class="hljs-keyword">throws</span> InterruptedException &#123;<br>        <span class="hljs-keyword">while</span> (accounts[from] &lt; amount)<br>            wait(); <span class="hljs-comment">// wait on intrinsic object lock&#x27;s single condition</span><br>        accounts[from] -= amount;<br>        accounts[to] += amount;<br>        notifyAll(); <span class="hljs-comment">// notify all threads waiting on the condition</span><br>    &#125;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">synchronized</span> <span class="hljs-type">double</span> <span class="hljs-title function_">getTotalBalance</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-comment">// ...</span><br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>可以看到，使用 <code>synchronized</code> 关键字可以得到更为简洁的代码。当然，要理解这个代码，你必须知道每个对象都有一个内部锁，并且这个锁有一个内部条件。这个锁会管理试图进入 <code>synchronized</code> 方法的线程，这个条件会管理调用了 <code>wait</code> 的线程。</p><p>将静态方法声明为同步也是合法的。如果调用这样一个方法，它会获得关联类对象的内部锁。例如，如果 <code>Bank</code> 类有一个静态同步方法，调用这个方法时，会锁定 <code>Bank.class</code> 对象的锁。因此，没有其他线程可以调用 <code>Bank</code> 类的这个方法或任何其他同步静态方法。</p><p>内部锁和条件存在一些限制，包括：</p><ul><li>不能中断一个正在尝试获得锁的线程。  </li><li>不能指定尝试获得锁的超时时间。  </li><li>每个锁只有一个条件，这很低效。</li></ul><p>在代码中应该使用哪一种做法呢？<code>Lock</code> 和 <code>Condition</code> 对象还是同步方法？下面是我们的一些建议：</p><ul><li>最好既不使用 <code>Lock/Condition</code> 也不使用 <code>synchronized</code> 关键字。在许多情况下，可以使用 <code>java.util.concurrent</code> 包中的某种机制，它会为你处理所有的锁定。</li><li>如果 <code>synchronized</code> 关键字适合你的程序，那么尽量使用这种做法，这样可以减少编写的代码量，还能减少出错。程序清单 12-5 给出了用同步方法实现的银行示例。  </li><li>如果特别需要 <code>Lock/Condition</code> 结构提供的额外能力，则使用 <code>Lock/Condition</code>。</li></ul><blockquote><p>[!TIP]</p><p>API java.lang.Object 1.0  </p><ul><li><p><code>void notifyAll()</code></p><p>解除在这个对象上调用 <code>wait</code> 方法的那些线程的阻塞状态。该方法只能在同步方法或同步块中调用。如果当前线程不是对象锁的所有者，该方法会抛出一个 <code>IllegalMonitorStateException</code> 异常。  </p></li><li><p><code>void notify()</code></p><p>随机选择一个在这个对象上调用 <code>wait</code> 方法的线程，解除其阻塞状态。该方法只能在一个同步方法或同步块中调用。如果当前线程不是对象锁的所有者，该方法会抛出一个 <code>IllegalMonitorStateException</code> 异常。  </p></li><li><p><code>void wait()</code></p><p>导致一个线程进入等待状态，直到它得到通知。该方法只能在一个同步方法或同步块中调用。如果当前线程不是对象锁的所有者，该方法会抛出一个 <code>IllegalMonitorStateException</code> 异常。  </p></li><li><p><code>void wait(long millis)</code>  </p></li><li><p><code>void wait(long millis, int nanos)</code></p><p>导致一个线程进入等待状态，直到它得到通知或者经过了指定的时间。这些方法只能在一个同步方法或同步块中调用。如果当前线程不是对象锁的所有者，这些方法会抛出 <code>IllegalMonitorStateException</code> 异常。纳秒数不能超过 1 000 000。</p></li></ul></blockquote><h4 id="12-4-6-同步块"><a href="#12-4-6-同步块" class="headerlink" title="12.4.6 同步块"></a>12.4.6 同步块</h4><p>正如前面讨论的，每个 Java 对象都有一个锁。线程可以通过调用同步方法获得这个锁。还有另一种机制可以获得这个锁：即进入一个<strong>同步块（synchronized block）</strong>。当线程进入有如下形式的一个块时：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">synchronized</span> (obj) &#123;  <span class="hljs-comment">// this is the syntax for a synchronized block</span><br>    critical section<br>&#125;<br></code></pre></td></tr></table></figure><p>它会获得 <code>obj</code> 的锁。</p><p>有时我们会看到一些“专用”（ad hoc）锁，例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Bank</span> &#123;<br>    <span class="hljs-keyword">private</span> <span class="hljs-type">double</span>[] accounts;<br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> <span class="hljs-type">Object</span> <span class="hljs-variable">lock</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Object</span>();<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">transfer</span><span class="hljs-params">(<span class="hljs-type">int</span> from, <span class="hljs-type">int</span> to, <span class="hljs-type">int</span> amount)</span> &#123;<br>        <span class="hljs-keyword">synchronized</span> (lock) &#123;  <span class="hljs-comment">// an ad-hoc lock</span><br>            accounts[from] -= amount;<br>            accounts[to] += amount;<br>            System.out.println(...);<br>        &#125;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>在这里，创建 <code>lock</code> 对象只是为了使用每个 Java 对象拥有的锁。</p><blockquote><p>警告：使用同步块时，要注意锁对象。例如，下面的代码是有问题的：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> <span class="hljs-type">String</span> <span class="hljs-variable">lock</span> <span class="hljs-operator">=</span> <span class="hljs-string">&quot;LOCK&quot;</span>;<br><span class="hljs-keyword">synchronized</span> (lock) &#123; ... &#125;  <span class="hljs-comment">// Don&#x27;t lock on string literal!</span><br>...<br></code></pre></td></tr></table></figure><p>如果这个代码在同一个程序中出现两次，锁将是同一个对象，因为字符串字面量会共享。这可能导致死锁。</p><p>另外，要避免使用基本类型包装器作为锁：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> <span class="hljs-type">Integer</span> <span class="hljs-variable">lock</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Integer</span>(<span class="hljs-number">42</span>); <span class="hljs-comment">// Don&#x27;t lock on wrappers</span><br></code></pre></td></tr></table></figure><p>构造器调用 <code>new Integer(42)</code> 已经废弃，而且你也不希望维护程序的程序员将这个调用改为 <code>Integer.valueOf(42)</code>。如果将同一个魔法数使用两次，这会意外地共享锁。</p><p>如果需要修改一个静态字段，会从特定的类上获得锁，而不是从 <code>getClass()</code> 返回的值上获得：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">synchronized</span> (MyClass.class) &#123; staticCounter++; &#125; <span class="hljs-comment">// ok</span><br><span class="hljs-keyword">synchronized</span> (getClass())  &#123; staticCounter++; &#125; <span class="hljs-comment">// Don&#x27;t</span><br></code></pre></td></tr></table></figure><p>如果从一个子类调用包含这个代码的方法，<code>getClass()</code> 会返回一个不同的 <code>Class</code> 对象！这就不再能保证互斥！</p><p>一般来讲，如果必须使用同步块，一定要了解你的锁对象！必须对所有受保护的访问路径使用相同的锁，而且别人不能使用你的锁。</p></blockquote><p>有时程序员使用一个对象的锁来实现额外的原子操作，这种做法称为客户端锁定（client-side locking）。例如，考虑 <code>Vector</code> 类，这是一个列表，它的方法是同步的。现在，假设我们将银行余额存储在一个 <code>Vector&lt;Double&gt;</code> 中。下面是 <code>transfer</code> 方法的一个原生实现：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">transfer</span><span class="hljs-params">(Vector&lt;Double&gt; accounts, <span class="hljs-type">int</span> from, <span class="hljs-type">int</span> to, <span class="hljs-type">int</span> amount)</span> &#123; <span class="hljs-comment">// ERROR</span><br>    accounts.set(from, accounts.get(from) - amount);<br>    accounts.set(to,   accounts.get(to)   + amount);<br>    System.out.println(...);<br>&#125;<br></code></pre></td></tr></table></figure><p><code>Vector</code> 类的 <code>get</code> 和 <code>set</code> 方法是同步的，但是，这对于我们并没有什么帮助。一个线程完全有可能在 <code>transfer</code> 方法中执行完第一个 <code>get</code> 调用之后被抢占。然后另一个线程可能会在相同的位置存储一个不同的值。不过，我们可以截获这个锁：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">transfer</span><span class="hljs-params">(Vector&lt;Double&gt; accounts, <span class="hljs-type">int</span> from, <span class="hljs-type">int</span> to, <span class="hljs-type">int</span> amount)</span> &#123;<br>    <span class="hljs-keyword">synchronized</span> (accounts) &#123;<br>        accounts.set(from, accounts.get(from) - amount);<br>        accounts.set(to,   accounts.get(to)   + amount);<br>        System.out.println(...);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>这个方法是可行的，但是完全依赖于这样一个事实：<code>Vector</code> 类会对自己的所有更改器方法使用内部锁。不过，确实如此吗？<code>Vector</code> 类的文档没有给出这样的承诺。你必须仔细研究源代码，而且还得希望将来的版本不会引入非同步的更改器方法。可以看到，客户端锁定是非常脆弱的，通常不建议使用。</p><blockquote><p><strong>注释</strong>：Java 虚拟机对同步方法提供了内置支持。不过，同步块会编译为很长的字节码序列来管理内部锁。</p></blockquote><h4 id="12-4-7-监视器概念"><a href="#12-4-7-监视器概念" class="headerlink" title="12.4.7 监视器概念"></a>12.4.7 监视器概念</h4><p>锁和条件是实现线程同步的强大工具，但是，严格地讲，它们不是面向对象的。多年来，研究人员在努力寻找方法，希望不要求程序员考虑显式锁就可以保证多线程的安全性。最成功的解决方案之一是监视器（monitor），这一概念最早是由 Per Brinch Hansen 和 Tony Hoare 在 20 世纪 70 年代提出的。用 Java 的术语来讲，监视器有如下属性：</p><ul><li>监视器是只包含私有字段的类。  </li><li>监视器类的每个对象有一个关联的锁。  </li><li>所有方法由这个锁锁定。换句话说，如果客户端调用 <code>obj.method()</code>，那么在方法调用开始时会自动获得 <code>obj</code> 对象的锁，并在方法返回时自动释放这个锁。因为所有的字段是私有的，这样的安排可以确保一个线程处理字段时，没有其他线程能够访问这些字段。  </li><li>锁可以有任意多个关联的条件。</li></ul><p>监视器的早期版本只有单一的条件，使用一种很优雅的语法：可以简单地调用 <code>await accounts[from] &gt;= amount</code>，而不使用任何显式的条件变量。不过，研究表明，盲目地重新测试条件是很低效的。可以利用显式的条件变量解决这一问题，每一个条件变量管理单独的一组线程。</p><p>Java 设计者以不太严格的方式调整了监视器概念：Java 中的每一个对象都有一个内部锁和一个内部条件。如果一个方法用 <code>synchronized</code> 关键字声明，那么，它表现得就像是一个监视器方法。可以通过调用 <code>wait</code> &#x2F; <code>notifyAll</code> &#x2F; <code>notify</code> 来访问条件变量。</p><p>不过，Java 对象在以下 3 个重要方面不同于监视器，这削弱了线程安全性：</p><ul><li>字段不要求是 <code>private</code>。  </li><li>方法不要求是 <code>synchronized</code>。  </li><li>内部锁对客户是可用的。</li></ul><h4 id="12-4-8-volatile-字段"><a href="#12-4-8-volatile-字段" class="headerlink" title="12.4.8 volatile 字段"></a>12.4.8 volatile 字段</h4><p>有时，如果只是为了读写一两个实例字段而使用同步，所带来的开销好像有些不合算。毕竟，怎么可能出错呢？遗憾的是，由于使用现代的处理器与编译器，出错的可能性很大。</p><ul><li><p>有多处理器的计算机能够暂时在寄存器或本地内存缓存中保存内存值。其结果是，运行在不同处理器上的线程可能看到同一个内存位置有不同的值。</p></li><li><p>编译器可能改变指令执行的顺序以得到最大的吞吐量。编译器不会选择可能改变代码语义的顺序，但是编译器有一个假定，认为内存值只在代码中有显式的修改指令时才会改变。不过，内存值有可能被另一个线程改变！</p></li></ul><p>如果你使用锁来保护可能被多个线程访问的代码，那么不存在这些问题。编译器必须遵守锁的要求，为此要在必要的时候刷新输出本地缓存，而且不能不适当地重排指令顺序。详细的解释见 JSR 133 的 Java 内存模型和线程规范（参见 <a href="http://www.jcp.org/en/jsr/detail?id=133%EF%BC%89%E3%80%82%E8%AF%A5%E8%A7%84%E8%8C%83%E7%9A%84%E5%A4%A7%E9%83%A8%E5%88%86%E5%86%85%E5%AE%B9%E9%83%BD%E5%BE%88%E5%A4%8D%E6%9D%82%E8%80%8C%E4%B8%94%E6%8A%80%E6%9C%AF%E6%80%A7%E5%BE%88%E5%BC%BA%EF%BC%8C%E4%B8%8D%E8%BF%87%E8%BF%99%E4%B8%AA%E6%96%87%E6%A1%A3%E4%B8%AD%E8%BF%98%E5%8C%85%E5%90%AB%E5%BE%88%E5%A4%9A%E8%A7%A3%E9%87%8A%E5%BE%97%E5%BE%88%E6%B8%85%E6%A5%9A%E7%9A%84%E4%BE%8B%E5%AD%90%E3%80%82Brian">http://www.jcp.org/en/jsr/detail?id=133）。该规范的大部分内容都很复杂而且技术性很强，不过这个文档中还包含很多解释得很清楚的例子。Brian</a> Goetz 写了一个更易懂的概述文章（<a href="http://www.ibm.com/developerworks/library/j-jtp02244%EF%BC%89%E3%80%82">www.ibm.com/developerworks/library/j-jtp02244）。</a></p><blockquote><p>注释：Brian Goetz 创造了以下“同步格言”：“如果写一个变量，而这个变量接下来可能会被另一个线程读取，或者，如果读一个变量，而这个变量可能已经被另一个线程写入值，那么必须使用同步。”</p></blockquote><p><code>volatile</code> 关键字为实例字段的同步访问提供了一种免锁机制。如果声明一个字段为 <code>volatile</code>，那么编译器和虚拟机就会考虑到该字段可能被另一个线程并发更新。</p><p>例如，假设一个对象有一个 <code>boolean</code> 标记 <code>done</code>，它的值由一个线程设置，而由另一个线程查询。如同我们讨论过的，你可以使用锁：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">private</span> <span class="hljs-type">boolean</span> done;<br><span class="hljs-keyword">public</span> <span class="hljs-keyword">synchronized</span> <span class="hljs-type">boolean</span> <span class="hljs-title function_">isDone</span><span class="hljs-params">()</span> &#123; <span class="hljs-keyword">return</span> done; &#125;<br><span class="hljs-keyword">public</span> <span class="hljs-keyword">synchronized</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">setDone</span><span class="hljs-params">()</span> &#123; done = <span class="hljs-literal">true</span>; &#125;<br></code></pre></td></tr></table></figure><p>或许使用内部对象锁不是个好主意。如果另一个线程已经对该对象加锁，<code>isDone()</code> 和 <code>setDone()</code> 方法可能会阻塞。如果这是个问题，可以只为这个变量使用一个单独的锁。但是，这会很麻烦。</p><p>在这种情况下，将字段声明为 <code>volatile</code> 就很合适：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">private</span> <span class="hljs-keyword">volatile</span> <span class="hljs-type">boolean</span> done;<br><span class="hljs-keyword">public</span> <span class="hljs-type">boolean</span> <span class="hljs-title function_">isDone</span><span class="hljs-params">()</span> &#123; <span class="hljs-keyword">return</span> done; &#125;<br><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">setDone</span><span class="hljs-params">()</span> &#123; done = <span class="hljs-literal">true</span>; &#125;<br></code></pre></td></tr></table></figure><p>编译器会插入适当的代码，以确保如果一个线程中对 <code>done</code> 变量做了修改，这个修改对读取这个变量的所有其他线程都可见。</p><blockquote><p>警告：<code>volatile</code> 变量不能提供原子性。例如，方法  </p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">flipDone</span><span class="hljs-params">()</span> &#123; done = !done; &#125; <span class="hljs-comment">// not atomic</span><br></code></pre></td></tr></table></figure><p>不能确保将字段中的值取反。无法保证读取、取反和写入不被中断。</p></blockquote><h4 id="12-4-9-final-变量"><a href="#12-4-9-final-变量" class="headerlink" title="12.4.9 final 变量"></a>12.4.9 final 变量</h4><p>除非使用锁或 <code>volatile</code> 修饰符，否则无法从多个线程安全地读取一个字段。</p><p>还有一种情况可以安全地访问一个共享字段，即这个字段声明为 <code>final</code> 时。考虑以下声明：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">final</span> <span class="hljs-type">var</span> <span class="hljs-variable">accounts</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">HashMap</span>&lt;String, Double&gt;();<br></code></pre></td></tr></table></figure><p>其他线程会在构造器完成构造之后才看到这个 <code>accounts</code> 变量。如果不使用 <code>final</code>，就不能保证其他线程看到的是 <code>accounts</code> 更新后的值，它们可能都只是看到 <code>null</code>，而不是新构造的 <code>HashMap</code>。当然，映射的操作并不是线程安全的。如果有多个线程更改和读取这个映射，仍然需要进行同步。</p><h4 id="12-4-10-原子性"><a href="#12-4-10-原子性" class="headerlink" title="12.4.10 原子性"></a>12.4.10 原子性</h4><p>假设对共享变量除了赋值之外并不做其他操作，那么可以将这些共享变量声明为 <code>volatile</code>。<code>java.util.concurrent.atomic</code> 包中有很多类使用了很高效的机器级指令来保证其他操作的原子性（而没有使用锁）。例如，<code>AtomicInteger</code> 类提供了方法 <code>incrementAndGet</code> 和 <code>decrementAndGet</code>，它们分别以原子方式对一个整数完成自增或自减操作。例如，可以安全地生成一个数值序列，如下所示：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-type">AtomicLong</span> <span class="hljs-variable">nextNumber</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">AtomicLong</span>();<br><span class="hljs-comment">// in some thread...</span><br><span class="hljs-type">long</span> <span class="hljs-variable">id</span> <span class="hljs-operator">=</span> nextNumber.incrementAndGet();<br></code></pre></td></tr></table></figure><p><code>incrementAndGet</code> 方法以原子方式将 <code>AtomicLong</code> 自增，并返回自增后的值。也就是说，获得值、增 1、设置值和生成新值的操作不会被中断。可以保证即使是多个线程并发地访问同一个实例，也会计算并返回正确的值。</p><p>有很多方法可以以原子方式设置和增减值，不过，如果希望完成更复杂的更新，就必须使用 <code>compareAndSet</code> 方法。例如，假设希望跟踪不同线程观察的最大值。下面的代码是不可行的：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-type">AtomicLong</span> <span class="hljs-variable">largest</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">AtomicLong</span>();<br><span class="hljs-comment">// in some thread...</span><br>largest.set(Math.max(largest.get(), observed)); <span class="hljs-comment">// ERROR--race condition!</span><br></code></pre></td></tr></table></figure><p>这个更新不是原子的。实际上，可以提供一个 lambda 表达式更新变量，它会为你完成更新。对于这个例子，我们可以调用：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">largest.updateAndGet(x -&gt; Math.max(x, observed));<br></code></pre></td></tr></table></figure><p>或</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">largest.accumulateAndGet(observed, Math::max);<br></code></pre></td></tr></table></figure><p><code>accumulateAndGet</code> 方法利用一个二元操作符来合并原子值和所提供的参数。还有 <code>getAndUpdate</code> 和 <code>getAndAccumulate</code> 方法可以返回原值。</p><blockquote><p>注释：类 <code>AtomicInteger</code>、<code>AtomicIntegerArray</code>、<code>AtomicIntegerFieldUpdater</code>、<code>AtomicLongArray</code>、<code>AtomicLongFieldUpdater</code>、<code>AtomicReference</code>、<code>AtomicReferenceArray</code> 和 <code>AtomicReferenceFieldUpdater</code> 也提供了这些方法。</p></blockquote><p>如果有大量线程要访问相同的原子值，性能会大幅下降，因为乐观更新需要太多次重试。<code>LongAdder</code> 和 <code>LongAccumulator</code> 类解决了这个问题。<code>LongAdder</code> 包括多个变量（加数），其总和为当前值。可以有多个线程更新不同的加数，线程数增加时会自动提供新的加数。通常情况下，只有当所有工作都完成之后才需要总和的值，对于这种情况，这种方法会很高效。性能会有显著的提升。</p><p>如果预期可能存在大量竞争，只需要使用 <code>LongAdder</code> 而不是 <code>AtomicLong</code>。方法名稍有区别。要调用 <code>increment</code> 让一个计数器自增，或者调用 <code>add</code> 来增加一个量，另外调用 <code>sum</code> 来获取总和。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">var</span> <span class="hljs-variable">adder</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">LongAdder</span>();<br><span class="hljs-keyword">for</span> (...) &#123;<br>    pool.submit(() -&gt; &#123;<br>        <span class="hljs-keyword">while</span> (...) &#123;<br>            <span class="hljs-keyword">if</span> (...) adder.increment();<br>        &#125;<br>    &#125;);<br>&#125;<br><span class="hljs-type">long</span> <span class="hljs-variable">total</span> <span class="hljs-operator">=</span> adder.sum();<br></code></pre></td></tr></table></figure><blockquote><p><strong>注释</strong>：当然，<code>increment</code> 方法不返回原值。这样做会消除将求和分解到多个加数所带来的性能提升。</p></blockquote><p><code>LongAccumulator</code> 将这种思想推广到任意的累加操作。在构造器中，可以提供这个操作以及它的零元素。要加入新的值，可以调用 <code>accumulate</code>。调用 <code>get</code> 来获得当前值。下面的代码可以得到与 <code>LongAdder</code> 同样的效果：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">var</span> <span class="hljs-variable">adder</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">LongAccumulator</span>(Long::sum, <span class="hljs-number">0</span>);<br><span class="hljs-comment">// in some thread...</span><br>adder.accumulate(value);<br></code></pre></td></tr></table></figure><p>在内部，这个累加器包含变量 a₁, a₂, …, aₙ。每个变量初始化为零元素（这个例子中零元素为 0）。</p><p>调用 <code>accumulate</code> 并提供值 v 时，其中一个变量会以原子方式更新为 aᵢ &#x3D; aᵢ op v，这里 op 是中缀形式的累加操作。在我们这个例子中，调用 <code>accumulate</code> 会对某个 i 计算 aᵢ &#x3D; aᵢ + v。</p><p><code>get</code> 的结果是 a₁ op a₂ op … op aₙ。在我们的例子中，这就是累加器的总和：a₁ + a₂ + … + aₙ。  </p><p>如果选择一个不同的操作，可以计算最小值或最大值。一般来说，这个操作必须满足结合律和交换律。这说明，最终结果不能依赖于以什么顺序结合这些中间值。</p><p>另外 <code>DoubleAdder</code> 和 <code>DoubleAccumulator</code> 做法也相同，只不过处理的是 <code>double</code> 值。</p><h4 id="12-4-11-死锁"><a href="#12-4-11-死锁" class="headerlink" title="12.4.11 死锁"></a>12.4.11 死锁</h4><p>锁和条件不能解决多线程中可能出现的所有问题。考虑下面的情况：</p><p>   1.账户 1：$200</p><ol start="2"><li>账户 2：$300  </li><li>线程 1：从账户 1 转 $300 到账户 2  </li><li>线程 2：从账户 2 转 $400 到账户 1</li></ol><p>如图 12-4 所示，线程 1 和线程 2 显然都被阻塞。因为账户 1 以及账户 2 中的余额都不足以进行转账，两个线程都无法继续执行。</p><p><img src="/2025/05/04/hello-world/%E7%AC%AC%E5%8D%81%E4%BA%8C%E7%AB%A0/image-20251208215838358.png" alt="image-20251208215838358"></p><p>有可能因为每一个线程都在等待更多的钱款存入而导致所有线程都被阻塞。这样的状态称为死锁（deadlock）。</p><p>在这个程序里，死锁不会发生，原因很简单。每一次转账金额至多 $1000。因为总共有 100 个账户，而且所有账户的总金额是 $100000，在任意时刻，至少有一个账户的余额高于 $1000。所以，从该账户转账的线程可以继续运行。</p><p>但是，如果修改线程的 <code>run</code> 方法，把每次转账至多 $1000 的限制去掉，很快就会发生死锁。试试看。将 <code>NACCOUNTS</code> 设置为 10。使用 <code>MAX_AMOUNT = 2 * INITIAL_BALANCE</code> 构造各个转账线程。然后运行该程序。程序运行一段时间后就会挂起。</p><blockquote><p>提示：当程序挂起时，按下组合键 Ctrl+\，将得到一个线程转储，这会列出所有线程。每一个线程有一个栈轨迹，告诉你线程当前在哪里阻塞。如第 7 章所述，可以运行 <code>jconsole</code> 并查看线程（Threads）面板（见图 12-5）。</p><p><img src="/2025/05/04/hello-world/%E7%AC%AC%E5%8D%81%E4%BA%8C%E7%AB%A0/image-20251208221248927.png" alt="image-20251208221248927"></p></blockquote><p>还有一种做法会导致死锁：让第 1 个线程负责向第 1 个账户存钱，而不是从第 1 个账户取钱。这样一来，有可能所有线程都集中到一个账户上，每一个线程都试图从这个账户中取出大于该账户余额的钱。试试看。在 <code>SynchBankTest</code> 程序中，来看 <code>TransferRunnable</code> 类的 <code>run</code> 方法。在 <code>transfer</code> 调用中，交换 <code>fromAccount</code> 和 <code>toAccount</code>。运行程序，会看到它几乎会立即死锁。</p><p>还有一种很容易导致死锁的情况：在 <code>SynchBankTest</code> 程序中，将 <code>signalAll</code> 方法改为 <code>signal</code> 方法，会发现程序最终会挂起。（同样，将 <code>NACCOUNTS</code> 设为 10 可以更快地看到这个结果。）<code>signalAll</code> 方法会通知所有等待增加资金的线程，与此不同，<code>signal</code> 方法只解除一个线程的阻塞。如果该线程不能继续运行，所有的线程都会阻塞。考虑下面的场景，这就可能发生死锁：</p><ol><li><p>账户 1：$1990</p></li><li><p>所有其他账户：分别有 $990  </p></li><li><p>线程 1：从账户 1 转 $995 到账户 2  </p></li><li><p>所有其他线程：从它们的账户转 $995 到另一个账户</p></li></ol><p>显然，除了线程 1，所有的线程都被阻塞，因为它们的账户中没有足够的金额。</p><p>线程 1 继续执行，现在情况如下：</p><ol><li>账户 1：$995  </li><li>账户 2：$1985  </li><li>所有其他账户：分别有 $990</li></ol><p>然后，线程 1 调用 <code>signal</code> 方法。<code>signal</code> 方法随机选择一个线程将它解除阻塞。假定它选择了线程 3。该线程被唤醒，发现在它的账户中没有足够的金额，它再次调用 <code>await</code>。但是，线程 1 仍在运行，将随机地产生一个新的交易，例如：</p><ol><li>线程 1：从账户 1 转 $997 到账户 2</li></ol><p>现在，线程 1 也调用 <code>await</code>，所有的线程都被阻塞。系统死锁。</p><p>这里的罪魁祸首是 <code>signal</code> 调用。它只为一个线程解除阻塞，而且，它很可能选择一个根本不能继续运行的线程（在我们的例子中，线程 2 必须从账户 2 中取钱）。  </p><p>遗憾的是，Java 程序设计语言中没有提供任何特性可以避免或打破这些死锁。你必须仔细设计程序，确保不会出现死锁。</p><h4 id="12-4-12-为什么废弃-stop-和-suspend-方法"><a href="#12-4-12-为什么废弃-stop-和-suspend-方法" class="headerlink" title="12.4.12 为什么废弃 stop 和 suspend 方法"></a>12.4.12 为什么废弃 <code>stop</code> 和 <code>suspend</code> 方法</h4><p>最初的 Java 版本定义了一个 <code>stop</code> 方法来终止一个线程，另外还有一个 <code>suspend</code> 方法来阻塞一个线程直至另一个线程调用 <code>resume</code>。<code>stop</code> 和 <code>suspend</code> 方法有一些共同点：它们都试图控制一个给定线程的行为，而没有线程的互操作。</p><p><code>stop</code>、<code>suspend</code> 和 <code>resume</code> 方法已经被废弃。<code>stop</code> 方法天生就不安全，经验证明，<code>suspend</code> 方法经常会导致死锁。在本节中，你将看到这些方法为什么有问题，以及怎样避免这些问题。</p><p>首先来看看 <code>stop</code> 方法，该方法会终止所有未完成的方法，包括 <code>run</code> 方法。一个线程终止时，它会立即释放被它锁定的所有对象的锁。这会导致对象处于不一致的状态。例如，假设一个 <code>TransferRunnable</code> 在从一个账户向另一个账户转账的过程中被终止，钱已经取出，但还没有存入目标账户，现在银行对象就被破坏了。因为锁已经释放，其他未终止的线程也可以观察到这种破坏。</p><p>当一个线程想要终止另一个线程时，它无法知道什么时候调用 <code>stop</code> 方法是安全的，而什么时候会导致对象被破坏。因此，这个方法已经被废弃。希望停止一个线程的时候应该中断该线程，然后被中断的线程可以在安全的时候终止。</p><blockquote><p>注释：一些作者声称 <code>stop</code> 方法被废弃是因为它会导致对象被一个已停止的线程永久锁定。但是，这一说法是错误的。从技术上讲，停止的线程会抛出 <code>ThreadDeath</code> 异常，从而退出它调用的所有同步方法。因此，这个线程会释放它持有的内部对象锁。</p></blockquote><p>接下来看看 <code>suspend</code> 方法有什么问题。与 <code>stop</code> 不同，<code>suspend</code> 不会破坏对象。但是，如果用 <code>suspend</code> 挂起一个持有锁的线程，那么，在这个线程恢复运行之前这个锁是不可用的。如果调用 <code>suspend</code> 方法的线程试图获得同一个锁，程序就会死锁：被挂起的线程等着被恢复，而将其挂起的线程等待获得锁。</p><p>在图形用户界面中经常出现这种情况。假设我们有一个图形化的银行模拟程序。Pause 按钮用来挂起转账线程，还有一个 Resume 按钮用来恢复线程。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs java">pauseButton.addActionListener(event -&gt; &#123;<br>    <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>; i &lt; threads.length; i++)<br>        threads[i].suspend(); <span class="hljs-comment">// don&#x27;t do this</span><br>&#125;);<br><br>resumeButton.addActionListener(event -&gt; &#123;<br>    <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>; i &lt; threads.length; i++)<br>        threads[i].resume();<br>&#125;);<br></code></pre></td></tr></table></figure><p>假设有一个 <code>paintComponent</code> 方法，它通过调用 <code>getBalances</code> 方法获得一个余额数组，从而绘制每个账户的一个图表。</p><p>就像在 12.7.3 节将要看到的，按钮动作和重绘动作都在同一个线程中，即事件分派线程（event dispatch thread）。考虑下面的情况：</p><ol><li>某个转账线程获得 <code>bank</code> 对象的锁。  </li><li>用户点击 Pause 按钮。  </li><li>所有转账线程被挂起；其中之一仍然持有 <code>bank</code> 对象的锁。  </li><li>因为某种原因，需要重新绘制账户图表。  </li><li><code>paintComponent</code> 方法调用 <code>getBalances</code> 方法。  </li><li>该方法试图获得 <code>bank</code> 对象的锁。</li></ol><p>现在程序会被冻结。事件分派线程不能继续运行，因为锁由一个挂起的线程持有。因此，用户不能点击 Resume 按钮，这些线程永远无法恢复。</p><p>如果想安全地挂起线程，可以引入一个变量 <code>suspendRequested</code>，并在 <code>run</code> 方法的某个安全的地方测试这个变量，安全的地方是指在这里该线程没有锁定其他线程需要的对象。当该线程发现 <code>suspendRequested</code> 变量已经设置，就要继续等待，直到再次可用。</p><h4 id="12-4-13-按需初始化"><a href="#12-4-13-按需初始化" class="headerlink" title="12.4.13 按需初始化"></a>12.4.13 按需初始化</h4><p>有时候，对于某些数据结构，你可能希望第一次需要它时才进行初始化，而且你希望确保这种初始化只发生一次。与其设计你自己的机制，不如利用这样一个事实：虚拟机会在第一次使用类时执行一个静态初始化器，而且只执行一次。虚拟机利用一个锁来确保这一点，所以你不需要自己编程实现。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">OnDemandData</span> &#123;<br>    <span class="hljs-comment">// private constructor to ensure only one object is constructed</span><br>    <span class="hljs-keyword">private</span> <span class="hljs-title function_">OnDemandData</span><span class="hljs-params">()</span> &#123;&#125;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> OnDemandData <span class="hljs-title function_">getInstance</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-keyword">return</span> Holder.INSTANCE;<br>    &#125;<br><br>    <span class="hljs-comment">// only initialized on first use, i.e. in the first call to getInstance</span><br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Holder</span> &#123;<br>        <span class="hljs-comment">// VM guarantees that this happens at most once</span><br>        <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">OnDemandData</span> <span class="hljs-variable">INSTANCE</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">OnDemandData</span>();<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><blockquote><p><strong>警告</strong>：要采用这种用法，必须确保构造器不会抛出任何异常。虚拟机不会做第二次尝试来初始化 <code>Holder</code> 类。</p></blockquote><h4 id="12-4-14-线程局部变量"><a href="#12-4-14-线程局部变量" class="headerlink" title="12.4.14 线程局部变量"></a>12.4.14 线程局部变量</h4><p>前面几节中，我们讨论了在线程间共享变量的风险。有时可能要避免共享变量，使用 <code>ThreadLocal</code> 辅助类为各个线程提供各自的实例。例如，<code>SimpleDateFormat</code> 类不是线程安全的。假设有一个静态变量：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">SimpleDateFormat</span> <span class="hljs-variable">dateFormat</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">SimpleDateFormat</span>(<span class="hljs-string">&quot;yyyy-MM-dd&quot;</span>);<br></code></pre></td></tr></table></figure><p>如果两个线程都执行以下操作：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">String</span> <span class="hljs-variable">dateStamp</span> <span class="hljs-operator">=</span> dateFormat.format(<span class="hljs-keyword">new</span> <span class="hljs-title class_">Date</span>());<br></code></pre></td></tr></table></figure><p>结果可能很混乱，因为 <code>dateFormat</code> 使用的内部数据结构可能会被并发访问所破坏。当然可以使用同步，但这样开销很大；或者也可以在需要时构造一个局部 <code>SimpleDateFormat</code> 对象，不过这也很浪费。</p><p>要为每个线程构造一个实例，可以使用以下代码：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> ThreadLocal&lt;SimpleDateFormat&gt; dateFormat =<br>        ThreadLocal.withInitial(() -&gt; <span class="hljs-keyword">new</span> <span class="hljs-title class_">SimpleDateFormat</span>(<span class="hljs-string">&quot;yyyy-MM-dd&quot;</span>));<br></code></pre></td></tr></table></figure><p>要访问具体的格式化方法，可以调用：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">String</span> <span class="hljs-variable">dateStamp</span> <span class="hljs-operator">=</span> dateFormat.get().format(<span class="hljs-keyword">new</span> <span class="hljs-title class_">Date</span>());<br></code></pre></td></tr></table></figure><p>在一个给定线程中首次调用 <code>get()</code> 时，会调用构造器中的 lambda 表达式。在此之后，<code>get()</code> 方法会返回属于当前线程的那个实例。</p><p>在多个线程中生成随机数也存在类似的问题。<code>java.util.Random</code> 类是线程安全的，但是如果多个线程需要等待一个共享的随机数生成器，这会很低效。</p><p>可以使用 <code>ThreadLocal</code> 辅助类为各个线程提供一个单独的生成器，不过 Java 7 还另外提供了一个便利类。只需要调用以下方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">int</span> <span class="hljs-variable">random</span> <span class="hljs-operator">=</span> ThreadLocalRandom.current().nextInt(upperBound);<br></code></pre></td></tr></table></figure><p><code>ThreadLocalRandom.current()</code> 调用会返回特定于当前线程的一个随机数生成器实例。</p><p>线程局部变量有时用于向协作完成某个任务的所有方法提供对象，而不必在调用者之间传递这个对象。例如，假设你想共享一个数据库连接。声明以下变量：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> ThreadLocal&lt;Connection&gt; connection =<br>        ThreadLocal.withInitial(() -&gt; <span class="hljs-literal">null</span>);<br></code></pre></td></tr></table></figure><p>任务开始时，为这个线程初始化这个连接：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">connection.set(connect(url, username, password));<br></code></pre></td></tr></table></figure><p>任务调用某些方法，所有方法都在同一个线程中，最终其中一个方法需要这个连接：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">var</span> <span class="hljs-variable">result</span> <span class="hljs-operator">=</span> connection.get().executeQuery(query);<br></code></pre></td></tr></table></figure><p>需要说明，同一个调用可以出现在多个线程中。每个线程会得到它自己的连接对象。</p><blockquote><p>警告：在前面的例子中，至关重要的一点是：只有一个任务使用线程。如果使用一个线程池执行任务，你可能不希望向共享相同线程的其他任务提供你的数据库连接。</p></blockquote><blockquote><p>[!TIP]</p><p>API java.lang.ThreadLocal 1.2  </p><ul><li><p><code>T get()</code></p><p>  得到这个线程的当前值。如果是首次调用 <code>get</code>，会调用 <code>initialize</code> 来得到这个值。  </p></li><li><p><code>void set(T t)</code></p><p>  为这个线程设置一个新值。  </p></li><li><p><code>void remove()</code></p><p>  删除对应这个线程的值。  </p></li><li><p><code>static &lt;S&gt; ThreadLocal&lt;S&gt; withInitial(Supplier&lt;? extends S&gt; supplier) 8</code></p><p>  创建一个线程局部变量，其初始值通过调用给定的提供者（supplier）生成。</p></li></ul><p>API java.util.concurrent.ThreadLocalRandom 7  </p><ul><li><p><code>static ThreadLocalRandom current()</code></p><p>  返回特定于当前线程的 <code>Random</code> 类的一个实例。</p></li></ul></blockquote><h3 id="12-5-线程安全的集合"><a href="#12-5-线程安全的集合" class="headerlink" title="12.5 线程安全的集合"></a>12.5 线程安全的集合</h3><p>如果多个线程要并发地修改一个数据结构，例如散列表，那么很容易破坏这个数据结构（有关散列表的详细信息见第 9 章）。例如，一个线程可能开始向表中插入一个新元素。假设在调整散列表各个桶之间的链接关系的过程中，这个线程的控制权被抢占。如果另一个线程开始遍历同一个散列表，可能会使用无效的链接并造成混乱，有可能抛出异常或者陷入无限循环。</p><p>可以通过提供锁来保护共享的数据结构，但是通常更容易的做法是选择线程安全的实现。在下面各小节中，将讨论 Java 类库提供的另外一些线程安全的集合。</p><h4 id="12-5-1-阻塞队列"><a href="#12-5-1-阻塞队列" class="headerlink" title="12.5.1 阻塞队列"></a>12.5.1 阻塞队列</h4><p>很多线程问题可以使用一个或多个队列以优雅而安全的方式来解决。生产者线程向队列插入元素，消费者线程则获取元素。使用队列，可以安全地从一个线程向另一个线程传递数据。例如，考虑银行转账程序，转账线程可以将转账指令对象插入一个队列，而不是直接访问银行对象。另一个线程从队列中取出指令并完成转账。只有这个线程可以访问银行对象的内部。因此不需要同步。（当然，线程安全的队列类的实现者必须考虑锁和条件，但那是他们的问题，而不是你要考虑的问题。）</p><p>当试图向队列添加元素而队列已满，或是想从队列移出元素而队列为空的时候，阻塞队列（blocking queue）将导致线程阻塞。在协调多个线程的工作时，阻塞队列是一个有用的工具。工作线程可以周期性地将中间结果存储在阻塞队列中。其他工作线程移除中间结果，并进一步修改。队列会自动地平衡负载。如果第一组线程运行得比第二组慢，第二组在等待结果时会阻塞。如果第一组线程运行得更快，队列会填满，直到第二组赶上来。表 12-1 给出了阻塞队列的方法。</p><p><img src="/2025/05/04/hello-world/%E7%AC%AC%E5%8D%81%E4%BA%8C%E7%AB%A0/image-20251208222125796.png" alt="image-20251208222125796"></p><p>阻塞队列方法分为以下 3 类，它们的区别在于当队列满或空时它们完成的动作。如果使用队列作为线程管理工具，要用到 <code>put</code> 和 <code>take</code> 方法。试图向满队列添加元素或者想从空队列得到队头元素时，<code>add</code>、<code>remove</code> 和 <code>element</code> 操作会抛出异常。当然，在一个多线程程序中，队列可能会在任何时候变空或变满，因此，你可能更想使用 <code>offer</code>、<code>poll</code> 和 <code>peek</code> 方法。如果不能完成任务，这些方法只是返回一个错误提示而不会抛出异常。</p><blockquote><p><strong>注释</strong>：<code>poll</code> 和 <code>peek</code> 方法返回 <code>null</code> 来指示失败。因此，向这些队列中插入 <code>null</code> 值是非法的。</p></blockquote><p>还有带有超时时间的 <code>offer</code> 方法和 <code>poll</code> 方法。例如，下面的调用：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">boolean</span> <span class="hljs-variable">success</span> <span class="hljs-operator">=</span> q.offer(x, <span class="hljs-number">100</span>, TimeUnit.MILLISECONDS);<br></code></pre></td></tr></table></figure><p>尝试在 100 毫秒时间内在队尾插入一个元素。如果成功返回 <code>true</code>；否则，如果超时，则返回 <code>false</code>。类似地，下面的调用：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">Object</span> <span class="hljs-variable">head</span> <span class="hljs-operator">=</span> q.poll(<span class="hljs-number">100</span>, TimeUnit.MILLISECONDS);<br></code></pre></td></tr></table></figure><p>尝试在 100 毫秒时间内移除队头元素；如果成功返回队头元素，否则，如果超时，则返回 <code>null</code>。</p><p>如果队列满，则 <code>put</code> 方法阻塞；如果队列空，则 <code>take</code> 方法阻塞。它们与不带超时参数的 <code>offer</code> 和 <code>poll</code> 方法等效。</p><p><code>java.util.concurrent</code> 包提供了阻塞队列的几个变体。默认情况下，<code>LinkedBlockingQueue</code> 的容量没有上界，但是，也可以选择指定一个最大容量。<code>LinkedBlockingDeque</code> 是一个双端队列。<code>ArrayBlockingQueue</code> 在构造时需要指定容量，另外可以有一个可选的参数来指定是否需要公平性。若指定了公平性，那么等待了最长时间的线程会优先得到处理。与以往一样，公平性会降低性能，应当在确实非常需要时才使用公平性参数。</p><p><code>PriorityBlockingQueue</code> 是一个优先队列，而不是先进先出队列。元素按照它们的优先级顺序移除。这个队列没有容量上限，但是，如果队列是空的，获取元素的操作会阻塞。（有关优先队列的详细内容参见第 9 章。）</p><p><code>DelayQueue</code> 包含实现了 <code>Delayed</code> 接口的对象：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">interface</span> <span class="hljs-title class_">Delayed</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Comparable</span>&lt;Delayed&gt; &#123;<br>    <span class="hljs-type">long</span> <span class="hljs-title function_">getDelay</span><span class="hljs-params">(TimeUnit unit)</span>;<br>&#125;<br></code></pre></td></tr></table></figure><p><code>getDelay</code> 方法返回对象的剩余延迟。负值表示延迟已经结束。元素只有在延迟结束的情况下才能从 <code>DelayQueue</code> 移除。还需要实现 <code>compareTo</code> 方法。<code>DelayQueue</code> 使用这个方法对元素排序。</p><p>Java 7 增加了一个 <code>TransferQueue</code> 接口，允许生产者线程等待，直到消费者准备就绪可以接收元素。如果生产者调用：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">q.transfer(item);<br></code></pre></td></tr></table></figure><p>这个调用会阻塞，直到另一个线程将元素删除。<code>LinkedTransferQueue</code> 类实现了这个接口。</p><p>程序清单 12-6 中的程序展示了如何使用阻塞队列来控制一组线程。程序在一个目录及其所有子目录下搜索所有文件，打印出包含指定关键字的行。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br><span class="line">98</span><br><span class="line">99</span><br><span class="line">100</span><br><span class="line">101</span><br><span class="line">102</span><br></pre></td><td class="code"><pre><code class="hljs java">程序清单 <span class="hljs-number">12</span>-<span class="hljs-number">6</span>  blockingQueue/BlockingQueueTest.java<br><br> <span class="hljs-number">1</span>  <span class="hljs-keyword">package</span> blockingQueue;<br> <span class="hljs-number">2</span> <br> <span class="hljs-number">3</span>  <span class="hljs-keyword">import</span> java.io.*;<br> <span class="hljs-number">4</span>  <span class="hljs-keyword">import</span> java.nio.charset.*;<br> <span class="hljs-number">5</span>  <span class="hljs-keyword">import</span> java.nio.file.*;<br> <span class="hljs-number">6</span>  <span class="hljs-keyword">import</span> java.util.*;<br> <span class="hljs-number">7</span>  <span class="hljs-keyword">import</span> java.util.concurrent.*;<br> <span class="hljs-number">8</span>  <span class="hljs-keyword">import</span> java.util.stream.*;<br> <span class="hljs-number">9</span> <br><span class="hljs-number">10</span>  <span class="hljs-comment">/**</span><br><span class="hljs-comment">11   * <span class="hljs-doctag">@version</span> 1.03 2018-03-17</span><br><span class="hljs-comment">12   * <span class="hljs-doctag">@author</span>  Cay Horstmann</span><br><span class="hljs-comment">13   */</span><br><span class="hljs-number">14</span>  <span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">BlockingQueueTest</span> &#123;<br><span class="hljs-number">15</span>      <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">int</span> <span class="hljs-variable">FILE_QUEUE_SIZE</span> <span class="hljs-operator">=</span> <span class="hljs-number">10</span>;<br><span class="hljs-number">16</span>      <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">int</span> <span class="hljs-variable">SEARCH_THREADS</span>   <span class="hljs-operator">=</span> <span class="hljs-number">100</span>;<br><span class="hljs-number">17</span>      <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">Path</span> <span class="hljs-variable">DUMMY</span> <span class="hljs-operator">=</span> Path.of(<span class="hljs-string">&quot;&quot;</span>);<br><span class="hljs-number">18</span> <br><span class="hljs-number">19</span>      <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> BlockingQueue&lt;Path&gt; queue = <br><span class="hljs-number">20</span>              <span class="hljs-keyword">new</span> <span class="hljs-title class_">ArrayBlockingQueue</span>&lt;&gt;(FILE_QUEUE_SIZE);<br><span class="hljs-number">21</span> <br><span class="hljs-number">22</span>      <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> &#123;<br><span class="hljs-number">23</span>          <span class="hljs-keyword">try</span> (<span class="hljs-type">var</span> <span class="hljs-variable">in</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Scanner</span>(System.in)) &#123;<br><span class="hljs-number">24</span>              System.out.print(<span class="hljs-string">&quot;Enter base directory (e.g. /opt/jdk-11-src): &quot;</span>);<br><span class="hljs-number">25</span>              <span class="hljs-type">String</span> <span class="hljs-variable">directory</span> <span class="hljs-operator">=</span> in.nextLine();<br><span class="hljs-number">26</span>              System.out.print(<span class="hljs-string">&quot;Enter keyword (e.g. volatile): &quot;</span>);<br><span class="hljs-number">27</span>              <span class="hljs-type">String</span> <span class="hljs-variable">keyword</span>  <span class="hljs-operator">=</span> in.nextLine();<br><span class="hljs-number">28</span> <br><span class="hljs-number">29</span>              <span class="hljs-type">Runnable</span> <span class="hljs-variable">enumerator</span> <span class="hljs-operator">=</span> () -&gt; &#123;<br><span class="hljs-number">30</span>                  <span class="hljs-keyword">try</span> &#123;<br><span class="hljs-number">31</span>                      enumerate(Path.of(directory));<br><span class="hljs-number">32</span>                      queue.put(DUMMY);          <span class="hljs-comment">// 结束标记</span><br><span class="hljs-number">33</span>                  &#125; <span class="hljs-keyword">catch</span> (IOException e) &#123;<br><span class="hljs-number">34</span>                      e.printStackTrace();<br><span class="hljs-number">35</span>                  &#125; <span class="hljs-keyword">catch</span> (InterruptedException e) &#123;<br><span class="hljs-number">36</span>                      <span class="hljs-comment">// 被中断则退出</span><br><span class="hljs-number">37</span>                  &#125;<br><span class="hljs-number">38</span>              &#125;;<br><span class="hljs-number">39</span>              <span class="hljs-keyword">new</span> <span class="hljs-title class_">Thread</span>(enumerator).start();<br><span class="hljs-number">40</span> <br><span class="hljs-number">41</span>              <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">1</span>; i &lt;= SEARCH_THREADS; i++) &#123;<br><span class="hljs-number">42</span>                  <span class="hljs-type">Runnable</span> <span class="hljs-variable">searcher</span> <span class="hljs-operator">=</span> () -&gt; &#123;<br><span class="hljs-number">43</span>                      <span class="hljs-keyword">try</span> &#123;<br><span class="hljs-number">44</span>                          <span class="hljs-type">boolean</span> <span class="hljs-variable">done</span> <span class="hljs-operator">=</span> <span class="hljs-literal">false</span>;<br><span class="hljs-number">45</span>                          <span class="hljs-keyword">while</span> (!done) &#123;<br><span class="hljs-number">46</span>                              <span class="hljs-type">Path</span> <span class="hljs-variable">file</span> <span class="hljs-operator">=</span> queue.take();<br><span class="hljs-number">47</span>                              <span class="hljs-keyword">if</span> (file == DUMMY) &#123;        <span class="hljs-comment">// 遇到结束标记</span><br><span class="hljs-number">48</span>                                  queue.put(file);        <span class="hljs-comment">// 放回，让其他搜索线程也结束</span><br><span class="hljs-number">49</span>                                  done = <span class="hljs-literal">true</span>;<br><span class="hljs-number">50</span>                              &#125; <span class="hljs-keyword">else</span> &#123;<br><span class="hljs-number">51</span>                                  search(file, keyword);<br><span class="hljs-number">52</span>                              &#125;<br><span class="hljs-number">53</span>                          &#125;<br><span class="hljs-number">54</span>                      &#125; <span class="hljs-keyword">catch</span> (IOException e) &#123;<br><span class="hljs-number">55</span>                          e.printStackTrace();<br><span class="hljs-number">56</span>                      &#125; <span class="hljs-keyword">catch</span> (InterruptedException e) &#123;<br><span class="hljs-number">57</span>                          <span class="hljs-comment">// 被中断则退出</span><br><span class="hljs-number">58</span>                      &#125;<br><span class="hljs-number">59</span>                  &#125;;<br><span class="hljs-number">60</span>                  <span class="hljs-keyword">new</span> <span class="hljs-title class_">Thread</span>(searcher).start();<br><span class="hljs-number">61</span>              &#125;<br><span class="hljs-number">62</span>          &#125;<br><span class="hljs-number">63</span>      &#125;<br><span class="hljs-number">64</span> <br><span class="hljs-number">65</span>      <span class="hljs-comment">/**</span><br><span class="hljs-comment">66       * 递归枚举目录及其子目录中的所有文件。</span><br><span class="hljs-comment">67       * <span class="hljs-doctag">@param</span> directory 起始目录</span><br><span class="hljs-comment">68       */</span><br><span class="hljs-number">69</span>      <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">enumerate</span><span class="hljs-params">(Path directory)</span> <br><span class="hljs-number">70</span>              <span class="hljs-keyword">throws</span> IOException, InterruptedException <br><span class="hljs-number">71</span>      &#123;<br><span class="hljs-number">72</span>          <span class="hljs-keyword">try</span> (Stream&lt;Path&gt; children = Files.list(directory)) &#123;<br><span class="hljs-number">73</span>              <span class="hljs-keyword">for</span> (Path child : children.toList()) &#123;<br><span class="hljs-number">74</span>                  <span class="hljs-keyword">if</span> (Files.isDirectory(child)) &#123;<br><span class="hljs-number">75</span>                      enumerate(child);<br><span class="hljs-number">76</span>                  &#125; <span class="hljs-keyword">else</span> &#123;<br><span class="hljs-number">77</span>                      queue.put(child);<br><span class="hljs-number">78</span>                  &#125;<br><span class="hljs-number">79</span>              &#125;<br><span class="hljs-number">80</span>          &#125;<br><span class="hljs-number">81</span>      &#125;<br><span class="hljs-number">82</span> <br><span class="hljs-number">83</span>      <span class="hljs-comment">/**</span><br><span class="hljs-comment">84       * 在文件中搜索给定关键字并打印所有匹配行。</span><br><span class="hljs-comment">85       * <span class="hljs-doctag">@param</span> file    待搜索文件</span><br><span class="hljs-comment">86       * <span class="hljs-doctag">@param</span> keyword 关键字</span><br><span class="hljs-comment">87       */</span><br><span class="hljs-number">88</span>      <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">search</span><span class="hljs-params">(Path file, String keyword)</span> <span class="hljs-keyword">throws</span> IOException &#123;<br><span class="hljs-number">89</span>          <span class="hljs-keyword">try</span> (<span class="hljs-type">var</span> <span class="hljs-variable">in</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Scanner</span>(file, StandardCharsets.UTF_8)) &#123;<br><span class="hljs-number">90</span>              <span class="hljs-type">int</span> <span class="hljs-variable">lineNumber</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>;<br><span class="hljs-number">91</span>              <span class="hljs-keyword">while</span> (in.hasNextLine()) &#123;<br><span class="hljs-number">92</span>                  lineNumber++;<br><span class="hljs-number">93</span>                  <span class="hljs-type">String</span> <span class="hljs-variable">line</span> <span class="hljs-operator">=</span> in.nextLine();<br><span class="hljs-number">94</span>                  <span class="hljs-keyword">if</span> (line.contains(keyword)) &#123;<br><span class="hljs-number">95</span>                      System.out.printf(<span class="hljs-string">&quot;%s:%d:%s%n&quot;</span>, file, lineNumber, line);<br><span class="hljs-number">96</span>                  &#125;<br><span class="hljs-number">96</span>              &#125;<br><span class="hljs-number">97</span>          &#125;<br><span class="hljs-number">98</span>      &#125;<br><span class="hljs-number">99</span> &#125;<br></code></pre></td></tr></table></figure><p>生产者线程遍历所有子目录下的所有文件并把它们放到一个阻塞队列中。这个操作很快，如果队列没有上限的话，很快就会包含文件系统中的所有文件。</p><p>我们同时启动了大量搜索线程。每个搜索线程从队列中取出一个文件，打开它，打印包含指定关键字的所有行，然后取出下一个文件。我们使用了一个小技巧，从而在没有更多工作时终止这个应用。为了发出完成信号，枚举线程会在队列中放置一个虚拟对象（这就像在行李传送带上放一个标着“last bag”的虚拟行李箱）。当搜索线程取到这个虚拟对象时，将其放回并终止。</p><p>注意，这里不需要显式的线程同步。在这个应用中，我们使用了队列数据结构作为一种同步机制。</p><blockquote><p>[!TIP]</p><p>API java.util.concurrent.ArrayBlockingQueue 5  </p><ul><li><p><code>ArrayBlockingQueue(int capacity)</code>  </p></li><li><p><code>ArrayBlockingQueue(int capacity, boolean fair)</code></p><p>  用指定的容量和公平性设置构造一个阻塞队列。队列实现为一个循环数组。</p></li></ul><p>API java.util.concurrent.LinkedBlockingQueue 5</p><p>API java.util.concurrent.LinkedBlockingDeque 6  </p><ul><li><p><code>LinkedBlockingQueue()</code>  </p></li><li><p><code>LinkedBlockingDeque()</code></p><p>  构造一个无上限的阻塞队列或双向队列，实现为一个链表。  </p></li><li><p><code>LinkedBlockingQueue(int capacity)</code>  </p></li><li><p><code>LinkedBlockingDeque(int capacity)</code></p><p>  根据指定容量构建一个有上限的阻塞队列或双向队列，实现为一个链表。</p></li></ul><p>API java.util.concurrent.DelayQueue 5  </p><ul><li><p><code>DelayQueue()</code></p><p>  构造一个包含 <code>Delayed</code> 元素的无上限阻塞队列。只有那些延迟结束的元素可以从队列中移除。</p></li></ul><p>API java.util.concurrent.Delayed 5  </p><ul><li><p><code>long getDelay(TimeUnit unit)</code></p><p>  得到该对象的延迟，用给定的时间单位度量。</p></li></ul><p>API java.util.concurrent.PriorityBlockingQueue 5  </p><ul><li><p><code>PriorityBlockingQueue()</code>  </p></li><li><p><code>PriorityBlockingQueue(int initialCapacity)</code>  </p></li><li><p><code>PriorityBlockingQueue(int initialCapacity, Comparator&lt;? super E&gt; comparator)</code></p><p>  构造一个无上限阻塞优先队列，实现为一个堆。优先队列的默认初始容量为 11。如果没有指定比较器，则元素必须实现 <code>Comparable</code> 接口。</p><p>  API java.util.concurrent.BlockingQueue 5  </p><ul><li><p><code>void put(E element)</code></p><p>  添加元素，在必要时阻塞。  </p></li><li><p><code>E take()</code></p><p>  移除并返回队头元素，必要时阻塞。  </p></li><li><p><code>boolean offer(E element, long time, TimeUnit unit)</code></p><p>  添加给定的元素，如果成功返回 <code>true</code>，必要时阻塞，直至元素已经添加或者时间已到。  </p></li><li><p><code>E poll(long time, TimeUnit unit)</code></p><p>  移除并返回队头元素，必要时阻塞，直至元素可用或时间已到。失败时返回 <code>null</code>。</p></li></ul><p>  API java.util.concurrent.BlockingDeque 6  </p><ul><li><p><code>void putFirst(E element)</code>  </p></li><li><p><code>void putLast(E element)</code></p><p>  添加元素，必要时阻塞。  </p></li><li><p><code>E takeFirst()</code>  </p></li><li><p><code>E takeLast()</code></p><p>  移除并返回队头或队尾元素，必要时阻塞。  </p></li><li><p><code>boolean offerFirst(E element, long time, TimeUnit unit)</code>  </p></li><li><p><code>boolean offerLast(E element, long time, TimeUnit unit)</code></p><p>  添加给定的元素，成功时返回 <code>true</code>，必要时阻塞，直至元素已经添加或时间已到。  </p></li><li><p><code>E pollFirst(long time, TimeUnit unit)</code>  </p></li><li><p><code>E pollLast(long time, TimeUnit unit)</code></p><p>  移除并返回队头或队尾元素，必要时阻塞，直至元素可用或时间已到。失败时返回 <code>null</code>。</p></li></ul><p>  API java.util.concurrent.TransferQueue 7  </p><ul><li><p><code>void transfer(E element)</code>  </p></li><li><p><code>boolean tryTransfer(E element, long time, TimeUnit unit)</code></p><p>  传输一个值，或者尝试在给定的超时时间内传输这个值，这个调用将阻塞，直到另一个线程将元素删除。第二个方法会在调用成功时返回 <code>true</code>。</p></li></ul></li></ul></blockquote><h4 id="12-5-2-高效的映射、集和队列"><a href="#12-5-2-高效的映射、集和队列" class="headerlink" title="12.5.2 高效的映射、集和队列"></a>12.5.2 高效的映射、集和队列</h4><p><code>java.util.concurrent</code> 包提供了映射、有序集和队列的高效实现：<code>ConcurrentHashMap</code>、<code>ConcurrentSkipListMap</code>、<code>ConcurrentSkipListSet</code> 和 <code>ConcurrentLinkedQueue</code>。</p><p>这些集合使用复杂的算法，通过允许并发地访问数据结构的不同部分尽可能减少竞争。</p><p>与大多数集合不同，这些类的 <code>size</code> 方法不一定在常量时间内完成操作。确定这些集合的当前大小通常需要遍历。</p><blockquote><p>注释：有些应用使用庞大的并发散列映射，这些映射太过庞大，以至于无法用 <code>size</code> 方法得到它的大小，因为这个方法只能返回 <code>int</code>。如果一个映射包含超过 20 亿个条目，该如何处理？<code>mappingCount</code> 方法可以把大小作为 <code>long</code> 返回。</p></blockquote><p>集合返回弱一致性（weakly consistent）的迭代器。这意味着迭代器不一定能反映出它们构造之后所做的全部更改，但是，它们不会将同一个值返回两次，也不会抛出 <code>ConcurrentModificationException</code> 异常。</p><blockquote><p>注释：与之形成对照的是，对于 <code>java.util</code> 包中的集合，如果集合在迭代器构造之后发生改变，集合的迭代器将抛出一个 <code>ConcurrentModificationException</code> 异常。</p></blockquote><p>并发散列映射可以高效地支持大量阅读器线程和有限的书写器线程。</p><blockquote><p>注释：散列映射将有相同散列码的所有条目放在同一个“桶”中。有些应用使用的散列函数不太好，以至于所有条目最后都放在很少的桶中，这会使性能严重恶化。即使是通常还算合理的散列函数，如 <code>String</code> 类的散列函数，也可能存在问题。例如，攻击者可以制造大量能得出相同散列值的字符串，让程序速度减慢。在较新的 Java 版本中，并发散列映射将桶组织为树，而不是列表，键类型实现 <code>Comparable</code>，从而可以保证 O(log n) 的性能。</p></blockquote><blockquote><p>[!TIP]</p><p>API java.util.concurrent.ConcurrentLinkedQueue 5  </p><ul><li><p><code>ConcurrentLinkedQueue&lt;E&gt;()</code></p><p>  构造一个可以由多个线程安全访问的无上限非阻塞的队列。</p></li></ul><p>API java.util.concurrent.ConcurrentSkipListSet 6  </p><ul><li><p><code>ConcurrentSkipListSet&lt;E&gt;()</code>  </p></li><li><p><code>ConcurrentSkipListSet&lt;E&gt;(Comparator&lt;? super E&gt; comp)</code></p><p>  构造一个可以由多个线程安全访问的有序集。第一个构造器要求元素实现 <code>Comparable</code> 接口。</p></li></ul><p>API java.util.concurrent.ConcurrentHashMap&lt;K,V&gt; 5</p><p>API java.util.concurrent.ConcurrentSkipListMap&lt;K,V&gt; 6  </p><ul><li><p><code>ConcurrentHashMap&lt;K,V&gt;()</code>  </p></li><li><p><code>ConcurrentHashMap&lt;K,V&gt;(int initialCapacity)</code>  </p></li><li><p><code>ConcurrentHashMap&lt;K,V&gt;(int initialCapacity, float loadFactor, int concurrencyLevel)</code></p><p>  构造一个可以由多个线程安全访问的散列映射。默认的初始容量为 16。如果每个桶的平均负载超过装填因子，表的大小会重新调整。装填因子默认值为 0.75。并发级别是估计的并发书写器线程数。</p><ul><li><p><code>ConcurrentSkipListMap&lt;K,V&gt;()</code>  </p></li><li><p><code>ConcurrentSkipListMap&lt;K,V&gt;(Comparator&lt;? super K&gt; comp)</code></p><p>  构造一个可以由多个线程安全访问的有序映射。第一个构造器要求键实现 <code>Comparable</code> 接口。</p></li></ul></li></ul></blockquote><h4 id="12-5-3-映射条目的原子更新"><a href="#12-5-3-映射条目的原子更新" class="headerlink" title="12.5.3 映射条目的原子更新"></a>12.5.3 映射条目的原子更新</h4><p><code>ConcurrentHashMap</code> 原来的版本只有为数不多的方法可以实现原子更新，这使得编程有些麻烦。假设我们希望统计观察到某些特性的频度。作为一个简单的例子，假设多个线程会遇到单词，我们想统计它们的频率。</p><p>可以使用 <code>ConcurrentHashMap&lt;String, Long&gt;</code> 吗？考虑让计数自增的代码。显然，下面的代码不是线程安全的：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">Long</span> <span class="hljs-variable">oldValue</span> <span class="hljs-operator">=</span> map.get(word);<br><span class="hljs-type">Long</span> <span class="hljs-variable">newValue</span> <span class="hljs-operator">=</span> oldValue == <span class="hljs-literal">null</span> ? <span class="hljs-number">1</span> : oldValue + <span class="hljs-number">1</span>;<br>map.put(word, newValue); <span class="hljs-comment">// ERROR—might not replace oldValue</span><br></code></pre></td></tr></table></figure><p>可能会有另一个线程在同时更新同一个计数。</p><blockquote><p>注释：有些程序员很奇怪为什么原本线程安全的数据结构会允许非线程安全的操作。有两种完全不同的情况。如果多个线程修改一个普通的 <code>HashMap</code>，它们可能会破坏内部结构（一个链表数组）。有些链接可能丢失，或者甚至会构成环，使得这个数据结构不再可用。对于 <code>ConcurrentHashMap</code> 绝对不会发生这种情况。在上面的例子中，<code>get</code> 和 <code>put</code> 代码永远不会破坏数据结构。不过，由于操作序列不是原子的，所以结果不可预知。</p></blockquote><p>在老版本的 Java 中，必须使用 <code>replace</code> 操作，它会以原子方式用一个新值替换原值，前提是之前没有其他线程把原值替换为其他值。必须一直这么做，直到替换成功：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">do</span> &#123;<br>    oldValue = map.get(word);<br>    newValue = oldValue == <span class="hljs-literal">null</span> ? <span class="hljs-number">1</span> : oldValue + <span class="hljs-number">1</span>;<br>&#125; <span class="hljs-keyword">while</span> (!map.replace(word, oldValue, newValue));<br></code></pre></td></tr></table></figure><p>或者，可以使用一个 <code>ConcurrentHashMap&lt;String, AtomicLong&gt;</code>，以及以下更新代码：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java">map.putIfAbsent(word, <span class="hljs-keyword">new</span> <span class="hljs-title class_">AtomicLong</span>());<br>map.get(word).incrementAndGet();<br></code></pre></td></tr></table></figure><p>很遗憾，这会为每个自增构造一个新的 <code>AtomicLong</code>，而不管是否需要。</p><p>如今，Java API 提供了一些新方法，可以更方便地完成原子更新。调用 <code>compute</code> 方法时可以提供一个键和一个计算新值的函数。这个函数接收键和相关联的值（如果没有值，则为 <code>null</code>），它会计算新值。例如，可以如下更新一个整数计数器映射：</p>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>《Java核心技术卷一》第九章</title>
    <link href="/2025/11/30/%E3%80%8AJava%E6%A0%B8%E5%BF%83%E6%8A%80%E6%9C%AF%E5%8D%B7%E4%B8%80%E3%80%8B%E7%AC%AC%E4%B9%9D%E7%AB%A0/"/>
    <url>/2025/11/30/%E3%80%8AJava%E6%A0%B8%E5%BF%83%E6%8A%80%E6%9C%AF%E5%8D%B7%E4%B8%80%E3%80%8B%E7%AC%AC%E4%B9%9D%E7%AB%A0/</url>
    
    <content type="html"><![CDATA[<h2 id="第9章-集合"><a href="#第9章-集合" class="headerlink" title="第9章 集合"></a>第9章 集合</h2><p>本章将介绍如何利用 Java 类库帮助我们实现程序设计所需的传统数据结构。</p><h3 id="9-1-Java-集合框架"><a href="#9-1-Java-集合框架" class="headerlink" title="9.1 Java 集合框架"></a>9.1 Java 集合框架</h3><p>Java 最初的版本只为最常用的数据结构提供了很少的一组类：<code>Vector</code>、<code>Stack</code>、<code>Hashtable</code>、<code>BitSet</code> 与 <code>Enumeration</code> 接口，其中 <code>Enumeration</code> 接口提供了一种抽象机制，用于访问任意容器中的元素。</p><h4 id="9-1-1-集合接口与实现分离"><a href="#9-1-1-集合接口与实现分离" class="headerlink" title="9.1.1 集合接口与实现分离"></a>9.1.1 集合接口与实现分离</h4><p><strong>Java 集合类库将接口（interface）与实现（implementation）分离</strong>。下面利用我们熟悉的一个数据结构——队列（queue）来说明接口与实现如何分离。</p><p>**队列接口（queue interface）**指出可以在队尾添加元素，在队头删除元素，并且可以查找队列中元素的个数。当需要收集对象并按照“先进先出”方式获取对象时，就应该使用队列（见图 9-1）。</p><p><img src="/2025/05/04/hello-world/%E7%AC%AC%E4%B9%9D%E7%AB%A0/image-20251201154009502.png" alt="image-20251201154009502"></p><p>队列接口的最简形式可能如下所示：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">interface</span> <span class="hljs-title class_">Queue</span>&lt;E&gt; <span class="hljs-comment">// a simplified form of the interface in the standard library</span><br>&#123;<br>    <span class="hljs-keyword">void</span> <span class="hljs-title function_">add</span><span class="hljs-params">(E element)</span>;<br>    E <span class="hljs-title function_">remove</span><span class="hljs-params">()</span>;<br>    <span class="hljs-type">int</span> <span class="hljs-title function_">size</span><span class="hljs-params">()</span>;<br>&#125;<br></code></pre></td></tr></table></figure><p>这个接口并没有说明队列是如何实现的。队列通常有两种实现方式：一种是使用循环数组；另一种是使用链表（见图 9-2）。</p><p>每个实现都可以用一个实现了 <code>Queue</code> 接口的类表示：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">CircularArrayQueue</span>&lt;E&gt; <span class="hljs-keyword">implements</span> <span class="hljs-title class_">Queue</span>&lt;E&gt; <span class="hljs-comment">// not an actual library class</span><br>&#123;<br>    <span class="hljs-keyword">private</span> <span class="hljs-type">int</span> head;<br>    <span class="hljs-keyword">private</span> <span class="hljs-type">int</span> tail;<br>    CircularArrayQueue(<span class="hljs-type">int</span> capacity) &#123; ... &#125;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">add</span><span class="hljs-params">(E element)</span> &#123; ... &#125;<br>    <span class="hljs-keyword">public</span> E <span class="hljs-title function_">remove</span><span class="hljs-params">()</span> &#123; ... &#125;<br>    <span class="hljs-keyword">public</span> <span class="hljs-type">int</span> <span class="hljs-title function_">size</span><span class="hljs-params">()</span> &#123; ... &#125;<br>    <span class="hljs-keyword">private</span> E[] elements;<br>&#125;<br></code></pre></td></tr></table></figure><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">LinkedListQueue</span>&lt;E&gt; <span class="hljs-keyword">implements</span> <span class="hljs-title class_">Queue</span>&lt;E&gt; <span class="hljs-comment">// not an actual library class</span><br>&#123;<br>    <span class="hljs-keyword">private</span> Link head;<br>    <span class="hljs-keyword">private</span> Link tail;<br>    LinkedListQueue() &#123; ... &#125;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">add</span><span class="hljs-params">(E element)</span> &#123; ... &#125;<br>    <span class="hljs-keyword">public</span> E <span class="hljs-title function_">remove</span><span class="hljs-params">()</span> &#123; ... &#125;<br>    <span class="hljs-keyword">public</span> <span class="hljs-type">int</span> <span class="hljs-title function_">size</span><span class="hljs-params">()</span> &#123; ... &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p><img src="/2025/05/04/hello-world/%E7%AC%AC%E4%B9%9D%E7%AB%A0/image-20251201154036456.png" alt="image-20251201154036456"></p><blockquote><p><strong>注释</strong>：实际上，Java 类库没有名为 <code>CircularArrayQueue</code> 和 <code>LinkedListQueue</code> 的类。这里只是以这些类为例来解释集合接口与实现在概念上的区分。如果需要一个循环数组队列，可以使用 <code>ArrayDeque</code> 类。如果需要一个链表队列，就直接使用 <code>LinkedList</code> 类，这个类实现了 <code>Queue</code> 接口。</p></blockquote><p>在程序中使用队列时，一旦已经构造了集合，你不需要知道究竟使用了哪种实现。因此，只是在构造集合对象时，才会使用具体的类。可以使用**接口类型（interface type）**存放集合引用。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java">Queue&lt;Customer&gt; expressLane = <span class="hljs-keyword">new</span> <span class="hljs-title class_">CircularArrayQueue</span>&lt;&gt;(<span class="hljs-number">100</span>);<br>expressLane.add(<span class="hljs-keyword">new</span> <span class="hljs-title class_">Customer</span>(<span class="hljs-string">&quot;Harry&quot;</span>));<br></code></pre></td></tr></table></figure><p>利用这种方法，一旦改变了想法，你可以很轻松地使用另外一种不同的实现。只需要修改程序中的一个地方（即调用构造器的语句）。如果觉得 <code>LinkedListQueue</code> 是个更好的选择，就将代码修改为：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java">Queue&lt;Customer&gt; expressLane = <span class="hljs-keyword">new</span> <span class="hljs-title class_">LinkedListQueue</span>&lt;&gt;();<br>expressLane.add(<span class="hljs-keyword">new</span> <span class="hljs-title class_">Customer</span>(<span class="hljs-string">&quot;Harry&quot;</span>));<br></code></pre></td></tr></table></figure><p>为什么选择这种实现，而不选择其他实现呢？接口本身并不能说明一种实现的效率如何。某种程度上讲，循环数组要比链表更高效，因此多数人优先选择循环数组。不过，通常来讲，这样做也需要付出一定的代价。</p><p><strong>循环数组是一个有界（bounded）集合</strong>，它的容量有限。如果程序中要收集的对象数量没有上限，就最好使用链表实现。</p><p>在研究 API 文档时，会发现另外一组名字以 Abstract 开头的类，例如，<code>AbstractQueue</code>。这些类是为类库实现者而设计的。如果想要实现你自己的队列类（也许不太可能），会发现扩展 <code>AbstractQueue</code> 类要比实现 <code>Queue</code> 接口中的所有方法更容易。</p><h4 id="9-1-2-Collection-接口"><a href="#9-1-2-Collection-接口" class="headerlink" title="9.1.2 Collection 接口"></a>9.1.2 Collection 接口</h4><p>在 Java 类库中，集合类的基本接口是 <code>Collection</code> 接口。这个接口有两个基本方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">interface</span> <span class="hljs-title class_">Collection</span>&lt;E&gt; &#123;<br>    <span class="hljs-type">boolean</span> <span class="hljs-title function_">add</span><span class="hljs-params">(E element)</span>;<br>    Iterator&lt;E&gt; <span class="hljs-title function_">iterator</span><span class="hljs-params">()</span>;<br>&#125;<br></code></pre></td></tr></table></figure><p>除了这两个方法之外，还有几个方法，稍后将会介绍。</p><ul><li><code>add</code> 方法用于向集合中添加元素。如果添加元素确实改变了集合就返回 <code>true</code>；如果集合没有发生变化就返回 <code>false</code>。例如，如果试图向集（set）中添加一个对象，而这个对象在集中已经存在，这个 <code>add</code> 请求就没有实效，因为集中不允许有重复的对象。</li><li><code>iterator</code> 方法用于返回一个实现了 <code>Iterator</code> 接口的对象。可以使用这个迭代器对象依次访问集合中的元素。下一节讨论迭代器。</li></ul><h4 id="9-1-3-迭代器"><a href="#9-1-3-迭代器" class="headerlink" title="9.1.3 迭代器"></a>9.1.3 迭代器</h4><p><code>Iterator</code> 接口包含 4 个方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">interface</span> <span class="hljs-title class_">Iterator</span>&lt;E&gt; &#123;<br>    E <span class="hljs-title function_">next</span><span class="hljs-params">()</span>;<br>    <span class="hljs-type">boolean</span> <span class="hljs-title function_">hasNext</span><span class="hljs-params">()</span>;<br>    <span class="hljs-keyword">void</span> <span class="hljs-title function_">remove</span><span class="hljs-params">()</span>;<br>    <span class="hljs-keyword">default</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">forEachRemaining</span><span class="hljs-params">(Consumer&lt;? <span class="hljs-built_in">super</span> E&gt; action)</span>;<br>&#125;<br></code></pre></td></tr></table></figure><p>通过反复调用 <code>next</code> 方法，可以逐个访问集合中的每个元素。但是，如果到达了集合的末尾，<code>next</code> 方法将抛出一个 <code>NoSuchElementException</code>。因此，在调用 <code>next</code> 之前需要调用 <code>hasNext</code> 方法。如果迭代器对象还有更多可以访问的元素，这个方法就返回 <code>true</code>。如果想要查看集合中的所有元素，就请求一个迭代器，当 <code>hasNext</code> 返回 <code>true</code> 时就反复地调用 <code>next</code> 方法。例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs java">Collection&lt;String&gt; c = ...<br>Iterator&lt;String&gt; iter = c.iterator();<br><span class="hljs-keyword">while</span> (iter.hasNext()) &#123;<br>    <span class="hljs-type">String</span> <span class="hljs-variable">element</span> <span class="hljs-operator">=</span> iter.next();<br>    <span class="hljs-keyword">do</span> something with element<br>&#125;<br></code></pre></td></tr></table></figure><p>可以更加简洁地将这个循环写为“foreach”循环：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">for</span> (String element : c)<br>    <span class="hljs-keyword">do</span> something with element<br></code></pre></td></tr></table></figure><p>编译器会把“for each”循环转换为一个带迭代器的循环。</p><p>“for each”循环可以处理任何实现了 <code>Iterable</code> 接口的对象，这个接口只有一个抽象方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">interface</span> <span class="hljs-title class_">Iterable</span>&lt;E&gt; &#123;<br>    Iterator&lt;E&gt; <span class="hljs-title function_">iterator</span><span class="hljs-params">()</span>;<br>&#125;<br></code></pre></td></tr></table></figure><p><code>Collection</code> 接口扩展了 <code>Iterable</code> 接口。因此，对于标准类库中的任何集合都可以使用“for each”循环。</p><p>也可以不写循环，而是调用 <code>forEachRemaining</code> 方法并提供一个 lambda 表达式（它会处理一个元素）。将对迭代器的每一个元素调用这个 lambda 表达式，直到再没有元素为止。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">iterator.forEachRemaining(element -&gt; <span class="hljs-keyword">do</span> something with element);<br></code></pre></td></tr></table></figure><p>访问元素的顺序取决于集合类型。如果迭代处理一个 <code>ArrayList</code>，迭代器将从索引 0 开始，每迭代一次，索引值加 1。不过，如果访问 <code>HashSet</code> 中的元素，会按照一种基本上随机的顺序获得元素。虽然可以确保在迭代过程中能够遍历到集合中的所有元素，但是无法预知访问各元素的顺序。这通常并不是什么向题，因为对于计算总和或统计匹配之类的计算，顺序并不重要。</p><blockquote><p><strong>注释</strong>：编程老手会注意到：<code>Iterator</code> 接口的 <code>next</code> 和 <code>hasNext</code> 方法与 <code>Enumeration</code> 接口的 <code>nextElement</code> 和 <code>hasMoreElements</code> 方法的作用一样。Java 集合类库的设计者本来可以选择使用 <code>Enumeration</code> 接口，但是他们不喜欢这个接口累赘的方法名，于是引入了具有较短方法名的一个新接口。</p></blockquote><p>Java 集合类库中的迭代器与其他类库中的迭代器在概念上有一个重要的区别。在传统的集合类库中，例如，C++ 的标准模板库，迭代器是根据数组索引创建的。如果给定这样一个迭代器，可以查找存储在指定位置上的元素，就像如果知道数组索引 <code>i</code>，就可以查找数组元素 <code>a[i]</code>。不需要查找元素，也可以将迭代器向前移动一个位置。这与不执行查找而通过调用 <code>i++</code> 向前移动数组索引的操作一样。但是，Java 迭代器并不是这样处理的。查找操作与位置变化紧密耦合。查找一个元素的唯一方法是调用 <code>next</code>，而在执行查找操作的同时，迭代器的位置就会随之向前移动。</p><p>因此，可以认为 Java 迭代器位于两个元素之间。当调用 <code>next</code> 时，迭代器就越过下一个元素，并返回刚刚越过的那个元素的引用（见图 9-3）。</p><p><img src="/2025/05/04/hello-world/%E7%AC%AC%E4%B9%9D%E7%AB%A0/image-20251201154700764.png" alt="image-20251201154700764"></p><blockquote><p><strong>注释</strong>：这里还可以做一个有用的对比。可以认为 <code>Iterator.next</code> 等价于 <code>InputStream.read</code>。从数据流中读取一个字节就会自动地“消耗掉”这个字节。下一次调用 <code>read</code> 将会消耗并返回输入中的下一个字节。类似地，反复地调用 <code>next</code> 就可以读取集合中的所有元素。</p></blockquote><p><code>Iterator</code> 接口的 <code>remove</code> 方法会删除上次调用 <code>next</code> 方法返回的元素。在大多数情况下，这是有道理的——在决定某个元素确实是要删除的元素之前，应该先看一下这个元素。不过，如果想要删除指定位置上的元素，仍然需要越过这个元素。例如，可以如下删除一个字符串集合中的第一个元素：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java">Iterator&lt;String&gt; it = c.iterator();<br>it.next();       <span class="hljs-comment">// skip over the first element</span><br>it.remove();     <span class="hljs-comment">// now remove it</span><br></code></pre></td></tr></table></figure><p>更重要的是，<code>next</code> 方法和 <code>remove</code> 方法调用之间存在依赖性。调用 <code>remove</code> 之前没有调用 <code>next</code>，将是不合法的。 如果这样做，将会抛出一个 <code>IllegalStateException</code> 异常。</p><p>如果想删除两个相邻的元素，不能直接这样调用：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java">it.remove();<br>it.remove(); <span class="hljs-comment">// ERROR</span><br></code></pre></td></tr></table></figure><p>实际上，必须先调用 <code>next</code> 越过将要删除的元素。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java">it.remove();<br>it.next();<br>it.remove(); <span class="hljs-comment">// OK</span><br></code></pre></td></tr></table></figure><h4 id="9-1-4-泛型实用方法"><a href="#9-1-4-泛型实用方法" class="headerlink" title="9.1.4 泛型实用方法"></a>9.1.4 泛型实用方法</h4><p>由于 <code>Collection</code> 与 <code>Iterator</code> 都是泛型接口，这意味着你可以编写处理任何集合类型的实用方法。例如，下面是一个检测任意集合是否包含指定元素的泛型方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> &lt;E&gt; <span class="hljs-type">boolean</span> <span class="hljs-title function_">contains</span><span class="hljs-params">(Collection&lt;E&gt; c, Object obj)</span> &#123;<br>    <span class="hljs-keyword">for</span> (E element : c)<br>        <span class="hljs-keyword">if</span> (element.equals(obj))<br>            <span class="hljs-keyword">return</span> <span class="hljs-literal">true</span>;<br>    <span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;<br>&#125;<br></code></pre></td></tr></table></figure><p>Java 类库的设计者认为：这些实用方法中有一些非常有用，应该将它们提供给用户使用。这样一来，类库的使用者就不必自己重新实现这些方法了。<code>contains</code> 就是这样一个实用方法。</p><p>事实上，<code>Collection</code> 接口声明了很多有用的方法，所有的实现类都必须提供这些方法。下面列举了其中的一部分：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">int</span> <span class="hljs-title function_">size</span><span class="hljs-params">()</span><br><span class="hljs-type">boolean</span> <span class="hljs-title function_">isEmpty</span><span class="hljs-params">()</span><br><span class="hljs-type">boolean</span> <span class="hljs-title function_">contains</span><span class="hljs-params">(Object obj)</span><br><span class="hljs-type">boolean</span> <span class="hljs-title function_">containsAll</span><span class="hljs-params">(Collection&lt;?&gt; c)</span><br><span class="hljs-type">boolean</span> <span class="hljs-title function_">equals</span><span class="hljs-params">(Object other)</span><br><span class="hljs-type">boolean</span> <span class="hljs-title function_">addAll</span><span class="hljs-params">(Collection&lt;? extends E&gt; from)</span><br><span class="hljs-type">boolean</span> <span class="hljs-title function_">remove</span><span class="hljs-params">(Object obj)</span><br><span class="hljs-type">boolean</span> <span class="hljs-title function_">removeAll</span><span class="hljs-params">(Collection&lt;?&gt; c)</span><br><span class="hljs-keyword">void</span> <span class="hljs-title function_">clear</span><span class="hljs-params">()</span><br><span class="hljs-type">boolean</span> <span class="hljs-title function_">retainAll</span><span class="hljs-params">(Collection&lt;?&gt; c)</span><br>Object[] toArray()<br></code></pre></td></tr></table></figure><p>在这些方法中，有许多方法的功能非常明确，不需要过多的解释。在本节末尾的 API 注释中可以找到有关它们的完整说明。</p><p>当然，如果实现 <code>Collection</code> 接口的每一个类都要提供如此多的例行方法，这将是一件很烦人的事情。为了能够让实现者更轻松一些，Java 类库提供了一个类 <code>AbstractCollection</code>，其中保持基础方法 <code>size</code> 和 <code>iterator</code> 仍为抽象方法，但是为实现者实现了其他例行方法。</p><p>例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">abstract</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">AbstractCollection</span>&lt;E&gt; <span class="hljs-keyword">implements</span> <span class="hljs-title class_">Collection</span>&lt;E&gt; &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">abstract</span> Iterator&lt;E&gt; <span class="hljs-title function_">iterator</span><span class="hljs-params">()</span>;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-type">boolean</span> <span class="hljs-title function_">contains</span><span class="hljs-params">(Object obj)</span> &#123;<br>        <span class="hljs-keyword">for</span> (E element : <span class="hljs-built_in">this</span>) <span class="hljs-comment">// calls iterator()</span><br>            <span class="hljs-keyword">if</span> (element.equals(obj))<br>                <span class="hljs-keyword">return</span> <span class="hljs-literal">true</span>;<br>        <span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;<br>    &#125;<br>    ...<br>&#125;<br></code></pre></td></tr></table></figure><p>这样一来，具体集合类可以扩展 <code>AbstractCollection</code> 类。现在要由具体的集合类提供 <code>iterator</code> 方法，而 <code>contains</code> 方法已由 <code>AbstractCollection</code> 超类提供。不过，如果子类有更加高效的方式实现 <code>contains</code> 方法，也完全可以提供 <code>contains</code> 方法。</p><p>这种做法有些过时了。这些方法最好是 <code>Collection</code> 接口的默认方法。但实际上并不是这样。不过，确实已经增加很多默认方法。其中大部分方法都与流的处理有关（有关内容将在卷Ⅱ中讨论）。另外，还有一个很有用的方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">default</span> <span class="hljs-type">boolean</span> <span class="hljs-title function_">removeIf</span><span class="hljs-params">(Predicate&lt;? <span class="hljs-built_in">super</span> E&gt; filter)</span><br></code></pre></td></tr></table></figure><p>这个方法用于删除满足某个条件的元素。</p><h3 id="9-2-集合框架中的接口"><a href="#9-2-集合框架中的接口" class="headerlink" title="9.2 集合框架中的接口"></a>9.2 集合框架中的接口</h3><p>Java 集合框架为不同类型的集合定义了大量接口，如图 9-4 所示。</p><p>集合有两个基本接口：<code>Collection</code> 和 <code>Map</code>。我们已经看到，可以用以下方法在集合中插入元素：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">boolean</span> <span class="hljs-title function_">add</span><span class="hljs-params">(E element)</span><br></code></pre></td></tr></table></figure><p>不过，映射包含键&#x2F;值对，要用 <code>put</code> 方法在映射中插入元素：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">V <span class="hljs-title function_">put</span><span class="hljs-params">(K key, V value)</span><br></code></pre></td></tr></table></figure><p>要从集合读取元素，可以用迭代器访问元素。不过，可以使用 <code>get</code> 方法从映射中读取值：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">V <span class="hljs-title function_">get</span><span class="hljs-params">(K key)</span><br></code></pre></td></tr></table></figure><p><code>List</code> 是一个<strong>有序集合（ordered collection）</strong>。元素会增加到容器中的特定位置。可以采用两种方式访问元素：使用迭代器访问，或者<strong>使用一个整数索引来访问。后面这种方法称为随机访问（random access）</strong>，因为这样可以按任意顺序访问元素。与之不同，使用迭代器访问时，必须顺序地访问元素。</p><p><img src="/2025/05/04/hello-world/%E7%AC%AC%E4%B9%9D%E7%AB%A0/image-20251201160408274.png" alt="image-20251201160408274"></p><p><code>List</code> 接口定义了多个用于随机访问的方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">void</span> <span class="hljs-title function_">add</span><span class="hljs-params">(<span class="hljs-type">int</span> index, E element)</span><br><span class="hljs-keyword">void</span> <span class="hljs-title function_">remove</span><span class="hljs-params">(<span class="hljs-type">int</span> index)</span><br>E <span class="hljs-title function_">get</span><span class="hljs-params">(<span class="hljs-type">int</span> index)</span><br>E <span class="hljs-title function_">set</span><span class="hljs-params">(<span class="hljs-type">int</span> index, E element)</span><br></code></pre></td></tr></table></figure><p><code>ListIterator</code> 接口是 <code>Iterator</code> 的一个子接口。它定义了一个方法用于在迭代器位置前面增加一个元素：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">void</span> <span class="hljs-title function_">add</span><span class="hljs-params">(E element)</span><br></code></pre></td></tr></table></figure><p>坦率地讲，集合框架的这个方面设计得很不好。实际上有两种有序集合，其性能开销有很大差异：</p><ul><li>由数组支持的有序集合可以快速地随机访问，因此适合使用 <code>List</code> 方法并提供一个整数索引来访问。</li><li>与之不同，链表尽管也是有序的，但是随机访问很慢，所以最好使用迭代器来遍历。</li></ul><p>如果原先提供两个接口就会容易一些了。</p><blockquote><p><strong>注释</strong>：为了避免对链表执行随机访问操作，Java 1.4 引入了一个标记接口 <code>RandomAccess</code>。这个接口不包含任何方法，不过可以用它来测试一个特定的集合是否支持高效的随机访问：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">if</span> (c <span class="hljs-keyword">instanceof</span> RandomAccess)<br>    use random access algorithm<br><span class="hljs-keyword">else</span><br>    use sequential access algorithm<br></code></pre></td></tr></table></figure></blockquote><p><code>Set</code> 接口等同于 <code>Collection</code> 接口，不过其方法的行为有更严谨的定义。集（set）的 <code>add</code> 方法不允许增加重复的元素。要适当地定义集的 <code>equals</code> 方法：只要两个集包含同样的元素就认为它们是相等的，而不要求这些元素有同样的顺序。<code>hashCode</code> 方法的定义要保证包含相同元素的两个集会得到相同的散列码。</p><p>既然方法签名是一样的，为什么还要建立一个单独的接口呢？从概念上讲，并不是所有集合都是集。建立一个 <code>Set</code> 接口可以允许程序员编写只接受集的方法。</p><p><code>SortedSet</code> 和 <code>SortedMap</code> 接口会提供用于排序的比较器对象，这两个接口定义了可以得到集合子集视图的方法。</p><p>最后，Java 6 引入了接口 <code>NavigableSet</code> 和 <code>NavigableMap</code>，其中包含额外的一些用于搜索和遍历有序集和映射的方法。（理想情况下，这些方法本应直接包含在 <code>SortedSet</code> 和 <code>SortedMap</code> 接口中。）<code>TreeSet</code> 和 <code>TreeMap</code> 类实现了这些接口。</p><h3 id="9-3-具体集合"><a href="#9-3-具体集合" class="headerlink" title="9.3 具体集合"></a>9.3 具体集合</h3><p>表 9-1 展示了 Java 类库中的集合，并简要描述了每个集合类的用途。（为简单起见，这里省略了线程安全集合，那些集合将在第 12 章中介绍。）</p><p><img src="/2025/05/04/hello-world/%E7%AC%AC%E4%B9%9D%E7%AB%A0/image-20251201160718124.png" alt="image-20251201160718124"></p><p>在表 9-1 中，除了以 Map 结尾的类之外，其他类都实现了 <code>Collection</code> 接口，而以 Map 结尾的类实现了 <code>Map</code> 接口。</p><p>图 9-5 显示了这些类之间的关系。</p><p><img src="/2025/05/04/hello-world/%E7%AC%AC%E4%B9%9D%E7%AB%A0/image-20251201160738547.png" alt="image-20251201160738547"></p><h4 id="9-3-1-链表"><a href="#9-3-1-链表" class="headerlink" title="9.3.1 链表"></a>9.3.1 链表</h4><p>本书的很多示例中已经使用了数组和它的动态“兄弟”——<code>ArrayList</code> 类。不过，数组和数组列表都有一个重大的缺陷：从数组中间删除一个元素开销很大，其原因是数组中位于被删除元素之后的所有元素都要向数组的前端移动（见图 9-6）。在数组中间插入一个元素也是如此。</p><p>大家都知道的另外一个数据结构——链表（linked list）解决了这个问题。数组是在连续的存储位置上存放对象引用，而链表则是将每个对象存放在单独的链接（link）中。每个链接还存放着序列中下一个链接的引用。<strong>在 Java 程序设计语言中，所有链表实际上都有双向链接（doubly linked），即每个链接还存储着其前驱的引用</strong>（见图 9-7）。</p><p><img src="/2025/05/04/hello-world/%E7%AC%AC%E4%B9%9D%E7%AB%A0/image-20251201161116849.png" alt="image-20251201161116849"></p><p><img src="/2025/05/04/hello-world/%E7%AC%AC%E4%B9%9D%E7%AB%A0/image-20251201161123947.png" alt="image-20251201161123947"></p><p>从链表中间删除一个元素是一个很轻松的操作，只需要更新所删除元素周围的链接即可（见图 9-8）。</p><p>也许你曾经在数据结构课程中学习过如何实现链表。在链表中添加或删除元素时，绕来绕去的链接可能给你留下了糟糕的印象。如果真是如此的话，你肯定会为 Java 集合类库提供了一个可以直接使用的 <code>LinkedList</code> 类而感到高兴。</p><p><img src="/2025/05/04/hello-world/%E7%AC%AC%E4%B9%9D%E7%AB%A0/image-20251201161229313.png" alt="image-20251201161229313"></p><p>在下面的代码示例中，先添加 3 个元素，然后再将第 2 个元素删除：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">var</span> <span class="hljs-variable">staff</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">LinkedList</span>&lt;String&gt;();<br>staff.add(<span class="hljs-string">&quot;Amy&quot;</span>);<br>staff.add(<span class="hljs-string">&quot;Bob&quot;</span>);<br>staff.add(<span class="hljs-string">&quot;Carl&quot;</span>);<br>Iterator&lt;String&gt; iter = staff.iterator();<br><span class="hljs-type">String</span> <span class="hljs-variable">first</span>  <span class="hljs-operator">=</span> iter.next(); <span class="hljs-comment">// visit first element</span><br><span class="hljs-type">String</span> <span class="hljs-variable">second</span> <span class="hljs-operator">=</span> iter.next(); <span class="hljs-comment">// visit second element</span><br>iter.remove();               <span class="hljs-comment">// remove last visited element</span><br></code></pre></td></tr></table></figure><p>不过链表与泛型集合之间有一个重要的区别。链表是一个有序集合（ordered collection），每个对象的位置十分重要。<code>LinkedList.add</code> 方法将对象添加到链表的尾部。但是，常常需要将元素添加到链表的中间。因为迭代器描述了集合中的位置，所以这种依赖于位置的 <code>add</code> 方法将由迭代器负责。不过，只有对自然有序的集合使用迭代器来添加元素才有意义。例如，下一节将要讨论的集（set）数据类型中，元素是完全无序的。因此，<code>Iterator</code> 接口中没有 <code>add</code> 方法。</p><p>实际上，集合类库提供了一个子接口 <code>ListIterator</code>，其中包含 <code>add</code> 方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">interface</span> <span class="hljs-title class_">ListIterator</span>&lt;E&gt; <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Iterator</span>&lt;E&gt; &#123;<br>    <span class="hljs-keyword">void</span> <span class="hljs-title function_">add</span><span class="hljs-params">(E element)</span>;<br>&#125;<br></code></pre></td></tr></table></figure><p>与 <code>Collection.add</code> 不同，这个方法不返回 <code>boolean</code> 类型的值，它假定 <code>add</code> 操作总会改变链表。</p><p>另外，<code>ListIterator</code> 接口有两个方法可以用来反向遍历链表。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java">E <span class="hljs-title function_">previous</span><span class="hljs-params">()</span><br><span class="hljs-type">boolean</span> <span class="hljs-title function_">hasPrevious</span><span class="hljs-params">()</span><br></code></pre></td></tr></table></figure><p>与 <code>next</code> 方法一样，<code>previous</code> 方法会返回越过的对象。</p><p><code>LinkedList</code> 类的 <code>listIterator</code> 方法返回一个实现了 <code>ListIterator</code> 接口的迭代器对象。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">ListIterator&lt;String&gt; iter = staff.listIterator();<br></code></pre></td></tr></table></figure><p><code>add</code> 方法在迭代器位置之前添加一个新对象。例如，下面的代码将越过链表中的第一个元素，在第二个元素之前添加 <code>&quot;Juliet&quot;</code>（见图 9-9）：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">var</span> <span class="hljs-variable">staff</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">LinkedList</span>&lt;String&gt;();<br>staff.add(<span class="hljs-string">&quot;Amy&quot;</span>);<br>staff.add(<span class="hljs-string">&quot;Bob&quot;</span>);<br>staff.add(<span class="hljs-string">&quot;Carl&quot;</span>);<br>ListIterator&lt;String&gt; iter = staff.listIterator();<br>iter.next(); <span class="hljs-comment">// skip past first element</span><br>iter.add(<span class="hljs-string">&quot;Juliet&quot;</span>);<br></code></pre></td></tr></table></figure><p><img src="/2025/05/04/hello-world/%E7%AC%AC%E4%B9%9D%E7%AB%A0/image-20251201162123418.png" alt="image-20251201162123418"></p><p>如果多次调用 <code>add</code> 方法，将按照提供元素的次序把元素添加到链表中。它们被依次添加到迭代器当前位置之前。</p><p>当用一个刚由 <code>listIterator()</code> 方法返回并指向链表表头的迭代器来调用 <code>add</code> 操作时，新添加的元素将变成列表的新表头。当迭代器越过链表的最后一个元素时（即 <code>hasNext()</code> 返回 <code>false</code> 时），添加的元素将成为列表的新表尾。</p><p>如果链表有 n 个元素，会有 n+1 个位置可以添加新元素。这些位置与迭代器的 n+1 个可能的位置相对应。例如，如果链表包含 3 个元素 A、B、C，就有 4 个位置（标记为 |）可以插入新元素：</p><figure class="highlight less"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs less">| <span class="hljs-selector-tag">A</span> <span class="hljs-selector-tag">B</span> <span class="hljs-selector-tag">C</span><br><span class="hljs-selector-tag">A</span> | <span class="hljs-selector-tag">B</span> <span class="hljs-selector-tag">C</span><br><span class="hljs-selector-tag">A</span> <span class="hljs-selector-tag">B</span> | <span class="hljs-selector-tag">C</span><br><span class="hljs-selector-tag">A</span> <span class="hljs-selector-tag">B</span> <span class="hljs-selector-tag">C</span> |<br></code></pre></td></tr></table></figure><blockquote><p><strong>注释</strong>：在用“光标”做类比时要当心。<code>remove</code> 操作与退格（Backspace）键的工作方式不太一样。在调用 <code>next</code> 之后，<code>remove</code> 方法确实会删除迭代器左侧的元素，这与退格键一样。但是，如果调用了 <code>previous</code>，则会删除迭代器右侧的元素。而且不能连续调用两次 <code>remove</code>。</p></blockquote><p><code>add</code> 方法只依赖于迭代器的位置，而 <code>remove</code> 方法不同，它依赖于迭代器的状态。</p><p>最后需要说明，<code>set</code> 方法会用一个新元素替换调用 <code>next</code> 或 <code>previous</code> 方法返回的上一个元素。例如，下面的代码将用一个新值替换列表的第一个元素：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java">ListIterator&lt;String&gt; iter = list.listIterator();<br><span class="hljs-type">String</span> <span class="hljs-variable">oldValue</span> <span class="hljs-operator">=</span> iter.next(); <span class="hljs-comment">// returns first element</span><br>iter.set(newValue);            <span class="hljs-comment">// sets first element to newValue</span><br></code></pre></td></tr></table></figure><p>可以想象，如果在某个迭代器修改集合时，另一个迭代器却在遍历这个集合，可能就会出现混乱。例如，假设一个迭代器指向一个元素前面的位置，而另一个迭代器刚刚删除了这个元素，现在前一个迭代器就是无效的，不能再使用。链表迭代器设计为可以检测到这种修改。如果一个迭代器发现它的集合被另一个迭代器修改了，或是被该集合自身的某个方法修改了，就会抛出一个 <code>ConcurrentModificationException</code> 异常。例如，考虑下面这段代码：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs java">List&lt;String&gt; list = ...<br>ListIterator&lt;String&gt; iter1 = list.listIterator();<br>ListIterator&lt;String&gt; iter2 = list.listIterator();<br>iter1.next();<br>iter1.remove();<br>iter2.next(); <span class="hljs-comment">// throws ConcurrentModificationException</span><br></code></pre></td></tr></table></figure><p>因为 <code>iter2</code> 检测出这个列表被外部修改，所以调用 <code>iter2.next</code> 会抛出一个 <code>ConcurrentModificationException</code> 异常。</p><p>为了避免发生并发修改异常，请遵循这样一个简单的规则：可以根据需要为一个集合关联多个迭代器，前提是这些迭代器只能读取集合。或者，可以关联一个能同时读写的迭代器。</p><p>检测并发修改的做法很简单。集合会跟踪更改操作（诸如添加或删除元素）的次数。每个迭代器都会为它负责的更改操作维护一个单独的更改操作数。在每个迭代器方法的开始处，迭代器会检查它自己的更改操作数与集合的更改操作数是否相等。如果不一致，就抛出一个 <code>ConcurrentModificationException</code> 异常。</p><blockquote><p>注释：不过，对于并发修改的检测有一个奇怪的例外。链表只跟踪对列表的结构性修改，例如，添加和删除链接。<code>set</code> 方法不被视为结构性修改。可以为一个链表关联多个迭代器，所有迭代器都可以调用 <code>set</code> 方法改变现有链接的内容。本章后面介绍的 <code>Collections</code> 类的许多算法都需要使用这个功能。</p></blockquote><p>现在我们已经了解了 <code>LinkedList</code> 类的基本方法。可以使用 <code>ListIterator</code> 类从前后两个方向遍历链表中的元素，以及添加和删除元素。</p><p>在 9.2 节已经看到，<code>Collection</code> 接口还声明了操作链表的很多其他有用的方法。其中大部分方法都是在 <code>LinkedList</code> 类的超类 <code>AbstractCollection</code> 中实现的。例如，<code>toString</code> 方法会调用所有元素的 <code>toString</code>，并生成一个格式为 <code>[A, B, C]</code> 的长字符串。这为调试工作提供了便利。可以使用 <code>contains</code> 方法检测某个元素是否出现在链表中。例如，如果链表中已经包含一个等于 <code>&quot;Harry&quot;</code> 的字符串，调用 <code>staff.contains(&quot;Harry&quot;)</code> 将会返回 <code>true</code>。</p><p>Java 类库还提供了许多理论上存在一定争议的方法。链表不支持快速随机访问。如果要查看链表中的第 n 个元素，就必须从头开始，越过 n-1 个元素。没有捷径可走。鉴于这个理由，需要按整数索引访问元素时，程序员通常不选用链表。</p><p>然而，<code>LinkedList</code> 类还是提供了一个 <code>get</code> 方法，用来访问某个特定元素：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java">LinkedList&lt;String&gt; list = ...;<br><span class="hljs-type">String</span> <span class="hljs-variable">obj</span> <span class="hljs-operator">=</span> list.get(n);<br></code></pre></td></tr></table></figure><p>当然，这个方法的效率不太高。如果你发现自己正在使用这个方法，说明对于所要解决的问题，你可能使用了错误的数据结构。</p><p>绝对不要使用这个“虚假”的随机访问方法来遍历链表。下面这段代码的效率极低：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>; i &lt; list.size(); i++)<br>    <span class="hljs-keyword">do</span> something with list.get(i);<br></code></pre></td></tr></table></figure><p>每次查找一个元素都要从列表开头重新开始搜索。<code>LinkedList</code> 对象根本不会缓存位置信息。</p><blockquote><p>注释：<code>get</code> 方法做了一个微小的优化：如果索引大于等于 <code>size() / 2</code>，就从列表尾端开始搜索元素。</p></blockquote><p>列表迭代器接口还有一个方法，可以告诉你当前位置的索引。实际上，从概念上讲，因为 Java 迭代器指向两个元素之间的位置，所以可以有两个索引：<code>nextIndex</code> 方法返回下一次调用 <code>next</code> 方法时所返回元素的整数索引；<code>previousIndex</code> 方法返回下一次调用 <code>previous</code> 方法时所返回元素的整数索引。当然，这个索引只比 <code>nextIndex</code> 返回的索引值小 1。这两个方法的效率非常高，因为迭代器会维护当前位置的计数值。最后需要说明一点，如果有一个整数索引 <code>n</code>，<code>list.listIterator(n)</code> 将返回一个迭代器，这个迭代器指向索引为 <code>n</code> 的元素前面的位置。也就是说，调用 <code>next</code> 与调用 <code>list.get(n)</code> 会得到同一个元素，只是获得迭代器的效率比较低。</p><p>如果链表中只有很少几个元素，就完全没有必要为 <code>get</code> 方法和 <code>set</code> 方法的开销而烦恼。但是，既然如此，最初为什么要使用链表呢？使用链表的唯一理由是尽可能地减少在列表中间插入或删除元素的开销。如果列表只有很少几个元素，就完全可以使用 <code>ArrayList</code>。</p><p>建议一定要远离所有使用整数索引表示链表中位置的方法。如果需要对集合进行随机访问，就使用数组或 <code>ArrayList</code>，而不要使用链表。</p><p>程序清单 9-1 中的程序具体使用了链表。它创建了两个列表，将它们合并在一起，然后从第二个列表中每隔一个元素删除一个元素，最后测试 <code>removeAll</code> 方法。建议跟踪一下程序流程，要特别注意迭代器。可以画出迭代器位置示意图，你会发现这很有帮助，如下所示：</p><figure class="highlight less"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs less"><span class="hljs-selector-tag">A</span> <span class="hljs-selector-tag">C</span> <span class="hljs-selector-tag">E</span> <span class="hljs-selector-tag">B</span> <span class="hljs-selector-tag">D</span> <span class="hljs-selector-tag">F</span> <span class="hljs-selector-tag">G</span><br><span class="hljs-selector-tag">A</span> <span class="hljs-selector-tag">C</span> <span class="hljs-selector-tag">E</span> <span class="hljs-selector-tag">B</span> <span class="hljs-selector-tag">D</span> <span class="hljs-selector-tag">F</span> <span class="hljs-selector-tag">G</span><br><span class="hljs-selector-tag">A</span> <span class="hljs-selector-tag">B</span> | <span class="hljs-selector-tag">C</span> <span class="hljs-selector-tag">E</span> <span class="hljs-selector-tag">B</span> | <span class="hljs-selector-tag">D</span> <span class="hljs-selector-tag">F</span> <span class="hljs-selector-tag">G</span><br></code></pre></td></tr></table></figure><p>注意以下调用：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">System.out.println(a);<br></code></pre></td></tr></table></figure><p>这会调用 <code>AbstractCollection</code> 类中的 <code>toString</code> 方法，打印链表 <code>a</code> 中的所有元素。</p><h4 id="9-3-2-数组列表"><a href="#9-3-2-数组列表" class="headerlink" title="9.3.2 数组列表"></a>9.3.2 数组列表</h4><p>在上一节中，我们了解了 <code>List</code> 接口和实现了这个接口的 <code>LinkedList</code> 类。<code>List</code> 接口描述一个有序集合，其中每个元素的位置很重要。有两种访问元素的协议：一种是通过迭代器，另一种是通过 <code>get</code> 和 <code>set</code> 方法随机访问。后者不适用于链表，但当然 <code>get</code> 和 <code>set</code> 方法对数组很有用。</p><p>集合类库提供了我们熟悉的 <code>ArrayList</code> 类，这个类也实现了 <code>List</code> 接口。<code>ArrayList</code> 封装了一个动态再分配的对象数组。</p><blockquote><p>注释：对于一个经验丰富的 Java 程序员来说，需要一个动态数组时，可能会使用 <code>Vector</code> 类。为什么要用 <code>ArrayList</code> 而不是 <code>Vector</code> 呢？原因很简单：<code>Vector</code> 类的所有方法都是同步的。可以安全地从两个线程访问一个 <code>Vector</code> 对象。但是，如果只从一个线程访问 <code>Vector</code>（这种情况更为常见），代码就会在同步操作上白白浪费大量的时间。而与之不同，<code>ArrayList</code> 方法不是同步的，因此，不需要同步时建议使用 <code>ArrayList</code>，而不要使用 <code>Vector</code>。</p></blockquote><h4 id="9-3-3-散列集"><a href="#9-3-3-散列集" class="headerlink" title="9.3.3 散列集"></a>9.3.3 散列集</h4><p>链表和数组允许你根据意愿指定元素的次序。但是，如果想要查找某个特定的元素，却又不记得它的位置，就需要访问所有元素，直到找到匹配的元素为止。如果集合中包含的元素很多，这就会耗费很长时间。如果不在意元素的顺序，还有几种数据结构允许你更快速地查找元素。缺点是，这些数据结构不允许你控制元素出现的次序，它们会按照对自己最方便的方式组织元素。</p><p>有一种众所周知的数据结构，可以用于快速地查找对象，这就是散列表（hash table）。散列表为每个对象计算一个整数，称为散列码（hash code）。散列码是以某种方式由对象的实例字段得出的一个整数，这种方式可以尽可能保证有不同数据的对象将生成不同的散列码。表 9-2 列出了几个散列码的示例，它们是由 <code>String</code> 类的 <code>hashCode</code> 方法得到的。</p><p><img src="/2025/05/04/hello-world/%E7%AC%AC%E4%B9%9D%E7%AB%A0/image-20251201165146703.png" alt="image-20251201165146703"></p><p>如果定义你自己的类，你就要负责实现自己的 <code>hashCode</code> 方法。有关 <code>hashCode</code> 方法的详细内容请参见第 5 章。注意，你的实现应该与 <code>equals</code> 方法兼容，即如果 <code>a.equals(b)</code> 为 <code>true</code>，那么 <code>a</code> 与 <code>b</code> 必须有相同的散列码。</p><p>现在，重要的是要能够快速地计算出散列码，并且这个计算只与要计算散列的那个对象的状态有关，与散列表中的其他对象无关。</p><p><strong>在 Java 中，散列表实现为链表数组。每个列表被称为桶（bucket）</strong>（参见图 9-10）。要想查找一个对象在表中的位置，就要先计算它的散列码，然后与桶的总数取余，所得到的数就是保存这个元素的那个桶的索引。例如，如果某个对象的散列码为 76268，总共有 128 个桶，那么这个对象应该保存在第 108 号桶中（因为 76268 % 128 的余数是 108）。或许很幸运，这个桶中没有其他元素，此时将元素直接插入这个桶中就可以了。当然，有时候会遇到桶已经填充了元素的情况。这种现象被称为散列冲突（hash collision）。这时，需要将新对象与那个桶中的所有对象进行比较，查看这个对象是否已经存在。如果散列码合理地随机分布，而且桶的数量足够大，需要比较的次数就会很少。</p><p><img src="/2025/05/04/hello-world/%E7%AC%AC%E4%B9%9D%E7%AB%A0/image-20251201165310431.png" alt="image-20251201165310431"></p><blockquote><p>注释：在 Java 8 中，桶满时会从链表变为平衡二叉树。如果选择的散列函数不好，会产生很多冲突，或者如果有恶意代码试图在散列表中填充多个有相同散列码的值，改为平衡二叉树能提高性能。</p></blockquote><blockquote><p>提示：散列表的键要尽可能属于一个实现了 <code>Comparable</code> 接口的类。这样一来，就能保证不会由于散列码分布不均匀而导致性能低下。</p></blockquote><p>如果想更多地控制散列表的性能，可以指定一个初始的桶数。桶数是指用于收集有相同散列值的桶的数目。如果要插入到散列表中的元素太多，冲突数就会增加，这会降低检索性能。</p><p>如果大致知道最终会有多少个元素要插入散列表中，就可以设置桶数。通常，要将桶数设置为预计元素个数的 75%～150%。有些研究人员认为：将桶数设置为一个素数是一个好主意，以防止键的聚集。不过，对此并没有确凿的证据。标准类库使用的桶数是 2 的幂，默认值为 16（为表大小提供的任何值都将自动调整为 2 的下一个幂值）。</p><p>当然，并不总是能够知道需要存储多少个元素，也有可能最初的估计过低。如果散列表太满，就需要再散列（rehashed）。如果要对散列表再散列，就需要创建一个桶数更多的表，并将所有元素插入这个新表中，然后丢弃原来的表。装填因子（load factor） 可以确定何时对散列表进行再散列。例如，如果装填因子为 0.75（默认值），而表中已经填满了 75% 以上，就会自动再散列，新表的桶数是原来的两倍。对于大多数应用来说，装填因子为 0.75 是合理的。</p><p>散列表可以用于实现很多重要的数据结构。其中最简单的是集类型。集是没有重复元素的元素集合。集的 <code>add</code> 方法首先尝试在这个集中查找要添加的对象，只有这个元素不存在时才会添加这个对象。</p><p>Java 集合类库提供了一个 <code>HashSet</code> 类，它基于散列表实现了一个集。可以用 <code>add</code> 方法添加元素。<code>contains</code> 方法被重新定义，以便可以快速查找一个元素是否已经在集中。它只查看一个桶中的元素，而不必查看集合中的所有元素。</p><p>散列集迭代器将依次访问所有的桶。因为散列将元素分散存放在表中，所以会以一种看起来随机的顺序访问元素。只有不关心集合中元素的顺序时才应该使用 <code>HashSet</code>。</p><blockquote><p>警告：在更改集中的元素时要格外小心。 如果元素的散列码发生了改变，这个元素在 数据结构中的位置也会变化。</p></blockquote><h4 id="9-3-4-树集"><a href="#9-3-4-树集" class="headerlink" title="9.3.4 树集"></a>9.3.4 树集</h4><p><code>TreeSet</code> 类与散列集十分类似，不过，它比散列集有所改进。树集是一个有序集合（sorted collection）。可以以任意顺序将元素插入集合中。在对集合进行遍历时，值将自动地按照排序后的顺序出现。例如，假设插入 3 个字符串，然后访问添加的所有元素：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">var</span> <span class="hljs-variable">sorter</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">TreeSet</span>&lt;String&gt;();<br>sorter.add(<span class="hljs-string">&quot;Bob&quot;</span>);<br>sorter.add(<span class="hljs-string">&quot;Amy&quot;</span>);<br>sorter.add(<span class="hljs-string">&quot;Carl&quot;</span>);<br><span class="hljs-keyword">for</span> (String s : sorter) System.out.println(s);<br></code></pre></td></tr></table></figure><p>这时，值将按照有序顺序打印：</p><figure class="highlight ebnf"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs ebnf"><span class="hljs-attribute">Amy</span><br><span class="hljs-attribute">Bob</span><br><span class="hljs-attribute">Carl</span><br></code></pre></td></tr></table></figure><p>正如 <code>TreeSet</code> 类名所示，排序是用一个树数据结构完成的（当前实现使用的是红黑树（red-black tree）。有关红黑树的详细介绍请参见 Introduction to Algorithms，作者是 Thomas Cormen、Charles Leiserson、Ronald Rivest 和 Clifford Stein [The MIT Press, 2009]）。每次将一个元素添加到树中时，都会将其放置在正确的排序位置上。因此，迭代器总是以有序的顺序访问每个元素。</p><p>将一个元素添加到树中要比添加到散列表中慢，参见表 9-3 中的比较，但是，与检查数组或链表中的重复元素相比，使用树还是会快得多。如果树中包含 n 个元素，查找新元素的正确位置平均需要 log₂n 次比较。例如，如果一棵树包含了 1000 个元素，添加一个新元素大约需要比较 10 次。</p><p><img src="/2025/05/04/hello-world/%E7%AC%AC%E4%B9%9D%E7%AB%A0/image-20251203152034030.png" alt="image-20251203152034030"></p><blockquote><p>注释：要使用树集，必须能够比较元素。这些元素必须实现 <code>Comparable</code> 接口，或者构造集时必须提供一个 <code>Comparator</code>（<code>Comparable</code> 和 <code>Comparator</code> 接口在第 6 章介绍过）。</p></blockquote><p>回头看表 9-3，你可能很想知道是否应该总是使用树集而不是散列集。毕竟，添加元素所花费的时间看上去并没有增加太多，而且元素会自动排序。答案取决于你所要收集的数据。如果不需要数据有序，就没有必要付出排序的开销。更重要的是，对于某些数据来说，对其进行排序要比给出一个散列函数更加困难。散列函数只需要将对象适当地打乱存放，而比较函数必须精确地区分各个对象。</p><p>为了具体地了解它们之间的差异，可以考虑收集一个矩形集的任务。如果使用 <code>TreeSet</code>，就需要提供 <code>Comparator&lt;Rectangle&gt;</code>。如何比较两个矩形呢？按面积比较吗？这行不通。可能会有两个不同的矩形，它们的坐标不同，但面积却相同。树的排序顺序必须是全序（total ordering）。也就是说，任意两个元素都必须是可比较的，只有在两个元素相等时比较结果才为 0。矩形确实有一种排序方式（按照坐标的词典顺序排序），但这很牵强，而且计算很麻烦。相比之下，已经为 <code>Rectangle</code> 类定义了散列函数，它直接对坐标计算散列。</p><blockquote><p>注释：从 Java 6 起，<code>TreeSet</code> 类实现了 <code>NavigableSet</code> 接口。这个接口增加了几个便利方法，用于查找元素以及反向遍历。详细信息请参见 API 注释。</p></blockquote><h4 id="9-3-5-队列与双端队列"><a href="#9-3-5-队列与双端队列" class="headerlink" title="9.3.5 队列与双端队列"></a>9.3.5 队列与双端队列</h4><p>前面已经讨论过，队列允许高效地在队尾添加元素，并在队头删除元素。</p><p>双端队列（deque）在队头和队尾都能高效地添加或删除元素。不支持在队列中间添加元素。</p><p>Java 6 中引入了 <code>Deque</code> 接口，<code>ArrayDeque</code> 和 <code>LinkedList</code> 类实现了这个接口。这两个类都可以提供双端队列，其大小可以根据需要扩展。</p><h4 id="9-3-6-优先队列"><a href="#9-3-6-优先队列" class="headerlink" title="9.3.6 优先队列"></a>9.3.6 优先队列</h4><p>优先队列（priority queue）中的元素可以按照任意的顺序插入，但会按照有序的顺序获取。也就是说，调用 <code>remove</code> 方法时，总会获得当前优先队列中最小的元素。不过，优先队列并没有对所有元素进行排序。如果迭代处理这些元素，并不需要对它们进行排序。</p><p>优先队列使用了一个精巧且高效的数据结构，称为堆（heap）。堆是一个自组织的二叉树，其添加（<code>add</code>）和删除（<code>remove</code>）操作会让最小的元素移动到根，而不必花费时间对元素进行排序。</p><p>与 <code>TreeSet</code> 一样，优先队列既可以包含实现了 <code>Comparable</code> 接口的类对象，也可以包含构造器中提供的 <code>Comparator</code> 对象。</p><p>优先队列的典型用法是任务调度。每一个任务有一个优先级，任务以随机顺序添加到队列中。每当启动一个新的任务时，将从队列中删除优先级最高的任务（因为习惯将 1 作为“最高”优先级，所以 <code>remove</code> 操作将删除最小的元素）。</p><h3 id="9-4-映射"><a href="#9-4-映射" class="headerlink" title="9.4 映射"></a>9.4 映射</h3><p>作为一个集合，集允许你快速地查找现有的元素。但是，要查找一个元素，需要有所查找的那个元素的准确副本。这不是一种常见的查找方式。通常，我们知道某些关键信息，希望查找与之关联的元素。映射（map）数据结构就是为此设计的。映射用来存放键&#x2F;值对。如果提供了键，可以查找一个值。例如，可以存储一个员工记录表，其中键为员工 ID，值为 <code>Employee</code> 对象。在下面的小节中，我们会学习如何使用映射。</p><h4 id="9-4-1-基本映射操作"><a href="#9-4-1-基本映射操作" class="headerlink" title="9.4.1 基本映射操作"></a>9.4.1 基本映射操作</h4><p>Java 类库为映射提供了两个通用的实现：<code>HashMap</code> 和 <code>TreeMap</code>。这两个类都实现了 <code>Map</code> 接口。</p><ul><li>散列映射对键进行散列，树映射根据键的顺序将它们组织为一个搜索树。</li><li>散列或比较函数只应用于键。与键关联的值不进行散列或比较。</li></ul><p>应该选择散列映射还是树映射呢？与集一样，散列稍微快一些，如果不需要按照有序的顺序访问键，最好选择散列映射。</p><p>可以如下建立一个散列映射来存储员工信息：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">var</span> <span class="hljs-variable">staff</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">HashMap</span>&lt;String, Employee&gt;(); <span class="hljs-comment">// HashMap implements Map</span><br><span class="hljs-type">var</span> <span class="hljs-variable">harry</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Employee</span>(<span class="hljs-string">&quot;Harry Hacker&quot;</span>);<br>staff.put(<span class="hljs-string">&quot;987-98-9996&quot;</span>, harry);<br></code></pre></td></tr></table></figure><p>每当向映射中添加一个对象时，必须同时提供一个键。在这里，键是一个字符串，对应的值是 <code>Employee</code> 对象。</p><p>要获取一个对象，必须使用键（因此必须记住键）：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">var</span> <span class="hljs-variable">id</span> <span class="hljs-operator">=</span> <span class="hljs-string">&quot;987-98-9996&quot;</span>;<br><span class="hljs-type">Employee</span> <span class="hljs-variable">e</span> <span class="hljs-operator">=</span> staff.get(id); <span class="hljs-comment">// gets harry</span><br></code></pre></td></tr></table></figure><p>如果映射中没有存储与指定键对应的信息，<code>get</code> 将返回 <code>null</code>。</p><p><code>null</code> 返回值可能并不方便。有时对于没有出现在映射中的键，可以有一个合适的默认值。然后使用 <code>getOrDefault</code> 方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java">Map&lt;String, Integer&gt; scores = ...;<br><span class="hljs-type">int</span> <span class="hljs-variable">score</span> <span class="hljs-operator">=</span> scores.getOrDefault(id, <span class="hljs-number">0</span>); <span class="hljs-comment">// gets 0 if the id is not present</span><br></code></pre></td></tr></table></figure><p>键必须是唯一的。不能对同一个键存放两个值。如果用同一个键调用两次 <code>put</code> 方法，第二个值就会取代第一个值。实际上，<code>put</code> 将返回与这个键参数关联的上一个值。</p><p><code>remove</code> 方法从映射中删除给定键对应的元素。<code>size</code> 方法返回映射中的元素数。</p><p>要迭代处理映射的键和值，最容易的方法是使用 <code>forEach</code> 方法。可以提供一个接收键和值的 lambda 表达式。映射中的每一项会依序调用这个表达式：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java">scores.forEach((k, v) -&gt;<br>    System.out.println(<span class="hljs-string">&quot;key=&quot;</span> + k + <span class="hljs-string">&quot;, value=&quot;</span> + v));<br></code></pre></td></tr></table></figure><p>程序清单 9-6 显示了映射的具体使用。首先将键&#x2F;值对添加到映射中。然后，从映射中删除一个键，同时与之关联的值也会删除。接下来，修改与某一个键关联的值，并调用 <code>get</code> 方法查找一个值。最后，迭代处理元素集。</p><h4 id="9-4-2-更新映射条目"><a href="#9-4-2-更新映射条目" class="headerlink" title="9.4.2 更新映射条目"></a>9.4.2 更新映射条目</h4><p>处理映射的一个难点是更新映射条目。正常情况下，可以得到与一个键关联的旧值，更新这个值，再放回更新后的值。不过，必须考虑一个特殊情况，即键第一次出现。</p><p>下面来看一个例子，考虑使用映射来统计一个单词在文件中出现的频度。看到一个单词（word）时，我们将计数器增 1，如下所示：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">counts.put(word, counts.get(word) + <span class="hljs-number">1</span>);<br></code></pre></td></tr></table></figure><p>这是可以的，不过有一种情况除外：就是第一次看到 word 时。在这种情况下，<code>get</code> 会返回 <code>null</code>，因此会出现一个 <code>NullPointerException</code> 异常。</p><p>一种简单的补救是使用 <code>getOrDefault</code> 方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">counts.put(word, counts.getOrDefault(word, <span class="hljs-number">0</span>) + <span class="hljs-number">1</span>);<br></code></pre></td></tr></table></figure><p>另一种方法是首先调用 <code>putIfAbsent</code> 方法。只有当键原先不存在（或者映射到 <code>null</code>）时才放入一个值：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java">counts.putIfAbsent(word, <span class="hljs-number">0</span>);<br>counts.put(word, counts.get(word) + <span class="hljs-number">1</span>); <span class="hljs-comment">// now we know that get will succeed</span><br></code></pre></td></tr></table></figure><p>不过还可以做得更好。<code>merge</code> 方法可以简化这个常见操作。如果键原先不存在，下面的调用：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">counts.merge(word, <span class="hljs-number">1</span>, Integer::sum);<br></code></pre></td></tr></table></figure><p>将把 word 与 1 关联，否则使用 <code>Integer::sum</code> 函数组合原值和 1（也就是将原值与 1 求和）。</p><p>API 注释还描述了另外一些更新映射条目的方法，不过这些方法不太常用。</p><h4 id="9-4-3-映射视图"><a href="#9-4-3-映射视图" class="headerlink" title="9.4.3 映射视图"></a>9.4.3 映射视图</h4><p>集合框架不认为映射本身是一个集合。（其他数据结构框架则认为映射是一个键&#x2F;值对集合，或者是按键索引的值集合。）不过，可以得到映射的视图（view）——实现了 <code>Collection</code> 接口或某个子接口的对象。</p><p>有 3 种视图：键集、值集合（不是一个集）以及键&#x2F;值对集。键和键&#x2F;值对可以构成一个集，因为映射中一个键只能有一个副本。下面的方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java">Set&lt;K&gt; <span class="hljs-title function_">keySet</span><span class="hljs-params">()</span><br>Collection&lt;V&gt; <span class="hljs-title function_">values</span><span class="hljs-params">()</span><br>Set&lt;Map.Entry&lt;K, V&gt;&gt; entrySet()<br></code></pre></td></tr></table></figure><p>会分别返回这 3 个视图。（映射条目集的元素是实现了 <code>Map.Entry</code> 接口的类的对象。）</p><p>需要说明的是，<code>keySet</code> 不是 <code>HashSet</code> 或 <code>TreeSet</code>，而是实现了 <code>Set</code> 接口的另外某个类的对象。<code>Set</code> 接口扩展了 <code>Collection</code> 接口。因此，可以像使用任何集合一样使用 <code>keySet</code>。</p><p>例如，可以枚举一个映射的所有键：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java">Set&lt;String&gt; keys = map.keySet();<br><span class="hljs-keyword">for</span> (String key : keys)<br>    <span class="hljs-keyword">do</span> something with key<br></code></pre></td></tr></table></figure><p>如果想同时查看键和值，可以通过枚举映射条目来避免查找值。可以使用以下代码：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">for</span> (Map.Entry&lt;String, Employee&gt; entry : staff.entrySet()) &#123;<br>    <span class="hljs-type">String</span> <span class="hljs-variable">k</span> <span class="hljs-operator">=</span> entry.getKey();<br>    <span class="hljs-type">Employee</span> <span class="hljs-variable">v</span> <span class="hljs-operator">=</span> entry.getValue();<br>    <span class="hljs-keyword">do</span> something with k, v<br>&#125;<br></code></pre></td></tr></table></figure><blockquote><p>提示：通过使用 <code>var</code> 声明可以避免笨重的 <code>Map.Entry</code>。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">for</span> (<span class="hljs-keyword">var</span> entry : map.entrySet())&#123;<br> <span class="hljs-keyword">do</span> something with entry.getKey(), entry.getValue()<br>&#125;<br></code></pre></td></tr></table></figure><p>或者直接使用 <code>forEach</code> 方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java">map.forEach((k, v) -&gt;&#123;<br> <span class="hljs-keyword">do</span> something with k, v<br>&#125;);<br></code></pre></td></tr></table></figure></blockquote><p>如果在键集视图上调用迭代器的 <code>remove</code> 方法，实际上会从映射中删除这个键和与它关联的值。不过，不能向键集视图中添加元素。另外，如果只添加一个键而没有同时添加值也是没有意义的。如果试图调用 <code>add</code> 方法，它会抛出一个 <code>UnsupportedOperationException</code>。映射条目集视图有同样的限制，尽管理论上好像可以增加新的键&#x2F;值对，但实际上并不允许。</p><h4 id="9-4-4-弱散列映射"><a href="#9-4-4-弱散列映射" class="headerlink" title="9.4.4 弱散列映射"></a>9.4.4 弱散列映射</h4><p>在集合类库中有几个专用的映射类，我们将在这一节和后面几节中对它们做简要介绍。</p><p>设计 <code>WeakHashMap</code> 类是为了解决一个有趣的问题。如果有一个值，它对应的键已经不再在程序中的任何地方使用，将会出现什么情况？假设对某个键的最后一个引用已经消失，那么再没有任何途径可以引用这个值对象了。但是，由于程序中的任何部分都不再有这个键，因此无法从映射中删除这个键&#x2F;值对。为什么垃圾回收器不能删除它呢？删除无用的对象不就是垃圾回收器的工作吗？</p><p>遗憾的是，事情没有这么简单。垃圾回收器会跟踪活动的对象。只要映射对象是活动的，其中的所有桶就是活动的，它们就不能被回收。因此，需要由程序负责从长期存活的映射中删除那些无用的值。或者，你可以使用 <code>WeakHashMap</code>。当键的唯一引用来自散列表条目时，这个数据结构将与垃圾回收器合作删除键&#x2F;值对。</p><p>下面介绍这种机制的内部工作原理。<code>WeakHashMap</code> 使用弱引用（weak reference）保存键。<code>WeakReference</code> 对象将包含另一个对象的引用，在这里，就是一个散列表键。对于这种类型的对象，垃圾回收器采用一种特殊的方式进行处理。正常情况下，如果垃圾回收器发现某个特定的对象已经没有引用了，就会将其回收。不过，如果这个对象只能由一个 <code>WeakReference</code> 引用，垃圾回收器也会将其回收，但会将引用这个对象的弱引用放入一个队列。<code>WeakHashMap</code> 的操作会定期地检查这个队列，查找新加入的弱引用。一个弱引用进入队列意味着这个键不再由任何人使用，并且已经回收。于是，<code>WeakHashMap</code> 将删除相关联的映射条目。</p><h4 id="9-4-5-链接散列集与映射"><a href="#9-4-5-链接散列集与映射" class="headerlink" title="9.4.5 链接散列集与映射"></a>9.4.5 链接散列集与映射</h4><p><code>LinkedHashSet</code> 和 <code>LinkedHashMap</code> 类会记住插入元素项的顺序。这样可以避免散列表中看起来随机的元素顺序。在散列表中插入元素项时，它们会加入一个双向链表中（见图 9-11）。</p><p><img src="/2025/05/04/hello-world/%E7%AC%AC%E4%B9%9D%E7%AB%A0/image-20251203153023449.png" alt="image-20251203153023449"></p><p>例如，考虑程序清单 9-6 中的以下映射插入操作：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">var</span> <span class="hljs-variable">staff</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">LinkedHashMap</span>&lt;String, Employee&gt;();<br>staff.put(<span class="hljs-string">&quot;144-25-5464&quot;</span>, <span class="hljs-keyword">new</span> <span class="hljs-title class_">Employee</span>(<span class="hljs-string">&quot;Amy Lee&quot;</span>));<br>staff.put(<span class="hljs-string">&quot;567-24-2546&quot;</span>, <span class="hljs-keyword">new</span> <span class="hljs-title class_">Employee</span>(<span class="hljs-string">&quot;Harry Hacker&quot;</span>));<br>staff.put(<span class="hljs-string">&quot;157-62-7935&quot;</span>, <span class="hljs-keyword">new</span> <span class="hljs-title class_">Employee</span>(<span class="hljs-string">&quot;Gary Cooper&quot;</span>));<br>staff.put(<span class="hljs-string">&quot;456-62-5527&quot;</span>, <span class="hljs-keyword">new</span> <span class="hljs-title class_">Employee</span>(<span class="hljs-string">&quot;Francesca Cruz&quot;</span>));<br></code></pre></td></tr></table></figure><p>然后，<code>staff.keySet().iterator()</code> 会以下面的顺序枚举键：</p><figure class="highlight apache"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs apache"><span class="hljs-attribute">144</span>-<span class="hljs-number">25</span>-<span class="hljs-number">5464</span><br><span class="hljs-attribute">567</span>-<span class="hljs-number">24</span>-<span class="hljs-number">2546</span><br><span class="hljs-attribute">157</span>-<span class="hljs-number">62</span>-<span class="hljs-number">7935</span><br><span class="hljs-attribute">456</span>-<span class="hljs-number">62</span>-<span class="hljs-number">5527</span><br></code></pre></td></tr></table></figure><p>然后，<code>staff.keySet().iterator()</code> 会以下面的顺序枚举键：</p><figure class="highlight ebnf"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs ebnf"><span class="hljs-attribute">Amy Lee</span><br><span class="hljs-attribute">Harry Hacker</span><br><span class="hljs-attribute">Gary Cooper</span><br><span class="hljs-attribute">Francesca Cruz</span><br></code></pre></td></tr></table></figure><p>或者，链接散列映射可以使用访问顺序（access order）而不是插入顺序来迭代处理映射条目。每次调用 <code>get</code> 或 <code>put</code> 时，受到影响的条目将从当前位置删除，并放在条目链表的末尾（只影响条目链表中的位置，而不影响散列表的桶）。一个映射条目总是在键散列码对应的桶中）。要构造这样一个散列映射，需要调用</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">LinkedHashMap&lt;K, V&gt;(initialCapacity, loadFactor, <span class="hljs-literal">true</span>)<br></code></pre></td></tr></table></figure><p>访问顺序对于实现缓存的“最近最少使用”原则十分重要。例如，你可能希望将访问频率高的元素放在内存中，而从数据库读取访问频率低的元素。如果在映射表中没有找到某个元素，而且此时映射表已经很满，可以得到映射表的一个迭代器，并删除它枚举的前几个元素，它们是最近最少使用的几个元素。</p><p>甚至可以自动完成这一过程。构造 <code>LinkedHashMap</code> 的一个子类，然后覆盖下面这个方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">protected</span> <span class="hljs-type">boolean</span> <span class="hljs-title function_">removeEldestEntry</span><span class="hljs-params">(Map.Entry&lt;K, V&gt; eldest)</span> &#123;<br>    <span class="hljs-keyword">return</span> size() &gt; <span class="hljs-number">100</span>;<br>&#125;<br></code></pre></td></tr></table></figure><p>或者，还可以考虑 eldest 映射条目来决定是否将它删除。例如，可以检查随这个映射条目存储的一个时间戳。</p><h4 id="9-4-6-枚举集与映射"><a href="#9-4-6-枚举集与映射" class="headerlink" title="9.4.6 枚举集与映射"></a>9.4.6 枚举集与映射</h4><p><code>EnumSet</code> 是一个高效的集实现，其元素属于一个枚举类型。因为枚举类型只有有限个实例，所以 <code>EnumSet</code> 在内部实现为一个位序列。如果对应的值在集中出现，相应的位置为 1。</p><p><code>EnumSet</code> 类没有公共构造器。要使用静态工厂方法构造这个集：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">enum</span> <span class="hljs-title class_">Weekday</span> &#123; MONDAY, TUESDAY, WEDNESDAY, THURSDAY, FRIDAY, SATURDAY, SUNDAY &#125;<br>EnumSet&lt;Weekday&gt; always = EnumSet.allOf(Weekday.class);<br>EnumSet&lt;Weekday&gt; never = EnumSet.noneOf(Weekday.class);<br>EnumSet&lt;Weekday&gt; workday = EnumSet.range(Weekday.MONDAY, Weekday.FRIDAY);<br>EnumSet&lt;Weekday&gt; mf = EnumSet.of(Weekday.MONDAY, Weekday.WEDNESDAY, Weekday.FRIDAY);<br></code></pre></td></tr></table></figure><p>可以使用 <code>Set</code> 接口的常用方法来修改 <code>EnumSet</code>。</p><p><code>EnumMap</code> 是一个映射，它的键属于一个枚举类型。<code>EnumMap</code> 可以简单高效地实现为一个值数组。需要在构造器中指定键类型：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">var</span> <span class="hljs-variable">personInCharge</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">EnumMap</span>&lt;Weekday, Employee&gt;(Weekday.class);<br></code></pre></td></tr></table></figure><blockquote><p>注释：在 <code>EnumSet</code> 的 API 文档中，会看到形如 <code>E extends EnumSet&lt;E&gt;</code> 的奇怪的类型参数。简单地说，它的意思就是 “E 是一个枚举类型。” 所有枚举类型都扩展了这个类型 <code>Enum</code> 类。例如，<code>Weekday</code> 扩展了 <code>Enum&lt;Weekdays&gt;</code>。</p></blockquote><h4 id="9-4-7-标识散列映射"><a href="#9-4-7-标识散列映射" class="headerlink" title="9.4.7 标识散列映射"></a>9.4.7 标识散列映射</h4><p><code>IdentityHashMap</code> 有一个很特殊的用途。在这里，键的散列值不是用 <code>hashCode</code> 函数计算的，而是用 <code>System.identityHashCode</code> 方法计算的。<code>Object.hashCode</code> 根据对象的内存地址计算散列码时，就使用了这个方法。另外，对两个对象进行比较时，<code>IdentityHashMap</code> 使用了 <code>==</code>，而不是 <code>equals</code>。也就是说，不同的键对象即使内容相同，也被视为不同的对象。在实现对象遍历算法（如对象串行化）时，如果你想跟踪哪些对象已经遍历过，这个类就很有用。</p><h3 id="9-5-副本与视图"><a href="#9-5-副本与视图" class="headerlink" title="9.5 副本与视图"></a>9.5 副本与视图</h3><p>如果查看图 9-4 和图 9-5，可能会认为用如此多的接口和抽象类来实现数量并不多的具体集合类似乎没有太大必要。不过，这两个图并没有展示出全部。通过使用视图（view），可以得到其他实现了 <code>Collection</code> 接口或 <code>Map</code> 接口的对象。你已经见过使用映射类 <code>keySet</code> 方法的这样一个例子。初看起来，好像这个方法创建了一个新集，并填入映射中的所有键，然后返回这个集。但是，情况并非如此。实际上，<code>keySet</code> 方法返回一个实现了 <code>Set</code> 接口的类对象，这个类的方法可以操纵原映射。这种集合称为视图。</p><p>视图技术在集合框架中有许多非常有用的应用。下面几节将讨论这些应用。</p><h4 id="9-5-1-小集合"><a href="#9-5-1-小集合" class="headerlink" title="9.5.1 小集合"></a>9.5.1 小集合</h4><p>Java 9 引入了一些静态方法，可以生成给定元素的集或列表，以及给定键&#x2F;值对的映射。例如，</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java">List&lt;String&gt; names = List.of(<span class="hljs-string">&quot;Peter&quot;</span>, <span class="hljs-string">&quot;Paul&quot;</span>, <span class="hljs-string">&quot;Mary&quot;</span>);<br>Set&lt;Integer&gt; numbers = Set.of(<span class="hljs-number">2</span>, <span class="hljs-number">3</span>, <span class="hljs-number">5</span>);<br></code></pre></td></tr></table></figure><p>会分别生成包含 3 个元素的一个列表和一个集。对于映射，需要指定键和值，如下所示：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">Map&lt;String, Integer&gt; scores = Map.of(<span class="hljs-string">&quot;Peter&quot;</span>, <span class="hljs-number">2</span>, <span class="hljs-string">&quot;Paul&quot;</span>, <span class="hljs-number">3</span>, <span class="hljs-string">&quot;Paul&quot;</span>, <span class="hljs-number">3</span>, <span class="hljs-string">&quot;Mary&quot;</span>, <span class="hljs-number">5</span>);<br></code></pre></td></tr></table></figure><p>元素、键或值不能为 <code>null</code>。集和映射键不能重复：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java">numbers = Set.of(<span class="hljs-number">13</span>, <span class="hljs-literal">null</span>); <span class="hljs-comment">// Error-null element</span><br>scores = Map.of(<span class="hljs-string">&quot;Peter&quot;</span>, <span class="hljs-number">4</span>, <span class="hljs-string">&quot;Peter&quot;</span>, <span class="hljs-number">2</span>); <span class="hljs-comment">// Error-duplicate key</span><br></code></pre></td></tr></table></figure><blockquote><p>警告：对于这些集和映射中的迭代顺序，并没有保证。实际上，会有意用每次虚拟机启动时随机得到的一个种子打乱这个顺序。来看看 jshell 的两次运行：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs shell"><span class="hljs-meta prompt_">$ </span><span class="language-bash">jshell&gt; Set.of(<span class="hljs-string">&quot;Peter&quot;</span>, <span class="hljs-string">&quot;Paul&quot;</span>, <span class="hljs-string">&quot;Mary&quot;</span>)</span><br><span class="hljs-meta prompt_">$</span><span class="language-bash">1 =&gt; [Peter, Mary, Paul]</span><br><span class="hljs-meta prompt_">jshell&gt; </span><span class="language-bash"><span class="hljs-built_in">exit</span></span><br><span class="hljs-meta prompt_">$ </span><span class="language-bash">jshell&gt; Set.of(<span class="hljs-string">&quot;Peter&quot;</span>, <span class="hljs-string">&quot;Paul&quot;</span>, <span class="hljs-string">&quot;Mary&quot;</span>)</span><br><span class="hljs-meta prompt_">$</span><span class="language-bash">1 =&gt; [Paul, Mary, Peter]</span><br></code></pre></td></tr></table></figure><p>有些 Java 程序员编写程序员编写程序时，其程序的正确性依赖于一个假设，即认为实现细节永远不会改变。这样会让实现类的程序员很难对实现很有用的修改。在这里，道理很明显，编写程序时不要对元素顺序做任何假设。</p></blockquote><p><code>List</code> 和 <code>Set</code> 接口有 11 个方法，分别有 0 到 10 个参数，另外还有一个参数个数可变的方法。提供这种特性是为了提高效率。</p><p>对于 <code>Map</code> 接口，则无法提供一个参数可变的版本，因为参数类型会交替为键类型和值类型。不过它有一个静态方法 <code>ofEntries</code>。能接受任意多个 <code>Map.Entry&lt;K, V&gt;</code> 对象（可以用静态方法 <code>entry</code> 创建这些对象）。例如，</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">import</span> <span class="hljs-keyword">static</span> java.util.Map.*;<br>Map.*;<br>Map&lt;String, Integer&gt; scores = ofEntries(<br>    entry(<span class="hljs-string">&quot;Peter&quot;</span>, <span class="hljs-number">2</span>),<br>    entry(<span class="hljs-string">&quot;Paul&quot;</span>, <span class="hljs-number">3</span>),<br>    entry(<span class="hljs-string">&quot;Mary&quot;</span>, <span class="hljs-number">5</span>));<br></code></pre></td></tr></table></figure><p><code>of</code> 和 <code>ofEntries</code> 方法可以生成某些类的对象，这些类对于每个元素会有一个实例变量，或者有一个后备数组提供支持。</p><p>这些集合对象是不可修改的（unmodifiable）。如果试图改变它们的内容，会导致一个 <code>UnsupportedOperationException</code> 异常。</p><p>如果需要一个可更改的集合，可以把这个不可修改的集合传递到构造器：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">var</span> <span class="hljs-variable">names</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">ArrayList</span>&lt;&gt;(list.of(<span class="hljs-string">&quot;Peter&quot;</span>, <span class="hljs-string">&quot;Paul&quot;</span>, <span class="hljs-string">&quot;Mary&quot;</span>);<br></code></pre></td></tr></table></figure><p>以下方法调用</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">Collections.nCopies(n, object)<br></code></pre></td></tr></table></figure><p>会返回一个实现了 <code>List</code> 接口的不可变对象，给人一种错觉：就像有 n 个元素，每个元素看起来是一个 <code>anObject</code>。</p><p>例如，下面的调用将创建一个包含 100 个字符串的 <code>List</code>，每个值都设置为 <code>&quot;DEFAULT&quot;</code>：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">List&lt;String&gt; settings = Collections.nCopies(<span class="hljs-number">100</span>, <span class="hljs-string">&quot;DEFAULT&quot;</span>);<br></code></pre></td></tr></table></figure><p>这样存储开销很小。对象只存储一次。</p><blockquote><p>注释：<code>of</code> 方法是 Java 9 新引入的。之前有一个静态方法 <code>Arrays.asList</code>，它会返回一个可更改但是大小不可变的列表。也就是说，在这个列表上可以调用 <code>set</code>，但是不能使用 <code>add</code> 或 <code>remove</code>。另外还有遗留方法 <code>Collections.emptySet</code> 和 <code>Collections.singletonList</code>。</p></blockquote><blockquote><p>注释：<code>Collections</code> 类包含很多实用方法，这些方法的参数或返回值是集合。不要将它们与 <code>Collection</code> 接口混淆。</p></blockquote><blockquote><p>提示：Java 没有 <code>Pair</code> 类，有些程序员会使用 <code>Map.Entry</code> 作为对（pair），但这种做法并不好。在 Java 9 之前，这会很麻烦，你必须使用 <code>new AbstractMap.SimpleImmutableEntry&lt;first, second&gt;</code> 构造对象。不过现在可以调用 <code>Map.entry(first, second)</code>。</p></blockquote><h4 id="9-5-2-不可修改的副本和视图"><a href="#9-5-2-不可修改的副本和视图" class="headerlink" title="9.5.2 不可修改的副本和视图"></a>9.5.2 不可修改的副本和视图</h4><p>为了建立一个集合的不可修改的副本（unmodifiable copy），可以使用集合类型的 <code>copyOf</code> 方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java">ArrayList&lt;String&gt; names = ...;<br>Set&lt;String&gt; nameSet = Set.copyOf(names); <span class="hljs-comment">// The names as an unmodifiable set</span><br>List.copyOf(names); <span class="hljs-comment">// The names as an unmodifiable list</span><br></code></pre></td></tr></table></figure><p>每个 <code>copyOf</code> 方法会建立集合的一个副本。如果修改了原集合，这个副本不受影响。如果原集合恰好是不可修改的，而且类型正确，<code>copyOf</code> 则会直接返回原集合：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java">Set&lt;String&gt; names = Set.of(<span class="hljs-string">&quot;Peter&quot;</span>, <span class="hljs-string">&quot;Paul&quot;</span>, <span class="hljs-string">&quot;Mary&quot;</span>);<br>Set&lt;String&gt; nameSet = Set.copyOf(names); <span class="hljs-comment">// No need to make a copy: names == nameSet</span><br></code></pre></td></tr></table></figure><p><code>Collections</code> 类还有一些方法可以生成集合的不可修改的视图（unmodifiable view）。这些视图对现有集合增加了一个运行时检查。如果检测到试图修改不可修改的集合，就抛出一个异常。</p><p>可以使用下面 8 个方法来获得不可修改的视图：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs java">Collections.unmodifiableCollection<br>Collections.unmodifiableList<br>Collections.unmodifiableSet<br>Collections.unmodifiableSortedSet<br>Collections.unmodifiableNavigableSet<br>Collections.unmodifiableMap<br>Collections.unmodifiableSortedMap<br>Collections.unmodifiableList<br></code></pre></td></tr></table></figure><p>每个方法都定义为处理一个接口。例如，<code>Collections.unmodifiableList</code> 可以处理 <code>ArrayList</code>、<code>LinkedList</code> 或者实现了 <code>List</code> 接口的任何其他类。</p><p>例如，假设想要让你的某些代码查看（但不修改）一个集合的内容，可以如下实现：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">var</span> <span class="hljs-variable">staff</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">LinkedList</span>();<br><span class="hljs-comment">// Collections.unmodifiableList(staff);</span><br>lookAt(Collections.unmodifiableList(staff));<br></code></pre></td></tr></table></figure><p><code>Collections.unmodifiableList</code> 方法将返回实现了 <code>List</code> 接口的一个类的对象。其访问器方法将从 <code>staff</code> 集合中获取值。当 <code>lookAt</code> 方法可以调用 <code>List</code> 接口中的所有方法，而不只是访问器但是所有的更改器方法（例如，<code>add</code>）已经重新定义为抛出一个 <code>UnsupportedOperationException</code> 异常，而不是将调用传递给底层集合。</p><p>不可修改的视图并不会让集合本身变为不可变。仍然可以通过集合的原始引用（在这里就是 <code>staff</code> 修改这个集合，并且仍然可以对集合的元素调用更改器方法。</p><p>因为视图只是包装了接口而不是具体的集合对象，所以只能访问图接口中定义的方法。例如，<code>LinkedList</code> 类有一些便利方法，如 <code>addFirst</code> 和 <code>addLast</code>，它们不是 <code>List</code> 接口的方法，不能通过不可修改的视图访问这些方法。</p><blockquote><p>警告：<code>unmodifiableCollection</code> 方法（以及本节稍后讨论的 synchronizedCollection 和 checked Collection 方法）将返回一个集合，它的 <code>equals</code> 方法不调用底层集合的 <code>equals</code> 方法。实际上，它继承了 <code>Object</code> 类的 <code>equals</code> 方法，这个方法只是检测两个对象是否是同一个对象。如果将集或列表转换成集合，就再也无法检测其内容是否相同了。视图采用了这种工作方式，因为这个层次上的相等性检测没有明确定义。视图会以同样的方式处理 <code>hashCode</code> 方法。</p><p>不过，<code>unmodifiableSet</code> 和 <code>unmodifiableList</code> 方法会使用底层集合的 <code>equals</code> 方法和 <code>hashCode</code> 方法。</p></blockquote><h4 id="9-5-3-子范围"><a href="#9-5-3-子范围" class="headerlink" title="9.5.3 子范围"></a>9.5.3 子范围</h4><p>可以为很多集合建立子范围（subrange）视图。例如，假设有一个列表 <code>staff</code>，想从中取出第 10 个～第 19 个元素。可以使用 <code>subList</code> 方法来获得这个列表子范围的视图。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">List&lt;Employee&gt; group2 = staff.subList(<span class="hljs-number">10</span>, <span class="hljs-number">20</span>);<br></code></pre></td></tr></table></figure><p>第一个索引包含在内，而不包含第二个索引。这与 <code>String</code> 类 <code>substring</code> 操作中的参数类似。可以对子范围应用任何操作，而且这些操作会自动反映到整个列表。例如，可以删除整个子范</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">group2.clear(); <span class="hljs-comment">// staff reduction</span><br></code></pre></td></tr></table></figure><p>这些元素会自动地从 <code>staff</code> 列表中清除，并且 <code>group2</code> 变为空。</p><p>对于有序集和映射，可以使用排序顺序而不是元素位置建立子范围。<code>SortedSet</code> 接口声明了 3 个方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs java">SortedSet&lt;E&gt; <span class="hljs-title function_">subSet</span><span class="hljs-params">(E from, E to)</span><br>SortedSet&lt;E to)<br>SortedSet&lt;E&gt; <span class="hljs-title function_">headSet</span><span class="hljs-params">(E to)</span><br>SortedSet&lt;E&gt; <span class="hljs-title function_">tailSet</span><span class="hljs-params">(E from)</span><br></code></pre></td></tr></table></figure><p>这些方法将返回大于等于 <code>from</code> 且小于 <code>to</code> 的所有元素构成的子集。有序映射也有类似的方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java">SortedMap&lt;K,V&gt; <span class="hljs-title function_">subMap</span><span class="hljs-params">(K from, K to)</span><br>SortedMap&lt;K,V&gt; <span class="hljs-title function_">headMap</span><span class="hljs-params">(K to)</span><br>SortedMap&lt;K,V&gt; <span class="hljs-title function_">tailMap</span><span class="hljs-params">(K from)</span><br></code></pre></td></tr></table></figure><p>这些方法会返回映射的视图，其中包含落在指定范围内的键相应的所有元素。</p><p>Java 6 引入的 <code>NavigableSet</code> 接口允许对这些子范围操作有更多控制。可以指定是否包括边界：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java">NavigableSet&lt;E&gt; <span class="hljs-title function_">subSet</span><span class="hljs-params">(E from, <span class="hljs-type">boolean</span> fromInclusive, E to, <span class="hljs-type">boolean</span> toInclusive)</span><br>NavigableSet&lt;E&gt; <span class="hljs-title function_">headSet</span><span class="hljs-params">(E to, <span class="hljs-type">boolean</span> toInclusive)</span><br>NavigableSet&lt;E&gt; <span class="hljs-title function_">tailSet</span><span class="hljs-params">(E from, <span class="hljs-type">boolean</span> fromInclusive)</span><br></code></pre></td></tr></table></figure><h4 id="9-5-4-检查型视图"><a href="#9-5-4-检查型视图" class="headerlink" title="9.5.4 检查型视图"></a>9.5.4 检查型视图</h4><p>检查型视图用来对泛型类型可能出现的问题提供调试支持。如同第 8 章中所述，实际上将错误类型的元素混入泛型集合中的情况极有可能发生。例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">var</span> <span class="hljs-variable">strings</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">ArrayList</span>&lt;String&gt;();<br><span class="hljs-type">ArrayList</span> <span class="hljs-variable">rawList</span> <span class="hljs-operator">=</span> strings; <span class="hljs-comment">// warning only, not an error,</span><br>rawList.add(<span class="hljs-keyword">new</span> <span class="hljs-title class_">Date</span>()); <span class="hljs-comment">// now strings contains a Date object</span><br></code></pre></td></tr></table></figure><p>这个错误的 add 命令在运行时检测不到。实际上，只有当另一部分代码调用 <code>get</code> 方法，并将结果强制转换为 <code>String</code> 时，才会出现一个类型转换异常。</p><p>检查型视图可以探测这类问题。下面定义了一个安全列表：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">List&lt;String&gt; safeStrings = Collections.checkedList(strings, String.class);<br></code></pre></td></tr></table></figure><p>这个视图的 <code>add</code> 方法将检查插入的对象是否属于给定的类。如果不属于给定的类，就立即抛出一个 <code>ClassCastException</code>。这样做的好处是会在正确的位置报告错误：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java">ArrayList rawList strings;<br>rawList.add(<span class="hljs-keyword">new</span> <span class="hljs-title class_">Date</span>()); <span class="hljs-comment">// checked list throws a ClassCastException</span><br></code></pre></td></tr></table></figure><blockquote><p>警告：检查型视图受限于虚拟机可以完成的运行时检查。例如，对于 <code>ArrayList&lt;Pair&lt;String&gt;</code>，就无法阻止插入 <code>Pair&lt;Date&gt;</code>，因为虚拟机有一个“原始” <code>Pair</code> 类。</p></blockquote><h4 id="9-5-5-同步视图"><a href="#9-5-5-同步视图" class="headerlink" title="9.5.5 同步视图"></a>9.5.5 同步视图</h4><p>如果从多个线程访问集合，就必须确保集合不会被意外地破坏。例如，如果一个线程试图为散列表增加元素，同时另一个线程正在对元素进行遍历，其结果将是灾难性的。</p><p>类库的设计者使用视图机制确保常规集合是线程安全的，而没有实现线程安全的集合类。例如，<code>Collections</code> 类的 <code>synchronizedMap</code> 方法可以将任何一个映射转换成有同步访问方法的 <code>Map</code>：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">HashMap&lt;String,Employee&gt;()<br></code></pre></td></tr></table></figure><p>现在就可以从多线程访问这个 map 对象了。<code>get</code> 和 <code>put</code> 等方法是同步的，即每个方法调用必须完全结束，另一个线程才能调用另一个方法。</p><h4 id="9-5-6-关于可选操作的说明"><a href="#9-5-6-关于可选操作的说明" class="headerlink" title="9.5.6 关于可选操作的说明"></a>9.5.6 关于可选操作的说明</h4><p>通常，视图有一些限制：可能只读，可能无法改变大小，或者可能只支持删除而不支持插入（如映射的键视图）。如果试图执行不恰当的操作，受限制的视图就会抛出一个 <code>UnsupportedOperationException</code>。</p><p>在集合和迭代器接口的 API 文档中，许多方法被描述为“可选操作”。这看起来与接口的概念有冲突。毕竟，接口的设计目的难道不就是明确一个类必须实现的方法吗？确实，从理论的角度看，这种安排不太令人满意。一个更好的解决方案是为只读视图和不能改变集合大小的视图建立单独的接口。不过，这将会使接口的数量增至原来的三倍，这让类库设计者无法接受。</p><p>是否应该将“可选”方法这一技术扩展到你自己的设计中呢？我们认为不应该。尽管集合被频繁地使用，但实现集合的编码方式未必适用于其他问题领域。集合类库的设计者必须解决一组极其严格而且相互冲突的需求。用户希望类库应该易于学习、使用方便、彻底泛型化、具有通用性，同时又与手写算法一样高效。要同时达到所有这些目标，或者甚至尽量兼顾所有目标都是不可能的。但是，在你自己的编程问题中，很少遇到这种极端的约束。你应该能够找到合适的解决方案，而不必依赖“可选”接口操作这种极端做法。</p><h3 id="9-6-算法"><a href="#9-6-算法" class="headerlink" title="9.6 算法"></a>9.6 算法</h3><p>除了实现集合类，Java 集合框架还提供了一些有用的算法。在下面的小节中，你会了解如何使用这些算法，以及如何编写适用于集合框架的你自己的算法。</p><h4 id="9-6-1-为什么使用泛型算法"><a href="#9-6-1-为什么使用泛型算法" class="headerlink" title="9.6.1 为什么使用泛型算法"></a>9.6.1 为什么使用泛型算法</h4><p>泛型集合接口有一个很大的优点，即算法只需要实现一次。例如，考虑计算集合中最大元素的一个简单算法。使用传统方式，程序设计人员可能会用循环实现这个算法。可以如下找出数组中最大的元素：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">if</span> (a.length == <span class="hljs-number">0</span>) <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">NoSuchElementException</span>();<br><span class="hljs-type">T</span> <span class="hljs-variable">largest</span> <span class="hljs-operator">=</span> a[<span class="hljs-number">0</span>];<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">1</span>; i &lt; a.length; i++)<br>    <span class="hljs-keyword">if</span> (largest.compareTo(a[i]) &lt; <span class="hljs-number">0</span>)<br>        largest = a[i];<br></code></pre></td></tr></table></figure><p>当然，要找出数组列表中的最大元素，编写的代码会稍有差别：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">if</span> (v.size() == <span class="hljs-number">0</span>) <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">NoSuchElementException</span>();<br><span class="hljs-type">T</span> <span class="hljs-variable">largest</span> <span class="hljs-operator">=</span> v.get(<span class="hljs-number">0</span>);<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">1</span>; i &lt; v.size(); i++)<br>    <span class="hljs-keyword">if</span> (largest.compareTo(v.get(i)) &lt; <span class="hljs-number">0</span>)<br>        largest = v.get(i);<br></code></pre></td></tr></table></figure><p>链表呢？链表没有高效的随机访问操作，不过可以使用迭代器：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">if</span> (l.isEmpty()) <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">NoSuchElementException</span>();<br>Iterator&lt;T&gt; iter = l.iterator();<br><span class="hljs-type">T</span> <span class="hljs-variable">largest</span> <span class="hljs-operator">=</span> iter.next();<br><span class="hljs-keyword">while</span> (iter.hasNext()) &#123;<br>    <span class="hljs-type">T</span> <span class="hljs-variable">next</span> <span class="hljs-operator">=</span> iter.next();<br>    <span class="hljs-keyword">if</span> (largest.compareTo(next) &lt; <span class="hljs-number">0</span>)<br>        largest = next;<br>&#125;<br></code></pre></td></tr></table></figure><p>编写这些循环很烦，而且比较容易出错。是否存在“差 1”错误（off-by-one error）？这些循环对于空容器能正常工作吗？对于只含有一个元素的容器又会发生什么情况呢？我们不希望每次都测试和调试这些代码，也不想实现如下的一系列方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">static</span> &lt;T <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Comparable</span>&gt; T <span class="hljs-title function_">max</span><span class="hljs-params">(T[] a)</span><br><span class="hljs-keyword">static</span> &lt;T <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Comparable</span>&gt; T <span class="hljs-title function_">max</span><span class="hljs-params">(ArrayList&lt;T&gt; v)</span><br><span class="hljs-keyword">static</span> &lt;T <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Comparable</span>&gt; T <span class="hljs-title function_">max</span><span class="hljs-params">(LinkedList&lt;T&gt; l)</span><br></code></pre></td></tr></table></figure><p>这里就可以使用集合接口。请考虑为了高效地执行这个算法所需要的最小集合接口。使用 <code>get</code> 和 <code>set</code> 方法的随机访问要比直接迭代的层次高。在计算链表中最大元素的过程中已经看到，这项任务并不需要随机访问。可以直接迭代处理元素来得出最大元素。因此，可以将 <code>max</code> 方法实现为能够接收任何实现了 <code>Collection</code> 接口的对象：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> &lt;T <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Comparable</span>&gt; T <span class="hljs-title function_">max</span><span class="hljs-params">(Collection&lt;T&gt; c)</span> &#123;<br>    <span class="hljs-keyword">if</span> (c.isEmpty()) <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">NoSuchElementException</span>();<br>    Iterator&lt;T&gt; iter = c.iterator();<br>    <span class="hljs-type">T</span> <span class="hljs-variable">largest</span> <span class="hljs-operator">=</span> iter.next();<br>    <span class="hljs-keyword">while</span> (iter.hasNext()) &#123;<br>        <span class="hljs-type">T</span> <span class="hljs-variable">next</span> <span class="hljs-operator">=</span> iter.next();<br>        <span class="hljs-keyword">if</span> (largest.compareTo(next) &lt; <span class="hljs-number">0</span>)<br>            largest = next;<br>    &#125;<br>    <span class="hljs-keyword">return</span> largest;<br>&#125;<br></code></pre></td></tr></table></figure><p>现在就可以使用一个方法来计算链表、数组列表或数组中的最大元素了。</p><p>这是一个功能很强大的概念。事实上，标准 C++ 类库有几十个有用的算法，每个算法都可以处理泛型集合。Java 类库中的算法没有那么丰富，但是确实包含了一些基本的算法：排序、二分查找和一些实用算法。</p><h4 id="9-6-2-排序与混排"><a href="#9-6-2-排序与混排" class="headerlink" title="9.6.2 排序与混排"></a>9.6.2 排序与混排</h4><p>计算机行业的前辈们有时会回忆起他们当年不得不使用穿孔卡片以及手动编写排序算法的情形。当然，如今排序算法已经成为大多数编程语言标准库中的一个组成部分，Java 程序设计语言也不例外。</p><p><code>Collections</code> 类中的 <code>sort</code> 方法可以对实现了 <code>List</code> 接口的集合进行排序：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">var</span> <span class="hljs-variable">staff</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">LinkedList</span>&lt;String&gt;();<br><span class="hljs-comment">// fill collection</span><br>Collections.sort(staff);<br></code></pre></td></tr></table></figure><p>这个方法假定列表元素实现了 <code>Comparable</code> 接口。如果想采用其他方式对列表进行排序，可以使用 <code>List</code> 接口的 <code>sort</code> 方法并传入一个 <code>Comparator</code> 对象。可以如下按工资对一个员工列表排序：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">staff.sort(Comparator.comparingDouble(Employee::getSalary));<br></code></pre></td></tr></table></figure><p>如果想按照降序对列表进行排序，可以使用静态的便利方法 <code>Collections.reverseOrder()</code>。这个方法将返回一个比较器，这个比较器将返回 <code>b.compareTo(a)</code>。例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">staff.sort(Comparator.reverseOrder());<br></code></pre></td></tr></table></figure><p>这个方法将根据元素类型的 <code>compareTo</code> 方法所给定的排序顺序，按逆序对列表 <code>staff</code> 中的元素进行排序。同样地：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">staff.sort(Comparator.comparingDouble(Employee::getSalary).reversed());<br></code></pre></td></tr></table></figure><p>将按工资逆序排序。</p><p>你可能会对 <code>sort</code> 方法如何对列表进行排序感到好奇。通常，在查看有关算法书籍中的排序算法时，会发觉介绍的都是有关数组的排序算法，而且使用的是随机访问方式。但是，链表的随机访问效率很低。实际上，可以使用一种归并排序对链表高效地排序。不过，Java 程序设计语言中的实现并不是这样做的。它只是将所有元素都放在一个数组，对这个数组进行排序，然后再将排序后的序列复制回列表。</p><p>集合类库中使用的排序算法比快速排序（QuickSort）要慢一些，快速排序是一种传统的通用排序算法选择。不过，集合类库中使用的排序算法有一个主要的优点：它是稳定的，也就是说，它不会改变相等元素的顺序。为什么要关注相等元素的顺序呢？下面来看一种常见的情况。假设有一个已经按照姓名排序的员工列表。现在，要按照工资再进行排序。如果两个员工的工资相等会发生什么情况？如果采用稳定的排序算法，将会保留按名字排序的顺序。换句话说，排序的结果是得到一个首先按照工资排序再按照姓名排序的列表。</p><p>集合不需要实现所有的“可选”方法，因此，所有接受集合参数的方法必须描述什么时候可以安全地将集合传递给算法。例如，显然不能将 <code>unmodifiableList</code> 列表传递给 <code>sort</code> 算法。那么，可以传递什么类型的列表呢？根据文档说明，列表必须是可修改的，但不一定可以改变大小。下面是有关的术语定义：</p><ul><li>如果列表支持 <code>set</code> 方法，这个列表则是可修改的（modifiable）。</li><li>如果列表支持 <code>add</code> 和 <code>remove</code> 方法，这个列表则是可改变大小的（resizable）。</li></ul><p><code>Collections</code> 类有一个算法 <code>shuffle</code>，其功能与排序刚好相反，它会随机地混排列表中元素的顺序。例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java">ArrayList&lt;Card&gt; cards = ...;<br>Collections.shuffle(cards);<br></code></pre></td></tr></table></figure><p>如果提供的列表没有实现 <code>RandomAccess</code> 接口，<code>shuffle</code> 方法会将元素复制到数组中，然后打乱数组元素的顺序，最后再将打乱顺序后的元素复制回列表。</p><p>程序清单 9-7 中的程序用 1～49 之间的 49 个 <code>Integer</code> 对象填充数组。然后，随机地混排列表，并从混排后的列表中选择前 6 个值。最后再将选择的数值进行排序并打印。</p><h4 id="9-6-3-二分查找"><a href="#9-6-3-二分查找" class="headerlink" title="9.6.3 二分查找"></a>9.6.3 二分查找</h4><p>要想在数组中查找一个对象，通常要依次访问数组中的每个元素，直到找到匹配的元素。不过，如果数组是有序的，可以检查中间的元素，查看是否大于要查找的元素。如果是，就在数组的前半部分继续查找；否则，在数组的后半部分继续查找。这样就可以将问题规模缩减一半，并以同样的方式继续下去。例如，如果数组中有 1024 个元素，那么 10 步之后就能找到匹配（或者可以确认数组中不存在这个元素），而线性查找平均需要 512 步（如果元素存在）；倘若元素不存在，需要 1024 步才能够确认。</p><p><code>Collections</code> 类的 <code>binarySearch</code> 方法实现了这个算法。注意，集合必须是有序的，否则算法会返回错误的答案。要想查找某个元素，必须提供集合（这个集合要实现 <code>List</code> 接口，下面还会更加详细地介绍这个问题）以及要查找的元素。如果集合没有采用 <code>Comparable</code> 接口的 <code>compareTo</code> 方法进行排序，那么还要提供一个比较器对象。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java">i = Collections.binarySearch(c, element);<br>i = Collections.binarySearch(c, element, comparator);<br></code></pre></td></tr></table></figure><p>如果 <code>binarySearch</code> 方法返回一个非负的值，这表示匹配对象的索引。也就是说，<code>c.get(i)</code> 等于在这个比较顺序下的 <code>element</code>。如果返回负值，则表示没有匹配的元素。不过，可以利用这个返回值来计算应该将 <code>element</code> 插入集合的哪个位置，以保持集合的有序性。插入的位置是：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">insertionPoint = -i - <span class="hljs-number">1</span>;<br></code></pre></td></tr></table></figure><p>而不是简单的 <code>i</code>，因为 0 值有二义性。也就是说，下面这个操作：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">if</span> (i &lt; <span class="hljs-number">0</span>)<br>    c.add(-i - <span class="hljs-number">1</span>, element);<br></code></pre></td></tr></table></figure><p>将把元素插入正确的位置。</p><p>只有采用随机访问方式，二分查找才有意义。如果必须依次查找链表的一半元素来找到中间元素，就会完全失去二分查找的优势。因此，如果为 <code>binarySearch</code> 算法提供了一个链表，它将退化为线性查找。</p><h4 id="9-6-4-简单算法"><a href="#9-6-4-简单算法" class="headerlink" title="9.6.4 简单算法"></a>9.6.4 简单算法</h4><p><code>Collections</code> 类中包含几个简单但很有用的算法。这一节最前面介绍的例子就是这样一个算法，即查找集合中的最大元素。其他算法还包括：将一个列表中的元素复制到另外一个列表中；用一个常量值填充容器；逆置一个列表的元素顺序。</p><p>为什么在标准类库中提供这些简单算法呢？大多数程序员肯定都能很容易地采用简单的循环实现这些任务。我们之所以喜欢这些算法，是因为它们可以让程序员更轻松地读懂代码。当阅读别人实现的一个循环时，必须揣摩编程者的意图。例如，请看下面这个循环：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>; i &lt; words.size(); i++)<br>    <span class="hljs-keyword">if</span> (words.get(i).equals(<span class="hljs-string">&quot;C++&quot;</span>)) words.set(i, <span class="hljs-string">&quot;Java&quot;</span>);<br></code></pre></td></tr></table></figure><p>现在将这个循环与以下调用进行比较：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">Collections.replaceAll(words, <span class="hljs-string">&quot;C++&quot;</span>, <span class="hljs-string">&quot;Java&quot;</span>);<br></code></pre></td></tr></table></figure><p>看到这个方法调用时，你马上就能知道这个代码要做什么。</p><p>本节最后的 API 注释描述了 <code>Collections</code> 类中的简单算法。</p><p>默认方法 <code>Collection.removeIf</code> 和 <code>List.replaceAll</code> 稍有些复杂。要提供一个 lambda 表达式来测试或转换元素。例如，下面的代码将删除所有短单词，并把其余单词改为小写：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java">words.removeIf(w -&gt; w.length() &lt;= <span class="hljs-number">3</span>);<br>words.replaceAll(String::toLowerCase);<br></code></pre></td></tr></table></figure><h4 id="9-6-5-批操作"><a href="#9-6-5-批操作" class="headerlink" title="9.6.5 批操作"></a>9.6.5 批操作</h4><p>很多操作会“成批”复制或删除元素。以下调用：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">coll1.removeAll(coll2);<br></code></pre></td></tr></table></figure><p>将从 <code>coll1</code> 中删除 <code>coll2</code> 中出现的所有元素。与之相反：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">coll1.retainAll(coll2);<br></code></pre></td></tr></table></figure><p>会从 <code>coll1</code> 中删除所有未在 <code>coll2</code> 中出现的元素。</p><p>下面是一个典型的应用。假设希望找出两个集的交集（intersection），也就是两个集中共有的元素。首先，建立一个新集来存放结果：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">var</span> <span class="hljs-variable">result</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">HashSet</span>&lt;String&gt;(firstSet);<br></code></pre></td></tr></table></figure><p>在这里，我们利用了一个事实：每一个集合都有这样一个构造器，其参数是包含初始值的另一个集合。</p><p>现在来使用 <code>retainAll</code> 方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">result.retainAll(secondSet);<br></code></pre></td></tr></table></figure><p>这会保留两个集中都出现的所有元素。这样就构成了交集，而无须编写循环。</p><p>可以按这个思路更进一步，对视图应用一个批操作。例如，假设有一个映射，将员工 ID 映射到员工对象，另外有一个集包含不再聘用的所有员工的 ID：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java">Map&lt;String, Employee&gt; staffMap = ...;<br>Set&lt;String&gt; terminatedIDs = ...;<br></code></pre></td></tr></table></figure><p>只需要建立一个键集，并删除终止聘用关系的所有员工的 ID：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">staffMap.keySet().removeAll(terminatedIDs);<br></code></pre></td></tr></table></figure><p>因为键集是映射的一个视图，所以键和相关联的员工名会自动从映射中删除。</p><p>通过使用子范围视图，可以限制批操作仅应用于子列表和子集。例如，假设希望把一个列表的前 10 个元素增加到另一个容器，可以建立一个子列表选出前 10 个元素：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">relocated.addAll(staff.subList(<span class="hljs-number">0</span>, <span class="hljs-number">10</span>));<br></code></pre></td></tr></table></figure><p>这个子范围还可以完成更改操作：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">staff.subList(<span class="hljs-number">0</span>, <span class="hljs-number">10</span>).clear();<br></code></pre></td></tr></table></figure><h4 id="9-6-6-集合与数组的转换"><a href="#9-6-6-集合与数组的转换" class="headerlink" title="9.6.6 集合与数组的转换"></a>9.6.6 集合与数组的转换</h4><p>因为 Java 平台 API 的大部分内容都是在集合框架创建之前设计的，所以有时候需要在传统数组和更现代的集合之间进行转换。</p><p>如果需要把一个数组转换为集合，<code>List.of</code> 包装器可以达到这个目的。例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java">String[] names = ...;<br>List&lt;String&gt; staff = List.of(names);<br></code></pre></td></tr></table></figure><p>从集合得到数组会更困难一些。当然，可以使用 <code>toArray</code> 方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">Object[] names = staff.toArray();<br></code></pre></td></tr></table></figure><p>不过，这样做的结果是一个对象数组。尽管你知道集合中包含的是一个特定类型的对象，但不能使用强制类型转换：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">String[] names = (String[]) staff.toArray(); <span class="hljs-comment">// ERROR</span><br></code></pre></td></tr></table></figure><p><code>toArray</code> 方法返回的数组创建为一个 <code>Object[]</code> 数组，不能改变它的类型。实际上，要向 <code>toArray</code> 方法传入一个数组构造器表达式。这样一来，返回的数组就会有正确的数组类型：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">String[] values = staff.toArray(String[]::<span class="hljs-keyword">new</span>);<br></code></pre></td></tr></table></figure><blockquote><p>自注释：在 JDK 11 之前，必须使用另一种形式的 <code>toArray</code> 方法，要传入有正确类型的数组：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">String[] values = staff.toArray(<span class="hljs-keyword">new</span> <span class="hljs-title class_">String</span>[<span class="hljs-number">0</span>]);<br></code></pre></td></tr></table></figure><p>这个 <code>toArray</code> 方法会构造有相同类型的另一个数组。或者，如果数组足够长，则会重用原数组：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">staff.toArray(<span class="hljs-keyword">new</span> <span class="hljs-title class_">String</span>[staff.size()]);<br></code></pre></td></tr></table></figure><p>这种情况下，不会创建新数组。</p></blockquote><h4 id="9-6-7-编写自己的算法"><a href="#9-6-7-编写自己的算法" class="headerlink" title="9.6.7 编写自己的算法"></a>9.6.7 编写自己的算法</h4><p>如果编写自己的算法（实际上，或者是以一个集合作为参数的任何方法），应该尽可能使用接口，而不要使用具体的实现。例如，假设你想处理集合元素。当然，可以实现类似下面的方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">processItems</span><span class="hljs-params">(ArrayList&lt;Item&gt; items)</span> &#123;<br>    <span class="hljs-keyword">for</span> (Item item : items)<br>        <span class="hljs-keyword">do</span> something with item<br>&#125;<br></code></pre></td></tr></table></figure><p>但是，这样会限制方法的调用者，即调用者必须在 <code>ArrayList</code> 中提供元素。如果这些元素正好在另一个集合中，首先必须对它们重新包装，因此，最好接受一个更加通用的集合。</p><p>要问自己：完成这项工作的最通用的集合接口是什么？你关心顺序吗？如果顺序很重要，就应当接受 <code>List</code>。不过，如果顺序不重要，那么可以接受任意类型的集合：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">processItems</span><span class="hljs-params">(Collection&lt;Item&gt; items)</span> &#123;<br>    <span class="hljs-keyword">for</span> (Item item : items)<br>        <span class="hljs-keyword">do</span> something with item<br>&#125;<br></code></pre></td></tr></table></figure><p>现在，任何人都可以用 <code>ArrayList</code> 或 <code>LinkedList</code>（甚至用 <code>List.of</code> 方法调用包装的数组）调用这个方法。</p><blockquote><p>提示：在这里，甚至还可以做得更好：可以接受一个 <code>Iterable&lt;Item&gt;</code>。<code>Iterable</code> 接口有一个抽象方法 <code>iterator</code>，增强的 for 循环在底层就使用了这个方法。<code>Collection</code> 接口扩展了 <code>Iterable</code>。</p></blockquote><p>反过来，如果你的方法返回多个元素，你肯定不希望限制将来的改进。例如，考虑下面的代码：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> ArrayList&lt;Item&gt; <span class="hljs-title function_">lookupItems</span><span class="hljs-params">(...)</span> &#123;<br>    <span class="hljs-type">var</span> <span class="hljs-variable">result</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">ArrayList</span>&lt;Item&gt;();<br>    <span class="hljs-keyword">return</span> result;<br>&#125;<br></code></pre></td></tr></table></figure><p>这个方法承诺返回一个 <code>ArrayList</code>，尽管调用者并不关心它是什么类型的列表。如果你返回一个 <code>List</code>，任何时候都可以增加一个分支，通过调用 <code>List.of</code> 返回一个空列表或单例列表。</p><blockquote><p>注释：既然将集合接口作为方法参数和返回类型是个很好的想法，为什么 Java 类库不一致地遵循这个规则呢？例如，<code>JComboBox</code> 有两个构造器：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java">JComboBox(Object[] items)<br>JComboBox(Vector&lt;?&gt; items)<br></code></pre></td></tr></table></figure><p>之所以没有这样做，原因很简单：时间问题。Swing 类库是在集合类库之前创建的。</p></blockquote><h3 id="9-7-遗留的集合"><a href="#9-7-遗留的集合" class="headerlink" title="9.7 遗留的集合"></a>9.7 遗留的集合</h3><p>从 Java 第 1 版问世以来，在集合框架出现之前已经存在大量“遗留的”容器类。</p><p>这些类已经集成到集合框架中，如图 9-12 所示。下面各节将简要介绍这些遗留的集合类。</p><p><img src="/2025/05/04/hello-world/%E7%AC%AC%E4%B9%9D%E7%AB%A0/image-20251203155628800.png" alt="image-20251203155628800"></p><h4 id="9-7-1-Hashtable-类"><a href="#9-7-1-Hashtable-类" class="headerlink" title="9.7.1 Hashtable 类"></a>9.7.1 Hashtable 类</h4><p>经典的 <code>Hashtable</code> 类与 <code>HashMap</code> 类的作用一样，实际上，接口也基本相同。类似 <code>Vector</code> 类的方法，<code>Hashtable</code> 方法也是同步的。如果不需要与遗留代码的兼容性，就应该使用 <code>HashMap</code>。如果需要并发访问，则要使用 <code>ConcurrentHashMap</code>，参见第 12 章。</p><h4 id="9-7-2-枚举"><a href="#9-7-2-枚举" class="headerlink" title="9.7.2 枚举"></a>9.7.2 枚举</h4><p>遗留的集合使用 <code>Enumeration</code> 接口遍历元素序列。<code>Enumeration</code> 接口有两个方法：<code>hasMoreElements</code> 和 <code>nextElement</code>。这两个方法完全类似于 <code>Iterator</code> 接口的 <code>hasNext</code> 方法和 <code>next</code> 方法。</p><p>如果发现遗留的类实现了这个接口，可以使用 <code>Collections.list</code> 将元素收集到一个 <code>ArrayList</code> 中。例如，<code>LogManager</code> 类只想将登录者的名字提供为一个 <code>Enumeration</code>。可以如下得到所有登录者的名字：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">ArrayList&lt;String&gt; loggerNames = Collections.list(LogManager.getLoggerNames());<br></code></pre></td></tr></table></figure><p>或者，在 Java 9 中，可以把一个枚举转换为一个迭代器：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java">LogManager.getLoggerNames().asIterator()<br>    .forEachRemaining(n -&gt; &#123; ... &#125;);<br></code></pre></td></tr></table></figure><p>有时还会遇到希望得到一个枚举参数的遗留方法。静态方法 <code>Collections.enumeration</code> 将生成一个枚举对象，它会枚举集合中的元素。例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java">List&lt;InputStream&gt; streams = ...;<br><span class="hljs-type">var</span> <span class="hljs-variable">in</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">SequenceInputStream</span>(Collections.enumeration(streams));<br><span class="hljs-comment">// the SequenceInputStream constructor expects an enumeration</span><br></code></pre></td></tr></table></figure><blockquote><p>注释：在 C++ 中，用迭代器作为参数十分普遍。幸好，在 Java 平台上，只有极少的程序员沿用这种习惯。传递集合要比传递迭代器更为明智。集合对象的用途更大。如果需要，接受者总能从集合获得迭代器，而且，还可以随时使用集合的所有方法。不过，你可能会在某些遗留代码中发现枚举，因为在 Java 1.2 的集合框架出现之前，这是唯一可以使用的泛型集合机制。</p></blockquote><h4 id="9-7-3-属性映射"><a href="#9-7-3-属性映射" class="headerlink" title="9.7.3 属性映射"></a>9.7.3 属性映射</h4><p>属性映射（property map）是一个特殊类型的映射结构。它有下面 3 个特性：</p><ul><li>键与值都是字符串。  </li><li>这个映射可以很容易地保存到文件以及从文件加载。  </li><li>有一个二级表存放默认值。</li></ul><p>实现属性映射的 Java 平台类名为 <code>Properties</code>。属性映射对于指定程序的配置选项很有用。例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">var</span> <span class="hljs-variable">settings</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Properties</span>();<br>settings.setProperty(<span class="hljs-string">&quot;width&quot;</span>, <span class="hljs-string">&quot;600.0&quot;</span>);<br>settings.setProperty(<span class="hljs-string">&quot;filename&quot;</span>, <span class="hljs-string">&quot;/home/cay/books/cj12/code/viche9/raven.html&quot;</span>);<br></code></pre></td></tr></table></figure><p>可以使用 <code>store</code> 方法将属性映射列表保存到一个文件中。在这里，我们将属性映射保存在文件 <code>program.properties</code> 中。第二个参数是包含在这个文件中的一个注释：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">var</span> <span class="hljs-variable">out</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">FileWriter</span>(<span class="hljs-string">&quot;program.properties&quot;</span>, StandardCharsets.UTF_8);<br>settings.store(out, <span class="hljs-string">&quot;Program Properties&quot;</span>);<br></code></pre></td></tr></table></figure><p>这个示例会给出以下输出：</p><figure class="highlight ini"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs ini"><span class="hljs-comment">#Program Properties</span><br><span class="hljs-comment">#Sun Dec 31 12:54:19 PST 2017</span><br><span class="hljs-attr">top</span>=<span class="hljs-number">227.0</span><br><span class="hljs-attr">left</span>=<span class="hljs-number">1286.0</span><br><span class="hljs-attr">width</span>=<span class="hljs-number">423.0</span><br><span class="hljs-attr">height</span>=<span class="hljs-number">547.0</span><br><span class="hljs-attr">filename</span>=/home/cay/books/cj12/code/v1che9/raven.html<br></code></pre></td></tr></table></figure><p>要从文件加载属性，可以使用以下调用：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">var</span> <span class="hljs-variable">in</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">FileReader</span>(<span class="hljs-string">&quot;program.properties&quot;</span>, StandardCharsets.UTF_8);<br>settings.load(in);<br></code></pre></td></tr></table></figure><blockquote><p>警告：如果使用 <code>load</code> 和 <code>store</code> 方法时提供了输入&#x2F;输出流，则会使用古老的 ISO 8859-1 字符编码。&gt;U+00FF 的字符会保存为 Unicode 转义字符。对于 UTF-8，要像以上代码一样使用读取器&#x2F;书写器。</p></blockquote><p><code>System.getProperties()</code> 方法会生成一个 <code>Properties</code> 对象来描述系统信息。例如，主目录包含键 <code>&quot;user.home&quot;</code>。可以用 <code>getProperty()</code> 方法读取这个信息，它将这个键对应的信息作为一个字符串返回：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">String</span> <span class="hljs-variable">userDir</span> <span class="hljs-operator">=</span> System.getProperty(<span class="hljs-string">&quot;user.home&quot;</span>);<br></code></pre></td></tr></table></figure><blockquote><p>警告：出于历史原因，<code>Properties</code> 类实现了 <code>Map&lt;Object, Object&gt;</code>。因此，可以使用 <code>Map</code> 接口的 <code>get</code> 和 <code>put</code> 方法。不过，<code>get</code> 方法返回类型为 <code>Object</code>，而 <code>put</code> 方法允许插入任意的对象。所以最好坚持使用处理字符串而不是对象的 <code>getProperty</code> 和 <code>setProperty</code> 方法。</p></blockquote><p>要得到虚拟机的 Java 版本，可以查找 <code>&quot;java.version&quot;</code> 属性。你会得到一个诸如 <code>&quot;17.0.1&quot;</code> 的字符串（不过，Java 8 之前的形式为 <code>&quot;1.8.0&quot;</code>)。</p><blockquote><p>提示：可以看到，Java 9 中版本编号发生了变化。这个看起来很小的改变却让大量依赖于老版本格式的工具无法正常工作。如果要解析版本字符串，一定要好好看看 JEP 322（<a href="http://openjdk.java.net/jeps/322%EF%BC%89%EF%BC%8C%E4%BA%86%E8%A7%A3%E5%B0%86%E6%9D%A5%EF%BC%88%E8%87%B3%E5%B0%91%E6%98%AF%E7%89%88%E6%9C%AC%E7%BC%96%E5%8F%B7%E6%9C%BA%E5%88%B6%E5%86%8D%E6%AC%A1%E6%94%B9%E5%8F%98%E4%B9%8B%E5%89%8D%EF%BC%89%E7%89%88%E6%9C%AC%E5%AD%97%E7%AC%A6%E4%B8%B2%E4%BC%9A%E9%87%87%E7%94%A8%E6%80%8E%E6%A0%B7%E7%9A%84%E6%A0%BC%E5%BC%8F%E3%80%82">http://openjdk.java.net/jeps/322），了解将来（至少是版本编号机制再次改变之前）版本字符串会采用怎样的格式。</a></p></blockquote><p><code>Properties</code> 类有两种提供默认值的机制。第一种方法是，只要查找一个字符串的值，可以指定一个默认值，当查找的键不存在时就会自动使用这个默认值：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">String</span> <span class="hljs-variable">filename</span> <span class="hljs-operator">=</span> settings.getProperty(<span class="hljs-string">&quot;filename&quot;</span>, <span class="hljs-string">&quot;&quot;</span>);<br></code></pre></td></tr></table></figure><p>如果属性映射中有一个 <code>&quot;filename&quot;</code> 属性，<code>filename</code> 就会设置为相应的字符串。否则，<code>filename</code> 会设置为空串。</p><p>如果觉得在每个 <code>getProperty</code> 调用中指定默认值太麻烦，可以把所有默认值都放在一个二级属性映射中，并在主属性映射的构造器中提供这个二级映射。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">var</span> <span class="hljs-variable">defaultSettings</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Properties</span>();<br>defaultSettings.setProperty(<span class="hljs-string">&quot;width&quot;</span>, <span class="hljs-string">&quot;600&quot;</span>);<br>defaultSettings.setProperty(<span class="hljs-string">&quot;height&quot;</span>, <span class="hljs-string">&quot;400&quot;</span>);<br>defaultSettings.setProperty(<span class="hljs-string">&quot;filename&quot;</span>, <span class="hljs-string">&quot;&quot;</span>);<br><br><span class="hljs-type">var</span> <span class="hljs-variable">settings</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Properties</span>(defaultSettings);<br></code></pre></td></tr></table></figure><p>没错，如果为 <code>defaultSettings</code> 构造器提供另一个属性映射参数，甚至可以为默认值指定默认值，不过一般不会这么做。</p><p>本书随附代码提供了一个示例程序，展示了如何使用属性存储和加载程序状态。这个程序使用第 2 章的 <code>ImageViewer</code> 程序，可以记住窗口位置、大小和最后加载的文件。运行这个程序，加载一个文件，然后移动窗口和调整窗口大小。再关闭程序，然后重新打开，看它是否记住了你的文件和你喜欢的窗口配置。还可以手动编辑主目录中的 <code>corejava/ImageViewer.properties</code> 文件。</p><p>属性是没有层次结构的简单表格。通常会用类似 <code>window.main.color</code>、<code>window.main.title</code> 等键名引入一个假想的层次结构。不过 <code>Properties</code> 类没有方法来帮助组织这样一个层次结构。如果要存储复杂的配置信息，就应该改为使用 <code>Preferences</code> 类，参见第 10 章的介绍。</p><h4 id="9-7-4-栈"><a href="#9-7-4-栈" class="headerlink" title="9.7.4 栈"></a>9.7.4 栈</h4><p>从 1.0 版开始，标准类库中就包含了 <code>Stack</code> 类，其中有大家熟悉的 <code>push</code> 方法和 <code>pop</code> 方法。但是，<code>Stack</code> 类扩展了 <code>Vector</code> 类，从理论角度看，<code>Vector</code> 类并不太令人满意，对于 <code>Vector</code>，你甚至可以使用并非栈操作的 <code>insert</code> 和 <code>remove</code> 方法在任何位置插入和删除值，而不只是在栈顶。</p><h4 id="9-7-5-位集"><a href="#9-7-5-位集" class="headerlink" title="9.7.5 位集"></a>9.7.5 位集</h4><p>Java 平台的 <code>BitSet</code> 类会存储一个位序列（它不是数学意义上的集，如果称为位向量（bit vector）或位数组（bit array）可能更为合适）。如果需要高效地存储一个位序列（例如，标志），就可以使用位集。由于位集将位包装在字节里，因此使用位集要比使用 <code>Boolean</code> 对象的 <code>ArrayList</code> 高效得多。</p><p><code>BitSet</code> 类提供了一个用于读取、设置或重置各个位的很方便的接口。使用这个接口可以避免掩码和其他调整位的操作，如果将位存储在 <code>int</code> 或 <code>long</code> 变量中就必须做这些烦琐的操作。</p><p>例如，对于一个名为 <code>bucketOfBits</code> 的 <code>BitSet</code>：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">bucketOfBits.get(i)<br></code></pre></td></tr></table></figure><p>如果第 <code>i</code> 位处于“开”状态，就返回 <code>true</code>；否则返回 <code>false</code>。类似地：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">bucketOfBits.set(i)<br></code></pre></td></tr></table></figure><p>将第 <code>i</code> 位置为“开”。最后：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">bucketOfBits.clear(i)<br></code></pre></td></tr></table></figure><p>将第 <code>i</code> 位置为“关”。</p><p>作为位集应用的一个示例，这里给出一个“埃拉托色尼筛选法”算法的实现，这个算法用来查找素数（素数是指只能被 1 和本身整除的数，例如 2、3 或 5，“埃拉托色尼筛选法”是最早发现的用来枚举这些基本素数的方法之一）。这并不是查找素数的一种非常好的方法，但是由于某些原因，它已经成为测试编译器性能的一种流行的基准（这也不是一个很好的测试基准，因为它主要用于测试位操作）。</p><p>在此，我们要向传统致敬，给出这个算法的一个实现。这个程序将计算 2～2 000 000 之间的所有素数（一共有 148 933 个素数，所以你可能不希望把它们全部打印出来）。</p><p>这里并不想深入程序的细节，关键是要遍历一个包含 200 万个位的位集。首先将所有的位“打开”，然后将已知素数的倍数所对应的位都置为“关”。经过这个操作后保留下来的位对应的就是素数。程序清单 9-8 是用 Java 程序设计语言实现的程序，程序清单 9-9 是用 C++ 实现的代码。</p><blockquote><p>注释：尽管这个筛选法并不是一种好的测试基准，不过我还是利用它测试了这个算法两个实现的运行时间。下面是在 Intel i5-8265U 处理器和 16 GB 内存（运行 Ubuntu 20.04）条件下的运行时间结果：</p><p>● C++（g++ 9.3.0）：115 毫秒</p><p>● Java（Java 17）：31 毫秒  </p><p>我们已经对《Java 核心技术》的 11 个版本进行了这项测试，在最近的 7 个版本中，Java 轻松地战胜了 C++。公平地说，如果提高 C++ 编译器的优化级别，会比 Java 快 14 毫秒。只有当程序运行的时间长到触发 HotSpot 即时编译器时，Java 才能与之相当。</p></blockquote>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>《Java核心技术卷一》第八章</title>
    <link href="/2025/11/22/%E3%80%8AJava%E6%A0%B8%E5%BF%83%E6%8A%80%E6%9C%AF%E5%8D%B7%E4%B8%80%E3%80%8B%E7%AC%AC%E5%85%AB%E7%AB%A0/"/>
    <url>/2025/11/22/%E3%80%8AJava%E6%A0%B8%E5%BF%83%E6%8A%80%E6%9C%AF%E5%8D%B7%E4%B8%80%E3%80%8B%E7%AC%AC%E5%85%AB%E7%AB%A0/</url>
    
    <content type="html"><![CDATA[<h2 id="第8章-泛型程序设计"><a href="#第8章-泛型程序设计" class="headerlink" title="第8章 泛型程序设计"></a>第8章 泛型程序设计</h2><h3 id="8-1-为什么要使用泛型程序设计"><a href="#8-1-为什么要使用泛型程序设计" class="headerlink" title="8.1 为什么要使用泛型程序设计"></a>8.1 为什么要使用泛型程序设计</h3><p><strong>泛型程序设计（generic programming）</strong> 意味着编写的代码可以对多种不同类型的对象重用。例如，你并不希望为收集 String 和 File 对象分别编写不同的类。实际上一个 ArrayList 类就可以收集任何类的对象。</p><h4 id="8-1-1-类型参数的好处"><a href="#8-1-1-类型参数的好处" class="headerlink" title="8.1.1 类型参数的好处"></a>8.1.1 类型参数的好处</h4><p>在 Java 中增加泛型类之前，泛型程序设计是用继承（inheritance）实现的。ArrayList 类只维护一个 Object 引用的数组：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">ArrayList</span> <span class="hljs-comment">// before generic classes</span><br>&#123;<br>    <span class="hljs-keyword">private</span> Object[] elementData;<br>    <span class="hljs-keyword">public</span> Object <span class="hljs-title function_">get</span><span class="hljs-params">(<span class="hljs-type">int</span> i)</span> &#123; ... &#125;<br><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">add</span><span class="hljs-params">(Object o)</span> &#123; ... &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>这种方法存在两个问题。获取一个值时必须进行强制类型转换：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">ArrayList</span> <span class="hljs-variable">files</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">ArrayList</span>();<br><span class="hljs-type">String</span> <span class="hljs-variable">filename</span> <span class="hljs-operator">=</span> (String) files.get(<span class="hljs-number">0</span>);<br></code></pre></td></tr></table></figure><p>此外，这里没有错误检查。可以向数组列表中添加任何类的值：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">files.add(<span class="hljs-keyword">new</span> <span class="hljs-title class_">File</span>(<span class="hljs-string">&quot;...&quot;</span>));<br></code></pre></td></tr></table></figure><p>对于这个调用，编译和运行都不会出错。不过在其他地方，如果将 <code>get</code> 的结果强制类型转换为 <code>String</code> 类型，就会产生一个错误。</p><p>泛型提供了一个更好的解决方案：<strong>类型参数（type parameter）</strong>。<code>ArrayList</code> 类现在有一个类型参数用来指示元素的类型：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">var</span> <span class="hljs-variable">files</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">ArrayList</span>&lt;String&gt;();<br></code></pre></td></tr></table></figure><p>这使得代码具有更好的可读性。人们一看就知道这个数组列表中包含的是 <code>String</code> 对象。</p><blockquote><p><strong>注释</strong>：如果用一个明确的类型而不是 <code>var</code> 声明一个变量，则可以通过使用“菱形”语法省略构造器中的类型参数：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">ArrayList&lt;String&gt; files = <span class="hljs-keyword">new</span> <span class="hljs-title class_">ArrayList</span>&lt;&gt;();<br></code></pre></td></tr></table></figure><p>省略的类型可以从变量的类型推断得出。</p><p>Java 9 扩展了菱形语法的使用范围，原先不接受这种语法的地方现在也可以使用了。例如，现在可以对匿名子类使用菱形语法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs java">ArrayList&lt;String&gt; passwords = <span class="hljs-keyword">new</span> <span class="hljs-title class_">ArrayList</span>&lt;&gt;() <br><span class="hljs-comment">// diamond OK in Java9</span><br>&#123; <br>    <span class="hljs-keyword">public</span> String <span class="hljs-title function_">get</span><span class="hljs-params">(<span class="hljs-type">int</span> n)</span> &#123;<br>        <span class="hljs-keyword">return</span> <span class="hljs-built_in">super</span>.get(n).replaceAll(<span class="hljs-string">&quot;.&quot;</span>, <span class="hljs-string">&quot;*&quot;</span>);<br>    &#125;<br>&#125;;<br></code></pre></td></tr></table></figure></blockquote><p>编译器也可以充分利用这个类型信息。调用 <code>get</code> 的时候，不再需要强制类型转换。编译器知道返回值类型为 <code>String</code>，而不是 <code>Object</code>：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">String</span> <span class="hljs-variable">filename</span> <span class="hljs-operator">=</span> files.get(<span class="hljs-number">0</span>);<br></code></pre></td></tr></table></figure><p>编译器还知道 <code>ArrayList&lt;String&gt;</code> 的 <code>add</code> 方法有一个类型为 <code>String</code> 的参数，这比有一个 <code>Object</code> 类型的参数要安全得多。现在，编译器会检查，防止你插入错误类型的对象。例如，以下语句：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">files.add(<span class="hljs-keyword">new</span> <span class="hljs-title class_">File</span>(<span class="hljs-string">&quot;..&quot;</span>)); <span class="hljs-comment">// can only add String objects to an ArrayList&lt;String&gt;</span><br></code></pre></td></tr></table></figure><p>是无法通过编译的。不过，得到编译错误要比运行时出现类的强制类型转换异常好得多。</p><p>这正是类型参数的魅力所在：它们会让你的程序更易读，也更安全。</p><h3 id="8-2-定义简单泛型类"><a href="#8-2-定义简单泛型类" class="headerlink" title="8.2 定义简单泛型类"></a>8.2 定义简单泛型类</h3><p>**泛型类（generic class）就是有一个或多个类型变量的类。**本章使用一个简单的 <code>Pair</code> 类作为例子。这个类使我们可以只关注泛型，而不用为数据存储的细节而分心。下面是泛型 <code>Pair</code> 类的代码：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Pair</span>&lt;T&gt; &#123;<br>    <span class="hljs-keyword">private</span> T first;<br>    <span class="hljs-keyword">private</span> T second;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-title function_">Pair</span><span class="hljs-params">()</span> &#123;<br>        first = <span class="hljs-literal">null</span>;<br>        second = <span class="hljs-literal">null</span>;<br>    &#125;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-title function_">Pair</span><span class="hljs-params">(T first, T second)</span> &#123;<br>        <span class="hljs-built_in">this</span>.first = first;<br>        <span class="hljs-built_in">this</span>.second = second;<br>    &#125;<br><br>    <span class="hljs-keyword">public</span> T <span class="hljs-title function_">getFirst</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-keyword">return</span> first;<br>    &#125;<br><br>    <span class="hljs-keyword">public</span> T <span class="hljs-title function_">getSecond</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-keyword">return</span> second;<br>    &#125;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">setFirst</span><span class="hljs-params">(T newValue)</span> &#123;<br>        first = newValue;<br>    &#125;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">setSecond</span><span class="hljs-params">(T newValue)</span> &#123;<br>        second = newValue;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p><code>Pair</code> 类引入了一个类型变量 <code>T</code>，用尖括号（<code>&lt;&gt;</code>）括起来，放在类名的后面。**泛型类可以有多个类型变量。**例如，可以定义 <code>Pair</code> 类，其中第一个字段和第二个字段使用不同的类型：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Pair</span>&lt;T, U&gt; &#123; ... &#125;<br></code></pre></td></tr></table></figure><p>**类型变量在整个类定义中用于指定方法的返回类型以及字段和局部变量的类型。**例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">private</span> T first; <span class="hljs-comment">// uses the type variable</span><br></code></pre></td></tr></table></figure><blockquote><p><strong>注释</strong>：常见的做法是<strong>类型变量使用大写字母</strong>，而且很简短。<strong>Java 类库使用变量 <code>E</code> 表示集合的元素类型，<code>K</code> 和 <code>V</code> 分别表示表的键和值的类型。<code>T</code>（必要时还可以用相邻的字母 <code>U</code> 和 <code>S</code>）表示“任意类型”。</strong></p></blockquote><p>可以用具体的类型替换类型变量来**实例化（instantiate）**泛型类型，例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">Pair&lt;String&gt;<br></code></pre></td></tr></table></figure><p>可以把结果想象成一个普通类，它有以下构造器：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java">Pair&lt;String&gt;()<br>Pair&lt;String&gt;(String, String)<br></code></pre></td></tr></table></figure><p>以及以下方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs java">String <span class="hljs-title function_">getFirst</span><span class="hljs-params">()</span><br>String <span class="hljs-title function_">getSecond</span><span class="hljs-params">()</span><br><span class="hljs-keyword">void</span> <span class="hljs-title function_">setFirst</span><span class="hljs-params">(String)</span><br><span class="hljs-keyword">void</span> <span class="hljs-title function_">setSecond</span><span class="hljs-params">(String)</span><br></code></pre></td></tr></table></figure><p>换句话说，泛型类相当于普通类的工厂。</p><h3 id="8-3-泛型方法"><a href="#8-3-泛型方法" class="headerlink" title="8.3 泛型方法"></a>8.3 泛型方法</h3><p><strong>可以定义一个带有类型参数的方法。</strong></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">class</span> <span class="hljs-title class_">ArrayAlg</span> &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> &lt;T&gt; T <span class="hljs-title function_">getMiddle</span><span class="hljs-params">(T... a)</span> &#123;<br>        <span class="hljs-keyword">return</span> a[a.length / <span class="hljs-number">2</span>];<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>这个方法是在普通类中定义的，而不是在泛型类中。不过，这是一个泛型方法，可以从尖括号和类型变量看出这一点。注意，类型变量放在修饰符（这里的修饰符就是 <code>public static</code>）的后面，并在返回类型的前面。</p><p>可以在普通类中定义泛型方法，也可以在泛型类中定义。</p><p><strong>当调用一个泛型方法时，可以把具体类型包围在尖括号中，放在方法名前面</strong>：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">String</span> <span class="hljs-variable">middle</span> <span class="hljs-operator">=</span> ArrayAlg.&lt;String&gt;getMiddle(<span class="hljs-string">&quot;John&quot;</span>, <span class="hljs-string">&quot;Q.&quot;</span>, <span class="hljs-string">&quot;Public&quot;</span>);<br></code></pre></td></tr></table></figure><p>在这种情况下，方法调用中可以省略 <code>&lt;String&gt;</code> 类型参数。编译器有足够的信息推断出你想要的方法。它将参数的类型与泛型类型 <code>T...</code> 进行匹配，推断出 <code>T</code> 一定是 <code>String</code>。也就是说，可以简单地调用：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">String</span> <span class="hljs-variable">middle</span> <span class="hljs-operator">=</span> ArrayAlg.getMiddle(<span class="hljs-string">&quot;John&quot;</span>, <span class="hljs-string">&quot;Q.&quot;</span>, <span class="hljs-string">&quot;Public&quot;</span>);<br></code></pre></td></tr></table></figure><p>几乎在所有情况下，泛型方法的类型推导都能正常工作。偶尔，编译器也会提示错误，此时你就需要解译错误报告。考虑下面这个示例：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">double</span> <span class="hljs-variable">middle</span> <span class="hljs-operator">=</span> ArrayAlg.getMiddle(<span class="hljs-number">3.14</span>, <span class="hljs-number">1729</span>, <span class="hljs-number">0</span>);<br></code></pre></td></tr></table></figure><p>错误消息以晦涩的方式指出：解释这个代码有两种方式，而且这两种方式都是合法的。简单地说，编译器将把参数自动装箱为 1 个 <code>Double</code> 和 2 个 <code>Integer</code> 对象，然后寻找这些类的共同超类型。事实上，它找到了 2 个超类型：<code>Number</code> 和 <code>Comparable</code> 接口，<code>Comparable</code> 接口本身也是一个泛型类型。在这种情况下，可以采取的补救措施是将所有的参数都写为 <code>double</code> 值。</p><blockquote><p><strong>提示</strong>：如果想知道编译器对一个泛型方法调用最终推断出哪种类型：故意引入一个错误，然后研究所得到的错误消息。例如，考虑调用 <code>ArrayAlg.getMiddle(&quot;Hello&quot;, 0, null)</code>。将结果赋给 <code>JButton</code>，这肯定是不对的。将会得到一个错误报告：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java">found:<br>java.lang.Object &amp; java.io.Serializable &amp; java.lang.Comparable&lt;? <span class="hljs-keyword">extends</span><br><span class="hljs-title class_">java</span>.lang.Object &amp; java.io.Serializable &amp; java.lang.Comparable&lt;?&gt;&gt;<br></code></pre></td></tr></table></figure><p>大致的意思是：可以将结果赋给 <code>Object</code>、<code>Serializable</code> 或 <code>Comparable</code>。</p></blockquote><h3 id="8-4-类型变量的限定"><a href="#8-4-类型变量的限定" class="headerlink" title="8.4 类型变量的限定"></a>8.4 类型变量的限定</h3><p>有时，类或方法需要对类型变量加以约束。下面是一个典型的例子。我们要计算数组中的最小元素：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">class</span> <span class="hljs-title class_">ArrayAlg</span> &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> &lt;T&gt; T <span class="hljs-title function_">min</span><span class="hljs-params">(T[] a)</span> &#123; <span class="hljs-comment">// almost correct</span><br>        <span class="hljs-keyword">if</span> (a == <span class="hljs-literal">null</span> || a.length == <span class="hljs-number">0</span>) <span class="hljs-keyword">return</span> <span class="hljs-literal">null</span>;<br>        <span class="hljs-type">T</span> <span class="hljs-variable">smallest</span> <span class="hljs-operator">=</span> a[<span class="hljs-number">0</span>];<br>        <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">1</span>; i &lt; a.length; i++)<br>            <span class="hljs-keyword">if</span> (smallest.compareTo(a[i]) &gt; <span class="hljs-number">0</span>) smallest = a[i];<br>        <span class="hljs-keyword">return</span> smallest;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>但是，这里有一个问题。请看 <code>min</code> 方法的代码。变量 <code>smallest</code> 的类型为 <code>T</code>，这意味着它可以是任何一个类的对象。如何知道 <code>T</code> 所属的类有一个 <code>compareTo</code> 方法呢？</p><p>解决这个问题的办法是限制 <code>T</code> 只能是实现了 <code>Comparable</code> 接口（包含一个方法 <code>compareTo</code> 的标准接口）的一个类。可以通过对类型变量 <code>T</code> 设置一个<strong>限定（bound）</strong> 来实现这一点：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> &lt;T <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Comparable</span>&gt; T <span class="hljs-title function_">min</span><span class="hljs-params">(T[] a)</span><br></code></pre></td></tr></table></figure><p>实际上 <code>Comparable</code> 接口本身就是一个泛型类型。</p><p>现在，泛型方法 <code>min</code> 只能在实现了 <code>Comparable</code> 接口的类（如 <code>String</code>、<code>LocalDate</code> 等）的数组上调用。因为 <code>Rectangle</code> 类没有实现 <code>Comparable</code> 接口，所以在 <code>Rectangle</code> 数组上调用 <code>min</code> 将会得到一个编译错误。</p><p>你或许会感到奇怪——在这里我们为什么使用关键字 <code>extends</code> 而不是 <code>implements</code>？毕竟，<code>Comparable</code> 是一个接口。下面的记法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">&lt;T <span class="hljs-keyword">extends</span> <span class="hljs-title class_">BoundingType</span>&gt;<br></code></pre></td></tr></table></figure><p>表示 <code>T</code> 应该是<strong>限定类型（bounding type）的子类型（subtype）</strong>。<code>T</code> 和限定类型可以是类，也可以是接口。选择关键字 <code>extends</code> 的原因是它更接近子类型的概念，并且 Java 的设计者也不打算在语言中再添加一个新的关键字（如 <code>sub</code>）。</p><p><strong>一个类型变量或通配符可以有多个限定</strong>，例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">&lt;T <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Comparable</span> &amp; Serializable&gt;<br></code></pre></td></tr></table></figure><p><strong>限定类型用 <code>&amp;</code> 分隔，而逗号用来分隔类型变量</strong>。</p><p>按照 Java 继承机制，可以根据需要拥有多个接口超类型，但最多有一个限定可以是类。如果有一个类作为限定，它必须是限定列表中的第一个限定。</p><h3 id="8-5-泛型代码和虚拟机"><a href="#8-5-泛型代码和虚拟机" class="headerlink" title="8.5 泛型代码和虚拟机"></a>8.5 泛型代码和虚拟机</h3><p>**虚拟机没有泛型类型对象——所有对象都属于普通类。**在泛型实现的早期版本中，甚至能够将使用泛型的程序编译为在 1.0 虚拟机上运行的类文件！</p><h4 id="8-5-1-类型擦除"><a href="#8-5-1-类型擦除" class="headerlink" title="8.5.1 类型擦除"></a>8.5.1 类型擦除</h4><p>无论何时定义一个泛型类型，都会自动提供一个相应的<strong>原始类型（raw type）</strong>。<strong>这个原始类型的名字就是去掉类型参数后的泛型类型名</strong>。类型变量会被<strong>擦除（erased）</strong>，并替换为其限定类型（或者，对于无限定的变量则替换为 <code>Object</code>）。</p><p>例如，<code>Pair&lt;T&gt;</code> 的原始类型如下所示：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Pair</span> &#123;<br>    <span class="hljs-keyword">private</span> Object first;<br>    <span class="hljs-keyword">private</span> Object second;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-title function_">Pair</span><span class="hljs-params">(Object first, Object second)</span> &#123;<br>        <span class="hljs-built_in">this</span>.first = first;<br>        <span class="hljs-built_in">this</span>.second = second;<br>    &#125;<br>    <span class="hljs-keyword">public</span> Object <span class="hljs-title function_">getFirst</span><span class="hljs-params">()</span> &#123; <span class="hljs-keyword">return</span> first; &#125;<br><span class="hljs-keyword">public</span> Object <span class="hljs-title function_">getSecond</span><span class="hljs-params">()</span> &#123; <span class="hljs-keyword">return</span> second; &#125;<br><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">setFirst</span><span class="hljs-params">(Object newValue)</span> &#123; first = newValue; &#125;<br><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">setSecond</span><span class="hljs-params">(Object newValue)</span> &#123; second = newValue; &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>因为 <code>T</code> 是一个无限定的类型变量，所以直接替换为 <code>Object</code>。</p><p>其结果是一个普通类，就好像 Java 语言中引入泛型之前实现的类一样。</p><p>在程序中可以包含不同类型的 <code>Pair</code>，例如，<code>Pair&lt;String&gt;</code> 或 <code>Pair&lt;LocalDate&gt;</code>。不过擦除类型后，它们都会变成原始的 <code>Pair</code> 类型。</p><p><strong>原始类型用第一个限定来替换类型变量，或者，如果没有给定限定，就替换为 <code>Object</code>。</strong></p><p>例如，类 <code>Pair&lt;T&gt;</code> 中的类型变量没有显式的限定，因此，原始类型用 <code>Object</code> 替换 <code>T</code>。假定我们声明了一个稍有不同的类型：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Interval</span>&lt;T <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Comparable</span> &amp; Serializable&gt; <span class="hljs-keyword">implements</span> <span class="hljs-title class_">Serializable</span> &#123;<br>    <span class="hljs-keyword">private</span> T lower;<br>    <span class="hljs-keyword">private</span> T upper;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-title function_">Interval</span><span class="hljs-params">(T first, T second)</span> &#123;<br>        <span class="hljs-keyword">if</span> (first.compareTo(second) &lt;= <span class="hljs-number">0</span>) &#123;<br>            lower = first;<br>            upper = second;<br>        &#125; <span class="hljs-keyword">else</span> &#123;<br>            lower = second;<br>            upper = first;<br>        &#125;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>原始类型 <code>Interval</code> 如下所示：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Interval</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">Serializable</span> &#123;<br>    <span class="hljs-keyword">private</span> Comparable lower;<br>    <span class="hljs-keyword">private</span> Comparable upper;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-title function_">Interval</span><span class="hljs-params">(Comparable first, Comparable second)</span> &#123; ... &#125;<br>&#125;<br></code></pre></td></tr></table></figure><blockquote><p><strong>注释</strong>：你可能想要知道限定切换为 <code>class Interval&lt;T extends Serializable &amp; Comparable&gt;</code> 会发生什么。如果这样做，原始类型会用 <code>Serializable</code> 替换 <code>T</code>，而且编译器会在必要时插入转换为 <code>Comparable</code> 的强制类型转换。为了提高效率，<strong>应该将标记（tagging）接口（即没有方法的接口，例如<code>Serializable</code>）放在限定列表的末尾。</strong></p></blockquote><h4 id="8-5-2-转换泛型表达式"><a href="#8-5-2-转换泛型表达式" class="headerlink" title="8.5.2 转换泛型表达式"></a>8.5.2 转换泛型表达式</h4><p>**编写一个泛型方法调用时，如果擦除了返回类型，编译器会插入强制类型转换。**例如，对于下面这个语句序列：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java">Pair&lt;Employee&gt; buddies = ...;<br><span class="hljs-type">Employee</span> <span class="hljs-variable">buddy</span> <span class="hljs-operator">=</span> buddies.getFirst();<br></code></pre></td></tr></table></figure><p><code>getFirst</code> 擦除类型后的返回类型是 <code>Object</code>。编译器自动插入转换到 <code>Employee</code> 的强制类型转换。也就是说，编译器把这个方法调用转换为两条虚拟机指令：</p><ul><li>调用原始方法 <code>Pair.getFirst</code>。</li><li>将返回的 <code>Object</code> 类型强制转换为 <code>Employee</code> 类型。</li></ul><p>**访问一个泛型字段时也会插入强制类型转换。**假设 <code>Pair</code> 类的 <code>first</code> 字段和 <code>second</code> 字段都是公共的（也许这不是一种好的编程风格，但在 Java 中是合法的）。以下表达式：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">Employee</span> <span class="hljs-variable">buddy</span> <span class="hljs-operator">=</span> buddies.first;<br></code></pre></td></tr></table></figure><p>也会在结果字节码中插入强制类型转换。</p><h4 id="8-5-3-转换泛型方法"><a href="#8-5-3-转换泛型方法" class="headerlink" title="8.5.3 转换泛型方法"></a>8.5.3 转换泛型方法</h4><p>**类型擦除也会出现在泛型方法中。**程序员通常认为类似下面的泛型方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> &lt;T <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Comparable</span>&gt; T <span class="hljs-title function_">min</span><span class="hljs-params">(T[] a)</span><br></code></pre></td></tr></table></figure><p>是整个一组方法，而擦除类型之后，只剩下一个方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> Comparable <span class="hljs-title function_">min</span><span class="hljs-params">(Comparable[] a)</span><br></code></pre></td></tr></table></figure><p>注意，类型参数 <code>T</code> 已经被擦除了，只留下了它的限定类型 <code>Comparable</code>。</p><p>方法的擦除带来了两个复杂问题。考虑下面这个示例：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">class</span> <span class="hljs-title class_">DateInterval</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Pair</span>&lt;LocalDate&gt; &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">setSecond</span><span class="hljs-params">(LocalDate second)</span> &#123;<br>        <span class="hljs-keyword">if</span> (second.compareTo(getFirst()) &gt;= <span class="hljs-number">0</span>)<br>            <span class="hljs-built_in">super</span>.setSecond(second);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>日期区间是一对 <code>LocalDate</code> 对象，而且我们想覆盖这个方法来确保第二个值永远不小于第一个值。这个类擦除后变成：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">class</span> <span class="hljs-title class_">DateInterval</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Pair</span> &#123; <span class="hljs-comment">// after erasure</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">setSecond</span><span class="hljs-params">(LocalDate second)</span> &#123; ... &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>令人感到奇怪的是，还有另一个从 <code>Pair</code> 继承的 <code>setSecond</code> 方法，即：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">setSecond</span><span class="hljs-params">(Object second)</span><br></code></pre></td></tr></table></figure><p>这显然是一个不同的方法，因为它有一个不同类型的参数——<code>Object</code>，而不是 <code>LocalDate</code>。不过，它不应该不一样。考虑下面的语句序列：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">var</span> <span class="hljs-variable">interval</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">DateInterval</span>(...);<br>Pair&lt;LocalDate&gt; pair = interval; <span class="hljs-comment">// OK--assignment to superclass</span><br>pair.setSecond(aDate);<br></code></pre></td></tr></table></figure><p>这里，我们希望 <code>setSecond</code> 调用具有多态性，应该调用适当的方法。因为 <code>pair</code> 引用一个 <code>DateInterval</code> 对象，所以应该调用 <code>DateInterval.setSecond</code>。问题在于类型擦除与多态发生了冲突。为了解决这个问题，编译器在 <code>DateInterval</code> 类中生成一个<strong>桥方法（bridge method）</strong>：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">setSecond</span><span class="hljs-params">(Object second)</span> &#123; setSecond((LocalDate) second); &#125;<br></code></pre></td></tr></table></figure><p>要想了解为什么这样可行，请仔细跟踪以下语句的执行：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">pair.setSecond(aDate);<br></code></pre></td></tr></table></figure><p>变量 <code>pair</code> 已经声明为类型 <code>Pair&lt;LocalDate&gt;</code>，并且这个类型只有一个名为 <code>setSecond</code> 的方法，即 <code>setSecond(Object)</code>。虚拟机在 <code>pair</code> 引用的对象上调用这个方法。这个对象是 <code>DateInterval</code> 类型，因而将会调用 <code>DateInterval.setSecond(Object)</code> 方法。这个方法是合成的桥方法。它会调用 <code>DateInterval.setSecond(LocalDate)</code>，这正是我们想要的。</p><p>桥方法可能会变得更奇怪。假设 <code>DateInterval</code> 类也覆盖了 <code>getSecond</code> 方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">class</span> <span class="hljs-title class_">DateInterval</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Pair</span>&lt;LocalDate&gt; &#123;<br>    <span class="hljs-keyword">public</span> LocalDate <span class="hljs-title function_">getSecond</span><span class="hljs-params">()</span> &#123; <span class="hljs-keyword">return</span> (LocalDate) <span class="hljs-built_in">super</span>.getSecond(); &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>在 <code>DateInterval</code> 类中，有两个 <code>getSecond</code> 方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java">LocalDate <span class="hljs-title function_">getSecond</span><span class="hljs-params">()</span> <span class="hljs-comment">// defined in DateInterval</span><br>Object   <span class="hljs-title function_">getSecond</span><span class="hljs-params">()</span> <span class="hljs-comment">// overrides the method defined in Pair to call the first method</span><br></code></pre></td></tr></table></figure><p>你不能编写这样的 Java 代码（两个方法有相同的参数类型是不合法的，在这里，两个方法都没有参数）。但是，在虚拟机中，会由参数类型以及返回类型共同指定一个方法。因此，编译器可以为两个仅返回类型不同的方法生成字节码，虚拟机能够正确地处理这种情况。</p><blockquote><p><strong>注释</strong>：<strong>桥方法不只是用于泛型类型</strong>。一个方法覆盖另一个方法时，可以指定一个更严格的返回类型，这是合法的。例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Employee</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">Cloneable</span> &#123;<br> <span class="hljs-keyword">public</span> Employee <span class="hljs-title function_">clone</span><span class="hljs-params">()</span> <span class="hljs-keyword">throws</span> CloneNotSupportedException &#123; ... &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p><code>Object.clone</code> 和 <code>Employee.clone</code> 方法被称为有协变的返回类型（covariant return type）。实际上，<code>Employee</code> 类有两个克隆方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java">Employee <span class="hljs-title function_">clone</span><span class="hljs-params">()</span> <span class="hljs-comment">// defined above</span><br>Object <span class="hljs-title function_">clone</span><span class="hljs-params">()</span>   <span class="hljs-comment">// synthesized bridge method, overrides Object.clone</span><br></code></pre></td></tr></table></figure><p>合成的桥方法会调用新定义的方法。</p></blockquote><p>总之，对于 Java 泛型的转换，需要记住以下几点：</p><ul><li>虚拟机中没有泛型，只有普通的类和方法。  </li><li>所有的类型参数都会替换为它们的限定类型。  </li><li>会合成桥方法来保持多态。  </li><li>为保持类型安全性，必要时会插入强制类型转换。</li></ul><h4 id="8-5-4-调用遗留代码"><a href="#8-5-4-调用遗留代码" class="headerlink" title="8.5.4 调用遗留代码"></a>8.5.4 调用遗留代码</h4><p>设计 Java 泛型时，主要目标是允许泛型代码和遗留代码之间能够互操作。下面看有关遗留代码的一个具体示例。Swing 用户界面工具包提供了一个 <code>JSlider</code> 类，它的“刻度”（tick）可以定制为包含文本或图像的标签。这些标签用以下调用设置：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">void</span> <span class="hljs-title function_">setLabelTable</span><span class="hljs-params">(Dictionary table)</span><br></code></pre></td></tr></table></figure><p><code>Dictionary</code> 类将整数映射到标签。在 Java 5 之前，这个类实现为一个 <code>Object</code> 实例映射。Java 5 把 <code>Dictionary</code> 实现为一个泛型类，不过 <code>JSlider</code> 从未更新。此时，没有类型参数的 <code>Dictionary</code> 是一个原始类型。这里就存在兼容性问题。</p><p>填充字典时，可以使用泛型类型：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java">Dictionary&lt;Integer, Component&gt; labelTable = <span class="hljs-keyword">new</span> <span class="hljs-title class_">Hashtable</span>&lt;&gt;();<br>labelTable.put(<span class="hljs-number">0</span>, <span class="hljs-keyword">new</span> <span class="hljs-title class_">JLabel</span>(<span class="hljs-keyword">new</span> <span class="hljs-title class_">ImageIcon</span>(<span class="hljs-string">&quot;nine.gif&quot;</span>)));<br>labelTable.put(<span class="hljs-number">20</span>, <span class="hljs-keyword">new</span> <span class="hljs-title class_">JLabel</span>(<span class="hljs-keyword">new</span> <span class="hljs-title class_">ImageIcon</span>(<span class="hljs-string">&quot;ten.gif&quot;</span>)));<br></code></pre></td></tr></table></figure><p>将 <code>Dictionary&lt;Integer, Component&gt;</code> 对象传递给 <code>setLabelTable</code> 时，编译器会发出一个警告：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">slider.setLabelTable(labelTable);  <span class="hljs-comment">// warning</span><br></code></pre></td></tr></table></figure><p>毕竟，编译器无法确定 <code>setLabelTable</code> 可能会对 <code>Dictionary</code> 对象做什么操作。这个方法可能会把字典的所有键替换为字符串。这就打破了键类型必须为 <code>Integer</code> 的承诺，未来的操作有可能导致糟糕的强制类型转换异常。</p><p>要仔细考虑这个问题，想想看 <code>JSlider</code> 到底会用 <code>Dictionary</code> 对象做什么。在这里十分清楚，<code>JSlider</code> 只读取这个信息，因此可以忽略这个警告。</p><p>现在看一个相反的情形，由一个遗留类得到一个原始类型的对象。可以将它赋给一个类型使用了泛型的变量，当然，这样做会得到一个警告。例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">Dictionary&lt;Integer, Component&gt; labelTable = slider.getLabelTable();  <span class="hljs-comment">// warning</span><br></code></pre></td></tr></table></figure><p>没关系。查看这个警告，确保标签表确实包含 <code>Integer</code> 和 <code>Component</code> 对象。当然，从来没有绝对的保证。恶意的程序员可能会在滑动条中安装一个不同的 <code>Dictionary</code>。不过，这种情况并不会比有泛型之前的情况更糟糕。最差的情况也就是程序抛出一个异常。</p><p>考虑了这个警告之后，可以使用**注解（annotation）**使之消失。可以对一个局部变量加注解，如下所示：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-meta">@SuppressWarnings(&quot;unchecked&quot;)</span><br>Dictionary&lt;Integer, Component&gt; labelTable = slider.getLabelTable();  <span class="hljs-comment">// no warning</span><br></code></pre></td></tr></table></figure><p>或者，可以对整个方法加注解，如下所示：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-meta">@SuppressWarnings(&quot;unchecked&quot;)</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">setupSlider</span><span class="hljs-params">()</span> &#123;<br>    ...<br>&#125;<br></code></pre></td></tr></table></figure><p>这个注解会关闭对方法中所有代码的检查。</p><h3 id="8-6-限制与局限性"><a href="#8-6-限制与局限性" class="headerlink" title="8.6 限制与局限性"></a>8.6 限制与局限性</h3><h4 id="8-6-1-不能用基本类型实例化类型参数"><a href="#8-6-1-不能用基本类型实例化类型参数" class="headerlink" title="8.6.1 不能用基本类型实例化类型参数"></a>8.6.1 不能用基本类型实例化类型参数</h4><p>**不能用基本类型代替类型参数。**因此，没有 <code>Pair&lt;double&gt;</code>，只有 <code>Pair&lt;Double&gt;</code>。当然，其原因就在于类型擦除。擦除之后，<code>Pair</code> 类含有 <code>Object</code> 类型的字段，而 <code>Object</code> 不能存储 <code>double</code> 值。</p><p>这样做与 Java 语言中基本类型的独立状态相一致。这并不是一个致命的缺陷——只有 8 种基本类型，而且即使不能接受包装器类型（wrapper type），也可以使用单独的类和方法来处理。</p><h4 id="8-6-2-运行时类型查询只适用于原始类型"><a href="#8-6-2-运行时类型查询只适用于原始类型" class="headerlink" title="8.6.2 运行时类型查询只适用于原始类型"></a>8.6.2 运行时类型查询只适用于原始类型</h4><p>虚拟机中的对象总是有一个特定的非泛型类型。因此，<strong>所有的类型查询只生成原始类型</strong>。例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">if</span> (a <span class="hljs-keyword">instanceof</span> Pair&lt;String&gt;) <span class="hljs-comment">// ERROR</span><br></code></pre></td></tr></table></figure><p>实际上仅仅测试 <code>a</code> 是否是任意类型的一个 <code>Pair</code>。下面的测试同样如此：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">if</span> (a <span class="hljs-keyword">instanceof</span> Pair&lt;T&gt;) <span class="hljs-comment">// ERROR</span><br></code></pre></td></tr></table></figure><p>或以下强制类型转换也是如此：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">Pair&lt;String&gt; p = (Pair&lt;String&gt;) a; <span class="hljs-comment">// warning-can only test that a is a Pair</span><br></code></pre></td></tr></table></figure><p>为了提醒这一风险，<strong>如果试图查询一个对象是否属于某个泛型类型，你会得到一个编译器错误（使用 <code>instanceof</code> 时），或者得到一个警告（使用强制类型转换时）</strong>。</p><p>同样的道理，<code>getClass</code> 方法总是返回原始类型。例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java">Pair&lt;String&gt; stringPair = ...;<br>Pair&lt;Employee&gt; employeePair = ...;<br><span class="hljs-keyword">if</span> (stringPair.getClass() == employeePair.getClass()) <span class="hljs-comment">// they are equal</span><br></code></pre></td></tr></table></figure><p>这个比较的结果是 <code>true</code>，因为两个 <code>getClass</code> 调用都返回 <code>Pair.class</code>。</p><h4 id="8-6-3-不能创建参数化类型的数组"><a href="#8-6-3-不能创建参数化类型的数组" class="headerlink" title="8.6.3 不能创建参数化类型的数组"></a>8.6.3 不能创建参数化类型的数组</h4><p>**不能实例化参数化类型的数组，**例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">var</span> <span class="hljs-variable">table</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Pair</span>&lt;String&gt;[<span class="hljs-number">10</span>]; <span class="hljs-comment">// ERROR</span><br></code></pre></td></tr></table></figure><p>这有什么问题呢？擦除之后，<code>table</code> 的类型是 <code>Pair[]</code>。可以把它转换为 <code>Object[]</code>：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">Object[] objarray = table;<br></code></pre></td></tr></table></figure><p>数组会记住它的元素类型，如果试图存储类型不正确的元素，就会抛出一个 <code>ArrayStoreException</code> 异常：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">objarray[<span class="hljs-number">0</span>] = <span class="hljs-string">&quot;Hello&quot;</span>; <span class="hljs-comment">// ERROR--component type is Pair</span><br></code></pre></td></tr></table></figure><p>不过对于泛型类型，擦除会使这种机制无效。以下赋值</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">objarray[<span class="hljs-number">0</span>] = <span class="hljs-keyword">new</span> <span class="hljs-title class_">Pair</span>&lt;Employee&gt;();<br></code></pre></td></tr></table></figure><p>尽管能够通过数组存储的检查，但仍会导致一个类型错误。出于这个原因，<strong>不允许创建参数化类型的数组。</strong></p><p>需要说明的是，只是<strong>不允许创建这些数组</strong>，而声明类型为 <code>Pair&lt;String&gt;[]</code> 的变量仍是合法的。不过不能用 <code>new Pair&lt;String&gt;[10]</code> 初始化这个变量。</p><blockquote><p><strong>注释</strong>：<strong>可以声明通配类型的数组，然后进行强制类型转换</strong>：</p><p>var table &#x3D; (Pair<String>[]) new Pair&lt;?&gt;[10];</p><p>结果将是不安全的。如果在 <code>table[0]</code> 中存储一个 <code>Pair&lt;Employee&gt;</code>，然后对 <code>table[0].getFirst()</code> 调用一个 <code>String</code> 方法，会得到一个 <code>ClassCastException</code> 异常。</p><p><strong>提示</strong>：如果需要收集参数化类型对象，可以直接使用 <code>ArrayList</code>：<code>ArrayList&lt;Pair&lt;String&gt;&gt;</code> 很安全也很有效。</p></blockquote><h4 id="8-6-4-Varargs-警告"><a href="#8-6-4-Varargs-警告" class="headerlink" title="8.6.4 Varargs 警告"></a>8.6.4 Varargs 警告</h4><p>Java 不支持泛型类型的数组。向参数个数可变的方法传递一个泛型类型的实例。</p><p>考虑下面这个简单的方法，它的参数个数是可变的：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> &lt;T&gt; <span class="hljs-keyword">void</span> <span class="hljs-title function_">addAll</span><span class="hljs-params">(Collection&lt;T&gt; coll, T... ts)</span> &#123;<br>    <span class="hljs-keyword">for</span> (T t : ts) coll.add(t);<br>&#125;<br></code></pre></td></tr></table></figure><p>回忆一下，实际上参数 <code>ts</code> 是一个数组，包含提供的所有实参。</p><p>现在考虑以下调用：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs java">Collection&lt;Pair&lt;String&gt;&gt; table = ...;<br>Pair&lt;String&gt; pair1 = ...;<br>Pair&lt;String&gt; pair2 = ...;<br>addAll(table, pair1, pair2);<br></code></pre></td></tr></table></figure><p>为了调用这个方法，Java 虚拟机必须建立一个 <code>Pair&lt;String&gt;</code> 数组，这就违反了规则。不过，对于这种情况，规则有所放松，你只会得到一个警告，而不是错误。</p><p>可以采用两种方法来抑制这个警告。一种方法是为包含 <code>addAll</code> 调用的方法增加注解：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-meta">@SuppressWarnings(&quot;unchecked&quot;)</span><br></code></pre></td></tr></table></figure><p>或者在 Java 7 中，还可以用 <code>@SafeVarargs</code> 直接注解 <code>addAll</code> 方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-meta">@SafeVarargs</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> &lt;T&gt; <span class="hljs-keyword">void</span> <span class="hljs-title function_">addAll</span><span class="hljs-params">(Collection&lt;T&gt; coll, T... ts)</span> &#123; ... &#125;<br></code></pre></td></tr></table></figure><p>现在就可以提供泛型类型来调用这个方法了。对于任何只需要读取参数数组元素的方法（这肯定是最常见的情况），都可以使用这个注解。</p><p><code>@SafeVarargs</code> 只能用于声明为 <code>static</code>、<code>final</code> 或（Java 9 中）<code>private</code> 的构造器和方法。所有其他方法都可能被覆盖，这会使这个注解失去意义。</p><blockquote><p>注释：可以使用 <code>@SafeVarargs</code> 注解来消除创建泛型数组的有关限制，方法如下：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-meta">@SafeVarargs</span><br><span class="hljs-keyword">static</span> &lt;E&gt; E[] array(E... array) &#123; <span class="hljs-keyword">return</span> array; &#125;<br></code></pre></td></tr></table></figure><p>现在可以调用：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">Pair&lt;String&gt;[] table = array(pair1, pair2);<br></code></pre></td></tr></table></figure><p>这看起来很方便，不过隐藏着危险。以下代码</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java">Object[] objarray = table;<br>objarray[<span class="hljs-number">0</span>] = <span class="hljs-keyword">new</span> <span class="hljs-title class_">Pair</span>&lt;Employee&gt;();<br></code></pre></td></tr></table></figure><p>能顺利运行而不会出现 <code>ArrayStoreException</code> 异常（因为数组存储只会检查擦除后的类型），但在处理 <code>table[0]</code> 时，你会在别处得到一个异常。</p></blockquote><h4 id="8-6-5-不能实例化类型变量"><a href="#8-6-5-不能实例化类型变量" class="headerlink" title="8.6.5 不能实例化类型变量"></a>8.6.5 不能实例化类型变量</h4><p>不能在类似 <code>new T(...)</code> 的表达式中使用类型变量。例如，下面的 <code>Pair&lt;T&gt;</code> 构造器就是非法的：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-title function_">Pair</span><span class="hljs-params">()</span> &#123; first = <span class="hljs-keyword">new</span> <span class="hljs-title class_">T</span>(); second = <span class="hljs-keyword">new</span> <span class="hljs-title class_">T</span>(); &#125; <span class="hljs-comment">// ERROR</span><br></code></pre></td></tr></table></figure><p>类型擦除将 <code>T</code> 变成 <code>Object</code>，而你肯定不希望调用 <code>new Object()</code>。</p><p>在 Java 8 之后，最好的解决办法是让调用者提供一个构造器表达式。例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">Pair&lt;String&gt; p = Pair.makePair(String::<span class="hljs-keyword">new</span>);<br></code></pre></td></tr></table></figure><p><code>makePair</code> 方法接收一个 <code>Supplier&lt;T&gt;</code>，这是一个函数式接口，表示一个无参数而且返回类型为 <code>T</code> 的函数：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> &lt;T&gt; Pair&lt;T&gt; <span class="hljs-title function_">makePair</span><span class="hljs-params">(Supplier&lt;T&gt; constr)</span> &#123;<br>    <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Pair</span>&lt;&gt;(constr.get(), constr.get());<br>&#125;<br></code></pre></td></tr></table></figure><p>比较传统的解决方法是通过反射调用 <code>Constructor.newInstance</code> 方法来构造泛型对象。</p><p>遗憾的是，细节有点复杂。不能如下调用：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">first = T.class.getConstructor().newInstance(); <span class="hljs-comment">// ERROR</span><br></code></pre></td></tr></table></figure><p>表达式 <code>T.class</code> 是不合法的，因为它会擦除为 <code>Object.class</code>。必须适当地设计 API 以便得到一个 <code>Class</code> 对象，如下所示：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> &lt;T&gt; Pair&lt;T&gt; <span class="hljs-title function_">makePair</span><span class="hljs-params">(Class&lt;T&gt; cl)</span> &#123;<br>    <span class="hljs-keyword">try</span> &#123;<br>        <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Pair</span>&lt;&gt;(cl.getConstructor().newInstance(),<br>                          cl.getConstructor().newInstance());<br>    &#125; <span class="hljs-keyword">catch</span> (Exception e) &#123;<br>        <span class="hljs-keyword">return</span> <span class="hljs-literal">null</span>;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>这个方法可以如下调用：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">Pair&lt;String&gt; p = Pair.makePair(String.class);<br></code></pre></td></tr></table></figure><p>注意，<code>Class</code> 类本身是泛型的。例如，<code>String.class</code> 是 <code>Class&lt;String&gt;</code> 的一个实例（事实上，它是唯一的实例）。因此，<code>makePair</code> 方法能够推断出所建立的对组（pair）的类型。</p><h4 id="8-6-6-不能构造泛型数组"><a href="#8-6-6-不能构造泛型数组" class="headerlink" title="8.6.6 不能构造泛型数组"></a>8.6.6 不能构造泛型数组</h4><p>不能实例化数组。数组本身也带有类型，用来监控虚拟机中的数组存储。这个类型会被擦除。例如，考虑下面的例子：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> &lt;T <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Comparable</span>&gt; T[] minmax(T... a) &#123;<br>    T[] mm = <span class="hljs-keyword">new</span> <span class="hljs-title class_">T</span>[<span class="hljs-number">2</span>]; <span class="hljs-comment">// ERROR</span><br>&#125;<br></code></pre></td></tr></table></figure><p>类型擦除会让这个方法总是构造 <code>Comparable[2]</code> 数组。</p><p>如果数组仅仅作为一个类的私有实例字段，那么可以将这个数组的元素类型声明为擦除后的类型并使用强制类型转换。例如，<code>ArrayList</code> 类可以如下实现：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">ArrayList</span>&lt;E&gt; &#123;<br>    <span class="hljs-keyword">private</span> Object[] elements;<br><br>    <span class="hljs-meta">@SuppressWarnings(&quot;unchecked&quot;)</span><br>    <span class="hljs-keyword">public</span> E <span class="hljs-title function_">get</span><span class="hljs-params">(<span class="hljs-type">int</span> n)</span> &#123; <span class="hljs-keyword">return</span> (E) elements[n]; &#125;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">set</span><span class="hljs-params">(<span class="hljs-type">int</span> n, E e)</span> &#123; elements[n] = e; &#125; <span class="hljs-comment">// no cast needed</span><br>&#125;<br></code></pre></td></tr></table></figure><p>但实际的实现没有这么清晰：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">ArrayList</span>&lt;E&gt; &#123;<br>    <span class="hljs-keyword">private</span> E[] elements;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-title function_">ArrayList</span><span class="hljs-params">()</span> &#123;<br>        elements = (E[]) <span class="hljs-keyword">new</span> <span class="hljs-title class_">Object</span>[<span class="hljs-number">10</span>];<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>这里，强制类型转换 <code>(E[])</code> 是一个假象，而类型擦除使其无法察觉。</p><p>这个技术并不适用于我们的 <code>minmax</code> 方法，因为 <code>minmax</code> 方法返回一个 <code>T[]</code> 数组，如果我们对类型“作假”，使用擦除后的类型，就会得到运行时错误结果。假设实现以下代码：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> &lt;T <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Comparable</span>&gt; T[] minmax(T... a) &#123;<br>    <span class="hljs-type">var</span> <span class="hljs-variable">result</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Comparable</span>[<span class="hljs-number">2</span>]; <span class="hljs-comment">// array of erased type</span><br>    ...<br>    <span class="hljs-keyword">return</span> (T[]) result; <span class="hljs-comment">// 运行期会抛 ClassCastException</span><br>&#125;<br></code></pre></td></tr></table></figure><p>以下调用</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">String[] names = ArrayAlg.minmax(<span class="hljs-string">&quot;Tom&quot;</span>, <span class="hljs-string">&quot;Dick&quot;</span>, <span class="hljs-string">&quot;Harry&quot;</span>);<br></code></pre></td></tr></table></figure><p>编译时不会有任何警告。当方法返回后 <code>Comparable[]</code> 引用被强制转换为 <code>String[]</code> 时，将会出现 <code>ClassCastException</code> 异常。</p><p>在这种情况下，最好让用户提供一个数组构造器表达式：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">String[] names = ArrayAlg.minmax(String[]::<span class="hljs-keyword">new</span>, <span class="hljs-string">&quot;Tom&quot;</span>, <span class="hljs-string">&quot;Dick&quot;</span>, <span class="hljs-string">&quot;Harry&quot;</span>);<br></code></pre></td></tr></table></figure><p>构造器表达式 <code>String[]::new</code> 指示一个函数，给定所需的长度，会构造一个指定长度的 <code>String</code> 数组。</p><p><code>minmax</code> 方法使用这个参数生成一个有正确类型的数组：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> &lt;T <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Comparable</span>&gt; T[] minmax(IntFunction&lt;T[]&gt; constr, T... a) &#123;<br>    T[] result = constr.apply(<span class="hljs-number">2</span>);<br>    <span class="hljs-comment">// ...</span><br>    <span class="hljs-keyword">return</span> result;<br>&#125;<br></code></pre></td></tr></table></figure><p>比较老式的方法是利用反射，并调用 <code>Array.newInstance</code>：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> &lt;T <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Comparable</span>&gt; T[] minmax(T... a) &#123;<br>    <span class="hljs-type">var</span> <span class="hljs-variable">result</span> <span class="hljs-operator">=</span> (T[]) Array.newInstance(a.getClass().getComponentType(), <span class="hljs-number">2</span>);<br>    <span class="hljs-comment">// ...</span><br>    <span class="hljs-keyword">return</span> result;<br>&#125;<br></code></pre></td></tr></table></figure><p><code>ArrayList</code> 类的 <code>toArray</code> 方法就没有这么幸运。它需要生成一个 <code>T[]</code> 数组，但没有元素类型。因此，有下面两种不同的形式：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java">Object[] toArray()<br>T[] toArray(T[] result)<br></code></pre></td></tr></table></figure><p>第二个方法接收一个数组参数。如果数组足够大，就使用这个数组。否则，用 <code>result</code> 的元素类型构造一个足够大的新数组。</p><h4 id="8-6-7-泛型类的静态上下文中类型变量无效"><a href="#8-6-7-泛型类的静态上下文中类型变量无效" class="headerlink" title="8.6.7 泛型类的静态上下文中类型变量无效"></a>8.6.7 泛型类的静态上下文中类型变量无效</h4><p><strong>不能在静态字段或方法中引用类型变量</strong>。例如，下面的做法看起来很聪明，但实际上行不通：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Singleton</span>&lt;T&gt; &#123;<br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> T singleInstance; <span class="hljs-comment">// ERROR</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> T <span class="hljs-title function_">getSingleInstance</span><span class="hljs-params">()</span> &#123; <span class="hljs-comment">// ERROR</span><br>        <span class="hljs-keyword">if</span> (singleInstance == <span class="hljs-literal">null</span>) construct <span class="hljs-keyword">new</span> <span class="hljs-title class_">instance</span> of T<br>        <span class="hljs-keyword">return</span> singleInstance;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>如果这样可行，程序就可以声明一个 <code>Singleton&lt;Random&gt;</code> 以共享一个随机数生成器，另外声明一个 <code>Singleton&lt;JFileChooser&gt;</code> 以共享一个文件选择器对话框。但是，这样是行不通的。类型擦除之后，只剩下 <code>Singleton</code> 类，它只包含一个 <code>singleInstance</code> 字段。因此，带有类型变量的静态字段和方法是完全非法的。</p><h4 id="8-6-8-不能抛出或捕获泛型类的实例"><a href="#8-6-8-不能抛出或捕获泛型类的实例" class="headerlink" title="8.6.8 不能抛出或捕获泛型类的实例"></a>8.6.8 不能抛出或捕获泛型类的实例</h4><p>**既不能抛出也不能捕获泛型类的对象。**实际上，甚至泛型类扩展 <code>Throwable</code> 都是不合法的。例如，以下定义就不能编译：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Problem</span>&lt;T&gt; <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Exception</span> &#123; <span class="hljs-comment">/* ... */</span> &#125;<br><span class="hljs-comment">// ERROR--can&#x27;t extend Throwable</span><br></code></pre></td></tr></table></figure><p><code>catch</code> 子句中不能使用类型变量。例如，以下方法将不能编译：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> &lt;T <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Throwable</span>&gt; <span class="hljs-keyword">void</span> <span class="hljs-title function_">doWork</span><span class="hljs-params">(Class&lt;T&gt; t)</span> &#123;<br>    <span class="hljs-keyword">try</span> &#123;<br>        <span class="hljs-comment">// do work</span><br>    &#125; <span class="hljs-keyword">catch</span> (T e) &#123; <span class="hljs-comment">// ERROR--can&#x27;t catch type variable</span><br>        Logger.getGlobal().info(..);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>不过，在异常规范中使用类型变量是允许的。以下方法是合法的：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> &lt;T <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Throwable</span>&gt; <span class="hljs-keyword">void</span> <span class="hljs-title function_">doWork</span><span class="hljs-params">(T t)</span> <span class="hljs-keyword">throws</span> T &#123; <span class="hljs-comment">// OK</span><br>    <span class="hljs-keyword">try</span> &#123;<br>        <span class="hljs-comment">// do work</span><br>    &#125; <span class="hljs-keyword">catch</span> (Throwable realCause) &#123;<br>        t.initCause(realCause);<br>        <span class="hljs-keyword">throw</span> t;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><h4 id="8-6-9-可以取消对检查型异常的检查"><a href="#8-6-9-可以取消对检查型异常的检查" class="headerlink" title="8.6.9 可以取消对检查型异常的检查"></a>8.6.9 可以取消对检查型异常的检查</h4><p>Java 异常处理的一个基本原则是，必须为所有检查型异常提供一个处理器。不过可以利用泛型取消这个机制。关键在于以下方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-meta">@SuppressWarnings(&quot;unchecked&quot;)</span><br><span class="hljs-keyword">static</span> &lt;T <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Throwable</span>&gt; <span class="hljs-keyword">void</span> <span class="hljs-title function_">throwAs</span><span class="hljs-params">(Throwable t)</span> <span class="hljs-keyword">throws</span> T &#123;<br>    <span class="hljs-keyword">throw</span> (T) t;<br>&#125;<br></code></pre></td></tr></table></figure><p>假设这个方法包含在接口 <code>Task</code> 中，如果有一个检查型异常 <code>e</code>，并调用</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">Task.&lt;RuntimeException&gt;throwAs(e);<br></code></pre></td></tr></table></figure><p>编译器就会认为 <code>e</code> 是一个非检查型异常。以下代码会把所有异常都转换为编译器所认为的非检查型异常：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">try</span> &#123;<br>    <span class="hljs-keyword">do</span> work<br>&#125; <span class="hljs-keyword">catch</span> (Throwable t) &#123;<br>    Task.&lt;RuntimeException&gt;throwAs(t);<br>&#125;<br></code></pre></td></tr></table></figure><p>下面使用这个技术解决一个棘手的问题。要在一个线程中运行代码，需要把代码放在一个实现了 <code>Runnable</code> 接口的类的 <code>run</code> 方法中。不过这个方法不允许抛出检查型异常。我们将提供一个从 <code>Task</code> 到 <code>Runnable</code> 的适配器，它的 <code>run</code> 方法可以抛出任意的异常。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">interface</span> <span class="hljs-title class_">Task</span> &#123;<br>    <span class="hljs-keyword">void</span> <span class="hljs-title function_">run</span><span class="hljs-params">()</span> <span class="hljs-keyword">throws</span> Exception;<br><br>    <span class="hljs-meta">@SuppressWarnings(&quot;unchecked&quot;)</span><br>    <span class="hljs-keyword">static</span> &lt;T <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Throwable</span>&gt; <span class="hljs-keyword">void</span> <span class="hljs-title function_">throwAs</span><span class="hljs-params">(Throwable t)</span> <span class="hljs-keyword">throws</span> T &#123;<br>        <span class="hljs-keyword">throw</span> (T) t;<br>    &#125;<br><br>    <span class="hljs-keyword">static</span> Runnable <span class="hljs-title function_">asRunnable</span><span class="hljs-params">(Task task)</span> &#123;<br>        <span class="hljs-keyword">return</span> () -&gt; &#123;<br>            <span class="hljs-keyword">try</span> &#123;<br>                task.run();<br>            &#125; <span class="hljs-keyword">catch</span> (Exception e) &#123;<br>                Task.&lt;RuntimeException&gt;throwAs(e);<br>            &#125;<br>        &#125;;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>例如，以下程序运行了一个线程，它会抛出一个检查型异常。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Test</span> &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> &#123;<br>        <span class="hljs-type">var</span> <span class="hljs-variable">thread</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Thread</span>(Task.asRunnable(() -&gt; &#123;<br>            Thread.sleep(<span class="hljs-number">1000</span>);<br>            System.out.println(<span class="hljs-string">&quot;Hello, World!&quot;</span>);<br>            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Exception</span>(<span class="hljs-string">&quot;Check this out!&quot;</span>);<br>        &#125;));<br>        thread.start();<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p><code>Thread.sleep</code> 方法声明为抛出一个 <code>InterruptedException</code>，我们不再需要捕获这个异常。因为我们没有中断这个线程，所以不会抛出这个异常。不过，程序会抛出一个检查型异常。运行程序时，你会得到一个栈轨迹。</p><p>这有什么意义呢？正常情况下，你必须捕获一个 <code>Runnable</code> 的 <code>run</code> 方法中的所有检查型异常，把它们“包装”到非检查型异常中，因为 <code>run</code> 方法声明为不抛出任何检查型异常。</p><p>不过在这里并没有做这种“包装”。我们只是抛出异常，并“哄骗”编译器，让它相信这不是一个检查型异常。</p><p>通过使用泛型类、擦除和 <code>@SuppressWarnings</code> 注解，我们就能消除 Java 类型系统的一个基本限制。</p><h4 id="8-6-10-注意擦除后的冲突"><a href="#8-6-10-注意擦除后的冲突" class="headerlink" title="8.6.10 注意擦除后的冲突"></a>8.6.10 注意擦除后的冲突</h4><p>擦除泛型类型后，不允许创建引发冲突的条件。下面来看一个示例。假定为 <code>Pair</code> 类增加一个 <code>equals</code> 方法，如下所示：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Pair</span>&lt;T&gt; &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-type">boolean</span> <span class="hljs-title function_">equals</span><span class="hljs-params">(T value)</span> &#123;<br>        <span class="hljs-keyword">return</span> first.equals(value) &amp;&amp; second.equals(value);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>考虑一个 <code>Pair&lt;String&gt;</code>。从概念上讲，它有两个 <code>equals</code> 方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">boolean</span> <span class="hljs-title function_">equals</span><span class="hljs-params">(String)</span>   <span class="hljs-comment">// defined in Pair&lt;T&gt;</span><br><span class="hljs-type">boolean</span> <span class="hljs-title function_">equals</span><span class="hljs-params">(Object)</span>   <span class="hljs-comment">// inherited from Object</span><br></code></pre></td></tr></table></figure><p>但是，直觉把我们引入歧途。方法</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">boolean</span> <span class="hljs-title function_">equals</span><span class="hljs-params">(T)</span><br></code></pre></td></tr></table></figure><p>擦除后就是</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">boolean</span> <span class="hljs-title function_">equals</span><span class="hljs-params">(Object)</span><br></code></pre></td></tr></table></figure><p>这会与 <code>Object.equals</code> 方法发生冲突。</p><p>当然，补救的办法是重新命名引发冲突的方法。</p><p>泛型规范还指出了另外一个规则：“为了支持擦除转换，我们要施加一个限制：倘若两个接口类型是同一接口的不同参数化，一个类或类型变量就不能同时作为这两个接口类型的子类。”例如，下面的代码是非法的：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Employee</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">Comparable</span>&lt;Employee&gt; &#123; ... &#125;<br><span class="hljs-keyword">class</span> <span class="hljs-title class_">Manager</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Employee</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">Comparable</span>&lt;Manager&gt; &#123; ... &#125;  <span class="hljs-comment">// ERROR</span><br></code></pre></td></tr></table></figure><p>如果以上代码可行，<code>Manager</code> 就会实现 <code>Comparable&lt;Employee&gt;</code> 和 <code>Comparable&lt;Manager&gt;</code>，而它们是同一接口的不同参数化。</p><p>这一限制与类型擦除的关系并不十分明显。毕竟，以下非泛型版本是合法的：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Employee</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">Comparable</span> &#123; ... &#125;<br><span class="hljs-keyword">class</span> <span class="hljs-title class_">Manager</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Employee</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">Comparable</span> &#123; ... &#125;<br></code></pre></td></tr></table></figure><p>其原因非常微妙，这有可能与合成的桥方法产生冲突。实现了 <code>Comparable&lt;X&gt;</code> 的类会获得一个桥方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-type">int</span> <span class="hljs-title function_">compareTo</span><span class="hljs-params">(Object other)</span> &#123; <span class="hljs-keyword">return</span> compareTo((X) other); &#125;<br></code></pre></td></tr></table></figure><p>不可能对不同的类型 <code>X</code> 有两个这样的方法。</p><h3 id="8-7-泛型类型的继承规则"><a href="#8-7-泛型类型的继承规则" class="headerlink" title="8.7 泛型类型的继承规则"></a>8.7 泛型类型的继承规则</h3><p>考虑一个类和一个子类，如 <code>Employee</code> 和 <code>Manager</code>。<code>Pair&lt;Manager&gt;</code> 是 <code>Pair&lt;Employee&gt;</code> 的一个子类型吗？或许人们会感到奇怪，答案是“不是”。例如，下面的代码将不能成功编译：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">Pair&lt;Employee&gt; buddies = <span class="hljs-keyword">new</span> <span class="hljs-title class_">Pair</span>&lt;Manager&gt;(ceo, cfo); <span class="hljs-comment">// illegal</span><br></code></pre></td></tr></table></figure><p>一般来讲，无论 <code>S</code> 与 <code>T</code> 有什么关系，<code>Pair&lt;S&gt;</code> 与 <code>Pair&lt;T&gt;</code> 都没有任何关系（如图 8-1 所示）。</p><p><img src="/2025/05/04/hello-world/%E7%AC%AC%E5%85%AB%E7%AB%A0/image-20251129174917794.png" alt="image-20251129174917794"></p><p>这看起来是一个很严格的限制，不过对于类型安全非常必要。假设允许将 <code>Pair&lt;Manager&gt;</code> 转换为 <code>Pair&lt;Employee&gt;</code>。考虑下面的代码：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">var</span> <span class="hljs-variable">managerBuddies</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Pair</span>&lt;Manager&gt;(ceo, cfo);<br>Pair&lt;Employee&gt; employeeBuddies = managerBuddies; <span class="hljs-comment">// illegal, but suppose it wasn&#x27;t</span><br>employeeBuddies.setFirst(lowlyEmployee);<br></code></pre></td></tr></table></figure><p>显然，最后一句是合法的。但是 <code>employeeBuddies</code> 和 <code>managerBuddies</code> 引用了同样的对象。现在我们会把 CFO 和一个底层员工组成一对，这对于 <code>Pair&lt;Manager&gt;</code> 来说应该是不可能的。</p><blockquote><p><strong>注释</strong>：可以将一个 <code>Manager[]</code> 数组赋给一个类型为 <code>Employee[]</code> 的变量：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java">Manager[] managerBuddies = &#123;ceo, cfo&#125;;<br>Employee[] employeeBuddies = managerBuddies; <span class="hljs-comment">// OK</span><br></code></pre></td></tr></table></figure><p>不过，数组有特别的保护。如果试图将一个底层员工存储到 <code>employeeBuddies[0]</code>，虚拟机将会抛出 <code>ArrayStoreException</code> 异常。</p></blockquote><p>总是可以将参数化类型转换为一个原始类型。例如，<code>Pair&lt;Employee&gt;</code> 是原始类型 <code>Pair</code> 的一个子类型。在与遗留代码交互时，这个转换非常必要。</p><p>转换成原始类型会导致类型错误吗？很遗憾，会！看一看下面这个示例：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">var</span> <span class="hljs-variable">managerBuddies</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Pair</span>&lt;Manager&gt;(ceo, cfo);<br><span class="hljs-type">Pair</span> <span class="hljs-variable">rawBuddies</span> <span class="hljs-operator">=</span> managerBuddies; <span class="hljs-comment">// OK</span><br>rawBuddies.setFirst(<span class="hljs-keyword">new</span> <span class="hljs-title class_">File</span>(<span class="hljs-string">&quot;...&quot;</span>)); <span class="hljs-comment">// only a compile-time warning</span><br></code></pre></td></tr></table></figure><p>当使用 <code>getFirst</code> 获得外来对象并赋给 <code>Manager</code> 变量时，与以前一样，会抛出 <code>ClassCastException</code> 异常。这里失去的只是泛型程序设计提供的附加安全性。</p><p>最后，泛型类可以扩展或实现其他的泛型类。就这一点而言，它们与普通的类没有什么区别。例如，<code>ArrayList&lt;T&gt;</code> 类实现了 <code>List&lt;T&gt;</code> 接口。这意味着，一个 <code>ArrayList&lt;Manager&gt;</code> 可以转换为一个 <code>List&lt;Manager&gt;</code>。但是，如前面所见，<code>ArrayList&lt;Manager&gt;</code> 不是一个 <code>ArrayList&lt;Employee&gt;</code> 或 <code>List&lt;Employee&gt;</code>。图 8-2 展示了它们之间的这种关系。</p><p><img src="/2025/05/04/hello-world/%E7%AC%AC%E5%85%AB%E7%AB%A0/image-20251129175537170.png" alt="image-20251129175537170"></p><h3 id="8-8-通配符类型"><a href="#8-8-通配符类型" class="headerlink" title="8.8 通配符类型"></a>8.8 通配符类型</h3><p>Java 的设计者发明了一种巧妙的（但很安全的）“逃生出口”：<strong>通配符类型（wildcard type）</strong>。下面几小节会介绍如何使用通配符。</p><h4 id="8-8-1-通配符概念"><a href="#8-8-1-通配符概念" class="headerlink" title="8.8.1 通配符概念"></a>8.8.1 通配符概念</h4><p>**在通配符类型中，允许类型参数变化。**例如，通配符类型</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">Pair&lt;? <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Employee</span>&gt;<br></code></pre></td></tr></table></figure><p>表示任何泛型 <code>Pair</code> 类型，它的类型参数是 <code>Employee</code> 的子类，如 <code>Pair&lt;Manager&gt;</code>，但不能是 <code>Pair&lt;String&gt;</code>。</p><p>假设要编写一个打印员工对的方法，如下所示：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">printBuddies</span><span class="hljs-params">(Pair&lt;Employee&gt; p)</span> &#123;<br>    <span class="hljs-type">Employee</span> <span class="hljs-variable">first</span>  <span class="hljs-operator">=</span> p.getFirst();<br>    <span class="hljs-type">Employee</span> <span class="hljs-variable">second</span> <span class="hljs-operator">=</span> p.getSecond();<br>    System.out.println(first.getName() + <span class="hljs-string">&quot; and &quot;</span> + second.getName() + <span class="hljs-string">&quot; are buddies.&quot;</span>);<br>&#125;<br></code></pre></td></tr></table></figure><p>正如前面讲到的，不能将 <code>Pair&lt;Manager&gt;</code> 传递给这个方法，这一点很有限制。不过解决的方法很简单——可以使用一个通配符类型：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">printBuddies</span><span class="hljs-params">(Pair&lt;? extends Employee&gt; p)</span><br></code></pre></td></tr></table></figure><p>类型 <code>Pair&lt;Manager&gt;</code> 是 <code>Pair&lt;? extends Employee&gt;</code> 的子类型（如图 8-3 所示）。</p><p><img src="/2025/05/04/hello-world/%E7%AC%AC%E5%85%AB%E7%AB%A0/image-20251129175758988.png" alt="image-20251129175758988"></p><p>使用通配符会通过 <code>Pair&lt;? extends Employee&gt;</code> 的引用破坏 <code>Pair&lt;Manager&gt;</code> 吗？</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">var</span> <span class="hljs-variable">managerBuddies</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Pair</span>&lt;Manager&gt;(ceo, cfo);<br>Pair&lt;? <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Employee</span>&gt; wildcardBuddies = managerBuddies; <span class="hljs-comment">// OK</span><br>wildcardBuddies.setFirst(lowlyEmployee); <span class="hljs-comment">// compile-time error</span><br></code></pre></td></tr></table></figure><p>这不可能引起破坏。对 <code>setFirst</code> 的调用有一个类型错误。要了解其中的缘由，请仔细看一看类型 <code>Pair&lt;? extends Employee&gt;</code>。它的方法如下：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java">? <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Employee</span> getFirst()<br><span class="hljs-keyword">void</span> <span class="hljs-title function_">setFirst</span><span class="hljs-params">(? extends Employee)</span><br></code></pre></td></tr></table></figure><p>不可能调用 <code>setFirst</code> 方法。考虑调用 <code>wildcardBuddies.setFirst(lowlyEmployee)</code>，编译器知道 <code>setFirst</code> 的参数有某个特定的类型，这个类型扩展了 <code>Employee</code>。这个特定类型是 <code>Employee</code> 吗？是 <code>Manager</code> 吗？还是另外某个子类？编译器无法知道。因此，编译器不能接受 <code>lowlyEmployee</code>。出于同样的原因，调用 <code>wildcardBuddies.setFirst(cio)</code>（其中 <code>cio</code> 是一个 <code>Manager</code> 实例）也会出错。除了 <code>null</code>，编译器必须拒绝传入 <code>setFirst</code> 的所有参数。</p><p><code>getFirst</code> 方法则可以继续工作。<code>getFirst</code> 的返回值是某个特定类型的实例，这是 <code>Employee</code> 的一个子类型。编译器不知道这个特定类型是什么，但它可以保证对 <code>Employee</code> 引用的赋值是安全的。</p><p>这就是引入有限定的通配符的关键之处。现在我们已经有办法区分安全的访问器方法和不安全的更改器方法了。</p><h4 id="8-8-2-通配符的超类型限定"><a href="#8-8-2-通配符的超类型限定" class="headerlink" title="8.8.2 通配符的超类型限定"></a>8.8.2 通配符的超类型限定</h4><p>通配符限定与类型变量限定十分类似，你可以指定一个<strong>超类型限定（supertype bound）</strong>，如下所示：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">? <span class="hljs-built_in">super</span> Manager<br></code></pre></td></tr></table></figure><p>这个通配符限制为 <code>Manager</code> 的所有超类型。</p><p>为什么想要这样做呢？带有超类型限定的通配符会提供一种行为。可以为方法提供参数，但不能使用返回值。例如，<code>Pair&lt;? super Manager&gt;</code> 有一些方法可以描述如下：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">void</span> <span class="hljs-title function_">setFirst</span><span class="hljs-params">(? <span class="hljs-built_in">super</span> Manager)</span><br>? <span class="hljs-built_in">super</span> Manager <span class="hljs-title function_">getFirst</span><span class="hljs-params">()</span><br></code></pre></td></tr></table></figure><p>这不是真正的 Java 语法，但是可以展示编译器知道什么。<code>setFirst</code> 的参数类型表示为 <code>? super Manager</code>，这是某个特定类型 <code>T</code>，而 <code>Manager</code> 是 <code>T</code> 的一个子类型。对于 <code>T</code> 实际上有 3 种选择：<code>Object</code>、<code>Employee</code> 或 <code>Manager</code>。（如果 <code>Manager</code> 或 <code>Employee</code> 实现了接口，可能还有更多选择）。不过，编译器无法知道其中哪个选择正确。所以，编译器不能接受参数类型为 <code>Employee</code> 或 <code>Object</code> 的调用。毕竟，<code>T</code> 可能是 <code>Manager</code>。只能传递 <code>Manager</code> 类型或某个子类型（如 <code>Executive</code>）的对象。</p><p>另外，如果调用 <code>getFirst</code>，不能保证返回对象的类型。只能把它赋给一个 <code>Object</code>。</p><p>下面是一个典型的示例。我们有一个经理数组，并且想把奖金最高和最低的经理放在一个 <code>Pair</code> 对象中。<code>Pair</code> 的类型是什么？在这里，<code>Pair&lt;Employee&gt;</code> 是合理的，或者对此而言，</p><p><code>Pair&lt;Object&gt;</code> 也是合理的（如图 8-4 所示）。下面的方法将接受任何合适的 <code>Pair</code>：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">minmaxBonus</span><span class="hljs-params">(Manager[] a, Pair&lt;? <span class="hljs-built_in">super</span> Manager&gt; result)</span> &#123;<br>    <span class="hljs-keyword">if</span> (a.length == <span class="hljs-number">0</span>) <span class="hljs-keyword">return</span>;<br>    <span class="hljs-type">Manager</span> <span class="hljs-variable">min</span> <span class="hljs-operator">=</span> a[<span class="hljs-number">0</span>];<br>    <span class="hljs-type">Manager</span> <span class="hljs-variable">max</span> <span class="hljs-operator">=</span> a[<span class="hljs-number">0</span>];<br>    <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">1</span>; i &lt; a.length; i++) &#123;<br>        <span class="hljs-keyword">if</span> (min.getBonus() &gt; a[i].getBonus()) min = a[i];<br>        <span class="hljs-keyword">if</span> (max.getBonus() &lt; a[i].getBonus()) max = a[i];<br>    &#125;<br>    result.setFirst(min);<br>    result.setSecond(max);<br>&#125;<br></code></pre></td></tr></table></figure><p><img src="/2025/05/04/hello-world/%E7%AC%AC%E5%85%AB%E7%AB%A0/image-20251129180046008.png" alt="image-20251129180046008"></p><p>直观地讲，带有超类型限定的通配符允许你写入一个泛型对象，而带有子类型限定的通配符允许你读取一个泛型对象。</p><p>下面是超类型限定的另一种应用。<code>Comparable</code> 接口本身就是一个泛型类型。声明如下：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">interface</span> <span class="hljs-title class_">Comparable</span>&lt;T&gt; &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-type">int</span> <span class="hljs-title function_">compareTo</span><span class="hljs-params">(T other)</span>;<br>&#125;<br></code></pre></td></tr></table></figure><p>在这里，类型变量指示了 <code>other</code> 参数的类型。例如，<code>String</code> 类实现了 <code>Comparable&lt;String&gt;</code>，它的 <code>compareTo</code> 方法声明为</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-type">int</span> <span class="hljs-title function_">compareTo</span><span class="hljs-params">(String other)</span><br></code></pre></td></tr></table></figure><p>这很好，显式参数有正确的类型。接口是泛型接口之前，<code>other</code> 是一个 <code>Object</code>，这个方法的实现中必须有一个强制类型转换。</p><p>由于 <code>Comparable</code> 是一个泛型类型，对于 <code>ArrayAlg</code> 类的 <code>minmax</code> 方法，也许我们还能做得更好一些？可以将它声明为：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> &lt;T <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Comparable</span>&lt;T&gt;&gt; Pair&lt;T&gt; <span class="hljs-title function_">minmax</span><span class="hljs-params">(T[] a)</span><br></code></pre></td></tr></table></figure><p>看起来，这样比只使用 <code>T extends Comparable</code> 更彻底，而且对于很多类都能很好地工作。例如，如果计算一个 <code>String</code> 数组的最小值，<code>T</code> 就是类型 <code>String</code>，而 <code>String</code> 是 <code>Comparable&lt;String&gt;</code> 的一个子类型。但是，处理一个 <code>LocalDate</code> 对象数组时，我们会遇到一个问题。<code>LocalDate</code> 实现了 <code>ChronoLocalDate</code>，而 <code>ChronoLocalDate</code> 扩展了 <code>Comparable&lt;ChronoLocalDate&gt;</code>。因此，<code>LocalDate</code> 实现的是 <code>Comparable&lt;ChronoLocalDate&gt;</code> 而不是 <code>Comparable&lt;LocalDate&gt;</code>。</p><p>在这种情况下，可以利用超类型来解决：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> &lt;T <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Comparable</span>&lt;? <span class="hljs-built_in">super</span> T&gt;&gt; Pair&lt;T&gt; <span class="hljs-title function_">minmax</span><span class="hljs-params">(T[] a)</span><br></code></pre></td></tr></table></figure><p>现在 <code>compareTo</code> 方法形式如下：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">int</span> <span class="hljs-title function_">compareTo</span><span class="hljs-params">(? <span class="hljs-built_in">super</span> T)</span><br></code></pre></td></tr></table></figure><p>它可以声明为接受类型 <code>T</code> 的对象，或者也可以是 <code>T</code> 的一个超类型的对象（例如，当 <code>T</code> 是 <code>LocalDate</code> 时）。无论如何，都可以安全地向 <code>compareTo</code> 方法传递一个 <code>T</code> 类型的对象。</p><p>对于初学者来说，类似 <code>&lt;T extends Comparable&lt;? super T&gt;&gt;</code> 的声明看起来有点吓人。很遗憾，因为这个声明的本意是帮助开发应用的程序员去除对调用参数的不必要的限制。对泛型没有兴趣的应用程序员可能很快就会略过这些声明，想当然地认为库程序员做的都是正确的。如果你是一名库程序员，一定要熟悉通配符，否则，就会受到用户的责备，他们要在代码中随机地添加强制类型转换直至代码能够编译。</p><blockquote><p><strong>注释</strong>：超类型限定的另一个常见的用法是作为一个函数式接口的参数类型。例如，<code>Collection</code> 接口有一个方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">default</span> <span class="hljs-type">boolean</span> <span class="hljs-title function_">removeIf</span><span class="hljs-params">(Predicate&lt;? <span class="hljs-built_in">super</span> E&gt; filter)</span><br></code></pre></td></tr></table></figure><p>这个方法会删除所有满足给定谓词条件的元素。例如，如果你不喜欢有奇怪散列码的员工，就可以如下将他们删除：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java">ArrayList&lt;Employee&gt; staff = ...;<br>Predicate&lt;Object&gt; oddHashCode = obj -&gt; obj.hashCode() % <span class="hljs-number">2</span> != <span class="hljs-number">0</span>;<br>staff.removeIf(oddHashCode);<br></code></pre></td></tr></table></figure><p>你希望能够传入一个 <code>Predicate&lt;Object&gt;</code>，而不只是 <code>Predicate&lt;Employee&gt;</code>。<code>super</code> 通配符可以使这个愿望成真。</p></blockquote><h4 id="8-8-3-无限定通配符"><a href="#8-8-3-无限定通配符" class="headerlink" title="8.8.3 无限定通配符"></a>8.8.3 无限定通配符</h4><p>甚至还可以使用根本无限定的通配符，例如，<code>Pair&lt;?&gt;</code>。初看起来，这好像与原始的 <code>Pair</code> 类型一样。实际上，这两种类型有很大的不同。类型 <code>Pair&lt;?&gt;</code> 有以下方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java">? getFirst()<br><span class="hljs-keyword">void</span> <span class="hljs-title function_">setFirst</span><span class="hljs-params">(?)</span><br></code></pre></td></tr></table></figure><p><code>getFirst</code> 的返回值只能赋给一个 <code>Object</code>。<code>setFirst</code> 方法不能调用，甚至不能用 <code>Object</code> 调用。</p><p><code>Pair&lt;?&gt;</code> 和 <code>Pair</code> 本质的不同在于：你可以用任意 <code>Object</code> 对象调用原始 <code>Pair</code> 类的 <code>setFirst</code> 方法。</p><blockquote><p><strong>注释</strong>：可以调用 <code>setFirst(null)</code>。</p></blockquote><p>为什么要使用这样一个脆弱的类型？它对于很多简单操作很有用。例如，下面这个方法可用来测试一个对组是否包含一个 <code>null</code> 引用，它不需要具体的类型。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-type">boolean</span> <span class="hljs-title function_">hasNulls</span><span class="hljs-params">(Pair&lt;?&gt; p)</span> &#123;<br>    <span class="hljs-keyword">return</span> p.getFirst() == <span class="hljs-literal">null</span> || p.getSecond() == <span class="hljs-literal">null</span>;<br>&#125;<br></code></pre></td></tr></table></figure><p>通过将 <code>hasNulls</code> 转换成泛型方法，可以避免使用通配符类型：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> &lt;T&gt; <span class="hljs-type">boolean</span> <span class="hljs-title function_">hasNulls</span><span class="hljs-params">(Pair&lt;T&gt; p)</span><br></code></pre></td></tr></table></figure><p>但是，带有通配符的版本可读性更好。</p><h4 id="8-8-4-通配符捕获"><a href="#8-8-4-通配符捕获" class="headerlink" title="8.8.4 通配符捕获"></a>8.8.4 通配符捕获</h4><p>下面编写一个方法来交换对组的元素：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">swap</span><span class="hljs-params">(Pair&lt;?&gt; p)</span><br></code></pre></td></tr></table></figure><p>**通配符不是类型变量，**因此，不能编写使用 <code>?</code> 作为一种类型的代码。也就是说，下面的代码是非法的：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java">? t = p.getFirst(); <span class="hljs-comment">// ERROR</span><br>p.setFirst(p.getSecond());<br>p.setSecond(t);<br></code></pre></td></tr></table></figure><p>这里有一个问题，因为在交换的时候，必须临时保存第一个元素。幸运的是，这个问题有一个有趣的解决方案。我们可以写一个辅助方法 <code>swapHelper</code>，如下所示：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> &lt;T&gt; <span class="hljs-keyword">void</span> <span class="hljs-title function_">swapHelper</span><span class="hljs-params">(Pair&lt;T&gt; p)</span> &#123;<br>    <span class="hljs-type">T</span> <span class="hljs-variable">t</span> <span class="hljs-operator">=</span> p.getFirst();<br>    p.setFirst(p.getSecond());<br>    p.setSecond(t);<br>&#125;<br></code></pre></td></tr></table></figure><p>注意，<code>swapHelper</code> 是一个泛型方法，而 <code>swap</code> 不是，它有一个固定的 <code>Pair&lt;?&gt;</code> 类型的参数。现在可以由 <code>swap</code> 调用 <code>swapHelper</code>：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">swap</span><span class="hljs-params">(Pair&lt;?&gt; p)</span> &#123;<br>    swapHelper(p);<br>&#125;<br></code></pre></td></tr></table></figure><p>在这种情况下，<code>swapHelper</code> 方法的参数 <code>T</code> 捕获通配符。并不知道通配符指示哪种类型，但是，这是一个明确的类型，并且从 <code>&lt;T&gt; swapHelper</code> 的定义可以清楚地看到 <code>T</code> 指示那个类型。</p><p>当然，在这种情况下，并不是一定要使用通配符。我们也可以直接把 <code>&lt;T&gt; void swap(Pair&lt;T&gt; p)</code> 实现为一个没有通配符的泛型方法。不过，考虑下面这个例子，这里通配符类型很自然地出现在一个计算中间：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">maxminBonus</span><span class="hljs-params">(Manager[] a, Pair&lt;? <span class="hljs-built_in">super</span> Manager&gt; result)</span> &#123;<br>    minmaxBonus(a, result);<br>    PairAlg.swapHelper(result); <span class="hljs-comment">// OK--swapHelper captures wildcard type</span><br>&#125;<br></code></pre></td></tr></table></figure><p>在这里，通配符捕获机制是不可避免的。</p><p>通配符捕获只有在非常有限的情况下是合法的。编译器必须能够保证通配符表示单个确定的类型。例如，<code>ArrayList&lt;Pair&lt;T&gt;&gt;</code> 中的 <code>T</code> 绝对不能捕获 <code>ArrayList&lt;Pair&lt;?&gt;&gt;</code> 中的通配符。数组列表可能包含两个 <code>Pair&lt;?&gt;</code>，其中的 <code>?</code> 可能分别有不同的类型。</p><h3 id="8-9-反射和泛型"><a href="#8-9-反射和泛型" class="headerlink" title="8.9 反射和泛型"></a>8.9 反射和泛型</h3><p>反射允许你在运行时分析任意对象。如果对象是泛型类的实例，关于泛型类型参数，你可能得不到多少信息，因为它们已经被擦除了。在下面的小节中，我们将学习利用反射可以获得泛型类的哪些信息。</p><h4 id="8-9-1-泛型-Class-类"><a href="#8-9-1-泛型-Class-类" class="headerlink" title="8.9.1 泛型 Class 类"></a>8.9.1 泛型 Class 类</h4><p>现在，<code>Class</code> 类是泛型类。例如，<code>String.class</code> 实际上是一个 <code>Class&lt;String&gt;</code> 类的对象（事实上，也是唯一的对象）。</p><p>类型参数十分有用，这是因为它允许 <code>Class&lt;T&gt;</code> 的方法有更特定的返回类型。<code>Class&lt;T&gt;</code> 的以下方法就利用了类型参数：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs java">T <span class="hljs-title function_">newInstance</span><span class="hljs-params">()</span><br>T <span class="hljs-title function_">cast</span><span class="hljs-params">(Object obj)</span><br>T[] getEnumConstants()<br>Class&lt;? <span class="hljs-built_in">super</span> T&gt; getSuperclass()<br>Constructor&lt;T&gt; <span class="hljs-title function_">getConstructor</span><span class="hljs-params">(Class&lt;?&gt;... parameterTypes)</span><br>Constructor&lt;T&gt; <span class="hljs-title function_">getDeclaredConstructor</span><span class="hljs-params">(Class&lt;?&gt;... parameterTypes)</span><br></code></pre></td></tr></table></figure><p><code>newInstance</code> 方法返回这个类的一个实例，由无参数构造器获得。它的返回类型现在声明为 <code>T</code>，其类型与 <code>Class&lt;T&gt;</code> 描述的类相同，这样就免除了强制类型转换。</p><p><code>cast</code> 方法返回给定的对象，如果给定对象的类型实际上是 <code>T</code> 的一个子类型，现在会声明为类型 <code>T</code>，否则，会抛出一个 <code>ClassCastException</code> 异常。</p><p>如果这个类不是一个 enum 类或 <code>T</code> 类型枚举值的一个数组，<code>getEnumConstants</code> 方法将返回 <code>null</code>。</p><p>最后，<code>getConstructor</code> 与 <code>getDeclaredConstructor</code> 方法返回一个 <code>Constructor&lt;T&gt;</code> 对象。<code>Constructor</code> 类也已经变成泛型，使得它的 <code>newInstance</code> 方法有一个正确的返回类型。</p><h4 id="8-9-2-使用-Class-参数进行类型匹配"><a href="#8-9-2-使用-Class-参数进行类型匹配" class="headerlink" title="8.9.2 使用 Class&lt;T&gt; 参数进行类型匹配"></a>8.9.2 使用 <code>Class&lt;T&gt;</code> 参数进行类型匹配</h4><p>匹配泛型方法中 <code>Class&lt;T&gt;</code> 参数的类型变量有时会很有用。下面是一个标准的示例：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> &lt;T&gt; Pair&lt;T&gt; <span class="hljs-title function_">makePair</span><span class="hljs-params">(Class&lt;T&gt; c)</span> <br>        <span class="hljs-keyword">throws</span> InstantiationException, IllegalAccessException<br>&#123;<br>    <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Pair</span>&lt;&gt;(c.newInstance(), c.newInstance());<br>&#125;<br></code></pre></td></tr></table></figure><p>如果调用</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">makePair(Employee.class)<br></code></pre></td></tr></table></figure><p><code>Employee.class</code> 是一个 <code>Class&lt;Employee&gt;</code> 类型的对象。<code>makePair</code> 方法的类型参数 <code>T</code> 与 <code>Employee</code> 匹配，编译器可以推断出这个方法将返回一个 <code>Pair&lt;Employee&gt;</code>。</p><h4 id="8-9-3-虚拟机中的泛型类型信息"><a href="#8-9-3-虚拟机中的泛型类型信息" class="headerlink" title="8.9.3 虚拟机中的泛型类型信息"></a>8.9.3 虚拟机中的泛型类型信息</h4><p>Java 泛型的突出特性之一是在虚拟机中擦除泛型类型。令人奇怪的是，擦除的类仍然保留原先泛型的一些微弱记忆。例如，原始 <code>Pair</code> 类知道它源于泛型类 <code>Pair&lt;T&gt;</code>，尽管一个 <code>Pair</code> 类型的对象无法区分它构造为 <code>Pair&lt;String&gt;</code> 还是 <code>Pair&lt;Employee&gt;</code>。</p><p>类似地，考虑以下方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> Comparable <span class="hljs-title function_">min</span><span class="hljs-params">(Comparable[] a)</span><br></code></pre></td></tr></table></figure><p>这是擦除以下泛型方法得到的：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> &lt;T <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Comparable</span>&lt;? <span class="hljs-built_in">super</span> T&gt;&gt; T <span class="hljs-title function_">min</span><span class="hljs-params">(T[] a)</span><br></code></pre></td></tr></table></figure><p>可以使用反射 API 确定：</p><ul><li>这个泛型方法有一个名为 <code>T</code> 的类型参数。  </li><li>这个类型参数有一个子类型限定，其自身又是一个泛型类型。  </li><li>这个限定类型有一个通配符参数。  </li><li>这个通配符参数有一个超类型限定。  </li><li>这个泛型方法有一个泛型数组参数。</li></ul><p>换句话说，你可以重新构造实现者声明的泛型类和方法的所有有关内容。但是，你不会知道对于特定的对象或方法调用会如何解析类型参数。</p><p>为了描述泛型类型声明，可以使用 <code>java.lang.reflect</code> 包中的接口 <code>Type</code>。这个接口有以下子类型：</p><ul><li><code>Class</code> 类，描述具体类型。  </li><li><code>TypeVariable</code> 接口，描述类型变量（如 <code>T extends Comparable&lt;? super T&gt;</code>）。  </li><li><code>WildcardType</code> 接口，描述通配符（如 <code>? super T</code>）。  </li><li><code>ParameterizedType</code> 接口，描述泛型类或接口类型（如 <code>Comparable&lt;? super T&gt;</code>）。  </li><li><code>GenericArrayType</code> 接口，描述泛型数组（如 <code>T[]</code>）。</li></ul><p>图 8-5 给出了继承层次结构。注意，最后 4 个子类型是接口，虚拟机会实例化实现这些接口的适当的类。</p><p><img src="/2025/05/04/hello-world/%E7%AC%AC%E5%85%AB%E7%AB%A0/image-20251130163141842.png" alt="image-20251130163141842"></p><h4 id="8-9-4-类型字面量"><a href="#8-9-4-类型字面量" class="headerlink" title="8.9.4 类型字面量"></a>8.9.4 类型字面量</h4><p>有时，你会希望由值的类型决定程序的行为。例如，在一种持久存储机制中，你可能希望用户指定一种方法来保存某个特定类的对象。通常的实现方法是将 <code>Class</code> 对象与一个动作关联。不过，如果有泛型类，擦除会带来问题。比如说，既然 <code>ArrayList&lt;Integer&gt;</code> 和 <code>ArrayList&lt;String&gt;</code> 都擦除为同一个原始类型 <code>ArrayList</code>，如何让它们有不同的动作呢？</p><p>这里有一个技巧，在某些情况下可以解决这个问题。可以捕获 <code>Type</code> 接口（上一节介绍过）的一个实例。然后构造一个匿名子类，如下所示：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">var</span> <span class="hljs-variable">type</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">TypeLiteral</span>&lt;ArrayList&lt;Integer&gt;&gt;() &#123; &#125;; <span class="hljs-comment">// note the &#123; &#125;</span><br></code></pre></td></tr></table></figure><p><code>TypeLiteral</code> 构造器会捕获泛型超类型：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">class</span> <span class="hljs-title class_">TypeLiteral</span> &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-title function_">TypeLiteral</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-type">Type</span> <span class="hljs-variable">parentType</span> <span class="hljs-operator">=</span> getClass().getGenericSuperclass();<br>        <span class="hljs-keyword">if</span> (parentType <span class="hljs-keyword">instanceof</span> ParameterizedType paramType)<br>            type = paramType.getActualTypeArguments()[<span class="hljs-number">0</span>];<br>        <span class="hljs-keyword">else</span><br>            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">UnsupportedOperationException</span>(<br>                <span class="hljs-string">&quot;Construct as new TypeLiteral&lt;..&gt;()&#123;&#125;&quot;</span>);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>如果运行时有一个泛型类型，可以将它与 <code>TypeLiteral</code> 匹配。我们无法从一个对象得到泛型类型（已经被擦除）。不过，正如上一节看到的，字段和方法参数的泛型类型还留存在虚拟机中。</p><p>CDI 和 Guice 等注入框架（Injection framework）就使用类型字面量来控制泛型类型的注入。程序清单 8-5 给出了一个更简单的例子。给定一个对象，我们可以罗列它的字段，哪些有泛型类型，并查找相关联的格式化动作。</p>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>《Java核心技术卷一》第七章</title>
    <link href="/2025/11/18/%E3%80%8AJava%E6%A0%B8%E5%BF%83%E6%8A%80%E6%9C%AF%E5%8D%B7%E4%B8%80%E3%80%8B%E7%AC%AC%E4%B8%83%E7%AB%A0/"/>
    <url>/2025/11/18/%E3%80%8AJava%E6%A0%B8%E5%BF%83%E6%8A%80%E6%9C%AF%E5%8D%B7%E4%B8%80%E3%80%8B%E7%AC%AC%E4%B8%83%E7%AB%A0/</url>
    
    <content type="html"><![CDATA[<h2 id="第-7-章-异常、断言和日志"><a href="#第-7-章-异常、断言和日志" class="headerlink" title="第 7 章 异常、断言和日志"></a>第 7 章 异常、断言和日志</h2><p>如果由于程序的错误或一些外部环境的影响，导致用户在运行程序期间做的所有工作统统丢失，这个用户有可能永远不会再使用这个程序了。为了尽量避免这类事情的发生，至少应该做到以下几点：</p><ul><li>向用户通知错误；  </li><li>保存所有工作；  </li><li>允许用户妥善地退出程序。</li></ul><p>对于异常情况，例如，可能造成程序崩溃的糟糕的输入数据，Java 使用了一种称为**异常处理（exception handling）**的错误捕获机制。</p><h3 id="7-1-处理错误"><a href="#7-1-处理错误" class="headerlink" title="7.1 处理错误"></a>7.1 处理错误</h3><p>**异常处理的任务就是将控制权从产生错误的地方转移到能够处理这种情况的一个错误处理器。**为了在程序中处理异常情况，必须考虑程序中可能出现的错误和问题。那么需要考虑哪些问题呢？</p><ul><li>用户输入错误。除了那些不可避免的键盘输入错误外，有些用户不遵守程序的要求。</li><li>设备错误。设备经常在完成任务的过程中出问题。</li><li>物理限制。磁盘已满，你可能已经用尽了所有可用内存。  </li><li>代码错误。方法有可能没有正确地完成工作。</li></ul><p>对于方法中的错误，<strong>传统的处理方法是返回一个特殊的错误码</strong>，由调用方法分析。例如，对于从文件中读取信息的方法，通常返回一个特殊值 -1（而不是一个标准字符）表示文件结束。还有一个表示错误状况的常用返回值是 <code>null</code> 引用。</p><p>但并不是任何情况下都能够返回一个错误码。有可能无法明确地区分合法数据与非法数据。一个返回整型的方法就不能简单地返回 -1 表示错误，因为 -1 很可能是一个完全合法的结果。</p><p>Java 允许每个方法有一个候选的退出路径，如果这个方法不能以正常的方式完成它的任务，就会选择这个退出路径。在这种情况下，方法不会返回一个值，而是**抛出（throw）**一个封装了错误信息的对象。<strong>需要注意的是，这个方法会立刻退出，并不返回正常值（或任何值）。<strong>此外，也不会从调用这个方法的代码继续执行，取而代之的是，异常处理机制开始搜索一个能够处理这种异常状况的</strong>异常处理器（exception handler）</strong>。</p><h4 id="7-1-1-异常分类"><a href="#7-1-1-异常分类" class="headerlink" title="7.1.1 异常分类"></a>7.1.1 异常分类</h4><p>在 Java 程序设计语言中，异常对象都是派生于 <code>Throwable</code> 类的一个类的实例。如果 Java 中内置的异常类不能满足需求，用户还可以创建自己的异常类。</p><p><img src="/2025/05/04/hello-world/%E7%AC%AC%E4%B8%83%E7%AB%A0/image-20251118222425666.png" alt="image-20251118222425666"></p><p>​<strong>图 7-1 是 Java 异常层次结构的一个简化示意图。</strong></p><p>所有的异常都是由 <code>Throwable</code> 继承而来，但在这个层次结构中，下一层立即分为两个分支：<code>Error</code> 和 <code>Exception</code>。</p><p><strong><code>Error</code> 类层次结构描述了 Java 运行时系统的内部错误和资源耗尽问题。</strong></p><p>编写 Java 程序时，要重点关注 <code>Exception</code> 层次结构。这个 <code>Exception</code> 层次结构又分为两个分支：一个分支派生于 <code>RuntimeException</code>；另一个分支包括其他异常，不继承这个类。一般规则是：<strong>由编程错误导致的异常属于 <code>RuntimeException</code>；如果程序本身没有问题，但由于 I&#x2F;O 错误之类的问题导致的异常属于其他异常。</strong></p><p>继承自 <code>RuntimeException</code> 的异常包括以下问题：</p><ul><li>错误的强制类型转换。  </li><li>越界的数组访问。  </li><li>访问 <code>null</code> 指针。</li></ul><p>不继承自 <code>RuntimeException</code> 的异常包括：</p><ul><li>试图越过文件末尾继续读取数据。  </li><li>试图打开一个不存在的文件。  </li><li>试图根据给定的字符串查找 <code>Class</code> 对象，而这个字符串表示的类并不存在。</li></ul><p>“如果出现 <code>RuntimeException</code> 异常，那么一定是你的问题”。</p><p><strong>Java 语言规范将派生于 <code>Error</code> 类或 <code>RuntimeException</code> 类的所有异常称为非检查型（unchecked）异常，所有其他异常称为检查型（checked）异常。</strong>。编译器将检查你是否为所有的检查型异常提供了异常处理器。</p><h4 id="7-1-2-声明检查型异常"><a href="#7-1-2-声明检查型异常" class="headerlink" title="7.1.2 声明检查型异常"></a>7.1.2 声明检查型异常</h4><p>如果遇到了无法处理的情况，Java 方法可以抛出一个异常。方法不仅需要告诉编译器将要返回什么值，<strong>还要告诉编译器有可能发生什么错误</strong>。</p><p>要在方法的首部指出这个方法可能抛出一个异常，要修改方法首部以反映这个方法可能抛出的检查型异常。例如，下面是标准类库中 <code>FileInputStream</code> 类的一个构造器的声明。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-title function_">FileInputStream</span><span class="hljs-params">(String name)</span> <span class="hljs-keyword">throws</span> FileNotFoundException<br></code></pre></td></tr></table></figure><p>这个声明表示这个构造器将根据给定的 <code>String</code> 参数生成一个 <code>FileInputStream</code> 对象，但也有可能出错而抛出一个 <code>FileNotFoundException</code> 异常。如果真的发生了这种情况，构造器将不会初始化一个新的 <code>FileInputStream</code> 对象，而是抛出一个 <code>FileNotFoundException</code> 类对象。如果这个方法真的抛出了这样一个异常对象，运行时系统就会开始搜索知道如何处理 <code>FileNotFoundException</code> 对象的异常处理器。</p><p>编写你自己的方法时，不必声明你的方法可能抛出的所有 <code>Throwable</code> 对象。什么时候需要在所写的方法中用 <code>throws</code> 子句声明异常，以及要用 <code>throws</code> 子句声明哪些异常，在遇到下面 4 种情况时会抛出异常：</p><ul><li><p><strong>调用了一个抛出检查型异常的方法</strong>，例如，<code>FileInputStream</code> 构造器。</p></li><li><p><strong>检测到一个错误，并且利用 <code>throw</code> 语句抛出一个检查型异常</strong>。  </p></li><li><p><strong>程序出现错误</strong>，例如，<code>a[-1] = 0</code> 会抛出一个非检查型异常（这里会抛出 <code>ArrayIndexOutOfBoundsException</code>）。  </p></li><li><p><strong>Java 虚拟机或运行时库出现内部错误</strong>。</p></li></ul><p>**如果出现前两种情况，则必须告诉使用这个方法的程序员有可能抛出异常。**如果没有处理器捕获这个异常，当前执行线程就会终止。</p><p>有些 Java 方法包含在对外提供的类中，对于这些方法，应该通过方法首部的**异常规范（exception specification）**声明这个方法可能抛出异常。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">class</span> <span class="hljs-title class_">MyAnimation</span> &#123;<br>    <span class="hljs-keyword">public</span> Image <span class="hljs-title function_">loadImage</span><span class="hljs-params">(String s)</span> <span class="hljs-keyword">throws</span> IOException &#123;<br>        ...<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>**如果一个方法有可能抛出多个检查型异常类型，那么就必须在方法的首部列出所有的异常类。每个异常类之间用逗号隔开。**如下面这个例子所示：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">class</span> <span class="hljs-title class_">MyAnimation</span> &#123;<br>    <span class="hljs-keyword">public</span> Image <span class="hljs-title function_">loadImage</span><span class="hljs-params">(String s)</span> <span class="hljs-keyword">throws</span> FileNotFoundException, EOFException <br>    &#123;<br>        ...<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p><strong>不需要声明 Java 的内部错误，即从 <code>Error</code> 继承的异常</strong>。任何代码都有可能抛出那些异常，而我们对此完全无法控制。</p><p>**也不应该声明从 <code>RuntimeException</code> 继承的那些非检查型异常。**这些运行时错误完全在我们的控制之中。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">class</span> <span class="hljs-title class_">MyAnimation</span> &#123;<br>    <span class="hljs-keyword">void</span> <span class="hljs-title function_">drawImage</span><span class="hljs-params">(<span class="hljs-type">int</span> i)</span> <span class="hljs-keyword">throws</span> ArrayIndexOutOfBoundsException   <span class="hljs-comment">// bad style</span><br>        ...<br>&#125;<br></code></pre></td></tr></table></figure><p>总之，<strong>一个方法必须声明所有可能抛出的检查型异常</strong>，<strong>而非检查型异常要么在你的控制之外（<code>Error</code>），要么是由从一开始就应该避免的情况导致的（<code>RuntimeException</code>）</strong>。</p><blockquote><p>⚠ <strong>警告</strong>：<strong>如果在子类中覆盖了超类的一个方法，子类方法中声明的检查型异常不能比超类方法中声明的异常更通用</strong>（子类方法可以抛出更特定的异常，或者根本不抛出任何异常）。特别需要说明的是，**如果超类方法没有抛出任何检查型异常，子类也不能抛出任何检查型异常。**例如，如果覆盖 <code>JComponent.paintComponent</code> 方法，由于超类中这个方法没有抛出任何检查型异常，所以，你的 <code>paintComponent</code> 也不能抛出任何检查型异常。</p><p>**如果类中的一个方法声明它会抛出一个异常，而这个异常是某个特定类的实例，那么这个方法抛出的异常可能属于这个类，也可能属于这个类的任意一个子类。**例如，<code>FileInputStream</code> 构造器声明有可能抛出一个 <code>IOException</code> 异常，在这种情况下，你并不知道具体是哪种 <code>IOException</code> 异常。它既可能是 <code>IOException</code>，也可能是其某个子类的对象，例如，<code>FileNotFoundException</code>。</p></blockquote><h4 id="7-1-3-如何抛出异常"><a href="#7-1-3-如何抛出异常" class="headerlink" title="7.1.3 如何抛出异常"></a>7.1.3 如何抛出异常</h4><p>现在假设在程序代码中一个名为 <code>readData</code> 的方法正在读取一个文件，文件首部承诺文件长度为 1024 个字符：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs yaml"><span class="hljs-attr">Content-length:</span> <span class="hljs-number">1024</span><br></code></pre></td></tr></table></figure><p>不过，读到 733 个字符之后文件就结束了。你希望抛出一个异常。</p><p>**首先要决定应该抛出什么类型的异常。**阅读 Java API 文档会发现，<code>EOFException</code> 异常的描述是：“指示输入过程中意外遇到了 EOF”。可以如下抛出这个异常：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">EOFException</span>();<br></code></pre></td></tr></table></figure><p>也可以写为：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">var</span> <span class="hljs-variable">e</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">EOFException</span>();<br><span class="hljs-keyword">throw</span> e;<br></code></pre></td></tr></table></figure><p>下面给出完整的代码：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs java">String <span class="hljs-title function_">readData</span><span class="hljs-params">(Scanner in)</span> <span class="hljs-keyword">throws</span> EOFException &#123;<br>    ...<br>    <span class="hljs-keyword">while</span> (...) &#123;<br>        <span class="hljs-keyword">if</span> (!in.hasNext()) &#123; <span class="hljs-comment">// EOF encountered</span><br>            <span class="hljs-keyword">if</span> (n &lt; len)<br>                <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">EOFException</span>();<br>        &#125;<br>        ...<br>    &#125;<br>    <span class="hljs-keyword">return</span> s;<br>&#125;<br></code></pre></td></tr></table></figure><p><code>EOFException</code> 类还有一个带一个字符串参数的构造器。你可以利用这个构造器更细致地描述异常情况。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">String</span> <span class="hljs-variable">gripe</span> <span class="hljs-operator">=</span> <span class="hljs-string">&quot;Content-length: &quot;</span> + len + <span class="hljs-string">&quot;, Received: &quot;</span> + n;<br><span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">EOFException</span>(gripe);<br></code></pre></td></tr></table></figure><p>如果一个已有的异常类能够满足你的要求，抛出这个异常非常容易。在这种情况下：</p><ol><li>找到一个合适的异常类。  </li><li>创建这个类的一个对象。  </li><li>将对象抛出。</li></ol><p>一旦方法抛出了异常，这个方法就不会返回到调用者。</p><h4 id="7-1-4-创建异常类"><a href="#7-1-4-创建异常类" class="headerlink" title="7.1.4 创建异常类"></a>7.1.4 创建异常类</h4><p>你的代码可能会遇到任何标准异常类都无法描述清楚的问题。在这种情况下，需要创建自己的异常类。我们要做的只是定义一个派生于 <code>Exception</code> 的类，或者派生于 <code>Exception</code> 的某个子类，如 <code>IOException</code>。习惯做法是，<strong>自定义的这个类应该包含两个构造器，一个是默认的构造器，另一个是包含详细描述信息的构造器</strong>（超类 <code>Throwable</code> 的 <code>toString</code> 方法会返回一个字符串，其中包含这个详细信息，这在调试中非常有用）。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">class</span> <span class="hljs-title class_">FileFormatException</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">IOException</span><br>&#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-title function_">FileFormatException</span><span class="hljs-params">()</span> &#123;&#125;<br>    <span class="hljs-keyword">public</span> <span class="hljs-title function_">FileFormatException</span><span class="hljs-params">(String gripe)</span> <br>    &#123;<br>        <span class="hljs-built_in">super</span>(gripe);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>现在，就可以抛出你自己定义的异常类型了。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><code class="hljs java">String <span class="hljs-title function_">readData</span><span class="hljs-params">(Scanner in)</span> <span class="hljs-keyword">throws</span> FileFormatException<br>&#123;<br>    <span class="hljs-keyword">while</span> (...) <br>    &#123;<br>        <span class="hljs-keyword">if</span> (ch == -<span class="hljs-number">1</span>) <span class="hljs-comment">// EOF encountered</span><br>        &#123; <span class="hljs-comment">// EOF encountered</span><br>            <span class="hljs-keyword">if</span> (n &lt; len)<br>                <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">FileFormatException</span>();<br>        &#125;<br>    &#125;<br>    <span class="hljs-keyword">return</span> s;<br>&#125;<br></code></pre></td></tr></table></figure><h3 id="7-2-捕获异常"><a href="#7-2-捕获异常" class="headerlink" title="7.2 捕获异常"></a>7.2 捕获异常</h3><h4 id="7-2-1-捕获异常概述"><a href="#7-2-1-捕获异常概述" class="headerlink" title="7.2.1 捕获异常概述"></a>7.2.1 捕获异常概述</h4><p>如果发生了某个异常，但没有在任何地方捕获这个异常，程序就会终止，并在控制台上打印一个消息，其中包括这个异常的类型和一个栈轨迹。不过，图形用户界面（GUI）程序可能会捕获异常，打印栈轨迹消息，然后返回用户界面处理循环。</p><p>要想捕获一个异常，需要建立 <code>try/catch</code> 语句块。最简单的 <code>try</code> 语句块如下所示：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">try</span> &#123;<br>    code<br>    more code<br>    more code<br>&#125; <span class="hljs-keyword">catch</span> (ExceptionType e) <br>&#123;<br>    handler <span class="hljs-keyword">for</span> <span class="hljs-built_in">this</span> type<br>&#125;<br></code></pre></td></tr></table></figure><p>如果 <code>try</code> 语句块中的任何代码抛出了 <code>catch</code> 子句中指定的一个异常类，那么：</p><ol><li><strong>程序将跳过 <code>try</code> 语句块的其余代码。</strong>  </li><li><strong>程序将执行 <code>catch</code> 子句中的处理器代码。</strong></li></ol><p><strong>如果 <code>try</code> 语句块中的代码没有抛出任何异常，那么程序将跳过 <code>catch</code> 子句。</strong>  </p><p>如果方法中的任何代码抛出了一个异常，但不是 <code>catch</code> 子句中指定的异常类型，那么这个方法会立即退出。</p><p>下面给出一个典型的读取数据的代码展示捕获异常的过程：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">read</span><span class="hljs-params">(String filename)</span> &#123;<br>    <span class="hljs-keyword">try</span> &#123;<br>        <span class="hljs-type">var</span> <span class="hljs-variable">in</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">FileInputStream</span>(filename);<br>        <span class="hljs-type">int</span> b;<br>        <span class="hljs-keyword">while</span> ((b = in.read()) != -<span class="hljs-number">1</span>)<br>            process input<br>    &#125; <span class="hljs-keyword">catch</span> (IOException exception) &#123;<br>        exception.printStackTrace();<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p><code>try</code> 子句中的大多数代码都很容易理解：读取并处理字节，直到遇到文件结束符为止。<code>read</code> 方法有可能抛出一个 <code>IOException</code> 异常。在这种情况下，将跳出整个 <code>while</code> 循环，进入 <code>catch</code> 子句，并生成一个栈轨迹。</p><p>如果 <code>read</code> 方法出现了错误，就让 <code>read</code> 方法的调用者去操心这个问题！如果采用这种处理方式，就必须声明这个方法可能会抛出一个 <code>IOException</code>。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">read</span><span class="hljs-params">(String filename)</span> <span class="hljs-keyword">throws</span> IOException &#123;<br>    <span class="hljs-type">var</span> <span class="hljs-variable">in</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">FileInputStream</span>(filename);<br>    <span class="hljs-type">int</span> b;<br>    <span class="hljs-keyword">while</span> ((b = in.read()) != -<span class="hljs-number">1</span>)<br>        process input<br>&#125;<br></code></pre></td></tr></table></figure><p>编译器严格地执行 <code>throws</code> 说明符。如果调用了一个抛出检查型异常的方法，就必须处理这个异常，或者继续传递这个异常。</p><p>要捕获那些你知道如何处理的异常，而继续传播那些你不知道怎样处理的异常。</p><p>如果想传播一个异常，就必须在方法的首部添加一个 <code>throws</code> 说明符，提醒调用者这个方法可能会抛出一个异常。</p><p>查看 Java API 文档，可以看到每个方法可能会抛出哪些异常，然后再决定是由自己处理，还是添加到 <code>throws</code> 列表中。</p><p>同时请记住，如果编写一个覆盖超类方法的方法，而这个超类方法没有抛出异常（如 <code>JComponent</code> 中的 <code>paintComponent</code>），就必须捕获你的方法代码中出现的每一个检查型异常。<strong>子类的 <code>throws</code> 列表中不允许出现超类方法中未列出的异常类。</strong></p><h4 id="7-2-2-捕获多个异常"><a href="#7-2-2-捕获多个异常" class="headerlink" title="7.2.2 捕获多个异常"></a>7.2.2 捕获多个异常</h4><p>在一个 <code>try</code> 语句块中可以捕获多个异常类型，并对不同类型的异常做出不同的处理。要为每个异常类型使用一个单独的 <code>catch</code> 子句，如下例所示：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">try</span> &#123;<br>    <span class="hljs-comment">// code that might throw exceptions</span><br>&#125; <span class="hljs-keyword">catch</span> (FileNotFoundException e) &#123;<br>    <span class="hljs-comment">// emergency action for missing files</span><br>&#125; <span class="hljs-keyword">catch</span> (UnknownHostException e) &#123;<br>    <span class="hljs-comment">// emergency action for unknown hosts</span><br>&#125; <span class="hljs-keyword">catch</span> (IOException e) &#123;<br>    <span class="hljs-comment">// emergency action for all other I/O problems</span><br>&#125;<br></code></pre></td></tr></table></figure><p>异常对象可能包含有关异常性质的信息。要想获得这个对象的更多信息，可以尝试使用</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">e.getMessage()<br></code></pre></td></tr></table></figure><p>得到详细的错误消息（如果有的话），或者使用</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">e.getClass().getName()<br></code></pre></td></tr></table></figure><p>得到异常对象的实际类型。</p><p>**在 Java 7 中，同一个 <code>catch</code> 子句中可以捕获多个异常类型。**例如，假设对应缺少文件和未知主机异常的动作是一样的，就可以合并 <code>catch</code> 子句：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">try</span> &#123;<br>    <span class="hljs-comment">// code that might throw exceptions</span><br>&#125; <span class="hljs-keyword">catch</span> (FileNotFoundException | UnknownHostException e) &#123;<br>    <span class="hljs-comment">// emergency action for missing files and unknown hosts</span><br>&#125; <span class="hljs-keyword">catch</span> (IOException e) &#123;<br>    <span class="hljs-comment">// emergency action for all other I/O problems</span><br>&#125;<br></code></pre></td></tr></table></figure><p>只有当捕获的异常类型彼此之间不存在子类关系时才需要这个特性。</p><blockquote><p><strong>注释</strong>：**捕获多个异常时，异常变量隐含为 <code>final</code> 变量。**例如，在以下子句体中不能为 <code>e</code> 赋一个不同的值：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">catch</span> (FileNotFoundException | UnknownHostException e) &#123; ... &#125;<br></code></pre></td></tr></table></figure></blockquote><h4 id="7-2-3-再次抛出异常与异常链"><a href="#7-2-3-再次抛出异常与异常链" class="headerlink" title="7.2.3 再次抛出异常与异常链"></a>7.2.3 再次抛出异常与异常链</h4><p>可以在 <code>catch</code> 子句中抛出一个异常。如果开发了一个供其他程序员使用的子系统，就可以使用一个指示子系统故障的异常类型。<code>ServletException</code> 就是这样一个异常类型的例子。执行一个 servlet 的代码可能不想知道发生错误的细节，但肯定希望知道这个 servlet 是否有问题。</p><p>可以如下捕获异常并将它再次抛出：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">try</span> &#123;<br>     access the database<br>&#125; <span class="hljs-keyword">catch</span> (SQLException e) &#123;<br>    <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">ServletException</span>(<span class="hljs-string">&quot;database error: &quot;</span> + e.getMessage());<br>&#125;<br></code></pre></td></tr></table></figure><p>在这里，构造 <code>ServletException</code> 时提供了异常的消息文本。</p><p><strong>可以把原始异常设置为新异常的“原因”：</strong></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">try</span> &#123;<br>     access the database<br>&#125; <span class="hljs-keyword">catch</span> (SQLException original) <br>&#123;<br>    <span class="hljs-type">var</span> <span class="hljs-variable">e</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">ServletException</span>(<span class="hljs-string">&quot;database error&quot;</span>);<br>e.initCause(original);<br><span class="hljs-keyword">throw</span> e;<br>&#125;<br></code></pre></td></tr></table></figure><p>捕获到这个异常时，可以使用下面这条语句获取原始异常：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">Throwable</span> <span class="hljs-variable">original</span> <span class="hljs-operator">=</span> caughtException.getCause();<br></code></pre></td></tr></table></figure><p>这种方法可以在子系统中抛出高层异常，而不会丢失原始异常的细节信息。</p><blockquote><p><strong>提示</strong>：如果在一个方法中出现了一个检查型异常，但这个方法不允许抛出检查型异常，我们可以捕获这个检查型异常，并将它包装成一个运行时异常。</p></blockquote><p>有时你可能只想记录一个异常，再将它重新抛出，而不做任何改变：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">try</span> &#123;<br>    <span class="hljs-comment">// access the database</span><br>&#125; <span class="hljs-keyword">catch</span> (Exception e) &#123;<br>    logger.log(level, message, e);<br>    <span class="hljs-keyword">throw</span> e;<br>&#125;<br></code></pre></td></tr></table></figure><p>在 Java 7 之前，这种方法存在一个问题。假设这个代码在以下方法中：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">updateRecord</span><span class="hljs-params">()</span> <span class="hljs-keyword">throws</span> SQLException<br></code></pre></td></tr></table></figure><p>Java 编译器查看 <code>catch</code> 块中的 <code>throw</code> 语句，然后查看 <code>e</code> 的类型，会指出这个方法可能抛出任何 <code>Exception</code> 而不只是 <code>SQLException</code>。现在这个问题已经得到改进。编译器会跟踪到 <code>e</code> 来自 <code>try</code> 块。假设这个 <code>try</code> 块中仅有的检查型异常是 <code>SQLException</code> 实例，另外假设 <code>e</code> 在 <code>catch</code> 块中未改变，将外围方法声明为 <code>throws SQLException</code> 就是合法的。</p><h4 id="7-2-4-finally-子句"><a href="#7-2-4-finally-子句" class="headerlink" title="7.2.4 finally 子句"></a>7.2.4 <code>finally</code> 子句</h4><p>代码抛出一个异常 ，就会停止处理这个方法中剩余的代码，并退出这个方法。如果这个方法已经获得了只有它自己才知道的一些本地资源，而这些资源必须清理，这就会有问题。一种解决方案是捕获所有异常，完成资源的清理，再重新抛出异常。这种解决方案需要在两个地方清理资源：一个是在正常的代码中，另一个是在异常代码中。<code>finally</code> 子句可以解决这个问题。</p><p>**不管是否捕获到异常，<code>finally</code> 子句中的代码都会执行。**在下面的示例中，所有情况下程序都将关闭输入流。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">var</span> <span class="hljs-variable">in</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">FileInputStream</span>(...);<br><span class="hljs-keyword">try</span> &#123;<br>    <span class="hljs-comment">//1</span><br>    code that might <span class="hljs-keyword">throw</span> exceptions<br><span class="hljs-comment">//2</span><br>&#125; <br><span class="hljs-keyword">catch</span> (IOException e)<br>&#123;<br>    <span class="hljs-comment">//3</span><br>     show error message<br>    <span class="hljs-comment">//4</span><br>&#125; <br><span class="hljs-keyword">finally</span><br>&#123;<br>    <span class="hljs-comment">//5</span><br>    in.close();<br>&#125;<br><span class="hljs-comment">//6</span><br></code></pre></td></tr></table></figure><p>下面来看这个程序执行 <code>finally</code> 子句的 3 种可能的情况：</p><ol><li><p>代码没有抛出异常。</p><p>在这种情况下，程序首先执行 <code>try</code> 语句块中的全部代码，然后执行 <code>finally</code> 子句中的代码。随后，继续执行 <code>finally</code> 子句之后的第一条语句。也就是说，执行的顺序是 1、2、5、6。</p></li><li><p>代码抛出一个异常，并在一个 <code>catch</code> 子句中捕获。</p><p>在上面的示例中就是 <code>IOException</code> 异常。在这种情况下，程序将执行 <code>try</code> 语句块中的所有代码，直到抛出异常为止。此时，将跳过 <code>try</code> 语句块中的剩余代码，转去执行与该异常匹配的 <code>catch</code> 子句中的代码，然后执行 <code>finally</code> 子句中的代码。  </p><ul><li>如果 <code>catch</code> 子句没有抛出异常，程序将执行 <code>finally</code> 子句之后的第一条语句。在这种情况下，执行顺序是 1、3、4、5、6。  </li><li>如果 <code>catch</code> 子句抛出了一个异常，异常将被抛回到这个方法的调用者。执行顺序则只是 1、3、5。</li></ul></li><li><p>代码抛出了一个异常，但没有任何 <code>catch</code> 子句捕获这个异常。</p><p>在这种情况下，程序将执行 <code>try</code> 语句块中的所有语句，直到抛出异常为止。此时，将跳过 <code>try</code> 语句块中的剩余代码，然后执行 <code>finally</code> 子句中的语句，并将异常抛回给这个方法的调用者。在这里，执行顺序只是 1、5。</p></li></ol><p>**<code>try</code> 语句可以只有 <code>finally</code> 子句，而没有 <code>catch</code> 子句。**例如，下面这条 <code>try</code> 语句：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">InputStream</span> <span class="hljs-variable">in</span> <span class="hljs-operator">=</span> ...;<br><span class="hljs-keyword">try</span> &#123;<br>    code that might <span class="hljs-keyword">throw</span> exceptions<br>&#125; <span class="hljs-keyword">finally</span> &#123;<br>    in.close();<br>&#125;<br></code></pre></td></tr></table></figure><p>无论在 <code>try</code> 语句块中是否遇到异常，<code>finally</code> 子句中的 <code>in.close()</code> 语句都会执行。当然，如果真的遇到一个异常，这个异常将会被重新抛出，并且必须由另一个 <code>catch</code> 子句捕获。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">InputStream</span> <span class="hljs-variable">in</span> <span class="hljs-operator">=</span> ...;<br><span class="hljs-keyword">try</span> &#123;<br>    <span class="hljs-keyword">try</span> &#123;<br>        code that might <span class="hljs-keyword">throw</span> exceptions<br>    &#125; <span class="hljs-keyword">finally</span> &#123;<br>        in.close();<br>    &#125;<br>&#125; <span class="hljs-keyword">catch</span> (IOException e) &#123;<br>    show error message<br>&#125;<br></code></pre></td></tr></table></figure><p>内层的 <code>try</code> 语句块只有一个职责，就是确保关闭输入流。外层的 <code>try</code> 语句块也只有一个职责，就是确保报告出现的错误。这种解决方案不仅清楚而且会报告 <code>finally</code> 子句中出现的错误。</p><blockquote><p>[!WARNING]</p><p>假设由 <code>return</code> 语句从 <code>try</code> 语句块中间退出。在方法返回前，会执行 <code>finally</code> 子句块。**如果 <code>finally</code> 块也有一个 <code>return</code> 语句，这个返回值将会遮蔽原来的返回值。**来看下面这个例子：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-type">int</span> <span class="hljs-title function_">parseInt</span><span class="hljs-params">(String s)</span> &#123;<br>    <span class="hljs-keyword">try</span> &#123;<br>        <span class="hljs-keyword">return</span> Integer.parseInt(s);<br>    &#125; <span class="hljs-keyword">finally</span> &#123;<br>        <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>; <span class="hljs-comment">// ERROR</span><br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>看起来在 <code>parseInt(&quot;42&quot;)</code> 调用中，<code>try</code> 块的体会返回整数 42。不过，这个方法真正返回之前，会执行 <code>finally</code> 子句，这就使得方法最后会返回 0，而忽略原先的返回值。</p><p>更糟糕的是，考虑调用 <code>parseInt(&quot;zero&quot;)</code>。<code>Integer.parseInt</code> 方法会抛出一个 <code>NumberFormatException</code>，然后执行 <code>finally</code> 子句，<code>return</code> 语句甚至“吞掉”了这个异常！</p><p><code>finally</code> 子句的体要用于清理资源。不要把改变控制流的语句（<code>return</code>、<code>throw</code>、<code>break</code>、<code>continue</code>）放在 <code>finally</code> 子句中。</p></blockquote><h4 id="7-2-5-try-with-Resources-语句"><a href="#7-2-5-try-with-Resources-语句" class="headerlink" title="7.2.5 try-with-Resources 语句"></a>7.2.5 try-with-Resources 语句</h4><p>在 Java 7 中，对于以下代码模式：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs java">open a resource<br><span class="hljs-keyword">try</span><br>&#123;<br>work with the resource<br>&#125;<br><span class="hljs-keyword">finally</span><br>&#123;<br>close the resource<br>&#125;<br></code></pre></td></tr></table></figure><p>假设这个资源属于一个实现了 <code>AutoCloseable</code> 接口的类，Java 7 为这种代码模式提供了一个很有用的快捷方式。<code>AutoCloseable</code> 接口有一个方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">void</span> <span class="hljs-title function_">close</span><span class="hljs-params">()</span> <span class="hljs-keyword">throws</span> Exception<br></code></pre></td></tr></table></figure><blockquote><p>[!NOTE]</p><p><strong>注释</strong>：另外，还有一个 <code>Closeable</code> 接口。这是 <code>AutoCloseable</code> 的子接口，也只包含一个 <code>close</code> 方法。不过，这个方法声明为抛出一个 <code>IOException</code>。</p></blockquote><p>try-with-resources 语句（带资源的 try 语句）的最简形式为：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">try</span> (<span class="hljs-type">Resource</span> <span class="hljs-variable">res</span> <span class="hljs-operator">=</span> ...) &#123;<br>    work with res<br>&#125;<br></code></pre></td></tr></table></figure><p><strong>try 块退出时，会自动调用 <code>res.close()</code></strong>。下面给出一个典型的例子，这里要读取一个文件中的所有单词：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">try</span> (<span class="hljs-type">var</span> <span class="hljs-variable">in</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Scanner</span>(Path.of(<span class="hljs-string">&quot;in.txt&quot;</span>), StandardCharsets.UTF_8)) &#123;<br>    <span class="hljs-keyword">while</span> (in.hasNext())<br>        System.out.println(in.next());<br>&#125;<br></code></pre></td></tr></table></figure><p>这个块正常退出时，或者存在一个异常时，都会调用 <code>in.close()</code> 方法，就好像使用了 <code>finally</code> 块一样。</p><p>还可以指定多个资源。例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">try</span> (<span class="hljs-type">var</span> <span class="hljs-variable">in</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Scanner</span>(Path.of(<span class="hljs-string">&quot;in.txt&quot;</span>), StandardCharsets.UTF_8);<br>     <span class="hljs-type">var</span> <span class="hljs-variable">out</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">PrintWriter</span>(<span class="hljs-string">&quot;out.txt&quot;</span>, StandardCharsets.UTF_8)) &#123;<br>    <span class="hljs-keyword">while</span> (in.hasNext())<br>        out.println(in.next().toUpperCase());<br>&#125;<br></code></pre></td></tr></table></figure><p>不论这个块如何退出，<code>in</code> 和 <code>out</code> 都会关闭。如果用常规方式手动编程，就需要两个嵌套的 <code>try/finally</code> 语句。</p><p>在 Java 9 中，可以在 try 首部提供之前声明的事实最终变量：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">printAll</span><span class="hljs-params">(String[] lines, PrintWriter out)</span> &#123;<br>    <span class="hljs-keyword">try</span> (out) &#123; <span class="hljs-comment">// effectively final variable</span><br>        <span class="hljs-keyword">for</span> (String line : lines)<br>            out.println(line);<br>    &#125;<span class="hljs-comment">// out.close() called here</span><br>&#125;<br></code></pre></td></tr></table></figure><p>如果 <code>try</code> 块抛出一个异常，而且 <code>close</code> 方法也抛出一个异常，这会带来一个难题。<code>try-with-resources</code> 语句可以很好地处理这种情况。原来的异常会重新抛出，而 <code>close</code> 方法抛出的所有异常会“被抑制”。这些异常将被自动捕获，并由 <code>addSuppressed</code> 方法添加到原来的异常中去。调用 <code>getSuppressed</code> 方法，它会生成一个数组，其中包含从 <code>close</code> 方法抛出的被抑制的异常。</p><p>只要需要关闭资源，就要尽可能使用 <code>try-with-resources</code> 语句。</p><blockquote><p>[!NOTE]</p><p><strong>注释</strong>：<code>try-with-resources</code> 语句自身也可以有 <code>catch</code> 子句，甚至还可以有一个 <code>finally</code> 子句。这些子句会在关闭资源之后执行。</p></blockquote><h4 id="7-2-6-分析栈轨迹元素"><a href="#7-2-6-分析栈轨迹元素" class="headerlink" title="7.2.6 分析栈轨迹元素"></a>7.2.6 分析栈轨迹元素</h4><p>**栈轨迹（stack trace）**是程序执行过程中某个特定点上所有挂起的方法调用的一个列表。当 Java 程序因为一个未捕获的异常而终止时，就会显示栈轨迹。</p><p>可以调用 <code>Throwable</code> 类的 <code>printStackTrace</code> 方法访问栈轨迹的文本描述信息。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">var</span> <span class="hljs-variable">t</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Throwable</span>();<br><span class="hljs-type">var</span> <span class="hljs-variable">out</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">StringWriter</span>();<br>t.printStackTrace(<span class="hljs-keyword">new</span> <span class="hljs-title class_">PrintWriter</span>(out));<br><span class="hljs-type">String</span> <span class="hljs-variable">description</span> <span class="hljs-operator">=</span> out.toString();<br></code></pre></td></tr></table></figure><p>一种更灵活的方法是使用 <code>StackWalker</code> 类，它会生成一个 <code>StackWalker.StackFrame</code> 实例流，其中每个实例分别描述一个<strong>栈帧（stack frame）</strong>。可以利用以下调用迭代处理这些栈帧：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">StackWalker</span> <span class="hljs-variable">walker</span> <span class="hljs-operator">=</span> StackWalker.getInstance();<br>walker.forEach(frame -&gt; analyze frame);<br></code></pre></td></tr></table></figure><p>如果想要以懒方式处理 <code>Stream&lt;StackWalker.StackFrame&gt;</code>，可以调用：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">walker.walk(stream -&gt; process stream);<br></code></pre></td></tr></table></figure><p><code>StackWalker.StackFrame</code> 类有一些方法可以得到正在执行的代码行的文件名和行号，以及类对象和方法名。<code>toString</code> 方法会生成一个格式化字符串，其中包含所有这些信息。</p><blockquote><p>[!NOTE]</p><p><strong>注释</strong>：在 Java 9 之前，<code>Throwable.getStackTrace</code> 方法会生成一个 <code>StackTraceElement[]</code> 数组，其中包含与 <code>StackWalker.StackFrame</code> 实例流类似的信息。不过，这个调用的效率不高，因为它要得到整个栈，即使调用者可能只需要几个栈帧。另外它只允许访问挂起方法的类名，而不能访问类对象。</p></blockquote><h3 id="7-3-使用异常的技巧"><a href="#7-3-使用异常的技巧" class="headerlink" title="7.3 使用异常的技巧"></a>7.3 使用异常的技巧</h3><p>下面给出适当使用异常的一些技巧。</p><ol><li><strong>异常处理不能代替简单的测试</strong><br> 假设有一段代码尝试将一个空栈弹出 10 000 000 次。第一种做法是：首先查看栈是否为空。</li></ol><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">if</span> (!s.empty()) s.pop();<br></code></pre></td></tr></table></figure><p>再看第二种做法，我们强制要求不管怎样都执行弹出操作，然后捕获 <code>EmptyStackException</code> 异常来告诉我们不该这样做。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">try</span> &#123;<br>    s.pop();<br>&#125; <span class="hljs-keyword">catch</span> (EmptyStackException e) &#123;<br>    ...<br>&#125;<br></code></pre></td></tr></table></figure><p>在我的测试机器上，调用 <code>isEmpty</code> 的版本运行时间为 646 毫秒。捕获 <code>EmptyStackException</code> 的版本运行时间为 21 739 毫秒。</p><p>与完成简单的测试相比，捕获异常所花费的时间大大超过了前者，因此使用异常的基本规则是：<strong>只在异常情况下使用异常。</strong></p><ol start="2"><li><strong>不要过分地细化异常</strong><br>很多程序员将每一条语句都分装在一个单独的 try 语句块中。</li></ol><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><code class="hljs java">PrintStream out;<br>Stack s;<br><span class="hljs-keyword">for</span> (i = <span class="hljs-number">0</span>; i &lt; <span class="hljs-number">100</span>; i++) &#123;<br>    <span class="hljs-keyword">try</span> &#123;<br>        n = s.pop();<br>    &#125; <span class="hljs-keyword">catch</span> (EmptyStackException e) &#123;<br>        <span class="hljs-comment">// stack was empty</span><br>    &#125;<br>    <span class="hljs-keyword">try</span> &#123;<br>        out.writeInt(n);<br>    &#125; <span class="hljs-keyword">catch</span> (IOException e) &#123;<br>        <span class="hljs-comment">// problem writing to file</span><br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>这种编程方式将导致代码量的急剧膨胀。我们希望从栈中弹出 100 个数，将它们存入一个文件中。如果栈是空的，它不会变成非空状态；如果文件包含错误，这个错误也不会消失。因此，合理的做法是将整个任务包在一个 try 语句块中，这样，当任何一个操作出现问题时，就可以取消整个任务。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">try</span> &#123;<br>    <span class="hljs-keyword">for</span> (i = <span class="hljs-number">0</span>; i &lt; <span class="hljs-number">100</span>; i++) &#123;<br>        n = s.pop();<br>        out.writeInt(n);<br>    &#125;<br>&#125; <span class="hljs-keyword">catch</span> (IOException e) &#123;<br>    <span class="hljs-comment">// problem writing to file</span><br>&#125; <span class="hljs-keyword">catch</span> (EmptyStackException e) &#123;<br>    <span class="hljs-comment">// stack was empty</span><br>&#125;<br></code></pre></td></tr></table></figure><p>这样也满足了异常处理的一个承诺：将正常处理与错误处理分开。</p><ol start="3"><li><strong>合理利用异常层次结构</strong></li></ol><p>不要只抛出 <code>RuntimeException</code> 异常。应该寻找一个适合的子类或创建你自己的异常类。</p><p>不要只捕获 <code>Throwable</code> 异常，否则，这会使你的代码很难读、很难维护。</p><p>考虑检查型异常与非检查型异常的区别。检查型异常本质上开销较大，不要为逻辑错误抛出这些异常。</p><p>如果能够将一种异常转换成另一种更加适合的异常就转换。例如，在解析某个文件中的一个整数时，可以捕获 <code>NumberFormatException</code> 异常，然后将它转换成 <code>IOException</code> 的一个子类或 <code>MySubsystemException</code>。</p><ol start="4"><li><strong>不要压制异常</strong></li></ol><p>如果你编写了一个方法要调用另一个方法，而那个方法有可能 100 年才抛出一个异常，但是，如果没有在你的方法的 <code>throws</code> 列表中声明这个异常，编译器就会报错。你不想把它放在 <code>throws</code> 列表中，因为这样一来，编译器会对调用你的方法的所有方法都报错。因此，你会关闭这个异常：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> Image <span class="hljs-title function_">loadImage</span><span class="hljs-params">(String s)</span> &#123;<br>    <span class="hljs-keyword">try</span> &#123;<br>        <span class="hljs-comment">// code that threatens to throw checked exceptions</span><br>    &#125; <span class="hljs-keyword">catch</span> (Exception e) &#123;<br>        <span class="hljs-comment">// so there</span><br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>现在你的代码可以顺利通过编译了。它能很好地运行，除非出现异常。然后这个异常会被悄无声息地忽略。如果你认为异常都非常重要，就应该适当地进行处理。</p><ol start="5"><li><p><strong>在检测错误时，“苛刻”要比放任更好</strong><br>调用一个方法时，如果提供了非法的参数，返回一个虚拟值是不是比抛出一个异常更好？例如，当栈为空时，<code>Stack.pop</code> 是该返回 <code>null</code>，还是要抛出一个异常？我们认为：最好在出错的地方抛出一个 <code>EmptyStackException</code> 异常，这要好于以后出现一个 <code>NullPointerException</code> 异常。</p></li><li><p><strong>不要羞于传递异常</strong><br>很多程序员感觉应该捕获抛出的全部异常。如果他们调用了一个抛出异常的方法，例如，<code>FileInputStream</code> 构造器或 <code>readLine</code> 方法，他们就会本能地捕获可能产生的异常。其实，很多时候，更好的做法是继续传递这个异常，而不是自己捕获：</p></li></ol><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">readStuff</span><span class="hljs-params">(String filename)</span> <span class="hljs-keyword">throws</span> IOException &#123; <span class="hljs-comment">// not a sign of shame!</span><br>    <span class="hljs-type">var</span> <span class="hljs-variable">in</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">FileInputStream</span>(filename, StandardCharsets.UTF_8);<br>    ...<br>&#125;<br></code></pre></td></tr></table></figure><p>更高层的方法通常可以更好地通知用户发生了错误，或者放弃不成功的命令。</p><p>（<strong>注释</strong>：规则 5、6 可以归纳为“早抛出，晚捕获”。）</p><ol start="7"><li><strong>使用标准方法报告 <code>null</code> 指针和越界异常</strong><br><code>Objects</code> 包含以下方法：</li></ol><ul><li><code>requireNonNull</code></li><li><code>checkIndex</code></li><li><code>checkFromToIndex</code></li><li><code>checkFromIndexSize</code></li></ul><p>来完成这些常见的检查。要用这些方法来完成参数检验：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">putData</span><span class="hljs-params">(<span class="hljs-type">int</span> position, Object newValue)</span> &#123;<br>    Objects.checkIndex(position, data.length);<br>    Objects.requireNonNull(newValue);<br>    ...<br>&#125;<br></code></pre></td></tr></table></figure><p>如果调用方法时提供了一个非法索引或一个 <code>null</code> 参数，要用我们熟悉的 Java 库使用的消息抛出一个异常。</p><ol start="8"><li><strong>不要向最终用户显示栈轨迹</strong><br>如果你的程序遇到一个预料之外的异常，看起来显示一个栈轨迹是个好主意，这样用户就能报告这个错误，使你能更容易地找出问题所在。不过，栈轨迹可能包含你不想暴露给潜在攻击者的实现细节，例如你使用的库的版本。</li></ol><p>应该将栈轨迹记入日志，以便以后获取，而只向用户显示一个总结消息。</p><h3 id="7-4-使用断言"><a href="#7-4-使用断言" class="headerlink" title="7.4 使用断言"></a>7.4 使用断言</h3><p>在一个具有自我保护能力的程序中，断言很常用。</p><h4 id="7-4-1-断言的概念"><a href="#7-4-1-断言的概念" class="headerlink" title="7.4.1 断言的概念"></a>7.4.1 断言的概念</h4><p>假设你确信满足某个特定属性，并且代码依赖于这个属性。例如，可能需要计算：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">double</span> <span class="hljs-variable">y</span> <span class="hljs-operator">=</span> Math.sqrt(x);<br></code></pre></td></tr></table></figure><p>你确信这里的 <code>x</code> 是一个非负数。原因可能是：<code>x</code> 是另外一个计算的结果，而这个计算的结果不可能为负；或者 <code>x</code> 是一个方法的参数，这个方法要求它的调用者只能提供一个正数输入。不过，你可能还是想再做一次检查，不希望计算中潜入让人困惑的“不是一个数”（NaN）浮点值。当然，也可以抛出一个异常：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">if</span> (x &lt; <span class="hljs-number">0</span>) <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">IllegalArgumentException</span>(<span class="hljs-string">&quot;x &lt; 0&quot;</span>);<br></code></pre></td></tr></table></figure><p>即使测试完成后，这个测试代码还一直保留在程序中。如果在程序中含有大量这种检查，程序运行起来会比应有的速度慢一些。</p><p>**断言（assertion）**机制允许你在测试期间在代码中插入一些检查，而在生产代码中自动删除这些检查。</p><p>Java 语言有一个关键字 <code>assert</code>。这个关键字有两种形式：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">assert</span> condition;<br></code></pre></td></tr></table></figure><p>和</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">assert</span> condition : expression;<br></code></pre></td></tr></table></figure><p>这两个语句都会计算条件（condition），如果结果为 <code>false</code>，则抛出一个 <code>AssertionError</code> 异常。在第二个语句中，表达式（expression）将传入 <code>AssertionError</code> 对象的构造器，并转换成一个消息字符串。</p><blockquote><p><strong>注释</strong>：<strong>表达式（expression）<strong>部分的唯一目的是</strong>生成一个消息字符串</strong>。<code>AssertionError</code> 对象并不存储具体的表达式值，因此，以后<strong>无法得到这个表达式值</strong>。</p></blockquote><p>要想断言 <code>x</code> 是一个非负数，只需要使用下面这条语句：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">assert</span> x &gt;= <span class="hljs-number">0</span>;<br></code></pre></td></tr></table></figure><p>或者将 <code>x</code> 的具体值传递给 <code>AssertionError</code> 对象，以便以后显示：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">assert</span> x &gt;= <span class="hljs-number">0</span> : x;<br></code></pre></td></tr></table></figure><h4 id="7-4-2-启用和禁用断言"><a href="#7-4-2-启用和禁用断言" class="headerlink" title="7.4.2 启用和禁用断言"></a>7.4.2 启用和禁用断言</h4><p>**在默认情况下，断言是禁用的。**可以在运行程序时用 <code>-enableassertions</code> 或 <code>-ea</code> 选项启用断言：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">java -enableassertions MyApp<br></code></pre></td></tr></table></figure><p>不必重新编译程序来启用或禁用断言。**启用或禁用断言是类加载器（class loader）的功能。**禁用断言时，类加载器会去除断言代码，因此，不会降低程序运行的速度。</p><p>甚至<strong>可以在特定的类或整个包中启用断言</strong>，例如：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">java -ea:MyClass -ea:com.mycompany.mylib MyApp<br></code></pre></td></tr></table></figure><p>这条命令将为 <code>MyClass</code> 类以及 <code>com.mycompany.mylib</code> 包及其子包中的所有类打开断言。选项 <code>-ea</code> 将为无名包中的所有类打开断言。</p><p>也可以用选项 <code>-disableassertions</code> 或 <code>-da</code> 在特定的类和包中禁用断言：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">java -ea:... -da:MyClass MyApp<br></code></pre></td></tr></table></figure><p>有些类不是由类加载器加载，而是直接由虚拟机加载。可以使用这些开关有选择地启用或禁用那些类中的断言。</p><p>不过，<strong>启用和禁用所有断言的 <code>-ea</code> 和 <code>-da</code> 开关不能应用于那些没有类加载器的“系统类”。需要使用 <code>-enablesystemassertions</code> &#x2F; <code>-esa</code> 开关启用系统类中的断言。</strong></p><p>也可以通过编程控制类加载器的断言状态。</p><h4 id="7-4-3-使用断言完成参数检查"><a href="#7-4-3-使用断言完成参数检查" class="headerlink" title="7.4.3 使用断言完成参数检查"></a>7.4.3 使用断言完成参数检查</h4><p>在 Java 语言中，提供了 3 种处理系统错误的机制：</p><ul><li>抛出一个异常  </li><li>记录日志  </li><li>使用断言</li></ul><p>什么时候应该选择使用断言呢？</p><ul><li>断言失败是致命的、不可恢复的错误。  </li><li>断言检查只在开发和测试阶段打开。</li></ul><p><strong>断言只应该用于在测试阶段确定程序内部错误的位置。</strong></p><p>下面看一个常见的场景：检查方法的参数。是否应该使用断言来检查非法的索引值或 <code>null</code> 引用呢？要想回答这个问题，首先来看这个方法的文档。假设你要实现一个排序方法。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-comment">/**</span><br><span class="hljs-comment">  Sorts the specified range of the specified array in ascending numerical order.</span><br><span class="hljs-comment">  The range to be sorted extends from fromIndex, inclusive, to toIndex, exclusive.</span><br><span class="hljs-comment">  <span class="hljs-doctag">@param</span> a the array to be sorted.</span><br><span class="hljs-comment">  <span class="hljs-doctag">@param</span> fromIndex the index of the first element (inclusive) to be sorted.</span><br><span class="hljs-comment">  <span class="hljs-doctag">@param</span> toIndex the index of the last element (exclusive) to be sorted.</span><br><span class="hljs-comment">  <span class="hljs-doctag">@throws</span> IllegalArgumentException if fromIndex &gt; toIndex</span><br><span class="hljs-comment">  <span class="hljs-doctag">@throws</span> ArrayIndexOutOfBoundsException if fromIndex &lt; 0 or toIndex &gt; a.length</span><br><span class="hljs-comment"> */</span><br><span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">sort</span><span class="hljs-params">(<span class="hljs-type">int</span>[] a, <span class="hljs-type">int</span> fromIndex, <span class="hljs-type">int</span> toIndex)</span><br></code></pre></td></tr></table></figure><p>文档指出，如果索引值不正确，这个方法会抛出一个异常。这是方法与其调用者之间约定的行为。如果实现这个方法，那就必须要遵守这个约定，抛出表示索引值有误的异常。这里使用断言不太合适。</p><p>是否应该断言 <code>a</code> 不是 <code>null</code> 呢？这也不太合适。这个方法的文档没有指出当 <code>a</code> 是 <code>null</code> 时应该采取什么行为。在这种情况下，调用者可以认为这个方法将会成功地返回，而不会抛出一个断言错误。</p><p>不过，假设对这个方法的约定做一点微小的改动：</p><figure class="highlight smali"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs smali">@param a the<span class="hljs-built_in"> array </span>to be sorted (must<span class="hljs-built_in"> not </span>be null)<br></code></pre></td></tr></table></figure><p>现在，这个方法的调用者就必须注意：对 <code>null</code> 数组调用这个方法是不合法的。这样一来，就可以在这个方法的开头使用断言：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">assert</span> a != <span class="hljs-literal">null</span>;<br></code></pre></td></tr></table></figure><p>计算机科学家将这种约定称为<strong>前置条件（Precondition）</strong>。原先的方法对参数没有前置条件，它承诺在任何情况下都有明确的行为。修改后的方法有一个前置条件，即 <code>a</code> 非 <code>null</code>。如果调用者没有满足这个前置条件，断言会失败，这个方法就能“为所欲为”。事实上，由于有这个断言，当方法被非法调用时，它的行为将是难以预料的。有时候会抛出一个断言错误，有时候会产生一个 <code>null</code> 指针异常，这完全取决于它的类加载器如何配置。</p><h4 id="7-4-4-使用断言提供假设文档"><a href="#7-4-4-使用断言提供假设文档" class="headerlink" title="7.4.4 使用断言提供假设文档"></a>7.4.4 使用断言提供假设文档</h4><p>通常，很多程序员使用注释来提供底层假设的文档。考虑 <a href="http://docs.oracle.com/javase/8/docs/technotes/guides/language/assert.html">Oracle 官方文档</a> 上的一个示例：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">if</span> (i % <span class="hljs-number">3</span> == <span class="hljs-number">0</span>)<br>    ...<br><span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (i % <span class="hljs-number">3</span> == <span class="hljs-number">1</span>)<br>    ...<br><span class="hljs-keyword">else</span> <span class="hljs-comment">// (i % 3 == 2)</span><br>    ...<br></code></pre></td></tr></table></figure><p>在这种情况下，使用断言会更合适。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">if</span> (i % <span class="hljs-number">3</span> == <span class="hljs-number">0</span>)<br>    ...<br><span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (i % <span class="hljs-number">3</span> == <span class="hljs-number">1</span>)<br>    ...<br><span class="hljs-keyword">else</span> &#123;<br>    <span class="hljs-keyword">assert</span> i % <span class="hljs-number">3</span> == <span class="hljs-number">2</span>;<br>&#125;<br></code></pre></td></tr></table></figure><p>当然，更好的做法是全面地考虑这个问题。<code>i % 3</code> 的值会是什么？如果 <code>i</code> 是正值，那么余数肯定是 0、1 或 2。如果 <code>i</code> 是负值，余数可以是 -1 和 -2。因此，真正的假设是 <code>i</code> 是非负值。最好是在语句之前使用以下断言：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">assert</span> i &gt;= <span class="hljs-number">0</span>;<br></code></pre></td></tr></table></figure><p>无论如何，这个示例说明了程序员应该充分使用断言来进行自我检查。你会看到，断言是一种用于测试和调试的战术性工具；与之不同，日志是一种用于程序整个生命周期的战略性工具。</p><h3 id="7-5-日志"><a href="#7-5-日志" class="headerlink" title="7.5 日志"></a>7.5 日志</h3><p>每个 Java 程序员都很熟悉在有问题的代码中插入一些 <code>System.out.println</code> 方法调用来帮助观察程序的行为。当然，一旦发现问题的根源，就要将这些 print 语句从代码中删去。如果接下来又出现了问题，只好再插入几个 print 语句。日志 API 就是为了解决这个问题而设计的。</p><p>下面先讨论这个 API 的主要优点：</p><ul><li><p>可以很容易地抑制全部日志记录，或者只抑制某个级别以下的日志，而且再次打开这些日志也很容易。</p></li><li><p>被抑制的日志开销低廉，因此，将这些日志代码留在应用中只有很小的开销。</p></li><li><p>日志记录可以定向到不同的处理器，如在控制台显示、写至文件，等等。</p></li><li><p>日志记录器和处理器都可以对记录进行过滤。过滤器可以根据实现过滤器的程序员提供的标准丢弃那些无用的日志记录。 </p></li><li><p>日志记录可以采用不同的方式格式化，例如，纯文本或 XML。  </p></li><li><p>应用程序可以使用多个日志记录器，它们使用与包名类似的有层次的名字，例如，<code>com.mycompany.myapp</code>。  </p></li><li><p>日志系统的配置由配置文件控制。</p></li></ul><blockquote><p><strong>注释</strong>：在 Java 9 中，Java 平台有一个单独的轻量级日志系统，它不依赖于 <code>java.logging</code> 模块（这个模块包含标准 Java 日志框架）。这个系统只用于 Java API。如果有 <code>java.logging</code> 模块，日志消息会自动地转发给它。第三方日志框架可以提供适配器来接收平台日志消息。我们不打算介绍平台日志，因为开发应用程序的程序员不太会用到平台日志。</p></blockquote><h4 id="7-5-1-基本日志"><a href="#7-5-1-基本日志" class="headerlink" title="7.5.1 基本日志"></a>7.5.1 基本日志</h4><p>对于简单的日志记录，可以使用**全局日志记录器（global logger）**并调用其 <code>info</code> 方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">Logger.getGlobal().info(<span class="hljs-string">&quot;File-&gt;Open menu item selected&quot;</span>);<br></code></pre></td></tr></table></figure><p>在默认情况下，会如下打印这个记录：</p><figure class="highlight apache"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs apache"><span class="hljs-attribute">May</span> <span class="hljs-number">10</span>, <span class="hljs-number">2013</span> <span class="hljs-number">4</span>:<span class="hljs-number">12</span>:<span class="hljs-number">15</span> PM LoggingImageViewer fileOpen<br><span class="hljs-attribute">INFO</span>: File-&gt;Open menu item selected<br></code></pre></td></tr></table></figure><p>但是，如果在适当的地方（如 <code>main</code> 的最前面）调用：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">Logger.getGlobal().setLevel(Level.OFF);<br></code></pre></td></tr></table></figure><p>将会抑制所有日志。</p><h4 id="7-5-2-高级日志"><a href="#7-5-2-高级日志" class="headerlink" title="7.5.2 高级日志"></a>7.5.2 高级日志</h4><p>在一个专业的应用程序中，你肯定不想将所有的日志都记录到一个全局日志记录器中。你可以定义自己的日志记录器。</p><p>可以调用 <code>getLogger</code> 方法创建或获取一个日志记录器：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">Logger</span> <span class="hljs-variable">myLogger</span> <span class="hljs-operator">=</span> Logger.getLogger(<span class="hljs-string">&quot;com.mycompany.myapp&quot;</span>);<br></code></pre></td></tr></table></figure><blockquote><p>[!NOTE]</p><p><strong>提示</strong>：未被任何变量引用的日志记录器可能会被垃圾回收。为了防止这种情况发生，要像上面的例子中一样，用静态变量存储日志记录器的一个引用。</p></blockquote><p>与包名类似，日志记录器名也有层次。事实上，与包相比，日志记录器的层次性更强。对于包来说，包与父包之间没有语义关系，但是日志记录器的父与子之间会共享某些属性。例如，如果对日志记录器 <code>&quot;com.mycompany&quot;</code> 设置了日志级别，它的子日志记录器也会继承这个级别。</p><p>通常，有以下 7 个日志级别：</p><ul><li>SEVERE  </li><li>WARNING  </li><li>INFO  </li><li>CONFIG  </li><li>FINE  </li><li>FINER  </li><li>FINEST</li></ul><p>在默认情况下，实际上只记录前 3 个级别。也可以设置一个不同的级别，例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">logger.setLevel(Level.FINE);<br></code></pre></td></tr></table></figure><p>现在，会记录 FINE 以及所有更高级别的日志。</p><p>另外，还可以使用 <code>Level.ALL</code> 开启所有级别的日志记录，或者使用 <code>Level.OFF</code> 关闭所有日志。</p><p><strong>所有级别都有日志记录方法</strong>，如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java">logger.warning(message);<br>logger.fine(message);<br></code></pre></td></tr></table></figure><p>或者，还<strong>可以使用 <code>log</code> 方法并指定级别</strong>，例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">logger.log(Level.FINE, message);<br></code></pre></td></tr></table></figure><blockquote><p><strong>提示</strong>：默认的日志配置会记录 INFO 或更高级别的所有日志，因此，对于那些有助于诊断但对用户意义不大的调试信息，应该使用 CONFIG、FINE、FINER 和 FINEST 级别。</p></blockquote><blockquote><p><strong>警告</strong>：如果将记录级别设置为比 INFO 更低的级别，还需要修改日志处理器的配置。默认的日志处理器会抑制低于 INFO 级别的消息。</p></blockquote><p>默认的日志记录会显示包含日志调用的类和方法的名字（根据调用栈得出）。不过，如果虚拟机对执行过程进行了优化，就可能得不到准确的调用信息。此时，可以使用 <code>logp</code> 方法获得调用类和方法的确切位置，这个方法的签名为：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">void</span> <span class="hljs-title function_">logp</span><span class="hljs-params">(Level l, String className, String methodName, String message)</span><br></code></pre></td></tr></table></figure><p>有一些用来跟踪执行流的便利方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">void</span> <span class="hljs-title function_">entering</span><span class="hljs-params">(String className, String methodName)</span><br><span class="hljs-keyword">void</span> <span class="hljs-title function_">entering</span><span class="hljs-params">(String className, String methodName, Object param)</span><br><span class="hljs-keyword">void</span> <span class="hljs-title function_">entering</span><span class="hljs-params">(String className, String methodName, Object[] params)</span><br><span class="hljs-keyword">void</span> <span class="hljs-title function_">exiting</span><span class="hljs-params">(String className, String methodName)</span><br></code></pre></td></tr></table></figure><p>例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">int</span> <span class="hljs-title function_">read</span><span class="hljs-params">(String file, String pattern)</span> &#123;<br>    logger.entering(<span class="hljs-string">&quot;com.mycompany.mylib.Reader&quot;</span>, <span class="hljs-string">&quot;read&quot;</span>,<br>        <span class="hljs-keyword">new</span> <span class="hljs-title class_">Object</span>[] &#123; file, pattern &#125;);<br>    ...<br>    logger.exiting(<span class="hljs-string">&quot;com.mycompany.mylib.Reader&quot;</span>, <span class="hljs-string">&quot;read&quot;</span>, count);<br>    <span class="hljs-keyword">return</span> count;<br>&#125;<br></code></pre></td></tr></table></figure><p>这些调用将生成 <code>FINER</code> 级别而且以字符串 <code>ENTRY</code> 和 <code>RETURN</code> 开头的日志记录。</p><blockquote><p>[!NOTE]</p><p><strong>注释</strong>： 在将来某个时候，带 <code>Object</code> 参数的日志记录方法可能会被重写，以支持可变参数列表（“ varargs ”）。那时就可以做出类似</p><p><code>logger.entering(&quot;com.mycompany.mylib.Reader&quot;, &quot;read&quot;, file, pattern)</code> 的调用了。</p></blockquote><p>记录日志的一个常见用途是记录那些预料之外的异常。可以使用下面两个便利方法在日志记录中包含异常的描述：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">void</span> <span class="hljs-title function_">throwing</span><span class="hljs-params">(String className, String methodName, Throwable t)</span><br><span class="hljs-keyword">void</span> <span class="hljs-title function_">log</span><span class="hljs-params">(Level l, String message, Throwable t)</span><br></code></pre></td></tr></table></figure><p>典型的用法是：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">if</span> (...) &#123;<br>    <span class="hljs-type">var</span> <span class="hljs-variable">e</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">IOException</span>(<span class="hljs-string">&quot;...&quot;</span>);<br>    logger.throwing(<span class="hljs-string">&quot;com.mycompany.mylib.Reader&quot;</span>, <span class="hljs-string">&quot;read&quot;</span>, e);<br>    <span class="hljs-keyword">throw</span> e;<br>&#125;<br></code></pre></td></tr></table></figure><p>和</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">try</span> &#123;<br>    ...<br>&#125; <span class="hljs-keyword">catch</span> (IOException e) &#123;<br>    Logger.getLogger(<span class="hljs-string">&quot;com.mycompany.myapp&quot;</span>).log(Level.WARNING, <span class="hljs-string">&quot;Reading image&quot;</span>, e);<br>&#125;<br></code></pre></td></tr></table></figure><p><code>throwing</code> 调用可以记录一条 <code>FINER</code> 级别的日志记录和一个以 <code>THROW</code> 开头的消息。</p><h4 id="7-5-3-修改日志管理器配置"><a href="#7-5-3-修改日志管理器配置" class="headerlink" title="7.5.3 修改日志管理器配置"></a>7.5.3 修改日志管理器配置</h4><p>可以通过编辑配置文件来修改日志系统的各个属性。默认的配置文件位于：</p><p><code>jdk/conf/logging.properties</code>（或者在 Java 9 之前，位于 <code>jre/lib/logging.properties</code>。）</p><p>要想使用另一个配置文件，就要将 <code>java.util.logging.config.file</code> 属性设置为那个文件的位置，为此要用以下命令启动你的应用程序：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">java -Djava.util.logging.config.file=configFile MainClass<br></code></pre></td></tr></table></figure><p>要想修改默认的日志级别，需要编辑配置文件，并修改下面这行设置：</p><figure class="highlight pgsql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs pgsql">.<span class="hljs-keyword">level</span>=<span class="hljs-keyword">INFO</span><br></code></pre></td></tr></table></figure><p>可以为你自己的日志记录器指定日志级别，例如，可以增加下面这行设置：</p><figure class="highlight ini"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs ini"><span class="hljs-attr">com.mycompany.myapp.level</span>=FINE<br></code></pre></td></tr></table></figure><p>也就是说，在日志记录器名后面追加后缀 <code>.level</code>。</p><p>稍后可以看到，日志记录器并不将消息发送到控制台，那是处理器的任务。处理器也有级别。要想在控制台上看到 <code>FINE</code> 级别的消息，就需要如下设置：</p><figure class="highlight stylus"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs stylus">java<span class="hljs-selector-class">.util</span><span class="hljs-selector-class">.logging</span><span class="hljs-selector-class">.ConsoleHandler</span>.level=FINE<br></code></pre></td></tr></table></figure><blockquote><p>[!NOTE]</p><p><strong>警告</strong>： 日志管理器配置中的属性设置不是系统属性，因此，用</p><p><code>-Dcom.mycompany.myapp.level=FINE</code></p><p>启动程序不会对日志记录器产生任何影响。</p></blockquote><p>日志管理器在虚拟机启动时初始化，也就是在 <code>main</code> 方法执行前。如果想要定制日志属性，但是没有用</p><p><code>-Djava.util.logging.config.file</code></p><p>命令行选项启动应用，可以在程序中调用：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">System.setProperty(<span class="hljs-string">&quot;java.util.logging.config.file&quot;</span>, file);<br></code></pre></td></tr></table></figure><p>不过，这样一来，你还必须调用：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">LogManager.getLogManager().readConfiguration();<br></code></pre></td></tr></table></figure><p>重新初始化日志管理器。</p><p>在 Java 9 中，可以通过调用以下方法更新日志配置：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">LogManager.getLogManager().updateConfiguration(mapper);<br></code></pre></td></tr></table></figure><p>这样就会从 <code>java.util.logging.config.file</code> 系统属性指定的位置读取一个新配置。然后应用这个映射器来解析新老配置中所有键的值。映射器是一个：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">Function&lt;String, BiFunction&lt;String, String, String&gt;&gt;<br></code></pre></td></tr></table></figure><p>它将现有配置中的键映射到替换函数。每个替换函数接收到与键关联的老值和新值（或者，如果没有关联的值则得到 <code>null</code>），生成一个替换，或者如果要在更新中删除这个键则返回 <code>null</code>。</p><p>这听起来相当复杂，所以我们来看几个例子。</p><p>一种很有用的映射机制是合并老配置和新配置，如果一个键在老配置和新配置中都出现，则优先选择新值。这样一个**映射器（mapper）**就是：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">key -&gt; ((oldValue, newValue) -&gt; newValue == <span class="hljs-literal">null</span> ? oldValue : newValue)<br></code></pre></td></tr></table></figure><p>或者你可能只想更新以 <code>com.mycompany</code> 开头的键，而其他的键保持不变：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java">key -&gt; key.startsWith(<span class="hljs-string">&quot;com.mycompany&quot;</span>)<br>    ? ((oldValue, newValue) -&gt; newValue)<br>    : ((oldValue, newValue) -&gt; oldValue)<br></code></pre></td></tr></table></figure><p>还可以使用 <code>jconsole</code> 程序改变一个正在运行的程序的日志级别。有关的信息参见：</p><p><a href="https://www.oracle.com/technetwork/articles/java/jconsole-1564139.html#LoggingControl">https://www.oracle.com/technetwork/articles/java/jconsole-1564139.html#LoggingControl</a></p><blockquote><p>[!NOTE]</p><p><strong>注释</strong>：日志属性文件由 <code>java.util.logging.LogManager</code> 类处理。可以通过将 <code>java.util.logging.manager</code> 系统属性设置为某个子类的名字来指定一个不同的日志管理器。或者，可以保留标准日志管理器，而绕过从日志属性文件初始化。可以将 <code>java.util.logging.config.class</code> 系统属性设置为某个类名，该类再以另外某种方式设置日志管理器属性。有关的更多信息请参见 <code>LogManager</code> 类的 API 文档。</p></blockquote><h4 id="7-5-4-本地化"><a href="#7-5-4-本地化" class="headerlink" title="7.5.4 本地化"></a>7.5.4 本地化</h4><p>下面简要说明本地化日志消息时需要牢记的一些要点。</p><p>本地化的应用程序包含资源包（resource bundle）中的本地特定信息。资源包包括一组映射，分别对应各个本地化环境（如美国或德国）。例如，一个资源包可能将字符串 <code>&quot;readingFile&quot;</code> 映射为英文的 <code>&quot;Reading file&quot;</code> 或者德文的 <code>&quot;Achtung! Datei wird eingelesen&quot;</code>。</p><p>一个程序可以包含多个资源包，例如一个用于菜单，另一个用于日志消息。每个资源包都有一个名字（如 <code>&quot;com.mycompany.logmessages&quot;</code>）。要想为资源包增加映射，需要对应每个本地化环境提供一个文件。英文消息映射位于：</p><figure class="highlight avrasm"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs avrasm"><span class="hljs-keyword">com</span>/mycompany/logmessages_en.properties<br></code></pre></td></tr></table></figure><p>德文消息映射位于：</p><figure class="highlight avrasm"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs avrasm"><span class="hljs-keyword">com</span>/mycompany/logmessages_de.properties<br></code></pre></td></tr></table></figure><p>（<code>en</code> 和 <code>de</code> 是语言编码。）可以将这些文件与应用程序的类文件放在一起，以便 <code>ResourceBundle</code> 类自动找到它们。这些文件都是纯文本文件，包含如下所示的条目：</p><figure class="highlight properties"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs properties"><span class="hljs-attr">readingFile</span>=<span class="hljs-string">Achtung! Datei wird eingelesen</span><br><span class="hljs-attr">renamingFile</span>=<span class="hljs-string">Datei wird umbenannt</span><br></code></pre></td></tr></table></figure><p>请求一个日志记录器时，可以指定一个资源包：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">Logger</span> <span class="hljs-variable">logger</span> <span class="hljs-operator">=</span> Logger.getLogger(loggerName, <span class="hljs-string">&quot;com.mycompany.logmessages&quot;</span>);<br></code></pre></td></tr></table></figure><p>然后，为日志消息指定资源包的键，而不是具体的日志消息字符串：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">logger.info(<span class="hljs-string">&quot;readingFile&quot;</span>);<br></code></pre></td></tr></table></figure><p>通常需要在本地化的消息中包含一些参数，因此，消息可以包括占位符 <code>&#123;0&#125;</code>、<code>&#123;1&#125;</code> 等。例如，要想在日志消息中包含文件名，可以如下使用占位符：</p><figure class="highlight apache"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs apache"><span class="hljs-attribute">Reading</span> file &#123;<span class="hljs-number">0</span>&#125;<br><span class="hljs-attribute">Achtung</span>! Datei &#123;<span class="hljs-number">0</span>&#125; wird eingelesen<br></code></pre></td></tr></table></figure><p>然后，通过调用下面的一个方法向占位符传递具体的值：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java">logger.log(Level.INFO, <span class="hljs-string">&quot;readingFile&quot;</span>, fileName);<br>logger.log(Level.INFO, <span class="hljs-string">&quot;renamingFile&quot;</span>, <span class="hljs-keyword">new</span> <span class="hljs-title class_">Object</span>[] &#123; oldName, newName &#125;);<br></code></pre></td></tr></table></figure><p>或者，在 Java 9 中，可以在 <code>logrb</code> 方法中指定资源包对象（而不是名字）：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">Logger.logrb(Level.INFO, bundle, <span class="hljs-string">&quot;renamingFile&quot;</span>, oldName, newName);<br></code></pre></td></tr></table></figure><blockquote><p>[!NOTE]</p><p><strong>注释</strong>： 这是唯一一个可以为消息参数使用可变参数的日志记录方法。</p></blockquote><h4 id="7-5-5-处理器"><a href="#7-5-5-处理器" class="headerlink" title="7.5.5 处理器"></a>7.5.5 处理器</h4><p>在默认情况下，日志记录器将记录发送到 <code>ConsoleHandler</code>，它会将记录输出到 <code>System.err</code> 流。具体地，日志记录器会把记录发送到父处理器，而最终的祖先处理器（名为 “”）有一个 <code>ConsoleHandler</code>。  </p><p>与日志记录器一样，处理器也有日志级别。对于一个要记录的日志记录，它的日志级别必须高于日志记录器和处理器二者的阈值。日志管理器配置文件将默认的控制台处理器的日志级别设置为  </p><figure class="highlight stylus"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs stylus">java<span class="hljs-selector-class">.util</span><span class="hljs-selector-class">.logging</span><span class="hljs-selector-class">.ConsoleHandler</span>.level=INFO  <br></code></pre></td></tr></table></figure><p>要想记录 FINE 级别的日志，就必须修改配置文件中的默认日志记录器级别和处理器级别。或者，还可以绕过配置文件，安装你自己的处理器。  </p><figure class="highlight abnf"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs abnf">Logger logger <span class="hljs-operator">=</span> Logger.getLogger(<span class="hljs-string">&quot;com.mycompany.myapp&quot;</span>)<span class="hljs-comment">;</span><br><br>logger.setLevel(Level.FINE)<span class="hljs-comment">;</span><br><br>logger.setUseParentHandlers(false)<span class="hljs-comment">;</span><br><br>var handler <span class="hljs-operator">=</span> new ConsoleHandler()<span class="hljs-comment">;</span><br><br>handler.setLevel(Level.FINE)<span class="hljs-comment">;</span><br><br>logger.addHandler(handler)<span class="hljs-comment">;  </span><br></code></pre></td></tr></table></figure><p>在默认情况下，日志记录器将记录发送到自己的处理器和父日志记录器的处理器。我们的日志记录器是祖先日志记录器（名为 “”）的子类，而这个祖先日志记录器会把所有等于或高于 INFO 级别的记录发送到控制台。不过，我们并不想两次看到这些记录，因此应该将 <code>useParentHandlers</code> 属性设置为 <code>false</code>。  </p><p>要想将日志记录发送到其他地方，就要添加其他的处理器。日志 API 为此提供了两个很有用的处理器，一个是 <code>FileHandler</code>，另一个是 <code>SocketHandler</code>。<code>SocketHandler</code> 将记录发送到指定的主机和端口。而更令人感兴趣的是 <code>FileHandler</code>，它可以将记录收集到一个文件中。  </p><p>可以如下直接将记录发送到默认文件处理器：  </p><figure class="highlight abnf"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs abnf">var handler <span class="hljs-operator">=</span> new FileHandler()<span class="hljs-comment">;</span><br><br>logger.addHandler(handler)<span class="hljs-comment">;  </span><br></code></pre></td></tr></table></figure><p>这些记录被发送到用户主目录的 <code>java0.log</code> 文件中，n 是保证文件唯一的一个编号。如果用户系统没有主目录的概念（例如，在 <code>Windows 95/98/ME</code> 中），文件就存储在一个默认位置（如 C:\Windows）。默认情况下，记录会格式化为 XML。一个典型的日志记录形式如下：  </p><p>可以通过设置日志管理器配置文件中的不同参数（请参见表 7-1），或者使用另一个构造器来修改文件处理器的默认行为。</p><p><img src="/2025/05/04/hello-world/%E7%AC%AC%E4%B8%83%E7%AB%A0/image-20251122143231999.png" alt="image-20251122143231999"></p><p>以下文本完全按照你提供的图片格式（含段落、表7-2、提示框等）逐行提取，未增删空格与标点：</p><p>也有可能不想使用默认的日志文件名，因此，应该使用另一种模式，例如，</p><p><code>%h/myapp.log</code>（有关模式变量的解释请参见表 7-2）。</p><p><img src="/2025/05/04/hello-world/%E7%AC%AC%E4%B8%83%E7%AB%A0/image-20251122143433058.png" alt="image-20251122143433058"></p><p>如果多个应用程序（或者同一个应用程序的多个副本）使用同一个日志文件，就应该打开 <code>append</code> 标志。或者，应该在文件名模式中使用 <code>%u</code>，这样每个应用程序会创建日志的唯一副本。</p><p>打开文件循环功能也是一个不错的主意。日志文件以循环序列的形式保存（如 <code>myapp.log</code>, <code>myapp.log.1</code>, <code>myapp.log.2</code> 等）。只要文件超出了大小限制，最老的文件就会被删除，其他的文件将重新命名，同时创建一个新文件，其生成编号为 <code>0</code>。</p><p>提示：很多程序员将日志记录作为辅助文档提供给技术支持人员。如果程序的行为有误，用户可以发回日志文件来查看原因。在这种情况下，应该打开 <code>append</code> 标志，或者使用循环日志，也可以二者同时使用。</p><p>还可以通过扩展 <code>Handler</code> 类或 <code>StreamHandler</code> 类自定义处理器。在本节末尾的示例程序中就定义了这样一个处理器。这个处理器将在一个窗口中显示日志记录（如图 7-2 所示）。</p><p><img src="/2025/05/04/hello-world/%E7%AC%AC%E4%B8%83%E7%AB%A0/image-20251122143517705.png" alt="image-20251122143517705"></p><p>这个处理器扩展了 <code>StreamHandler</code> 类，并安装了一个流，这个流的 <code>write</code> 方法将流输出显示到一个文本区中。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">class</span> <span class="hljs-title class_">WindowHandler</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">StreamHandler</span> &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-title function_">WindowHandler</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-type">var</span> <span class="hljs-variable">output</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">JTextArea</span>();<br>        setOutputStream(<span class="hljs-keyword">new</span> <span class="hljs-title class_">OutputStream</span>() &#123;<br>            <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">write</span><span class="hljs-params">(<span class="hljs-type">int</span> b)</span> &#123; &#125; <span class="hljs-comment">// not called</span><br>            <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">write</span><span class="hljs-params">(<span class="hljs-type">byte</span>[] b, <span class="hljs-type">int</span> off, <span class="hljs-type">int</span> len)</span> &#123;<br>                output.append(<span class="hljs-keyword">new</span> <span class="hljs-title class_">String</span>(b, off, len));<br>            &#125;<br>        &#125;);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>使用这种方式只有一个问题，这就是处理器会缓存记录，并且只有在缓冲区满的时候才将它们写入流中。因此，需要覆盖 <code>publish</code> 方法，使得处理器获得每个记录之后就会刷新输出缓冲区。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">class</span> <span class="hljs-title class_">WindowHandler</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">StreamHandler</span> &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">publish</span><span class="hljs-params">(LogRecord record)</span> &#123;<br>        <span class="hljs-built_in">super</span>.publish(record);<br>        flush();<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>如果希望编写更加复杂的流处理器，可以扩展 Handler 类，并定义 publish、flush 和 close 方法。</p><h4 id="7-5-6-过滤器"><a href="#7-5-6-过滤器" class="headerlink" title="7.5.6 过滤器"></a>7.5.6 过滤器</h4><p>在默认情况下，会根据日志记录的级别进行过滤。每个日志记录器和处理器都可以有一个可选的过滤器来完成额外的过滤。要定义一个过滤器，需要实现 <code>Filter</code> 接口并定义以下方法：  </p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">boolean</span> <span class="hljs-title function_">isLoggable</span><span class="hljs-params">(LogRecord record)</span><br></code></pre></td></tr></table></figure><p>在这个方法中，可以使用你喜欢的标准分析日志记录，对那些应该包含在日志中的记录返回 true。例如，某个过滤器可能只对 <code>entering</code> 方法和 <code>exiting</code> 方法生成的消息感兴趣，这个过滤器就可以调用 <code>record.getMessage()</code> 方法，并检查消息是否以 ENTRY 或 RETURN 开头。</p><p>要想将一个过滤器安装到一个日志记录器或处理器中，只需要调用 <code>setFilter</code> 方法。注意，一次最多只能有一个过滤器。</p><h4 id="7-5-7-格式化器"><a href="#7-5-7-格式化器" class="headerlink" title="7.5.7 格式化器"></a>7.5.7 格式化器</h4><p><code>ConsoleHandler</code> 类和 <code>FileHandler</code> 类可以生成文本和 XML 格式的日志记录。不过，你也可以自定义格式。这需要扩展 <code>Formatter</code> 类并覆盖下面这个方法：  </p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">String <span class="hljs-title function_">format</span><span class="hljs-params">(LogRecord record)</span><br></code></pre></td></tr></table></figure><p>可以用你喜欢的任何方式对记录中的信息进行格式化，并返回结果字符串。在 <code>format</code> 方法中，可能会调用下面这个方法：  </p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">String <span class="hljs-title function_">formatMessage</span><span class="hljs-params">(LogRecord record)</span><br></code></pre></td></tr></table></figure><p>这个方法对记录中的消息部分进行格式化，将替换参数并应用本地化处理。</p><p>很多文件格式（如 XML）需要在已格式化的记录的前后加上一个头部和尾部。为此，要覆盖下面两个方法：  </p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java">String <span class="hljs-title function_">getHead</span><span class="hljs-params">(Handler h)</span><br>String <span class="hljs-title function_">getTail</span><span class="hljs-params">(Handler h)</span><br></code></pre></td></tr></table></figure><p>最后，调用 <code>setFormatter</code> 方法将格式化器安装到处理器中。</p><h4 id="7-5-8-日志技巧"><a href="#7-5-8-日志技巧" class="headerlink" title="7.5.8 日志技巧"></a>7.5.8 日志技巧</h4><p>面对日志记录如此之多的选项，很容易让人忘记了最基本的东西。下面的技巧总结了一些最常用的操作。  </p><ol><li>对一个简单的应用，选择一个日志记录器。可以把日志记录器命名为与主应用包同名，例如，<code>com.mycompany.myprog</code>，这是一个好主意。总是可以通过以下调用得到日志记录器：</li></ol><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">Logger</span> <span class="hljs-variable">logger</span> <span class="hljs-operator">=</span> Logger.getLogger(<span class="hljs-string">&quot;com.mycompany.myprog&quot;</span>);<br></code></pre></td></tr></table></figure><p>为方便起见，你可能希望为有大量日志记录活动的类增加静态字段：</p><p>以下文本完全依照图片中的缩进、换行、代码段及注释格式逐字提取，未增删任何字符：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">Logger</span> <span class="hljs-variable">logger</span> <span class="hljs-operator">=</span> Logger.getLogger(<span class="hljs-string">&quot;com.mycompany.myprog&quot;</span>);<br></code></pre></td></tr></table></figure><ol start="2"><li>默认的日志配置会把级别等于或高于 INFO 的所有消息记录到控制台。用户可以覆盖这个默认配置。但是正如前面所述，改变配置的过程有些复杂。因此，最好在你的应用中安装一个更合适的默认日志处理器。</li></ol><p>以下代码确保将所有的消息记录到应用特定的一个文件中。可以将这段代码放置在应用程序的 main 方法中。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">if</span> (System.getProperty(<span class="hljs-string">&quot;java.util.logging.config.class&quot;</span>) == <span class="hljs-literal">null</span><br>        &amp;&amp; System.getProperty(<span class="hljs-string">&quot;java.util.logging.config.file&quot;</span>) == <span class="hljs-literal">null</span>)<br>    <span class="hljs-keyword">try</span> &#123;<br>        Logger.getLogger(<span class="hljs-string">&quot;&quot;</span>).setLevel(Level.ALL);<br>        <span class="hljs-keyword">final</span> <span class="hljs-type">int</span> <span class="hljs-variable">LOG_ROTATION_COUNT</span> <span class="hljs-operator">=</span> <span class="hljs-number">10</span>;<br>        <span class="hljs-type">var</span> <span class="hljs-variable">handler</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">FileHandler</span>(<span class="hljs-string">&quot;%h/myapp.log&quot;</span>, <span class="hljs-number">0</span>, LOG_ROTATION_COUNT);<br>        Logger.getLogger(<span class="hljs-string">&quot;&quot;</span>).addHandler(handler);<br>    &#125; <span class="hljs-keyword">catch</span> (IOException e) &#123;<br>        logger.log(Level.SEVERE, <span class="hljs-string">&quot;Can&#x27;t create log file handler&quot;</span>, e);<br>    &#125;<br></code></pre></td></tr></table></figure><ol start="3"><li>现在，可以记录自己想要的内容了。需要牢记：所有级别为 INFO、WARNING 和 SEVERE 的消息都将显示到控制台上。因此，要记录对程序用户有意义的消息，可以使用这几个级别。</li></ol><p>对于程序员想要的日志消息，FINE 级别是一个很好的选择。</p><p>想要调用 <code>System.out.println</code> 时，可以换成发出以下日志消息：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">logger.fine(<span class="hljs-string">&quot;File open dialog canceled&quot;</span>);<br></code></pre></td></tr></table></figure><p>记录那些预料之外的异常也是一个不错的想法，例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">try</span> &#123;<br>    ...<br>&#125; <span class="hljs-keyword">catch</span> (SomeException e) &#123;<br>    logger.log(Level.FINE, <span class="hljs-string">&quot;explanation&quot;</span>, e);<br>&#125;<br></code></pre></td></tr></table></figure>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>《Java核心技术卷一》第六章</title>
    <link href="/2025/11/11/%E3%80%8AJava%E6%A0%B8%E5%BF%83%E6%8A%80%E6%9C%AF%E5%8D%B7%E4%B8%80%E3%80%8B%E7%AC%AC%E5%85%AD%E7%AB%A0/"/>
    <url>/2025/11/11/%E3%80%8AJava%E6%A0%B8%E5%BF%83%E6%8A%80%E6%9C%AF%E5%8D%B7%E4%B8%80%E3%80%8B%E7%AC%AC%E5%85%AD%E7%AB%A0/</url>
    
    <content type="html"><![CDATA[<h2 id="第6章-接口、lambda-表达式与内部类"><a href="#第6章-接口、lambda-表达式与内部类" class="headerlink" title="第6章 接口、lambda 表达式与内部类"></a>第6章 接口、lambda 表达式与内部类</h2><p>首先介绍第一种技术，即<strong>接口（interface）</strong>，<strong>接口用来描述类应该做什么，而不指定它们具体应该如何做</strong>。一个类可以实现（implement）一个或多个接口。只要符合所要求的接口，就可以使用实现了这个接口的类（即实现类）的对象。讨论接口以后，我们会继续介绍 lambda 表达式，这是一种简洁的方法，用来创建可以在将来某个时间点执行的代码块。通过使用 lambda 表达式，可以用一种精巧而简洁的方式表示使用回调或可变行为的代码。</p><p>接下来，我们将讨论内部类（inner class）机制。理论上讲，内部类有些复杂，内部类定义在另外一个类的内部，它们的方法可以访问其外部类的字段。内部类技术在设计合作类集合时很有用。</p><h3 id="6-1-接口"><a href="#6-1-接口" class="headerlink" title="6.1 接口"></a>6.1 接口</h3><p>在下面的小节中，你会了解 Java 接口是什么以及如何使用，另外还会了解 Java 最新的儿个版本中接口的功能有怎样的提升。</p><h4 id="6-1-1-接口的概念"><a href="#6-1-1-接口的概念" class="headerlink" title="6.1.1 接口的概念"></a>6.1.1 接口的概念</h4><p><strong>在 Java 程序设计语言中，接口不是类，而是对希望符合这个接口的类的一组需求</strong>。</p><p>我们经常听到某个服务的提供商这样说：“如果你的类符合某个特定接口，我就会履行这项服务。”下面给出一个具体的示例。<code>Arrays</code>类中的<code>sort</code>方法承诺可以对对象数组进行排序，但要求满足下面这个条件：对象所属的类必须实现<code>Comparable</code>接口。</p><p>下面是<code>Comparable</code>接口的代码：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">interface</span> <span class="hljs-title class_">Comparable</span> &#123;<br>    <span class="hljs-type">int</span> <span class="hljs-title function_">compareTo</span><span class="hljs-params">(Object other)</span>;<br>&#125;<br></code></pre></td></tr></table></figure><p><strong>在这个接口中，compareTo 方法是抽象的，它没有具体实现。任何实现 Comparable 接口的类都需要包含一个 compareTo 方法，这个方法必须接受一个 Object 参数，并返回一个整数。否则，这个类也应当是抽象的，也就是说，你不能构造这个类的对象。</strong></p><blockquote><p>注释：在 Java 5 中，Comparable 接口已经提升为一个泛型类型。  </p><p>例如，在实现 <code>Comparable&lt;Employee&gt;</code> 接口的类中，必须提供以下方法：  </p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">interface</span> <span class="hljs-title class_">Comparable</span>&lt;T&gt; &#123;<br>    <span class="hljs-type">int</span> <span class="hljs-title function_">compareTo</span><span class="hljs-params">(T other)</span>; <span class="hljs-comment">// parameter has type T</span><br>    &#125;<br></code></pre></td></tr></table></figure><p>仍然可以使用不带类型参数的 “ 原始 ” Comparable 类型。这样一来，compareTo 方法就有一个 Object 类型的参数，你必须手动将 compareTo 方法的这个参数强制转换为所希望的类型。稍后我们再做这个工作，所以不用担心同时学习两个新概念。</p></blockquote><p><strong>接口中的所有方法都自动是 public 方法。因此，在接口中声明方法时，不必提供关键字 public 。</strong></p><p>当然，还有一个接口没有明确说明的额外要求：调用 <code>x.compareTo(y)</code> 的时候，这个 compareTo 方法实际上必须能够比较两个对象，并返回比较的结果，即 x 和 y 哪一个更大。当 x 小于 y 时，返回一个负数；当 x 等于 y 时，返回 0 ；否则返回一个正数。</p><p>这个特定接口只有一个方法，而有些接口可能包含多个方法。稍后可以看到，接口还可以定义常量。不过，更重要的是要知道接口不能提供什么。<strong>接口绝不会有实例字段，在 Java 8 之前，接口中的方法都是抽象方法</strong>。（在6.1.4节和6.1.5节中可以看到，现在接口中还可以有其他方法。当然，那些方法不能引用实例字段——接口没有实例。）</p><p>现在，假设希望使用 Arrays 类的 sort 方法对 Employee 对象数组进行排序，Employee 类就必须实现 Comparable 接口。</p><p>为了让类实现一个接口，需要完成下面两个步骤：</p><ol><li><strong>将类声明为实现给定的接口。</strong>  </li><li><strong>对接口中的所有方法提供定义。</strong></li></ol><p>要声明一个类实现某个接口，需要使用关键字 implements：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Employee</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">Comparable</span><br></code></pre></td></tr></table></figure><p>当然，现在 Employee 类需要提供 compareTo 方法。假设我们希望根据员工的薪水进行比较。以下是 compareTo 方法的一个实现：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-type">int</span> <span class="hljs-title function_">compareTo</span><span class="hljs-params">(Object otherObject)</span> &#123;<br>    <span class="hljs-type">Employee</span> <span class="hljs-variable">other</span> <span class="hljs-operator">=</span> (Employee) otherObject;<br>    <span class="hljs-keyword">return</span> Double.compare(salary, other.salary);<br>&#125;<br></code></pre></td></tr></table></figure><p>在这里，我们使用了静态 <code>Double.compare</code> 方法。如果第一个参数小于第二个参数，它会返回一个负值；如果二者相等则返回 0；否则返回一个正值。</p><blockquote><p>⚠️ <strong>警告</strong>：在接口声明中，没有将 compareTo 方法声明为 public ，这是因为，接口中的所有方法都自动是 public 方法。不过，<strong>在实现接口时，必须把方法声明为 public</strong>，否则，编译器将认为这个方法的访问属性是包可访问，这是类中默认的访问属性，之后编译器就会报错，指出你试图提供更严格的访问权限。</p></blockquote><p>我们可以做得更好一些。可以为泛型 Comparable 接口提供一个类型参数：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Employee</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">Comparable</span>&lt;Employee&gt; &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-type">int</span> <span class="hljs-title function_">compareTo</span><span class="hljs-params">(Employee other)</span> &#123;<br>        <span class="hljs-keyword">return</span> Double.compare(salary, other.salary);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>请注意，对 Object 参数进行强制类型转换总是让人感觉不太顺眼，但现在已经不见了。</p><blockquote><p><strong>提示</strong>：Comparable 接口中的 compareTo 方法将返回一个整数。如果两个对象不相等，返回哪个正值或者负值并不重要。在对两个整数字段进行比较时，这种灵活性非常有用。例如，假设每个员工都有一个唯一的整数id，你希望根据员工ID号进行排序，那么可以直接返回 <code>id - other.id</code>。如果第一个ID号小于另一个ID，这个值将是一个负值；如果两个ID相等，这个值就是0；否则，这将是一个正值。但有一点需要注意：整数的范围要足够小，以避免减法运算溢出。如果能够确信ID为非负数，或者它们的绝对值不会超过 <code>(Integer.MAX_VALUE - 1) / 2</code>，就不会出现问题。否则，可以调用静态 <code>Integer.compare</code> 方法。</p></blockquote><p>当然，这里的相减技巧不适用于浮点数。因为如果 salary 和 other.salary 很接近但又不相等，它们的差经过四舍五入后有可能变成 0。如果 x &lt; y，<code>Double.compare(x, y)</code> 调用会返回 -1；如果 x &gt; y 则返回1。</p><blockquote><p><strong>注释</strong>：Comparable 接口的文档建议 compareTo 方法应当与 equals 方法兼容。也就是说，当 <code>x.equals(y)</code> 时，<code>x.compareTo(y)</code> 就应当等于0。Java API 中大多数实现 Comparable 接口的类都遵从了这个建议。不过有一个重要的例外，就是 <code>BigDecimal</code> 。考虑 <code>x = new BigDecimal(&quot;1.0&quot;)</code> 和 <code>y = new BigDecimal(&quot;1.00&quot;)</code>。这里 <code>x.equals(y)</code> 为false，因为两个数的精度不同。不过 <code>x.compareTo(y)</code> 为0。理想情况下应该不返回0，但是没有明确的方法能够确定这两个数哪一个更大。</p></blockquote><p>现在，我们已经看到，要让一个类使用排序服务必须让它实现 compareTo 方法。这是理所当然的，因为要向 sort 方法提供对象的比较方式。但是为什么不能在 Employee 类中直接提供一个 compareTo 方法（而不实现Comparable接口）呢？</p><p>使用接口的主要原因在于：Java程序设计语言是一种<strong>强类型（strongly typed）语言</strong>。调用方法时，编译器要能检查这个方法确实存在。在 sort 方法中可能会有类似下面的语句：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">if</span> (a[i].compareTo(a[j]) &gt; <span class="hljs-number">0</span>)<br>&#123;<br>    <span class="hljs-comment">//rearrange a[i] and a[j]</span><br>    . . .<br>&#125;<br></code></pre></td></tr></table></figure><p>编译器必须确认 <code>a[i]</code> 一定有一个 <code>compareTo</code> 方法。如果 <code>a</code> 是一个 <code>Comparable</code> 对象的数组，就可以确保肯定有 <code>compareTo</code> 方法，因为每个实现 <code>Comparable</code> 接口的类都必须提供这个方法。</p><blockquote><p><strong>注释</strong>：你可能认为，如果将 <code>Arrays</code> 类中的 <code>sort</code> 方法定义为接受一个 <code>Comparable</code> 数组，倘若有人调用 <code>sort</code> 方法时所提供数组的元素类型没有实现 <code>Comparable</code> 接口，编译器就能报错。遗憾的是，事实并非如此。实际上，<code>sort</code> 方法接受一个 <code>Object</code> 数组，并使用一个笨拙的强制类型转换：  </p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs java"> <span class="hljs-comment">// approach used in the standard library -- not recommended</span><br><span class="hljs-keyword">if</span> (((Comparable) a[i]).compareTo(a[j]) &gt; <span class="hljs-number">0</span>)<br>&#123;<br> <span class="hljs-comment">//rearrange a[i] and a[j]</span><br>    . . .<br>&#125;<br></code></pre></td></tr></table></figure><p>如果 <code>a[i]</code> 不属于一个实现了 <code>Comparable</code> 接口的类，虚拟机就会抛出一个异常。</p></blockquote><blockquote><p><strong>注释</strong>：语言标准规定：“对于任意的 x 和 y，实现者必须确保 <code>sgn（x.compareTo(y））= -sgn(y.compareTo（x））</code>（也就是说，如果 y.compareTo（x）抛出一个异常，那么 x.compareTo（y）也应该抛出一个异常。）”这里的 sgn 是一个数的<strong>符号</strong>：如果 n 是负值，sgn(n）为 -1；如果 n 等于 0，sgn（n）为 0 ；如果 n 是正值，sgn(n）为 1。简单地讲，如果翻转 compareTo 的参数，结果的符号也应该翻转（但具体值不一定）。</p><p>与 equals 方法一样，使用继承时有可能会出现问题。</p><p>这是因为 Manager 扩展了 Employee，而 Employee 实现了 <code>Comparable&lt;Employee&gt;</code>，而不是 <code>Comparable&lt;Manager&gt;</code>。如果 Manager 要盖 compareTo，就必须做好准备比较经理与员工，绝不能简单地将员工强制转换成经理：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Manager</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Employee</span> &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-type">int</span> <span class="hljs-title function_">compareTo</span><span class="hljs-params">(Employee other)</span> &#123;<br>        <span class="hljs-type">Manager</span> <span class="hljs-variable">otherManager</span> <span class="hljs-operator">=</span> (Manager) other; <span class="hljs-comment">// No</span><br>        ...<br>    &#125;<br>    ...<br>&#125;<br></code></pre></td></tr></table></figure><p>违反了 “ 反对称 ” 规则。如果 x 是一个 Employee 对象，y 是一个 Manager 对象，调用 x.compareTo(y）不会抛出异常，它只是将 x 和 y 都作为员工进行比较。但是反过来，y.compareTo（x）将会抛出一个ClassCastException。</p><p>这种情况与第5章中讨论的 equals 方法一样，补救方式也一样。有两种不同的情况。</p><p>如果不同子类中的比较有不同的含义，就应该将属于不同类的对象之间的比较视为非法。每个 compareTo 方法首先都应该进行以下检测：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">if</span> (getClass() != other.getClass()) <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">ClassCastException</span>();<br></code></pre></td></tr></table></figure><p>如果存在一个比较子类对象的通用算法，那么只需要在超类中提供一个 compareTo 方法，并将这个方法声明为final。</p><p>例如，假设你希望经理大于普通员工，而不论薪水多少，那么诸如 Executive 和 Secretary 等其他子类呢？如果要按照职位排序，那就应该在 Employee 类中提供一个rank方法。让每个子类覆盖 rank，并实现一个考虑 rank值的 compareTo 方法。</p></blockquote><h4 id="6-1-2-接口的属性"><a href="#6-1-2-接口的属性" class="headerlink" title="6.1.2 接口的属性"></a>6.1.2 接口的属性</h4><p>**接口不是类。**具体来说，不能使用 <code>new</code> 操作符实例化一个接口：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">x = <span class="hljs-keyword">new</span> <span class="hljs-title class_">Comparable</span>(...); <span class="hljs-comment">// ERROR</span><br></code></pre></td></tr></table></figure><p>尽管不能构造接口对象，但仍然<strong>能声明接口变量</strong>：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">Comparable x; <span class="hljs-comment">// OK</span><br></code></pre></td></tr></table></figure><p><strong>接口变量必须引用实现了这个接口的一个类对象</strong>：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">x = <span class="hljs-keyword">new</span> <span class="hljs-title class_">Employee</span>(...); <span class="hljs-comment">// OK provided Employee implements Comparable</span><br></code></pre></td></tr></table></figure><p>如同使用 <code>instanceof</code> 检查一个对象是否属于某个特定类一样，也<strong>可以使用 <code>instanceof</code> 检查一个对象是否实现了某个特定的接口</strong>：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">if</span> (anObject <span class="hljs-keyword">instanceof</span> Comparable) &#123; ... &#125;<br></code></pre></td></tr></table></figure><p>与建立类的继承层次结构一样，也可以扩展接口。这允许有多条接口链，从通用性较高的接口扩展到专用性较高的接口。例如，假设有一个名为 <code>Moveable</code> 的接口：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">interface</span> <span class="hljs-title class_">Moveable</span> &#123;<br>    <span class="hljs-keyword">void</span> <span class="hljs-title function_">move</span><span class="hljs-params">(<span class="hljs-type">double</span> x, <span class="hljs-type">double</span> y)</span>;<br>&#125;<br></code></pre></td></tr></table></figure><p>然后，可以假设一个名为 <code>Powered</code> 的接口扩展了以上 <code>Moveable</code> 接口：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">interface</span> <span class="hljs-title class_">Powered</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Moveable</span> &#123;<br>    <span class="hljs-type">double</span> <span class="hljs-title function_">milesPerGallon</span><span class="hljs-params">()</span>;<br>&#125;<br></code></pre></td></tr></table></figure><p><strong>虽然接口中不能包含实例字段，但是可以包含常量</strong>。例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">interface</span> <span class="hljs-title class_">Powered</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Moveable</span> &#123;<br>    <span class="hljs-type">double</span> <span class="hljs-title function_">milesPerGallon</span><span class="hljs-params">()</span>;<br>    <span class="hljs-type">double</span> <span class="hljs-variable">SPEED_LIMIT</span> <span class="hljs-operator">=</span> <span class="hljs-number">95</span>; <span class="hljs-comment">// a public static final constant</span><br>&#125;<br></code></pre></td></tr></table></figure><p>接口中的方法都自动为 <code>public</code>，类似地，接口中的字段总是 <code>public static final</code>。</p><blockquote><p>**注释：**可以将接口方法显式标记为 <code>public</code>，将字段标记为 <code>public static final</code>，这是合法的。有些程序员出于习惯或者提高清晰度的考虑，可能会这样做。但 Java 语言规范建议不要提供冗余的关键字，本书也采纳了这个建议。</p></blockquote><p>尽管每个类只能有一个超类，但可以实现多个接口。这就为定义类的行为提供了极大的灵活性。例如，Java 程序设计语言有一个非常重要的内置接口，名为 <code>Cloneable</code>。如果你的类实现了这个 <code>Cloneable</code> 接口，<code>Object</code> 类中的 <code>clone</code> 方法就可以创建你的类对象的一个完全副本。如果你希望自己设计的类既能够克隆又能够比较，只要实现这两个接口就可以了。可以使用逗号将想要实现的各个接口分隔开。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Employee</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">Cloneable</span>, Comparable<br></code></pre></td></tr></table></figure><blockquote><p><strong>注释</strong>： <strong>记录和枚举类不能扩展其他类</strong>（因为它们隐式地扩展了 <code>Record</code> 和 <code>Enum</code> 类）。不过，它们可以实现接口。</p></blockquote><blockquote><p><strong>注释</strong>： <strong>接口可以是密封的</strong>（<code>sealed</code>）。与密封类一样，直接子类型（可以是类或接口）必须在 <code>permits</code> 子句中声明，或者要放在同一个源文件中。</p></blockquote><h4 id="6-1-3-接口与抽象类"><a href="#6-1-3-接口与抽象类" class="headerlink" title="6.1.3 接口与抽象类"></a>6.1.3 接口与抽象类</h4><p>如果阅读了第5章中有关抽象类的那一节，可能会产生这样一个疑问：为什么 Java 程序设计语言的设计者要那么麻烦地引入接口概念呢？为什么不将 <code>Comparable</code> 直接设计成一个抽象类呢？如下所示：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">abstract</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Comparable</span> &#123; <span class="hljs-comment">// why not?</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">abstract</span> <span class="hljs-type">int</span> <span class="hljs-title function_">compareTo</span><span class="hljs-params">(Object other)</span>;<br>&#125;<br></code></pre></td></tr></table></figure><p>这样一来，<code>Employee</code> 类只需要扩展这个抽象类，并提供 <code>compareTo</code> 方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Employee</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Comparable</span> &#123; <span class="hljs-comment">// why not?</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-type">int</span> <span class="hljs-title function_">compareTo</span><span class="hljs-params">(Object other)</span> &#123; ... &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>非常遗憾，使用抽象基类表示通用属性存在一个严重的问题。每个类只能扩展一个类。假设 <code>Employee</code> 类已经扩展了另一个类，例如 <code>Person</code>，它就不能再扩展第二个类了：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Employee</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Person</span>, Comparable <span class="hljs-comment">// ERROR</span><br></code></pre></td></tr></table></figure><p>但每个类可以实现任意多个接口，如下所示：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Employee</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Person</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">Comparable</span> <span class="hljs-comment">// OK</span><br></code></pre></td></tr></table></figure><p>其他程序设计语言（尤其是 C++）允许一个类有多个超类。这个特性称为<strong>多重继承（multiple inheritance）</strong>。Java 的设计者选择不支持多重继承，其主要原因是多重继承会让语言变得非常复杂（如 C++），或者效率会降低（如 Eiffel）。</p><p>实际上，接口可以提供多重继承的大多数好处，同时还能避免多重继承的复杂性和低效性。</p><h4 id="6-1-4-静态和私有方法"><a href="#6-1-4-静态和私有方法" class="headerlink" title="6.1.4 静态和私有方法"></a>6.1.4 静态和私有方法</h4><p>在 Java 8 中，允许在接口中增加静态方法。理论上讲，没有任何理由认为这是不合法的。只是这似乎有违于将接口作为抽象规范的初衷。</p><p>目前为止，通常的做法都是将静态方法放在伴随类中。在标准库中，你会看到成对出现的接口和实用工具类，如 <code>Collection</code>&#x2F;<code>Collections</code> 或 <code>Path</code>&#x2F;<code>Paths</code>。</p><p>可以由一个 URI 或者字符串序列构造一个文件或目录的路径，如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">Paths.get(<span class="hljs-string">&quot;jdk-17&quot;</span>, <span class="hljs-string">&quot;conf&quot;</span>, <span class="hljs-string">&quot;security&quot;</span>);<br></code></pre></td></tr></table></figure><p>在 Java 11 中，<code>Path</code> 接口提供了等价的方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">interface</span> <span class="hljs-title class_">Path</span> &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> Path <span class="hljs-title function_">of</span><span class="hljs-params">(URI uri)</span> &#123; ... &#125;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> Path <span class="hljs-title function_">of</span><span class="hljs-params">(String first, String... more)</span> &#123; ... &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>这样一来，<code>Paths</code> 类就不再是必要的了。</p><p>类似地，实现你自己的接口时，没有理由再为实用工具方法另外提供一个伴随类。</p><p>在 Java 9 中，接口中的方法可以是 <code>private</code> 方法。<code>private</code> 方法可以是静态方法或实例方法。由于私有方法只能在接口本身的方法中使用，所以它们的用途很有限，只是作为接口中其他方法的辅助方法。</p><h4 id="6-1-5-默认方法"><a href="#6-1-5-默认方法" class="headerlink" title="6.1.5 默认方法"></a>6.1.5 默认方法</h4><p>可以为任何接口方法提供一个默认实现。必须用 <code>default</code> 修饰符标记这样一个方法。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">interface</span> <span class="hljs-title class_">Comparable</span>&lt;T&gt; &#123;<br>    <span class="hljs-keyword">default</span> <span class="hljs-type">int</span> <span class="hljs-title function_">compareTo</span><span class="hljs-params">(T other)</span> &#123; <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>; &#125;  <span class="hljs-comment">// by default, all elements are the same</span><br>&#125;<br></code></pre></td></tr></table></figure><p>当然，这并没有太大用处，因为 <code>Comparable</code> 的每一个具体实现都会覆盖这个方法。不过有些情况下，默认方法可能很有用。例如，在第9章会看到一个 <code>Iterator</code> 接口，用于访问一个数据结构中的元素。这个接口声明了一个 <code>remove</code> 方法，如下所示：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">interface</span> <span class="hljs-title class_">Iterator</span>&lt;E&gt; &#123;<br>    <span class="hljs-type">boolean</span> <span class="hljs-title function_">hasNext</span><span class="hljs-params">()</span>;<br>    E <span class="hljs-title function_">next</span><span class="hljs-params">()</span>;<br><span class="hljs-keyword">default</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">remove</span><span class="hljs-params">()</span> &#123; <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">UnsupportedOperationException</span>(<span class="hljs-string">&quot;remove&quot;</span>); &#125;<br>    ...<br>&#125;<br></code></pre></td></tr></table></figure><p>如果你要实现一个迭代器，就需要提供 <code>hasNext</code> 和 <code>next</code> 方法。这些方法没有默认实现——它们依赖于你要遍历访问的数据结构。不过，如果你的迭代器是只读的，就不用操心实现 <code>remove</code> 方法。</p><p><strong>默认方法可以调用其他方法</strong>。例如，<code>Collection</code> 接口可以定义一个便利方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">interface</span> <span class="hljs-title class_">Collection</span> &#123;<br>    <span class="hljs-type">int</span> <span class="hljs-title function_">size</span><span class="hljs-params">()</span>; <span class="hljs-comment">// an abstract method</span><br>    <span class="hljs-keyword">default</span> <span class="hljs-type">boolean</span> <span class="hljs-title function_">isEmpty</span><span class="hljs-params">()</span> &#123; <span class="hljs-keyword">return</span> size() == <span class="hljs-number">0</span>; &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>这样实现 <code>Collection</code> 的程序员就不用再操心实现 <code>isEmpty</code> 方法了。</p><blockquote><p><strong>注释</strong>：Java API 中的 <code>Collection</code> 接口并没有这样做。实际上，有一个 <code>AbstractCollection</code> 类实现了 <code>Collection</code>，并利用 <code>size</code> 定义了 <code>isEmpty</code>。建议实现集合的程序员扩展 <code>AbstractCollection</code>。不过那个技术已经过时，现在可以直接在接口中实现方法。</p></blockquote><p>默认方法的一个重要用法是 <strong>“ 接口演化 ”（interface evolution）</strong>。以 <code>Collection</code> 接口为例，这个接口作为 Java 的一部分已经有很多年了。假设很久以前你提供了这样一个类：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Bag</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">Collection</span> &#123;<br>    ...<br>&#125;<br></code></pre></td></tr></table></figure><p>后来，在 Java 8 中，又为这个接口增加了一个 <code>stream</code> 方法。</p><p>假设 <code>stream</code> 方法不是一个默认方法，那么 <code>Bag</code> 类将不能编译，因为它没有实现这个新方法。为接口增加一个非默认方法不能保证 <strong>“ 源代码兼容 ”（source compatible）</strong>。</p><p>不过，假设不重新编译这个类，而只是使用原先的一个包含这个类的 JAR 文件。这个类仍能正常加载，尽管没有这个新方法。程序仍然可以正常构造 <code>Bag</code> 实例，不会有意外发生。（为接口增加方法可以做到“二进制兼容”。）不过，如果一个程序在一个 <code>Bag</code> 实例上调用 <code>stream</code> 方法，就会出现一个 <code>AbstractMethodError</code>。</p><p>将方法实现为一个默认（default）方法就可以解决这两个问题。<code>Bag</code> 类又能正常编译了。另外如果没有重新编译而直接加载这个类，并在一个 <code>Bag</code> 实例上调用 <code>stream</code> 方法，则会调用 <code>Collection.stream</code> 方法。</p><h4 id="6-1-6-解决默认方法冲突"><a href="#6-1-6-解决默认方法冲突" class="headerlink" title="6.1.6 解决默认方法冲突"></a>6.1.6 解决默认方法冲突</h4><p>如果先在一个接口中将一个方法定义为默认方法，然后又在超类或另一个接口中定义了同样的方法，会发生什么情况？诸如 Scala 和 C++ 等语言对于解决这种二义性有一些复杂的规则。幸运的是，Java 的相应规则要简单得多。规则如下：</p><ol><li><p><strong>超类优先</strong>。如果超类提供了一个具体方法，同名而且有相同参数类型的默认方法会被忽略。</p></li><li><p><strong>接口冲突</strong>。如果一个接口提供了一个默认方法，另一个接口提供了一个同名而且参数类型相同的方法（不论是否是默认方法），必须覆盖这个方法来解决冲突。</p></li></ol><p>下面来看第二个规则。考虑两个包含 <code>getName</code> 方法的接口：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">interface</span> <span class="hljs-title class_">Person</span> &#123;<br>    <span class="hljs-keyword">default</span> String <span class="hljs-title function_">getName</span><span class="hljs-params">()</span> &#123; <span class="hljs-keyword">return</span> <span class="hljs-string">&quot;&quot;</span>; &#125;<br>&#125;<br><br><span class="hljs-keyword">interface</span> <span class="hljs-title class_">Named</span> &#123;<br>    <span class="hljs-keyword">default</span> String <span class="hljs-title function_">getName</span><span class="hljs-params">()</span> &#123; <span class="hljs-keyword">return</span> getClass().getName() + <span class="hljs-string">&quot;_&quot;</span> + hashCode(); &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>如果有一个类同时实现了这两个接口会怎么样呢？</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Student</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">Person</span>, Named &#123; ... &#125;<br></code></pre></td></tr></table></figure><p>这个类会继承 <code>Person</code> 和 <code>Named</code> 接口提供的两个不一致的 <code>getName</code> 方法。并不是从中选择一个，Java 编译器会报告一个错误，让程序员来解决这个二义性问题。只需要在 <code>Student</code> 类中提供一个 <code>getName</code> 方法即可。在这个方法中，可以选择两个冲突方法中的一个，如下所示：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Student</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">Person</span>, Named &#123;<br>    <span class="hljs-keyword">public</span> String <span class="hljs-title function_">getName</span><span class="hljs-params">()</span> &#123; <span class="hljs-keyword">return</span> Person.<span class="hljs-built_in">super</span>.getName(); &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>现在假设 <code>Named</code> 接口没有为 <code>getName</code> 提供默认实现：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">interface</span> <span class="hljs-title class_">Named</span> &#123;<br>    String <span class="hljs-title function_">getName</span><span class="hljs-params">()</span>;<br>&#125;<br></code></pre></td></tr></table></figure><p><code>Student</code> 类会从 <code>Person</code> 接口继承默认方法吗？这好像挺合理，不过，Java 设计者决定更强凋一致性。两个接口如何冲突并不重要。如果至少有一个接口提供了一个实现，编译器就会报告错误，必须由程序员解决这个二义性。</p><blockquote><p><strong>注释</strong>：当然，如果两个接口都没有为共享方法提供默认实现，那么就与 Java 8 之前的情况一样，这里不存在冲突。实现类可以有两个选择：实现这个方法，或者干脆不实现。如果是后一种情况，这个类本身就是抽象的。</p></blockquote><p>我们只讨论了两个接口的命名冲突。现在来考虑另一种情况，一个类扩展了一个超类，同时实现了一个接口，并从超类和接口继承了相同的方法。例如，假设 <code>Person</code> 是一个类，<code>Student</code> 定义为：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Student</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Person</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">Named</span> &#123; ... &#125;<br></code></pre></td></tr></table></figure><p>在这种情况下，只会考虑超类方法，接口的所有默认方法都会被忽略。在我们的例子中，<code>Student</code> 从 <code>Person</code> 继承了 <code>getName</code> 方法，<code>Named</code> 接口是否为 <code>getName</code> 提供了默认实现并不会带来什么区别。这正是 <strong>“ 类优先 ”</strong> 规则。</p><p>“ 类优先 ” 规则可以确保与 Java 7 的兼容性。如果为一个接口增加默认方法，这对于有默认方法之前能正常工作的代码不会有任何影响。</p><blockquote><p><strong>警告</strong>： 绝对不能创建一个默认方法重新定义 <code>Object</code> 类中的某个方法。例如，不能为 <code>toString</code> 或 <code>equals</code> 定义默认方法，尽管对于 <code>List</code> 之类的接口这可能很有吸引力。由于“类优先”规则，这样的方法绝对无法超越 <code>Object.toString</code> 或 <code>Object.equals</code>。</p></blockquote><h4 id="6-1-7-接口与回调"><a href="#6-1-7-接口与回调" class="headerlink" title="6.1.7 接口与回调"></a>6.1.7 接口与回调</h4><p>**回调（callback）**是一种常见的程序设计模式。在这种模式中，可以指定某个特定事件发生时应该采取的动作。例如，点击一个按钮或选择某个菜单项时，你可能希望完成某个特定的动作。不过，由于目前还没有介绍如何实现用户界面，所以我们来考虑一种类似但更简单的情况。</p><p>在 <code>javax.swing</code> 包中有一个 <code>Timer</code> 类，如果希望经过一定时间间隔就得到通知，<code>Timer</code> 类就很有用。例如，假如程序中有一个时钟，你可以请求每秒通知一次，以便更新时钟的表盘。</p><p>构造定时器时，需要设置一个时间间隔，并告诉定时器经过这个时间间隔时要做些什么。</p><p>如何告诉定时器要做什么呢？在很多程序设计语言中，可以提供一个函数名，定时器要定期地调用这个函数。但是，Java 标准类库中的类采用了一种面向对象方法。你可以向定时器传入某个类的对象，然后，定时器调用这个对象的某个方法。由于对象可以携带额外的信息，所以传递一个对象比传递一个函数要灵活得多。</p><p>当然，定时器需要知道要调用哪一个方法。它要求你指定一个类的对象，这个类要实现 <code>java.awt.event</code> 包中的 <code>ActionListener</code> 接口。下面是这个接口：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">interface</span> <span class="hljs-title class_">ActionListener</span> &#123;<br>    <span class="hljs-keyword">void</span> <span class="hljs-title function_">actionPerformed</span><span class="hljs-params">(ActionEvent event)</span>;<br>&#125;<br></code></pre></td></tr></table></figure><p>当达到指定的时间间隔时，定时器就调用 <code>actionPerformed</code> 方法。</p><p>假设你希望每秒打印一条消息 “At the tone, the time is”，然后响一声，那么可以定义一个实现 <code>ActionListener</code> 接口的类，然后将想要执行的语句放在 <code>actionPerformed</code> 方法中。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">class</span> <span class="hljs-title class_">TimePrinter</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">ActionListener</span> &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">actionPerformed</span><span class="hljs-params">(ActionEvent event)</span> &#123;<br>        System.out.println(<span class="hljs-string">&quot;At the tone, the time is &quot;</span><br>            + Instant.ofEpochMilli(event.getWhen()));<br>        Toolkit.getDefaultToolkit().beep();<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>需要注意 <code>actionPerformed</code> 方法的 <code>ActionEvent</code> 参数。这个参数提供了事件的相关信息，例如，发生这个事件的时间。<code>event.getWhen()</code> 调用会返回这个事件时间，表示为“纪元”（1970年 1 月 1 日）以来的毫秒数。如果把它传入静态方法 <code>Instant.ofEpochMilli</code>，可以得到一个更可读的描述。</p><p>接下来，构造这个类的一个对象，并将它传递到 <code>Timer</code> 构造器。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">var</span> <span class="hljs-variable">listener</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">TimePrinter</span>();<br><span class="hljs-type">Timer</span> <span class="hljs-variable">t</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Timer</span>(<span class="hljs-number">1000</span>, listener);<br></code></pre></td></tr></table></figure><p><code>Timer</code> 构造器的第一个参数是一个时间间隔（单位是毫秒），即经过多长时间通知一次。这里希望每秒通知一次。第二个参数是监听器对象。</p><p>最后，启动定时器：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">t.start();<br></code></pre></td></tr></table></figure><p>每过 1 秒就会显示下面的消息，然后响一声铃：</p><figure class="highlight apache"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs apache"><span class="hljs-attribute">At</span> the tone, the time is <span class="hljs-number">2017</span>-<span class="hljs-number">12</span>-<span class="hljs-number">16</span>T05:<span class="hljs-number">01</span>:<span class="hljs-number">49</span>.<span class="hljs-number">550</span>Z<br></code></pre></td></tr></table></figure><blockquote><p><strong>警告</strong>： 一定要导入 <code>javax.swing.Timer</code>。另外还有一个稍有区别的 <code>java.util.Timer</code> 类。</p></blockquote><h4 id="6-1-8-Comparator-接口"><a href="#6-1-8-Comparator-接口" class="headerlink" title="6.1.8 Comparator 接口"></a>6.1.8 Comparator 接口</h4><p>6.1.1 节中，我们已经了解了如何对一个对象数组进行排序，前提是这些对象是实现了 <code>Comparable</code> 接口的类的实例。例如，可以对一个字符串数组排序，因为 <code>String</code> 类实现了 <code>Comparable&lt;String&gt;</code>，而且 <code>String.compareTo</code> 方法可以按字典顺序比较字符串。</p><p>现在假设我们希望按长度递增的顺序对字符串进行排序，而不是按字典顺序进行排序。</p><p>肯定不能让 <code>String</code> 类用两种不同的方式实现 <code>compareTo</code> 方法——更何况，<code>String</code> 类也不应由我们来修改。</p><p>要处理这种情况，<code>Arrays.sort</code> 方法还有第二个版本，接受一个数组和一个比较器（comparator）作为参数，比较器是实现了 <code>Comparator</code> 接口的类的实例。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">interface</span> <span class="hljs-title class_">Comparator</span>&lt;T&gt; &#123;<br>    <span class="hljs-type">int</span> <span class="hljs-title function_">compare</span><span class="hljs-params">(T first, T second)</span>;<br>&#125;<br></code></pre></td></tr></table></figure><p>要按长度比较字符串，可以如下定义一个实现 <code>Comparator&lt;String&gt;</code> 的类：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">class</span> <span class="hljs-title class_">LengthComparator</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">Comparator</span>&lt;String&gt; &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-type">int</span> <span class="hljs-title function_">compare</span><span class="hljs-params">(String first, String second)</span> &#123;<br>        <span class="hljs-keyword">return</span> first.length() - second.length();<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>具体完成比较时，需要建立一个实例：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">var</span> <span class="hljs-variable">comp</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">LengthComparator</span>();<br><span class="hljs-keyword">if</span> (comp.compare(words[i], words[j]) &gt; <span class="hljs-number">0</span>) ...<br></code></pre></td></tr></table></figure><p>将这个调用与 <code>words[i].compareTo(words[j])</code> 做个比较。这个 <code>compare</code> 方法要在比较器对象上调用，而不是在字符串本身调用。</p><blockquote><p><strong>注释</strong>：尽管 <code>LengthComparator</code> 对象没有状态，不过还是需要创建一个对象实例。我们需要这个实例来调用 <code>compare</code> 方法——它不是一个静态方法。</p></blockquote><p>要对一个数组排序，需要为 <code>Arrays.sort</code> 方法传入一个 <code>LengthComparator</code> 对象：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java">String[] friends = &#123; <span class="hljs-string">&quot;Peter&quot;</span>, <span class="hljs-string">&quot;Paul&quot;</span>, <span class="hljs-string">&quot;Mary&quot;</span> &#125;;<br>Arrays.sort(friends, <span class="hljs-keyword">new</span> <span class="hljs-title class_">LengthComparator</span>());<br></code></pre></td></tr></table></figure><p>现在这个数组可能是 <code>[&quot;Paul&quot;, &quot;Mary&quot;, &quot;Peter&quot;]</code> 或 <code>[&quot;Mary&quot;, &quot;Paul&quot;, &quot;Peter&quot;]</code>。</p><h4 id="6-1-9-对象克隆"><a href="#6-1-9-对象克隆" class="headerlink" title="6.1.9 对象克隆"></a>6.1.9 对象克隆</h4><p>本节我们会讨论 <code>Cloneable</code> 接口，这个接口表示一个类提供了一个安全的 <code>clone</code> 方法。</p><p>要了解克隆的具体含义，先来回忆为一个包含对象引用的变量建立副本时会发生什么（这就是拷贝）。<strong>拷贝原变量和副本都是同一个对象的引用</strong>（见图 6-1）。这说明，任何一个变量的改变都会影响另一个变量。</p><p><img src="/2025/05/04/hello-world/%E7%AC%AC%E5%85%AD%E7%AB%A0/image-20251112162316224.png" alt="image-20251112162316224"></p><p>​<strong>图 6-1 拷贝和克隆</strong></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">var</span> <span class="hljs-variable">original</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Employee</span>(<span class="hljs-string">&quot;John Public&quot;</span>, <span class="hljs-number">50000</span>);<br><span class="hljs-type">Employee</span> <span class="hljs-variable">copy</span> <span class="hljs-operator">=</span> original;<br>copy.raiseSalary(<span class="hljs-number">10</span>); <span class="hljs-comment">// oops--also changed original</span><br></code></pre></td></tr></table></figure><p>如果希望 <code>copy</code> 是一个新对象，它的初始状态与 <code>original</code> 相同，但是之后它们的状态可能不同，这种情况下就要使用 <code>clone</code> 方法（即克隆）。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">Employee</span> <span class="hljs-variable">copy</span> <span class="hljs-operator">=</span> original.clone();<br>copy.raiseSalary(<span class="hljs-number">10</span>); <span class="hljs-comment">// OK--original unchanged</span><br></code></pre></td></tr></table></figure><p>不过并没有这么简单。<strong><code>clone</code> 方法是 <code>Object</code> 的一个 <code>protected</code> 方法</strong>，这说明你的代码不能直接调用这个方法。只有 <code>Employee</code> 类可以克隆 <code>Employee</code> 对象。这个限制是有原因的。想想看 <code>Object</code> 类如何实现 <code>clone</code>。它对于这个对象一无所知，所以只能逐个字段地进行拷贝。如果对象中的所有实例字段都是数值或其他基本类型，拷贝这些字段没有任何问题。但是如果对象包含子对象的引用，拷贝字段就会得到相同子对象的另一个引用，这样一来，原对象和克隆的对象仍然会共享一些信息。</p><p>为了更直观地说明这个问题，考虑第 4 章介绍过的 <code>Employee</code> 类。图 6-2 显示了使用 <code>Object</code> 类的 <code>clone</code> 方法克隆这样一个 <code>Employee</code> 对象会发生什么。可以看到，默认的克隆操作是“浅拷贝”，并没有克隆对象中引用的其他对象。（这个图显示了一个共享的 <code>Date</code> 对象。出于某种原因（稍后就会解释这个原因），这个例子使用了 <code>Employee</code> 类的老版本，其中的雇佣日期仍用 <code>Date</code> 表示。）</p><p><img src="/2025/05/04/hello-world/%E7%AC%AC%E5%85%AD%E7%AB%A0/image-20251114112834579.png" alt="image-20251114112834579"></p><p>​<strong>图 6-2 浅拷贝</strong></p><p>浅拷贝会有什么影响吗？这要看具体情况。如果原对象和浅克隆对象共享的子对象是不可变的，那么这种共享就是安全的。如果子对象属于一个不可变的类，如 <code>String</code>，就是这种情况；或者，在对象的生命期中，子对象一直保持不变，没有更改器方法改变它，也没有方法会生成它的引用，这种情况下同样是安全的。</p><p>不过，通常子对象都是可变的，必须重新定义 <code>clone</code> 方法来建立一个<strong>深拷贝（deep copy）</strong>。</p><p>这会克隆所有子对象。在这个例子中，<code>hireDay</code> 字段是一个 <code>Date</code>，这是可变的，所以它也必须克隆。（正是由于这个原因，这个例子使用 <code>Date</code> 类型的字段而不是 <code>LocalDate</code> 来展示克隆过程。如果 <code>hireDay</code> 是不可变的 <code>LocalDate</code> 类的一个实例，就无须我们做任何操作了。  ）</p><p>对于每一个类，需要确定以下选项是否成立：  </p><ol><li><strong>默认的<code>clone</code>方法就能满足要求；</strong>  </li><li><strong>可以在可变的子对象上调用<code>clone</code>来弥补默认的<code>clone</code>方法；</strong>  </li><li><strong>不该使用<code>clone</code>。</strong></li></ol><p>实际上第3个选项是默认选项。如果选择第1项或第2项，类必须：  </p><ol><li>实现<code>Cloneable</code>接口；  </li><li>重新定义<code>clone</code>方法，并指定<code>public</code>访问修饰符。</li></ol><blockquote><p><strong>注释</strong>：<code>Object</code>类中的<code>clone</code>方法声明为<code>protected</code>，所以你的代码不能直接调用<code>anObject.clone</code>。但是，不是所有子类都能访问受保护方法吗？不是所有类都是<code>Object</code>的子类吗？幸运的是，受保护访问的规则比较微妙（见第5章）。子类只能调用受保护的<code>clone</code>方法来克隆它自己的对象。必须重新定义<code>clone</code>为<code>public</code>才允许所有方法克隆对象。  </p></blockquote><p>在这里，<code>Cloneable</code>接口的出现与接口的正常使用并没有关系。具体来说，它没有指定<code>clone</code>方法，这个方法是从<code>Object</code>类继承的。这个接口只是作为一个标记，指示类设计者了解克隆过程。对象对于克隆很“偏执”，如果一个对象请求克隆，但是没有实现这个接口，就会生成一个检查型异常。  </p><blockquote><p><strong>注释</strong>：<code>Cloneable</code>接口是 Java 提供的少数标记接口（tagging interface）之一。（有些程序员称之为记号接口（marker interface）。）应该记得，<code>Comparable</code>等接口的通常用途是确保一个类实现一个特定的方法或一组方法。标记接口不包含任何方法，它唯一的作用</p><p>就是允许在类型查询中使用<code>instanceof</code>：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">if</span> (obj <span class="hljs-keyword">instanceof</span> Cloneable)  <br></code></pre></td></tr></table></figure><p><strong>建议你自己的程序中不要使用标记接口。</strong>  </p></blockquote><p>即使<code>clone</code>的默认（浅拷贝）实现能够满足要求，还是需要实现<code>Cloneable</code>接口，将<code>clone</code>重新定义为<code>public</code>，再调用<code>super.clone()</code>。下面给出一个例子：  </p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Employee</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">Cloneable</span> &#123;<br>    <span class="hljs-comment">// public access, change return type</span><br>    <span class="hljs-keyword">public</span> Employee <span class="hljs-title function_">clone</span><span class="hljs-params">()</span> <span class="hljs-keyword">throws</span> CloneNotSupportedException &#123;<br>        <span class="hljs-keyword">return</span> (Employee) <span class="hljs-built_in">super</span>.clone();<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><blockquote><p><strong>注释</strong>：在 Java 1.4 之前，<code>clone</code> 方法的返回类型总是<code>Object</code>，而现在可以为你的<code>clone</code>方法指定正确的返回类型。这是协变返回类型的一个例子（参见第5章）。</p></blockquote><p>与 <code>Object.clone</code> 提供的浅拷贝相比，前面看到的 <code>clone</code> 方法并没有增加任何功能。这里只是让这个方法是的公共的。要建立深拷贝，还需要做更多工作，克隆对象中可变的实例字段。</p><p>下面来看创建深拷贝的 <code>clone</code> 方法的一个例子：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Employee</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">Cloneable</span> &#123;<br>    <span class="hljs-keyword">public</span> Employee <span class="hljs-title function_">clone</span><span class="hljs-params">()</span> <span class="hljs-keyword">throws</span> CloneNotSupportedException &#123;<br>        <span class="hljs-comment">// call Object.clone()</span><br>        <span class="hljs-type">Employee</span> <span class="hljs-variable">cloned</span> <span class="hljs-operator">=</span> (Employee) <span class="hljs-built_in">super</span>.clone();<br><br>        <span class="hljs-comment">// clone mutable fields</span><br>        cloned.hireDay = (Date) hireDay.clone();<br><br>        <span class="hljs-keyword">return</span> cloned;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p><code>Object</code> 类的 <code>clone</code> 方法有可能抛出一个 <code>CloneNotSupportedException</code>，如果在一个对象上调用 <code>clone</code>，但这个对象的类并没有实现 <code>Cloneable</code> 接口，就会发生这种情况。当然，<code>Employee</code> 和 <code>Date</code> 类实现了 <code>Cloneable</code> 接口，所以不会抛出这个异常。不过，编译器并不知道这一点，因此，我们声明了这个异常：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> Employee <span class="hljs-title function_">clone</span><span class="hljs-params">()</span> <span class="hljs-keyword">throws</span> CloneNotSupportedException<br></code></pre></td></tr></table></figure><blockquote><p><strong>注释</strong>：捕获这个异常是不是更好一些？</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> Employee <span class="hljs-title function_">clone</span><span class="hljs-params">()</span> <br>&#123;<br> <span class="hljs-keyword">try</span> <br> &#123;<br>     <span class="hljs-type">Employee</span> <span class="hljs-variable">cloned</span> <span class="hljs-operator">=</span> (Employee) <span class="hljs-built_in">super</span>.clone();<br>     ...<br> &#125;<br>    <span class="hljs-keyword">catch</span> (CloneNotSupportedException e) &#123;<span class="hljs-keyword">return</span> <span class="hljs-literal">null</span>;&#125;<br>    <span class="hljs-comment">// this won&#x27;t happen, since we are Cloneable</span><br>&#125;<br></code></pre></td></tr></table></figure><p>这适用于 <code>final</code> 类。否则，最好还是保留 <code>throws</code> 说明符。这样就允许子类在不支持克隆时选择抛出一个 <code>CloneNotSupportedException</code>。</p></blockquote><p>必须当心子类的克隆。例如，一旦为 <code>Employee</code> 类定义了 <code>clone</code> 方法，任何人都可能用它来克隆 <code>Manager</code> 对象。<code>Employee</code> 的克隆方法能完成这个任务吗？这取决于 <code>Manager</code> 类的字段。在这里是没有问题的，因为 <code>bonus</code> 字段是基本类型。但是 <code>Manager</code> 可能有需要深拷贝的字段或者不可克隆的字段。不能保证子类的实现者一定会修正 <code>clone</code> 方法让它正确地完成工作。出于这个原因，在 <code>Object</code> 类中 <code>clone</code> 方法声明为 <code>protected</code>。不过，如果希望你的类的使用者调用 <code>clone</code>，这就做不到了。</p><p>要不要在自己的类中实现 <code>clone</code> 呢？如果你的客户需要建立深拷贝，可能就应当实现这个方法。有些人认为应该完全避免使用 <code>clone</code>，而实现另一个方法来达到同样的目的。<code>clone</code>相当别扭，这一点我们也同意，不过如果让另一个方法来完成这个工作，还是会遇到同样的问题。毕竟，克隆没有你想象中那么常用。标准库中只有不到 5% 的类实现了 <code>clone</code>。</p><blockquote><p><strong>注释</strong>：所有数组类型都有一个公共的 <code>clone</code> 方法，而不是受保护的。可以用这个方法建立一个新数组，包含原数组所有元素的副本。例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">int</span>[] luckyNumbers = &#123; <span class="hljs-number">2</span>, <span class="hljs-number">3</span>, <span class="hljs-number">5</span>, <span class="hljs-number">7</span>, <span class="hljs-number">11</span>, <span class="hljs-number">13</span> &#125;;<br><span class="hljs-type">int</span>[] cloned = luckyNumbers.clone();<br>cloned[<span class="hljs-number">5</span>] = <span class="hljs-number">12</span>; <span class="hljs-comment">// doesn&#x27;t change luckyNumbers[5]</span><br></code></pre></td></tr></table></figure></blockquote><h3 id="6-2-lambda表达式"><a href="#6-2-lambda表达式" class="headerlink" title="6.2 lambda表达式"></a>6.2 lambda表达式</h3><p>接下来几节中，你会了解如何使用 lambda 表达式采用一种简洁的语法定义代码块，以及如何编写处理 lambda 表达式的代码。</p><h4 id="6-2-1-为什么引入-lambda-表达式"><a href="#6-2-1-为什么引入-lambda-表达式" class="headerlink" title="6.2.1 为什么引入 lambda 表达式"></a>6.2.1 为什么引入 lambda 表达式</h4><p>lambda 表达式是一个可传递的代码块，可以在以后执行一次或多次。在具体介绍语法（甚至解释这个让人好奇的名字）之前，下面先退一步，观察一下我们在 Java 中的哪些地方用过这种代码块。</p><p>在 6.1.7 节中，你已经了解了如何按指定时间间隔完成工作。将这个工作放在一个 <code>ActionListener</code> 的 <code>actionPerformed</code> 方法中：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Worker</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">ActionListener</span> &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">actionPerformed</span><span class="hljs-params">(ActionEvent event)</span> &#123;<br>        <span class="hljs-comment">// do some work</span><br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>然后，想要反复执行这个代码时，可以构造 <code>Worker</code> 类的一个实例，再把这个实例提交到一个 <code>Timer</code> 对象。</p><p>这里的重点是：<code>actionPerformed</code> 方法包含希望以后执行的代码。</p><p>或者可以考虑如何用一个定制比较器完成排序。如果想按长度而不是默认的字典顺序对字符串进行排序，可以向 <code>sort</code> 方法传入一个 <code>Comparator</code> 对象：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">class</span> <span class="hljs-title class_">LengthComparator</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">Comparator</span>&lt;String&gt; &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-type">int</span> <span class="hljs-title function_">compare</span><span class="hljs-params">(String first, String second)</span> &#123;<br>        <span class="hljs-keyword">return</span> first.length() - second.length();<br>    &#125;<br>&#125;<br><br>Arrays.sort(strings, <span class="hljs-keyword">new</span> <span class="hljs-title class_">LengthComparator</span>());<br></code></pre></td></tr></table></figure><p><code>compare</code> 方法并不是立即调用。实际上，在数组完成排序之前，<code>sort</code> 方法会一直调用 <code>compare</code> 方法，只要元素的顺序不正确就会重新排列元素。将比较元素所需的代码段放在 <code>sort</code> 方法中，这个代码将与其余的排序逻辑集成（你可能不打算重新实现其余的这部分逻辑）。</p><p>这两个例子有一些共同点：都是将一个代码块传递到某个目标（一个定时器，或者一个 <code>sort</code> 方法）。这个代码块会在将来某个时间调用。</p><p>到目前为止，在 Java 中传递一个代码段并不容易，你不能直接传递代码段。Java 是一种面向对象语言，所以必须构造一个对象，这个对象的类要有一个方法包含所需的代码。</p><p>在其他语言中，可以直接处理代码块。Java 设计者很长时间以来一直拒绝增加这个特性。毕竟，Java 的强大之处就在于其简单性和一致性。倘若只要一个特性能够让代码稍简洁一些，就把这个特性增加到语言中，那么这个语言很快就会变得一团糟，无法管理。不过，在另外那些语言中，并不只是创建线程或注册按钮点击事件处理器更容易；它们的大部分 API 都更简单、更一致而且更强大。在 Java 中，也可以编写类似的 API 处理实现了某个特定接口的类对象，不过这种 API 使用可能很不方便。</p><h4 id="6-2-2-lambda-表达式的语法"><a href="#6-2-2-lambda-表达式的语法" class="headerlink" title="6.2.2 lambda 表达式的语法"></a>6.2.2 lambda 表达式的语法</h4><p>再来考虑上一节讨论的排序例子。我们传入代码来检查一个字符串是否比另一个字符串短。</p><p>这里要计算：</p><figure class="highlight applescript"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs applescript"><span class="hljs-keyword">first</span>.<span class="hljs-built_in">length</span>() - <span class="hljs-keyword">second</span>.<span class="hljs-built_in">length</span>()<br></code></pre></td></tr></table></figure><p><code>first</code> 和 <code>second</code> 是什么？它们都是字符串。Java 是一种强类型语言，所以我们还要指定它们的类型：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java">(String first, String second) -&gt;<br>    first.length() - second.length()<br></code></pre></td></tr></table></figure><p>这就是你看到的第一个 lambda 表达式。lambda 表达式就是一个代码块，以及必须传入代码的所有变量的规范。</p><p>逻辑学家 Alonzo Church 想要形式化地表示能有效计算的数学函数。他使用了希腊字母 lambda（λ）来标记参数。如果他知道 Java API，可能就会写为</p><figure class="highlight applescript"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs applescript">λ<span class="hljs-keyword">first</span>.λ<span class="hljs-keyword">second</span>.<span class="hljs-keyword">first</span>.<span class="hljs-built_in">length</span>() - <span class="hljs-keyword">second</span>.<span class="hljs-built_in">length</span>()<br></code></pre></td></tr></table></figure><blockquote><p><strong>注释</strong>：为什么是字母 λ？Church 已经把字母表里的所有其他字母都用完了吗？实际上，权威的《数学原理》（Principia Mathematica）一书中就使用重音符 ^ 来表示自由变量，受此启发，Church 使用大写 lambda（Λ）表示参数。不过，最后他还是改为使用小写的 lambda（λ）。从那以后，带参数变量的表达式就被称为 lambda 表达式。</p></blockquote><p>你已经见过 Java 中一种<strong>简单的 lambda 表达式形式：参数，箭头（-&gt;）以及一个表达式。</strong></p><p>如果代码要完成的计算无法放在一个表达式中，就可以像写方法一样，把这些代码放在 <code>&#123;&#125;</code> 中，并包含显式的 return 语句。例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java">(String first, String second) -&gt; &#123;<br>    <span class="hljs-keyword">if</span> (first.length() &lt; second.length()) <span class="hljs-keyword">return</span> -<span class="hljs-number">1</span>;<br>    <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (first.length() &gt; second.length()) <span class="hljs-keyword">return</span> <span class="hljs-number">1</span>;<br>    <span class="hljs-keyword">else</span> <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;<br>&#125;<br></code></pre></td></tr></table></figure><p><strong>即使 lambda 表达式没有参数，仍然要提供空括号，就像无参数方法一样：</strong></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java">() -&gt; &#123;<br>    <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">100</span>; i &gt;= <span class="hljs-number">0</span>; i--) System.out.println(i);<br>&#125;<br></code></pre></td></tr></table></figure><p><strong>如果可以推导出一个 lambda 表达式的参数类型，则可以忽略其类型</strong>。例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java">Comparator&lt;String&gt; comp =<br>    (first, second)    <span class="hljs-comment">// same as (String first, String second) -&gt;</span><br>        first.length() - second.length();<br></code></pre></td></tr></table></figure><p>在这里，编译器可以推导出 <code>first</code> 和 <code>second</code> 必然是字符串，因为这个 lambda 表达式将赋给一个字符串比较器。（下一节会更详细地分析这个赋值。）</p><p><strong>如果方法只有一个参数，而且这个参数的类型可以推导得出，那么甚至还可以省略小括号</strong>：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">ActionListener</span> <span class="hljs-variable">listener</span> <span class="hljs-operator">=</span> event -&gt;<br>    System.out.println(<span class="hljs-string">&quot;The time is &quot;</span> +<br>        Instant.ofEpochMilli(event.getWhen()));<br></code></pre></td></tr></table></figure><p>instead of <code>(event)-&gt;...</code> or <code>(ActionEvent event)-&gt;...</code>。</p><p>**无须指定 lambda 表达式的返回类型。lambda 表达式的返回类型总是会由上下文推导得出。**例如，下面的表达式</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">(String first, String second) -&gt; first.length() - second.length()<br></code></pre></td></tr></table></figure><p>可以在需要 <code>int</code> 类型结果的上下文中使用。</p><p>最后，<strong>可以使用 <code>var</code> 指示一个推导的类型</strong>。这不常见，发明这个语法是为了关联注解：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">(<span class="hljs-meta">@NonNull</span> <span class="hljs-keyword">var</span> first, <span class="hljs-meta">@NonNull</span> <span class="hljs-keyword">var</span> second) -&gt; first.length() - second.length()<br></code></pre></td></tr></table></figure><blockquote><p>**注释：如果一个 lambda 表达式只在某些分支返回一个值，而另外一些分支不返回值，这是不合法的。**例如，<code>(int x)-&gt; &#123; if (x &gt;= 0) return 1; &#125;</code> 就不合法。</p></blockquote><h4 id="6-2-3-函数式接口"><a href="#6-2-3-函数式接口" class="headerlink" title="6.2.3 函数式接口"></a>6.2.3 函数式接口</h4><p>前面已经讨论过，Java 中有很多封装代码块的接口，如 <code>ActionListener</code> 或 <code>Comparator</code>。lambda 表达式与这些接口是兼容的。</p><p><strong>对于只有一个抽象方法的接口，需要这种接口的对象时，就可以提供一个 lambda 表达式。这种接口称为函数式接口（functional interface）。</strong></p><blockquote><p>**注释：**你可能想知道为什么函数式接口必须有一个抽象方法。不是接口中的所有方法都是抽象的吗？实际上，接口完全有可能重新声明 Object 类的方法，如 <code>toString</code> 或 <code>clone</code>，这些声明有可能会让方法不再是抽象的。（Java API 中的一些接口会重新声明 Object 方法来附加 javadoc 注释。<code>Comparator</code> API 就是这样一个例子。）更重要的是，正如 6.1.5 节所述，接口可以声明非抽象方法。</p></blockquote><p>为了展示如何转换为函数式接口，下面考虑 <code>Arrays.sort</code> 方法。它的第二个参数需要一个 <code>Comparator</code> 实例，<code>Comparator</code> 就是只有一个方法的接口，所以可以提供一个 lambda 表达式：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java">Arrays.sort(words,<br>    (first, second) -&gt; first.length() - second.length());<br></code></pre></td></tr></table></figure><p>在底层，<code>Arrays.sort</code> 方法会接收实现了 <code>Comparator&lt;String&gt;</code> 的某个类的对象。在这个对象上调用 <code>compare</code> 方法会执行这个 lambda 表达式的体。这些对象和类的管理完全取决于具体实现，与使用传统的内联类相比，这样可能要高效得多。最好把 lambda 表达式看作是一个函数，而不是一个对象，另外要接受一个事实：<strong>lambda 表达式可以传递到函数式接口。</strong></p><p><strong>lambda 表达式可以转换为接口</strong>，这一点让 lambda 表达式很有吸引力。具体的语法很简短。下面再来看一个例子：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">var</span> <span class="hljs-variable">timer</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Timer</span>(<span class="hljs-number">1000</span>, event -&gt;<br>    System.out.println(<span class="hljs-string">&quot;At the tone, the time is &quot;</span> +<br>        Instant.ofEpochMilli(event.getWhen()));<br>Toolkit.getDefaultToolkit().beep();<br></code></pre></td></tr></table></figure><p>与使用实现了 <code>ActionListener</code> 接口的类相比，这段代码的可读性要好得多。</p><p>实际上，在 Java 中，对 lambda 表达式所能做的也只是转换为函数式接口。在其他支持函数字面量的程序设计语言中，可以声明函数类型（如 <code>(String, String) -&gt; int</code>），声明这些类型的变量，还可以使用变量保存函数表达式。不过，Java 设计者还是决定保持我们熟悉的接口概念，而没有为 Java 语言增加函数类型。</p><blockquote><p><strong>注释</strong>：<strong>甚至不能把 lambda 表达式赋给类型为 Object 的变量，Object 不是一个函数式接口。</strong></p></blockquote><p>Java API 在 <code>java.util.function</code> 包中定义了很多非常通用的函数式接口。其中一个接口 <code>BiFunction&lt;T, U, R&gt;</code> 描述了参数类型为 T 和 U 而且返回类型为 R 的函数。可以把我们的字符串比较 lambda 表达式保存在这个类型的变量中：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java">BiFunction&lt;String, String, Integer&gt; comp =<br>    (first, second) -&gt; first.length() - second.length();<br></code></pre></td></tr></table></figure><p>不过，这对于排序并没有帮助。没有哪个 <code>Arrays.sort</code> 方法想要接收一个 <code>BiFunction</code>。如果你之前用过某种函数式编程语言，可能会发现这很奇怪。不过，对于 Java 程序员而言，这非常自然。类似 <code>Comparator</code> 的接口往往有一个特定的用途，而不只是提供一个有指定参数和返回类型的方法。想要用 lambda 表达式做某些处理时，还是希望谨记表达式的用途，为它建立一个特定的函数式接口。</p><p><code>java.util.function</code> 包中有一个尤其有用的接口 <code>Predicate</code>：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">interface</span> <span class="hljs-title class_">Predicate</span>&lt;T&gt; &#123;<br>    <span class="hljs-type">boolean</span> <span class="hljs-title function_">test</span><span class="hljs-params">(T t)</span>;<br>    <span class="hljs-comment">// additional default and static methods</span><br>&#125;<br></code></pre></td></tr></table></figure><p><code>ArrayList</code> 类有一个 <code>removeIf</code> 方法，它的参数就是一个 <code>Predicate</code>。这个接口专门用来传递 lambda 表达式。例如，下面的语句将从一个数组列表删除所有 null 值：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">list.removeIf(e -&gt; e == <span class="hljs-literal">null</span>);<br></code></pre></td></tr></table></figure><p>另一个有用的函数式接口是 <code>Supplier&lt;T&gt;</code>：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">interface</span> <span class="hljs-title class_">Supplier</span>&lt;T&gt; &#123;<br>    T <span class="hljs-title function_">get</span><span class="hljs-params">()</span>;<br>&#125;<br></code></pre></td></tr></table></figure><p>供应者（supplier）没有参数，调用时会生成一个 <code>T</code> 类型的值。供应者用于实现懒计算（lazy evaluation）。例如，考虑以下调用：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">LocalDate</span> <span class="hljs-variable">hireDay</span> <span class="hljs-operator">=</span> Objects.requireNonNullElse(day,<br>                                 LocalDate.of(<span class="hljs-number">1970</span>, <span class="hljs-number">1</span>, <span class="hljs-number">1</span>));<br></code></pre></td></tr></table></figure><p>这不是最优的。我们预计 <code>day</code> 很少为 null，所以希望只在必要时才构造默认的 <code>LocalDate</code>。通过使用供应者，我们就能延迟这个计算：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">LocalDate</span> <span class="hljs-variable">hireDay</span> <span class="hljs-operator">=</span> Objects.requireNonNullElseGet(day,<br>                                 () -&gt; LocalDate.of(<span class="hljs-number">1970</span>, <span class="hljs-number">1</span>, <span class="hljs-number">1</span>));<br></code></pre></td></tr></table></figure><p><code>requireNonNullElseGet</code> 方法只在需要值时才调用供应者。</p><h4 id="6-2-4-方法引用"><a href="#6-2-4-方法引用" class="headerlink" title="6.2.4 方法引用"></a>6.2.4 方法引用</h4><p>有时，lambda 表达式涉及一个方法。例如，假设你希望只要出现一个定时器事件就打印这个事件对象。当然，为此也可以调用：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">var</span> <span class="hljs-variable">timer</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Timer</span>(<span class="hljs-number">1000</span>, event -&gt; System.out.println(event));<br></code></pre></td></tr></table></figure><p>但是，如果直接把 <code>println</code> 方法传递到 <code>Timer</code> 构造器就更好了。具体做法如下：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">var</span> <span class="hljs-variable">timer</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Timer</span>(<span class="hljs-number">1000</span>, System.out::println);<br></code></pre></td></tr></table></figure><p>表达式 <code>System.out::println</code> 是一个<strong>方法引用（method reference）</strong>，它指示编译器生成一个函数式接口的实例，覆盖这个接口的抽象方法来调用给定的方法。在这个例子中，会生成一个 <code>ActionListener</code>，它的 <code>actionPerformed(ActionEvent e)</code> 方法要调用 <code>System.out.println(e)</code>。</p><blockquote><p>**注释：类似于 lambda 表达式，方法引用也不是一个对象。**不过，为一个类型为函数式接口的变量赋值时会生成一个对象。</p></blockquote><blockquote><p><strong>注释：</strong><code>PrintStream</code> 类（<code>System.out</code> 就是 <code>PrintStream</code> 类的一个实例）中有 10 个重载的 <code>println</code> 方法。编译器需要根据上下文确定使用哪一个方法。在我们的例子中，方法引用 <code>System.out::println</code> 必须转换为一个包含以下方法的 <code>ActionListener</code> 实例：  </p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">void</span> <span class="hljs-title function_">actionPerformed</span><span class="hljs-params">(ActionEvent e)</span><br></code></pre></td></tr></table></figure><p>这样会从 10 个重载的 <code>println</code> 方法中选出 <code>println(Object x)</code> 方法，因为 <code>Object</code> 与 <code>ActionEvent</code> 最匹配。调用 <code>actionPerformed</code> 方法时，就会打印这个事件对象。</p><p>现在假设我们把同样的这个方法引用赋给另一个函数式接口：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">Runnable</span> <span class="hljs-variable">task</span> <span class="hljs-operator">=</span> System.out::println;<br></code></pre></td></tr></table></figure><p>这个 <code>Runnable</code> 函数式接口有一个无参数的抽象方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">void</span> <span class="hljs-title function_">run</span><span class="hljs-params">()</span>;<br></code></pre></td></tr></table></figure><p>在这种情况下，会选择无参数的 <code>println</code> 方法。调用 <code>task.run()</code> 会向 <code>System.out</code> 打印一个空行。</p></blockquote><p>再来看一个例子，假设你想对字符串进行排序，而不考虑字母的大小写。可以传递以下方法引用：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">Arrays.sort(strings, String::compareToIgnoreCase);<br></code></pre></td></tr></table></figure><p>从这些例子可以看出，要用 <code>::</code> 操作符分隔方法名与对象或类名。主要有 3 种情况：</p><ol><li><code>object::instanceMethod</code>  </li><li><code>Class::instanceMethod</code>  </li><li><code>Class::staticMethod</code></li></ol><p>在第 1 种情况下，方法引用等价于一个 lambda 表达式，其参数要传递到方法。对于 <code>System.out::println</code>，对象是 <code>System.out</code>，所以这个方法引用等价于 <code>x -&gt; System.out.println(x)</code>。</p><p>对于第 2 种情况，第 1 个参数会成为方法的隐式参数。例如，<code>String::compareToIgnoreCase</code>等同于<code>(x, y) -&gt; x.compareToIgnoreCase(y)</code>。</p><p>在第3种情况下，所有参数都传递到静态方法：<code>Math::pow</code> 等价于 <code>(x, y) -&gt; Math.pow(x, y)</code>。</p><p>注意，只有当 lambda 表达式的体只调用一个方法而不做其他操作时，才能把 lambda 表达式重写为方法引用。考虑以下 lambda 表达式：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">s -&gt; s.length() == <span class="hljs-number">0</span><br></code></pre></td></tr></table></figure><p>这里有一个方法调用。但是还有一个比较，所以这里不能使用方法引用。</p><blockquote><p><strong>注释</strong>：</p><ul><li><p>如果有多个同名的重载方法，编译器就会尝试从上下文中找出你指的是哪一个方法。例如，<code>Math.max</code> 方法有两个版本，一个用于整数，另一个用于 <code>double</code> 值。选择哪一个版本取决于 <code>Math::max</code> 转换为哪个函数式接口的方法参数。类似于 lambda 表达式，方法引用不会独立存在，总是会转换为函数式接口的实例。</p></li><li><p>有时 API 包含一些专门用作方法引用的方法。例如，<code>Objects</code> 类有一个方法 <code>isNull</code>，用于测试一个对象引用是否为 <code>null</code>。乍看上去这好像没有什么用，因为测试 <code>obj == null</code> 比 <code>Objects.isNull(obj)</code> 更有可读性。不过可以把方法引用传递到任何有 <code>Predicate</code> 参数的方法。例如，要从一个列表删除所有 <code>null</code> 引用，就可以调用：</p></li><li><pre><code class="language-java">  list.removeIf(Objects::isNull);  //这比 list.removeIf(e -&gt; e == null)更易读。  <figure class="highlight autohotkey"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs autohotkey"><br>![image-<span class="hljs-number">20251116165653881</span>](image-<span class="hljs-number">20251116165653881</span>.png)<br><br><span class="hljs-title">**注释：**包含对象的方法引用与等价的 lambda 表达式还有一个细微的差别。考虑一个方法引用，如 `separator::equals`。如果 `separator` 为 `null`，构造 `separator::</span>equals` 时就会立即抛出一个 `NullPointerException` 异常。而 lambda 表达式 `x -&gt; separator.equals(x)` 只在调用时才会抛出 `NullPointerException`。<br><br><span class="hljs-title">可以在方法引用中使用 `this` 参数。例如，`this::</span>equals` 等同于 `x -&gt; this.equals(x)`。使用 `super` 也是合法的。下面的方法表达式：<br><br>```java<br><span class="hljs-title">super::</span>instanceMethod<br></code></pre></td></tr></table></figure></code></pre></li></ul></blockquote><p>使用 <code>this</code> 作为目标，会调用给定方法的超类版本。为了展示这一点，下面给出一个假想的例子：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Greeter</span> &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">greet</span><span class="hljs-params">(ActionEvent event)</span> &#123;<br>        System.out.println(<span class="hljs-string">&quot;Hello, the time is &quot;</span><br>            + Instant.ofEpochMilli(event.getWhen()));<br>    &#125;<br>&#125;<br><br><span class="hljs-keyword">class</span> <span class="hljs-title class_">RepeatedGreeter</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Greeter</span> &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">greet</span><span class="hljs-params">(ActionEvent event)</span> &#123;<br>        <span class="hljs-type">var</span> <span class="hljs-variable">timer</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Timer</span>(<span class="hljs-number">1000</span>, <span class="hljs-built_in">super</span>::greet);<br>        timer.start();<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p><code>RepeatedGreeter.greet</code> 方法开始执行时，会构造一个 <code>Timer</code>，每次定时器滴答时会执行 <code>super::greet</code> 方法。</p><h4 id="6-2-5-构造器引用"><a href="#6-2-5-构造器引用" class="headerlink" title="6.2.5 构造器引用"></a>6.2.5 构造器引用</h4><p>构造器引用与方法引用很类似，只不过方法名为 <code>new</code>。例如，<code>Person::new</code> 是 <code>Person</code> 构造器的一个引用。哪一个构造器呢？这取决于上下文。假设你有一个字符串列表。可以在各个字符串上调用构造器，把这个字符串列表转换为一个 <code>Person</code> 对象数组，调用如下：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java">ArrayList&lt;String&gt; names = ...;<br>Stream&lt;Person&gt; stream = names.stream().map(Person::<span class="hljs-keyword">new</span>);<br>List&lt;Person&gt; people = stream.toList();<br></code></pre></td></tr></table></figure><p> <code>map</code> 方法会为各个列表元素调用 <code>Person(String)</code> 构造器。如果有多个 <code>Person</code> 构造器，编译器会选择有一个 <code>String</code> 参数的构造器，因为它从上下文推导出这是在调用带一个字符串的构造器。</p><p>可以用数组类型建立构造器引用。例如，<code>int[]::new</code> 是一个构造器引用，它有一个参数：数组的长度。这等价于 lambda 表达式 <code>x -&gt; new int[x]</code>。</p><p>**Java 有一个限制：无法构造泛型类型 <code>T</code> 的数组。**数组构造器引用对于克服这个限制很有用。（表达式 <code>new T[n]</code> 会产生错误，因为这会“擦除”为 <code>new Object[n]</code>）。</p><p>对于开发类库的人来说，这是一个问题。例如，假设我们需要一个 Person 对象数组。Stream 接口有一个 <code>toArray</code> 方法可以返回 Object 数组：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">Object[] people = stream.toArray();<br></code></pre></td></tr></table></figure><p>用户希望得到一个 Person 引用数组，而不是 Object 引用数组。</p><p>流库利用构造器引用解决了这个问题。可以把 <code>Person[]::new</code> 传入 <code>toArray</code> 方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">Person[] people = stream.toArray(Person[]::<span class="hljs-keyword">new</span>);<br></code></pre></td></tr></table></figure><p><code>toArray</code> 方法调用这个构造器来得到一个有正确类型的数组。然后填充并返回这个数组。</p><h4 id="6-2-6-变量作用域"><a href="#6-2-6-变量作用域" class="headerlink" title="6.2.6 变量作用域"></a>6.2.6 变量作用域</h4><p>你可能希望能够在 lambda 表达式中访问外围方法或类中的变量。考虑下面这个例子：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">repeatMessage</span><span class="hljs-params">(String text, <span class="hljs-type">int</span> delay)</span> &#123;<br>    <span class="hljs-type">ActionListener</span> <span class="hljs-variable">listener</span> <span class="hljs-operator">=</span> event -&gt; &#123;<br>        System.out.println(text);<br>        Toolkit.getDefaultToolkit().beep();<br>    &#125;;<br>    <span class="hljs-keyword">new</span> <span class="hljs-title class_">Timer</span>(delay, listener).start();<br>&#125;<br></code></pre></td></tr></table></figure><p>来看这样一个调用：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">repeatMessage(<span class="hljs-string">&quot;Hello&quot;</span>, <span class="hljs-number">1000</span>); <span class="hljs-comment">// prints Hello every 1000 milliseconds</span><br></code></pre></td></tr></table></figure><p>现在来看 lambda 表达式中的变量 <code>text</code>。注意这个变量并不是在这个 lambda 表达式中定义的。实际上，这是 <code>repeatMessage</code> 方法的一个参数变量。</p><p>lambda 表达式的代码可能在 <code>repeatMessage</code> 调用返回很久以后才运行，而那时这个参数变量已经不存在了。<code>text</code> 变量是如何保留下来的呢？</p><p>lambda 表达式的组成：</p><ol><li>一个代码块；  </li><li>参数；  </li><li>自由变量的值，这是指非参数而且不在代码中定义的变量。</li></ol><p>在我们的例子中，这个 lambda 表达式有一个自由变量 <code>text</code>。表示 lambda 表达式的数据结构必须存储自由变量的值，在这里就是字符串 <code>&quot;Hello&quot;</code>。我们说这些值被 lambda 表达式<strong>捕获（captured）</strong>。</p><blockquote><p><strong>注释</strong>：关于代码块连同自由变量值有一个术语：闭包（closure）。在 Java 中，lambda 表达式就是闭包。</p></blockquote><p>**可以看到，lambda 表达式可以捕获外围作用域中变量的值。**在 Java 中，为了确保所捕获的值是明确定义的，这里有一个重要的限制。**在 lambda 表达式中，只能引用值不会改变的变量。**例如，下面的做法是不合法的：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">countDown</span><span class="hljs-params">(<span class="hljs-type">int</span> start, <span class="hljs-type">int</span> delay)</span> &#123;<br>    <span class="hljs-type">ActionListener</span> <span class="hljs-variable">listener</span> <span class="hljs-operator">=</span> event -&gt; &#123;<br>        start--; <span class="hljs-comment">// ERROR: Can&#x27;t mutate captured variable</span><br>        System.out.println(start);<br>    &#125;;<br>    <span class="hljs-keyword">new</span> <span class="hljs-title class_">Timer</span>(delay, listener).start();<br>&#125;<br></code></pre></td></tr></table></figure><p>如果在 lambda 表达式中更改变量，并发执行多个动作时就会不安全。</p><p>另外，**如果在 lambda 表达式中引用一个变量，而这个变量可能在外部改变，这也是不合法的。**例如，下面就是不合法的：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">repeat</span><span class="hljs-params">(String text, <span class="hljs-type">int</span> count)</span> &#123;<br>    <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">1</span>; i &lt;= count; i++) &#123;<br>        <span class="hljs-type">ActionListener</span> <span class="hljs-variable">listener</span> <span class="hljs-operator">=</span> event -&gt;<br>            System.out.println(i + <span class="hljs-string">&quot;:&quot;</span> + text); <span class="hljs-comment">// ERROR: Cannot refer to changing i</span><br>        <span class="hljs-keyword">new</span> <span class="hljs-title class_">Timer</span>(<span class="hljs-number">1000</span>, listener).start();<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>这里有一条规则：<strong>lambda 表达式中捕获的变量必须是事实最终变量（effectively final）</strong>。**事实最终变量是指，这个变量初始化之后就不会再为它赋新值。**在这里，<code>text</code> 总是指示同一个 String 对象，所以捕获这个变量是可以的。不过，<code>i</code> 的值会改变，因此不能捕获 <code>i</code>。</p><p>lambda 表达式的体与嵌套块有相同的作用域。这里同样适用命名冲突和遮蔽的有关规则。<strong>在 lambda 表达式中声明与一个局部变量同名的参数或局部变量是不合法的。</strong></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">Path</span> <span class="hljs-variable">first</span> <span class="hljs-operator">=</span> Path.of(<span class="hljs-string">&quot;/usr/bin&quot;</span>);<br>Comparator&lt;String&gt; comp =<br>    (first, second) -&gt; first.length() - second.length(); <span class="hljs-comment">// ERROR: Variable first already defined</span><br></code></pre></td></tr></table></figure><p>在一个方法中，不能有两个同名的局部变量，因此，<strong>lambda 表达式中同样也不能有同名的局部变量。</strong></p><p>**在一个 lambda 表达式中使用 <code>this</code> 关键字时，是指创建这个 lambda 表达式的方法的 <code>this</code> 参数。**例如，考虑下面的代码：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Application</span> <br>&#123;<br>   <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">init</span><span class="hljs-params">()</span> <br>   &#123;<br>    <span class="hljs-type">ActionListener</span> <span class="hljs-variable">listener</span> <span class="hljs-operator">=</span> event -&gt;&#123;<br>        System.out.println(<span class="hljs-built_in">this</span>.toString());<br>        ...<br>    &#125;<br>&#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>表达式 <code>this.toString()</code> 会调用 <code>Application</code> 对象的 <code>toString</code> 方法，而不是 <code>ActionListener</code> 实例的方法。在 lambda 表达式中，<code>this</code> 的使用并没有任何特殊之处。lambda 表达式的作用域嵌套在 <code>init</code> 方法中，不论 <code>this</code> 在 lambda 表达式中，还是出现在这个方法中的其他位置，其含义并没有不同。</p><h4 id="6-2-7-处理-lambda-表达式"><a href="#6-2-7-处理-lambda-表达式" class="headerlink" title="6.2.7 处理 lambda 表达式"></a>6.2.7 处理 lambda 表达式</h4><p>下面来看如何编写方法处理 lambda 表达式。</p><p>使用 lambda 表达式的重点是<strong>延迟执行（deferred execution）</strong>。毕竟，如果想要立即执行代码，完全可以直接执行，而无须把它包装在一个 lambda 表达式中。之所以希望以后再执行代码，这有很多原因，如：</p><ul><li>在一个单独的线程中运行代码；</li><li>多次运行代码；</li><li>在算法的适当位置运行代码（例如，排序中的比较操作）；</li><li>发生某种情况时运行代码（如，点击了一个按钮，数据已经到达，等等）；</li><li>只在必要时才运行代码。</li></ul><p>假设你想要重复一个动作 n 次。将这个动作和重复次数传递到一个 <code>repeat</code> 方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">repeat(<span class="hljs-number">10</span>, () -&gt; System.out.println(<span class="hljs-string">&quot;Hello, World!&quot;</span>));<br></code></pre></td></tr></table></figure><p>要接受这个 lambda 表达式，需要选择（偶尔可能需要提供）一个函数式接口。表6-2列出了 Java API 中提供的最重要的函数式接口。在这里，我们可以使用 <code>Runnable</code> 接口：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">repeat</span><span class="hljs-params">(<span class="hljs-type">int</span> n, Runnable action)</span> <br>&#123;<br>    <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>; i &lt; n; i++) action.run();<br>&#125;<br></code></pre></td></tr></table></figure><p><img src="/2025/05/04/hello-world/%E7%AC%AC%E5%85%AD%E7%AB%A0/image-20251117155511784.png" alt="image-20251117155511784"></p><p><img src="/2025/05/04/hello-world/%E7%AC%AC%E5%85%AD%E7%AB%A0/image-20251117155518998.png" alt="image-20251117155518998"></p><p>需要说明，调用 <code>action.run()</code> 时会执行这个 lambda 表达式的主体。</p><p>我们希望告诉这个动作它出现在哪一次迭代中。为此，需要选择一个合适的函数式接口，其中要包含一个方法，这个方法有一个 <code>int</code> 参数而且返回类型为 <code>void</code>。处理 <code>int</code> 值的标准接口如下：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">interface</span> <span class="hljs-title class_">IntConsumer</span> &#123;<br>    <span class="hljs-keyword">void</span> <span class="hljs-title function_">accept</span><span class="hljs-params">(<span class="hljs-type">int</span> value)</span>;<br>&#125;<br></code></pre></td></tr></table></figure><p>下面给出 <code>repeat</code> 方法的改进版本：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">repeat</span><span class="hljs-params">(<span class="hljs-type">int</span> n, IntConsumer action)</span> &#123;<br>    <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>; i &lt; n; i++) action.accept(i);<br>&#125;<br></code></pre></td></tr></table></figure><p>可以如下调用：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">repeat(<span class="hljs-number">10</span>, i -&gt; System.out.println(<span class="hljs-string">&quot;Countdown: &quot;</span> + (<span class="hljs-number">9</span> - i)));<br></code></pre></td></tr></table></figure><p>表6-3列出了基本类型 <code>int</code>、<code>long</code> 和 <code>double</code> 的 34 个可用的特殊化接口。使用这些特殊化接口比使用通用接口更高效。</p><p><img src="/2025/05/04/hello-world/%E7%AC%AC%E5%85%AD%E7%AB%A0/image-20251117155701856.png" alt="image-20251117155701856"></p><p><img src="/2025/05/04/hello-world/%E7%AC%AC%E5%85%AD%E7%AB%A0/image-20251117155714497.png" alt="image-20251117155714497"></p><blockquote><p><strong>提示</strong>：最好使用表6-2或表6-3中的接口。例如，假设要编写一个方法来处理满足某个特定条件的文件。对此有一个遗留接口 <code>java.io.FileFilter</code>，不过最好使用标准的 <code>Predicate&lt;File&gt;</code>。只有一种情况下可以不这么做，那就是你已经有很多有用的方法可以产生 <code>FileFilter</code> 实例。</p></blockquote><blockquote><p><strong>注释</strong>：**大多数标准函数式接口都提供了非抽象方法来生成或合并函数。**例如：</p><ul><li><code>Predicate.isEqual(a)</code> 等同于 <code>a::equals</code>，不过如果 <code>a</code> 为 <code>null</code> 也能正常工作。</li><li>已经提供了默认方法 <code>and</code>、<code>or</code> 和 <code>negate</code> 来合并谓词。例如：</li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">Predicate.isEqual(a).or(Predicate.isEqual(b))<br></code></pre></td></tr></table></figure><p>就等同于：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">x -&gt; a.equals(x) || b.equals(x)<br></code></pre></td></tr></table></figure></blockquote><blockquote><p><strong>注释</strong>：如果你设计你自己的接口，其中只有一个抽象方法，可以用 <code>@FunctionalInterface</code> 注解来标记这个接口。这样做有两个优点：</p><ol><li>如果你无意中增加了另一个抽象方法，编译器会给出一个错误消息。</li><li>另外 javadoc 页中会指出你的接口是一个函数式接口。</li></ol><p>并不是必须使用注解。根据定义，任何只有一个抽象方法的接口都是函数式接口。不过使用 <code>@FunctionalInterface</code> 注解确实是一个好主意。</p></blockquote><blockquote><p><strong>注释</strong>：有些程序员喜欢将方法调用串起来，如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">String</span> <span class="hljs-variable">input</span> <span class="hljs-operator">=</span> <span class="hljs-string">&quot;618970019642690137449562111&quot;</span>;<br><span class="hljs-type">boolean</span> <span class="hljs-variable">isPrime</span> <span class="hljs-operator">=</span> input.strip()<br>                       .transform(BigInteger::<span class="hljs-keyword">new</span>)<br>                       .isProbablePrime(<span class="hljs-number">20</span>);<br></code></pre></td></tr></table></figure><p><code>String</code> 类的 <code>transform</code> 方法（Java 12 中新增）对字符串应用一个 <code>Function</code>，并生成结果。同样地，这些调用也可以写为：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">boolean</span> <span class="hljs-variable">prime</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">BigInteger</span>(input.strip()).isProbablePrime(<span class="hljs-number">20</span>);<br></code></pre></td></tr></table></figure><p>不过这样一来，你的视线必须左右跳来跳去，要找出哪一个先执行，哪一个后执行：首先调用 <code>strip</code>，然后构造 <code>BigInteger</code>，最后检测它是否是一个可能的素数。</p><p>遗憾的是，它只适用于字符串。</p></blockquote><h4 id="6-2-8-再谈-Comparator"><a href="#6-2-8-再谈-Comparator" class="headerlink" title="6.2.8 再谈 Comparator"></a>6.2.8 再谈 Comparator</h4><p><code>Comparator</code> 接口包含很多方便的静态方法来创建比较器。这些方法可以用于 lambda 表达式或方法引用。</p><p>静态 <code>comparing</code> 方法接受一个“键提取器”函数，它将类型 T 映射为一个可比较的类型（如 String）。对要比较的对象应用这个函数，然后对返回的键完成比较。例如，假设有一个 <code>Person</code> 对象数组，可以如下按姓名对这些对象进行排序：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">Arrays.sort(people, Comparator.comparing(Person::getName));<br></code></pre></td></tr></table></figure><p>可以把比较器与 <code>thenComparing</code> 方法串起来，来处理比较结果相同的情况。例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java">Arrays.sort(people,<br>    Comparator.comparing(Person::getLastName)<br>              .thenComparing(Person::getFirstName));<br></code></pre></td></tr></table></figure><p>如果两个人的姓相同，就会使用第二个比较器。</p><p>这些方法有很多变体形式。可以为 <code>comparing</code> 和 <code>thenComparing</code> 方法提取的键指定一个比较器。例如，可以如下根据人名长度完成排序：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java">Arrays.sort(people, Comparator.comparing(Person::getName,<br>    (s, t) -&gt; Integer.compare(s.length(), t.length())));<br></code></pre></td></tr></table></figure><p>另外，<code>comparing</code> 和 <code>thenComparing</code> 方法都有变体形式，可以避免 <code>int</code>、<code>long</code> 或 <code>double</code> 值的装箱。要完成前一个操作，还有一种更容易的做法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">Arrays.sort(people, Comparator.comparingInt(p -&gt; p.getName().length()));<br></code></pre></td></tr></table></figure><p>如果键函数可能返回 <code>null</code>，可能就要用到 <code>nullsFirst</code> 和 <code>nullsLast</code> 适配器。这些静态方法会修改现有的比较器，从而在遇到 <code>null</code> 值时不会抛出异常，而是将这个值标记为小于或大于正常值。例如，假设一个人没有中名时 <code>getMiddleName</code> 会返回一个 <code>null</code>，就可以使用：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">Comparator.comparing(Person::getMiddleName, Comparator.nullsFirst(...));<br></code></pre></td></tr></table></figure><p><code>nullsFirst</code> 方法需要一个比较器，在这里就是比较两个字符串的比较器。<code>naturalOrder</code> 方法可以为任何实现了 <code>Comparable</code> 的类建立一个比较器。在这里，<code>Comparator.&lt;String&gt;naturalOrder()</code> 正是我们需要的。下面是一个完整的调用，可以按可能为 <code>null</code> 的中名进行排序。这里使用了静态导入 <code>java.util.Comparator.*</code>，使这个表达式更为简洁，更便于阅读。注意 <code>naturalOrder</code> 的类型会推导得出：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">Arrays.sort(people, comparing(Person::getMiddleName, nullsFirst(naturalOrder())));<br></code></pre></td></tr></table></figure><p>静态 <code>reverseOrder</code> 方法会提供自然顺序的逆序。要让比较器逆序比较，可以使用 <code>reversed</code> 实例方法。例如 <code>naturalOrder().reversed()</code> 等同于 <code>reverseOrder()</code>。</p><h3 id="6-3-内部类"><a href="#6-3-内部类" class="headerlink" title="6.3 内部类"></a>6.3 内部类</h3><p><strong>内部类（inner class）是定义在另一个类中的类</strong>。为什么需要使用内部类呢？主要有两个原因：</p><ul><li>内部类可以对同一个包中的其他类隐藏。</li><li>内部类方法可以访问定义这些方法的作用域中的数据，包括原本私有的数据。</li></ul><p>内部类的对象会有一个隐式引用，指向实例化这个对象的外部类对象。通过这个指针，它可以访问外部对象的全部状态。例如，在 Java 中，<code>Iterator</code> 类不需要它所指的 <code>LinkedList</code> 的一个显式指针。</p><p>在 Java 中，静态内部类没有这个附加的指针，所以 Java 的静态内部类就相当于 C++ 中的嵌套类。</p><h4 id="6-3-1-使用内部类访问对象状态"><a href="#6-3-1-使用内部类访问对象状态" class="headerlink" title="6.3.1 使用内部类访问对象状态"></a>6.3.1 使用内部类访问对象状态</h4><p>我们将重构 <code>TimerTest</code> 示例，提取出一个 <code>TalkingClock</code> 类。构造一个语音时钟时需要提供两个参数：发出通知的间隔和开关铃声的标志。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">TalkingClock</span> &#123;<br>    <span class="hljs-keyword">private</span> <span class="hljs-type">int</span> interval;<br>    <span class="hljs-keyword">private</span> <span class="hljs-type">boolean</span> beep;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-title function_">TalkingClock</span><span class="hljs-params">(<span class="hljs-type">int</span> interval, <span class="hljs-type">boolean</span> beep)</span> &#123; ... &#125;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">start</span><span class="hljs-params">()</span> &#123; ... &#125;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">TimePrinter</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">ActionListener</span> &#123;<br>        <span class="hljs-comment">// an inner class</span><br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>需要注意，这里的 <code>TimePrinter</code> 类位于 <code>TalkingClock</code> 类内部。这并不意味着每个 <code>TalkingClock</code> 都有一个 <code>TimePrinter</code> 实例字段。如前所示，<code>TimePrinter</code> 对象是由 <code>TalkingClock</code> 类的方法构造的。</p><p>下面是 <code>TimePrinter</code> 类的详细内容。需要注意一点，<code>actionPerformed</code> 方法在发出铃声之前会检查 <code>beep</code> 标志。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">TimePrinter</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">ActionListener</span> &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">actionPerformed</span><span class="hljs-params">(ActionEvent event)</span> &#123;<br>        System.out.println(<span class="hljs-string">&quot;At the tone, the time is &quot;</span><br>            + Instant.ofEpochMilli(event.getWhen()));<br>        <span class="hljs-keyword">if</span> (beep) Toolkit.getDefaultToolkit().beep();<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>令人惊讶的事情发生了。<code>TimePrinter</code> 类没有实例字段或者名为 <code>beep</code> 的变量，实际上，<code>beep</code> 指示创建这个 <code>TimePrinter</code> 的 <code>TalkingClock</code> 对象中的字段。可以看到，一个内部类方法可以访问自身的实例字段，也可以访问创建它的外部类对象的实例字段。</p><p>为此，内部类的对象总有一个隐式引用，指向创建它的外部类对象，如图6-3所示。</p><p><img src="/2025/05/04/hello-world/%E7%AC%AC%E5%85%AD%E7%AB%A0/image-20251117225059058.png" alt="image-20251117225059058"></p><p>这个引用在内部类的定义中是不可见的。不过，为了说明这个概念，我们将外部类对象的引用称为 <code>outer</code>。于是 <code>actionPerformed</code> 方法将等价于以下代码：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">actionPerformed</span><span class="hljs-params">(ActionEvent event)</span> &#123;<br>    System.out.println(<span class="hljs-string">&quot;At the tone, the time is &quot;</span><br>        + Instant.ofEpochMilli(event.getWhen()));<br>    <span class="hljs-keyword">if</span> (outer.beep) Toolkit.getDefaultToolkit().beep();<br>&#125;<br></code></pre></td></tr></table></figure><p>外部类的引用在构造器中设置。编译器会修改所有的内部类构造器，添加一个对应外部类引用的参数。因为 <code>TimePrinter</code> 类没有定义构造器，所以编译器为这个类生成了一个无参数构造器，生成的代码如下所示：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-title function_">TimePrinter</span><span class="hljs-params">(TalkingClock clock)</span> &#123; <span class="hljs-comment">// automatically generated code</span><br>    outer = clock;<br>&#125;<br></code></pre></td></tr></table></figure><p>再次强调，注意 <code>outer</code> 不是 Java 的关键字。我们只是用它说明内部类的有关机制。</p><p>在 <code>start</code> 方法中构造一个 <code>TimePrinter</code> 对象后，编译器就会将当前语音时钟的 <code>this</code> 引用传递给这个构造器：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">var</span> <span class="hljs-variable">listener</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">TimePrinter</span>(<span class="hljs-built_in">this</span>); <span class="hljs-comment">// parameter automatically added</span><br></code></pre></td></tr></table></figure><p>下面我们再来看访问控制。如果 <code>TimePrinter</code> 类是一个普通的类，它就需要通过 <code>TalkingClock</code> 类的公共方法访问 <code>beep</code> 标志，而使用内部类是一个改进，现在就不再需要提供只有另外一个类感兴趣的访问器了。</p><blockquote><p><strong>注释</strong>：我们也可以把 <code>TimePrinter</code> 类声明为私有（<code>private</code>）。这样一来，只有 <code>TalkingClock</code> 方法才能够构造 <code>TimePrinter</code> 对象。只有内部类可以是私有的，而常规类可以有包可见性或公共可见性。</p></blockquote><h4 id="6-3-2-内部类的特殊语法规则"><a href="#6-3-2-内部类的特殊语法规则" class="headerlink" title="6.3.2 内部类的特殊语法规则"></a>6.3.2 内部类的特殊语法规则</h4><p>我们解释了内部类有一个外部类的引用，我们把它叫作 <code>outer</code>。表达式</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">OuterClass.<span class="hljs-built_in">this</span><br></code></pre></td></tr></table></figure><p>表示外部类引用。例如，可以像下面这样编写 <code>TimePrinter</code> 内部类的 <code>actionPerformed</code> 方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">actionPerformed</span><span class="hljs-params">(ActionEvent event)</span> &#123;<br>    <span class="hljs-keyword">if</span> (TalkingClock.<span class="hljs-built_in">this</span>.beep) Toolkit.getDefaultToolkit().beep();<br>&#125;<br></code></pre></td></tr></table></figure><p>反过来，可以采用以下语法更加明确地编写内部类对象的构造器：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">outerObject.<span class="hljs-keyword">new</span> <span class="hljs-title class_">InnerClass</span>(construction parameters)<br></code></pre></td></tr></table></figure><p>例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">ActionListener</span> <span class="hljs-variable">listener</span> <span class="hljs-operator">=</span> <span class="hljs-built_in">this</span>.<span class="hljs-keyword">new</span> <span class="hljs-title class_">TimePrinter</span>();<br></code></pre></td></tr></table></figure><p>在这里，新构造的 <code>TimePrinter</code> 对象的外部类引用被设置为创建内部类对象的方法的 <code>this</code> 引用。这是最常见的情况。通常，<code>this.</code> 限定符是多余的。不过，也有可能通过显式地命名将外部类引用设置为其他对象。例如，由于 <code>TimePrinter</code> 是一个公共内部类，可以为任意的语音时钟构造一个 <code>TimePrinter</code>：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">var</span> <span class="hljs-variable">jabberer</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">TalkingClock</span>(<span class="hljs-number">1000</span>, <span class="hljs-literal">true</span>);<br>TalkingClock.<span class="hljs-type">TimePrinter</span> <span class="hljs-variable">listener</span> <span class="hljs-operator">=</span> jabberer.<span class="hljs-keyword">new</span> <span class="hljs-title class_">TimePrinter</span>();<br></code></pre></td></tr></table></figure><p>需要注意，在外部类的作用域之外，可以这样引用内部类：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">OuterClass.InnerClass<br></code></pre></td></tr></table></figure><blockquote><p><strong>注释</strong>：<strong>内部类中声明的所有静态字段都必须是 <code>final</code></strong>，并初始化为一个编译时常量。如果这个字段不是一个常量，就可能不唯一。</p><p>内部类不能有 <code>static</code> 方法。Java 语言规范对这个限制没有做任何解释。</p></blockquote><h4 id="6-3-3-内部类是否有用、必要和安全"><a href="#6-3-3-内部类是否有用、必要和安全" class="headerlink" title="6.3.3 内部类是否有用、必要和安全"></a>6.3.3 内部类是否有用、必要和安全</h4><p>内部类将转换为常规的类文件，用 <code>$</code>（美元符号）分隔外部类名与内部类名。例如，<code>TalkingClock</code> 类内部的 <code>TimePrinter</code> 类将转换成类文件 <code>TalkingClock$TimePrinter.class</code>。为了查看它的实际工作，可以尝试下面的实验：运行第 5 章中的程序 <code>ReflectionTest</code>，并提供类 <code>TalkingClock$TimePrinter</code> 来完成反射。或者，也可以直接使用 <code>javap</code> 工具，如下所示：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">javap -private ClassName<br></code></pre></td></tr></table></figure><p>会得到以下输出结果：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">innerClass</span>.TalkingClock$TimePrinter<br>  <span class="hljs-keyword">implements</span> <span class="hljs-title class_">java</span>.awt.event.ActionListener<br>&#123;<br>  <span class="hljs-keyword">final</span> innerClass.TalkingClock <span class="hljs-built_in">this</span>$<span class="hljs-number">0</span>;<br>  <span class="hljs-keyword">public</span> innerClass.TalkingClock$TimePrinter(innerClass.TalkingClock);<br>  <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">actionPerformed</span><span class="hljs-params">(java.awt.event.ActionEvent)</span>;<br>&#125;<br></code></pre></td></tr></table></figure><p>可以清楚地看到，编译器生成了一个额外的实例字段 <code>this$0</code>，对应外部类的引用。（名字 <code>this$0</code> 是编译器合成的，在你自己编写的代码中不能引用这个字段。）另外，还可以看到构造器的 <code>TalkingClock</code> 参数。</p><p>如果编译器能够自动完成这个转换，那么能不能自己编写程序实现这种机制呢？让我们试试看。将 <code>TimePrinter</code> 定义成一个常规类，把它置于 <code>TalkingClock</code> 类的外部。在构造 <code>TimePrinter</code> 对象的时候，传入创建它的对象的 <code>this</code> 指针。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">class</span> <span class="hljs-title class_">TalkingClock</span> &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">start</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-type">var</span> <span class="hljs-variable">listener</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">TimePrinter</span>(<span class="hljs-built_in">this</span>);<br>        <span class="hljs-type">var</span> <span class="hljs-variable">timer</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Timer</span>(interval, listener);<br>        timer.start();<br>    &#125;<br>&#125;<br><br><span class="hljs-keyword">class</span> <span class="hljs-title class_">TimePrinter</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">ActionListener</span> &#123;<br>    <span class="hljs-keyword">private</span> TalkingClock outer;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-title function_">TimePrinter</span><span class="hljs-params">(TalkingClock clock)</span> &#123;<br>        outer = clock;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>现在来看 <code>actionPerformed</code> 方法，它需要访问 <code>outer.beep</code>：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">if</span> (outer.beep) ... <span class="hljs-comment">// ERROR</span><br></code></pre></td></tr></table></figure><p>这就遇到了一个问题。内部类可以访问外部类的私有数据，但我们的外部 <code>TimePrinter</code> 类则不行。</p><p>在 Java 11 之前，内部类纯粹是一种编译器现象，虚拟机对它们并没有任何特别的了解。那时，如果用 <code>ReflectionTest</code> 程序查看 <code>TalkingClock</code> 类，或者使用 <code>javap</code> 并提供 <code>-private</code> 选项来查看，会显示以下结果：</p><figure class="highlight arduino"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs arduino"><span class="hljs-keyword">class</span> <span class="hljs-title class_">TalkingClock</span><br>&#123;<br>  <span class="hljs-keyword">private</span> <span class="hljs-type">int</span> interval;<br>  <span class="hljs-keyword">private</span> <span class="hljs-type">boolean</span> beep;<br>  <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">TalkingClock</span><span class="hljs-params">(<span class="hljs-type">int</span>, <span class="hljs-type">boolean</span>)</span></span>;<br>  <span class="hljs-type">static</span> <span class="hljs-type">boolean</span> access$<span class="hljs-number">0</span>(TalkingClock); <span class="hljs-comment">// Prior to Java 11</span><br>  <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-type">void</span> <span class="hljs-title">start</span><span class="hljs-params">()</span></span>;<br>&#125;<br></code></pre></td></tr></table></figure><p>请注意编译器在外部类中添加的静态方法 <code>access$0</code>。它将返回作为参数传递的那个对象的 <code>beep</code> 字段。（方法名可能稍有不同，如可能是 <code>access$000</code>，这取决于你的编译器。）</p><p>在 Java 11 中，虚拟机了解类之间的嵌套关系，不再生成访问方法。</p><h4 id="6-3-4-局部内部类"><a href="#6-3-4-局部内部类" class="headerlink" title="6.3.4 局部内部类"></a>6.3.4 局部内部类</h4><p>如果仔细查看 <code>TalkingClock</code> 示例的代码就会发现，类型 <code>TimePrinter</code> 的名字只出现了一次：就是在 <code>start</code> 方法中创建这个类型的对象时使用了一次。</p><p>在类似这样的情况下，可以在一个方法中局部地定义这个类。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">start</span><span class="hljs-params">()</span> &#123;<br>    <span class="hljs-keyword">class</span> <span class="hljs-title class_">TimePrinter</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">ActionListener</span> &#123;<br>        <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">actionPerformed</span><span class="hljs-params">(ActionEvent event)</span> &#123;<br>            System.out.println(<span class="hljs-string">&quot;At the tone, the time is &quot;</span><br>                + Instant.ofEpochMilli(event.getWhen()));<br>            <span class="hljs-keyword">if</span> (beep) Toolkit.getDefaultToolkit().beep();<br>        &#125;<br>    &#125;<br><br>    <span class="hljs-type">var</span> <span class="hljs-variable">listener</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">TimePrinter</span>();<br>    <span class="hljs-type">var</span> <span class="hljs-variable">timer</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Timer</span>(interval, listener);<br>    timer.start();<br>&#125;<br></code></pre></td></tr></table></figure><p><strong>声明局部类时不能有访问说明符（即 <code>public</code> 或 <code>private</code>）。局部类的作用域总是限定在声明这个局部类的块中。</strong></p><p>局部类有一个很大的优势，即对外部世界完全隐藏，甚至 <code>TalkingClock</code> 类中的其他代码也不能访问它。除 <code>start</code> 方法之外，没有任何方法知道 <code>TimePrinter</code> 类的存在。</p><h4 id="6-3-5-由外部方法访问变量"><a href="#6-3-5-由外部方法访问变量" class="headerlink" title="6.3.5 由外部方法访问变量"></a>6.3.5 由外部方法访问变量</h4><p>与其他内部类相比较，局部类还有另外一个优点。它们不仅能够访问外部类的字段，还可以访问局部变量！不过，那些局部变量必须是<strong>事实最终变量（effectively final）</strong>。这说明，<strong>它们一旦赋值就绝不会改变。</strong></p><p>下面是一个典型的示例。这里，将 <code>TalkingClock</code> 构造器的参数 <code>interval</code> 和 <code>beep</code> 移至 <code>start</code> 方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">start</span><span class="hljs-params">(<span class="hljs-type">int</span> interval, <span class="hljs-type">boolean</span> beep)</span> &#123;<br>    <span class="hljs-keyword">class</span> <span class="hljs-title class_">TimePrinter</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">ActionListener</span> &#123;<br>        <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">actionPerformed</span><span class="hljs-params">(ActionEvent event)</span> &#123;<br>            System.out.println(<span class="hljs-string">&quot;At the tone, the time is &quot;</span><br>                + Instant.ofEpochMilli(event.getWhen()));<br>            <span class="hljs-keyword">if</span> (beep) Toolkit.getDefaultToolkit().beep();<br>        &#125;<br>    &#125;<br><br>    <span class="hljs-type">var</span> <span class="hljs-variable">listener</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">TimePrinter</span>();<br>    <span class="hljs-type">var</span> <span class="hljs-variable">timer</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Timer</span>(interval, listener);<br>    timer.start();<br>&#125;<br></code></pre></td></tr></table></figure><p>请注意，<code>TalkingClock</code> 类不再需要存储 <code>beep</code> 实例字段。它只是引用 <code>start</code> 方法的 <code>beep</code> 参数变量。</p><p>这看起来好像没什么值得大惊小怪的。毕竟，下面这行代码</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">if</span> (beep) ...<br></code></pre></td></tr></table></figure><p>最后总会在 <code>start</code> 方法中，为什么不能访问 <code>beep</code> 变量的值呢？</p><p>为了能够清楚地看到这里一个微妙的问题，让我们仔细考虑这个控制流程：</p><ol><li>调用 <code>start</code> 方法。  </li><li>调用内部类 <code>TimePrinter</code> 的构造器，从而初始化对象变量 <code>listener</code>。  </li><li>将 <code>listener</code> 引用传递给 <code>Timer</code> 构造器，定时器开始计时，<code>start</code> 方法退出。此时，<code>start</code> 方法的 <code>beep</code> 参数变量不复存在。  </li><li>1 秒之后，<code>actionPerformed</code> 方法执行 <code>if (beep)</code>…</li></ol><p>要让 <code>actionPerformed</code> 方法中的代码正常工作，<code>TimePrinter</code> 类必须在 <code>beep</code> 参数值消失之前将 <code>beep</code> 字段复制为 <code>start</code> 方法的一个局部变量。实际上也是这样做的。在我们的例子中，编译器为局部内部类合成了名字 <code>TalkingClock$1TimePrinter</code>。如果再次使用 <code>ReflectionTest</code> 程序或者 <code>javap</code> 工具查看 <code>TalkingClock$1TimePrinter</code> 类，就会看到以下结果：</p><figure class="highlight angelscript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs angelscript"><span class="hljs-keyword">class</span> <span class="hljs-symbol">TalkingClock</span>$<span class="hljs-symbol">1TimePrinter</span> &#123;<br>    TalkingClock$<span class="hljs-number">1</span>TimePrinter(<span class="hljs-built_in">bool</span>ean);<br>    <span class="hljs-keyword">public</span> <span class="hljs-built_in">void</span> actionPerformed(java.awt.event.ActionEvent);<br>    <span class="hljs-keyword">final</span> <span class="hljs-built_in">bool</span>ean val$beep;<br>    <span class="hljs-keyword">final</span> TalkingClock <span class="hljs-keyword">this</span>$<span class="hljs-number">0</span>;<br>&#125;<br></code></pre></td></tr></table></figure><p>创建一个对象的时候，<code>beep</code> 变量的当前值会存储在 <code>val$beep</code> 字段中。在 Java 11 中，可以利用“嵌套伴侣”（nestmate）访问来实现。之前，内部类构造器有一个额外的参数来设置这个字段。不论采用哪种方法，即使局部变量出了作用域，内部类字段都将持久保存。</p><h4 id="6-3-6-匿名内部类"><a href="#6-3-6-匿名内部类" class="headerlink" title="6.3.6 匿名内部类"></a>6.3.6 匿名内部类</h4><p>使用局部内部类时，通常还可以再进一步。假如只想创建这个类的一个对象，甚至不需要为类指定名字。这样一个类被称为<strong>匿名内部类（anonymous inner class）</strong>。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">start</span><span class="hljs-params">(<span class="hljs-type">int</span> interval, <span class="hljs-type">boolean</span> beep)</span> &#123;<br>    <span class="hljs-type">var</span> <span class="hljs-variable">listener</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">ActionListener</span>() &#123;<br>        <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">actionPerformed</span><span class="hljs-params">(ActionEvent event)</span> &#123;<br>            System.out.println(<span class="hljs-string">&quot;At the tone, the time is &quot;</span><br>                + Instant.ofEpochMilli(event.getWhen()));<br>            <span class="hljs-keyword">if</span> (beep) Toolkit.getDefaultToolkit().beep();<br>        &#125;<br>    &#125;;<br>    <span class="hljs-type">var</span> <span class="hljs-variable">timer</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Timer</span>(interval, listener);<br>    timer.start();<br>&#125;<br></code></pre></td></tr></table></figure><p>这个语法确实很晦涩难懂。它的含义是：创建一个类的新对象，这个类实现了 <code>ActionListener</code> 接口，需要实现的方法 <code>actionPerformed</code> 是在大括号 <code>&#123;&#125;</code> 中定义的方法。</p><p>一般地，语法如下：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">new</span> <span class="hljs-title class_">SuperType</span>(construction parameters) &#123;<br>    inner <span class="hljs-keyword">class</span> <span class="hljs-title class_">methods</span> and data<br>&#125;<br></code></pre></td></tr></table></figure><p>在这里，<code>SuperType</code> 可以是接口，如 <code>ActionListener</code>，如果是这样，内部类就要实现这个接口。<code>SuperType</code> 也可以是一个类，如果是这样，内部类就要扩展这个类。</p><p>**由于构造器的名字必须与类名相同，而匿名内部类没有类名，所以，匿名内部类不能有构造器。**实际上，构造参数要传递给超类（superclass）构造器。具体地，只要内部类实现一个接口，就不能有任何构造参数。不过，仍然要提供一组小括号，如下所示：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">new</span> <span class="hljs-title class_">InterfaceType</span>() &#123;<br>    methods and data<br>&#125;<br></code></pre></td></tr></table></figure><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">var</span> <span class="hljs-variable">queen</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Person</span>(<span class="hljs-string">&quot;Mary&quot;</span>);         <span class="hljs-comment">// a Person object</span><br><span class="hljs-type">var</span> <span class="hljs-variable">count</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Person</span>(<span class="hljs-string">&quot;Dracula&quot;</span>) &#123; ... &#125;; <span class="hljs-comment">// an object of an inner class extending Person</span><br></code></pre></td></tr></table></figure><p><strong>如果构造参数列表的结束小括号后面跟一个开始大括号，就是在定义匿名内部类。</strong></p><blockquote><p><strong>注释</strong>：<strong>尽管匿名类不能有构造器，但可以提供一个对象初始化块：</strong></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">var</span> <span class="hljs-variable">count</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Person</span>(<span class="hljs-string">&quot;Dracula&quot;</span>) &#123;<br>    <span class="hljs-comment">// initialization</span><br>&#125;;<br></code></pre></td></tr></table></figure></blockquote><blockquote><p><strong>注释</strong>：如果将一个匿名类实例存储在用 <code>var</code> 定义的一个变量中，这个变量会“了解”增加的方法或字段：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">var</span> <span class="hljs-variable">bob</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Object</span>() &#123; <span class="hljs-type">String</span> <span class="hljs-variable">name</span> <span class="hljs-operator">=</span> <span class="hljs-string">&quot;Bob&quot;</span>; &#125;;<br>System.out.println(bob.name);<br></code></pre></td></tr></table></figure><p>如果声明 <code>bob</code> 的类型为 <code>Object</code>，<code>bob.name</code> 将无法编译。</p><p>用 <code>new Object() &#123; String name = &quot;Bob&quot;; &#125;</code> 构造的对象类型为“有一个 <code>String name</code> 字段的 <code>Object</code>”。这是一个**“不可指示的”（nondenotable）**类型，即无法用 Java 语法表示的一个类型。不过，编译器理解这个类型，可以为 <code>bob</code> 变量设置这个类型。</p></blockquote><blockquote><p><strong>注释</strong>：下面的技巧称为**“双括号初始化”**（double brace initialization），这里利用了内部类语法。假设你想构造一个数组列表，并将它传递到一个方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">var</span> <span class="hljs-variable">friends</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">ArrayList</span>&lt;String&gt;();<br>friends.add(<span class="hljs-string">&quot;Harry&quot;</span>);<br>friends.add(<span class="hljs-string">&quot;Tony&quot;</span>);<br>invite(friends);<br></code></pre></td></tr></table></figure><p>如果不再需要这个数组列表，最好让它作为一个匿名列表。不过作为一个匿名列表，该如何为它添加元素呢？方法如下：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs java">invite(<span class="hljs-keyword">new</span> <span class="hljs-title class_">ArrayList</span>&lt;String&gt;() &#123;&#123;<br>    add(<span class="hljs-string">&quot;Harry&quot;</span>);<br>    add(<span class="hljs-string">&quot;Tony&quot;</span>);<br>&#125;&#125;);<br></code></pre></td></tr></table></figure><p>注意这里的双括号。外层括号建立了 <code>ArrayList</code> 的一个匿名子类。内层括号则是一个对象初始化块。</p><p>在实际中，这个技巧很少使用。大多数情况下，<code>invite</code> 方法会接受任何 <code>List&lt;String&gt;</code>，所以可以直接传入：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">List.of(<span class="hljs-string">&quot;Harry&quot;</span>, <span class="hljs-string">&quot;Tony&quot;</span>)<br></code></pre></td></tr></table></figure></blockquote><blockquote><p>⚠ <strong>警告</strong>：建立一个与超类大体类似（但并不完全相同）的匿名子类通常会很方便。不过对于 <code>equals</code> 方法要特别当心。第 5 章中，我们曾建议 <code>equals</code> 方法使用以下测试：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">if</span> (getClass() != other.getClass()) <span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;<br></code></pre></td></tr></table></figure><p>但是对于匿名子类，这个测试会失败。</p></blockquote><blockquote><p>💡 <strong>提示</strong>：生成日志或调试消息时，通常希望包含当前类的类名，如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">System.err.println(<span class="hljs-string">&quot;Something awful happened in &quot;</span> + getClass());<br></code></pre></td></tr></table></figure><p>不过，这对于静态方法不奏效。毕竟，调用 <code>getClass()</code> 时调用的是 <code>this.getClass()</code>，而静态方法没有 <code>this</code>。所以应该使用以下表达式：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">new</span> <span class="hljs-title class_">Object</span>() &#123;&#125;.getClass().getEnclosingClass() <span class="hljs-comment">// gets class of static method</span><br></code></pre></td></tr></table></figure><p>在这里，<code>new Object() &#123;&#125;</code> 会建立 <code>Object</code> 的匿名子类的一个匿名对象，<code>getEnclosingClass()</code> 则得到其外围类，也就是包含这个静态方法的类。</p></blockquote><h4 id="6-3-7-静态内部类"><a href="#6-3-7-静态内部类" class="headerlink" title="6.3.7 静态内部类"></a>6.3.7 静态内部类</h4><p>有时候，使用内部类只是为了把一个类隐藏在另外一个类的内部，并不需要内部类有外部类对象的一个引用。为此，可以将内部类声明为 <code>static</code>，这样就不会生成那个引用。</p><p>下面是一个想要使用静态内部类的典型例子。考虑这样一个任务：计算数组中的最小值和最大值。当然，可以编写两个方法，一个方法用于计算最小值，另一个方法用于计算最大值。在调用这两个方法的时候，数组被遍历两次。如果只遍历数组一次，同时计算出最小值和最大值，这样会更为高效。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">double</span> <span class="hljs-variable">min</span> <span class="hljs-operator">=</span> Double.POSITIVE_INFINITY;<br><span class="hljs-type">double</span> <span class="hljs-variable">max</span> <span class="hljs-operator">=</span> Double.NEGATIVE_INFINITY;<br><span class="hljs-keyword">for</span> (<span class="hljs-type">double</span> v : values) &#123;<br>    <span class="hljs-keyword">if</span> (min &gt; v) min = v;<br>    <span class="hljs-keyword">if</span> (max &lt; v) max = v;<br>&#125;<br></code></pre></td></tr></table></figure><p>不过，这个方法必须返回两个数，为此，可以定义一个包含两个值的类 <code>Pair</code>：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Pair</span> &#123;<br>    <span class="hljs-keyword">private</span> <span class="hljs-type">double</span> first;<br>    <span class="hljs-keyword">private</span> <span class="hljs-type">double</span> second;<br>    <span class="hljs-keyword">public</span> <span class="hljs-title function_">Pair</span><span class="hljs-params">(<span class="hljs-type">double</span> f, <span class="hljs-type">double</span> s)</span> <br>    &#123;<br>    first = f;<br>    second = s;<br>&#125;<br><span class="hljs-keyword">public</span> <span class="hljs-type">double</span> <span class="hljs-title function_">getFirst</span><span class="hljs-params">()</span> &#123; <span class="hljs-keyword">return</span> first; &#125;<br><span class="hljs-keyword">public</span> <span class="hljs-type">double</span> <span class="hljs-title function_">getSecond</span><span class="hljs-params">()</span> &#123; <span class="hljs-keyword">return</span> second; &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p><code>minmax</code> 方法可以返回一个 <code>Pair</code> 类型的对象。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">class</span> <span class="hljs-title class_">ArrayAlg</span> &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> Pair <span class="hljs-title function_">minmax</span><span class="hljs-params">(<span class="hljs-type">double</span>[] values)</span> &#123;<br>        <span class="hljs-comment">// ... 计算 min 和 max ...</span><br>        <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Pair</span>(min, max);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>这个方法的调用者可以使用 <code>getFirst</code> 和 <code>getSecond</code> 方法获得答案：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">Pair</span> <span class="hljs-variable">p</span> <span class="hljs-operator">=</span> ArrayAlg.minmax(d);<br>System.out.println(<span class="hljs-string">&quot;min = &quot;</span> + p.getFirst());<br>System.out.println(<span class="hljs-string">&quot;max = &quot;</span> + p.getSecond());<br></code></pre></td></tr></table></figure><p>当然，<code>Pair</code> 是一个十分大众化的名字。在大型项目中，其他程序员也很有可能使用这个名字，只不过可能会定义一个 <code>Pair</code> 类包含一对字符串。这样就会产生名字冲突，解决这个问题的办法是将 <code>Pair</code> 定义为 <code>ArrayAlg</code> 的一个公共内部类。这样一来，就可以通过 <code>ArrayAlg.Pair</code> 访问这个类了：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">ArrayAlg.<span class="hljs-type">Pair</span> <span class="hljs-variable">p</span> <span class="hljs-operator">=</span> ArrayAlg.minmax(d);<br></code></pre></td></tr></table></figure><p>不过，与前面例子中所使用的内部类不同，我们不希望 <code>Pair</code> 对象中有其他对象的引用，为此，可以将这个内部类声明为 <code>static</code>，从而不生成那个引用：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">class</span> <span class="hljs-title class_">ArrayAlg</span> &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Pair</span> &#123;<br>        <span class="hljs-comment">// ...</span><br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>当然，只有内部类可以声明为 <code>static</code>。静态内部类就类似于其他内部类，只不过静态内部类的对象没有其外部类对象的引用。在我们的示例中，必须使用静态内部类，这是因为内部类对象是在一个静态方法中构造的：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> Pair <span class="hljs-title function_">minmax</span><span class="hljs-params">(<span class="hljs-type">double</span>[] d)</span> &#123;<br>    <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Pair</span>(min, max);<br>&#125;<br></code></pre></td></tr></table></figure><p>如果没有将 <code>Pair</code> 类声明为 <code>static</code>，那么编译器将会报错，指出没有可用的 <code>ArrayAlg</code> 类型隐式对象来初始化内部类对象。</p><blockquote><p><strong>注释</strong>：只要内部类不需要访问外部类对象，就应该使用静态内部类。有些程序员用嵌套类（nested class）表示静态内部类。</p><p><strong>注释</strong>：与常规内部类不同，静态内部类可以有静态字段和方法。</p><p><strong>注释</strong>：在接口中声明的内部类自动是 <code>static</code> 和 <code>public</code>。</p><p><strong>注释</strong>：类中声明的接口、记录和枚举都自动为 <code>static</code>。</p></blockquote><h3 id="6-4-服务加载器"><a href="#6-4-服务加载器" class="headerlink" title="6.4 服务加载器"></a>6.4 服务加载器</h3><p>JDK 提供了一个加载服务的简单机制（这里会介绍）。这种机制由 Java 平台模块系统提供支持。</p><p>提供一个服务时，程序通常希望服务设计者对于如何实现这个服务的特性能有一些自由。另外还希望有多个实现可供选择。利用 <code>ServiceLoader</code> 类可以很容易地加载符合一个公共接口的服务。</p><p>**定义一个接口（或者，如果愿意，也可以定义一个超类），其中包含这个服务的各个实例应当提供的方法。**例如，假设你的服务要提供加密。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">package</span> serviceLoader;<br><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">interface</span> <span class="hljs-title class_">Cipher</span> &#123;<br>    <span class="hljs-type">byte</span>[] encrypt(<span class="hljs-type">byte</span>[] source, <span class="hljs-type">byte</span>[] key);<br>    <span class="hljs-type">byte</span>[] decrypt(<span class="hljs-type">byte</span>[] source, <span class="hljs-type">byte</span>[] key);<br>    <span class="hljs-type">int</span> <span class="hljs-title function_">strength</span><span class="hljs-params">()</span>;<br>&#125;<br></code></pre></td></tr></table></figure><p><strong>服务提供者可以提供一个或多个实现这个服务的类</strong>，例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">package</span> serviceLoader.impl;<br><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">CaesarCipher</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">Cipher</span> &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-type">byte</span>[] encrypt(<span class="hljs-type">byte</span>[] source, <span class="hljs-type">byte</span>[] key) &#123;<br>        <span class="hljs-type">var</span> <span class="hljs-variable">result</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">byte</span>[source.length];<br>        <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>; i &lt; source.length; i++)<br>            result[i] = (<span class="hljs-type">byte</span>)(source[i] + key[<span class="hljs-number">0</span>]);<br>        <span class="hljs-keyword">return</span> result;<br>    &#125;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-type">byte</span>[] decrypt(<span class="hljs-type">byte</span>[] source, <span class="hljs-type">byte</span>[] key) &#123;<br>        <span class="hljs-keyword">return</span> encrypt(source, <span class="hljs-keyword">new</span> <span class="hljs-title class_">byte</span>[] &#123; (<span class="hljs-type">byte</span>) -key[<span class="hljs-number">0</span>] &#125;);<br>    &#125;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-type">int</span> <span class="hljs-title function_">strength</span><span class="hljs-params">()</span> &#123; <span class="hljs-keyword">return</span> <span class="hljs-number">1</span>; &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p><strong>实现类可以放在任意的包中，而不一定是服务接口所在的包。每个实现类必须有一个无参数构造器。</strong></p><p>现在把这些类的类名增加到 <code>META-INF/services</code> 目录下的一个 UTF-8 编码的文本文件中，文件名必须与接口的完全限定名一致。在我们的例子中，文件 <code>META-INF/services/serviceLoader.Cipher</code> 必须包含这样一行：</p><figure class="highlight flix"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs flix">serviceLoader.<span class="hljs-keyword">impl</span>.CaesarCipher<br></code></pre></td></tr></table></figure><p>在这个例子中，我们提供了一个实现类。你也可以提供多个类，以后可以从中选择。</p><p>完成这个准备工作之后，程序可以如下初始化一个服务加载器：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> ServiceLoader&lt;Cipher&gt; cipherLoader = ServiceLoader.load(Cipher.class);<br></code></pre></td></tr></table></figure><p>这个初始化工作只在程序中完成一次。</p><p>服务加载器的 <code>iterator</code> 方法会返回一个迭代器来迭代处理所提供的所有服务实现。最容易的做法是使用一个增强的 <code>for</code> 循环进行遍历。在循环中，选择一个适当的对象来完成服务。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> Cipher <span class="hljs-title function_">getCipher</span><span class="hljs-params">(<span class="hljs-type">int</span> minStrength)</span> &#123;<br>    <span class="hljs-keyword">for</span> (Cipher cipher : cipherLoader) <span class="hljs-comment">// implicitly calls cipherLoader.iterator()</span><br>        <span class="hljs-keyword">if</span> (cipher.strength() &gt;= minStrength) <span class="hljs-keyword">return</span> cipher;<br>    <span class="hljs-keyword">return</span> <span class="hljs-literal">null</span>;<br>&#125;<br></code></pre></td></tr></table></figure><p>或者，也可以使用流查找我所要的服务。<code>stream()</code> 方法会生成 <code>ServiceLoader.Provider</code> 实例的一个流。这个接口包含 <code>type()</code> 和 <code>get()</code> 方法，可以用来得到提供者类和提供者实例。如果按类型选择一个提供者，只需要调用 <code>type()</code>，而没有必要实例化任何服务实例。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> Optional&lt;Cipher&gt; <span class="hljs-title function_">getCipher2</span><span class="hljs-params">(<span class="hljs-type">int</span> minStrength)</span> &#123;<br>    <span class="hljs-keyword">return</span> cipherLoader.stream()<br>        .filter(descr -&gt; descr.type() == serviceLoader.impl.CaesarCipher.class)<br>        .findFirst()<br>        .map(ServiceLoader.Provider::get);<br>&#125;<br></code></pre></td></tr></table></figure><p>最后，如果想要得到任何服务实例，只需要调用 <code>findFirst()</code>：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">Optional&lt;Cipher&gt; cipher = cipherLoader.findFirst();<br></code></pre></td></tr></table></figure><h3 id="6-5-代理"><a href="#6-5-代理" class="headerlink" title="6.5 代理"></a>6.5 代理</h3><p>在本章的最后，我们来讨论代理（proxy）。利用代理可以在运行时创建实现了一组给定接口的新类。只有在编译时无法确定需要实现哪个接口时才有必要使用代理。</p><h4 id="6-5-1-何时使用代理"><a href="#6-5-1-何时使用代理" class="headerlink" title="6.5.1 何时使用代理"></a>6.5.1 何时使用代理</h4><p>假设你想构造一个类的对象，这个类实现了一个或多个接口，但是在编译时你可能并不知道这些接口到底是什么。这个问题确实有些难度。要想构造一个具体的类，只需要使用 <code>newInstance</code> 方法或者使用反射找出构造器。但是，不能实例化接口。需要在运行的程序中定义一个新类。</p><p>为了解决这个问题，有些程序会生成代码，将这些代码放在一个文件中，调用编译器，然后再加载得到的类文件。很自然地，这样做的速度会比较慢，并且需要部署编译器以及程序。而代理机制则是一种更好的解决方案。代理类可以在运行时创建全新的类。这样一个代理类能够实现你指定的接口。具体地，代理类包含以下方法：</p><ul><li>指定接口所需要的全部方法。  </li><li><code>Object</code> 类中定义的全部方法（<code>toString</code>、<code>equals</code> 等）。</li></ul><p>不过，不能在运行时为这些方法定义新代码。实际上，必须提供一个调用处理器（invocation handler）。调用处理器是实现了 <code>InvocationHandler</code> 接口的类的对象。这个接口只有一个方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">Object <span class="hljs-title function_">invoke</span><span class="hljs-params">(Object proxy, Method method, Object[] args)</span><br></code></pre></td></tr></table></figure><p>无论何时调用代理对象的方法，都会调用这个调用处理器的 <code>invoke</code> 方法，并提供 <code>Method</code> 对象和原调用的参数。之后，调用处理器必须确定如何处理这个调用。</p><h4 id="6-5-2-创建代理对象"><a href="#6-5-2-创建代理对象" class="headerlink" title="6.5.2 创建代理对象"></a>6.5.2 创建代理对象</h4><p>要想创建一个代理对象，需要使用 <code>Proxy</code> 类的 <code>newProxyInstance</code> 方法。这个方法有三个参数：</p><ul><li>一个类加载器（class loader）。作为 Java 安全模型的一部分，对于平台和应用类、从因特网下载的类等等可以使用不同的类加载器。在这个例子中，我们指定了加载平台和应用类的“系统类加载器”。  </li><li>一个 <code>Class</code> 对象数组，每个元素对应需要实现的各个接口。  </li><li>一个调用处理器。</li></ul><p>还有两个需要解决的问题。如何定义处理器？另外，对于得到的代理对象能够做些什么？当然，这两个问题的答案取决于我们想要用代理机制解决什么问题。使用代理可能出于很多目的，例如：</p><ul><li>将方法调用路由到远程服务器。  </li><li>将用户界面事件与正在运行的程序中的动作关联起来。  </li><li>为了调试而跟踪方法调用。</li></ul><p>在示例程序中，我们要使用代理和调用处理器跟踪方法调用。我们定义了一个 <code>TraceHandler</code> 包装器类存储一个包装的对象，其 <code>invoke</code> 方法会打印所调用方法的名字和参数，随后调用这个方法，并提供所包装的对象作为隐式参数。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">class</span> <span class="hljs-title class_">TraceHandler</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">InvocationHandler</span> &#123;<br>    <span class="hljs-keyword">private</span> Object target;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-title function_">TraceHandler</span><span class="hljs-params">(Object t)</span> &#123;<br>        target = t;<br>    &#125;<br><br>    <span class="hljs-keyword">public</span> Object <span class="hljs-title function_">invoke</span><span class="hljs-params">(Object proxy, Method m, Object[] args)</span><br>            <span class="hljs-keyword">throws</span> Throwable &#123;<br>        <span class="hljs-comment">// print method name and parameters</span><br>        System.out.print(target + <span class="hljs-string">&quot;.&quot;</span> + m.getName() + <span class="hljs-string">&quot;(&quot;</span>);<br>        <span class="hljs-keyword">if</span> (args != <span class="hljs-literal">null</span>) &#123;<br>            <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>; i &lt; args.length; i++) &#123;<br>                <span class="hljs-keyword">if</span> (i &gt; <span class="hljs-number">0</span>) System.out.print(<span class="hljs-string">&quot;, &quot;</span>);<br>                System.out.print(args[i]);<br>            &#125;<br>        &#125;<br>        System.out.println(<span class="hljs-string">&quot;)&quot;</span>);<br><br>        <span class="hljs-comment">// invoke actual method</span><br>        <span class="hljs-keyword">return</span> m.invoke(target, args);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>可以如下构造一个代理对象，只要调用它的某个方法，就会触发跟踪行为：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">Object</span> <span class="hljs-variable">value</span> <span class="hljs-operator">=</span> ...;<br><br><span class="hljs-comment">// construct wrapper</span><br><span class="hljs-type">var</span> <span class="hljs-variable">handler</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">TraceHandler</span>(value);<br><br><span class="hljs-comment">// construct proxy for one or more interfaces</span><br><span class="hljs-type">Object</span> <span class="hljs-variable">proxy</span> <span class="hljs-operator">=</span> Proxy.newProxyInstance(<br>        ClassLoader.getSystemClassLoader(),<br>        <span class="hljs-keyword">new</span> <span class="hljs-title class_">Class</span>[] &#123; Comparable.class &#125;,<br>        handler);<br></code></pre></td></tr></table></figure><p>现在，只要在 <code>proxy</code> 上调用了某个接口的方法，就会打印这个方法的名字和参数，之后再用 <code>value</code> 调用这个方法。</p><p>在程序清单 6-10 所示的程序中，我们使用代理对象跟踪一个二分查找。这里首先在数组中填充整数 1～1000 的代理，然后调用 <code>Arrays</code> 类的 <code>binarySearch</code> 方法在数组中查找一个随机整数。最后，打印出匹配的元素。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">var</span> <span class="hljs-variable">elements</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Object</span>[<span class="hljs-number">1000</span>];<br><span class="hljs-comment">// fill elements with proxies for the integers 1 ... 1000</span><br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>; i &lt; elements.length; i++) &#123;<br>    <span class="hljs-type">Integer</span> <span class="hljs-variable">value</span> <span class="hljs-operator">=</span> i + <span class="hljs-number">1</span>;<br>    elements[i] = Proxy.newProxyInstance(...); <span class="hljs-comment">// proxy for value;</span><br>&#125;<br><br><span class="hljs-comment">// construct a random integer</span><br><span class="hljs-type">Integer</span> <span class="hljs-variable">key</span> <span class="hljs-operator">=</span> (<span class="hljs-type">int</span>) (Math.random() * elements.length) + <span class="hljs-number">1</span>;<br><br><span class="hljs-comment">// search for the key</span><br><span class="hljs-type">int</span> <span class="hljs-variable">result</span> <span class="hljs-operator">=</span> Arrays.binarySearch(elements, key);<br><br><span class="hljs-comment">// print match if found</span><br><span class="hljs-keyword">if</span> (result &gt;= <span class="hljs-number">0</span>) System.out.println(elements[result]);<br></code></pre></td></tr></table></figure><p><code>Integer</code> 类实现了 <code>Comparable</code> 接口。代理对象属于在运行时定义的一个类（它有一个类似 <code>$Proxy0</code> 的名字）。这个类也实现了 <code>Comparable</code> 接口。不过，它的 <code>compareTo</code> 方法调用了代理对象处理器的 <code>invoke</code> 方法。</p><blockquote><p>注释：在本章前面已经看到，<code>Integer</code> 类实际上实现了 <code>Comparable&lt;Integer&gt;</code>。不过，在运行时，所有的泛型类型都会擦除，会用对应原始 <code>Comparable</code> 类的类对象构造代理。</p></blockquote><p><code>binarySearch</code> 方法有以下调用：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">if</span> (elements[i].compareTo(key) &lt; <span class="hljs-number">0</span>) ...<br></code></pre></td></tr></table></figure><p>由于数组中填充了代理对象，所以 <code>compareTo</code> 会调用 <code>TraceHandler</code> 类中的 <code>invoke</code> 方法。这个方法会打印方法名和参数，之后在包装的 <code>Integer</code> 对象上调用 <code>compareTo</code>。</p><p>最后，在示例程序的最后调用：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">System.out.println(elements[result]);<br></code></pre></td></tr></table></figure><p>这个 <code>println</code> 方法调用代理对象的 <code>toString</code>，这个调用也会重定向到调用处理器。</p><p>下面是程序运行时完整的跟踪结果：</p><figure class="highlight scss"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs scss"><span class="hljs-number">588</span><span class="hljs-selector-class">.compareTo</span>(<span class="hljs-number">288</span>)<br><span class="hljs-number">294</span><span class="hljs-selector-class">.compareTo</span>(<span class="hljs-number">288</span>)<br><span class="hljs-number">375</span><span class="hljs-selector-class">.compareTo</span>(<span class="hljs-number">288</span>)<br><span class="hljs-number">312</span><span class="hljs-selector-class">.compareTo</span>(<span class="hljs-number">288</span>)<br><span class="hljs-number">281</span><span class="hljs-selector-class">.compareTo</span>(<span class="hljs-number">288</span>)<br><span class="hljs-number">296</span><span class="hljs-selector-class">.compareTo</span>(<span class="hljs-number">288</span>)<br><span class="hljs-number">288</span><span class="hljs-selector-class">.compareTo</span>(<span class="hljs-number">288</span>)<br><span class="hljs-number">288</span><span class="hljs-selector-class">.toString</span>()<br></code></pre></td></tr></table></figure><p>可以看到二分查找算法是如何查找 key 值的，每一步都会将查找区间缩减一半。注意，尽管 <code>toString</code> 方法不属于 <code>Comparable</code> 接口，但这个方法也会被代理。在下一节中会看到，某些 <code>Object</code> 方法总是会被代理。</p><h4 id="6-5-3-代理类的特性"><a href="#6-5-3-代理类的特性" class="headerlink" title="6.5.3 代理类的特性"></a>6.5.3 代理类的特性</h4><p>需要记住，代理类是在程序运行过程中动态创建的。不过，一旦创建，它们就是常规的类，与虚拟机中的任何其他类没有什么区别。</p><p>所有的代理类都扩展 <code>Proxy</code> 类。一个代理类只有一个实例字段——即调用处理器，它在 <code>Proxy</code> 超类中定义。完成代理对象任务所需要的任何额外数据都必须存储在调用处理器中。</p><p>所有的代理类都要覆盖 <code>Object</code> 类的 <code>toString</code>、<code>equals</code> 和 <code>hashCode</code> 方法。如同所有代理方法一样，这些方法只是在调用处理器上调用 <code>invoke</code>。<code>Object</code> 类中的其他方法（如 <code>clone</code> 和 <code>getClass</code>）没有重新定义。</p><p>没有定义代理类的名字，Oracle 虚拟机中的 <code>Proxy</code> 类会生成以字符串 <code>$Proxy</code> 开头的类名。对于一个特定的类加载器和一组接口，只能有一个代理类。也就是说，如果使用同一个类加载器和接口数组调用两次 <code>newProxyInstance</code> 方法，将得到同一个类的两个对象。也可以利用 <code>getProxyClass</code> 方法获得这个类：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">Class</span> <span class="hljs-variable">proxyClass</span> <span class="hljs-operator">=</span> Proxy.getProxyClass(<span class="hljs-literal">null</span>, interfaces);<br></code></pre></td></tr></table></figure><p>代理类总是 <code>public</code> 和 <code>final</code>。如果代理类实现的所有接口都是 <code>public</code>，这个代理类就不属于任何特定的包；否则，所有非公共的接口都必须属于同一个包，而且代理类也属于这个包。</p><p>可以通过调用 <code>Proxy</code> 类的 <code>isProxyClass</code> 方法检测一个特定的 <code>Class</code> 对象是否表示一个代理类。</p><blockquote><p><strong>注释</strong>：调用一个目标代理的默认方法会触发调用处理器。要具体调用这个方法，可以使用 <code>InvocationHandler</code> 接口的静态方法 <code>invokeDefault</code>。例如，下面是一个调用处理器，它会调用默认方法，并把抽象方法传递到另一个目标：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">InvocationHandler</span> <span class="hljs-variable">handler</span> <span class="hljs-operator">=</span> (proxy, method, args) -&gt; &#123;<br>    <span class="hljs-keyword">if</span> (method.isDefault())<br>        <span class="hljs-keyword">return</span> InvocationHandler.invokeDefault(proxy, method, args);<br>    <span class="hljs-keyword">else</span><br>        <span class="hljs-keyword">return</span> method.invoke(target, args);<br>&#125;<br></code></pre></td></tr></table></figure></blockquote>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>《Java核心技术卷一》第四章</title>
    <link href="/2025/10/30/%E3%80%8AJava%E6%A0%B8%E5%BF%83%E6%8A%80%E6%9C%AF%E5%8D%B7%E4%B8%80%E3%80%8B%E7%AC%AC%E5%9B%9B%E7%AB%A0/"/>
    <url>/2025/10/30/%E3%80%8AJava%E6%A0%B8%E5%BF%83%E6%8A%80%E6%9C%AF%E5%8D%B7%E4%B8%80%E3%80%8B%E7%AC%AC%E5%9B%9B%E7%AB%A0/</url>
    
    <content type="html"><![CDATA[<h1 id="第4章-对象与类"><a href="#第4章-对象与类" class="headerlink" title="第4章 对象与类"></a>第4章 对象与类</h1><h2 id="4-1-面向对象程序设计概述"><a href="#4-1-面向对象程序设计概述" class="headerlink" title="4.1 面向对象程序设计概述"></a>4.1 面向对象程序设计概述</h2><p>**面向对象程序设计（Object-Oriented Programming, OOP）**是当今的主流程序设计范型，它取代了 20 世纪 70 年代的”结构化”或过程式编程技术。</p><p> <strong>Java 是面向对象的。</strong></p><p>面向对象的程序是由对象组成的，每个对象包含对用户公开的特定功能和隐藏的实现。程序中的很多对象是来自标准类的”成员”，还有一些是自定义的。究竟是自己构造对象，还是从外界购买，这完全取决于开发项目的预算和时间。但是，从根本上说，只要对象能够满足要求，就不必关心其功能到底是如何实现的。</p><p>传统的结构化程序设计通过设计一系列的过程（即算法）来求解问题。一旦确定了这些过程，下一步往往要考虑存储数据的适当方式。首先，他们会确定操作数据的过程，然后再决定如何组织数据的结构，以便于操作数据。而 OOP 却调整了这个次序，将数据放在第一位，然后再考虑操作数据的算法。</p><h4 id="4-1-1-类"><a href="#4-1-1-类" class="headerlink" title="4.1.1 类"></a>4.1.1 类</h4><p><strong>类（class）</strong> 指定了如何构造对象。可以将类想象成制作小甜饼的模具，将对象想象为小甜饼。由一个类<strong>构造（construct）</strong> 对象的过程称为创建这个类的一个<strong>实例（instance）</strong>。</p><p>用 Java 编写的所有代码都在某个类中。</p><p><strong>封装（encapsulation，有时称为信息隐藏）<strong>是处理对象的一个重要概念。从形式上看，封装就是将数据和行为组合在一个包中，并对对象的使用者隐藏具体的实现细节。对象中的数据称为</strong>实例字段（instance field）</strong>，操作数据的过程称为<strong>方法（method）</strong>。作为一个类的实例，一个特定对象有一组特定的实例字段值。这些值的集合就是这个对象的当前<strong>状态（state）</strong>。只要在对象上调用一个方法，它的状态就有可能发生改变。</p><p>实现封装的关键在于，绝对不能让其他类中的方法直接访问这个类的实例字段。程序只能通过对象的方法与对象数据进行交互。封装给对象赋予了 “ 黑盒 ” 特征，这是提高可用性和可靠性的关键。这意味着一个类可以完全改变存储数据的方式，只要仍旧使用同样的方法操作数据，其他对象就不会知道也不用关心这个类所发生的变化。</p><p>OOP 的另一个原则会让用户自定义 Java 类变得更为容易，这就是：可以通过<strong>扩展</strong>其他类来构建新类。事实上，Java 提供了一个“普通”的类，名为 Object。所有其他类都扩展了这个 Object 类。</p><p>扩展一个已有的类时，这个新类具有被扩展的那个类的全部属性和方法。你只需要在新类中提供适用于这个新类的新方法和实例字段。通过扩展一个类来得到另外一个类的概念称为<strong>继承</strong>（inheritance）。</p><h4 id="4-1-2-对象"><a href="#4-1-2-对象" class="headerlink" title="4.1.2 对象"></a>4.1.2 对象</h4><p>要想使用 OOP，一定要清楚对象的三个主要特性：</p><ul><li>对象的<strong>行为</strong>（behavior）——可以对这个对象做哪些操作，或者可以对这个对象应用哪些方法？</li><li>对象的<strong>状态</strong>（state）——调用那些方法时，对象会如何响应？</li><li>对象的<strong>标识</strong>（identity）——如何区分可能有相同行为和状态的不同对象？</li></ul><p>同一个类的所有实例对象都有一种家族相似性，它们都支持相同的行为。一个对象的行为由所能调用的方法来定义。</p><p>此外，每个对象都会保存着描述当前状况的信息，这就是对象的状态。对象的状态可能会随着时间而发生改变，但这种改变不是自发的。对象状态的改变必然是调用方法的结果（如果不经过方法调用就可以改变对象状态，这说明破坏了封装性）。</p><p>但是，对象的状态并不能完全描述一个对象，因为每个对象都有一个唯一的<strong>标识</strong>（identity，或称身份）。例如，在一个订单处理系统中，任何两个订单都是不同的，即使它们订购的商品完全相同。需要注意，作为同一个类的实例，每个对象的标识总是不同的，状态也通常有所不同。</p><p>对象的这些关键特性会彼此相互影响。例如，对象的状态会影响它的行为（如果一个订单”已发货”或”已付款”，就应该拒绝要求增加商品的方法调用。反过来，如果订单是”空的”，即还没有订购任何商品，就不应该允许”发货”。</p><h4 id="4-1-3-识别类"><a href="#4-1-3-识别类" class="headerlink" title="4.1.3 识别类"></a>4.1.3 识别类</h4><p>传统的过程式程序中，必须从最上面的 main 函数开始编写程序。设计一个面向对象系统时，则没有所谓的”最上面”。因此，学习 OOP 的初学者常常会感觉无从下手。答案是：首先从识别类开始，然后再为各个类添加方法。</p><p>识别类的一个简单经验是在分析问题的过程中寻找名词，而方法对应动词。例如，在订单处理系统中，有这样一些名词：</p><ul><li>商品（Item）；</li><li>订单（Order）；</li><li>发货地址（Shipping address）；</li><li>付款（Payment）；</li><li>账户（Account）。</li></ul><p>从这些名词就可以得到类 Item、Order 等。</p><p>接下来查找动词。商品要添加（add）到订单中，订单可以发货（ship）或取消（cancel），另外可以对订单完成付款（apply）。对于每一个动词，如”添加””发货””取消”或者”完成付款”，要识别出负责完成相应动作的对象。例如，当一个新商品添加到订单中时，订单对象就是负责的对象，因为它知道如何存储商品以及如何对商品进行排序。也就是说，add 应该是 Order 类的一个方法，它接受一个 Item 对象作为参数。</p><p>当然，这种”名词与动词”原则只是一种经验，在创建类的时候，只有经验能帮助你确定哪些名词和动词重要。</p><h4 id="4-1-4-类之间的关系"><a href="#4-1-4-类之间的关系" class="headerlink" title="4.1.4 类之间的关系"></a>4.1.4 类之间的关系</h4><p>类之间最常见的关系有</p><ul><li><strong>依赖</strong>（ “uses-a” ）；</li><li><strong>聚合</strong>（ “has-a” ）；</li><li><strong>继承</strong>（ “is-a” ）。</li></ul><p><strong>依赖（dependence）</strong>，即 “ uses-a “ 关系，是一种最明显的也最一般的关系。例如，Order 类使用了 Account 类，因为 Order 对象需要访问 Account 对象来查看信用状态。但是 Item 类不依赖于 Account 类，因为 Item 对象不需要考虑客户账户。因此，如果一个类的方法要使用或操作另一个类的对象，我们就说前一个类依赖于后一个类。</p><p>应当尽可能减少相互依赖的类。这里的关键是，如果类 A 不知道 B 的存在，它就不会关心 B 的任何改变（这意味着 B 的改变不会在 A 中引入 bug）。用软件工程的方法来说，就是要尽可能减少类之间的<strong>耦合</strong>（coupling）。</p><p><strong>聚合（aggregation）</strong>，即 “ has-a “ 关系，很容易理解，因为这种关系很具体。例如，一个 Order 对象包含一些 Item 对象。包含关系意味着类 A 的对象包含类 B 的对象。</p><p><strong>注释：</strong> 有些方法学家不喜欢聚合这个概念，而更喜欢使用更一般的”关联”关系。从建模的角度看，这是可以理解的。但对于程序员来说，”has-a”关系更加形象。我喜欢使用聚合还有另一个原因：关联的标准记法不是很清楚，请参见表 4-1。</p><p><strong>表 4-1 表达类关系的 UML 记法</strong></p><table><thead><tr><th>关系</th><th>UML 连接符</th></tr></thead><tbody><tr><td>继承</td><td>─────▷</td></tr><tr><td>接口实现</td><td>─ - - - ▷</td></tr><tr><td>依赖</td><td>┄┄┄┄&gt;</td></tr><tr><td>聚合</td><td>◇─────</td></tr><tr><td>关联</td><td>──────</td></tr><tr><td>直接关联</td><td>─────&gt;</td></tr></tbody></table><p><strong>继承</strong>（inheritance），即”is-a”关系，表示一个更特殊的类与一个更一般的类之间的关系。例如，RushOrder 类继承了 Order 类。在特殊化的 RushOrder 类中包含一些用于优先处理的特殊方法，还提供了一个计算运费的不同方法；而其他的方法，如添加商品、生成账单等都是从 Order 类继承来的。一般而言，如果类 D 扩展了类 C，类 D 会继承类 C 的方法，另外还会有一些额外的功能。</p><h3 id="4-2-使用预定义类"><a href="#4-2-使用预定义类" class="headerlink" title="4.2 使用预定义类"></a>4.2 使用预定义类</h3><p>在 Java 中，没有类就无法做任何事情，我们前面曾经接触过几个类。然而，并不是所有的类都表现出面向对象的典型特征。以 Math 类为例。你已经看到，可以直接使用 Math 类的方法，如 Math.random，而不必了解它具体是如何实现的，你只需要知道方法名和参数（如果有的话）。这正是封装的关键所在，当然所有类都是这样。但 Math 类只封装了功能，它不需要也不必隐藏数据。由于没有数据，因此也不必考虑创建对象和初始化它们的实例字段，因为根本没有实例字段！</p><p>下一节将会介绍一个更典型的类——Date 类，从中可以了解如何构造对象，以及如何调用类的方法。</p><h4 id="4-2-1-对象与对象变量"><a href="#4-2-1-对象与对象变量" class="headerlink" title="4.2.1 对象与对象变量"></a>4.2.1 对象与对象变量</h4><p>要想使用对象，首先必须构造对象，并指定其初始状态。然后对对象应用方法。</p><p>在 Java 程序设计语言中，要使用**构造器（constructor，或称构造函数）**构造新实例。构造器是一种特殊的方法，其作用是构造并初始化对象。下面来看一个例子。标准 Java 库中包含一个 Date 类。它的对象可以描述一个时间点，例如，”December 31, 1999, 23:59:59 GMT”。</p><p>***构造器总是与类同名。***因此，Date 类的构造器就名为 Date。要想构造一个 Date 对象，需要在构造器前面加上 new 操作符，如下所示：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">new</span> <span class="hljs-title class_">Date</span>()<br></code></pre></td></tr></table></figure><p>这个表达式会构造一个新对象。这个对象初始化为当前的日期和时间。</p><p>如果需要的话，可以将这个对象传递给一个方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">System.out.println(<span class="hljs-keyword">new</span> <span class="hljs-title class_">Date</span>());<br></code></pre></td></tr></table></figure><p>或者，可以对刚构造的对象应用一个方法。Date 类中有一个 toString 方法。这个方法将生成日期的一个字符串描述。可以如下对新构造的 Date 对象应用 toString 方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">String</span> <span class="hljs-variable">s</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Date</span>().toString();<br></code></pre></td></tr></table></figure><p>在这两个例子中，构造的对象仅使用了一次。通常，你可能希望保留所构造的对象从而能继续使用，为此，需要将对象存放在一个变量中：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">Date</span> <span class="hljs-variable">rightNow</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Date</span>();<br></code></pre></td></tr></table></figure><p>图 4-3 显示了对象变量 rightNow，它引用了新构造的对象。</p><p><img src="/2025/10/30/%E3%80%8AJava%E6%A0%B8%E5%BF%83%E6%8A%80%E6%9C%AF%E5%8D%B7%E4%B8%80%E3%80%8B%E7%AC%AC%E5%9B%9B%E7%AB%A0/image-20251101154933019.png" alt="image-20251101154933019"></p><p>​<strong>图 4-3  创建一个新对象</strong></p><p>对象与对象变量之间存在着一个重要的区别。例如，以下语句</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">Date startTime; <span class="hljs-comment">// startTime doesn&#x27;t refer to any object</span><br></code></pre></td></tr></table></figure><p>定义了一个对象变量 startTime，它可以引用 Date 类型的对象。但是，一定要认识到：***变量 startTime 不是一个对象，而且实际上它甚至还没有引用任何对象。***此时不能在这个变量上使用任何 Date 方法。下面的语句</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">s = startTime.toString(); <span class="hljs-comment">// not yet</span><br></code></pre></td></tr></table></figure><p>将产生编译错误。</p><p><em><strong>必须首先初始化 startTime 变量</strong></em>，这里有两个选择。当然，可以初始化这个变量，让它引用一个新构造的对象：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">startTime = <span class="hljs-keyword">new</span> <span class="hljs-title class_">Date</span>();<br></code></pre></td></tr></table></figure><p>也可以设置这个变量，让它引用一个已有的对象：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">startTime = rightNow;<br></code></pre></td></tr></table></figure><p>现在，这两个变量都引用同一个对象（如图 4-4 所示）。</p><p><img src="/2025/10/30/%E3%80%8AJava%E6%A0%B8%E5%BF%83%E6%8A%80%E6%9C%AF%E5%8D%B7%E4%B8%80%E3%80%8B%E7%AC%AC%E5%9B%9B%E7%AB%A0/image-20251101155157478.png" alt="image-20251101155157478"></p><p>​<strong>图 4-4 对象变量引用同一个对象</strong></p><p>要认识到重要的一点：<em><strong>对象变量并不实际包含一个对象，它只是引用一个对象。</strong></em><br><em><strong>在 Java 中，任何对象变量的值都是一个引用，指向存储在另外一个地方的某个对象。</strong></em></p><p>new 操作符的返回值也是一个引用。下面的语句：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">Date</span> <span class="hljs-variable">startTime</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Date</span>();<br></code></pre></td></tr></table></figure><p>有两个部分。<em>表达式 new Date( ) 构造了一个 Date 类型的对象，它的值是新创建对象的一个引用。再将这个引用存储在 startTime 变量中。</em></p><p>可以显式地将对象变量设置为 null，指示这个对象变量目前没有引用任何对象。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs java">startTime = <span class="hljs-literal">null</span>;<br>...<br><span class="hljs-keyword">if</span> (startTime != <span class="hljs-literal">null</span>)<br>    System.out.println(startTime);<br></code></pre></td></tr></table></figure><p>我们将在 4.3.6 节更详细地讨论 null。</p><p><strong>C++ 注释：</strong> 很多人错误地认为 Java 中的对象变量就相当于 C++ 中的引用。然而，C++ 中没有 null 引用，而且引用不能赋值。应当把 Java 中的对象变量看作类似于 C++ 的对象指针。例如，</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">Date rightNow; <span class="hljs-comment">// Java</span><br></code></pre></td></tr></table></figure><p>实际上等同于</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs cpp">Date* rightNow; <span class="hljs-comment">// C++</span><br></code></pre></td></tr></table></figure><p>一旦建立了这种关联，一切就清楚了。当然，只有使用了 new 调用后 Date* 指针才会初始化。就这一点而言，C++ 与 Java 的语法几乎是一样的。</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs cpp">Date* rightNow = <span class="hljs-keyword">new</span> <span class="hljs-built_in">Date</span>(); <span class="hljs-comment">// C++</span><br></code></pre></td></tr></table></figure><p>**如果把一个变量复制到另一个变量，两个变量就指向同一个日期，即它们是同一个对象的指针。**Java 中的 null 引用对应于 C++ 中的 NULL 指针。</p><p><strong>所有的 Java 对象都存储在堆中。当一个对象包含另一个对象变量时，它只是包含另一个堆对象的指针。</strong></p><p>在 C++ 中，指针十分令人头疼，因为它们很容易出错。稍不小心就会创建一个错误的指针，或者使内存管理出现问题。在 Java 语言中，这些问题都不存在。如果使用一个没有初始化的指针，那么运行时系统将会产生一个运行时错误，而不是生成随机的结果。另外，你不必担心内存管理问题，垃圾回收器会处理相关的事宜。</p><p>C++ 确实做了很大的努力，它通过支持复制构造器和赋值运算符来实现对象的自动复制。例如，一个链表（linked list）的副本是一个新链表，其内容与原始链表相同，但是有一组独立的链接。这样一来就可以适当地设计类，使它们与内置类型有相同的复制行为。在 Java 中，必须使用 clone 方法获得一个对象的完整副本。</p><h4 id="4-2-2-Java-类库中的-LocalDate-类"><a href="#4-2-2-Java-类库中的-LocalDate-类" class="headerlink" title="4.2.2 Java 类库中的 LocalDate 类"></a>4.2.2 Java 类库中的 LocalDate 类</h4><p>在前面的例子中，我们使用了 Java 标准类库中的 Date 类。Date 类的实例有一个状态，也就是一个特定的时间点。</p><p>尽管在使用 Date 类时不必知道这一点，但时间是用距离一个固定时间点的毫秒数（可正可负）表示的，这个时间点就是所谓的<strong>纪元</strong>（epoch），它是 UTC 时间 1970 年 1 月 1 日 00:00:00。UTC 就是 Coordinated Universal Time（国际协调时间），与大家熟悉的 GMT（即 Greenwich Mean Time，格林尼治时间）一样，是一种实用的科学标准时间。</p><p>但是，Date 类对于处理人类记录日期的日历信息并不是很有用，如 “December 31, 1999”。这个特定的日期描述遵循 Gregorian 阳历，这是世界上大多数国家使用的日历。但是，同样的这个时间点采用中国或希伯来的阴历来描述会大不相同。</p><p>类库设计者决定将保存时间与给时间点命名分开。所以，标准 Java 类库分别包含了两个类：一个是用来表示时间点的 Date 类；另一个是用来表示熟悉的日历表示法表示日期的 LocalDate 类。Java 8 引入了另外一些类来处理日期和时间的不同方面。</p><p>将时间度量与日历分开是一种很好的面向对象设计。通常，最好使用不同的类表示不同的概念。</p><p><strong>不要使用构造器来构造 LocalDate 类的对象。</strong> 实际上，应当使用<strong>静态工厂方法</strong>（factory method），<strong>它会代表你调用构造器</strong>。下面的表达式：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">LocalDate.now()<br></code></pre></td></tr></table></figure><p>会构造一个新对象，表示构造这个对象时的日期。</p><p>可以提供年、月和日来构造对应一个特定日期的对象：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">LocalDate.of(<span class="hljs-number">1999</span>, <span class="hljs-number">12</span>, <span class="hljs-number">31</span>)<br></code></pre></td></tr></table></figure><p>当然，通常我们都希望将构造的对象保存在一个对象变量中：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">LocalDate</span> <span class="hljs-variable">newYearsEve</span> <span class="hljs-operator">=</span> LocalDate.of(<span class="hljs-number">1999</span>, <span class="hljs-number">12</span>, <span class="hljs-number">31</span>);<br></code></pre></td></tr></table></figure><p>一旦有了一个 LocalDate 对象，可以用方法 getYear、getMonthValue 和 getDayOfMonth 得到年、月和日：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">int</span> <span class="hljs-variable">year</span> <span class="hljs-operator">=</span> newYearsEve.getYear(); <span class="hljs-comment">// 1999</span><br><span class="hljs-type">int</span> <span class="hljs-variable">month</span> <span class="hljs-operator">=</span> newYearsEve.getMonthValue(); <span class="hljs-comment">// 12</span><br><span class="hljs-type">int</span> <span class="hljs-variable">day</span> <span class="hljs-operator">=</span> newYearsEve.getDayOfMonth(); <span class="hljs-comment">// 31</span><br></code></pre></td></tr></table></figure><p>看起来这似乎没有多大的意义，因为这是构造对象时使用的那些值。不过，有时可能有一个计算得到的日期，然后你希望调用这些方法来了解它的更多信息。例如，plusDays 方法会生成一个新的 LocalDate*，如果把应用这个方法的对象称为当前对象，那么这个新日期对象则是距当前对象指定天数的一个新日期*：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">LocalDate</span> <span class="hljs-variable">aThousandDaysLater</span> <span class="hljs-operator">=</span> newYearsEve.plusDays(<span class="hljs-number">1000</span>);<br>year = aThousandDaysLater.getYear(); <span class="hljs-comment">// 2002</span><br>month = aThousandDaysLater.getMonthValue(); <span class="hljs-comment">// 09</span><br>day = aThousandDaysLater.getDayOfMonth(); <span class="hljs-comment">// 26</span><br></code></pre></td></tr></table></figure><p>LocalDate 类封装了一些实例字段来维护所设置的日期。如果不查看源代码，就不可能知道类内部的日期表示。当然，封装的定义就在于内部表示并不重要，重要的是类对外提供的方法。</p><p><strong>注释：</strong> 实际上，Date 类也有得到日、月、年的方法，分别是 getDay、getMonth 以及 getYear，不过这些方法已经<strong>废弃</strong>。当类库设计者意识到某个方法最初就不该引入时，就把它标记为废弃，不鼓励使用。最好不要使用废弃的方法，因为将来的某个类库版本很有可能会将它们完全删除。</p><p><strong>提示：</strong> JDK提供了jdeprscan工具来检查你的代码中是否使用了Java API已经废弃的特性。有关说明参见<a href="https://docs.oracle.com/en/java/javase/17/docs/specs/man/jdeprscan.html%E3%80%82">https://docs.oracle.com/en/java/javase/17/docs/specs/man/jdeprscan.html。</a></p><h4 id="4-2-3-更改器方法与访问器方法"><a href="#4-2-3-更改器方法与访问器方法" class="headerlink" title="4.2.3 更改器方法与访问器方法"></a>4.2.3 更改器方法与访问器方法</h4><p>再来看上一节中的plusDays方法调用：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">LocalDate</span> <span class="hljs-variable">aThousandDaysLater</span> <span class="hljs-operator">=</span> newYearsEve.plusDays(<span class="hljs-number">1000</span>);<br></code></pre></td></tr></table></figure><p>这个调用之后 newYearsEve 会有什么变化？它会改为1000天之后的日期吗？事实上，并没有。p<em>lusDays 方法会生成一个新的 LocalDate 对象</em>，然后把这个新对象赋给 aThousandDaysLater 变量。原来的对象不做任何改动。我们说plusDays 方法没有**更改（mutate）**调用这个方法的对象。（这类似于第3章中见过的 String 类的 toUpperCase 方法。在一个字符串上调用 toUpperCase 时，这个字符串仍保持不变，并返回一个包含大写字符的新字符串。）</p><p>Java库的一个较早版本曾经有另一个处理日历的类，名为 GregorianCalendar 。可以如下为这个类表示的一个日期增加1000天：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">GregorianCalendar</span> <span class="hljs-variable">someDay</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">GregorianCalendar</span>(<span class="hljs-number">1999</span>, <span class="hljs-number">11</span>, <span class="hljs-number">31</span>);<br><span class="hljs-comment">// odd feature of that class: month numbers go from 0 to 11</span><br>someDay.add(Calendar.DAY_OF_MONTH, <span class="hljs-number">1000</span>);<br></code></pre></td></tr></table></figure><p>与 LocalDate.plusDays 方法不同，GregorianCalendar.add 方法是一个<strong>更改器方法</strong>（mutator method）。调用这个方法后，someDay 对象的状态会改变。可以如下查看新状态：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java">year = someDay.get(Calendar.YEAR); <span class="hljs-comment">// 2002</span><br>month = someDay.get(Calendar.MONTH) + <span class="hljs-number">1</span>; <span class="hljs-comment">// 09</span><br>day = someDay.get(Calendar.DAY_OF_MONTH); <span class="hljs-comment">// 26</span><br></code></pre></td></tr></table></figure><p>正是因为这个原因，我们将变量命名为 someDay 而不是 newYearsEve ——调用这个更改器方法之后，它不再是新年前夜。</p><p>相反，<strong>只访问对象而不修改对象的方法有时称为<em>访问器方法</em></strong>（accessor method）。例如，LocalDate.getYear 和GregorianCalendar.get 就是访问器方法。</p><p>在Java语言中，访问器方法与更改器方法在语法上没有明显的区别。</p><p>下面用一个具体应用 LocalDate 类的程序来结束这一节。这个程序将显示当前月的日历，格式如下：</p><figure class="highlight apache"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs apache"><span class="hljs-attribute">Mon</span> Tue Wed Thu Fri Sat Sun<br>                  <span class="hljs-attribute">1</span><br>  <span class="hljs-attribute">2</span>   <span class="hljs-number">3</span>   <span class="hljs-number">4</span>   <span class="hljs-number">5</span>   <span class="hljs-number">6</span>   <span class="hljs-number">7</span>   <span class="hljs-number">8</span><br>  <span class="hljs-attribute">9</span>  <span class="hljs-number">10</span>  <span class="hljs-number">11</span>  <span class="hljs-number">12</span>  <span class="hljs-number">13</span>  <span class="hljs-number">14</span>  <span class="hljs-number">15</span><br> <span class="hljs-attribute">16</span>  <span class="hljs-number">17</span>  <span class="hljs-number">18</span>  <span class="hljs-number">19</span>  <span class="hljs-number">20</span>  <span class="hljs-number">21</span>  <span class="hljs-number">22</span><br> <span class="hljs-attribute">23</span>  <span class="hljs-number">24</span>  <span class="hljs-number">25</span>  <span class="hljs-number">26</span>* <span class="hljs-number">27</span>  <span class="hljs-number">28</span>  <span class="hljs-number">29</span><br> <span class="hljs-attribute">30</span><br></code></pre></td></tr></table></figure><p>当前日期标记有一个 * 号。可以看到，这个程序需要知道如何计算某月份的天数以及一个给定日期是星期几。</p><p>下面来看这个程序的关键步骤。首先构造一个对象，并用当前的日期初始化。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">LocalDate</span> <span class="hljs-variable">date</span> <span class="hljs-operator">=</span> LocalDate.now();<br></code></pre></td></tr></table></figure><p>下面获得当前的月份和日期。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">int</span> <span class="hljs-variable">month</span> <span class="hljs-operator">=</span> date.getMonthValue();<br><span class="hljs-type">int</span> <span class="hljs-variable">today</span> <span class="hljs-operator">=</span> date.getDayOfMonth();<br></code></pre></td></tr></table></figure><p>然后，将 date 设置为这个月的第一天，并得到这一天为星期几。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java">date = date.minusDays(today - <span class="hljs-number">1</span>); <span class="hljs-comment">// set to start of month</span><br><span class="hljs-type">DayOfWeek</span> <span class="hljs-variable">weekday</span> <span class="hljs-operator">=</span> date.getDayOfWeek();<br><span class="hljs-type">int</span> <span class="hljs-variable">value</span> <span class="hljs-operator">=</span> weekday.getValue(); <span class="hljs-comment">// 1 = Monday, . . . , 7 = Sunday</span><br></code></pre></td></tr></table></figure><p>变量 weekday 设置为 DayOfWeek 类型的对象。我们调用这个对象的 getValue 方法来得到对应星期几的一个数值。这会得到一个整数，这里遵循国际惯例，即周末是一周的结束，星期一就返回 1，星期二返回 2，依此类推。星期日则返回 7。</p><p>注意，日历的第一行是缩进的，使当月第一天对应正确的星期几。下面的代码会打印表头和第一行的缩进：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java">System.out.println(<span class="hljs-string">&quot;Mon Tue Wed Thu Fri Sat Sun&quot;</span>);<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">1</span>; i &lt; value; i++)<br>    System.out.print(<span class="hljs-string">&quot;    &quot;</span>);<br></code></pre></td></tr></table></figure><p>现在我们来打印日历的主体。进入一个循环，其中 date 遍历一个月中的每一天。<br>每次迭代中，我们要打印日期值。如果 date 是当前日期，这个日期则用一个 * 标记。接下来，把 date 推进到下一天。如果到达新的一周的第一天，则换行打印：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">while</span> (date.getMonthValue() == month)<br>&#123;<br>    System.out.printf(<span class="hljs-string">&quot;%3d&quot;</span>, date.getDayOfMonth());<br>    <span class="hljs-keyword">if</span> (date.getDayOfMonth() == today)<br>        System.out.print(<span class="hljs-string">&quot;*&quot;</span>);<br>    <span class="hljs-keyword">else</span><br>        System.out.print(<span class="hljs-string">&quot; &quot;</span>);<br>    date = date.plusDays(<span class="hljs-number">1</span>);<br>    <span class="hljs-keyword">if</span> (date.getDayOfWeek().getValue() == <span class="hljs-number">1</span>) System.out.println();<br>&#125;<br></code></pre></td></tr></table></figure><p>什么时候结束呢？我们不知道这个月有几天，是 31 天、30 天、29 天还是 28 天？实际上，只要 date 还在当月就要继续迭代。</p><p>可以看到，利用 LocalDate 类可以编写一个日历程序，它能处理星期几以及各月天数不同等复杂问题。你并不需要知道 LocalDate 类如何计算月和星期几，只需要使用这个类的接口，也就是诸如 plusDays 和 getDayOfWeek 等方法。</p><h3 id="4-3-自定义类"><a href="#4-3-自定义类" class="headerlink" title="4.3 自定义类"></a>4.3 自定义类</h3><p>在第 3 章中，我们已经开始编写一些简单的类。但是，那些类都只包含一个简单的 main 方法。现在来学习如何编写更复杂的应用所需要的那种<strong>主力类</strong>（workhorse class）。通常，这些类没有 main 方法，而有自己的实例字段和实例方法。要想构建一个完整的程序，会结合使用多个类，其中<strong>只有一个类有 main 方法。</strong></p><h4 id="4-3-1-Employee-类"><a href="#4-3-1-Employee-类" class="headerlink" title="4.3.1 Employee 类"></a>4.3.1 Employee 类</h4><p>在 Java 中，最简单的类定义形式为：</p><figure class="highlight perl"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><code class="hljs perl"><span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">ClassName</span></span><br><span class="hljs-class"></span>&#123;<br>    <span class="hljs-keyword">field</span>₁<br>    <span class="hljs-keyword">field</span>₂<br>    ...<br>    constructor₁<br>    constructor₂<br>    ...<br>    <span class="hljs-function"><span class="hljs-keyword">method</span>₁</span><br><span class="hljs-function">    <span class="hljs-title">method</span>₂</span><br><span class="hljs-function">    ...</span><br><span class="hljs-function">&#125;</span><br></code></pre></td></tr></table></figure><p>下面看一个非常简单的 Employee 类，编写工资管理系统时可能会用到：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Employee</span><br>&#123;<br>    <span class="hljs-comment">// instance fields</span><br>    <span class="hljs-keyword">private</span> String name;<br>    <span class="hljs-keyword">private</span> <span class="hljs-type">double</span> salary;<br>    <span class="hljs-keyword">private</span> LocalDate hireDay;<br><br>    <span class="hljs-comment">// constructor</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-title function_">Employee</span><span class="hljs-params">(String n, <span class="hljs-type">double</span> s, <span class="hljs-type">int</span> year, <span class="hljs-type">int</span> month, <span class="hljs-type">int</span> day)</span><br>    &#123;<br>        name = n;<br>        salary = s;<br>        hireDay = LocalDate.of(year, month, day);<br>    &#125;<br><br>    <span class="hljs-comment">// a method</span><br>    <span class="hljs-keyword">public</span> String <span class="hljs-title function_">getName</span><span class="hljs-params">()</span><br>    &#123;<br>        <span class="hljs-keyword">return</span> name;<br>    &#125;<br>    <br><span class="hljs-keyword">public</span> <span class="hljs-type">double</span> <span class="hljs-title function_">getSalary</span><span class="hljs-params">()</span><br>&#123;<br>    <span class="hljs-keyword">return</span> salary;<br>&#125;<br><br><span class="hljs-keyword">public</span> LocalDate <span class="hljs-title function_">getHireDay</span><span class="hljs-params">()</span><br>&#123;<br>    <span class="hljs-keyword">return</span> hireDay;<br>&#125;<br><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">raiseSalary</span><span class="hljs-params">(<span class="hljs-type">double</span> byPercent)</span><br>&#123;<br>    <span class="hljs-type">double</span> <span class="hljs-variable">raise</span> <span class="hljs-operator">=</span> salary * byPercent / <span class="hljs-number">100</span>;<br>    salary += raise;<br>&#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>在这个程序中，我们构造了一个 Employee 数组，并填入了 3 个 Employee 对象：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java">Employee[] staff = <span class="hljs-keyword">new</span> <span class="hljs-title class_">Employee</span>[<span class="hljs-number">3</span>];<br><br>staff[<span class="hljs-number">0</span>] = <span class="hljs-keyword">new</span> <span class="hljs-title class_">Employee</span>(<span class="hljs-string">&quot;Carl Cracker&quot;</span>, <span class="hljs-number">75000</span>, <span class="hljs-number">1987</span>, <span class="hljs-number">12</span>, <span class="hljs-number">15</span>);<br>staff[<span class="hljs-number">1</span>] = <span class="hljs-keyword">new</span> <span class="hljs-title class_">Employee</span>(<span class="hljs-string">&quot;Harry Hacker&quot;</span>, <span class="hljs-number">50000</span>, <span class="hljs-number">1989</span>, <span class="hljs-number">10</span>, <span class="hljs-number">1</span>);<br>staff[<span class="hljs-number">2</span>] = <span class="hljs-keyword">new</span> <span class="hljs-title class_">Employee</span>(<span class="hljs-string">&quot;Tony Tester&quot;</span>, <span class="hljs-number">40000</span>, <span class="hljs-number">1990</span>, <span class="hljs-number">3</span>, <span class="hljs-number">15</span>);<br></code></pre></td></tr></table></figure><p>接下来，使用 Employee 类的 raiseSalary 方法将每个员工的薪水提高 5%：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">for</span> (Employee e : staff)<br>    e.raiseSalary(<span class="hljs-number">5</span>);<br></code></pre></td></tr></table></figure><p>最后，调用 getName 方法、getSalary 方法和 getHireDay 方法打印各个员工的信息：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">for</span> (Employee e : staff)<br>    System.out.println(<span class="hljs-string">&quot;name=&quot;</span> + e.getName()<br>        + <span class="hljs-string">&quot;,salary=&quot;</span> + e.getSalary()<br>        + <span class="hljs-string">&quot;,hireDay=&quot;</span> + e.getHireDay());<br></code></pre></td></tr></table></figure><p>注意，在这个示例程序中包含两个类：Employee 类和带有 public 访问修饰符的 EmployeeTest 类。EmployeeTest 类包含 main 方法，其中使用了前面介绍的代码。</p><p>源文件名是 EmployeeTest.java，<strong>这是因为文件名必须与 public 类的名字匹配。一个源文件中只能有一个公共类，但可以有任意数目的非公共类。</strong></p><p>接下来，编译这段代码的时候，编译器将在目录中创建两个类文件：EmployeeTest.class 和 Employee.class。</p><p>然后启动这个程序，为字节码解释器提供程序中包含 main 方法的那个类的类名：</p><figure class="highlight ebnf"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs ebnf"><span class="hljs-attribute">java EmployeeTest</span><br></code></pre></td></tr></table></figure><p>字节码解释器开始运行 EmployeeTest 类的 main 方法中的代码。这个代码会先后构造 3 个 Employee 对象，并显示它们的状态。</p><h4 id="4-3-2-使用多个源文件"><a href="#4-3-2-使用多个源文件" class="headerlink" title="4.3.2 使用多个源文件"></a>4.3.2 使用多个源文件</h4><p>在程序清单 4-2 中，一个源文件包含了两个类。许多程序员习惯将各个类放在一个单独的源文件中。例如，将 Employee 类存放在文件 Employee.java 中，而将 EmployeeTest 类存放在文件 EmployeeTest.java 中。</p><p>如果喜欢这样组织文件，可以有两种编译程序的方法。一种是使用通配符调用 Java 编译器：</p><figure class="highlight nginx"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs nginx"><span class="hljs-attribute">javac</span> <span class="hljs-regexp">*.java</span><br></code></pre></td></tr></table></figure><p>这样一来，所有与通配符匹配的源文件都将被编译成类文件。或者可以简单地键入以下命令：</p><figure class="highlight mipsasm"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs mipsasm"><span class="hljs-keyword">javac </span>EmployeeTest.<span class="hljs-keyword">java</span><br></code></pre></td></tr></table></figure><p>你可能会感到惊讶，使用第二种方式时并没有显式地编译 Employee.java。不过，当 Java 编译器发现 EmployeeTest.java 中使用了 Employee 类时，它会查找名为 Employee.class 的文件。如果没有找到这个类文件，就会自动搜索 Employee.java 并编译这个文件。另外，如果 Employee.java 的版本较已有的 Employee.class 文件版本更新，Java 编译器就会自动重新编译这个文件。</p><h4 id="4-3-3-剖析-Employee-类"><a href="#4-3-3-剖析-Employee-类" class="headerlink" title="4.3.3 剖析 Employee 类"></a>4.3.3 剖析 Employee 类</h4><p>下面各小节将对 Employee 类进行剖析。首先从这个类的方法开始。通过查看源代码会发现，这个类包含一个构造器和 4 个方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-title function_">Employee</span><span class="hljs-params">(String n, <span class="hljs-type">double</span> s, <span class="hljs-type">int</span> year, <span class="hljs-type">int</span> month, <span class="hljs-type">int</span> day)</span><br><span class="hljs-keyword">public</span> String <span class="hljs-title function_">getName</span><span class="hljs-params">()</span><br><span class="hljs-keyword">public</span> <span class="hljs-type">double</span> <span class="hljs-title function_">getSalary</span><span class="hljs-params">()</span><br><span class="hljs-keyword">public</span> LocalDate <span class="hljs-title function_">getHireDay</span><span class="hljs-params">()</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">raiseSalary</span><span class="hljs-params">(<span class="hljs-type">double</span> byPercent)</span><br></code></pre></td></tr></table></figure><p>这个类的所有方法都被标记为 public。<strong>关键字 public 意味着任何类的任何方法都可以调用这些方法</strong>（共有 4 种访问级别）。</p><p>接下来，需要注意在 Employee 类的实例中有 3 个实例字段，用来存放将要操作的数据：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">private</span> String name;<br><span class="hljs-keyword">private</span> <span class="hljs-type">double</span> salary;<br><span class="hljs-keyword">private</span> LocalDate hireDay;<br></code></pre></td></tr></table></figure><p><strong>关键字 private 确保只有 Employee 类本身的方法能够访问这些实例字段，任何其他类的方法都不能读写这些字段。</strong></p><p><strong>注释：</strong> 可以用 public 标记实例字段，但这是一种很不好的做法。<strong>public 实例字段允许程序的任何部分都能对其进行读取和修改，这就完全破坏了<em>封装</em>。任何类的任何方法都可以修改 public 字段</strong>，从我们的经验来看，有些代码将利用这种做法获取权限，而这是我们最不希望看到的。因此，这里强烈建议将实例字段标记为 private。</p><p>最后，请注意，有两个实例字段本身就是对象：name 字段是 String 类对象的引用，hireDay 字段是 LocalDate 类对象的引用。类经常包含类类型的实例字段。</p><h4 id="4-3-4-从构造器开始"><a href="#4-3-4-从构造器开始" class="headerlink" title="4.3.4 从构造器开始"></a>4.3.4 从构造器开始</h4><p>下面先看看 Employee 类的构造器：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-title function_">Employee</span><span class="hljs-params">(String n, <span class="hljs-type">double</span> s, <span class="hljs-type">int</span> year, <span class="hljs-type">int</span> month, <span class="hljs-type">int</span> day)</span><br>&#123;<br>    name = n;<br>    salary = s;<br>    hireDay = LocalDate.of(year, month, day);<br>&#125;<br></code></pre></td></tr></table></figure><p>可以看到，构造器与类同名。构造 Employee 类的对象时，构造器会运行，这会将实例字段初始化为所希望的初始状态。</p><p>例如，使用下面这个代码创建 Employee 类的一个实例时：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">new</span> <span class="hljs-title class_">Employee</span>(<span class="hljs-string">&quot;James Bond&quot;</span>, <span class="hljs-number">100000</span>, <span class="hljs-number">1950</span>, <span class="hljs-number">1</span>, <span class="hljs-number">1</span>)<br></code></pre></td></tr></table></figure><p>将如下设置实例字段：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java">name = <span class="hljs-string">&quot;James Bond&quot;</span>;<br>salary = <span class="hljs-number">100000</span>;<br>hireDay = LocalDate.of(<span class="hljs-number">1950</span>, <span class="hljs-number">1</span>, <span class="hljs-number">1</span>); <span class="hljs-comment">// January 1, 1950</span><br></code></pre></td></tr></table></figure><p>构造器与其他方法有一个重要的不同。**构造器总是结合 new 操作符来调用。不能对一个已经存在的对象调用构造器来重新设置实例字段。**例如，</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">james.Employee(<span class="hljs-string">&quot;James Bond&quot;</span>, <span class="hljs-number">200000</span>, <span class="hljs-number">1950</span>, <span class="hljs-number">1</span>, <span class="hljs-number">1</span>) <span class="hljs-comment">// ERROR</span><br></code></pre></td></tr></table></figure><p>将产生编译错误。</p><p>本章后面还会更详细地介绍有关构造器的内容。现在只需要记住：</p><ul><li><strong>构造器与类同名。</strong></li><li><strong>每个类可以有一个以上的构造器。</strong></li><li><strong>构造器可以有 0 个、1 个或多个参数。</strong></li><li><strong>构造器没有返回值。</strong></li><li><strong>构造器总是结合 new 操作符一起调用。</strong></li></ul><p><strong>所有 Java 对象都是在堆中构造的，构造器总是结合 new 操作符一起使用。</strong></p><p><strong>警告：</strong> 请注意，<strong>不要引入与<em>实例字段</em>同名的局部变量</strong>。例如，下面的构造器将不会设置 name 或 salary 实例字段：</p><p>（实例字段声明在类内部但在任何方法之外）</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-title function_">Employee</span><span class="hljs-params">(String n, <span class="hljs-type">double</span> s, ...)</span><br>&#123;<br>    <span class="hljs-type">String</span> <span class="hljs-variable">name</span> <span class="hljs-operator">=</span> n; <span class="hljs-comment">// ERROR</span><br>    <span class="hljs-type">double</span> <span class="hljs-variable">salary</span> <span class="hljs-operator">=</span> s; <span class="hljs-comment">// ERROR</span><br>&#125;<br></code></pre></td></tr></table></figure><p>这个构造器声明了局部变量name和salary。这些变量只能在构造器内部访问，它们会<strong>遮蔽</strong>（shadow）同名的实例字段。因此，必须注意在所有的方法中都不要使用与实例字段同名的变量。</p><h4 id="4-3-5-用var声明局部变量"><a href="#4-3-5-用var声明局部变量" class="headerlink" title="4.3.5 用var声明局部变量"></a>4.3.5 用var声明局部变量</h4><p>**在 Java 10 中，如果可以从变量的初始值推导出它们的类型，那么可以用var关键字声明局部变量，而无须指定类型。**例如，可以不这样声明：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">Employee</span> <span class="hljs-variable">harry</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Employee</span>(<span class="hljs-string">&quot;Harry Hacker&quot;</span>, <span class="hljs-number">50000</span>, <span class="hljs-number">1989</span>, <span class="hljs-number">10</span>, <span class="hljs-number">1</span>);<br></code></pre></td></tr></table></figure><p>只需要写为：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">var</span> <span class="hljs-variable">harry</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Employee</span>(<span class="hljs-string">&quot;Harry Hacker&quot;</span>, <span class="hljs-number">50000</span>, <span class="hljs-number">1989</span>, <span class="hljs-number">10</span>, <span class="hljs-number">1</span>);<br></code></pre></td></tr></table></figure><p>这一点很好，因为这样可以避免重复写类型名 Employee。</p><p><strong>注意</strong>：<strong>var关键字只能用于方法中的局部变量。参数和字段的类型必须声明。</strong></p><h4 id="4-3-6-使用null引用"><a href="#4-3-6-使用null引用" class="headerlink" title="4.3.6 使用null引用"></a>4.3.6 使用null引用</h4><p>对象变量包含一个对象的引用，或者包含一个特殊值 null，后者表示没有引用任何对象。</p><p>如果对 null 值应用一个方法，会产生一个 NullPointerException 异常。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">LocalDate</span> <span class="hljs-variable">birthday</span> <span class="hljs-operator">=</span> <span class="hljs-literal">null</span>;<br><span class="hljs-type">String</span> <span class="hljs-variable">s</span> <span class="hljs-operator">=</span> birthday.toString(); <span class="hljs-comment">// NullPointerException</span><br></code></pre></td></tr></table></figure><p>这是一个很严重的错误，类似于 “索引越界” 异常。如果你的程序没有 “捕获” 异常，那么程序就会终止。正常情况下，程序并不捕获这些异常，而是依赖于程序员从一开始就不要带来异常。</p><p><strong>提示：</strong> 程序因 NullPointerException 异常终止时，栈轨迹会显示问题出现在哪一行代码中。从 Java 17 开始，错误消息会包含 null 值的变量或方法名。例如，在以下调用中：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">String</span> <span class="hljs-variable">s</span> <span class="hljs-operator">=</span> e.getHireDay().toString();<br></code></pre></td></tr></table></figure><p>错误消息会告诉你 e 是否为 null 或者 getHireDay 是否返回 null。</p><p>定义一个类时，最好清楚地知道哪些字段可能为 null。在我们的例子中，我们不希望 name 或 hireDay 字段为 null 。不用担心 salary 字段，这个字段是基本类型，所以不能使用 null 。</p><p>hireDay 字段肯定是非 null 的，因为它初始化为一个新的 LocalDate 对象。但是 name 可能为 null ，如果调用构造器时为 n 提供的变量是 null，name 就会是 null 。</p><p>对此有两种解决方法。”宽容” 方法是把 null 参数转换为一个适当的非 null 值：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">if</span> (n == <span class="hljs-literal">null</span>) name = <span class="hljs-string">&quot;unknown&quot;</span>; <span class="hljs-keyword">else</span> name = n;<br></code></pre></td></tr></table></figure><p>Objects 类对此提供了一个便利方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-title function_">Employee</span><span class="hljs-params">(String n, <span class="hljs-type">double</span> s, <span class="hljs-type">int</span> year, <span class="hljs-type">int</span> month, <span class="hljs-type">int</span> day)</span><br>&#123;<br>    name = Objects.requireNonNullElse(n, <span class="hljs-string">&quot;unknown&quot;</span>);<br>    ...<br>&#125;<br></code></pre></td></tr></table></figure><p>“严格” 方法则干脆拒绝 null 参数：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-title function_">Employee</span><span class="hljs-params">(String n, <span class="hljs-type">double</span> s, <span class="hljs-type">int</span> year, <span class="hljs-type">int</span> month, <span class="hljs-type">int</span> day)</span><br>&#123;<br>    name = Objects.requireNonNull(n, <span class="hljs-string">&quot;The name cannot be null&quot;</span>);<br>    ...<br>&#125;<br></code></pre></td></tr></table></figure><p>如果用 null 名字构造一个 Employee 对象，就会产生 NullPointerException 异常。乍看上去这种补救方法好像不太有用，不过这种方法有两个好处：</p><ol><li>异常报告会提供这个问题的描述。</li><li>异常报告会准确地指出问题所在的位置，否则 NullPointerException 异常会出现在其他地方，而很难追踪到正导致问题的构造器参数。</li></ol><p><strong>注释：</strong> 如果要接受一个对象引用作为构造参数，就要问问自己：是不是真的希望接受可有可无的值。如果不是，那么”严格”方法更合适。</p><h4 id="4-3-7-隐式参数与显式参数"><a href="#4-3-7-隐式参数与显式参数" class="headerlink" title="4.3.7 隐式参数与显式参数"></a>4.3.7 隐式参数与显式参数</h4><p>**方法会操作对象并访问它们的实例字段。**例如，以下方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">raiseSalary</span><span class="hljs-params">(<span class="hljs-type">double</span> byPercent)</span><br>&#123;<br>    <span class="hljs-type">double</span> <span class="hljs-variable">raise</span> <span class="hljs-operator">=</span> salary * byPercent / <span class="hljs-number">100</span>;<br>    salary += raise;<br>&#125;<br></code></pre></td></tr></table></figure><p>将调用这个方法的对象的 salary 实例字段设置为一个新值。考虑下面这个调用：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">number007.raiseSalary(<span class="hljs-number">5</span>);<br></code></pre></td></tr></table></figure><p>其作用是将 number007.salary 字段的值增加5%。具体地说，这个调用将执行以下指令：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">double</span> <span class="hljs-variable">raise</span> <span class="hljs-operator">=</span> number007.salary * <span class="hljs-number">5</span> / <span class="hljs-number">100</span>;<br>number007.salary += raise;<br></code></pre></td></tr></table></figure><p>raiseSalary 方法有两个参数。第一个参数称为<strong>隐式（implicit）参数</strong>，是出现在方法名前面的 Employee 类型的对象。第二个参数是位于方法名后面括号中的数值，这是一个<strong>显式（explicit）参数</strong>。（有人把隐式参数称为方法调用的目标或接收者。）</p><p>可以看到，显式参数显示在程序中，例如 double byPercent 。隐式参数则没有出现在方法声明中。</p><p>在每一个方法中，关键字 <strong>this</strong> 指示隐式参数。如果愿意，可以如下写 raiseSalary 方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">raiseSalary</span><span class="hljs-params">(<span class="hljs-type">double</span> byPercent)</span><br>&#123;<br>    <span class="hljs-type">double</span> <span class="hljs-variable">raise</span> <span class="hljs-operator">=</span> <span class="hljs-built_in">this</span>.salary * byPercent / <span class="hljs-number">100</span>;<br>    <span class="hljs-built_in">this</span>.salary += raise;<br>&#125;<br></code></pre></td></tr></table></figure><p>有些程序员更偏爱这样的风格，因为这样可以将实例字段与局部变量明显地区分开来。</p><p>在 Java 中，所有的方法都必须在类的内部定义，但这并不表示它们是内联方法。是否将某个方法设置为内联方法是 Java 虚拟机的任务。即时编译器会关注那些简短、经常调用而且没有被覆盖的方法调用，并进行优化。</p><h4 id="4-3-8-封装的优点"><a href="#4-3-8-封装的优点" class="headerlink" title="4.3.8 封装的优点"></a>4.3.8 封装的优点</h4><p>最后再仔细看一下非常简单的 getName 方法、getSalary 方法和 getHireDay 方法。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> String <span class="hljs-title function_">getName</span><span class="hljs-params">()</span><br>&#123;<br>    <span class="hljs-keyword">return</span> name;<br>&#125;<br><br><span class="hljs-keyword">public</span> <span class="hljs-type">double</span> <span class="hljs-title function_">getSalary</span><span class="hljs-params">()</span><br>&#123;<br>    <span class="hljs-keyword">return</span> salary;<br>&#125;<br><br><span class="hljs-keyword">public</span> LocalDate <span class="hljs-title function_">getHireDay</span><span class="hljs-params">()</span><br>&#123;<br>    <span class="hljs-keyword">return</span> hireDay;<br>&#125;<br></code></pre></td></tr></table></figure><p>这些都是典型的访问器方法。由于它们只返回实例字段的值，因此又称为<strong>字段访问器</strong>（field accessor）。</p><p>name 是一个只读字段，一旦在构造器中设置，就没有办法能够修改这个字段。这样我们可以确保 name 字段不会受到外界的破坏。</p><p>虽然 salary 不是只读字段，但是它只能用 raiseSalary 方法修改。具体地，如果这个值出现了错误，那么只需要调试这个 raiseSalary 方法就可以了。如果 salary 字段是公共的，破坏这个字段值的捣蛋程序有可能出现在任何地方（那就很难调试了）。</p><p>有些时候，可能想要获得或设置实例字段的值，那么你需要提供下面三项内容：</p><ul><li>一个私有的实例字段；</li><li>一个公共的字段访问器方法；</li><li>一个公共的字段更改器方法。</li></ul><p>首先，可以改变内部实现，而不影响该类方法之外的任何其他代码。例如，如果将存储姓名的字段改为：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java">String firstName;<br>String lastName;<br></code></pre></td></tr></table></figure><p>那么 getName 方法可以改为返回</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">firstName + <span class="hljs-string">&quot; &quot;</span> + lastName<br></code></pre></td></tr></table></figure><p>这个修改对于程序的其余部分是完全不可见的。</p><p>当然，为了进行新旧数据表示之间的转换，访问器方法和更改器方法可能需要做许多工作。这将为我们带来第二点好处：更改器方法可以完成错误检查，而只对字段赋值的代码不会费心这么做。例如，setSalary 方法可以检查工资是否小于 0。</p><p><strong>警告：</strong> 注意不要编写返回可变对象引用的访问器方法。在本节之前的一版中，我们的 Employee 类就违反了这个设计原则，其中的 getHireDay 方法返回了一个 Date 类对象：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Employee</span><br>&#123;<br>    <span class="hljs-keyword">private</span> Date hireDay;<br>    ...<br>    <span class="hljs-keyword">public</span> Date <span class="hljs-title function_">getHireDay</span><span class="hljs-params">()</span><br>    &#123;<br>        <span class="hljs-keyword">return</span> hireDay; <span class="hljs-comment">// BAD</span><br>    &#125;<br>    ...<br>&#125;<br></code></pre></td></tr></table></figure><p>LocalDate 类没有更改器方法，与之不同，Date 类有一个更改器方法 setTime，可以设置毫秒数。</p><p>Date 对象是可变的，这一点就破坏了封装性！考虑下面这段有问题的代码：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">Employee</span> <span class="hljs-variable">harry</span> <span class="hljs-operator">=</span> . . .;<br><span class="hljs-type">Date</span> <span class="hljs-variable">d</span> <span class="hljs-operator">=</span> harry.getHireDay();<br><span class="hljs-type">double</span> <span class="hljs-variable">tenYearsInMilliSeconds</span> <span class="hljs-operator">=</span> <span class="hljs-number">10</span> * <span class="hljs-number">365.25</span> * <span class="hljs-number">24</span> * <span class="hljs-number">60</span> * <span class="hljs-number">60</span> * <span class="hljs-number">1000</span>;<br>d.setTime(d.getTime() - (<span class="hljs-type">long</span>) tenYearsInMilliSeconds);<br><span class="hljs-comment">// let&#x27;s give harry ten years added seniority</span><br></code></pre></td></tr></table></figure><p>出错的原因很微妙。d 和 harry.hireDay 引用同一个对象（请参见图 4-5）。对 d 调用更改器方法就可以自动地改变这个成员对象的私有状态！</p><p><img src="/2025/10/30/%E3%80%8AJava%E6%A0%B8%E5%BF%83%E6%8A%80%E6%9C%AF%E5%8D%B7%E4%B8%80%E3%80%8B%E7%AC%AC%E5%9B%9B%E7%AB%A0/image-20251101163412352.png" alt="image-20251101163412352"></p><p>如果需要返回一个可变对象的引用，应该首先对它进行<strong>克隆</strong>（clone）。对象克隆是指存放在另一个位置上的对象副本。有关对象克隆的详细内容将在第 6 章中讨论。下面是修改后的代码：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Employee</span><br>&#123;<br>    <span class="hljs-keyword">public</span> Date <span class="hljs-title function_">getHireDay</span><span class="hljs-params">()</span><br>    &#123;<br>        <span class="hljs-keyword">return</span> (Date) hireDay.clone();<br>    &#125;<br>    . . .<br></code></pre></td></tr></table></figure><p>凭经验可知，如果需要返回一个可变数据域的拷贝，就应该使用克隆。</p><h4 id="4-3-9-基于类的访问权限"><a href="#4-3-9-基于类的访问权限" class="headerlink" title="4.3.9 基于类的访问权限"></a>4.3.9 基于类的访问权限</h4><p>从前面已经知道，方法可以访问所调用对象的私有数据。<strong>一个方法可以访问所属类的所有对象的私有数据</strong>。例如，下面看一下用来比较两个雇员的equals方法。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Employee</span><br>&#123;<br>    ...<br>    <span class="hljs-type">boolean</span> <span class="hljs-title function_">equals</span><span class="hljs-params">(Employee other)</span><br>    &#123;<br>        <span class="hljs-keyword">return</span> name.equals(other.name);<br>    &#125;<br>    ...<br>&#125;<br></code></pre></td></tr></table></figure><p>典型的调用方式是</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">if</span> (harry.equals(boss)) ...<br></code></pre></td></tr></table></figure><p>这个方法访问 harry 的私有域，这点并不会引发奇怪。然而，它还访问 boss 的私有域。这是合法的，其原因是 boss 是 Employee 类对象，而 Employee 类的方法可以访问 Employee 类的任何一个对象的私有域。</p><h4 id="4-3-10-私有方法"><a href="#4-3-10-私有方法" class="headerlink" title="4.3.10 私有方法"></a>4.3.10 私有方法</h4><p>在Java中，为了实现一个私有的方法，只需要将关键字public改为private即可。</p><p>如果将一个方法设置为私有，倘若你改变了方法的具体实现，并没有义务保证这个方法依然可用。如果数据的表示发生了变化，那么这个方法可能变得更复杂，或者不再需要；这并不重要。重点在于，只要方法是私有的，类的设计者就可以确信它不会在别处使用，所以可以将其删去。如果一个方法是公共的，就不能简单地将其删除，因为可能会有其他代码依赖这个方法。</p><h4 id="4-3-11-final-实例字段"><a href="#4-3-11-final-实例字段" class="headerlink" title="4.3.11 final 实例字段"></a>4.3.11 final 实例字段</h4><p>可以将实例字段定义为 final。这样的字段必须在构造对象时初始化。也就是说，必须确保每一个构造器执行之后，这个字段的值已经设置，并且以后不能再修改这个字段。例如，可以将 Employee 类中的 name 字段声明为 final，因为在对象构造之后，这个值不会再改变，即没有 setName 方法。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Employee</span><br>&#123;<br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> String name;<br>    ...<br>&#125;<br></code></pre></td></tr></table></figure><p><strong>final 修饰符对于类型为基本类型或者不可变类的字段尤其有用</strong>。（如果类中的所有方法都不会改变其对象，这样的类就是不可变类。例如，String 类就是不可变的。）</p><p>对于可变类，使用 final 修饰符可能会造成混乱。例如，考虑以下字段：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> StringBuilder evaluations;<br></code></pre></td></tr></table></figure><p>它在 Employee 构造器中初始化为</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">evaluations = <span class="hljs-keyword">new</span> <span class="hljs-title class_">StringBuilder</span>();<br></code></pre></td></tr></table></figure><p>final 关键字只是表示存储在 evaluations 变量中的对象引用不会再指示另一个不同的 StringBuilder 对象。不过这个对象可以更改：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">giveGoldStar</span><span class="hljs-params">()</span><br>&#123;<br>    evaluations.append(LocalDate.now() + <span class="hljs-string">&quot;: Gold star!\n&quot;</span>);<br>&#125;<br></code></pre></td></tr></table></figure><h2 id="4-4-静态字段与静态方法"><a href="#4-4-静态字段与静态方法" class="headerlink" title="4.4 静态字段与静态方法"></a>4.4 静态字段与静态方法</h2><p>在前面给出的示例程序中，main 方法都标记了 static 修饰符。下面来讨论这个修饰符的含义。</p><h4 id="4-4-1-静态字段"><a href="#4-4-1-静态字段" class="headerlink" title="4.4.1 静态字段"></a>4.4.1 静态字段</h4><p>如果将一个字段定义为 <strong>static</strong>，<strong>那么这个字段并不出现在每个类的对象中</strong>。每个静态字段只有一个副本。可以认为静态字段属于类，而不属于单个对象。例如，假设需要为每一个员工分配唯一的标识码，这里为 Employee 类添加一个实例字段 id 和一个静态字段 nextId：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Employee</span><br>&#123;<br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-type">int</span> <span class="hljs-variable">nextId</span> <span class="hljs-operator">=</span> <span class="hljs-number">1</span>;<br>    <span class="hljs-keyword">private</span> <span class="hljs-type">int</span> id;<br>    ...<br>&#125;<br></code></pre></td></tr></table></figure><p>现在，每一个 Employee 对象都有自己的 id 字段，但这个类的所有实例将共享一个 nextId 字段。换句话说，如果有 1000 个 Employee 类对象，则有 1000 个实例字段 id，每个对象有一个实例字段 id。但是，只有一个静态字段 nextId。即使没有 Employee 对象，静态字段 nextId 也存在。<strong>它属于类，而不属于任何单个对象。</strong></p><p><strong>注释：</strong> 在一些面向对象程序设计语言中，静态字段被称为类字段。术语”静态”只是沿用了 C++ 的叫法，并无实际意义。</p><p>在构造器中，我们为新 Employee 对象分配下一个可用的 ID，然后将其增 1：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java">id = nextId;<br>nextId++;<br></code></pre></td></tr></table></figure><p>假设我们构造了对象 harry。harry 的 id 字段设置为静态字段 nextId 的当前值，并将静态字段 nextId 的值加 1：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java">harry.id = Employee.nextId;<br>Employee.nextId++;<br></code></pre></td></tr></table></figure><h4 id="4-4-2-静态常量"><a href="#4-4-2-静态常量" class="headerlink" title="4.4.2 静态常量"></a>4.4.2 静态常量</h4><p>静态变量使用比较少，但静态常量却很常用。例如，Math类中定义了一个静态常量：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Math</span><br>&#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">double</span> <span class="hljs-variable">PI</span> <span class="hljs-operator">=</span> <span class="hljs-number">3.14159265358979323846</span>;<br>&#125;<br></code></pre></td></tr></table></figure><p>在你的程序中，可以用 Math.PI 来访问这个常量。</p><p>如果省略关键字 static，那么 PI 就变成了 Math 类的一个实例字段。也就是说，需要通过 Math 类的一个对象来访问PI，并且每一个 Math 对象都有它自己的一个PI副本。</p><p>另一个你已经多次使用的静态常量是 System.out 。它在 System 类中声明如下：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">System</span><br>&#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">PrintStream</span> <span class="hljs-variable">out</span> <span class="hljs-operator">=</span> . . .<br>&#125;<br></code></pre></td></tr></table></figure><p>前面曾经多次提到过，最好不要有公共字段，因为谁都可以修改公共字段。不过，公共常量（即 final 字段）却没问题。因为 out 被声明为 final ，所以，不允许再将它重新赋值为另一个打印流：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">System.out = <span class="hljs-keyword">new</span> <span class="hljs-title class_">PrintStream</span>(. . .); <span class="hljs-comment">// ERROR--out is final</span><br></code></pre></td></tr></table></figure><p><strong>注释：</strong> 如果查看 System 类，就会发现有一个 setOut 方法可以将 System.out 设置为不同的流。你可能会感到奇怪，为什么这个方法可以修改 final 变量的值。原因在于，setOut 方法是一个原生方法，而不是在 Java 语言中实现的。原生方法可以绕过 Java 语言的访问控制机制。这是一种特殊的解决方法，你自己编写程序时不要模仿这种做法。</p><h4 id="4-4-3-静态方法"><a href="#4-4-3-静态方法" class="headerlink" title="4.4.3 静态方法"></a>4.4.3 静态方法</h4><p>**静态方法是不操作对象的方法。**例如，Math 类的 pow 方法就是一个静态方法。以下表达式：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">Math.pow(x, a)<br></code></pre></td></tr></table></figure><p>会计算幂 x^a。它并不使用任何 Math 对象来完成这个任务。换句话说，它没有隐含参数。</p><p><strong>可以认为静态方法是没有 this 参数的方法</strong>（在一个非静态方法中，this 参数指示这个方法的隐含参数）。</p><p>Employee 类的静态方法不能访问 id 实例字段，因为它并不操作对象。但是，静态方法可以访问静态字段。下面是这样一个静态方法的示例：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-type">int</span> <span class="hljs-title function_">advanceId</span><span class="hljs-params">()</span><br>&#123;<br>    <span class="hljs-type">int</span> <span class="hljs-variable">r</span> <span class="hljs-operator">=</span> nextId; <span class="hljs-comment">// obtain next available id</span><br>    nextId++;<br>    <span class="hljs-keyword">return</span> r;<br>&#125;<br></code></pre></td></tr></table></figure><p>要调用这个方法，需要提供类名：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">int</span> <span class="hljs-variable">n</span> <span class="hljs-operator">=</span> Employee.advanceId();<br></code></pre></td></tr></table></figure><p>这个方法可以省略关键字 static 。但是，这样一来，你就需要通过 Employee 类型的对象引用来调用这个方法。</p><p><strong>注释：</strong> 可以使用对象调用静态方法，这是合法的。例如，如果 harry 是一个 Employee 对象，那么可以调用 harry.advanceId( ) 而不是 Employee.advanceId( )。不过，我发现这种写法很容易造成混淆，其原因是 advanceId 方法计算的结果与 harry 毫无关系。我们建议使用类名而不是对象来调用静态方法。</p><p>下面两种情况可以使用静态方法：</p><ul><li>方法不需要访问对象状态，因为它需要的所有参数都通过显式参数提供（例如 Math.pow）。</li><li>方法只需要访问类的静态字段（例如 Employee.advanceId）。</li></ul><h4 id="4-4-4-工厂方法"><a href="#4-4-4-工厂方法" class="headerlink" title="4.4.4 工厂方法"></a>4.4.4 工厂方法</h4><p>静态方法还有另外一种常见的用途。类似 LocalDate 和 NumberFormat 的类使用<strong>静态工厂方法</strong>（factory method）来构造对象。你已经见过工厂方法 LocalDate.now 和 LocalDate.of。可以如下得到不同样式的格式化对象：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">NumberFormat</span> <span class="hljs-variable">currencyFormatter</span> <span class="hljs-operator">=</span> NumberFormat.getCurrencyInstance();<br><span class="hljs-type">NumberFormat</span> <span class="hljs-variable">percentFormatter</span> <span class="hljs-operator">=</span> NumberFormat.getPercentInstance();<br><span class="hljs-type">double</span> <span class="hljs-variable">x</span> <span class="hljs-operator">=</span> <span class="hljs-number">0.1</span>;<br>System.out.println(currencyFormatter.format(x)); <span class="hljs-comment">// prints $0.10</span><br>System.out.println(percentFormatter.format(x)); <span class="hljs-comment">// prints 10%</span><br></code></pre></td></tr></table></figure><p>为什么 NumberFormat 类不使用构造器来创建对象呢？这有两个原因：</p><ul><li><p>无法为构造器命名。构造器的名字总是要与类名相同。但是，这里希望有两个不同的名字，分别得到货币实例和百分比实例。</p></li><li><p>使用构造器时，无法改变所构造对象的类型。而工厂方法实际上将返回 DecimalFormat 类的对象，这是继承 NumberFormat 的一个子类。</p></li></ul><h4 id="4-4-5-main-方法"><a href="#4-4-5-main-方法" class="headerlink" title="4.4.5 main 方法"></a>4.4.5 main 方法</h4><p>需要指出，可以调用静态方法不需要任何对象。例如，不需要构造 Math 类的任何对象就可以调用 Math.pow。</p><p>同理，main 方法也是一个静态方法。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Application</span><br>&#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span><br>    &#123;<br>        <span class="hljs-comment">// construct objects here</span><br>        ...<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>main 方法不对任何对象进行操作。事实上，启动程序时还没有任何对象。将执行静态 main 方法，并构造程序所需要的对象。</p><p><strong>提示：</strong> 每一个类都可以有一个 main 方法。这是为类增加演示代码的一个技巧。例如，可以在 Employee 类中添加一个 main 方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Employee</span><br>&#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-title function_">Employee</span><span class="hljs-params">(String n, <span class="hljs-type">double</span> s, <span class="hljs-type">int</span> year, <span class="hljs-type">int</span> month, <span class="hljs-type">int</span> day)</span><br>    &#123;<br>        name = n;<br>        salary = s;<br>        hireDay = LocalDate.of(year, month, day);<br>    &#125;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> <span class="hljs-comment">// unit test</span><br>    &#123;<br>        <span class="hljs-type">var</span> <span class="hljs-variable">e</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Employee</span>(<span class="hljs-string">&quot;Romeo&quot;</span>, <span class="hljs-number">50000</span>, <span class="hljs-number">2003</span>, <span class="hljs-number">3</span>, <span class="hljs-number">31</span>);<br>        e.raiseSalary(<span class="hljs-number">10</span>);<br>        System.out.println(e.getName() + <span class="hljs-string">&quot; &quot;</span> + e.getSalary());<br>    &#125;<br>    ...<br></code></pre></td></tr></table></figure><p>要运行 Employee 类的演示，只需要执行：</p><figure class="highlight ebnf"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs ebnf"><span class="hljs-attribute">java Employee</span><br></code></pre></td></tr></table></figure><p>如果 Employee 类是一个更大的应用的一部分，那么可以使用以下命令运行这个应用：</p><figure class="highlight ebnf"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs ebnf"><span class="hljs-attribute">java Application</span><br></code></pre></td></tr></table></figure><p>Employee 类的 main 方法永远不会执行。</p><h3 id="4-5-方法参数"><a href="#4-5-方法参数" class="headerlink" title="4.5 方法参数"></a>4.5 方法参数</h3><p><strong>按值调用</strong>（call by value）表示方法接收的是调用者提供的值。而<strong>按引用调用</strong>（call by reference）表示方法接收的是调用者提供的变量位置（location）。所以，方法可以修改按引用传递的变量的值，而不能修改按值传递的变量的值。”按……调用”（call by）是一个标准的计算机科学术语，用来描述各种程序设计语言（不只是 Java）中方法参数的行为（事实上，以前还有一种<strong>按名调用</strong>（call by name），Algol 程序设计语言是最古老的高级语言之一，它就采用了按名调用方式。不过，这种传递方式已经成为历史）。</p><p>Java 程序设计语言总是采用<strong>按值调用</strong>。也就是说，方法会得到所有参数值的一个副本。具体来说，方法不能修改传递给它的任何参数变量的内容。</p><p>例如，考虑下面的调用：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">double</span> <span class="hljs-variable">percent</span> <span class="hljs-operator">=</span> <span class="hljs-number">10</span>;<br>harry.raiseSalary(percent);<br></code></pre></td></tr></table></figure><p>不论这个方法具体如何实现，我们知道，在这个方法调用之后，percent 的值还是 10。下面再仔细研究一下这种情况。假定一个方法试图将一个参数值增加至 3 倍：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">tripleValue</span><span class="hljs-params">(<span class="hljs-type">double</span> x)</span> <span class="hljs-comment">// doesn&#x27;t work</span><br>&#123;<br>    x = <span class="hljs-number">3</span> * x;<br>&#125;<br></code></pre></td></tr></table></figure><p>然后调用这个方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">double</span> <span class="hljs-variable">percent</span> <span class="hljs-operator">=</span> <span class="hljs-number">10</span>;<br>tripleValue(percent);<br></code></pre></td></tr></table></figure><p>不过，这并不起作用。调用这个方法之后，percent 的值还是 10。下面来看发生了什么：</p><ol><li>x 初始化为 percent 值的一个副本（也就是 10）。</li><li>x 乘以 3 后等于 30，但是 percent 仍然是 10（如图 4-6 所示）。</li><li>这个方法结束之后，参数变量 x 不再使用。</li></ol><p><img src="/2025/10/30/%E3%80%8AJava%E6%A0%B8%E5%BF%83%E6%8A%80%E6%9C%AF%E5%8D%B7%E4%B8%80%E3%80%8B%E7%AC%AC%E5%9B%9B%E7%AB%A0/image-20251102103557869.png" alt="image-20251102103557869"></p><p>不过，有两种不同类型的方法参数：</p><ul><li>基本数据类型（数字、布尔值）。</li><li>对象引用。</li></ul><p>你已经看到，一个方法不可能修改基本数据类型的参数，而对象参数就不同了，可以很容易地实现一个方法将一个员工的工资增至 3 倍：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">tripleSalary</span><span class="hljs-params">(Employee x)</span> <span class="hljs-comment">// works</span><br>&#123;<br>    x.raiseSalary(<span class="hljs-number">200</span>);<br>&#125;<br></code></pre></td></tr></table></figure><p>如下调用这个方法时，</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java">harry = <span class="hljs-keyword">new</span> <span class="hljs-title class_">Employee</span>(...);<br>tripleSalary(harry);<br></code></pre></td></tr></table></figure><p>具体的执行过程为：</p><ol><li><p>x 初始化为 harry 值的一个副本，这里就是一个对象引用。</p></li><li><p>raiseSalary 方法应用于这个对象引用。x 和 harry 同时引用的那个 Employee 对象的工资提高了 200%。</p></li><li><p>方法结束后，参数变量 x 不再使用。当然，对象变量 harry 继续引用那个工资增至 3 倍的员工对象（如图 4-7 所示）。</p><p> <img src="/2025/10/30/%E3%80%8AJava%E6%A0%B8%E5%BF%83%E6%8A%80%E6%9C%AF%E5%8D%B7%E4%B8%80%E3%80%8B%E7%AC%AC%E5%9B%9B%E7%AB%A0/image-20251102225551589.png" alt="image-20251102225551589"></p><p> ​                                <strong>图 4-7 修改参数引用的对象有持续效果</strong></p></li></ol><p>可以看到，实现方法改变对象参数的状态是完全可以的，实际上也相当常见。理由很简单，方法得到的是对象引用的副本，原来的对象引用和这个副本都引用同一个对象。</p><p>很多程序设计语言（特别是 C++ 和 Pascal）提供了两种参数传递方式：按值调用和按引用调用。有些程序员（甚至有些书的作者）声称 Java 对对象采用的是按引用调用，实际上，这是不对的。由于这种误解很常见，所以很有必要给出一个反例来详细地说明这个问题。</p><p>下面来编写一个交换两个 Employee 对象的方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">swap</span><span class="hljs-params">(Employee x, Employee y)</span> <span class="hljs-comment">// doesn&#x27;t work</span><br>&#123;<br>    <span class="hljs-type">Employee</span> <span class="hljs-variable">temp</span> <span class="hljs-operator">=</span> x;<br>    x = y;<br>    y = temp;<br>&#125;<br></code></pre></td></tr></table></figure><p>如果 Java 对对象采用的是按引用调用，那么这个方法就应该能够实现交换：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">var</span> <span class="hljs-variable">a</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Employee</span>(<span class="hljs-string">&quot;Alice&quot;</span>, . . .);<br><span class="hljs-type">var</span> <span class="hljs-variable">b</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Employee</span>(<span class="hljs-string">&quot;Bob&quot;</span>, . . .);<br>swap(a, b);<br><span class="hljs-comment">// does a now refer to Bob, b to Alice?</span><br></code></pre></td></tr></table></figure><p>但是，这个方法并没有改变存储在变量 a 和 b 中的对象引用。swap 方法的参数 x 和 y 初始化为两个对象引用的副本，这个方法交换的是这两个副本。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-comment">// x refers to Alice, y to Bob</span><br><span class="hljs-type">Employee</span> <span class="hljs-variable">temp</span> <span class="hljs-operator">=</span> x;<br>x = y;<br>y = temp;<br><span class="hljs-comment">// now x refers to Bob, y to Alice</span><br></code></pre></td></tr></table></figure><p>方法结束时，参数变量 x 和 y 被丢弃了。原来的变量 a 和 b 仍然引用这个方法调用之前所引用的对象（如图4-8所示）。</p><p><img src="/2025/10/30/%E3%80%8AJava%E6%A0%B8%E5%BF%83%E6%8A%80%E6%9C%AF%E5%8D%B7%E4%B8%80%E3%80%8B%E7%AC%AC%E5%9B%9B%E7%AB%A0/image-20251102231655672.png" alt="image-20251102231655672"></p><p>​<strong>图 4-8 交换参数变量没有持续效果</strong></p><p>这说明：Java程序设计语言对对象采用的不是按引用调用。实际上，<strong>对象引用（object reference）是按值传递的</strong>。</p><p>下面来总结在Java中对方法参数能做什么和不能做什么：</p><ul><li>方法不能修改基本数据类型的参数（即数值型或布尔型）。</li><li>方法可以改变对象参数的状态。</li><li>方法不能让一个对象参数引用一个新对象。</li></ul><p>程序清单4-4 中的程序展示了这几点。在这个程序中，首先试图将一个数值参数的值增至3倍，但没有成功：</p><figure class="highlight oxygene"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs oxygene">Testing tripleValue:<br>Before: percent=<span class="hljs-number">10.0</span><br><span class="hljs-keyword">End</span> <span class="hljs-keyword">of</span> <span class="hljs-keyword">method</span>: x=<span class="hljs-number">30.0</span><br>After: percent=<span class="hljs-number">10.0</span><br></code></pre></td></tr></table></figure><p>随后，成功地将一个员工的工资增至3倍：</p><figure class="highlight oxygene"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs oxygene">Testing tripleSalary:<br>Before: salary=<span class="hljs-number">50000.0</span><br><span class="hljs-keyword">End</span> <span class="hljs-keyword">of</span> <span class="hljs-keyword">method</span>: salary=<span class="hljs-number">150000.0</span><br>After: salary=<span class="hljs-number">150000.0</span><br></code></pre></td></tr></table></figure><p>方法结束之后，harry 引用的对象的状态发生了改变。这是因为这个方法可以通过对象引用的副本修改所引用对象的状态。</p><p>最后，程序演示了 swap 方法的失败结果：</p><figure class="highlight oxygene"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs oxygene">Testing swap:<br>Before: a=Alice<br>Before: b=Bob<br><span class="hljs-keyword">End</span> <span class="hljs-keyword">of</span> <span class="hljs-keyword">method</span>: x=Bob<br><span class="hljs-keyword">End</span> <span class="hljs-keyword">of</span> <span class="hljs-keyword">method</span>: y=Alice<br>After: a=Alice<br>After: b=Bob<br></code></pre></td></tr></table></figure><p>可以看出，参数变量 x 和 y 交换了，但是变量 a 和 b 没有受到影响。</p><h3 id="4-6-对象构造"><a href="#4-6-对象构造" class="headerlink" title="4.6 对象构造"></a>4.6 对象构造</h3><h4 id="4-6-1-重载"><a href="#4-6-1-重载" class="headerlink" title="4.6.1 重载"></a>4.6.1 重载</h4><p>有些类有多个构造器。例如，可以如下构造一个空的StringBuilder对象：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">var</span> <span class="hljs-variable">messages</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">StringBuilder</span>();<br></code></pre></td></tr></table></figure><p>或者，可以指定一个初始字符串：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">var</span> <span class="hljs-variable">todoList</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">StringBuilder</span>(<span class="hljs-string">&quot;To do:\n&quot;</span>);<br></code></pre></td></tr></table></figure><p>这种功能叫作<strong>重载</strong>（overloading）。如果多个方法（比如，StringBuilder构造器方法）有相同的方法名但有不同的参数，便出现了重载。编译器必须挑选出具体调用哪个方法。它用各个方法首部中的参数类型与特定方法调用中所使用的值类型进行匹配，来选出正确的方法。如果编译器无法匹配参数，就会产生编译时错误，这可能因为根本不存在匹配，或者所有重载方法中没有一个相对更好的方法（这个查找匹配的过程称为<strong>重载解析</strong>（overloading resolution））。</p><p><strong>注释：</strong> Java允许重载任何方法，而不只是构造器方法。因此，要完整地描述一个方法，需要指定方法名以及参数类型，这叫作方法的<strong>签名</strong>（signature）。例如，String类有4个名为indexOf的公共方法。它们的签名是</p><figure class="highlight stylus"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs stylus"><span class="hljs-function"><span class="hljs-title">indexOf</span><span class="hljs-params">(int)</span></span><br><span class="hljs-function"><span class="hljs-title">indexOf</span><span class="hljs-params">(int, int)</span></span><br><span class="hljs-function"><span class="hljs-title">indexOf</span><span class="hljs-params">(String)</span></span><br><span class="hljs-function"><span class="hljs-title">indexOf</span><span class="hljs-params">(String, int)</span></span><br></code></pre></td></tr></table></figure><p>返回类型不是方法签名的一部分。也就是说，不能有两个名字相同、参数类型也相同却有不同返回类型的方法。</p><h4 id="4-6-2-默认字段初始化"><a href="#4-6-2-默认字段初始化" class="headerlink" title="4.6.2 默认字段初始化"></a>4.6.2 默认字段初始化</h4><p>如果在构造器中没有显式地为一个字段设置初始值，就会将它自动设置为默认值：数值将设置为0，布尔值为false，对象引用为null。有些人认为依赖默认值的做法是一种不好的编程实践。确实，如果不明确地对字段进行初始化，就会影响程序代码的可读性。</p><p><strong>注释：</strong> 这是字段与局部变量的一个重要区别。方法中的局部变量必须明确地初始化。但是在类中，如果没有初始化类中的字段，将会自动初始化为默认值（0、false 或 null）。</p><p>例如，考虑 Employee 类。假定没有在构造器中指定如何初始化某些字段，默认情况下，就会将 salary 字段初始化为0，将 name 和 hireDay 字段初始化为 null 。</p><p>但是，这并不是一个好主意。如果有人调用 getName 方法或 getHireDay 方法，就会得到一个 null 引用，这可能不是他们想要的结果：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">LocalDate</span> <span class="hljs-variable">h</span> <span class="hljs-operator">=</span> harry.getHireDay();<br><span class="hljs-type">int</span> <span class="hljs-variable">year</span> <span class="hljs-operator">=</span> h.getYear(); <span class="hljs-comment">// throws exception if h is null</span><br></code></pre></td></tr></table></figure><h4 id="4-6-3-无参数的构造器"><a href="#4-6-3-无参数的构造器" class="headerlink" title="4.6.3 无参数的构造器"></a>4.6.3 无参数的构造器</h4><p>很多类都包含无参数的构造器，由无参数构造器创建对象时，对象的状态会设置为适当的默认值。例如，以下是 Employee 类的一个无参数构造器：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-title function_">Employee</span><span class="hljs-params">()</span><br>&#123;<br>    name = <span class="hljs-string">&quot;&quot;</span>;<br>    salary = <span class="hljs-number">0</span>;<br>    hireDay = LocalDate.now();<br>&#125;<br></code></pre></td></tr></table></figure><p>如果你写的类没有构造器，就会为你提供一个无参数构造器。这个构造器将所有的实例字段设置为相应的默认值。所以，实例字段中的所有数值型数据会设置为 0，所有布尔值设置为 false，所有对象变量将设置为 null。</p><p>如果类中提供了至少一个构造器，但是没有提供无参数构造器，那么构造对象时就必须提供参数，否则就是不合格的。例如，程序清单 4-2 中的 Employee 类提供了一个构造器：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-title function_">Employee</span><span class="hljs-params">(String n, <span class="hljs-type">double</span> s, <span class="hljs-type">int</span> year, <span class="hljs-type">int</span> month, <span class="hljs-type">int</span> day)</span><br></code></pre></td></tr></table></figure><p>对于这个类，构造默认的员工就是不合格的。也就是说，以下调用</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">e = <span class="hljs-keyword">new</span> <span class="hljs-title class_">Employee</span>();<br></code></pre></td></tr></table></figure><p>将产生错误。</p><p><strong>警告：</strong> 请记住，仅当类没有任何其他构造器的时候，你才会得到一个默认的无参数构造器。编写类的时候，如果写了一个你自己的构造器，要想让这个类的使用者能够通过以下调用创建一个实例：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">new</span> <span class="hljs-title class_">ClassName</span>()<br></code></pre></td></tr></table></figure><p>你就必须提供一个无参数的构造器。当然，如果接受所有字段设置为默认值，则只需要提供以下代码：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-title function_">ClassName</span><span class="hljs-params">()</span><br>&#123;<br>&#125;<br></code></pre></td></tr></table></figure><h4 id="4-6-4-显式字段初始化"><a href="#4-6-4-显式字段初始化" class="headerlink" title="4.6.4 显式字段初始化"></a>4.6.4 显式字段初始化</h4><p>通过重载类的构造器方法，可以采用多种形式设置类实例字段的初始状态。不论调用哪个构造器，每个实例字段都要设置为一个有意义的初始值，确保这一点总是一个好主意。</p><p>可以在类定义中直接为任何字段赋值。例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Employee</span><br>&#123;<br>    <span class="hljs-keyword">private</span> <span class="hljs-type">String</span> <span class="hljs-variable">name</span> <span class="hljs-operator">=</span> <span class="hljs-string">&quot;&quot;</span>;<br>    ...<br>&#125;<br></code></pre></td></tr></table></figure><p>在执行构造器之前完成这个赋值。如果一个类的所有构造器都需要把某个特定的实例字段设置为同一个值，那么这个语法尤其有用。</p><p>初始值不一定是常量值。在下面的例子中，就是利用方法调用初始化一个字段。考虑以下Employee类，其中每个员工有一个id字段。可以使用以下方式进行初始化：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Employee</span><br>&#123;<br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-type">int</span> nextId;<br>    <span class="hljs-keyword">private</span> <span class="hljs-type">int</span> <span class="hljs-variable">id</span> <span class="hljs-operator">=</span> assignId();<br>    ...<br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-type">int</span> <span class="hljs-title function_">assignId</span><span class="hljs-params">()</span><br>    &#123;<br>        <span class="hljs-type">int</span> <span class="hljs-variable">r</span> <span class="hljs-operator">=</span> nextId;<br>        nextId++;<br>        <span class="hljs-keyword">return</span> r;<br>    &#125;<br>    ...<br>&#125;<br></code></pre></td></tr></table></figure><h4 id="4-6-5-参数名"><a href="#4-6-5-参数名" class="headerlink" title="4.6.5 参数名"></a>4.6.5 参数名</h4><p>在编写很小的构造器时（这十分常见），在为参数命名时可能有些困惑。<br>我们通常喜欢用单个字母作为参数名：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-title function_">Employee</span><span class="hljs-params">(String n, <span class="hljs-type">double</span> s)</span><br>&#123;<br>    name = n;<br>    salary = s;<br>&#125;<br></code></pre></td></tr></table></figure><p>但这样做有一个缺点：只有阅读代码才能够了解参数n和参数s的含义。</p><p>有些程序员在每个参数前面加上一个前缀”a”：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-title function_">Employee</span><span class="hljs-params">(String aName, <span class="hljs-type">double</span> aSalary)</span><br>&#123;<br>    name = aName;<br>    salary = aSalary;<br>&#125;<br></code></pre></td></tr></table></figure><p>这样更好一些。读者一眼就能够看懂参数的含义。</p><p>还有一种常用的技巧，它基于这样的事实：参数变量会遮蔽同名的实例字段。例如，如果将参数命名为salary，那么salary将指示这个参数，而不是实例字段。但是，还是可以用this.salary访问实例字段。回想一下，this指示隐式参数，也就是所构造的对象。下面来看一个示例：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-title function_">Employee</span><span class="hljs-params">(String name, <span class="hljs-type">double</span> salary)</span><br>&#123;<br>    <span class="hljs-built_in">this</span>.name = name;<br>    <span class="hljs-built_in">this</span>.salary = salary;<br>&#125;<br></code></pre></td></tr></table></figure><h4 id="4-6-6-调用另一个构造器"><a href="#4-6-6-调用另一个构造器" class="headerlink" title="4.6.6 调用另一个构造器"></a>4.6.6 调用另一个构造器</h4><p>关键字 this 指示一个方法的隐式参数。不过，这个关键字还有另外一个含义。<br>如果构造器的第一个语句形如 this( … )，这个构造器将调用同一个类的另一个构造器。<br>下面是一个典型的例子：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-title function_">Employee</span><span class="hljs-params">(<span class="hljs-type">double</span> s)</span><br>&#123;<br>    <span class="hljs-comment">// calls Employee(String, double)</span><br>    <span class="hljs-built_in">this</span>(<span class="hljs-string">&quot;Employee #&quot;</span> + nextId, s);<br>    nextId++;<br>&#125;<br></code></pre></td></tr></table></figure><p>当调用 new Employee(60000) 时，Employee(double) 构造器将调用 Employee(String, double) 构造器。<br>采用这种方式使用 this 关键字非常有用，这样只需要写一次公共构造代码。</p><h4 id="4-6-7-初始化块"><a href="#4-6-7-初始化块" class="headerlink" title="4.6.7 初始化块"></a>4.6.7 初始化块</h4><p>前面已经介绍过两种初始化实例字段的方法：</p><ul><li>在构造器中设置值；</li><li>在声明中赋值。</li></ul><p>实际上，Java 还有第三种机制，称为<strong>初始化块</strong>（initialization block）。在一个类的声明中，可以包含任意的代码块。构造这个类的对象时，这些块就会执行。例如，</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Employee</span><br>&#123;<br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-type">int</span> nextId;<br>    <span class="hljs-keyword">private</span> <span class="hljs-type">int</span> id;<br>    <span class="hljs-keyword">private</span> String name;<br>    <span class="hljs-keyword">private</span> <span class="hljs-type">double</span> salary;<br>    <br>    <span class="hljs-comment">// object initialization block</span><br>    &#123;<br>    id = nextId;<br>    nextId++;<br>&#125;<br><br><span class="hljs-keyword">public</span> <span class="hljs-title function_">Employee</span><span class="hljs-params">(String n, <span class="hljs-type">double</span> s)</span><br>&#123;<br>    name = n;<br>    salary = s;<br>&#125;<br><br><span class="hljs-keyword">public</span> <span class="hljs-title function_">Employee</span><span class="hljs-params">()</span><br>&#123;<br>    name = <span class="hljs-string">&quot;&quot;</span>;<br>    salary = <span class="hljs-number">0</span>;<br>&#125;<br>    . . .<br>&#125;<br></code></pre></td></tr></table></figure><p>在这个示例中，无论使用哪个构造器构造对象，id 字段都会在对象初始化块中初始化。首先运行初始化块，然后才运行构造器的主体部分。<br>这种机制不是必需的，也不常见。通常会直接将初始化代码放在构造器中。</p><p><strong>注释：</strong> 可以在初始化块中设置字段，即使这些字段在类后面才定义，这是合法的。但是，为了避免循环定义，不允许读取在后面初始化的字段。具体规则请参见 Java 语言规范的 8.3.3 节（<a href="http://docs.oracle.com/javase/specs%EF%BC%89%E3%80%82%E8%BF%99%E4%BA%9B%E8%A7%84%E5%88%99%E5%A4%AA%E8%BF%87%E5%A4%8D%E6%9D%82%EF%BC%8C%E8%AE%A9%E7%BC%96%E8%AF%91%E5%99%A8%E7%9A%84%E5%AE%9E%E7%8E%B0%E8%80%85%E9%83%BD%E5%BE%88%E5%A4%B4%E7%96%BC%EF%BC%8C%E6%89%80%E4%BB%A5%E8%BE%83%E6%97%A9%E7%9A%84">http://docs.oracle.com/javase/specs）。这些规则太过复杂，让编译器的实现者都很头疼，所以较早的</a> Java 版本中这些规则的实现存在一些小错误。因此，建议总是将初始化块放在字段定义之后。</p><p>由于初始化实例字段有多种途径，所以列出构造过程的所有路径可能让人很费解。下面是调用构造器时的具体处理步骤：</p><ol><li>如果构造器的第一行调用了另一个构造器，则基于所提供的参数执行第二个构造器。</li><li>否则，<br>a) 所有实例字段初始化为其默认值（0、false 或 null）。<br>b) 按照在类声明中出现的顺序，执行所有字段初始化方法和初始化块。</li><li>执行构造器主体代码。</li></ol><p>可以通过提供一个初始值，或者使用一个静态的初始化块来初始化静态字段。前面已经介绍过第一种机制：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-type">int</span> <span class="hljs-variable">nextId</span> <span class="hljs-operator">=</span> <span class="hljs-number">1</span>;<br></code></pre></td></tr></table></figure><p>如果类的静态字段需要复杂的初始化代码，那么可以使用静态的初始化块。<br>将代码放在一个块中，并标记关键字 static。下面是一个示例。我们希望将员工 ID 的起始值赋为一个小于 10000 的随机整数。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-type">Random</span> <span class="hljs-variable">generator</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Random</span>();<br><span class="hljs-comment">// static initialization block</span><br><span class="hljs-keyword">static</span><br>&#123;<br>    nextId = generator.nextInt(<span class="hljs-number">10000</span>);<br>&#125;<br></code></pre></td></tr></table></figure><p>在类第一次加载的时候，会完成静态字段的初始化。与实例字段一样，除非将静态字段显式地设置成其他值，否则默认的初始值为0、false或null。所有的静态字段初始化方法以及静态初始化块都将依照类声明中出现的顺序执行。</p><p><strong>注释：</strong> 让人惊讶的是，在JDK 6之前，完全可以用Java编写一个没有main方法的”Hello, World”程序。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Hello</span><br>&#123;<br>    <span class="hljs-keyword">static</span><br>    &#123;<br>        System.out.println(<span class="hljs-string">&quot;Hello, World&quot;</span>);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>当用 java Hello 调用这个类时，就会加载这个类，静态初始化块将会打印 “Hello, World”。在此之后才会显示一个消息指出 main 未定义。从 Java 7 以后，java 程序会首先检查是否有一个 main 方法。</p><p>这个例子使用了 Random 类来生成随机数。从 JDK 17 开始，java.util.random 包提供了考虑多种因素的强算法的实现。阅读 java.util.random 包的API文档，其中对如何选择算法给出了建议。然后通过提供算法名来得到一个实例，如下所示：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">RandomGenerator</span> <span class="hljs-variable">generator</span> <span class="hljs-operator">=</span> RandomGenerator.of(<span class="hljs-string">&quot;L64X128MixRandom&quot;</span>);<br></code></pre></td></tr></table></figure><p>调用 generator.nextInt(n) 或其他 RandomGenerator 方法来生成随机数。（RandomGenerator 是一个接口，第6章将介绍接口概念。Random 类的对象可以使用所有 RandomGenerator 方法。）</p><h4 id="4-6-8-对象析构与-finalize-方法"><a href="#4-6-8-对象析构与-finalize-方法" class="headerlink" title="4.6.8 对象析构与 finalize 方法"></a>4.6.8 对象析构与 finalize 方法</h4><p>有些面向对象的程序设计语言（特别是 C++）有显式的析构器方法，其中放置一些清理代码，当对象不再使用可能需要执行这些清理代码。在析构器中，最常见的操作是回收分配给对象的存储空间。由于 Java 会完成自动的垃圾回收，不需要人工回收内存，所以 Java 不支持析构器。</p><p>当然，某些对象使用了内存之外的其他资源，例如，文件或使用系统资源的另一个对象的句柄。在这种情况下，当资源不再需要时，将其回收和再利用就十分重要。</p><p>如果一个资源一旦使用完就需要立即关闭，那么应当提供一个 close 方法来完成必要的清理工作。可以在对象使用完时调用这个 close 方法。</p><p>如果可以等到虚拟机退出，那么可以用方法 <code>Runtime.addShutdownHook</code> 增加一个”关闭钩”（shutdown hook）。在 Java 9 中，可以使用 Cleaner 类注册一个动作，当对象不再可达时（除了清洁器还能访问，其他对象都无法访问这个对象），就会完成这个动作。在实际中这些情况很少见。可以参见 API 文档来了解这两种方法的详细内容。</p><p><strong>警告：</strong> 不要使用 finalize 方法来完成清理。这个方法原本要在垃圾回收器清理对象之前调用。不过，你并不能知道这个方法到底什么时候调用，而且该方法已经被废弃。</p><h3 id="4-7-记录"><a href="#4-7-记录" class="headerlink" title="4.7 记录"></a>4.7 记录</h3><p>有时，数据就只是数据，而面向对象程序设计提供的数据隐藏有些碍事。考虑一个类 Point，这个类描述平面上的一个点，有 x 和 y 坐标。</p><p>当然，可以如下创建一个类：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Point</span><br>&#123;<br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> <span class="hljs-type">double</span> x;<br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> <span class="hljs-type">double</span> y;<br>    <span class="hljs-keyword">public</span> <span class="hljs-title function_">Point</span><span class="hljs-params">(<span class="hljs-type">double</span> x, <span class="hljs-type">double</span> y)</span> &#123; <span class="hljs-built_in">this</span>.x = x; <span class="hljs-built_in">this</span>.y = y; &#125;<br>    <span class="hljs-keyword">public</span> <span class="hljs-type">double</span> <span class="hljs-title function_">getX</span><span class="hljs-params">()</span> &#123; <span class="hljs-keyword">return</span> x; &#125;<br>    <span class="hljs-keyword">public</span> <span class="hljs-type">double</span> <span class="hljs-title function_">getY</span><span class="hljs-params">()</span> &#123; <span class="hljs-keyword">return</span> y; &#125;<br>    <span class="hljs-keyword">public</span> String <span class="hljs-title function_">toString</span><span class="hljs-params">()</span> &#123; <span class="hljs-keyword">return</span> <span class="hljs-string">&quot;Point[x=%d, y=%d]&quot;</span>.formatted(x, y); &#125;<br>    <span class="hljs-comment">// More methods . . .</span><br>&#125;<br></code></pre></td></tr></table></figure><p>这里隐藏了 x 和 y，然后通过获取方法来获得这些值，不过，这种做法对我们确实有好处吗？</p><p>我们将来想改变 Point 的实现吗？当然，还有极坐标，不过对于图形 API，你可能不会使用极坐标。在实际中，平面上的一点就用 x 和 y 坐标来描述。</p><p>为了更简洁地定义这些类，JDK 14 引入了一个预览特性：”记录”。最终版本在 JDK 16 中发布。</p><h4 id="4-7-1-记录概念"><a href="#4-7-1-记录概念" class="headerlink" title="4.7.1 记录概念"></a>4.7.1 记录概念</h4><p><strong>记录</strong>（record）是一种特殊形式的类，其状态不可变，而且公共可读。可以如下将 Point 定义为一个记录：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">record</span> <span class="hljs-title class_">Point</span><span class="hljs-params">(<span class="hljs-type">double</span> x, <span class="hljs-type">double</span> y)</span> &#123;&#125;<br></code></pre></td></tr></table></figure><p>其结果是有以下实例字段的类：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> <span class="hljs-type">double</span> x;<br><span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> <span class="hljs-type">double</span> y;<br></code></pre></td></tr></table></figure><p>在 Java 语言规范中，一个记录的实例字段称为<strong>组件</strong>（component）。</p><p>这个类有一个构造器：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">Point(<span class="hljs-type">double</span> x, <span class="hljs-type">double</span> y)<br></code></pre></td></tr></table></figure><p>和以下访问方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-type">double</span> <span class="hljs-title function_">x</span><span class="hljs-params">()</span><br><span class="hljs-keyword">public</span> <span class="hljs-type">double</span> <span class="hljs-title function_">y</span><span class="hljs-params">()</span><br></code></pre></td></tr></table></figure><p>注意，访问方法名为 x 和 y，而不是 getX 和 getY。（Java 中实例字段可以与方法同名，这是合法的。）</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">var</span> <span class="hljs-variable">p</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Point</span>(<span class="hljs-number">3</span>, <span class="hljs-number">4</span>);<br>System.out.println(p.x() + <span class="hljs-string">&quot; &quot;</span> + p.y());<br></code></pre></td></tr></table></figure><p><strong>注释：</strong> Java没有遵循 get 约定，因为那有些麻烦。对于布尔字段，通常使用 is 而不是 get。而且首字母大写可能有问题。如果一个类既有 x 字段又有 X 字段，会发生什么？有些程序员不太满意，因为他们原先的类不能轻松地变为记录。不过实际上，那些遗留类中，很多都是可变的，所以并不适合转换为记录。</p><p>除了字段访问方法，每个记录有3个自动定义的方法：toString、equals和hashCode。下一章会更多地了解这些方法。</p><p><strong>警告：</strong> 对于这些自动提供的方法，也可以定义你自己的版本，只要它们有相同的参数和返回类型。例如，下面的定义就是合法的：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">record</span> <span class="hljs-title class_">Point</span><span class="hljs-params">(<span class="hljs-type">double</span> x, <span class="hljs-type">double</span> y)</span><br>&#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-type">double</span> <span class="hljs-title function_">x</span><span class="hljs-params">()</span> &#123; <span class="hljs-keyword">return</span> y; &#125; <span class="hljs-comment">// BAD</span><br>&#125;<br></code></pre></td></tr></table></figure><p>不过，这并不是一个好主意。</p><p>可以为一个记录增加你自己的方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">record</span> <span class="hljs-title class_">Point</span><span class="hljs-params">(<span class="hljs-type">double</span> x, <span class="hljs-type">double</span> y)</span><br>&#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-type">double</span> <span class="hljs-title function_">distanceFromOrigin</span><span class="hljs-params">()</span> &#123; <span class="hljs-keyword">return</span> Math.hypot(x, y); &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>与所有其他类一样，记录可以有静态字段和方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">record</span> <span class="hljs-title class_">Point</span><span class="hljs-params">(<span class="hljs-type">double</span> x, <span class="hljs-type">double</span> y)</span><br>&#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-type">Point</span> <span class="hljs-variable">ORIGIN</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Point</span>(<span class="hljs-number">0</span>, <span class="hljs-number">0</span>);<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-type">double</span> <span class="hljs-title function_">distance</span><span class="hljs-params">(Point p, Point q)</span><br>    &#123;<br>        <span class="hljs-keyword">return</span> Math.hypot(p.x - q.x, p.y - q.y);<br>    &#125;<br>    ...<br>&#125;<br></code></pre></td></tr></table></figure><p>不过，不能为记录增加实例字段：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">record</span> <span class="hljs-title class_">Point</span><span class="hljs-params">(<span class="hljs-type">double</span> x, <span class="hljs-type">double</span> y)</span><br>&#123;<br>    <span class="hljs-keyword">private</span> <span class="hljs-type">double</span> r; <span class="hljs-comment">// ERROR</span><br>&#125;<br></code></pre></td></tr></table></figure><p><strong>警告：</strong> 记录的实例字段自动为 final 字段。不过，它们可能是可变对象的引用。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">record</span> <span class="hljs-title class_">PointInTime</span><span class="hljs-params">(<span class="hljs-type">double</span> x, <span class="hljs-type">double</span> y, Date <span class="hljs-keyword">when</span>)</span> &#123;&#125;<br></code></pre></td></tr></table></figure><p>这样记录实例将是可变的：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">var</span> <span class="hljs-variable">pt</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">PointInTime</span>(<span class="hljs-number">0</span>, <span class="hljs-number">0</span>, <span class="hljs-keyword">new</span> <span class="hljs-title class_">Date</span>());<br>pt.<span class="hljs-keyword">when</span>().setTime(<span class="hljs-number">0</span>);<br></code></pre></td></tr></table></figure><p>如果希望记录实例是不可变的，那么字段就不能使用可变的类型。</p><p><strong>提示：</strong> 对于完全由一组变量表示的不可变数据，要使用记录而不是类。如果数据是可变的，或者数据表示可能随时间改变，则使用类。记录更易读、更高效，而且在并发程序中更安全。</p><h4 id="4-7-2-构造器：标准、自定义和简洁"><a href="#4-7-2-构造器：标准、自定义和简洁" class="headerlink" title="4.7.2 构造器：标准、自定义和简洁"></a>4.7.2 构造器：标准、自定义和简洁</h4><p>自动定义地设置所有实例字段的构造器称为<strong>标准构造器</strong>（canonical constructor）。还可以定义另外的<strong>自定义构造器</strong>（custom constructor）。这种构造器的第一个语句必须调用另一个构造器，所以最终会调用标准构造器。下面来看一个例子：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">record</span> <span class="hljs-title class_">Point</span><span class="hljs-params">(<span class="hljs-type">double</span> x, <span class="hljs-type">double</span> y)</span><br>&#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-title function_">Point</span><span class="hljs-params">()</span> &#123; <span class="hljs-built_in">this</span>(<span class="hljs-number">0</span>, <span class="hljs-number">0</span>); &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>这个记录有两个构造器：标准构造器和一个生成原点的无参数构造器。如果标准构造器需要完成额外的工作，那么可以提供你自己的实现：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">record</span> <span class="hljs-title class_">Range</span><span class="hljs-params">(<span class="hljs-type">int</span> from, <span class="hljs-type">int</span> to)</span><br>&#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-title function_">Range</span><span class="hljs-params">(<span class="hljs-type">int</span> from, <span class="hljs-type">int</span> to)</span><br>    &#123;<br>        <span class="hljs-keyword">if</span> (from &lt;= to)<br>        &#123;<br>            <span class="hljs-built_in">this</span>.from = from;<br>            <span class="hljs-built_in">this</span>.to = to;<br>        &#125;<br>        <span class="hljs-keyword">else</span><br>        &#123;<br>            <span class="hljs-built_in">this</span>.from = to;<br>            <span class="hljs-built_in">this</span>.to = from;<br>        &#125;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>不过，实现标准构造器时，建议使用一种<strong>简洁</strong>（compact）形式。不用指定参数列表：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">record</span> <span class="hljs-title class_">Range</span><span class="hljs-params">(<span class="hljs-type">int</span> from, <span class="hljs-type">int</span> to)</span><br>&#123;<br>    <span class="hljs-keyword">public</span> Range <span class="hljs-comment">// Compact form</span><br>    &#123;<br>        <span class="hljs-keyword">if</span> (from &gt; to) <span class="hljs-comment">// Swap the bounds</span><br>        &#123;<br>            <span class="hljs-type">int</span> <span class="hljs-variable">temp</span> <span class="hljs-operator">=</span> from;<br>            from = to;<br>            to = temp;<br>        &#125;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>简洁形式的主体是标准构造器的”前奏”。它只是在为实例字段 this.from 和 this.to 赋值之前修改参数变量 from 和 to 。不能在简洁构造器的主体中读取或修改实例字段。</p><h3 id="4-8-包"><a href="#4-8-包" class="headerlink" title="4.8 包"></a>4.8 包</h3><p>Java允许使用**包（package）**将类组织在一个集合中。借助包可以方便地组织你的代码，并将你自己的代码与其他人提供的代码库分开。下面我们将介绍如何使用和创建包。</p><h4 id="4-8-1-包名"><a href="#4-8-1-包名" class="headerlink" title="4.8.1 包名"></a>4.8.1 包名</h4><p>**使用包的主要原因是确保类名的唯一性。**假如两个程序员不约而同地提供了 Employee 类，只要他们将自己的类放置在不同的包中，就不会产生冲突。事实上，为了保证包名的绝对唯一性，可以使用一个因特网域名（这显然是唯一的）以逆序的形式作为包名，然后对于不同的项目使用不同的子包。例如，考虑域名 horstmann.com 。如果逆序来写，就得到了包名 com.horstmann 。然后可以追加一个项目名，如 com.horstmann.corejava 。如果再把 Employee 类放在这个包里，那么这个类的”完全限定”名就是 com.horstmann.corejava.Employee 。</p><p><strong>注释：</strong> 从编译器的角度来看，嵌套的包之间没有任何关系。例如，java.util 包与 java.util.jar 包毫无关系。每一个包都是独立的类集合。</p><h4 id="4-8-2-类的导入"><a href="#4-8-2-类的导入" class="headerlink" title="4.8.2 类的导入"></a>4.8.2 类的导入</h4><p>一个类可以使用所属包（这个类所在的包）中的所有类，以及其他包中的公共类（public class）。</p><p>我们可以采用两种方式访问另一个包中的公共类。第一种方式是使用<strong>完全限定名</strong>（fully qualified name），也就是包名后面跟着类名。例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">java.time.<span class="hljs-type">LocalDate</span> <span class="hljs-variable">today</span> <span class="hljs-operator">=</span> java.time.LocalDate.now();<br></code></pre></td></tr></table></figure><p>这显然很烦琐。更简单且更常用的方式是使用 import 语句。import 语句的关键是可以提供一种简写方式来引用包中各个类。一旦增加了 import 语句，在使用类时，就不必写出类的全名了。</p><p>可以使用 import 语句导入一个特定的类或者整个包。import 语句应该位于源文件的顶部（但位于 package 语句的后面）。例如，可以使用下面这条语句导入 java.time 包中的所有类。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">import</span> java.time.*;<br></code></pre></td></tr></table></figure><p>然后，就可以使用</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">LocalDate</span> <span class="hljs-variable">today</span> <span class="hljs-operator">=</span> LocalDate.now();<br></code></pre></td></tr></table></figure><p>而不需要在前面加上包前缀。还可以导入一个包中的特定类：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">import</span> java.time.LocalDate;<br></code></pre></td></tr></table></figure><p>java.time.* 的语法比较简单，对代码的规模也没有任何负面影响。不过，如果能够明确地指出所导入的类，那么代码的读者就能更加准确地知道你使用了哪些类。</p><p><strong>提示：</strong> 在 Eclipse 中，可以使用菜单选项 Source → Organize Imports。诸如 <code>import java.util.*;</code> 等语句将会自动扩展为一组特定的导入语句，如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">import</span> java.util.ArrayList;<br><span class="hljs-keyword">import</span> java.util.Date;<br></code></pre></td></tr></table></figure><p>这是一个十分便捷的特性。</p><p>但是，需要注意的是，只能使用星号 (*) 导入一个包，而不能使用 <code>import java.*</code> 或 <code>import java.*.*</code> 导入以 java 为前缀的所有包。</p><p>在大多数情况下，可以只导入你需要的包，并无须过多考虑。但在发生命名冲突的时候，就要注意包了。例如，java.util 和 java.sql 包都有 Date 类。假设在程序中导入了这两个包：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">import</span> java.util.*;<br><span class="hljs-keyword">import</span> java.sql.*;<br></code></pre></td></tr></table></figure><p>在程序中使用 Date 类的时候，就会出现一个编译错误：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">Date today; <span class="hljs-comment">// ERROR--java.util.Date or java.sql.Date?</span><br></code></pre></td></tr></table></figure><p>此时，编译器无法确定你想使用的是哪一个 Date 类。可以增加一个特定的 import 语句来解决这个问题：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">import</span> java.util.*;<br><span class="hljs-keyword">import</span> java.sql.*;<br><span class="hljs-keyword">import</span> java.util.Date;<br></code></pre></td></tr></table></figure><p>如果这两个 Date 类都需要使用，又该怎么办呢？答案是，在每个类名的前面加上完整的包名。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">var</span> <span class="hljs-variable">startTime</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">java</span>.util.Date();<br><span class="hljs-type">var</span> <span class="hljs-variable">today</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">java</span>.sql.Date(...);<br></code></pre></td></tr></table></figure><p>在包中定位类是编译器（compiler）的工作。类文件中的字节码总是使用完整的包名来引用其他类。</p><h4 id="4-8-3-静态导入"><a href="#4-8-3-静态导入" class="headerlink" title="4.8.3 静态导入"></a>4.8.3 静态导入</h4><p>有一种 import 语句允许导入静态方法和静态字段，而不只是类。例如，如果在源文件最上面添加一条指令：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">import</span> <span class="hljs-keyword">static</span> java.lang.System.*;<br></code></pre></td></tr></table></figure><p>就可以使用 System 类的静态方法和静态字段，而不必加类名前缀：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java">out.println(<span class="hljs-string">&quot;Goodbye, World!&quot;</span>); <span class="hljs-comment">// i.e., System.out</span><br>exit(<span class="hljs-number">0</span>); <span class="hljs-comment">// i.e., System.exit</span><br></code></pre></td></tr></table></figure><p>另外，还可以导入特定的方法或字段：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">import</span> <span class="hljs-keyword">static</span> java.lang.System.out;<br></code></pre></td></tr></table></figure><p>实际上，是否有很多程序员想要用简写 System.out 或 System.exit，这一点很让人怀疑。这样写出的代码看起来不太清晰。不过，</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">sqrt(pow(x, <span class="hljs-number">2</span>) + pow(y, <span class="hljs-number">2</span>))<br></code></pre></td></tr></table></figure><p>看起来则比</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">Math.sqrt(Math.pow(x, <span class="hljs-number">2</span>) + Math.pow(y, <span class="hljs-number">2</span>))<br></code></pre></td></tr></table></figure><p>简洁得多。</p><h4 id="4-8-4-在包中增加类"><a href="#4-8-4-在包中增加类" class="headerlink" title="4.8.4 在包中增加类"></a>4.8.4 在包中增加类</h4><p>要想将类放入包中，就必须将包名放在源文件的开头，即放在定义这个包中各个类的代码之前。例如，程序清单 4-8 中的文件 Employee.java 开头是这样的：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">package</span> com.horstmann.corejava;<br><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Employee</span><br>&#123;<br>    ...<br></code></pre></td></tr></table></figure><p>如果没有在源文件中放置package语句，那么这个源文件中的类就属于<strong>无名包</strong>（unnamed package）。无名包没有包名。到目前为止，我们定义的所有类都在无名包中。</p><p>将源文件放到与完整包名匹配的子目录中。例如，com.horstmann.corejava 包中的所有源文件应该放置在子目录com&#x2F;horstmann&#x2F;corejava 中（Windows中则是 com\horstmann\corejava ）。编译器将类文件也放在相同的目录结构中。</p><p>程序清单4-7 和程序清单4-8 中的程序分别放在两个包中：PackageTest 类属于无名包；Employee 类属于com.horstmann.corejava 包 。因此，Employee.java文件必须在子目录 com&#x2F;horstmann&#x2F;corejava 中。换句话说，目录结构如下所示：</p><figure class="highlight axapta"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs axapta">(base directory)<br>  PackageTest.java<br>  PackageTest.<span class="hljs-keyword">class</span><br>  <span class="hljs-title class_">com</span>/<br>    horstmann/<br>      corejava/<br>        Employee.java<br>        Employee.<span class="hljs-keyword">class</span><br></code></pre></td></tr></table></figure><p>要想编译这个程序，只需切换到基目录，并运行以下命令</p><figure class="highlight mipsasm"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs mipsasm"><span class="hljs-keyword">javac </span>PackageTest.<span class="hljs-keyword">java</span><br></code></pre></td></tr></table></figure><p>编译器就会自动地查找文件 com&#x2F;horstmann&#x2F;corejava&#x2F;Employee.java 并进行编译。</p><p>下面看一个更加实际的例子。在这里没有使用无名包，而是将类分别放在不同的包中（com.horstmann.corejava 和com.mycompany）。</p><figure class="highlight nix"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs nix">(base directory)<br>  com<span class="hljs-symbol">/</span><br>    horstmann<span class="hljs-symbol">/</span><br>      corejava<span class="hljs-symbol">/</span><br>        Employee.java<br>        Employee.class<br>    mycompany<span class="hljs-symbol">/</span><br>      PayrollApp.java<br>      PayrollApp.class<br></code></pre></td></tr></table></figure><p>在这种情况下，仍然要从基目录编译和运行类，即包含 com 目录的目录：</p><figure class="highlight avrasm"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs avrasm">javac <span class="hljs-keyword">com</span>/mycompany/PayrollApp.java<br>java <span class="hljs-keyword">com</span>.mycompany.PayrollApp<br></code></pre></td></tr></table></figure><p>再次强调，编译器处理文件（带有文件分隔符和扩展名.java 的文件），而 Java 解释器加载类（带有.分隔符）。</p><p><strong>提示：</strong> 从下一章开始，我们将对源代码使用包。这样一来，就可以为各章建立一个IDE项目，而不是各小节分别建立项目。</p><p><strong>警告：</strong> 编译器在编译源文件的时候不检查目录结构。例如，假设一个源文件开头有以下指令：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">package</span> com.mycompany;<br></code></pre></td></tr></table></figure><p>即使这个源文件不在子目录 com&#x2F;mycompany下，这个文件也可以编译。如果它不依赖于其他包，就可以通过编译而不会出现编译错误。但是，最终的程序将无法运行，除非先将所有类文件移到正确的位置上。如果包与目录不匹配，虚拟机就找不到这些类。</p><h4 id="4-8-5-包访问"><a href="#4-8-5-包访问" class="headerlink" title="4.8.5 包访问"></a>4.8.5 包访问</h4><p>前面已经见过访问修饰符 public 和 private。标记为 public 的部分可以由任意类使用；标记为 private 的部分只能由定义它们的类使用。如果没有指定 public 或 private，这个部分（类、方法或变量）可以由同一个包中的所有方法访问。</p><p>下面再来考虑程序清单 4-2。在这个程序中，没有将 Employee 类定义为公共类，因此只有在同一个包（在此是无名包）中的其他类（例如 EmployeeTest）可以访问这个类。对于类来说，这种默认方式是合乎情理的。但是，对于变量来说就有些不适宜了，变量必须显式地标记为 private，不然的话将默认为包可访问。显然，这样会破坏封装性。问题是人们经常忘记键入关键字 private。以 java.awt 包中的 Window 类为例（java.awt 包是 JDK 提供的源代码的一部分）：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Window</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Container</span><br>&#123;<br>    String warningString;<br>&#125;<br></code></pre></td></tr></table></figure><p>请注意，这里的 warningString 变量不是 private！这意味着 java.awt 包中的所有类的方法都可以访问该变量，并将它设置为任意值（例如，”Trust me!”）。实际上，只有 Window 类的方法访问这个变量，因此本应该将它设置为私有变量才合适。可能是程序员敲代码时匆忙之中忘记 private 修饰符了？也可能没有人关心这个问题？已经 20 多年了，这个变量仍然不是私有变量。不仅如此，这个类还陆续增加了一些新的字段，而其中大约有一半也不是私有的。</p><p>这可能会成为一个问题。在默认情况下，包不是封闭的实体。也就是说，任何人都可以向包中添加更多的类。当然，有恶意或糟糕的程序员很可能利用包访问添加一些能够改变变量的代码。例如，在 Java 程序设计语言的早期版本中，只需要将以下这条语句放在类文件的开头，就可以很容易地在 java.awt 包中混入其他类：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">package</span> java.awt;<br></code></pre></td></tr></table></figure><p>然后，把得到的类文件放置在类路径上某处的 java&#x2F;awt 子目录下，这样就可以访问 java.awt 包的内部了。使用这一手段，完全可以修改警告字符串（如图 4-9 所示）。</p><p>从 1.2 版开始，JDK 的实现者修改了类加载器，明确地禁止加载包名以 “java.” 开头的用户自定义的类！当然，用户自定义的类无法从这种保护中受益。另一种机制是让 JAR 文件声明包为密封的（sealed），以防止第三方修改，但这种机制已经过时。现在应当使用模块封装包。我们会在卷 II 的第 9 章详细讨论模块。</p><p><img src="/2025/10/30/%E3%80%8AJava%E6%A0%B8%E5%BF%83%E6%8A%80%E6%9C%AF%E5%8D%B7%E4%B8%80%E3%80%8B%E7%AC%AC%E5%9B%9B%E7%AB%A0/image-20251103163513243.png" alt="image-20251103163513243"></p><p>​<strong>图 4-9 在一个 applet 窗口中修改警告字符串</strong></p><h4 id="4-8-6-类路径"><a href="#4-8-6-类路径" class="headerlink" title="4.8.6 类路径"></a>4.8.6 类路径</h4><p>在前面已经看到，类存储在文件系统的子目录中。类的路径必须与包名匹配。</p><p>另外，类文件也可以存储在 JAR（Java 归档）文件中。在一个 JAR 文件中，可以包含多个压缩格式的类文件和子目录，这样既可以节省空间又可以改善性能。在程序中用到第三方的库时，你通常会得到一个或多个需要包含的 JAR 文件。</p><p><strong>提示：</strong> JAR 文件使用 ZIP 格式组织文件和子目录。可以使用任何 ZIP 工具查看 JAR 文件。</p><p>为了使类能够被多个程序共享，需要做到下面几点：</p><ol><li>把类文件放到一个目录中，例如 &#x2F;home&#x2F;user&#x2F;classdir。需要注意，这个目录是包树状结构的基目录。如果希望增加 com.horstmann.corejava.Employee 类，那么 Employee.class 类文件就必须位于子目录 &#x2F;home&#x2F;user&#x2F;classdir&#x2F;com&#x2F;horstmann&#x2F;corejava 中。</li><li>将 JAR 文件放在一个目录中，例如 &#x2F;home&#x2F;user&#x2F;archives。</li><li>设置类路径（class path）。类路径是所有包含类文件的路径的集合。</li></ol><p>在 UNIX 环境中，类路径中的各项之间用冒号（:）分隔：</p><figure class="highlight elixir"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs elixir">/home/user/<span class="hljs-symbol">classdir:</span>.<span class="hljs-symbol">:/home/user/archives/archive</span>.jar<br></code></pre></td></tr></table></figure><p>而在 Windows 环境中，则以分号（;）分隔：</p><figure class="highlight r"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs r"><span class="hljs-built_in">c</span><span class="hljs-operator">:</span><span class="hljs-punctuation">\</span>classdir;.;<span class="hljs-built_in">c</span><span class="hljs-operator">:</span><span class="hljs-punctuation">\</span>archives<span class="hljs-punctuation">\</span>archive.jar<br></code></pre></td></tr></table></figure><p>不论是 UNIX 还是 Windows，都用句点（.）表示当前目录。</p><p>类路径包括：</p><ul><li>基目录 &#x2F;home&#x2F;user&#x2F;classdir 或 c:\classdir；</li><li>当前目录（.）；</li><li>JAR 文件 &#x2F;home&#x2F;user&#x2F;archives&#x2F;archive.jar 或 c:\archives\archive.jar。</li></ul><p>从 Java 6 开始，可以为 JAR 文件目录指定一个通配符，如下：</p><figure class="highlight elixir"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs elixir">/home/user/<span class="hljs-symbol">classdir:</span>.<span class="hljs-symbol">:/home/user/archives/*</span><br></code></pre></td></tr></table></figure><p>或者</p><figure class="highlight r"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs r"><span class="hljs-built_in">c</span><span class="hljs-operator">:</span><span class="hljs-punctuation">\</span>classdir;.;<span class="hljs-built_in">c</span><span class="hljs-operator">:</span><span class="hljs-punctuation">\</span>archives<span class="hljs-punctuation">\</span><span class="hljs-operator">*</span><br></code></pre></td></tr></table></figure><p>在 UNIX 中，* 必须转义以防止 shell 扩展。</p><p>archives 目录中的所有 JAR 文件（但不包括 .class 文件）都包含在这个类路径中。</p><p>由于总是会搜索 Java API 的类，所以不必显式地包含在类路径中。</p><p><strong>警告：</strong> javac 编译器总是在当前目录中查找文件，但只有当类路径中包含 “ . “ 目录时，java 虚拟机才会查看当前目录。如果你没有设置类路径，那么没有什么问题，因为默认的类路径会包含 “ . “ 目录。但是如果你设置了类路径却忘记包含 “ . “ 目录，那么尽管你的程序可以没有错误地通过编译，但不能运行。</p><p>类路径所列出的目录和归档文件是搜寻类的起始点。下面看一个类路径示例：</p><figure class="highlight elixir"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs elixir">/home/user/<span class="hljs-symbol">classdir:</span>.<span class="hljs-symbol">:/home/user/archives/archive</span>.jar<br></code></pre></td></tr></table></figure><p>假定虚拟机要搜寻 com.horstmann.corejava.Employee 类的类文件。它首先要查看 Java API 类。显然，在那里找不到相应的类文件，所以转而查看类路径。它会查找以下文件：</p><ul><li>&#x2F;home&#x2F;user&#x2F;classdir&#x2F;com&#x2F;horstmann&#x2F;corejava&#x2F;Employee.class</li><li>com&#x2F;horstmann&#x2F;corejava&#x2F;Employee.class（从当前目录开始）</li><li>com&#x2F;horstmann&#x2F;corejava&#x2F;Employee.class（&#x2F;home&#x2F;user&#x2F;archives&#x2F;archive.jar 中）</li></ul><p>编译器查找文件要比虚拟机复杂得多。如果引用了一个类，而没有指定这个类的包，那么编译器将首先查找包含这个类的包。它会查看所有的 import 指令，确定其中是否包含这个类。例如，假定源文件包含指令：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">import</span> java.util.*;<br><span class="hljs-keyword">import</span> com.horstmann.corejava.*;<br></code></pre></td></tr></table></figure><p>并且源代码引用了 Employee 类。编译器将尝试查找 java.lang.Employee（因为总是会默认导入 java.lang 包）、java.util.Employee、com.horstmann.corejava.Employee 和当前包中的 Employee。它会在类路径所有位置中搜索以上各个类。如果找到了一个以上的类，就会产生编译时错误（因为完全限定类名必须是唯一的，所以 import 语句的次序并不重要）。</p><p>编译器的任务不止这些，它还要查看源文件是否比类文件新。如果是这样的话，那么源文件就会自动地重新编译。在前面已经知道，只可以导入其他包中的公共类。一个源文件只能包含一个公共类，并且文件名与公共类名必须匹配。因此，编译器很容易找到公共类的源文件。不过，还可以从当前包中导入非公共类。这些类有可能在与类名不同的源文件中定义。如果从当前包中导入一个类，那么编译器就要搜索当前包中的所有源文件，查看哪个源文件定义了这个类。</p><h4 id="4-8-7-设置类路径"><a href="#4-8-7-设置类路径" class="headerlink" title="4.8.7 设置类路径"></a>4.8.7 设置类路径</h4><p>最好使用 -classpath（或 -cp，或者 Java 9 中的 –class-path）选项指定类路径：</p><figure class="highlight elixir"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs elixir">java -classpath /home/user/<span class="hljs-symbol">classdir:</span>.<span class="hljs-symbol">:/home/user/archives/archive</span>.jar <span class="hljs-title class_">MyProg</span><br></code></pre></td></tr></table></figure><p>或者</p><figure class="highlight llvm"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs llvm">java -classpath <span class="hljs-keyword">c</span>:\classdir<span class="hljs-comment">;.;c:\archives\archive.jar MyProg</span><br></code></pre></td></tr></table></figure><p>整个指令必须写在一行中。将这样一个很长的命令行放在一个 shell 脚本或一个批处理文件中是个不错的主意。</p><p>利用 -classpath 选项设置类路径是首选的方法，另一种方法是通过设置 CLASSPATH 环境变量来指定类路径。具体细节依赖于所使用的 shell。在 Bourne Again shell（bash）中，命令如下：</p><figure class="highlight elixir"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs elixir">export <span class="hljs-title class_">CLASSPATH</span>=/home/user/<span class="hljs-symbol">classdir:</span>.<span class="hljs-symbol">:/home/user/archives/archive</span>.jar<br></code></pre></td></tr></table></figure><p>在 Windows shell 中，命令如下：</p><figure class="highlight routeros"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs routeros"><span class="hljs-built_in">set</span> <span class="hljs-attribute">CLASSPATH</span>=c:\classdir;.;c:\archives\archive.jar<br></code></pre></td></tr></table></figure><p>直到退出 shell 为止，类路径设置均有效。</p><p><strong>警告：</strong> 有人建议永久地设置 CLASSPATH 环境变量。一般来说这是一个糟糕的想法。人们有可能会忘记全局设置，因此，当他们的类没有正确地加载时，就会感到很奇怪。一个颇受诟病的示例是 Windows 中 Apple QuickTime 安装程序。很多年来，它都将 CLASSPATH 全局设置为指向它需要的一个 JAR 文件，而没有在类路径中包含当前目录。因此，当程序编译后却不能运行时，无数 Java 程序员不得不花费很多精力去解决这个问题。</p><p><strong>警告：</strong> 过去，有人建议完全绕过类路径，将所有的 JAR 文件都放在 jre&#x2F;lib&#x2F;ext 目录中。这种机制在 Java 9 中已经过时，不过不管怎样这都是一个不好的建议。从扩展目录加载一些已经遗忘很久的类时，这会让人非常困惑。</p><p><strong>注释：</strong> 在 Java 9 中，还可以从模块路径加载类。本书卷Ⅱ的第 9 章将讨论模块和模块路径。</p><h2 id="4-9-JAR-文件"><a href="#4-9-JAR-文件" class="headerlink" title="4.9 JAR 文件"></a>4.9 JAR 文件</h2><p>在将应用程序打包时，你希望只向用户提供一个单独的文件，而不是一个包含大量类文件的目录结构，Java 归档（JAR）文件就是为此目的而设计的。JAR 文件既可以包含类文件，也可以包含诸如图像和声音等其他类型的文件。此外，JAR 文件是压缩的，它使用了我们熟悉的 ZIP 压缩格式。</p><h4 id="4-9-1-创建-JAR-文件"><a href="#4-9-1-创建-JAR-文件" class="headerlink" title="4.9.1 创建 JAR 文件"></a>4.9.1 创建 JAR 文件</h4><p>可以使用 jar 工具制作 JAR 文件（在默认的 JDK 安装中，这个工具位于 jdk&#x2F;bin 目录下）。创建一个新 JAR 文件最常用的命令使用以下语法：</p><figure class="highlight gams"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs gams"><span class="hljs-function"><span class="hljs-title">jar</span></span> cvf jarFileName file1 file2 ...<br></code></pre></td></tr></table></figure><p>例如：</p><figure class="highlight axapta"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs axapta">jar cvf CalculatorClasses.jar *.<span class="hljs-keyword">class</span> <span class="hljs-title class_">icon</span>.gif<br></code></pre></td></tr></table></figure><p>通常，jar 命令的格式如下：</p><figure class="highlight gams"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs gams"><span class="hljs-function"><span class="hljs-title">jar</span></span> <span class="hljs-keyword">options</span> file1 file2 ...<br></code></pre></td></tr></table></figure><p>表 4-2 列出了 jar 程序的所有选项。它们类似于 UNIX tar 命令的选项。</p><p>​<strong>表 4-2 jar 程序选项</strong></p><table><thead><tr><th>选项</th><th>说明</th></tr></thead><tbody><tr><td>c</td><td>创建一个新的或者空的存档文件并加入文件。如果指定的文件名是目录，jar 程序将会对它们进行递归处理</td></tr><tr><td>C</td><td>临时改变目录，例如：<br><code>jar cvf jarFileName.jar -C classes *.class</code><br>切换到 classes 子目录以便增加类文件</td></tr><tr><td>e</td><td>在清单文件中创建一个入口点（请参见 4.9.3 节）</td></tr><tr><td>f</td><td>指定 JAR 文件名作为第二个命令行参数。如果没有这个参数，jar 命令会将结果写至标准输出（在创建 JAR 文件时）或者从标准输入读取输入（在解压或者列出 JAR 文件内容时）</td></tr><tr><td>i</td><td>创建索引文件（用于加快大型归档中的查找）</td></tr><tr><td>m</td><td>将一个清单文件添加到 JAR 文件中。清单文件是对归档内容和来源的一个说明。每个归档有一个默认的清单文件。但是，如果想验证归档文件的内容，可以提供你自己的清单文件</td></tr><tr><td>M</td><td>不为条目创建清单文件</td></tr><tr><td>t</td><td>显示内容表</td></tr><tr><td>u</td><td>更新一个已有的 JAR 文件</td></tr><tr><td>v</td><td>生成详细的输出</td></tr><tr><td>x</td><td>解压文件。如果提供一个或多个文件名，只解压这些文件；否则，解压所有文件</td></tr><tr><td>0</td><td>存储，但不进行 ZIP 压缩</td></tr></tbody></table><p>可以将应用程序和代码库打包在 JAR 文件中。例如，如果想在一个 Java 程序中发送邮件，可以使用打包在文件 javax.mail.jar 中的一个库。</p><h4 id="4-9-2-清单文件"><a href="#4-9-2-清单文件" class="headerlink" title="4.9.2 清单文件"></a>4.9.2 清单文件</h4><p>除了类文件、图像和其他资源外，每个 JAR 文件还包含一个清单文件（manifest），用于描述归档文件的特殊特性。</p><p>清单文件被命名为 MANIFEST.MF，它位于 JAR 文件的一个特殊的 META-INF 子目录中。合法的最小清单文件极其简单：</p><figure class="highlight apache"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs apache"><span class="hljs-attribute">Manifest</span>-Version: <span class="hljs-number">1</span>.<span class="hljs-number">0</span><br></code></pre></td></tr></table></figure><p>复杂的清单文件可能包含更多条目。这些清单条目被分组为多个节。第一节被称为<strong>主节</strong>（main section）。它作用于整个 JAR 文件。随后的条目可以指定命名实体的属性，如单个文件、包或者 URL。它们都必须以一个 Name 条目开始。节与节之间用空行分开。例如：</p><figure class="highlight gradle"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs gradle">Manifest-Version: <span class="hljs-number">1.0</span><br>lines describing <span class="hljs-keyword">this</span> archive<br><br>Name: Woozle.<span class="hljs-keyword">class</span><br>lines describing <span class="hljs-keyword">this</span> <span class="hljs-keyword">file</span><br>Name: com<span class="hljs-regexp">/mycompany/my</span>pkg/<br>lines describing <span class="hljs-keyword">this</span> <span class="hljs-keyword">package</span><br></code></pre></td></tr></table></figure><p>要想编辑清单文件，需要将希望添加到清单文件中的行放到文本文件中，然后运行</p><figure class="highlight mipsasm"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs mipsasm"><span class="hljs-keyword">jar </span>cfm <span class="hljs-keyword">jarFileName </span>manifestFileName . . .<br></code></pre></td></tr></table></figure><p>例如，要创建一个包含清单文件的 JAR 文件，应该运行</p><figure class="highlight coffeescript"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs coffeescript">jar cfm MyArchive.jar manifest.mf com<span class="hljs-regexp">/mycompany/mypkg/</span>*.<span class="hljs-keyword">class</span><br></code></pre></td></tr></table></figure><p>要想更新一个已有的 JAR 文件的清单，则需要将增加的部分放置到一个文本文件中，然后执行以下命令：</p><figure class="highlight mipsasm"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs mipsasm"><span class="hljs-keyword">jar </span>ufm MyArchive.<span class="hljs-keyword">jar </span>manifest-<span class="hljs-keyword">additions.mf</span><br></code></pre></td></tr></table></figure><p><strong>注释：</strong> 请参见 <a href="https://docs.oracle.com/javase/10/docs/specs/jar/jar.html">https://docs.oracle.com/javase/10/docs/specs/jar/jar.html</a> 获得有关 JAR 文件和清单文件格式的更多信息。</p><h4 id="4-9-3-可执行-JAR-文件"><a href="#4-9-3-可执行-JAR-文件" class="headerlink" title="4.9.3 可执行 JAR 文件"></a>4.9.3 可执行 JAR 文件</h4><p>可以使用 jar 命令中的 e 选项指定程序的入口点，即通常调用 java 执行程序时指定的类：</p><figure class="highlight stylus"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs stylus">jar cvfe MyProgram<span class="hljs-selector-class">.jar</span> com<span class="hljs-selector-class">.mycompany</span><span class="hljs-selector-class">.mypkg</span><span class="hljs-selector-class">.MainAppClass</span> files to add<br></code></pre></td></tr></table></figure><p>或者，可以在清单文件中指定程序的主类，包括以下形式的语句：</p><figure class="highlight stylus"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs stylus">Main-Class: com<span class="hljs-selector-class">.mycompany</span><span class="hljs-selector-class">.mypkg</span>.MainAppClass<br></code></pre></td></tr></table></figure><p>不要为主类名加扩展名 .class。</p><p><strong>警告：</strong> 清单文件的最后一行必须以换行符结束。否则，将无法正确地读取清单文件。常见的一个错误是创建了一个只包含 Main-Class 行而没有行结束符的文本文件。</p><p>不论使用哪一种方法，用户都可以简单地通过下面的命令来启动程序：</p><figure class="highlight mipsasm"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs mipsasm"><span class="hljs-keyword">java </span>-<span class="hljs-keyword">jar </span>MyProgram.<span class="hljs-keyword">jar</span><br></code></pre></td></tr></table></figure><p>取决于操作系统的配置，用户甚至可以通过双击 JAR 文件图标来启动应用程序。下面是各种操作系统的操作方式：</p><ul><li>在 Windows 平台中，Java 运行时安装程序将为 “.jar” 扩展名创建一个文件夹联，会用 javaw -jar 命令启动文件（与 java 命令不同，javaw 命令不打开 shell 窗口）。</li><li>在 Mac OS X 平台中，操作系统能够识别 “.jar” 扩展名文件。双击 JAR 文件时就会执行 Java 程序。</li></ul><p>在 Windows 平台中，可以使用第三方的包装器工具将 JAR 文件转换成 Windows 可执行文件。包装器是一个 Windows 程序，有大家熟悉的扩展名.exe，它可以查找和加载 Java 虚拟机（JVM），或者在没有找到 JVM 时会告诉用户应该做些什么。</p><h4 id="4-9-4-多版本-JAR-文件"><a href="#4-9-4-多版本-JAR-文件" class="headerlink" title="4.9.4 多版本 JAR 文件"></a>4.9.4 多版本 JAR 文件</h4><p>随着模块和包强封装的引入，之前可以访问的一些内部 API 不再可用。这可能要求库提供商为不同 Java 版本发布不同的代码。为此，Java 9 引入了多版本 JAR（multi-release JAR）。为了保证向后兼容，特定于版本的类文件放在 META-INF&#x2F;versions 目录中：</p><figure class="highlight coffeescript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs coffeescript">Application.<span class="hljs-keyword">class</span><br><span class="hljs-title class_">BuildingBlocks</span>.<span class="hljs-keyword">class</span><br><span class="hljs-title class_">Util</span>.<span class="hljs-keyword">class</span><br><span class="hljs-title class_">META</span>-INF<br> |_   MANIFEST.MF (<span class="hljs-keyword">with</span> line Multi-Release: <span class="hljs-literal">true</span>)<br> |_   versions<br>    |_    <span class="hljs-number">9</span><br>    |     |_    Application.<span class="hljs-keyword">class</span><br>    |     |_    BuildingBlocks.<span class="hljs-keyword">class</span><br>    |_    <span class="hljs-number">10</span><br>        |_    BuildingBlocks.<span class="hljs-keyword">class</span><br></code></pre></td></tr></table></figure><p>假设 Application 类使用了 CssParser 类，那么遗留版本的 Application.class 文件可以使用 com.sun.javafx.css.CssParser，而 Java 9 版本可以使用 javafx.css.CssParser。</p><p>Java 8 完全不知道 META-INF&#x2F;versions 目录，它只会加载遗留的类。Java 9 读取这个 JAR 文件时，则会使用新版本。</p><p>要增加不同版本的类文件，可以使用 –release 标志：</p><figure class="highlight arduino"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs arduino">jar uf MyProgram.jar --release <span class="hljs-number">9</span> Application.<span class="hljs-keyword">class</span><br></code></pre></td></tr></table></figure><p>要从头构建一个多版本 JAR 文件，可以使用 -C 选项，对应每个版本要切换到一个不同的类文件目录：</p><figure class="highlight mipsasm"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs mipsasm"><span class="hljs-keyword">jar </span>cf MyProgram.<span class="hljs-keyword">jar </span>-C <span class="hljs-keyword">bin/8 </span>. --release <span class="hljs-number">9</span> -C <span class="hljs-keyword">bin/9 </span>Application.class<br></code></pre></td></tr></table></figure><p>面向不同版本编译时，要使用 –release 标志和 -d 标志来指定输出目录：</p><figure class="highlight apache"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs apache"><span class="hljs-attribute">javac</span> -d bin/<span class="hljs-number">8</span> --release <span class="hljs-number">8</span> ...<br></code></pre></td></tr></table></figure><p>在 Java 9 中，-d 选项会创建这个目录（如果原先该目录不存在）。<br>–release 标志也是 Java 9 新增的。在较早的版本中，需要使用 -source、-target 和 -bootclasspath 标志。JDK 现在为之前的两个 API 版本提供了符号文件。在 Java 9 中，编译时可以将 –release 设置为 9、8 或 7。</p><p>多版本 JAR 并不适用于不同版本的程序或库。对于不同的版本，所有类的公共 API 都应当是一样的。多版本 JAR 的唯一作用是使你的某个特定版本的程序或库能够使用多个不同的 JDK 版本。如果你增加了功能或者改变了一个 API，就应当提供一个新版本的 JAR。</p><p><strong>注释：</strong> javap 之类的工具并没有改造为可以处理多版本 JAR 文件。如果调用</p><figure class="highlight gradle"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs gradle">javap -<span class="hljs-keyword">classpath</span> MyProgram.jar Application.<span class="hljs-keyword">class</span><br></code></pre></td></tr></table></figure><p>你会得到类的基本版本（毕竟，它与更新的版本应该有相同的公共 API）。如果必须查看更新的版本，则可以调用：</p><figure class="highlight gradle"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs gradle">javap -<span class="hljs-keyword">classpath</span> MyProgram.jar!<span class="hljs-regexp">/META-INF/</span>versions<span class="hljs-regexp">/9/</span>Application.<span class="hljs-keyword">class</span><br></code></pre></td></tr></table></figure><h4 id="4-9-5-关于命令行选项的说明"><a href="#4-9-5-关于命令行选项的说明" class="headerlink" title="4.9.5 关于命令行选项的说明"></a>4.9.5 关于命令行选项的说明</h4><p>Java 开发包（JDK）的命令行选项一直以来都使用单个短横线加多字母选项名的形式，如：</p><figure class="highlight mipsasm"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs mipsasm"><span class="hljs-keyword">java </span>-<span class="hljs-keyword">jar </span>...<br><span class="hljs-keyword">javac </span>-Xlint:unchecked -classpath ...<br></code></pre></td></tr></table></figure><p>但 jar 命令是个例外，这个命令遵循经典的 tar 命令选项格式，而没有短横线：</p><figure class="highlight gams"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs gams"><span class="hljs-function"><span class="hljs-title">jar</span></span> cvf ...<br></code></pre></td></tr></table></figure><p>从 Java 9 开始，Java 工具开始转向一种更常用的选项格式，多字母选项名前面加两个短横线，另外对于常用的选项可以使用单字母快捷方式。例如，调用 Linux ls 命令时可以提供一个”human-readable”选项：</p><figure class="highlight jboss-cli"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs jboss-cli"><span class="hljs-keyword">ls</span> <span class="hljs-params">--human-readable</span><br></code></pre></td></tr></table></figure><p>或者</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">ls</span> -h<br></code></pre></td></tr></table></figure><p>在 Java 9 中，可以使用 <code>--version</code> 而不是 <code>-version</code>，另外可以使用 <code>--class-path</code> 而不是 <code>-classpath</code>。在本书卷Ⅱ的第 9 章中可以看到，<code>--module-path</code> 选项有一个快捷方式 <code>-p</code>。</p><p>详细内容可以参见 JEP 293 增强请求（<a href="http://openjdk.java.net/jeps/293%EF%BC%89%E3%80%82%E5%9C%A8%E6%89%80%E6%9C%89%E6%B8%85%E7%90%86%E5%B7%A5%E4%BD%9C%E4%B8%AD%EF%BC%8C%E4%BD%9C%E8%80%85%E8%BF%98%E6%8F%90%E5%87%BA%E8%A6%81%E6%A0%87%E5%87%86%E5%8C%96%E9%80%89%E9%A1%B9%E5%8F%82%E6%95%B0%E3%80%82%E5%B8%A6">http://openjdk.java.net/jeps/293）。在所有清理工作中，作者还提出要标准化选项参数。带</a> <code>--</code> 和多字母选项的参数用空格或者一个等号 (&#x3D;) 分隔：</p><figure class="highlight jboss-cli"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs jboss-cli">javac <span class="hljs-params">--class-path</span> <span class="hljs-string">/home/user/classdir</span> <span class="hljs-string">...</span><br></code></pre></td></tr></table></figure><p>或</p><figure class="highlight arduino"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs arduino">javac --<span class="hljs-keyword">class</span>-path=/home/user/classdir ...<br></code></pre></td></tr></table></figure><p>单字母选项的参数可以用空格分隔，或者直接跟在选项后面：</p><figure class="highlight gams"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs gams"><span class="hljs-function"><span class="hljs-title">javac</span></span> -p moduledir ...<br></code></pre></td></tr></table></figure><p>或</p><figure class="highlight gams"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs gams"><span class="hljs-function"><span class="hljs-title">javac</span></span> -pmoduledir ...<br></code></pre></td></tr></table></figure><p><strong>警告：</strong> 后一种方式现在不能使用，而且一般来讲这也不是一个好主意。如果模块目录恰好是 <code>arameters</code> 或 <code>rocessor</code>，这就很容易与遗留的选项（<code>parameters</code> 或 <code>processor</code>）发生冲突，这又何必呢？</p><p>无参数的单字母选项可以组合在一起：</p><figure class="highlight mipsasm"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs mipsasm"><span class="hljs-keyword">jar </span>-cvf MyProgram.<span class="hljs-keyword">jar </span>-e mypackage.MyProgram *<span class="hljs-comment">/*.class</span><br></code></pre></td></tr></table></figure><p><strong>警告：</strong> 目前不能使用这种方式，这肯定会带来混淆。假设 javac 有一个 -c 选项，那么 javac -cp 是指 javac -c -p 还是 -cp？</p><p>这就会带来一些混乱，希望过段时间能够解决这个问题。尽管我们想要远离这些古老的 jar 选项，但最好还是等到尘埃落定为妙。不过，如果你想做到最现代化，那么可以安全地使用 jar 命令的长选项：</p><figure class="highlight brainfuck"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs brainfuck"><span class="hljs-comment">jar</span> <span class="hljs-literal">--</span><span class="hljs-comment">create</span> <span class="hljs-literal">--</span><span class="hljs-comment">verbose</span> <span class="hljs-literal">--</span><span class="hljs-comment">file jarFileName file1 file2</span> <span class="hljs-string">.</span> <span class="hljs-string">.</span> <span class="hljs-string">.</span><br></code></pre></td></tr></table></figure><p>对于单字母选项，如果不组合，也是可以使用的：</p><figure class="highlight mipsasm"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs mipsasm"><span class="hljs-keyword">jar </span>-c -v -f <span class="hljs-keyword">jarFileName </span>file1 file2 . . .<br></code></pre></td></tr></table></figure><h2 id="4-10-文档注释"><a href="#4-10-文档注释" class="headerlink" title="4.10 文档注释"></a>4.10 文档注释</h2><p>JDK 包含一个很有用的工具，叫作 javadoc，它可以由源文件生成一个 HTML 文档。事实上，在第 3 章介绍的联机 API 文档就是通过对标准 Java 类库的源代码运行 javadoc 生成的。</p><p>如果在源代码中添加以特殊定界符 &#x2F;** 开始的注释，那么你也可以很容易地生成一个看上去具有专业水准的文档。这是一种很好的方法，因为这样可以将代码与注释放在一个地方。应该知道，如果将文档存放在一个单独的文件中，随着时间的推移，代码和注释很可能出现不一致。不过，如果文档注释与源代码在同一个文件中，就可以很容易地同时修改源代码和注释，然后重新运行 javadoc。</p><h4 id="4-10-1-注释的插入"><a href="#4-10-1-注释的插入" class="headerlink" title="4.10.1 注释的插入"></a>4.10.1 注释的插入</h4><p>javadoc 实用工具从下面几项中抽取信息：</p><ul><li>模块；</li><li>包；</li><li>公共类与接口；</li><li>公共的和受保护的字段；</li><li>公共的和受保护的构造器及方法。</li></ul><p>注释以 &#x2F;** 开始，并以 *&#x2F; 结束。</p><p>每个 &#x2F;** . . . *&#x2F; 文档注释包含标记以及之后紧跟着的自由格式文本（free-form text）。标记以 @ 开始，如 @since 或 @param。</p><p>自由格式文本的第一个句子应该是一个概要陈述。javadoc 工具自动地将这些句子抽取出来生成概要页。</p><p>在自由格式文本中，可以使用 HTML 修饰符，例如，用于强调的 <code>&lt;em&gt;...&lt;/em&gt;</code>、用于着重强调的 <code>&lt;strong&gt;...&lt;/strong&gt;</code>、用于项目符号列表的 <code>&lt;ul&gt;/&lt;li&gt;</code> 以及用于包含图像的 <code>&lt;img .../&gt;</code> 等。要键入等宽代码，需要使用 <code>&#123;@code ... &#125;</code> 而不是 <code>&lt;code&gt;...&lt;/code&gt;</code>——这样一来，就不用操心对代码中的 &lt; 字符转义了。</p><p><strong>注释：</strong> 如果文档中有到其他文件的链接，如图像文件（例如，图表或用户界面组件的图像），就应该将这些文件放到包含源文件目录下的一个子目录 doc-files 中。javadoc 工具将从源目录将 doc-files 目录及其内容复制到文档目录中。在链接中需要使用 doc-files 目录，例如 <code>&lt;img src=&quot;doc-files/uml.png&quot; alt=&quot;UML diagram&quot;/&gt;</code>。</p><h4 id="4-10-2-类注释"><a href="#4-10-2-类注释" class="headerlink" title="4.10.2 类注释"></a>4.10.2 类注释</h4><p>类注释必须放在 import 语句之后，class 定义之前。<br>下面是一个类注释的例子：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-comment">/**</span><br><span class="hljs-comment"> * A &#123;<span class="hljs-doctag">@code</span> Card&#125; object represents a playing card, such</span><br><span class="hljs-comment"> * as &quot;Queen of Hearts&quot;. A card has a suit (Diamond, Heart,</span><br><span class="hljs-comment"> * Spade or Club) and a value (1 = Ace, 2 . . . 10, 11 = Jack,</span><br><span class="hljs-comment"> * 12 = Queen, 13 = King)</span><br><span class="hljs-comment"> */</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Card</span><br>&#123;<br>    ...<br>&#125;<br></code></pre></td></tr></table></figure><p><strong>注释：</strong> 没有必要在每一行的开始都添加 *，例如，以下注释同样是合法的：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-comment">/**</span><br><span class="hljs-comment">  A &lt;code&gt;Card&lt;/code&gt; object represents a playing card, such</span><br><span class="hljs-comment">  as &quot;Queen of Hearts&quot;. A card has a suit (Diamond, Heart,</span><br><span class="hljs-comment">  Spade or Club) and a value (1 = Ace, 2 . . . 10, 11 = Jack,</span><br><span class="hljs-comment">  12 = Queen, 13 = King).</span><br><span class="hljs-comment">*/</span><br></code></pre></td></tr></table></figure><p>不过，大部分 IDE 会自动提供星号，而且换行改变时，还会重新放置星号。</p><h4 id="4-10-3-方法注释"><a href="#4-10-3-方法注释" class="headerlink" title="4.10.3 方法注释"></a>4.10.3 方法注释</h4><p>每个方法注释必须放在所描述的方法之前。除了通用标记之外，还可以使用下面的标记：</p><ul><li><p><code>@param variable description</code><br>这个标记将给当前方法的”parameters”（参数）部分添加一个条目。这个描述可以占据多行，并且可以使用 HTML 标记。一个方法的所有 @param 标记必须放在一起。</p></li><li><p><code>@return description</code><br>这个标记将给当前方法添加”returns”（返回）部分。这个描述可以跨多行，并且可以使用 HTML 标记。</p></li></ul><ul><li><code>@throws class description</code><br>这个标记将添加一个注释，表示这个方法有可能抛出异常。有关异常的详细内容将在第7章中讨论。</li></ul><p>下面是一个方法注释的示例：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-comment">/**</span><br><span class="hljs-comment"> * Raises the salary of an employee.</span><br><span class="hljs-comment"> * <span class="hljs-doctag">@param</span> byPercent the percentage by which to raise the salary (e.g., 10 means 10%)</span><br><span class="hljs-comment"> * <span class="hljs-doctag">@return</span> the amount of the raise</span><br><span class="hljs-comment"> */</span><br><span class="hljs-keyword">public</span> <span class="hljs-type">double</span> <span class="hljs-title function_">raiseSalary</span><span class="hljs-params">(<span class="hljs-type">double</span> byPercent)</span><br>&#123;<br>    <span class="hljs-type">double</span> <span class="hljs-variable">raise</span> <span class="hljs-operator">=</span> salary * byPercent / <span class="hljs-number">100</span>;<br>    salary += raise;<br>    <span class="hljs-keyword">return</span> raise;<br>&#125;<br></code></pre></td></tr></table></figure><h4 id="4-10-4-字段注释"><a href="#4-10-4-字段注释" class="headerlink" title="4.10.4 字段注释"></a>4.10.4 字段注释</h4><p>只需要对公共字段（通常指的是静态常量）增加文档注释。例如，</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-comment">/**</span><br><span class="hljs-comment"> * The &quot;Hearts&quot; card suit</span><br><span class="hljs-comment"> */</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">int</span> <span class="hljs-variable">HEARTS</span> <span class="hljs-operator">=</span> <span class="hljs-number">1</span>;<br></code></pre></td></tr></table></figure><h4 id="4-10-5-通用注释"><a href="#4-10-5-通用注释" class="headerlink" title="4.10.5 通用注释"></a>4.10.5 通用注释</h4><p>标记 <code>@since text</code> 会建立一个”since”（始于）条目。text（文本）可以是对引入这个特性的版本的描述。例如，<code>@since 1.7.1</code>。</p><p>类文档注释中可以使用下面的标记：</p><ul><li><p><code>@author name</code><br>这个标记将建立一个”author”（作者）条目。可以有多个@author标记，每个@author标记对应一个作者。并不是非得使用这个标记，你的版本控制系统能够更好地跟踪作者。</p></li><li><p><code>@version text</code><br>这个标记将建立一个”version”（版本）条目。这里的text可以是对当前版本的任何描述。</p></li></ul><p>通过<code>@see</code>和<code>@link</code>标记，可以使用超链接，链接到javadoc文档的相关部分或外部文档。</p><p>标记<code>@see reference</code>将在”see also”（参见）部分增加一个超链接。它可以用于类中，也可以用于方法中。这里的reference（引用）可以有以下选择：</p><figure class="highlight dockerfile"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs dockerfile">package.class<span class="hljs-comment">#feature label</span><br>&lt;a href=<span class="hljs-string">&quot;...&quot;</span>&gt;<span class="hljs-keyword">label</span><span class="language-bash">&lt;/a&gt;</span><br><span class="hljs-string">&quot;text&quot;</span><br></code></pre></td></tr></table></figure><p>第一种情况是最有用的。只要提供类、方法或变量的名字，javadoc就在文档中插入一个超链接。例如，</p><figure class="highlight leaf"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs leaf">@see com.horstmann.corejava.Employee<span class="hljs-punctuation">#</span><span class="hljs-keyword">raiseSalary</span><span class="hljs-params">(<span class="hljs-variable">double</span>)</span><br></code></pre></td></tr></table></figure><p>会建立一个超链接，链接到 com.horstmann.corejava.Employee 类的 raiseSalary(double) 方法。可以省略包名，甚至把包名和类名都省去，这样一来，这会位于当前包或当前类。</p><p>需要注意，一定要使用井号（#），而不要使用句号（.）分隔类名与方法名（或类名与变量名）。Java 编译器自身可以熟练地确定句点在分隔包、子包、类、内部类以及方法和变量时的不同含义。但是 javadoc 工具就没有这么聪明了，因此必须对它提供帮助。</p><p>如果 @see 标记后面有一个 &lt; 字符，就需要指定一个超链接。可以超链接到任何 URL。例如：</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs xml">@see <span class="hljs-tag">&lt;<span class="hljs-name">a</span> <span class="hljs-attr">href</span>=<span class="hljs-string">&quot;www.horstmann.com/corejava.html&quot;</span>&gt;</span>The Core Java home page<span class="hljs-tag">&lt;/<span class="hljs-name">a</span>&gt;</span><br></code></pre></td></tr></table></figure><p>在上述各种情况下，都可以指定一个可选的标签（label），这会显示为链接锚（link anchor）。如果省略了标签，则用户看到的锚就是目标代码名或 URL。</p><p>如果 @see 标记后面有一个双引号（”）字符，文本就会显示在”see also”部分。例如，</p><figure class="highlight aspectj"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs aspectj"><span class="hljs-meta">@see</span> <span class="hljs-string">&quot;Core Java 2 volume 2&quot;</span><br></code></pre></td></tr></table></figure><p>可以为一个特性添加多个 @see 标记，但必须将它们放在一起。</p><p>如果愿意，可以在任何文档注释中放置指向其他类或方法的超链接。可以在注释中的任何位置插入一个形式如下的特殊标记：</p><figure class="highlight ceylon"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs ceylon">&#123;<span class="hljs-meta">@link</span> <span class="hljs-keyword">package</span>.<span class="hljs-keyword">class</span><span class="hljs-number">#fea</span>ture label&#125;<br></code></pre></td></tr></table></figure><p>这里的特性描述规则与 @see 标记的规则相同。</p><p>最后，在 Java 9 中，还可以使用 <code>&#123;@index entry&#125;</code> 标记为搜索框增加一个条目。</p><h4 id="4-10-6-包注释"><a href="#4-10-6-包注释" class="headerlink" title="4.10.6 包注释"></a>4.10.6 包注释</h4><p>可以直接将类、方法和变量的注释放置在 Java 源文件中，只要用 &#x2F;**…*&#x2F; 文档注释界定就可以了。但是，要想产生包注释，就需要在每一个包目录中添加一个单独的文件。可以有如下两个选择：</p><ol><li><p>提供一个名为 package-info.java 的 Java 文件。这个文件必须包含一个初始的 Javadoc 注释，以 &#x2F;** 和 *&#x2F; 界定，后面是一个 package 语句。它不能包含更多的代码或注释。</p></li><li><p>提供一个名为 package.html 的 HTML 文件，抽取标记 <code>&lt;body&gt;...&lt;/body&gt;</code> 之间的所有文本。</p></li></ol><h4 id="4-10-7-注释提取"><a href="#4-10-7-注释提取" class="headerlink" title="4.10.7 注释提取"></a>4.10.7 注释提取</h4><p>在这里，假设你希望 HTML 文件将放在名为 docDirectory 的目录下。执行以下步骤：</p><ol><li><p>切换到源文件目录，其中包含想要生成文档的源文件。如果有嵌套的包要生成文档，例如 com.horstmann.corejava，就必须切换到包含子目录 com 的目录（如果提供 overview.html 文件的话，这就是这个文件所在的目录）。</p></li><li><p>如果是一个包，应该运行命令：</p></li></ol><figure class="highlight ebnf"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs ebnf"><span class="hljs-attribute">javadoc -d docDirectory nameOfPackage</span><br></code></pre></td></tr></table></figure><p>或者，如果要为多个包生成文档，运行：</p><figure class="highlight gams"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs gams"><span class="hljs-function"><span class="hljs-title">javadoc</span></span> -d docDirectory nameOfPackage1 nameOfPackage2...<br></code></pre></td></tr></table></figure><p>如果你的文件在无名包中，则应该运行：</p><figure class="highlight nginx"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs nginx"><span class="hljs-attribute">javadoc</span> -d docDirectory <span class="hljs-regexp">*.java</span><br></code></pre></td></tr></table></figure><p>如果省略了 -d docDirectory 选项，HTML 文件就会提取到当前目录下。这样可能很混乱，因此我不提倡这种做法。</p><p>可以使用很多命令行选项对 javadoc 程序进行微调。例如，可以使用 -author 和 -version 选项在文档中包含 @author 和 @version 标记（默认情况下，这些标记会被省略）。另一个很有用的选项是 -link，用来为标准类添加超链接。例如，如果使用命令</p><figure class="highlight awk"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs awk">javadoc -link http:<span class="hljs-regexp">//</span>docs.oracle.com<span class="hljs-regexp">/javase/</span><span class="hljs-number">9</span><span class="hljs-regexp">/docs/</span>api *.java<br></code></pre></td></tr></table></figure><p>那么，所有的标准类的类都会自动地链接到 Oracle 网站的文档。</p><p>如果使用 -linksource 选项，那么每个源文件将会转换为 HTML（不对代码着色，但包含符号），并且每个类和方法名称变为指向源代码的超链接。</p><p>还可以为所有源文件提供一个概要注释。把它放在一个类似 overview.html 的文件中，运行 javadoc 工具，并提供命令行选项 -overview filename。将抽取标记 <code>&lt;body&gt;...&lt;/body&gt;</code> 之间的所有文本。当用户从导航栏中选择”Overview”时，就会显示这些内容。</p><p>有关其他的选项，请查阅 javadoc 工具的联机文档 <a href="https://docs.oracle.com/javase/9/javadoc/javadoc.htm%E3%80%82">https://docs.oracle.com/javase/9/javadoc/javadoc.htm。</a></p><h2 id="4-11-类设计技巧"><a href="#4-11-类设计技巧" class="headerlink" title="4.11 类设计技巧"></a>4.11 类设计技巧</h2><p>我们不会面面俱到，也不希望过于沉闷，所以在这一章结束之前再简单地介绍几点技巧。应用这些技巧可以使你设计的类更能得到专业 OOP 圈子的认可。</p><ol><li><p><strong>一定要保证数据私有。</strong><br>这是最重要的；绝对不要破坏封装性。有时候，可能需要编写一个访问方法或更改器方法，但是最好还是保持实例字段的私有性。很多惨痛的教训告诉我们，数据的表示形式很可能会改变，但它们的使用方式却不会经常变化。当数据保持私有时，表示形式的变化不会对类的使用者产生影响，而且也更容易检测 bug。</p></li><li><p><strong>一定要初始化数据。</strong><br>Java 不会为你初始化局部变量，但是会对对象的实例字段进行初始化。最好不要依赖于系统的默认值，而是应该显式地初始化所有变量，可以提供默认值，也可以在所有构造器中设置默认值。</p></li><li><p><strong>不要在类中使用过多的基本类型。</strong><br>其想法是要用其他的类，而不是使用多个相关的基本类型。这样会使类更易于理解，也更易于修改。例如，可以用一个名为 Address 的新类替换一个 Customer 类中的以下实例字段：</p></li></ol><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">private</span> String street;<br><span class="hljs-keyword">private</span> String city;<br><span class="hljs-keyword">private</span> String state;<br><span class="hljs-keyword">private</span> <span class="hljs-type">int</span> zip;<br></code></pre></td></tr></table></figure><p>这样一来，可以很容易地处理地址的变化，例如，可能需要处理国际地址。</p><ol start="4"><li><p><strong>不是所有的字段都需要单独的字段访问器和更改器。</strong><br>你可能需要获得或设置员工的工资。而一旦构造了员工对象，肯定不需要更改雇用日期。另外，在对象中，常常包含一些不希望别人获得或设置的实例字段，例如，Address类中的州缩写数组。</p></li><li><p><strong>分解有过多职责的类。</strong><br>这样说似乎有点含糊，究竟多少算是”过多”？每个人的看法都不同。但是，如果明显地可以将一个复杂的类分解成两个概念上更为简单的类，就应该进行分解。（但另一方面，也不要走极端。如果设计10个类，每个类只有一个方法，显然就有些矫枉过正了。）</p><p>下面是一个反面的设计示例。</p></li></ol><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">CardDeck</span> <span class="hljs-comment">// bad design</span><br>&#123;<br>    <span class="hljs-keyword">private</span> <span class="hljs-type">int</span>[] value;<br>    <span class="hljs-keyword">private</span> <span class="hljs-type">int</span>[] suit;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-title function_">CardDeck</span><span class="hljs-params">()</span> &#123; . . . &#125;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">shuffle</span><span class="hljs-params">()</span> &#123; . . . &#125;<br>    <span class="hljs-keyword">public</span> <span class="hljs-type">int</span> <span class="hljs-title function_">getTopValue</span><span class="hljs-params">()</span> &#123; . . . &#125;<br>    <span class="hljs-keyword">public</span> <span class="hljs-type">int</span> <span class="hljs-title function_">getTopSuit</span><span class="hljs-params">()</span> &#123; . . . &#125;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">draw</span><span class="hljs-params">()</span> &#123; . . . &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>实际上，这个类实现了两个独立的概念：一副牌（包含shuffle方法和draw方法）和一张牌（包含查看面值和花色的方法）。最好引入一个表示一张牌的Card类。现在有两个类，每个类分别完成自己的职责：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">CardDeck</span><br>&#123;<br>    <span class="hljs-keyword">private</span> Card[] cards;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-title function_">CardDeck</span><span class="hljs-params">()</span> &#123; . . . &#125;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">shuffle</span><span class="hljs-params">()</span> &#123; . . . &#125;<br>    <span class="hljs-keyword">public</span> Card <span class="hljs-title function_">getTop</span><span class="hljs-params">()</span> &#123; . . . &#125;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">draw</span><span class="hljs-params">()</span> &#123; . . . &#125;<br>&#125;<br><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Card</span><br>&#123;<br>    <span class="hljs-keyword">private</span> <span class="hljs-type">int</span> value;<br>    <span class="hljs-keyword">private</span> <span class="hljs-type">int</span> suit;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-title function_">Card</span><span class="hljs-params">(<span class="hljs-type">int</span> aValue, <span class="hljs-type">int</span> aSuit)</span> &#123; . . . &#125;<br>    <span class="hljs-keyword">public</span> <span class="hljs-type">int</span> <span class="hljs-title function_">getValue</span><span class="hljs-params">()</span> &#123; . . . &#125;<br>    <span class="hljs-keyword">public</span> <span class="hljs-type">int</span> <span class="hljs-title function_">getSuit</span><span class="hljs-params">()</span> &#123; . . . &#125;<br></code></pre></td></tr></table></figure><ol start="6"><li><p><strong>类名和方法名要能够体现它们的职责。</strong><br>变量应该有一个能够反映其含义的名字，类似地，类也应该如此（在标准类库中，确实存在着一些含义不明确的例子，如 Date 类实际上是一个描述时间的类）。</p><p>对此有一个很好的惯例：类名应当是一个名词（Order），或者是前面有形容词修饰的名词（RushOrder），或者是有动名词（有”-ing”后缀）修饰的名词（例如，BillingAddress）。对于方法来说，要遵循标准惯例：访问器方法用小写 get 开头（getSalary），更改器方法用小写的 set 开头（setSalary）。</p></li><li><p><strong>优先使用不可变的类。</strong><br>LocalDate 类以及 java.time 包中的其他类是不可变的——没有方法能修改对象的状态。类似 plusDays 的方法并不会更改对象，而是会返回状态已修改的新对象。</p><p>更改对象的问题在于，如果多个线程试图同时更新一个对象，就会发生并发更改，其结果是不可预料的。如果类是不可变的，就可以安全地在多个线程间共享其对象。</p><p>因此，要尽可能让类是不可变的，这是一个很好的想法。对于表示值的类，如一个字符串或一个时间点，这尤其容易。计算会生成新值，而不是更新原来的值。</p><p>当然，并不是所有类都应当是不可变的。如果员工加薪时让 raiseSalary 方法返回一个新的 Employee 对象，这会很奇怪。</p></li></ol>]]></content>
    
    
    <categories>
      
      <category>Java</category>
      
    </categories>
    
    
    <tags>
      
      <tag>原创</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>《Java核心技术卷一》第五章</title>
    <link href="/2025/10/20/%E3%80%8AJava%E6%A0%B8%E5%BF%83%E6%8A%80%E6%9C%AF%E5%8D%B7%E4%B8%80%E3%80%8B%E7%AC%AC%E4%BA%94%E7%AB%A0/"/>
    <url>/2025/10/20/%E3%80%8AJava%E6%A0%B8%E5%BF%83%E6%8A%80%E6%9C%AF%E5%8D%B7%E4%B8%80%E3%80%8B%E7%AC%AC%E4%BA%94%E7%AB%A0/</url>
    
    <content type="html"><![CDATA[<figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Manager</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Employee</span><br>&#123;<br>    added methods and fields<br>&#125;<br></code></pre></td></tr></table></figure><p>在Java中，所有的继承都是公共继承。</p><p><strong>关键字extends指示正在构造的新类派生于一个已存在的类。</strong></p><p>这个已存在的类称为超类、基类或父类；新类称为子类或派生类。</p><p>子类比超类拥有的功能更多。所有员工组成的集合包含所有经理组成的集合，员工集合是经理集合的超集，经理集合是员工集合的子集。</p><p>声明为私有的类成员不会被这个类的子类继承。（私有字段不会继承，因为子类不能直接访问这些私有字段，子类拥有这些字段）</p><p><strong>记录，就是状态完全由构造器参数定义的类，不能扩展记录，而且记录也不能扩展其他类。</strong></p><p>关键字 super 指示编译器调用超类方法</p><p><code>super(name, salary,...);</code>  “调用超类中带有name，salary等参数的构造器的简写形式”</p><p>由于子类的构造器不能访问超类的私有字段，所以必须通过一个构造器来初始化这些私有字段，可以利用特殊的super语法调用这个构造器，使用super调用构造器的语句必须是子类构造器的第一条语句。</p><p><strong>如果构造子类对象时没有显式的调用超类的构造器，那么超类必须有一个无参数构造器，这个构造器要在子类构造之前调用。</strong></p><p> this 的两个含义：一是指示隐式参数的引用；二是调用该类的其他构造器</p><p> super 的两个含义：一是调用超类的方法；二是调用超类的构造器</p><p>总结：核心逻辑链</p><p>创建子类对象 → 需初始化父类 + 子类 → 子类无法直接初始化父类私有字段 → 必须通过“调用父类构造器”完成父类初始化 →  super(参数列表) 是调用父类构造器的语法 → 该语句必须是子类构造器第一条语句 → 若子类没显式调用，编译器自动插  super() → 要求父类必须有无参构造器。</p><p>一个对象变量可以指示多种实际类型，这一点称为多态，在运行时能够自动地选择适当的方法，这称为动态绑定。</p><p>继承并不仅限于一个层次。</p><p>由一个公共超类派生出来的所有类的集合称为继承层次结构，在继承层次结构中，从某个特定的类到其祖先的路径称为该类的继承链。</p><p><strong>Java不支持多重继承。</strong>（多重继承允许一个类同时继承多个父类的属性和方法）</p><p>有一个简单的规则可以用来判断是否应该将数据设计为继承关系，这就是“is－a”规则，它指出子类的每个对象也是超类的对象。</p><p>“is－a”规则的另一种表述是替换原则。它指出程序中需要超类对象的任何地方都可以使用子类对象替换。</p><p>在java中，对象变量是多态的。一个Employee类型的变量既可以引用一个Employee类型的对象，也可以引用Employee类的任何一个子类的对象。</p><p>不能将超类的引用赋给子类变量。</p><p>在java中，子类引用数组可以转换成超类引用数组，而不需要使用强制类型转换。（存在风险）</p><h2 id="理解方法调用"><a href="#理解方法调用" class="headerlink" title="理解方法调用"></a>理解方法调用</h2><p>准确地理解如何在对象上应用方法调用非常重要。下面假设要调用x.f(args)，隐式参数</p><p>x声明为类C的一个对象。下面是调用过程的详细描述：</p><ol><li><p>编译器查看对象的声明类型和方法名。需要注意的是：有可能存在多个名字为f（参数类类型不一样）的方法。例如，可能存在方法f(int)和方法f(string)。编译器将会一一访问。接下来，编译器已知所有可能调用的候选方法。</p><p> 至此，编译器已知所有可能调用的候选方法。</p></li><li><p>接下来，编译器要确定方法调用中提供的参数类型。如果在所有名为f的方法中存在一个与所提供参数类型完全匹配的方法，就选择这个方法。这个过程称为<strong>重载解析</strong>(overloading resolution)。例如，对于调用x.f(“Hello”)，编译器将会挑选f(string)，而不是f(int)。由于允许类型转换（int可以转换成double，Manager可以转换成Employee，等等），所以情况可能会变得很复杂。如果编译器没有找到与参数类型匹配的方法，或者发现经过类型转换后有多个方法与之匹配，编译器就会报告一个错误。</p><p> 至此，编译器已经知道需要调用的方法的名字和参数类型。</p></li></ol><p>注释：前面曾经说过，方法的名字和参数列表称为方法的<strong>签名</strong>(signature)。例如，f(int)和f(string)是两个有相同名字、不同签名的方法。如果在子类中定义了一个与超类签名相同的方法，那么子类中的这个方法就会覆盖(override)超类中有相同签名的方法。</p><p>返回类型不是签名的一部分。不过在覆盖一个方法时，需要保证返回类型的兼容性。允许子类将覆盖方法的返回类型改为原返回类型的子类型。例如，假设Employee类型有以下方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> Employee <span class="hljs-title function_">getBuddy</span><span class="hljs-params">()</span> &#123;...&#125;<br></code></pre></td></tr></table></figure><p>经理不会想找底层员作为工作搭档。为了反映这一点，在子类Manager中，可以如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> Manager <span class="hljs-title function_">getBuddy</span><span class="hljs-params">()</span> &#123;...&#125;<span class="hljs-comment">//ok to change return type</span><br></code></pre></td></tr></table></figure><p>我们说，这两个getBuddy方法有<strong>协变</strong>(covariant)的返回类型。</p><ol start="3"><li>如果是private方法、static方法、final方法（final修饰符将在下一节解释）或者构造器，那么编译器可以准确地知道应该调用哪个方法。这称为<strong>静态绑定</strong>(static binding)。与此对应的是，如果要调用的方法依赖于隐式参数的实际类型，那么必须在运行时使用动态绑定。在我们的示例中，编译器会利用动态绑定生成一个调用f(string)的指令。</li><li>程序运行并且采用动态绑定调用方法时，虚拟机必须调用与x所引用对象的实际类型对应的那个方法。假设x的实际类型是D，它是C类的子类。如果D类定义了方法f(string)，就会调用这个方法；否则，将在D类的超类中寻找f(string)，依此类推。每次调用方法都要完成这个搜索，时间开销相当大。因此，虚拟机预先为每个类计算了一个<strong>方法表</strong>(method table)，其中列出了所有方法的签名和要调用的实际方法。虚拟机加载一个类之后可以构建这个方法表，为此要结合它在类文件中找到的方法以及超类的方法表。</li></ol><p>这样一来，真正调用方法的时候，虚拟机仅查找这个表就行了。在前面的例子中，虚拟机搜索D类的方法表，寻找与f(String)相匹配的方法。这个方法既有可能是D.f(String)，也有可能是X.f(String)，这里的X是D的某个超类。这种情况下需要提醒一点，如果调用是super.f(param)，那么编译器将搜索超类的方法表。</p><p>现在来详细分析程序清单5-1中调用e.getSalary()的过程。e声明为Employee类型。Employee类只有一个名叫getSalary的方法，这个方法没有参数。因此，在这里不必担心重载解析的问题。</p><p>由于getSalary不是private方法、static方法或final方法，所以将采用动态绑定。虚拟机为Employee和Manager类生成方法表。在Employee的方法表中列出了这个Employee类本身定义的所有方法:</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs java">Employee:<br><br>getName() -&gt; Employee.getName()<br><br>getSalary() -&gt; Employee.getSalary()<br><br>getHireDay() -&gt; Employee.getHireDay()<br><br>raiseSalary(<span class="hljs-type">double</span>) -&gt; Employee.raiseSalary(<span class="hljs-type">double</span>)<br></code></pre></td></tr></table></figure><p>实际上，上面列出的方法并不完整，稍后会看到Employee类有一个超类Object，Employee类从这个超类中还继承了大量方法，在此，我们略去了Object方法。</p><p>Manager方法表稍微有些不同。其中有三个方法是继承而来的，一个方法是重新定义的，还有一个方法是新增的。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs java">Manager:<br><br>getName() -&gt; Employee.getName()<br><br>getSalary() -&gt; Manager.getSalary()<br><br>getHireDay() -&gt; Employee.getHireDay()<br><br>setBonus(<span class="hljs-type">double</span>) -&gt; Manager.setBonus(<span class="hljs-type">double</span>)<br></code></pre></td></tr></table></figure><p>在运行时，调用e.getSalary()的解析过程为:</p><ol><li>首先，虚拟机获取e的实际类型的方法表。这可能是Employee、Manager的方法表，也可能是Employee类的其他子类的方法表。</li><li>接下来，虚拟机查找定义了getSalary()签名的类。此时，虚拟机已经知道应该调用哪个方法。</li><li>最后，虚拟机调用这个方法。</li></ol><p>动态绑定有一个非常重要的特性:无须修改现有的代码就可以对程序进行<strong>扩展</strong>。假设增加一个新类Executive，并且变量e有可能引用这个类的对象，我们不需要对包含调用e.getSalary()的代码重新进行编译。如果e恰好引用一个Executive类的对象，就会自动地调用Executive.getSalary()方法。</p><p>警告:在覆盖一个方法的时候，子类方法<strong>不能低于</strong>超类方法的<strong>可见性</strong>。具体地，如果不小心遗漏了public修饰符。此时，编译器就会报错，指出你试图提供更严格的访问权限。</p><p>不允许扩展的类称为final类。</p><p>如果在类定义中使用了final修饰符，就表明这个类是fianl类，final类中的方法自动地称为final方法。可以将类中的某个特定方法声明为fianl，如果这样做，那么所有子类都不能覆盖这个方法。字段也可以声明为final，对于final字段来说，构造对象之后就不允许改变了。不过将一个类声明为final，只有其中的方法自动地成为final，不包括字段。</p><p>将方法或类声明为final只有一个原因：确保他们不会在子类中改变语义。</p><p>如果一个方法没有被覆盖并且很短，编译器就能够对它进行优化处理，则这个过程称为<strong>内联</strong>（inlining）。</p><p>枚举和记录总是final，它们不允许扩展。</p><p>要完成对象引用的强制类型转换，转换语法与数值表达式的强制类型转换类似。用一对圆括号将目标类名括起来，并放置在需要转换的对象引用之前。例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">var</span> <span class="hljs-variable">staff</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Employee</span>[<span class="hljs-number">3</span>];<br>staff[<span class="hljs-number">0</span>] = <span class="hljs-keyword">new</span> <span class="hljs-title class_">Manager</span>(<span class="hljs-string">&quot;...&quot;</span>);<br>staff[<span class="hljs-number">1</span>] = <span class="hljs-keyword">new</span> <span class="hljs-title class_">Employee</span>(<span class="hljs-string">&quot;.....&quot;</span>);<br><span class="hljs-type">Manager</span> <span class="hljs-variable">boss</span> <span class="hljs-operator">=</span> (Manager) staff[<span class="hljs-number">0</span>];<br><span class="hljs-type">Manager</span> <span class="hljs-variable">boss</span> <span class="hljs-operator">=</span> (Manager) staff[<span class="hljs-number">1</span>];  <span class="hljs-comment">//error</span><br></code></pre></td></tr></table></figure><p>进行强制类型转换的唯一原因是：要在暂时忘记对象的实际类型之后使用对象的全部功能。 （编译器只认编译时类型，无法访问子类的特有方法）</p><p>instanceof操作符，判断是否能够成功进行强制类型转换。(如果x为null  那么  x  instanceof C 不会产生异常，只是返回false。因为null没有引用任何对象，当然也不会引用C类型的对象)        例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">if</span> (staff[i] <span class="hljs-keyword">instanceof</span> Manager)<br>&#123;<br>boss = (Manager) staff[i];<br>...<br>&#125;<br></code></pre></td></tr></table></figure><p>综上所述：</p><p>只能在继承层次结构内进行强制类型转换。</p><p>在将超类强制转换成子类之前，应该使用instanceof进行检查。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">if</span> (staff[i] <span class="hljs-keyword">instanceof</span> Manager)<br>&#123;<br>(Manager) boss = (Manager) staff[i];<br>boss.setBouns(<span class="hljs-number">5000</span>);<br>&#125;<br></code></pre></td></tr></table></figure><p>在java16中可以简化，可以直接在instanceof测试中声明子类变量：（在instanceof判读的同时直接完成类型转换，并声明一个对应的子类变量）</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">if</span> (staff[i] <span class="hljs-keyword">instanceof</span> Manager boss)<br>&#123;<br>boss.setBouns(<span class="hljs-number">5000</span>);<br>&#125;<br></code></pre></td></tr></table></figure><p>当instanceof模式引入一个变量时，可以立即在同一个表达式中使用这个变量（前提是instanceof结果为true）</p><p>声明变量的instanceof形式称为“模式匹配”，这是因为它类似于switch中的类型模式，这是java17的一个“预览”特性。</p><p>类似于其他局部变量，instanceof模式定义的局部变量会遮蔽字段。</p><p>可以将一个类特性（方法或字段）声明为受保护（protected）</p><p>在Java中，受保护字段只能由同一个包中的类访问。</p><p>1.仅本类可以访问——private</p><p>2.可由外部访问——public</p><p>3.本包和所有子类可以访问——protected</p><p>4.本包中可以访问——默认，不需要修饰符</p><h3 id="Object类：所有类的超类"><a href="#Object类：所有类的超类" class="headerlink" title="Object类：所有类的超类"></a>Object类：所有类的超类</h3><p>Java中的每一个类都扩展了Object，如果没有明确地指出超类，那么理所当然Object就是这个类的超类。</p><p>可以使用Object类型的变量引用任何类型的对象，当然，Object类型的变量只能用于作为任意值的一个泛型容器。</p><p>在Java中，只有<strong>基本类型</strong>（primitive type）不是对象，例如，数值、字符和布尔类型的值都不是对象，</p><p>所有的数组类型（不管是对象数组还是基本类型的数组）都扩展了Object类的类类型</p><p>Object类中的equals方法用于检测一个对象是否等于另外一个对象。Object类中实现的equals方法将确定两个对象引用是否相同。（即如果两个对象相同，则这两个对象肯定就相等）不过，经常需要基于状态检测对象的相等性。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">return</span> name.equals(name, other.name)<br>&amp;&amp; salary == other.salary<br>&amp;&amp; hireDay.equals(other.hireDay);<br></code></pre></td></tr></table></figure><p>为了防止name或hireDay可能为null的情况，需要使用Objects.equals方法。如果两个参数都为null，Objects.equals(a,b)调用将返回true；如果其中一个参数为null，则返回false；如果两个参数都不为null，则调用a.equals(b)。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">return</span> Objects.equals(name, other.name)<br>&amp;&amp; salary == other.salary<br>&amp;&amp; Objects.equals(hireDay, other.hireDay);<br></code></pre></td></tr></table></figure><p>在子类中定义equals方法时，首先调用超类的equals。若检测失败，那么对象就不可能相等。如果超类中的字段都相等，则可以继续比较子类中的实例字段。</p><p>注释：记录是一种特殊形式的不可变类，其状态完全由“标准”构造器中设置的字段来定义。记录会自动定义一个比较字段的equals方法。两个记录实例中相应字段值相等时，这两个记录实例就相等。</p><h4 id="Java语言规范要求equals方法具有下述性质。"><a href="#Java语言规范要求equals方法具有下述性质。" class="headerlink" title="Java语言规范要求equals方法具有下述性质。"></a>Java语言规范要求equals方法具有下述性质。</h4><p>1.自反性：对于任何非null引用x，x.equals(x)应该返回true。</p><p>2.对称性：对于任何引用x和y，当且仅当y.equals(x)返回true时，x.equals(y)返回true。</p><p>3.传递性：对于任何引用x、y和z，如果x.equals(y)返回true，y.equals(z)返回true，则x.equals(z)也应该返回true。</p><p>4.一致性：如果x和y引用的对象没有发生变化，则反复调用x.equals(y)应该返回同样的结果。</p><p>5.对于任意非null引用x，x.equals(null)应该返回false。</p><h4 id="下面给出编写完美-equals-方法的技巧："><a href="#下面给出编写完美-equals-方法的技巧：" class="headerlink" title="下面给出编写完美 equals 方法的技巧："></a>下面给出编写完美 equals 方法的技巧：</h4><ol><li><p>将显式参数命名为 otherObject，稍后需要将它强制转换成另一个名为 other 的变量。</p></li><li><p>检测 this 与 otherObject 是否相同：</p> <figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">if</span> (<span class="hljs-built_in">this</span> == otherObject) <span class="hljs-keyword">return</span> <span class="hljs-literal">true</span>;<br></code></pre></td></tr></table></figure><p> 这条语句只是一个优化。实际上，这种情况很常见，因为检查同一性要比逐个比较字段开销小。</p></li><li><p>检测 otherObject 是否为 null，如果为 null，则返回 false。这个检测是必要的。</p> <figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">if</span> (otherObject == <span class="hljs-literal">null</span>) <span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;<br></code></pre></td></tr></table></figure></li><li><p>比较 this 与 otherObject 的类。如果 equals 的语义可以在子类中改变，就使用 getClass 检测：</p></li></ol><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">if</span> (getClass() != otherObject.getClass()) <span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;<br><br><span class="hljs-type">ClassName</span> <span class="hljs-variable">other</span> <span class="hljs-operator">=</span> (ClassName) otherObject;<br></code></pre></td></tr></table></figure><p>如果所有的子类都有相同的相等性语义，则可以使用 instanceof 检测：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">if</span> (!(otherObject <span class="hljs-keyword">instanceof</span> ClassName other)) <span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;<br></code></pre></td></tr></table></figure><p>注意，如果 instanceof 检测成功，它会把 other 设置为 otherObject。不再需要强制类型转换。</p><p>5.现在根据相等性概念的要求来比较字段。使用 &#x3D;&#x3D; 比较基本类型字段，使用 Objects.equals 比较对象字段。如果所有的字段都匹配，就返回 true；否则，返回 false。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">return</span> field1 == other.field1<br><br>&amp;&amp; Objects.equals(field2, other.field2)<br><br>&amp;&amp; ... ;<br></code></pre></td></tr></table></figure><p>如果在子类中重新定义 equals，就要在其中包含一个 super.equals(other) 调用。</p><p>提示：对于数组类型的字段，可以使用静态的Arrays.equals方法检查相应的数组元素是否相等。对于多维数组，可以使用Arrays.deepEquals方法。</p><p>可以使用 @Override 标记要覆盖超类方法的那些子类方法，如果犯了错误，没有覆盖方法而是在定义一个新方法，编译器就会报告一个错误。</p><p>散列码（hash code）是由对象导出的一个整型值。散列码是没有规律的。如果x和y是两个不同的对象，那么 x.hashCode()与y.hashCode()基本上不会相同。</p><p>由于hashCode方法定义在 Object 类中，因此每个对象都有一个默认的散列码，其值由对象的存储地址得出。</p><p>字符串的散列码是由<strong>内容</strong>导出的。</p><p>String 类使用以下算法计算散列码：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-built_in">int</span> <span class="hljs-built_in">hash</span> = <span class="hljs-number">0</span>;<br><span class="hljs-keyword">for</span> (<span class="hljs-built_in">int</span> i = <span class="hljs-number">0</span> ; i&lt; length(); i++)<br><span class="hljs-built_in">hash</span> = <span class="hljs-number">31</span> * <span class="hljs-built_in">hash</span> + charAt(i);<br></code></pre></td></tr></table></figure><p>hashCode方法应该返回一个整数（可以是负数）</p><p>equals 与 hashCode 的定义必须相容：如果 x.equals(y) 返回 true，那么 x.hashCode() 就必须返回与 y.hashCode() 相同的值，</p><p>如果有数组类型的字段，那么可以使用静态的 Arrays.hashCode 方法计算一个散列码，这个散列码由数组元素的散列码组成。</p><p>record 类型会自动提供一个 hashCode 方法，它会由字段值的散列码得出一个散列码。</p><p>Object中的 toString 方法，它会返回一个字符串，表示这个对象的值。</p><p>绝大多数（非全部）toString 方法都遵循这样的格式：首先是类名，随后是一对方括号括起来的字段值。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> String <span class="hljs-title function_">toString</span><span class="hljs-params">()</span><br>&#123;<br><span class="hljs-keyword">return</span> getClass().getName() + <span class="hljs-string">&quot;[name=&quot;</span> + name + <span class="hljs-string">&quot;, salary=&quot;</span> + salary + <span class="hljs-string">&quot;,hireDay= &quot;</span> + hireDay + <span class="hljs-string">&quot;]&quot;</span>;<br>&#125;<br></code></pre></td></tr></table></figure><p>只要对象与一个字符串通过操作符“+”拼接起来，Java编译器就会自动地调用toStirng 方法来获得这个对象的字符串描述。</p><p>提示：可以不写为 <code>x.toString()</code>，而写作 <code>&quot;&quot;+x</code>。这条语句将一个空串与 <code>x</code>的字符串表示（也就是 <code>x.toString()</code>）拼接起来。与 <code>toString</code>不同是，即使 <code>x</code>是基本类型，这条语句也能正常工作。</p><p>如果 <code>x</code>是一个任意对象，并调用</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">System.out.println(x);<br></code></pre></td></tr></table></figure><p><code>println</code>方法就会简单地调用 <code>x.toString()</code>，并打印得到的字符串。</p><p><code>Object</code>类定义了 <code>toString</code>方法，会打印对象的<strong>类名</strong>和<strong>散列码</strong>。例如，调用</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">System.out.println(System.out)<br></code></pre></td></tr></table></figure><p>将生成以下输出：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">java.io.PrintStream@2f6684<br></code></pre></td></tr></table></figure><p>之所以得到这样的结果，是因为 <code>PrintStream</code>类的实现者没有覆盖 <code>toString</code>方法。</p><p>警告：令人烦恼的是，数组继承了 <code>Object</code>类的 <code>toString</code>方法，更有甚者，数组类型将采用一种古老的格式打印。例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">int</span>[] luckyNumbers = &#123; <span class="hljs-number">2</span>, <span class="hljs-number">3</span>, <span class="hljs-number">5</span>, <span class="hljs-number">7</span>, <span class="hljs-number">11</span>, <span class="hljs-number">13</span> &#125;;<br><span class="hljs-type">String</span> <span class="hljs-variable">s</span> <span class="hljs-operator">=</span> <span class="hljs-string">&quot;&quot;</span> + luckyNumbers;<br></code></pre></td></tr></table></figure><p>会生成字符串 <code>“[I@1a46e30”</code>（前缀 <code>[I</code>表示这是一个整型数组）。补救方法是调用静态方法<code>Arrays.toString</code>。以下代码：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">String</span> <span class="hljs-variable">s</span> <span class="hljs-operator">=</span> Arrays.toString(luckyNumbers);<br></code></pre></td></tr></table></figure><p>将生成字符串<code>&quot;[2,3,5,7,11,13]&quot;</code>。</p><p>要想正确地打印多维数组（即数组的数组），则需要调用<code>Arrays.deepToString</code>方法。</p><p>toString 方法是一种非常有用的调试工具。在标准类库中，许多类都定义了 toString 方法，以便用户能够获得有关对象状态的有用信息。这在记录日志消息时尤其有用，</p><figure class="highlight stylus"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs stylus">System<span class="hljs-selector-class">.out</span><span class="hljs-selector-class">.println</span>(<span class="hljs-string">&quot;Current position = &quot;</span> + <span class="hljs-attribute">position</span>);<br></code></pre></td></tr></table></figure><p><strong>提示</strong>：强烈建议为你自定义的每一个类添加<code>toString</code>方法。</p><h3 id="5-3-泛型数组列表"><a href="#5-3-泛型数组列表" class="headerlink" title="5.3 泛型数组列表"></a>5.3 泛型数组列表</h3><p>在一些程序设计语言（如 C 或 C++）中，必须在编译时就确定所有数组的大小。程序员对此十分反感，因为这样做将迫使他们做出一些不情愿的折中。例如，一个部门会有多少员工？肯定不会超过 100 人。一旦出现一个拥有 150 名员工的大型部门呢？另外，你愿意为那些仅有 10 名员工的部门浪费 90 个存储空间吗？</p><p>在 Java 中，情况就好多了。<strong>它允许在运行时确定数组的大小。</strong></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">int</span> <span class="hljs-variable">actualSize</span> <span class="hljs-operator">=</span> . . .;<br><span class="hljs-type">var</span> <span class="hljs-variable">staff</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Employee</span>[actualSize];<br></code></pre></td></tr></table></figure><p>当然，这个代码并没有完全解决运行时动态修改数组的问题。一旦确定了数组的大小，就无法再轻松地改变了。在 Java 中，要处理这个常见的情况，可以使用 Java 中的另外一个类，名为 <code>ArrayList</code>。ArrayList 类与数组类似，但在添加或删除元素时，它能够自动地调整容量，而不需要为此额外编写代码。</p><p>ArrayList 是一个<strong>有类型参数（type parameter）的泛型类（generic class）</strong>。<strong>为了指定数组列表保存的元素对象的类型，需要用一对尖括号将类名括起来追加到 ArrayList 后面</strong>，例如 ArrayList<Employee>。</p><h4 id="5-3-1-声明数组列表"><a href="#5-3-1-声明数组列表" class="headerlink" title="5.3.1 声明数组列表"></a>5.3.1 声明数组列表</h4><p>可以如下声明和构造一个保存 Employee 对象的数组列表：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">ArrayList&lt;Employee&gt; staff = <span class="hljs-keyword">new</span> <span class="hljs-title class_">ArrayList</span>&lt;Employee&gt;();<br></code></pre></td></tr></table></figure><p>在 Java 10 中，最好使用 var 关键字以避免重复写类名：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">var</span> <span class="hljs-variable">staff</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">ArrayList</span>&lt;Employee&gt;();<br></code></pre></td></tr></table></figure><p><strong>如果没有使用 var 关键字，则可以省略右边的类型参数</strong>：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">ArrayList&lt;Employee&gt; staff = <span class="hljs-keyword">new</span> <span class="hljs-title class_">ArrayList</span>&lt;&gt;();<br></code></pre></td></tr></table></figure><p>这称为 “ 菱形 “ 语法，因为它尖括号 &lt;&gt; 就像是一个菱形。可以结合 new 操作符使用菱形语法。编译器会检查新值要做什么。如果赋值给一个变量，或传递给某个方法，或者从某个方法返回，编译器会检查这个变量、参数或方法的泛型类型，然后将这个类型放在 &lt;&gt; 中。在这个例子中，new ArrayList&lt;&gt;( ) 将赋值给一个类型为 ArrayList<Employee> 的变量，所以泛型类型为 Employee。</p><p><strong>警告：</strong> **如果使用 var 声明 ArrayList，就不要使用菱形语法。**以下声明</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">var</span> <span class="hljs-variable">elements</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">ArrayList</span>&lt;&gt;();<br></code></pre></td></tr></table></figure><p>会生成一个 ArrayList<Object>。</p><p><strong>注释：</strong> Java 5 以前的版本没有提供泛型类，而是有一个保存 Object 类型元素的 ArrayList 类，它是一个”自适应大小”（one-size-fits-all）的集合。你仍然可以使用没有后缀 &lt;…&gt; 的 ArrayList。它被认为是一个擦除了类型参数的”原始”类型。</p><p><strong>注释：</strong> 在更老的 Java 版本中，程序员使用 Vector 类实现动态数组。不过，ArrayList 类更加高效，没有任何理由再使用 Vector 类。</p><p>**使用 add 方法可以将元素添加到数组列表中。**例如，下面展示了如何将 Employee 对象添加到一个数组列表中：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java">staff.add(<span class="hljs-keyword">new</span> <span class="hljs-title class_">Employee</span>(<span class="hljs-string">&quot;Harry Hacker&quot;</span>, ...));<br>staff.add(<span class="hljs-keyword">new</span> <span class="hljs-title class_">Employee</span>(<span class="hljs-string">&quot;Tony Tester&quot;</span>, ...));<br></code></pre></td></tr></table></figure><p>数组列表管理着一个内部的对象引用数组。最终，这个数组的空间有可能会部分用尽。这时就显现出数组列表的魅力了：<strong>如果调用 <code>add</code> 而内部数组已经满了，数组列表就会自动地创建一个更大的数组，并将所有对象从较小的数组拷贝到较大的数组中</strong>。</p><p>如果已经知道或能够估计出数组可能存储的元素数量，就可以在填充数组之前调用 <code>ensureCapacity</code> 方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">staff.ensureCapacity(<span class="hljs-number">100</span>);<br></code></pre></td></tr></table></figure><p>这个方法调用将分配一个包含 100 个对象的内部数组。这样一来，前 100 次 <code>add</code> 调用不会带来开销很大的重新分配空间。</p><p>另外，还可以把初始容量传递给 <code>ArrayList</code> 构造器：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">ArrayList&lt;Employee&gt; staff = <span class="hljs-keyword">new</span> <span class="hljs-title class_">ArrayList</span>&lt;&gt;(<span class="hljs-number">100</span>);<br></code></pre></td></tr></table></figure><p>警告：如下分配数组列表：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">new</span> <span class="hljs-title class_">ArrayList</span>&lt;&gt;(<span class="hljs-number">100</span>) <span class="hljs-comment">// capacity is 100</span><br></code></pre></td></tr></table></figure><p>这与分配一个新数组有所不同：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">new</span> <span class="hljs-title class_">Employee</span>[<span class="hljs-number">100</span>] <span class="hljs-comment">// size is 100</span><br></code></pre></td></tr></table></figure><p>数组列表的容量与数组的大小有一个非常重要的区别：</p><ul><li>如果分配一个有 100 个元素的数组，数组就有 100 个空位置（槽）可以使用。</li><li>而容量为 100 个元素的数组列表只是可能保存 100 个元素（实际上也可以超过 100，不过要以重新分配空间为代价），但是在一开始，甚至完成初始化构造之后，数组列表并不包含任何元素。</li></ul><p><code>size</code> 方法将返回数组列表中包含的实际元素个数。例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">staff.size()<br></code></pre></td></tr></table></figure><p>将返回 <code>staff</code> 数组列表的当前元素个数，它等价于数组 <code>a</code> 的 <code>a.length</code>。</p><p>一旦能够确认数组列表的大小将保持恒定，不再发生变化，就可以调用 <code>trimToSize</code> 方法。<strong>这个方法将内存块的大小调整为保存当前元素数量所需要的存储空间</strong>。垃圾回收器将回收多余的存储空间。</p><p>**注意：**一旦削减了数组列表的大小，添加新元素就需要再次移动内存块，这很耗费时间，所以应当只有在确认不会再向数组列表添加任何元素时才调用 <code>trimToSize</code>。</p><h4 id="5-3-2-访问数组列表元素"><a href="#5-3-2-访问数组列表元素" class="headerlink" title="5.3.2 访问数组列表元素"></a>5.3.2 访问数组列表元素</h4><p>为了提供数组列表自动扩展容量的便利，这要求使用一种更复杂的语法来访问元素。其原因是 <code>ArrayList</code> 类并不是 Java 程序设计语言的一部分，它只是由某个人编写并放在标准库中的一个实用工具类。</p><p>不能使用我们喜爱的 <code>[]</code> 语法格式访问或改变数组的元素，而要使用 <code>get</code> 和 <code>set</code> 方法。</p><p>例如，要设置第 i 个元素，可以使用：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">staff.set(i, harry);<br></code></pre></td></tr></table></figure><p>它等价于对数组 a 的元素赋值（与数组一样，索引值从 0 开始）：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">a[i] = harry;<br></code></pre></td></tr></table></figure><p>警告：只有当数组列表的大小大于 i 时，才能够调用 <code>list.set(i, x)</code>。例如，下面这段代码是错误的：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">var</span> <span class="hljs-variable">list</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">ArrayList</span>&lt;Employee&gt;(<span class="hljs-number">100</span>); <span class="hljs-comment">// capacity 100, size 0</span><br>list.set(<span class="hljs-number">0</span>, x); <span class="hljs-comment">// no element 0 yet</span><br></code></pre></td></tr></table></figure><p><strong>要使用 <code>add</code> 方法为数组添加新元素，而不是 <code>set</code> 方法。<code>set</code> 方法只是用来替换数组中之前增加的一个元素。</strong></p><p>要得到一个数组列表的元素，可以使用：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">Employee</span> <span class="hljs-variable">e</span> <span class="hljs-operator">=</span> staff.get(i);<br></code></pre></td></tr></table></figure><p>这等价于：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">Employee</span> <span class="hljs-variable">e</span> <span class="hljs-operator">=</span> a[i];<br></code></pre></td></tr></table></figure><p>**注释：**没有泛型类时，原始 <code>ArrayList</code> 类提供的 <code>get</code> 方法别无选择，只能返回 <code>Object</code>。因此，<code>get</code> 方法的调用者必须将返回值强制转换为所需的类型。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">Employee</span> <span class="hljs-variable">e</span> <span class="hljs-operator">=</span> (Employee) staff.get(i);<br></code></pre></td></tr></table></figure><p>原始 <code>ArrayList</code> 还存在一定的危险性。它的 <code>add</code> 和 <code>set</code> 方法接受任意类型的对象。对于下面这个调用：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">staff.set(i, <span class="hljs-string">&quot;Harry Hacker&quot;</span>);<br></code></pre></td></tr></table></figure><p>它能正常编译而不会给出任何警告，只有在获取对象并试图对它进行强制类型转换时，才会发现有问题。如果使用 <code>ArrayList&lt;Employee&gt;</code>，编译器就会检测到这个错误。</p><p>下面这个技巧可以一举两得，既可以灵活地扩展数组，又可以方便地访问数组元素。首先，创建一个数组列表，并添加所有的元素：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">var</span> <span class="hljs-variable">list</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">ArrayList</span>&lt;X&gt;();<br><span class="hljs-keyword">while</span> (...)<br>&#123;<br>    x = ...;<br>    list.add(x);<br>&#125;<br></code></pre></td></tr></table></figure><p>执行完上述操作后，使用 <code>toArray</code> 方法将数组元素复制到一个数组中：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">var</span> <span class="hljs-variable">a</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">X</span>[list.size()];<br>list.toArray(a);<br></code></pre></td></tr></table></figure><p>有时需要在数组列表的中间增加元素，为此可以使用 <code>add</code> 方法并提供一个索引参数：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">int</span> <span class="hljs-variable">n</span> <span class="hljs-operator">=</span> staff.size() / <span class="hljs-number">2</span>;<br>staff.add(n, e);<br></code></pre></td></tr></table></figure><p>位置 n 及之后的所有元素都要向后移动一个位置，为新元素留出空间。插入新元素后，如果数组列表的新大小超过了容量，数组列表就会重新分配它的存储数组。</p><p>类似地，可以从数组列表中间删除一个元素：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">Employee</span> <span class="hljs-variable">e</span> <span class="hljs-operator">=</span> staff.remove(n);<br></code></pre></td></tr></table></figure><p>位于这个位置之后的所有元素都向前移动一个位置，并且数组的大小减 1。</p><p>插入和删除元素的效率很低。对于较小的数组列表来说，不必担心这个问题。但如果存储的元素很多，又经常需要在中间插入和删除元素，就应该考虑使用链表了。</p><p>可以使用 “ for each “ 循环遍历数组列表的内容：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">for</span> (Employee e : staff)<br>    <span class="hljs-keyword">do</span> something with e<br></code></pre></td></tr></table></figure><p>这个循环和以下代码具有相同的效果：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>; i &lt; staff.size(); i++)<br>&#123;<br>    <span class="hljs-type">Employee</span> <span class="hljs-variable">e</span> <span class="hljs-operator">=</span> staff.get(i);<br>    <span class="hljs-keyword">do</span> something with e<br>&#125;<br></code></pre></td></tr></table></figure><h4 id="5-3-3-类型化与原始数组列表的兼容性"><a href="#5-3-3-类型化与原始数组列表的兼容性" class="headerlink" title="5.3.3 类型化与原始数组列表的兼容性"></a>5.3.3 类型化与原始数组列表的兼容性</h4><p>在你自己的代码中，你可能总是想用类型参数来增加安全性。在本节中，你会了解如何与遗留代码（没有使用类型参数）互操作。</p><p>假设有以下遗留类：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">EmployeeDB</span><br>&#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">update</span><span class="hljs-params">(ArrayList list)</span> &#123; . . . &#125;<br>    <span class="hljs-keyword">public</span> ArrayList <span class="hljs-title function_">find</span><span class="hljs-params">(String query)</span> &#123; . . . &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>可以将一个类型化的数组列表传递给 update 方法，但并不需要进行任何强制类型转换。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java">ArrayList&lt;Employee&gt; staff = . . .;<br>employeeDB.update(staff);<br></code></pre></td></tr></table></figure><p>staff 对象直接传递到 update 方法。</p><p>**警告：**尽管编译器没有给出任何错误信息或警告，但是这样调用并不太安全。update 方法可能会在数组列表中增加不是 Employee 类型的元素。访问这些元素时就会出现异常。听起来似乎很吓人，但考虑一下就会发现，这种行为与 Java 中引入泛型之前是一样的。虚拟机的完整性并没有受到威胁。在这种情况下，没有降低安全性，但也没能从编译时检查中受益。</p><p>相反，将一个原始 ArrayList 赋给一个类型化 ArrayList 时，会得到一个警告。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">ArrayList&lt;Employee&gt; result = employeeDB.find(query); <span class="hljs-comment">// yields warning</span><br></code></pre></td></tr></table></figure><p>注释：为了能够看到警告的文本信息，编译时要提供选项 <code>-Xlint:unchecked</code>。</p><p>使用强制类型转换并不能避免出现警告。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java">ArrayList&lt;Employee&gt; result = (ArrayList&lt;Employee&gt;) employeeDB.find(query);<br><span class="hljs-comment">// yields another warning</span><br></code></pre></td></tr></table></figure><p>这样将会得到另外一个警告信息，指出类型转换有误。</p><p>这就是 Java 中不尽如人意的泛型类型限制所带来的结果。出于兼容性的考虑，编译器检查到没有发现违反规则的现象之后，就将所有类型化数组列表转换成原始 ArrayList 对象。在程序运行时，所有的数组列表都是一样的，即虚拟机中没有类型参数。因此，强制类型转换 <code>(ArrayList)</code> 和 <code>(ArrayList&lt;Employee&gt;)</code> 将执行相同的运行时检查。</p><p>在这种情况下，你并不能做什么。在与遗留的代码交互时，要研究编译器的警告，确保这些警告不太严重。</p><p>一旦确保问题不太严重，可以用 <code>@SuppressWarnings(&quot;unchecked&quot;)</code> 注解来标记接受强制类型转换的变量，如下所示：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-meta">@SuppressWarnings(&quot;unchecked&quot;)</span> ArrayList&lt;Employee&gt; result<br>= (ArrayList&lt;Employee&gt;) employeeDB.find(query); <span class="hljs-comment">// yields another warning</span><br></code></pre></td></tr></table></figure><h3 id="5-4-对象包装器与自动装箱"><a href="#5-4-对象包装器与自动装箱" class="headerlink" title="5.4 对象包装器与自动装箱"></a>5.4 对象包装器与自动装箱</h3><p>有时，需要将 int 这样的基本类型转换为对象。所有的基本类型都有一个与之对应的类。例如，Integer 类对应基本类型 int。通常，这些类称为<strong>包装器（wrapper）</strong>。这些包装器类有显而易见的名字：<code>Integer、Long、Float、Double、Short、Byte、Character 和 Boolean</code>（前 6 个类派生于公共超类 Number）。<strong>包装器类是不可变的，即一旦构造了包装器，就不允许更改包装在其中的值。同时，包装器类还是 final，因此不能派生它们的子类。</strong></p><p>假设想要定义一个整型数组列表。遗憾的是，<strong>尖括号中的类型参数不允许是基本类型</strong>，也就是说，不允许写成 <code>ArrayList&lt;int&gt;</code>。这里就可以用到 Integer 包装器类。我们可以声明一个 Integer 对象的数组列表。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">var</span> <span class="hljs-variable">list</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">ArrayList</span>&lt;Integer&gt;();<br></code></pre></td></tr></table></figure><p>**警告：**由于每个值分别包装在一个对象中，所以 ArrayList<Integer> 的效率远远低于 int[ ] 数组。因此，只有当程序员操作的方便性比执行效率更重要的时候，才会考虑对较小的集合使用这种构造。</p><p>幸运的是，有一个很有用的特性，从而可以很容易地向 ArrayList<Integer> 添加 int 类型的元素。下面这个调用</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">list.add(<span class="hljs-number">3</span>);<br></code></pre></td></tr></table></figure><p>将自动地转换成</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">list.add(Integer.valueOf(<span class="hljs-number">3</span>));<br></code></pre></td></tr></table></figure><p>这种转换称为<strong>自动装箱（autoboxing）</strong>。</p><p>**注释：**你可能认为自动包装（autowrapping）与包装器更一致，不过 “ 装箱 ”（boxing）这个词源于 C#。</p><p>反过来，当将一个 Integer 对象赋给一个 int 值时，将会<strong>自动拆箱（unboxed）</strong>。也就是说，编译器将以下语句</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">int</span> <span class="hljs-variable">n</span> <span class="hljs-operator">=</span> list.get(i);<br></code></pre></td></tr></table></figure><p>转换成</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">int</span> <span class="hljs-variable">n</span> <span class="hljs-operator">=</span> list.get(i).intValue();<br></code></pre></td></tr></table></figure><p>自动装箱和自动拆箱甚至也适用于算术表达式。例如，可以将自增运算符应用于一个包装器引用：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">Integer</span> <span class="hljs-variable">n</span> <span class="hljs-operator">=</span> <span class="hljs-number">3</span>;<br>n++;<br></code></pre></td></tr></table></figure><p>编译器将自动地插入指令对对象拆箱，然后将结果值增 1，最后再将其装箱。</p><p>大多数情况下容易有一种假象，认为基本类型与它们的对象包装器是一样的。但它们有一点有很大不同：同一性。大家知道，运算符可以应用于包装器对象，不过检测的是对象是否有相同的内存位置，因此，下面的比较可能会失败：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">Integer</span> <span class="hljs-variable">a</span> <span class="hljs-operator">=</span> <span class="hljs-number">1000</span>;<br><span class="hljs-type">Integer</span> <span class="hljs-variable">b</span> <span class="hljs-operator">=</span> <span class="hljs-number">1000</span>;<br><span class="hljs-keyword">if</span> (a == b) . . .<span class="hljs-comment">//比较的是对象引用（内存地址）</span><br></code></pre></td></tr></table></figure><p>不过，Java 实现可以（如果选择这么做）将经常出现的值包装到相同的对象中，这样一来，以上比较就可能成功。但这种不确定性并不是我们想要的。解决这个问题的办法是在比较两个包装器对象时调用 equals 方法。</p><p>**注释：**自动装箱规范要求 boolean、byte、char (≤127)，介于 -128 和 127 之间的 short 和 int 包装到固定的对象中。例如，在前面的例子中，如果将 a 和 b 初始化为 100，那么它们的比较结果一定会成功。</p><ul><li><code>boolean</code>：全部缓存</li><li><code>byte</code>：全部缓存</li><li><code>char</code>：≤ 127</li><li><code>short</code>、<code>int</code>：-128 到 127</li></ul><p>**提示：**绝对不要依赖包装器对象的同一性。不要用 &#x3D;&#x3D; 比较包装器对象，也不要将包装器对象作为锁。</p><p>不要使用包装器类构造器，它们已被弃用，并将被完全删除。例如，可以使用 Integer.valueOf(1000)，而绝对不要使用 <code>new Integer(1000)</code>。或者，可以依赖自动装箱：<code>Integer a=1000</code>。</p><p>关于自动装箱还有几点需要说明。首先，由于包装器类引用可以为 null，所以自动装箱有可能会抛出一个 NullPointerException 异常：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">Integer</span> <span class="hljs-variable">n</span> <span class="hljs-operator">=</span> <span class="hljs-literal">null</span>;<br>System.out.println(<span class="hljs-number">2</span> * n); <span class="hljs-comment">// throws NullPointerException</span><br></code></pre></td></tr></table></figure><p>另外，如果在一个条件表达式中混合使用 Integer 和 Double 类型，则 Integer 值就会拆箱，提升为 double，再装箱为 Double：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">Integer</span> <span class="hljs-variable">n</span> <span class="hljs-operator">=</span> <span class="hljs-number">1</span>;<br><span class="hljs-type">Double</span> <span class="hljs-variable">x</span> <span class="hljs-operator">=</span> <span class="hljs-number">2.0</span>;<br>System.out.println(<span class="hljs-literal">true</span> ? n : x); <span class="hljs-comment">// prints 1.0</span><br></code></pre></td></tr></table></figure><p>最后强调一下，<strong>装箱和拆箱是编译器要做的工作，而不是虚拟机</strong>。编译器生成类的字节码时会插入必要的方法调用。虚拟机只是执行这些字节码。</p><p>**注释：**Java 将来的版本可能允许类似基本类型的用户自定义类型，其值并不存储在对象中。例如，基本类型 Point 的值（包含 double 字段 x 和 y）只是内存中一个 16 字节的块，并且有两个相邻的 double 值。可以复制这个值，但不能有它的引用。</p><p>如果需要一个引用，可以使用一个自动生成的伴随类（在当前提案中，这个类名为 Point.ref）。装箱和拆箱是自动的，这与当前的基本类型相同。</p><p>将来某个时候，基本包装器类将与那些类统一起来。例如，Double 将是 double.ref 的一个别名。</p><p>使用数值包装器通常还有一个原因。Java 设计者发现，可以将某些基本方法放在包装器中，这会很方便，例如将一个数字字符串转换成数值。</p><p>要想将字符串转换成整型，可以使用下面这条语句：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">int</span> <span class="hljs-variable">x</span> <span class="hljs-operator">=</span> Integer.parseInt(s);<br></code></pre></td></tr></table></figure><p>这里与 Integer 对象没有任何关系，parseInt 是一个静态方法。但 Integer 类是放置这个方法的一个好地方。</p><p><strong>警告：<strong>有些人认为包装器类可以用来实现能够修改数值参数的方法，不过这是错误的。在第 4 章中曾经讲到，由于 <strong>Java 方法的参数总是按值传递的</strong>，所以</strong>不可能编写一个能够让整型参数自增的 Java 方法</strong>。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">triple</span><span class="hljs-params">(<span class="hljs-type">int</span> x)</span> <span class="hljs-comment">// won&#x27;t work</span><br>&#123;<br>    x = <span class="hljs-number">3</span> * x; <span class="hljs-comment">// modifies local variable</span><br>&#125;<br></code></pre></td></tr></table></figure><p>将 int 替换成 Integer 能解决这个问题吗？</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">triple</span><span class="hljs-params">(Integer x)</span> <span class="hljs-comment">// won&#x27;t work</span><br>&#123;<br>    ...<br>&#125;<br></code></pre></td></tr></table></figure><p>问题在于 <strong>Integer 对象是不可变的：包含在包装器中的信息不会改变</strong>。所以，不能使用这些包装器类来创建修改数值参数的方法。</p><h3 id="5-5-参数个数可变的方法"><a href="#5-5-参数个数可变的方法" class="headerlink" title="5.5 参数个数可变的方法"></a>5.5 参数个数可变的方法</h3><p>可以提供参数个数可变的方法（有时，这些方法被称为 “ 变参 ” (varargs)方法）。前面已经看到过这样一个方法：<code>printf</code> 。例如，下面的方法调用</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">System.out.printf(<span class="hljs-string">&quot;%d&quot;</span>, n);<br></code></pre></td></tr></table></figure><p>和</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">System.out.printf(<span class="hljs-string">&quot;%d %s&quot;</span>, n, <span class="hljs-string">&quot;widgets&quot;</span>);<br></code></pre></td></tr></table></figure><p>这两条语句都调用同一个方法，不过一个调用有2个参数，另一个调用有3个参数。</p><p><code>printf</code> 方法是这样定义的：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">PrintStream</span><br>&#123;<br>    <span class="hljs-keyword">public</span> PrintStream <span class="hljs-title function_">printf</span><span class="hljs-params">(String fmt, Object... args)</span><br>    &#123;<br>        <span class="hljs-keyword">return</span> format(fmt, args);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p><strong>这里的省略号 <code>...</code> 是Java代码的一部分，它表明这个方法可以接收任意数量的对象</strong>（除 fmt 参数以外）。</p><p>实际上，printf 方法接收两个参数，<strong>一个是格式字符串，另一个是 <code>Object[]</code> 数组</strong>，其中保存着所有其他参数（如果调用者提供的是整数或者其他基本类型的值，则会把它们自动装箱为对象）。现在，不可避免地要扫描 fmt 字符串，并将第 i 个格式说明符与 <code>args[i]</code> 的值匹配。</p><p>换句话说，对于 printf 的实现者来说，<code>Object...</code> 参数类型与 <code>Object[]</code> 完全一样。</p><p>编译器需要转换每个 printf 调用，将参数打包到一个数组中，并根据需要自动装箱：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">System.out.printf(<span class="hljs-string">&quot;%d %s&quot;</span>, <span class="hljs-keyword">new</span> <span class="hljs-title class_">Object</span>[] &#123; Integer.valueOf(n), <span class="hljs-string">&quot;widgets&quot;</span> &#125; );<br></code></pre></td></tr></table></figure><p>你自己也可以定义有可变参数的方法，可以为参数指定任意类型，甚至是基本类型。下面是一个简单的示例，这个函数会计算若干个数值中的最大值（数值个数可变）。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-type">double</span> <span class="hljs-title function_">max</span><span class="hljs-params">(<span class="hljs-type">double</span>... values)</span><br>&#123;<br>    <span class="hljs-type">double</span> <span class="hljs-variable">largest</span> <span class="hljs-operator">=</span> Double.NEGATIVE_INFINITY;<br>    <span class="hljs-keyword">for</span> (<span class="hljs-type">double</span> v : values) <span class="hljs-keyword">if</span> (v &gt; largest) largest = v;<br>    <span class="hljs-keyword">return</span> largest;<br>&#125;<br></code></pre></td></tr></table></figure><p>可以像下面这样调用这个函数：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">double</span> <span class="hljs-variable">m</span> <span class="hljs-operator">=</span> max(<span class="hljs-number">3.1</span>, <span class="hljs-number">40.4</span>, -<span class="hljs-number">5</span>);<br></code></pre></td></tr></table></figure><p>编译器将 <code>new double[] &#123;3.1, 40.4, -5&#125;</code> 传递给max函数。</p><p>**注释：**允许将数组作为最后一个参数传递给有可变参数的方法。例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">System.out.printf(<span class="hljs-string">&quot;%d %s&quot;</span>, <span class="hljs-keyword">new</span> <span class="hljs-title class_">Object</span>[] &#123; Integer.valueOf(<span class="hljs-number">1</span>), <span class="hljs-string">&quot;widgets&quot;</span> &#125; );<br></code></pre></td></tr></table></figure><p>因此，如果一个已有函数的最后一个参数是数组，则可以把它重新定义为有可变参数的方法，而不会破坏任何已有的代码。例如，Java 5 中就采用这种方式增强了 <code>MessageFormat.format</code>。如果愿意，甚至可以将 main 方法声明为以下形式：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String... args)</span><br></code></pre></td></tr></table></figure><h3 id="5-6-抽象类"><a href="#5-6-抽象类" class="headerlink" title="5.6 抽象类"></a>5.6 抽象类</h3><p>如果自下而上在类的继承层次结构中上移，那么位于上层的类更具有一般性，也可能更加抽象。从某种角度看，祖先类更有一般性，人们只将它作为派生其他类的基类，而不是用来构造你想使用的特定实例。例如，考虑扩展 Employee 类层次结构。员工是一个人，学生也是一个人。下面扩展我们的类层次结构来加入类 Person 和类 Student。图 5-2 显示了这三个类之间的继承关系。</p><p><img src="/2025/10/20/%E3%80%8AJava%E6%A0%B8%E5%BF%83%E6%8A%80%E6%9C%AF%E5%8D%B7%E4%B8%80%E3%80%8B%E7%AC%AC%E4%BA%94%E7%AB%A0/image-20251104162723467.png" alt="image-20251104162723467"></p><p>为什么要那么麻烦提供这样一个高层次的抽象呢？每个人都有一些属性，如姓名。学生与员工都有姓名，通过引入一个公共的超类，我们就可以把 <code>getName</code> 方法放在继承层次结构中更高的一层。</p><p>现在，再增加一个 <code>getDescription</code> 方法，它可以返回对一个人的简短描述，例如</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java">an employee with a salary of $<span class="hljs-number">50</span>,<span class="hljs-number">000.00</span><br>a student majoring in computer science<br></code></pre></td></tr></table></figure><p>在 Employee 类和 Student 类中实现这个方法很容易。但是在 Person 类中你能提供什么信息呢？除了姓名之外，Person 类对这个人一无所知。当然，可以实现 <code>Person.getDescription()</code> 来返回一个空字符串。不过还有一个更好的方法，如果使用 <code>abstract</code> 关键字，这样就根本不需要实现这个方法了。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">abstract</span> String <span class="hljs-title function_">getDescription</span><span class="hljs-params">()</span>;<br><span class="hljs-comment">// no implementation required    抽象方法只有声明，没有实现（没有方法体）   它强制要求所有子类必须实现这个方法</span><br></code></pre></td></tr></table></figure><p>为了提高程序的清晰性，包含一个或多个抽象方法的类本身必须被声明为抽象的。（如果一个类包含<strong>至少一个抽象方法</strong>，那么这个类本身也必须声明为<strong>抽象类</strong>）</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">abstract</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Person</span><br>&#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">abstract</span> String <span class="hljs-title function_">getDescription</span><span class="hljs-params">()</span>;<br>&#125;<br></code></pre></td></tr></table></figure><p>除了抽象方法之外，抽象类还可以包含字段和具体方法。例如，Person 类还保存着一个人的姓名，另外有一个返回姓名的具体方法。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">abstract</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Person</span><br>&#123;<br>    <span class="hljs-keyword">private</span> String name;<br>    <br>    <span class="hljs-keyword">public</span> <span class="hljs-title function_">Person</span><span class="hljs-params">(String name)</span><br>    &#123;<br>        <span class="hljs-built_in">this</span>.name = name;<br>    &#125;<br>    <br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">abstract</span> String <span class="hljs-title function_">getDescription</span><span class="hljs-params">()</span>;<br>    <br>    <span class="hljs-keyword">public</span> String <span class="hljs-title function_">getName</span><span class="hljs-params">()</span><br>    &#123;<br>        <span class="hljs-keyword">return</span> name;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p><strong>提示</strong>：有些程序员认为，在抽象类中不能包含具体方法。建议尽量将通用的字段和方法（不管是否为抽象）放在超类（不管是否为抽象）中。</p><p><strong>抽象方法相当于子类中实现的具体方法的占位符</strong>。扩展一个抽象类时，可以有两种选择。一种是在子类中保留抽象类中的部分或所有抽象方法仍未定义，这样就必须将子类也标记为抽象类；另一种做法是定义全部方法，这样一来，子类就不再是抽象的。</p><p>例如，我们将定义 Student 类来扩展抽象 Person 类，并实现 getDescription 方法。由于在 Student 类中不再含有抽象方法，所以不需要将这个类声明为抽象类。</p><p>抽象类的两种用途：</p><p>1.作为占位符</p><ul><li>抽象方法相当于**”占位符”**，要求子类必须实现</li><li>确保所有子类都有统一的行为接口</li></ul><p>2.作为通用代码容器</p><ul><li>存放子类共有的字段和方法</li><li>遵循良好设计原则：<strong>“将通用内容放在超类中”</strong></li></ul><p><strong>即使不含抽象方法，也可以将类声明为 abstract</strong>。（防止直接实例化      只能被继承，不能单独使用）</p><p><strong>抽象类不能实例化</strong>。也就是说，如果将一个类声明为 abstract，就不能创建这个类的对象。例如，以下表达式</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">new</span> <span class="hljs-title class_">Person</span>(<span class="hljs-string">&quot;Vince Vu&quot;</span>)<br></code></pre></td></tr></table></figure><p>是错误的，但可以创建具体子类的对象。</p><p>需要注意，仍然可以创建一个抽象类的<strong>对象变量</strong>（object variable），但是这样<strong>一个变量只能引用非抽象子类的对象</strong>。例如，</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">Person</span> <span class="hljs-variable">p</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Student</span>(<span class="hljs-string">&quot;Vince Vu&quot;</span>, <span class="hljs-string">&quot;Economics&quot;</span>);<br></code></pre></td></tr></table></figure><p>这里的 p 是抽象类型 Person 的一个变量，它引用了非抽象子类 Student 的一个实例。</p><p>下面定义一个扩展抽象类 Person 的具体子类 Student：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Student</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Person</span><br>&#123;<br>    <span class="hljs-keyword">private</span> String major;<br>    <br>    <span class="hljs-keyword">public</span> <span class="hljs-title function_">Student</span><span class="hljs-params">(String name, String major)</span><br>    &#123;<br>        <span class="hljs-built_in">super</span>(name);<br>        <span class="hljs-built_in">this</span>.major = major;<br>    &#125;<br>    <br>    <span class="hljs-keyword">public</span> String <span class="hljs-title function_">getDescription</span><span class="hljs-params">()</span><br>    &#123;<br>        <span class="hljs-keyword">return</span> <span class="hljs-string">&quot;a student majoring in &quot;</span> + major;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>Student 类定义了 getDescription 方法。因此，在 Student 类中的全部方法都是具体的，这个类不再是抽象类。</p><h3 id="5-7-枚举类"><a href="#5-7-枚举类" class="headerlink" title="5.7 枚举类"></a>5.7 枚举类</h3><p>下面是一个典型的例子：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">enum</span> <span class="hljs-title class_">Size</span> &#123; SMALL, MEDIUM, LARGE, EXTRA_LARGE &#125;<br></code></pre></td></tr></table></figure><p>实际上，这个声明定义的类型是一个类，它刚好有4个实例，不可能构造新的对象。</p><p>因此，在比较枚举类型的值时，并不需要使用equals，可以直接使用<code>==</code>来比较。</p><p>如果需要的话，可以为枚举类型增加构造器、方法和字段。当然，构造器只是在构造枚举常量的时候调用。下面是一个示例：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">enum</span> <span class="hljs-title class_">Size</span><br>&#123;<br>    SMALL(<span class="hljs-string">&quot;S&quot;</span>), MEDIUM(<span class="hljs-string">&quot;M&quot;</span>), LARGE(<span class="hljs-string">&quot;L&quot;</span>), EXTRA_LARGE(<span class="hljs-string">&quot;XL&quot;</span>);<br><br>    <span class="hljs-keyword">private</span> String abbreviation;<br><br>    Size(String abbreviation) &#123; <span class="hljs-built_in">this</span>.abbreviation = abbreviation; &#125;<br>    <span class="hljs-comment">// automatically private</span><br><br>    <span class="hljs-keyword">public</span> String <span class="hljs-title function_">getAbbreviation</span><span class="hljs-params">()</span> &#123; <span class="hljs-keyword">return</span> abbreviation; &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>**枚举的构造器总是私有的。**可以像前例中一样省略<code>private</code>修饰符。如果声明一个<code>enum</code>构造器为<code>public</code>或<code>protected</code>，则会出现语法错误。</p><p><strong>所有的枚举类型都是抽象类<code>Enum</code>的子类</strong>。它们继承了这个类的许多方法。其中，最有用的是**<code>toString</code>，这个方法会返回枚举常量名**。例如，<code>Size.SMALL.toString()</code>将返回字符串<code>&quot;SMALL&quot;</code>。</p><p><code>toString</code>的逆方法是静态方法<code>valueOf</code>。例如，以下语句</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">Size</span> <span class="hljs-variable">s</span> <span class="hljs-operator">=</span> Enum.valueOf(Size.class, <span class="hljs-string">&quot;SMALL&quot;</span>);<br></code></pre></td></tr></table></figure><p>将<code>s</code>设置成<code>Size.SMALL</code>。</p><p>每个枚举类型都有一个静态的<code>values</code>方法，它将返回一个包含全部枚举值的数组。例如，如下调用</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">Size[] values = Size.values();<br></code></pre></td></tr></table></figure><p>将返回包含元素<code>Size.SMALL</code>、<code>Size.MEDIUM</code>、<code>Size.LARGE</code>和<code>Size.EXTRA_LARGE</code>的数组。</p><p><code>ordinal</code>方法返回一个枚举常量在<code>enum</code>声明中的位置，位置从0开始计数。例如，<code>Size.MEDIUM.ordinal()</code>返回<code>1</code>。</p><p><strong>注释</strong>：<code>Enum</code>类有一个类型参数，不过为简单起见，我们省略了这个类型参数。例如，实际上枚举类型<code>Size</code>扩展了<code>Enum&lt;Size&gt;</code>。类型参数会在<code>compareTo</code>方法中使用。</p><h3 id="5-8-密封类"><a href="#5-8-密封类" class="headerlink" title="5.8 密封类"></a>5.8 密封类</h3><p>除非一个类声明为 final，否则任何人都可以派生这个类的子类。如果想对它有更多控制权呢？例如，假设需要编写你自己的 JSON 库，因为现有的库都不能完全满足你的需要。</p><p><strong>JSON 标准指出，JSON 值是一个数组、数值、字符串、布尔值、对象或 null</strong> 。对此，显然可以使用 JSONArray、JSONNumber 等类来表示，它们都扩展一个抽象类 JSONValue：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">abstract</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">JSONValue</span><br>&#123;<br>    <span class="hljs-comment">// Methods that apply to all JSON values</span><br>&#125;<br><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">JSONArray</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">JSONValue</span><br>&#123;<br>    ...<br>&#125;<br><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">JSONNumber</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">JSONValue</span><br>&#123;<br>    ...<br>&#125;<br></code></pre></td></tr></table></figure><p>通过将 JSONArray、JSONNumber 等类声明为 final，可以确保没有人能派生它们的子类。但我们无法阻止人们派生 JSONValue 的另一个子类。</p><p>为什么我们想要控制这一点呢？考虑以下代码：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">JSONValue</span> <span class="hljs-variable">v</span> <span class="hljs-operator">=</span> ...;<br><span class="hljs-keyword">if</span> (v <span class="hljs-keyword">instanceof</span> JSONArray a) ...<br><span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (v <span class="hljs-keyword">instanceof</span> JSONNumber n) ...<br><span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (v <span class="hljs-keyword">instanceof</span> JSONString s) ...<br><span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (v <span class="hljs-keyword">instanceof</span> JSONBoolean b) ...<br><span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (v <span class="hljs-keyword">instanceof</span> JSONObject o) ...<br><span class="hljs-keyword">else</span> ... <span class="hljs-comment">// Must be JSONNull</span><br></code></pre></td></tr></table></figure><p>在这里，从控制流程可以看出，我们知道 JSONValue 的所有直接子类。这不是一个开放性的层次结构。JSON 标准不会改变，如果确实有改变，作为库实现者，我们完全可以增加第 7 个子类。我们不希望别人混乱这个类层次结构。</p><p>在 Java 中，<strong>密封类（sealed class）会控制哪些类可以继承它</strong>。Java 15 中作为一个预览特性增加了密封类，并在 <strong>Java 17 中最终确定了这个特性</strong>。</p><p>可以如下将 JSONValue 类声明为密封类：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">abstract</span> <span class="hljs-keyword">sealed</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">JSONValue</span><br>    <span class="hljs-keyword">permits</span> JSONArray, JSONNumber, JSONString, JSONBoolean, JSONObject, JSONNull<br>&#123;<br>    ...<br>&#125;<br></code></pre></td></tr></table></figure><p>如果试图定义一个未经允许的子类，那么这将是一个错误：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">JSONComment</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">JSONValue</span> &#123;...&#125; <span class="hljs-comment">// Error</span><br></code></pre></td></tr></table></figure><p>这是有道理的，因为 JSON 不支持注释。所以，密封类可以准确地描述领域约束。</p><p><strong>一个密封类允许的子类必须是可访问的</strong>。<strong>它们不能是嵌套在另一个类中的私有类，也不能是位于另一个包中的包可见的类。</strong></p><p><strong>对于允许的公共子类</strong>，规则要更为严格。<strong>它们必须与密封类在同一个包中</strong>。不过，如果使用<strong>模块，则必须在同一个模块中</strong>。</p><p><strong>注释</strong>：**声明密封类可以不加 permits 子句。**这样一来，它的所有直接子类都必须在同一个文件中声明。不能访问这个文件的程序员就不能派生它的子类。</p><p>一个文件最多只能有一个 public 类，所以看起来只有当子类不会公共使用时，这种组织（即所有子类都在同一个文件中）才有用。<br>不过，可以使用内联类作为公共子类。</p><p>使用密封类的一个重要原因是编译时检查。考虑 JSONValue 类的以下这个方法，其中使用了一个带模式匹配的 switch 表达式（这是 Java 17 中的一个预览特性）：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> String <span class="hljs-title function_">type</span><span class="hljs-params">()</span><br>&#123;<br>    <span class="hljs-keyword">return</span> <span class="hljs-keyword">switch</span> (<span class="hljs-built_in">this</span>)<br>    &#123;<br>        <span class="hljs-keyword">case</span> JSONArray j -&gt; <span class="hljs-string">&quot;array&quot;</span>;<br>        <span class="hljs-keyword">case</span> JSONNumber j -&gt; <span class="hljs-string">&quot;number&quot;</span>;<br>        <span class="hljs-keyword">case</span> JSONString j -&gt; <span class="hljs-string">&quot;string&quot;</span>;<br>        <span class="hljs-keyword">case</span> JSONBoolean j -&gt; <span class="hljs-string">&quot;boolean&quot;</span>;<br>        <span class="hljs-keyword">case</span> JSONObject j -&gt; <span class="hljs-string">&quot;object&quot;</span>;<br>        <span class="hljs-keyword">case</span> JSONNull j -&gt; <span class="hljs-string">&quot;null&quot;</span>;<br>        <span class="hljs-comment">// No default needed here</span><br>    &#125;;<br>&#125;<br></code></pre></td></tr></table></figure><p>编译器可以检查出这里不需要 default 子句，因为 JSONValue 的所有直接子类都已经出现在 case 分支中。</p><p><strong>注释</strong>：前面的 type 方法看起来不太具有面向对象特点。按照 OOP 的精神，这 6 个类应当提供自己的 type 方法，应该依赖多态性而不是一个 switch。对于一个开放性的层次结构，这是一种好方法。不过，对于一组固定的类，通常更方便的做法是在一个方法中处理所有候选类。</p><p>乍一看，似乎密封类的子类必须是 final 类。但对于穷尽测试，我们只需要知道所有直接子类。如果那些类有自己的子类，那么这并没有问题。例如，我们的 JSON 层次结构如图 5-3 所示。</p><p><img src="/2025/10/20/%E3%80%8AJava%E6%A0%B8%E5%BF%83%E6%8A%80%E6%9C%AF%E5%8D%B7%E4%B8%80%E3%80%8B%E7%AC%AC%E4%BA%94%E7%AB%A0/image-20251104163123122.png" alt="image-20251104163123122"></p><p>​<strong>图 5-3 表示 JSON 值的类的完整层次结构</strong></p><p>在这个层次结构中，JSONValue 允许有 3 个子类：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">abstract</span> <span class="hljs-keyword">sealed</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">JSONValue</span> <span class="hljs-keyword">permits</span> JSONObject, JSONArray, JSONPrimitive<br>&#123;<br>    . . .<br>&#125;<br></code></pre></td></tr></table></figure><p>JSONPrimitive 类也是密封的：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">abstract</span> <span class="hljs-keyword">sealed</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">JSONPrimitive</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">JSONValue</span><br>    <span class="hljs-keyword">permits</span> JSONString, JSONNumber, JSONBoolean, JSONNull<br>&#123;<br>    . . .<br>&#125;<br></code></pre></td></tr></table></figure><p><strong>密封类的子类必须指定它是 <code>sealed</code>、<code>final</code>，还是允许继续派生子类</strong>。<strong>对于最后一种情况，必须声明为 <code>non-sealed</code></strong>。</p><p><strong>注释</strong>：<code>non-sealed</code> <strong>关键字是第一个带连字符的 Java 关键字</strong>。这可能是将来的一个趋势。在语言中增加关键字总是会带来风险。现有的代码可能无法再编译。由于这个原因，<strong><code>sealed</code> 是一个“有上下文的”关键字。仍然可以声明名为 <code>sealed</code> 的变量或方法</strong>：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">int</span> <span class="hljs-variable">sealed</span> <span class="hljs-operator">=</span> <span class="hljs-number">1</span>; <span class="hljs-comment">// OK to use contextual keyword as identifier</span><br></code></pre></td></tr></table></figure><p>利用带连字符的关键字，我们可以不用担心这个问题。唯一可能产生二义性的是减法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">int</span> <span class="hljs-variable">non</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>;<br>non = non - <span class="hljs-keyword">sealed</span>; <span class="hljs-comment">// Subtraction, not keyword</span><br></code></pre></td></tr></table></figure><p>为什么想要使用一个 <code>non-sealed</code> 子类呢？考虑一个 XML 节点类，它有 6 个直接子类：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">abstract</span> <span class="hljs-keyword">sealed</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Node</span> <span class="hljs-keyword">permits</span> Element, Text, Comment,<br>    CDATASection, EntityReference, ProcessingInstruction<br>&#123;<br>    . . .<br>&#125;<br></code></pre></td></tr></table></figure><p>我们允许任意派生 Element 的子类：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">non-sealed</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Element</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Node</span><br>&#123;<br>    . . .<br>&#125;<br><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">HTMLDivElement</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Element</span><br>&#123;<br>    . . .<br>&#125;<br></code></pre></td></tr></table></figure><p>接口是抽象类的泛化。Java 接口还可以有子类型。密封接口的做法与密封类完全相同，会控制直接子类型。</p><h3 id="5-9-反射"><a href="#5-9-反射" class="headerlink" title="5.9 反射"></a>5.9 反射</h3><p>**反射库（reflection library）**提供了一个丰富且精巧的工具集，可以用来编写动态操纵 Java 代码的程序。使用反射，Java 可以支持用户界面生成器、对象关系映射器以及很多其他需要动态查询类能力的开发工具。</p><p><strong>能够分析类能力的程序称为可反射（reflective）</strong>。反射机制的功能极其强大，如下几节所示，可以用它来：</p><ul><li>在运行时分析类的能力。</li><li>在运行时检查对象，例如，编写一个适用于所有类的 toString 方法。</li><li>实现泛型数组操作代码。</li><li>利用 Method 对象，这个对象很像 C++ 中的函数指针。</li></ul><p>反射是一种功能强大且复杂的机制，不过，主要是开发工具的程序员对它感兴趣，一般的应用程序员并不需要考虑反射机制。如果你只对编写应用程序感兴趣，而不是要为其他 Java 程序员构建工具，那么可以跳过本章的剩余部分，等以后再返回来学习。</p><h4 id="5-9-1-Class类"><a href="#5-9-1-Class类" class="headerlink" title="5.9.1 Class类"></a>5.9.1 Class类</h4><p>在程序运行期间，Java 运行时系统始终为所有对象维护一个<strong>运行时类型标识（runtime type identification）</strong>。这个信息会跟踪每个对象所属的类。虚拟机利用运行时类型信息选择要执行的正确方法。</p><p>不过，还可以使用一个特殊的 Java 类访问这些信息。保存这些信息的类名为 Class，这个名字有些让人困惑。Object 类中的 <code>getClass( )</code> 方法将会返回同一个 Class 类型的实例。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java">Employee e;<br><span class="hljs-type">Class</span> <span class="hljs-variable">cl</span> <span class="hljs-operator">=</span> e.getClass();<br></code></pre></td></tr></table></figure><p>就像 Employee 对象描述一个特定员工的属性一样，Class 对象会描述一个特定类的属性。可能最常用的 Class 方法就是 getName。这个方法将返回类的名字。例如，下面这条语句：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">System.out.println(e.getClass().getName() + <span class="hljs-string">&quot; &quot;</span> + e.getName());<br></code></pre></td></tr></table></figure><p>如果 e 是一个员工，则会输出：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">Employee Harry Hacker<br></code></pre></td></tr></table></figure><p>如果 e 是经理，则会输出：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">Manager Harry Hacker<br></code></pre></td></tr></table></figure><p>如果类在一个包里，包名也作为类名的一部分：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">var</span> <span class="hljs-variable">generator</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Random</span>();<br><span class="hljs-type">Class</span> <span class="hljs-variable">cl</span> <span class="hljs-operator">=</span> generator.getClass();<br><span class="hljs-type">String</span> <span class="hljs-variable">name</span> <span class="hljs-operator">=</span> cl.getName(); <span class="hljs-comment">// name is set to &quot;java.util.Random&quot;</span><br></code></pre></td></tr></table></figure><p>还可以使用静态方法 forName 获得类名对应的 Class 对象。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">String</span> <span class="hljs-variable">className</span> <span class="hljs-operator">=</span> <span class="hljs-string">&quot;java.util.Random&quot;</span>;<br><span class="hljs-type">Class</span> <span class="hljs-variable">cl</span> <span class="hljs-operator">=</span> Class.forName(className);<br></code></pre></td></tr></table></figure><p>如果类名称存在一个字符串中，这个字符串会在运行时变化，就可以使用这个方法。如果 className 是一个类名或接口名，这个方法可以正常执行。否则，forName 方法将抛出一个<strong>检查型异常（checked exception）</strong>。无论何时使用这个方法，都应该提供一个<strong>异常处理器（exception handler）</strong>。</p><p>获得 Class 类对象的第三种方法是一个很方便的快捷方式。如果 T 是任意的 Java 类型（或 void 关键字），T.class 将是匹配的类对象。例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">Class</span> <span class="hljs-variable">cl1</span> <span class="hljs-operator">=</span> Random.class; <span class="hljs-comment">// if you import java.util.*;</span><br><span class="hljs-type">Class</span> <span class="hljs-variable">cl2</span> <span class="hljs-operator">=</span> <span class="hljs-type">int</span>.class;<br><span class="hljs-type">Class</span> <span class="hljs-variable">cl3</span> <span class="hljs-operator">=</span> Double[].class;<br></code></pre></td></tr></table></figure><p>请注意，Class 对象实际上描述的是一个类型，这可能是类，也可能不是类。例如，int 不是类，但 int.class 确实是一个 Class 类型的对象。</p><p><strong>注释</strong>：**Class 类实际上是一个泛型类。**例如，Employee.class 的类型是 Class<Employee>。我们没有深究这个问题，这是因为它会让已经很抽象的概念变得更加复杂。在大多数实际应用中，可以忽略类型参教，而使用原始的 Class 类。</p><p><strong>警告</strong>：鉴于历史原因，<code>getName</code> 方法对数组类型会返回有些奇怪的名字：</p><ul><li><code>Double[].class.getName()</code> 返回 <code>&quot;[Ljava.lang.Double;&quot;</code>；</li><li><code>int[].class.getName()</code> 返回 <code>&quot;[I&quot;</code>。</li></ul><p>虚拟机为每个类型管理一个唯一的 <code>Class</code> 对象。因此，可以使用 <code>==</code> 运算符比较两个类对象。例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">if</span> (e.getClass() == Employee.class) . . .<br></code></pre></td></tr></table></figure><p>如果 <code>e</code> 是一个 <code>Employee</code> 实例，则这个测试将通过。与条件 <code>e instanceof Employee</code> 不同，如果 <code>e</code> 是某个子类（如 <code>Manager</code>）的实例，则这个测试将失败。</p><p>如果有一个 <code>Class</code> 类型的对象，可以用它构造类的实例。调用 <code>getConstructor</code> 方法将得到一个 <code>Constructor</code> 类型的对象，然后使用 <code>newInstance</code> 方法来构造一个实例。例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">var</span> <span class="hljs-variable">className</span> <span class="hljs-operator">=</span> <span class="hljs-string">&quot;java.util.Random&quot;</span>; <span class="hljs-comment">// or any other name of a class with</span><br>                                    <span class="hljs-comment">// a no-arg constructor</span><br><span class="hljs-type">Class</span> <span class="hljs-variable">cl</span> <span class="hljs-operator">=</span> Class.forName(className);<br><span class="hljs-type">Object</span> <span class="hljs-variable">obj</span> <span class="hljs-operator">=</span> cl.getConstructor().newInstance();<br></code></pre></td></tr></table></figure><p>如果这个类没有无参数的构造器，则 <code>getConstructor</code> 方法会抛出一个异常。可以参见 5.9.7 节了解如何调用其他构造器。</p><p><strong>注释</strong>：有一个已经废弃的 <code>Class.toInstance</code> 方法，它也可以利用无参数构造器构造一个实例。不过，如果构造器抛出一个检查型异常，那么这个异常将不做任何检查重新抛出。这违反了编译时异常检查的原则。与之不同，<code>Constructor.newInstance</code> 会把所有构造器异常包装到一个 <code>InvocationTargetException</code> 中。</p><h4 id="5-9-2-声明异常入门"><a href="#5-9-2-声明异常入门" class="headerlink" title="5.9.2 声明异常入门"></a>5.9.2 声明异常入门</h4><p>当运行时发生错误时，程序就会”抛出一个异常”。抛出异常比终止程序要灵活得多，这是因为你可以提供一个处理器（handler）”捕获”这个异常并进行处理。</p><p>如果没有提供处理器，程序就会终止，并在控制台上打印出一个消息，给出异常的类型。你可能在前面已经看到过一些异常报告，例如，不小心使用了<code>null</code>引用或者数组越界时。</p><p>异常有两种类型：非检查型（unchecked）异常和检查型（checked）异常。对于检查型异常，编译器将会检查你（程序员）是否知道这个异常并做好准备来处理后果。不过，有很多常见的异常（例如，越界错误或者访问<code>null</code>引用）都属于非检查型异常。编译器并不期望你为这些异常提供处理器。毕竟，你应该集中精力避免这些错误的发生，而不是为它们编写处理器。</p><p>不是所有的错误都是可以避免的。如果竭尽全力还是可能发生异常，大多数Java API都会抛出一个检查型异常。<code>Class.forName</code>方法就是一个例子。没有办法确保有指定名字的类一定存在。在第7章中，将会看到几种异常处理策略。现在，我们只介绍最简单的一个策略。</p><p>如果一个方法包含一条可能抛出检查型异常的语句，则在方法名上增加一个<code>throws</code>子句。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">doSomethingWithClass</span><span class="hljs-params">(String name)</span><br>    <span class="hljs-keyword">throws</span> ReflectiveOperationException<br>&#123;<br>    <span class="hljs-type">Class</span> <span class="hljs-variable">cl</span> <span class="hljs-operator">=</span> Class.forName(name); <span class="hljs-comment">// might throw exception</span><br>    <span class="hljs-comment">// do something with cl</span><br>&#125;<br></code></pre></td></tr></table></figure><p>调用这个方法的任何方法也都需要一个<code>throws</code>声明，这也包括<code>main</code>方法。如果一个异常确实出现，则<code>main</code>方法将终止并提供一个栈轨迹。（在第7章中，你将了解如何捕获异常而不是因异常终止程序。）</p><p>只需要为检查型异常提供一个<code>throws</code>子句。很容易找出哪些方法会抛出检查型异常——只要你调用了一个可能抛出检查型异常的方法而没有提供相应的异常处理器，编译器就会报错。</p><h4 id="5-9-3-资源"><a href="#5-9-3-资源" class="headerlink" title="5.9.3 资源"></a>5.9.3 资源</h4><p>类通常有一些关联的数据文件，例如：</p><ul><li>图像和声音文件。</li><li>包含消息字符串和按钮标签的文本文件。</li></ul><p>在 Java 中，这些关联的文件被称为<strong>资源</strong>（resource）。</p><p>例如，考虑一个显示消息的对话框，如图 5-4 所示。<br>当然，对于本书的下一版，这个面板中显示的书名和版权年会改变。为了便于追踪这个变化，我们将把这个文本放在一个文件中，而不是作为一个字符串硬编码写到代码中。</p><p>但是，应该将类似 <code>about.txt</code> 的文件放在哪儿呢？当然，将它与其他程序文件一起放在 JAR 文件中会很方便。</p><p><code>Class</code> 类提供了一个很有用的服务可以查找资源文件。下面给出必要的步骤：</p><ol><li>获得拥有资源的类的 <code>Class</code> 对象，例如 <code>ResourceTest.class</code>。</li><li>有些方法（如 <code>ImageIcon</code> 类的 <code>getImage</code> 方法）接受描述资源位置的 URL。那么，可以调用<figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">URL</span> <span class="hljs-variable">url</span> <span class="hljs-operator">=</span> cl.getResource(<span class="hljs-string">&quot;about.gif&quot;</span>);<br></code></pre></td></tr></table></figure></li><li>否则，使用 <code>getResourceAsStream</code> 方法得到一个输入流来读取文件中的数据。</li></ol><p>这里的重点在于 Java 虚拟机知道如何查找一个类，所以它能搜索相同位置上的关联资源。例如，假设 <code>ResourceTest</code> 类在一个 <code>resources</code> 包中。<code>ResourceTest.class</code> 文件就位于 <code>resources</code> 目录中，可以把一个图标文件放在同一个目录下。</p><p>除了可以将资源文件与类文件放在同一个目录中，还可以提供一个相对或绝对路径，如：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash">data/about.txt<br>/corejava/title.txt<br></code></pre></td></tr></table></figure><p>文件的自动装载是利用资源加载特性完成的。没有标准的方法来解释资源文件的内容。每个程序必须有自己的方法来解释它的资源文件。</p><p>另一个经常使用资源的地方是程序的国际化。与语言相关的字符串（如消息和用户界面标签）都存放在资源文件中，每种语言对应一个文件。国际化 API（internationalization API）将在卷 II 的第 7 章中讨论。它支 持一种标准方法来组织和访问这些本地化文件。</p><p>程序清单 5-14 的程序展示了资源加载。（先不用担心读取文本和显示对话框的代码，这些内容稍后会详细介绍。）编译、构建一个 JAR 文件并执行：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs bash">java resource/ResourceTest.java<br>jar cvfe ResourceTest.jar resources.ResourceTest \<br>    resources/*.class resources/*.gif resources/data/*.txt corejava/*.txt<br>java -jar ResourceTest.jar<br></code></pre></td></tr></table></figure><p>将 JAR 文件移到另外一个不同的目录中，再次运行，以确认程序是从 JAR 文件而不是从当前目录读取资源。</p><h4 id="5-9-4-利用反射分析类的能力"><a href="#5-9-4-利用反射分析类的能力" class="headerlink" title="5.9.4 利用反射分析类的能力"></a>5.9.4 利用反射分析类的能力</h4><p>下面简要介绍反射机制最重要的内容，这允许你检查类的结构。</p><p><code>java.lang.reflect</code> 包中有三个类 <code>Field</code>、<code>Method</code> 和 <code>Constructor</code>，分别用于描述类的字段、方法和构造器。这三个类都有一个名为 <code>getName</code> 的方法，用来返回字段、方法或构造器的名字。<code>Field</code> 类有一个 <code>getType</code> 方法，用来返回描述字段类型的一个对象，这个对象的类型同样是 <code>Class</code>。<code>Method</code> 和 <code>Constructor</code> 类有报告参数类型的方法，<code>Method</code> 类还有一个报告返回类型的方法。这三个类都有一个名为 <code>getModifiers</code> 的方法，它将返回一个整数，用不同的 0&#x2F;1 位描述所使用的修饰符，如 <code>public</code> 和 <code>static</code>。然后，可以利用 <code>java.lang.reflect</code> 包中 <code>Modifier</code> 类的静态方法分析 <code>getModifiers</code> 返回的这个整数。例如，可以使用 <code>Modifier</code> 类中的 <code>isPublic</code>、<code>isPrivate</code> 或<code>isFinal</code> 判断一个方法或构造器是 <code>public</code>、<code>private</code> 还是 <code>final</code>。我们需要做的就是在 <code>getModifiers</code> 返回的整数上调用 <code>Modifier</code> 类中适当的方法。另外，还可以利用 <code>Modifier.toString</code> 方法打印修饰符。</p><p><code>Class</code> 类中的 <code>getFields</code>、<code>getMethods</code> 和 <code>getConstructors</code> 方法将分别返回这个类支持的公共字段、方法和构造器的数组，其中包括超类的公共成员。<code>Class</code> 类的 <code>getDeclaredFields</code>、<code>getDeclaredMethods</code> 和 <code>getDeclaredConstructors</code> 方法将分别返回这个类中声明的全部字段、方法和构造器组成的数组，其中包括私有成员、包成员和受保护成员，以及有包访问权限的成员，但不包括超类的成员。</p><p>程序清单 5-15 显示了如何打印一个类的全部信息。这个程序提示用户输入一个类名，然后输出类中所有的方法和构造器的签名，以及全部实例字段名。例如，如果输入</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">java.lang.Double<br></code></pre></td></tr></table></figure><p>这个程序将会输出：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br></pre></td><td class="code"><pre><code class="hljs JAVA"><span class="hljs-keyword">public</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">java</span>.lang.Double <span class="hljs-keyword">extends</span> <span class="hljs-title class_">java</span>.lang.Number<br>&#123;<br>    <span class="hljs-keyword">public</span> java.lang.Double(<span class="hljs-type">double</span>);<br>    <span class="hljs-keyword">public</span> java.lang.Double(java.lang.String);<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-type">boolean</span> <span class="hljs-title function_">equals</span><span class="hljs-params">(java.lang.Object)</span>;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> java.lang.String <span class="hljs-title function_">toString</span><span class="hljs-params">(<span class="hljs-type">double</span>)</span>;<br>    <span class="hljs-keyword">public</span> java.lang.String <span class="hljs-title function_">toString</span><span class="hljs-params">()</span>;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-type">int</span> <span class="hljs-title function_">hashCode</span><span class="hljs-params">(<span class="hljs-type">double</span>)</span>;<br>    <span class="hljs-keyword">public</span> <span class="hljs-type">int</span> <span class="hljs-title function_">hashCode</span><span class="hljs-params">()</span>;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-type">double</span> <span class="hljs-title function_">min</span><span class="hljs-params">(<span class="hljs-type">double</span>, <span class="hljs-type">double</span>)</span>;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-type">double</span> <span class="hljs-title function_">max</span><span class="hljs-params">(<span class="hljs-type">double</span>, <span class="hljs-type">double</span>)</span>;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">native</span> <span class="hljs-type">long</span> <span class="hljs-title function_">doubleToRawLongBits</span><span class="hljs-params">(<span class="hljs-type">double</span>)</span>;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-type">long</span> <span class="hljs-title function_">doubleToLongBits</span><span class="hljs-params">(<span class="hljs-type">double</span>)</span>;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">native</span> <span class="hljs-type">double</span> <span class="hljs-title function_">LongBitsToDouble</span><span class="hljs-params">(<span class="hljs-type">long</span>)</span>;<br>    <span class="hljs-keyword">public</span> <span class="hljs-type">int</span> <span class="hljs-title function_">compareTo</span><span class="hljs-params">(java.lang.Double)</span>;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">volatile</span> <span class="hljs-type">int</span> <span class="hljs-title function_">compareTo</span><span class="hljs-params">(java.lang.Object)</span>;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-type">int</span> <span class="hljs-title function_">compare</span><span class="hljs-params">(<span class="hljs-type">double</span>, <span class="hljs-type">double</span>)</span>;<br>    <span class="hljs-keyword">public</span> <span class="hljs-type">byte</span> <span class="hljs-title function_">byteValue</span><span class="hljs-params">()</span>;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-type">short</span> <span class="hljs-title function_">shortValue</span><span class="hljs-params">()</span>;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-type">int</span> <span class="hljs-title function_">intValue</span><span class="hljs-params">()</span>;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-type">long</span> <span class="hljs-title function_">longValue</span><span class="hljs-params">()</span>;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-type">float</span> <span class="hljs-title function_">floatValue</span><span class="hljs-params">()</span>;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-type">double</span> <span class="hljs-title function_">doubleValue</span><span class="hljs-params">()</span>;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> java.lang.Double <span class="hljs-title function_">valueOf</span><span class="hljs-params">(java.lang.String)</span>;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> java.lang.Double <span class="hljs-title function_">valueOf</span><span class="hljs-params">(<span class="hljs-type">double</span>)</span>;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> java.lang.String <span class="hljs-title function_">toHexString</span><span class="hljs-params">(<span class="hljs-type">double</span>)</span>;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">volatile</span> java.lang.Object <span class="hljs-title function_">resolveConstantDesc</span><span class="hljs-params">(</span><br><span class="hljs-params">        java.lang.invoke.MethodHandles$Lookup)</span>;<br>    <span class="hljs-keyword">public</span> java.lang.Double <span class="hljs-title function_">resolveConstantDesc</span><span class="hljs-params">(java.lang.invoke.MethodHandles$Lookup)</span>;<br>    <span class="hljs-keyword">public</span> java.util.Optional <span class="hljs-title function_">describeConstable</span><span class="hljs-params">()</span>;<br>    <span class="hljs-keyword">public</span> <span class="hljs-type">boolean</span> <span class="hljs-title function_">isNaN</span><span class="hljs-params">()</span>;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-type">boolean</span> <span class="hljs-title function_">isNaN</span><span class="hljs-params">(<span class="hljs-type">double</span>)</span>;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-type">double</span> <span class="hljs-title function_">sum</span><span class="hljs-params">(<span class="hljs-type">double</span>, <span class="hljs-type">double</span>)</span>;<br>    <span class="hljs-keyword">public</span> <span class="hljs-type">boolean</span> <span class="hljs-title function_">isInfinite</span><span class="hljs-params">()</span>;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-type">boolean</span> <span class="hljs-title function_">isInfinite</span><span class="hljs-params">(<span class="hljs-type">double</span>)</span>;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-type">boolean</span> <span class="hljs-title function_">isFinite</span><span class="hljs-params">(<span class="hljs-type">double</span>)</span>;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-type">double</span> <span class="hljs-title function_">parseDouble</span><span class="hljs-params">(java.lang.String)</span>;<br><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">double</span> POSITIVE_INFINITY;<br><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">double</span> NEGATIVE_INFINITY;<br><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">double</span> NaN;<br><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">double</span> MAX_VALUE;<br><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">double</span> MIN_NORMAL;<br><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">double</span> MIN_VALUE;<br><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">int</span> MAX_EXPONENT;<br><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">int</span> MIN_EXPONENT;<br><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">int</span> SIZE;<br><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">int</span> BYTES;<br><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> java.lang.Class TYPE;<br><span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> <span class="hljs-type">double</span> value;<br><span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">long</span> serialVersionUID;<br>&#125;<br></code></pre></td></tr></table></figure><p>令人赞叹的是，这个程序可以分析 Java 解释器能加载的任何类，而不仅仅是编译程序时可用的类。</p><h4 id="5-9-5-使用反射在运行时分析对象"><a href="#5-9-5-使用反射在运行时分析对象" class="headerlink" title="5.9.5 使用反射在运行时分析对象"></a>5.9.5 使用反射在运行时分析对象</h4><p>从前面一节中，我们已经知道如何查看任意对象实例字段的名字和类型：</p><ul><li>获得对应的 Class 对象。</li><li>在这个 Class 对象上调用 getDeclaredFields。</li></ul><p>本节将进一步具体查看字段的内容。当然，在编写程序时，如果知道想要查看的字段名和类型，查看对象中指定字段的内容是一件很容易的事情。而利用反射机制可以查看在编译时还不知道的对象字段。</p><p>要做到这一点，关键方法是 Field 类中的 get 方法。如果 f 是一个 Field 类型的对象（例如，通过 getDeclaredFields 得到的对象），obj 是某个包含 f 字段的类的对象，则 f.get(obj) 将返回一个对象，其值为 obj 的当前字段值。这样说起来显得有点抽象，下面来看一个例子。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">var</span> <span class="hljs-variable">harry</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Employee</span>(<span class="hljs-string">&quot;Harry Hacker&quot;</span>, <span class="hljs-number">59800</span>, <span class="hljs-number">10</span>, <span class="hljs-number">1</span>, <span class="hljs-number">1989</span>);<br><span class="hljs-type">Class</span> <span class="hljs-variable">cl</span> <span class="hljs-operator">=</span> harry.getClass();<br><span class="hljs-comment">// the class object representing Employee</span><br><span class="hljs-type">Field</span> <span class="hljs-variable">f</span> <span class="hljs-operator">=</span> cl.getDeclaredField(<span class="hljs-string">&quot;name&quot;</span>);<br><span class="hljs-comment">// the name field of the Employee class</span><br><span class="hljs-type">Object</span> <span class="hljs-variable">v</span> <span class="hljs-operator">=</span> f.get(harry);<br><span class="hljs-comment">// the value of the name field of the harry object, i.e.,</span><br><span class="hljs-comment">// the String object &quot;Harry Hacker&quot;</span><br></code></pre></td></tr></table></figure><p>当然，不仅可以获得值，也可以设置值。调用 f.set(obj, value) 将把对象 obj 中 f 表示的字段设置为新值。</p><p>实际上，这段代码存在一个问题。由于 name 是一个私有字段，所以 get 和 set 方法会抛出一个 IllegalAccessException。只能对可以访问的字段使用 get 和 set 方法。Java 安全机制允许查看一个对象有哪些字段，但是除非拥有访问权限，否则不允许读写那些字段的值。</p><p>反射机制的默认行为受限于 Java 的访问控制。不过，可以调用 Field、Method 或 Constructor 对象的 setAccessible 方法覆盖 Java 的访问控制。例如，</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">f.setAccessible(<span class="hljs-literal">true</span>); <span class="hljs-comment">// now OK to call f.get(harry)</span><br></code></pre></td></tr></table></figure><p>setAccessible 方法是 AccessibleObject 类中的一个方法，它是 Field、Method 和 Constructor 类的公共超类。这个特性是为调试、持久存储和类似机制提供的。本节稍后将用它编写一个通用的 <code>toString</code> 方法。</p><p>如果不允许访问，<code>setAccessible</code> 调用会抛出一个异常。访问可能被模块系统（见卷Ⅱ的第9章）或安全管理器（卷Ⅱ的第10章）拒绝。安全管理器并不常用，而且在 Java 17 后已被废弃。不过，在 Java 9 中，由于 Java API 是模块化的，每个程序都包含模块。<br>例如，本节最后的示例程序会在看 <code>ArrayList</code> 和 <code>Integer</code> 对象的内容。在 Java 9 直到 Java 16 中运行这个程序时，会出现以下警告消息：</p><figure class="highlight asciidoc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs asciidoc"><span class="hljs-symbol">WARNING: </span>An illegal reflective access operation has occurred<br><span class="hljs-symbol">WARNING: </span>Illegal reflective access by objectAnalyzer.ObjectAnalyzer (file:/home/cay /books/cjll/code/vlcb05/bin/) to field java.util.ArrayList.serialVersionUID<br><span class="hljs-symbol">WARNING: </span>Please consider reporting this to the maintainers of objectAnalyzer.ObjectAnalyzer<br><span class="hljs-symbol">WARNING: </span>Use --illegal-access=warn to enable warnings of further illegal reflective access operations<br><span class="hljs-symbol">WARNING: </span>All illegal access operations will be denied in a future release<br></code></pre></td></tr></table></figure><p>在 Java 17 中运行这个程序时，会出现一个 <code>InaccessibleObjectException</code> 异常。<br>要让程序继续运行，需要把 <code>java.base</code> 模块中的 <code>java.util</code> 和 <code>java.lang</code> 包”打开”到”无名模块”。详细内容参见卷Ⅱ的第9章。语法如下：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs bash">java --add-opens java.base/java.util=ALL-UNNAMED \<br>    --add-opens java.base/java.lang=ALL-UNNAMED \<br>    objectAnalyzer.ObjectAnalyzerTest<br></code></pre></td></tr></table></figure><p><strong>注释</strong>：将来的库有可能使用可变句柄（variable handle）而不是反射来读写字段。<code>VarHandle</code> 与 <code>Field</code> 类似，可以用它读写一个特定类任意实例的特定字段。不过，要得到一个 <code>VarHandle</code>，库代码需要一个 <code>Lookup</code> 对象：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> Object <span class="hljs-title function_">getFieldValue</span><span class="hljs-params">(Object obj, String fieldName, Lookup lookup)</span> <br>    <span class="hljs-keyword">throws</span> NoSuchFieldException, IllegalAccessException &#123;<br>    Class&lt;?&gt; cl = obj.getClass();<br>    <span class="hljs-type">Field</span> <span class="hljs-variable">field</span> <span class="hljs-operator">=</span> cl.getDeclaredField(fieldName);<br>    <span class="hljs-type">VarHandle</span> <span class="hljs-variable">handle</span> <span class="hljs-operator">=</span> MethodHandles.privateLookupIn(cl, lookup).unreflectVarHandle(field);<br>    <span class="hljs-keyword">return</span> handle.get(obj);<br>&#125;<br></code></pre></td></tr></table></figure><p>如果生成这个 <code>Lookup</code> 对象的模块有访问这个字段的权限，那么这种做法是可行的。模块中有些方法会直接调用 <code>MethodHandles.lookup()</code>，这会得到封装了调用者访问权限的一个对象。采用这种方式，一个模块可以为另一个模块提供访问私有成员的权限。实际问题是如何能在提供这些权限的同时尽量减少麻烦。</p><p>尽管仍然可以这么做，不过我们来看一个可用于任意类的通用 <code>toString</code> 方法（见程序清单5-16）。这个通用 <code>toString</code> 方法使用 <code>getDeclaredFields</code> 获得所有实例字段，然后使用 <code>setAccessible</code> 便利方法将所有的字段设置为可访问的。对于每个字段，将获得名字和值。通过递归调用 <code>toString</code> 方法，将每个值转换成字符串。</p><p>这个通用的 <code>toString</code> 方法需要解决几个复杂的问题。引用循环有可能导致无限递归。因此，<code>ObjectAnalyzer</code>（见程序清单 5-17）会跟踪已访问过的对象。另外，为了能够查看数组内部，需要采用一种不同的方法。有关这种方法的具体内容将在下一节中详细介绍。</p><p>可以使用这个 <code>toString</code> 方法查看任意对象的内部信息。例如，下面这个调用</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">var</span> <span class="hljs-variable">squares</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">ArrayList</span>&lt;Integer&gt;();<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">1</span>; i &lt;= <span class="hljs-number">5</span>; i++) squares.add(i * i);<br>System.out.println(<span class="hljs-keyword">new</span> <span class="hljs-title class_">ObjectAnalyzer</span>().toString(squares));<br></code></pre></td></tr></table></figure><p>将生成以下结果：</p><figure class="highlight stylus"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs stylus">java<span class="hljs-selector-class">.util</span><span class="hljs-selector-class">.ArrayList</span><span class="hljs-selector-attr">[elementData=class java.lang.Object]</span><span class="hljs-selector-attr">[java.lang.Integer[value=1]</span><span class="hljs-selector-attr">[]</span><span class="hljs-selector-attr">[]</span>],<br>java<span class="hljs-selector-class">.lang</span><span class="hljs-selector-class">.Integer</span><span class="hljs-selector-attr">[value=4]</span><span class="hljs-selector-attr">[]</span><span class="hljs-selector-attr">[]</span>],java<span class="hljs-selector-class">.lang</span><span class="hljs-selector-class">.Integer</span><span class="hljs-selector-attr">[value=9]</span><span class="hljs-selector-attr">[]</span><span class="hljs-selector-attr">[]</span>,<br>java<span class="hljs-selector-class">.lang</span><span class="hljs-selector-class">.Integer</span><span class="hljs-selector-attr">[value=16]</span><span class="hljs-selector-attr">[]</span><span class="hljs-selector-attr">[]</span>],<br>java<span class="hljs-selector-class">.lang</span><span class="hljs-selector-class">.Integer</span><span class="hljs-selector-attr">[value=25]</span><span class="hljs-selector-attr">[]</span><span class="hljs-selector-attr">[]</span>],null,null,null,null,null,size=<span class="hljs-number">5</span>]<span class="hljs-selector-attr">[modCount=5]</span><span class="hljs-selector-attr">[]</span><span class="hljs-selector-attr">[]</span>]<br></code></pre></td></tr></table></figure><p>还可以使用这个通用的 <code>toString</code> 方法实现你的自定义类的 <code>toString</code> 方法，如下所示：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> String <span class="hljs-title function_">toString</span><span class="hljs-params">()</span><br>&#123;<br>    <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">ObjectAnalyzer</span>().toString(<span class="hljs-built_in">this</span>);<br>&#125;<br></code></pre></td></tr></table></figure><p>这样可以轻松地提供一个通用 <code>toString</code> 方法，无疑也很有用。不过，先不要高兴得太早，不要以为再也不用实现 <code>toString</code> 了，记住：能够不受控地访问类内部的日子将屈指可数。</p><h4 id="5-9-6-使用反射编写泛型数组代码"><a href="#5-9-6-使用反射编写泛型数组代码" class="headerlink" title="5.9.6 使用反射编写泛型数组代码"></a>5.9.6 使用反射编写泛型数组代码</h4><p><code>java.lang.reflect</code> 包中的 <code>Array</code> 类允许动态地创建数组。例如，<code>Arrays</code> 类的 <code>copyOf</code> 方法实现中就使用了这个类。应该记得，这个方法可以用于扩展一个已经填满的数组。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">var</span> <span class="hljs-variable">a</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Employee</span>[<span class="hljs-number">100</span>];<br>...<br><span class="hljs-comment">// array is full</span><br>a = Arrays.copyOf(a, <span class="hljs-number">2</span> * a.length);<br></code></pre></td></tr></table></figure><p>如何编写这样一个通用的方法呢？好在 <code>Employee[]</code> 数组能够转换为 <code>Object[]</code> 数组，这听起来很有希望。下面进行第一次尝试：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> Object[] badCopyOf(Object[] a, <span class="hljs-type">int</span> newLength) <span class="hljs-comment">// not useful</span><br>&#123;<br>    <span class="hljs-type">var</span> <span class="hljs-variable">newArray</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Object</span>[newLength];<br>    System.arraycopy(a, <span class="hljs-number">0</span>, newArray, <span class="hljs-number">0</span>, Math.min(a.length, newLength));<br>    <span class="hljs-keyword">return</span> newArray;<br>&#125;<br></code></pre></td></tr></table></figure><p>不过，在实际使用得到的数组时会遇到一个问题。这段代码返回的数组类型是一个对象数组 (<code>Object[]</code>)，这是因为我们使用了下面运行代码来创建这个数组：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">new</span> <span class="hljs-title class_">Object</span>[newLength]<br></code></pre></td></tr></table></figure><p>对象数组不能强制转换成员工数组 (<code>Employee[]</code>)。如果这样做，Java 虚拟机会在运行时生成一个 <code>ClassCastException</code> 异常。这里的关键是，前面已经提到，Java 数组会记住每个元素的类型，即创建数组时 <code>new</code> 表达式中使用的元素类型。将一个 <code>Employee[]</code> 临时转换成 <code>Object[]</code> 数组，然后再把它转换回来是可以的，但一个从开始就是 <code>Object[]</code> 的数组却永远不能转换成 <code>Employee[]</code> 数组。为了编写这类通用的数组代码，需要能够创建与原数组类型相同的新数组。为此，需要使用 <code>java.lang.reflect</code> 包中 <code>Array</code> 类的一些方法。其中，最关键的是 <code>Array</code> 类的静态方法 <code>newInstance</code>，这个方法能够构造一个新数组。在调用这个方法时必须提供两个参数，一个是数组的元素类型，另一个是期望的数组长度。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">Object</span> <span class="hljs-variable">newArray</span> <span class="hljs-operator">=</span> Array.newInstance(componentType, newLength);<br></code></pre></td></tr></table></figure><p>为了具体执行这个调用，需要获得新数组的长度和元素类型。<br>可以通过调用 <code>Array.getLength(a)</code> 获得数组的长度。<code>Array</code> 类的静态 <code>getLength</code> 方法会返回一个数组的长度。要获得新数组的元素类型，需要完成以下工作：</p><ol><li>首先获得 <code>a</code> 的类对象。</li><li>确认它确实是一个数组。</li><li>使用 <code>Class</code> 类的 <code>getComponentType</code> 方法（只为表示数组的类对象定义了这个方法）得到数组的正确类型。</li><li>反过来，对于表示类 <code>C</code> 的 <code>Class</code> 对象，<code>arrayType</code> 方法会生成表示 <code>C[]</code> 的 <code>Class</code> 对象。</li></ol><p>为什么 <code>getLength</code> 是 <code>Array</code> 的方法，而 <code>getComponentType</code> 是 <code>Class</code> 的方法呢？我们也不清楚——反射方法的分布有时候确实显得有点古怪。</p><p>下面给出这段代码：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> Object <span class="hljs-title function_">goodCopyOf</span><span class="hljs-params">(Object a, <span class="hljs-type">int</span> newLength)</span><br>&#123;<br>    <span class="hljs-type">Class</span> <span class="hljs-variable">cl</span> <span class="hljs-operator">=</span> a.getClass();<br>    <span class="hljs-keyword">if</span> (!cl.isArray()) <span class="hljs-keyword">return</span> <span class="hljs-literal">null</span>;<br>    <span class="hljs-type">Class</span> <span class="hljs-variable">componentType</span> <span class="hljs-operator">=</span> cl.getComponentType();<br>    <span class="hljs-type">int</span> <span class="hljs-variable">length</span> <span class="hljs-operator">=</span> Array.getLength(a);<br>    <span class="hljs-type">Object</span> <span class="hljs-variable">newArray</span> <span class="hljs-operator">=</span> Array.newInstance(componentType, newLength);<br>    System.arraycopy(a, <span class="hljs-number">0</span>, newArray, <span class="hljs-number">0</span>, Math.min(length, newLength));<br>    <span class="hljs-keyword">return</span> newArray;<br>&#125;<br></code></pre></td></tr></table></figure><p>请注意，这个 <code>copyOf</code> 方法可以用来扩展任意类型的数组，而不仅是对象数组。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">int</span>[] a = &#123; <span class="hljs-number">1</span>, <span class="hljs-number">2</span>, <span class="hljs-number">3</span>, <span class="hljs-number">4</span>, <span class="hljs-number">5</span> &#125;;<br>a = (<span class="hljs-type">int</span>[]) goodCopyOf(a, <span class="hljs-number">10</span>);<br></code></pre></td></tr></table></figure><p>为了使用这个方法，要将 <code>goodCopyOf</code> 的参数声明为 <code>Object</code> 类型，而不是一个对象数组（<code>Object[]</code>）。整型数组类型 <code>int[]</code> 可以转换为一个 <code>Object</code>，而不是转换成对象数组！</p><h4 id="5-9-7-调用任意方法和构造器"><a href="#5-9-7-调用任意方法和构造器" class="headerlink" title="5.9.7 调用任意方法和构造器"></a>5.9.7 调用任意方法和构造器</h4><p>在 C 和 C++ 中，可以通过一个函数指针执行任意函数。从表面上看，Java 没有提供方法指针，也就是说，Java 没有提供途径将一个方法的存储地址传给另外一个方法，以便第二个方法以后调用。事实上，Java 的设计者曾说过：方法指针很危险，而且很容易出错。他们认为 Java 的接口（interface）和 lambda 表达式（将在下一章讨论）是一种更好的解决方案。不过，反射机制允许你调用任意的方法。</p><p>回想一下，可以用 <code>Field</code> 类的 <code>get</code> 方法查看一个对象的字段。与之类似，<code>Method</code> 类有一个 <code>invoke</code> 方法，允许你调用包含当前 <code>Method</code> 对象中的方法。<code>invoke</code> 方法的签名为：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">Object <span class="hljs-title function_">invoke</span><span class="hljs-params">(Object obj, Object... args)</span><br></code></pre></td></tr></table></figure><p>第一个参数是隐含参数，其余的对象提供了显式参数。<br>对于静态方法，第一个参数会忽略，即可以将它设置为 <code>null</code>。</p><p>例如，假设用 <code>m1</code> 表示 <code>Employee</code> 类的 <code>getName</code> 方法，下面这条语句显示了如何调用这个方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">String</span> <span class="hljs-variable">n</span> <span class="hljs-operator">=</span> (String) m1.invoke(harry);<br></code></pre></td></tr></table></figure><p>如果返回类型是基本类型，则 <code>invoke</code> 方法会返回包装器类型。例如，假设 <code>m2</code> 表示 <code>Employee</code> 类的 <code>getSalary</code> 方法，那么返回的对象实际上是一个 <code>Double</code>，必须相应地完成强制类型转换。可以使用自动拆箱将它转换为一个 <code>double</code>：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">double</span> <span class="hljs-variable">s</span> <span class="hljs-operator">=</span> (Double) m2.invoke(harry);<br></code></pre></td></tr></table></figure><p>如何得到 <code>Method</code> 对象呢？当然，可以调用 <code>getDeclaredMethods</code> 方法，然后搜索返回的 <code>Method</code> 对象数组，直到发现想要的方法为止。也可以调用 <code>Class</code> 类的 <code>getMethod</code> 方法。这与 <code>getField</code> 方法类似。<code>getField</code> 方法接受一个表示字段名的字符串，返回一个 <code>Field</code> 对象。不过，有可能存在若干个同名的方法，因此要准确地得到想要的那个方法必须格外小心。有鉴于此，还必须提供想要的方法的参数类型。<code>getMethod</code> 的签名为：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">Method <span class="hljs-title function_">getMethod</span><span class="hljs-params">(String name, Class... parameterTypes)</span><br></code></pre></td></tr></table></figure><p>例如，下面展示了如何获得 <code>Employee</code> 类的 <code>getName</code> 方法和 <code>raiseSalary</code> 方法的方法指针：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">Method</span> <span class="hljs-variable">m1</span> <span class="hljs-operator">=</span> Employee.class.getMethod(<span class="hljs-string">&quot;getName&quot;</span>);<br><span class="hljs-type">Method</span> <span class="hljs-variable">m2</span> <span class="hljs-operator">=</span> Employee.class.getMethod(<span class="hljs-string">&quot;raiseSalary&quot;</span>, <span class="hljs-type">double</span>.class);<br></code></pre></td></tr></table></figure><p>可以使用类似的方法调用任意的构造器。将构造器的参数类型提供给 <code>Class.getConstructor</code> 方法，并为 <code>Constructor.newInstance</code> 方法提供参数值：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">Class</span> <span class="hljs-variable">cl</span> <span class="hljs-operator">=</span> Random.class; <span class="hljs-comment">// or any other class with a constructor that</span><br>                         <span class="hljs-comment">// accepts a long parameter</span><br><span class="hljs-type">Constructor</span> <span class="hljs-variable">cons</span> <span class="hljs-operator">=</span> cl.getConstructor(<span class="hljs-type">long</span>.class);<br><span class="hljs-type">Object</span> <span class="hljs-variable">obj</span> <span class="hljs-operator">=</span> cons.newInstance(<span class="hljs-number">42L</span>);<br></code></pre></td></tr></table></figure><p><strong>注释</strong>：<code>Method</code> 和 <code>Constructor</code> 类扩展了 <code>Executable</code> 类。在 Java 17 中，<code>Executable</code> 类是密封类，只允许 <code>Method</code> 和 <code>Constructor</code> 作为子类。</p><p>到此为止，我们已经了解了使用 <code>Method</code> 对象的规则。下面来看如何具体使用。程序清单 5-19 中的程序会打印一个数字函数（如 <code>Math.sqrt</code> 或 <code>Math.sin</code>）的取值表。打印的结果如下所示：</p><figure class="highlight apache"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs apache"><span class="hljs-attribute">public</span> static native double java.lang.Math.sqrt(double)<br>    <span class="hljs-attribute">1</span>.<span class="hljs-number">0000</span>    <span class="hljs-number">1</span>.<span class="hljs-number">0000</span><br>    <span class="hljs-attribute">2</span>.<span class="hljs-number">0000</span>    <span class="hljs-number">1</span>.<span class="hljs-number">4142</span><br>    <span class="hljs-attribute">3</span>.<span class="hljs-number">0000</span>    <span class="hljs-number">1</span>.<span class="hljs-number">7321</span><br> <span class="hljs-attribute">4</span>.<span class="hljs-number">0000</span>   <span class="hljs-number">2</span>.<span class="hljs-number">0000</span> <br> <span class="hljs-attribute">5</span>.<span class="hljs-number">0000</span>   <span class="hljs-number">2</span>.<span class="hljs-number">2361</span> <br> <span class="hljs-attribute">6</span>.<span class="hljs-number">0000</span>   <span class="hljs-number">2</span>.<span class="hljs-number">4495</span> <br> <span class="hljs-attribute">7</span>.<span class="hljs-number">0000</span>   <span class="hljs-number">2</span>.<span class="hljs-number">6458</span> <br> <span class="hljs-attribute">8</span>.<span class="hljs-number">0000</span>   <span class="hljs-number">2</span>.<span class="hljs-number">8284</span> <br> <span class="hljs-attribute">9</span>.<span class="hljs-number">0000</span>   <span class="hljs-number">3</span>.<span class="hljs-number">0000</span> <br> <span class="hljs-attribute">10</span>.<span class="hljs-number">0000</span>  <span class="hljs-number">3</span>.<span class="hljs-number">1623</span> <br></code></pre></td></tr></table></figure><p>当然，打印表格的代码与表格中计算的数学函数无关。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">double</span> <span class="hljs-variable">dx</span> <span class="hljs-operator">=</span> (to - from) / (n - <span class="hljs-number">1</span>);<br><span class="hljs-keyword">for</span> (<span class="hljs-type">double</span> <span class="hljs-variable">x</span> <span class="hljs-operator">=</span> from; x &lt;= to; x += dx)<br>&#123;<br>    <span class="hljs-type">double</span> <span class="hljs-variable">y</span> <span class="hljs-operator">=</span> (Double) f.invoke(<span class="hljs-literal">null</span>, x);<br>    System.out.printf(<span class="hljs-string">&quot;%10.4f | %10.4f%n&quot;</span>, x, y);<br>&#125;<br></code></pre></td></tr></table></figure><p>在这里，<code>f</code> 是一个 <code>Method</code> 类型的对象。由于我们调用的方法是一个静态方法，所以 <code>invoke</code> 的第一个参数是 <code>null</code>。</p><p>要打印 <code>Math.sqrt</code> 函数的取值表，可以如下设置 <code>f</code>：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">Math.class.getMethod(<span class="hljs-string">&quot;sqrt&quot;</span>, <span class="hljs-type">double</span>.class)<br></code></pre></td></tr></table></figure><p>这是 <code>Math</code> 类的一个方法，名为 <code>sqrt</code>，有一个 <code>double</code> 类型的参数。</p><p>这个例子清楚地表明，利用 <code>Method</code> 对象可以实现 C 语言中函数指针（或 C# 中的委托）所能完成的所有操作。同 C 中一样，这种编程风格不是很方便，而且总是很容易出错。如果在调用方法的时候提供了错误的参数会发生什么？<code>invoke</code> 方法将会抛出一个异常。</p><p>另外，<code>invoke</code> 的参数和返回值必须是 <code>Object</code> 类型。这就意味着必须来回进行多次强制类型转换。这样一来，编译器会丧失检查代码的机会，以至于等到测试阶段才会发现错误，而这个时候查找和修正错误会麻烦得多。不仅如此，使用反射获得方法指针的代码要比直接调用方法的代码慢得多。</p><p>有鉴于此，建议仅在绝对必要的时候才在你自己的程序中使用 <code>Method</code> 对象。通常，更好的做法是使用接口以及 Java 8 引入的 lambda 表达式（第 6 章中介绍）。特别要强调：我们建议 Java 开发人员不要使用回调函数的 <code>Method</code> 对象。可以使用回调的接口，这样不仅代码的执行速度更快，也更易于维护。</p><h3 id="5-10-继承的设计技巧"><a href="#5-10-继承的设计技巧" class="headerlink" title="5.10 继承的设计技巧"></a>5.10 继承的设计技巧</h3><p>在本章的最后，我会给出使用继承时很有用的一些技巧。</p><ol><li><p><strong>将公共操作和字段放在超类中。</strong><br>正是因为这个原因，我们将姓名字段放在 <code>Person</code> 类中，而没有将它重复放在 <code>Employee</code> 和 <code>Student</code> 类中。</p></li><li><p><strong>不要使用受保护的字段。</strong><br>有些程序员认为，将大多数的实例字段定义为 <code>protected</code> 是一个不错的主意，”以防万一”，这样子类就能够在需要的时候访问这些字段。不过，<code>protected</code> 机制并不能提供太多保护，这有两方面的原因。第一，子类集合是无限制的，任何一个人都能够由你的类派生一个子类，然后编写代码直接访问 <code>protected</code> 实例字段，从而破坏封装性。第二，在 Java 中，同一个包中的所有类都可以访问 <code>protected</code> 字段，而不论它们是否为这个类的子类。<br>不过，有些方法不打算作为通用方法，要在子类中重新定义，<code>protected</code> 方法对于指示这种方法可能很有用。</p></li><li><p><strong>使用继承实现 “is-a” 关系。</strong><br>使用继承很容易达到节省代码量的目的，但有时候也会被人们滥用。例如，假设需要定义一个 <code>Contractor</code> 类。钟点工有姓名和雇用日期，但是没有工资。他们按小时计薪，并且不会因为拖延时间而获得加薪。这似乎在诱导人们由 <code>Employee</code> 派生出子类 <code>Contractor</code>，然后再增加一个 <code>hourlyWage</code> 字段。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Contractor</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Employee</span><br>&#123;<br>    <span class="hljs-keyword">private</span> <span class="hljs-type">double</span> hourlyWage;<br>&#125;<br></code></pre></td></tr></table></figure><p>不过，这并不是一个好主意。因为这样一来，每个钟点工对象中都同时包含了工资和时薪这两个字段。在实现打印薪水或税单的方法时，这会带来无尽的麻烦。与不使用继承相比，使用继承的做法最后反而会多写很多代码。<br>钟点工与员工之间不是一种 “is-a” 关系。钟点工不是员工的一个特例。</p></li><li><p><strong>除非所有继承的方法都有意义，否则不要使用继承。</strong><br>假设我们想编写一个 <code>Holiday</code> 类。毫无疑问，每个假日也是一天，并且一天可以用 <code>GregorianCalendar</code> 类的实例表示，因此可以使用继承。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Holiday</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">GregorianCalendar</span> &#123; ... &#125;<br></code></pre></td></tr></table></figure><p>很遗憾，在继承的操作中，假日集合不是闭合的。<code>GregorianCalendar</code> 中有一个公共方法 <code>add</code>，这个方法可以将假日转换成非假日：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java">Holiday christmas;<br>christmas.add(Calendar.DAY_OF_MONTH, <span class="hljs-number">12</span>);<br></code></pre></td></tr></table></figure><p>因此，继承对于这个例子来说不太适合。</p></li></ol><p>​需要指出，如果扩展一个不可变的类，就不会出现这个问题。假设有一个不可变的日期类，类似 <code>LocalDate</code> 但不是 <code>final</code> 类。如果派生一个 <code>Holiday</code> 子类，就没有任何方法能够把假日变成非假日。</p><ol start="5"><li><p><strong>覆盖方法时，不要改变预期的行为。</strong><br>替换原则不仅应用于语法，更重要的是，它也适用于行为。覆盖一个方法的时候，不应该毫无缘由地改变它的行为。就这一点而言，编译器不会提供任何帮助，编译器不会检查你重新定义的行为是否有意义。例如，可以重新定义 <code>add</code> 来”修正” <code>Holiday</code> 类中 <code>add</code> 方法的问题，可能让它什么也不做，或者抛出一个异常，或者是前进到下一个假日。<br>不过，这种”修正”会违反替换原则。对于以下语句序列</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">int</span> <span class="hljs-variable">d1</span> <span class="hljs-operator">=</span> x.get(Calendar.DAY_OF_MONTH);<br>x.add(Calendar.DAY_OF_MONTH, <span class="hljs-number">1</span>);<br><span class="hljs-type">int</span> <span class="hljs-variable">d2</span> <span class="hljs-operator">=</span> x.get(Calendar.DAY_OF_MONTH);<br>System.out.println(d2 - d1);<br></code></pre></td></tr></table></figure><p>不管 <code>x</code> 的类型是 <code>GregorianCalendar</code> 还是 <code>Holiday</code>，执行上述语句都应该有预期的行为。<br>当然，这是个难题。理智和不理智的人们可能就预期行为是什么争论不休。例如，有些人争论说，替换原则要求 <code>Manager.equals</code> 忽略 <code>bonus</code> 字段，因为 <code>Employee.equals</code> 就忽略了这个字段。实际上，凭空讨论这些问题毫无意义。归根结底，关键在于在子类中覆盖方法时，不要偏离最初的设计初衷。</p></li><li><p><strong>使用多态，而不要使用类型信息。</strong><br>只要看到以下形式的代码</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">if</span> (x is of type <span class="hljs-number">1</span>)<br>    action1(x);<br><span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (x is of type <span class="hljs-number">2</span>)<br>    action2(x);<br></code></pre></td></tr></table></figure><p>都应该考虑使用多态。<br><code>action1</code> 与 <code>action2</code> 表示的是一个通用概念吗？如果是，就应该将这个概念定义为这两个类型的公共超类或接口中的一个方法。然后，就可以调用</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">x.action();<br></code></pre></td></tr></table></figure><p>并利用多态性固有的动态分派机制执行正确的动作。<br>与使用多个类型检测的代码相比，使用多态方法或接口实现的代码更易于维护和扩展。</p></li><li><p><strong>不要滥用反射。</strong><br>反射机制使人们可以在运行时查看字段和方法，从而能编写出极具通用性的程序。这种功能对于系统编程极其有用，但是通常并不适合编写应用程序。反射很脆弱，如果使用反射，编译器将无法帮助你查找编程错误，直到运行时才会发现错误并导致异常。</p><p>现在已经了解了 Java 如何支持面向对象编程的基础：类、继承和多态。下一章中我们将介绍两个高级主题：接口和 lambda 表达式。它们对于有效地使用 Java 非常重要。</p></li></ol>]]></content>
    
    
    <categories>
      
      <category>Java</category>
      
    </categories>
    
    
    <tags>
      
      <tag>原创</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>《Java核心技术卷一》第三章</title>
    <link href="/2025/08/29/%E3%80%8AJava%E6%A0%B8%E5%BF%83%E6%8A%80%E6%9C%AF%E5%8D%B7%E4%B8%80%E3%80%8B%E7%AC%AC%E4%B8%89%E7%AB%A0/"/>
    <url>/2025/08/29/%E3%80%8AJava%E6%A0%B8%E5%BF%83%E6%8A%80%E6%9C%AF%E5%8D%B7%E4%B8%80%E3%80%8B%E7%AC%AC%E4%B8%89%E7%AB%A0/</url>
    
    <content type="html"><![CDATA[<h3 id="基础知识"><a href="#基础知识" class="headerlink" title="基础知识"></a>基础知识</h3><p>Java 区分大小写。</p><p>类是所有 Java 应用的构建模块，Java 程序中的所有内容都必须放在类中。</p><p>类名必须以字母开头，不能使用 Java 保留字作为类名。</p><p>标准命名（骆驼命名）：类名是以大写字母开头的名词。如果名字由多个单词组成，每个单词的第一个字母都应该大写。</p><p>源代码的文件名必须与公共类的类名相同。</p><p>访问修饰符   关键字 public</p><p>main 方法必须声明为 public 。运行一个已编译的程序时，Java 虚拟机总是从指定类中 main 方法的代码开始执行。</p><p>Java 中的所有函数都是某个类的方法，Java 中的 main 方法必须有一个外壳类。</p><p>Java 中的 main 方法总是静态的，关键字 void 表示这个方法不返回值。</p><p>main 方法不会为操作系统返回一个 “ 退出码 ” ，如果 main 方法正常退出，那么 Java 程序的退出码为 0 。</p><p>在 Java 中，每个语句必须用分号结束，回车不是语句的结束标志。</p><p>点号（.）用于调用方法。</p><p>Java 中的方法可以没有参数，也可以有一个或多个参数（实参），即使一个方法没有参数，也需要使用空括号。</p><p>从 &#x2F;&#x2F; 开始到本行结尾都是注释。</p><p>&#x2F;* * &#x2F;表示一段比较长的注释。注意：&#x2F;*  *&#x2F; 注释不能嵌套。</p><p>Java 是一种强类型语言，必须为每一个变量声明一个类型。</p><p>Java 中有一个能够表示任意精度的算术包，所谓的**“大数”**是 Java 对象，而不是一个基本 Java 类型。</p><h3 id="类型"><a href="#类型" class="headerlink" title="类型"></a>类型</h3><h4 id="整型"><a href="#整型" class="headerlink" title="整型"></a>整型</h4><p>整型用于表示没有小数部分的数，可以是负数。</p><table><thead><tr><th>整型</th><th>存储需求</th><th>取值范围</th></tr></thead><tbody><tr><td>int</td><td>4字节</td><td>- $2^31$ ~ $2^31$ - 1</td></tr><tr><td>short</td><td>2字节</td><td>- $2^15$ ~ $2^15$ - 1</td></tr><tr><td>long</td><td>8字节</td><td>- $2^63$ ~ $2^63$ - 1</td></tr><tr><td>byte</td><td>1字节</td><td>- $2^7$ ~ $2^7$ -1</td></tr></tbody></table><p>整型的范围与运行 Java 代码的机器无关，所以各种数据类型的取值范围是固定的。</p><p>长整型数值有一个后缀 L 和 l 。十六进制数值有一个前缀 0x 或 0X 。八进制有一个前缀 0 。二进制前缀 0b 或 0B 。</p><p>Java 没有无符号形式的 int、long、short、byte。</p><h4 id="浮点类型"><a href="#浮点类型" class="headerlink" title="浮点类型"></a>浮点类型</h4><p>浮点类型用于表示有小数部分的数值。</p><p>double 表示这种类型的数值精度是 float 类型的两倍</p><p>float 类型的数值有一个后缀 F 和 f</p><p>没有后缀 F 的浮点数值总是默认为 double 类型（后缀 d 或 D ）</p><p>可以使用十六进制表示浮点数字面量，使用 p 表示指数，尾数采用十六进制，指数采用十进制，指数基数是2  （0.125 写为  0x1.0p-3）</p><p>有3个特殊的浮点数值表示溢出和出错情况：正无穷大 、负无穷大、NaN（不是一个数）（所有 NaN 的数值都认为是不同的）</p><p>浮点数值不适用于无法接受舍入误差的金融计算</p><h4 id="char-类型"><a href="#char-类型" class="headerlink" title="char 类型"></a>char 类型</h4><p>char 类型的字面量值要用单引号括起来</p><p>char 类型的值可以表示为十六进制值，其范围从 \u0000~\uFFFF。</p><p>转义序列 \u 还可以在加引号字符常量和字符串之外使用（而其他所有转义序列不可以。）</p><p>可以在加引号的字符字面量或字符串中使用这些转义序列。</p><p>Unicode 转义序列会在解析代码之前处理。</p><p>一定要当心注释中的 \u。</p><p>特殊字符的转义序列</p><table><thead><tr><th>转义序列</th><th>名称</th><th>Unicode值</th></tr></thead><tbody><tr><td>\b</td><td>退格</td><td>\u0008</td></tr><tr><td>\t</td><td>制表</td><td>\u0009</td></tr><tr><td>\n</td><td>换行</td><td>\u000a</td></tr><tr><td>\r</td><td>回车</td><td>\u000d</td></tr><tr><td>\f</td><td>换页</td><td>\u000c</td></tr><tr><td>\“</td><td>双引号</td><td>\u0022</td></tr><tr><td>\‘</td><td>单引号</td><td>\u0027</td></tr><tr><td>\\</td><td>反斜线</td><td>\u005c</td></tr><tr><td>\s</td><td>空格。在文本块中用来保留末尾空白符</td><td>\u0020</td></tr><tr><td>\newline</td><td>只在文本块中使用：连接这一行和下一行</td><td></td></tr></tbody></table><p><strong>码点</strong>（code point）是指与一个编码表中的某个字符对应的代码值。在 Unicode 标准中，码点采用十六进制书写，并加上前缀 U+ 。Unicode 的码点可以分成17个<strong>代码平面</strong>（code plane）。第一个代码平面称为基本多语言平面（basic multilingual plane），包括码点从 U+0000 到 U+FFFF 的“经典” Unicode 代码；其余的16个平面的码点从 U+10000 到 U+10FFFF ， 包含各种辅助字符（supplementary character）。</p><p>UTF-16 编码采用不同长度的代码表示所有 Unicode 码点。在基本多语言平面中，每个字符用16位表示，通常称为代码单元（code unit）；而辅助字符编码为一对连续的代码单元。采用这种编码对表示的每个值都属于基本多语言平面中未用的2048个值范围，通常称为替代区域（surrogate area）（U+D800<del>U+DBFF用于第一个代码单元，U+DC00</del>U+DFFF用于第二个代码单元）。</p><p>在 Java 中，char 类型描述了采用 UTF-16 编码的一个代码单元。</p><h4 id="boolean类型"><a href="#boolean类型" class="headerlink" title="boolean类型"></a>boolean类型</h4><p>boolean（布尔）类型有两个值：false 和 true，用来判定逻辑条件。整型值和布尔值之间不能进行相互转换。</p><h3 id="变量与常量"><a href="#变量与常量" class="headerlink" title="变量与常量"></a>变量与常量</h3><p>Java 使用变量来存储值。常量就是值不变的变量。</p><p>在 Java 中，每个变量都有一个类型（type）。声明一个变量时，先指定变量的类型，然后是变量名。注意每个声明都以分号结束。</p><p>作为变量名（以及其他名字）的标识符由字母、数字、货币符号以及“标点连接符”组成。需遵循以下规则：</p><ul><li>第一个字符不能是数字，<code>@</code>、<code>+</code>之类的符号也不能出现在变量名中，空格也不行；</li><li>字母区分大小写（如 <code>main</code>和 <code>Main</code>是不同的标识符）；</li><li>标识符的长度基本上没有限制。</li></ul><p>与大多数程序设计语言相比，Java 中“字母”和“货币符号”的范围更大：</p><ul><li><strong>字母</strong>：指一种语言中表示字母的任何 Unicode 字符（例如，德国用户可在变量名中使用字母 <code>ß</code>；讲希腊语的人可使用 <code>π</code>）；</li><li><strong>数字</strong>：包括 <code>0-9</code>和表示一位数字的任何 Unicode 字符；</li><li><strong>货币符号</strong>：如 <code>$</code>、<code>€</code>、<code>₹</code>等；</li><li><strong>标点连接符</strong>：包括下画线 <code>_</code>、“波浪线”<code>~</code>及其他一些符号（实际开发中，多数程序员常用 <code>A-Z</code>、<code>a-z</code>、<code>0-9</code>和下画线）。</li></ul><p>尽管<code>$</code>是一个合法的标识符字符，但不要在你自己的代码中使用这个字符（它只用于 Java 编译器或其他工具生成的名字）</p><p>不能使用 Java 关键字作为变量名</p><p>在Java 9 中，单下画线 <code>_</code>是保留字。</p><p>可以在一行中声明多个变量。</p><h4 id="初始化变量"><a href="#初始化变量" class="headerlink" title="初始化变量"></a>初始化变量</h4><p>声明一个变量之后，必须用赋值语句显式地初始化变量，千万不要使用未初始化的变量的值。</p><p>要想对一个已声明的变量进行赋值，需要将变量名放在等号（&#x3D;）左侧，再把一个有适当值的 Java 表达式放在等号的右侧。也可以将变量的声明和初始化放在同一行中。最后，Java 中可以将声明放在代码中的任何地方。</p><p>在 Java 中，并不区分变量的声明和定义。</p><p>注释：从 Java 10 开始，对于局部变量，如果可以从变量的初始值推断出它的类型，就不再需要声明类型。只需要使用关键字 var 而无须指定类型。</p><h4 id="常量"><a href="#常量" class="headerlink" title="常量"></a>常量</h4><p>在 Java 中，可以用关键字 final 指示常量。</p><p>关键字 final 表示这个变量只能被赋值一次。一旦赋值，就不能再更改了。习惯上，常量名使用全大写。</p><p>在 Java 中，可能经常需要创建一个常量以便在一个类的多个方法中使用，通常将这些常量称为类常量（class constant）。可以使用关键字 static final 设置一个类常量。</p><p>需要注意的是，类常量的定义位于 main 方法之外。这样一来，同一个类的其他方法也可以使用这个常量。另外，如果一个常量被声明为 public，那么其他类的方法也可以使用这个常量。</p><p>const 是 Java 保留的关键字。在 Java 中，必须使用 final 声明常量。</p><h4 id="枚举类型"><a href="#枚举类型" class="headerlink" title="枚举类型"></a>枚举类型</h4><p>自定义枚举类型（enumerated type）。枚举类型包括有限个命名值。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">enum</span> <span class="hljs-title class_">Size</span> &#123; SMALL, MEDIUM, LARGE, EXTRA_LARGE &#125;;<br></code></pre></td></tr></table></figure><p>现在，可以声明这种类型的变量：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">Size</span> <span class="hljs-variable">s</span> <span class="hljs-operator">=</span> Size.MEDIUM;<br></code></pre></td></tr></table></figure><p>Size 类型的变量只能存储这个类型声明中所列的某个值，或者特殊值 null，null 表示这个变量没有设置任何值。</p><h4 id="运算符"><a href="#运算符" class="headerlink" title="运算符"></a>运算符</h4><p>在 Java 中，使用通常的算术运算符 +、-、*、&#x2F; 分别表示加、减、乘、除运算。当参与 &#x2F; 运算的两个操作数都是整数时，&#x2F; 表示整数除法；否则，这表示浮点除法。整数的求余操作有时称为取模 (modulus)）用 % 表示。例如，15&#x2F;2 等于 7，15%2 等于 1，15.0&#x2F;2 等于 7.5。</p><p>需要注意，整数被 0 除将产生一个异常，而浮点数被 0 除将会得到一个无穷大或 NaN 结果。</p><p>Math 类中包含你可能会用到的各种数学函数，这取决于你要编写什么类型的程序。</p><p>要想计算一个数的平方根，可以使用 sqrt 方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">double</span> <span class="hljs-variable">x</span> <span class="hljs-operator">=</span> <span class="hljs-number">4</span>;<br><span class="hljs-type">double</span> <span class="hljs-variable">y</span> <span class="hljs-operator">=</span> Math.sqrt(x);  <br>System.out.println(y); <span class="hljs-comment">// prints 2.0</span><br></code></pre></td></tr></table></figure><p>注释：println 方法和 sqrt 方法有一个微小的差异。println 方法处理 System.out 对象，而 Math 类中的 sqrt 方法<em>并不处理任何对象</em>，这样的方法被称为<strong>静态方法</strong>（static method）。</p><p>在 Java 中，没有完成幂运算的运算符，因此必须使用 Math 类的 pow 方法。以下语句：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">double</span> <span class="hljs-variable">y</span> <span class="hljs-operator">=</span> Math.pow(x, a);<br></code></pre></td></tr></table></figure><p>将 y 的值设置为 x 的 a 次幂（xᵃ）。pow 方法有两个 double 类型的参数，其返回结果也为 double 类型。</p><p>提示：不必在数学方法名和常量名前添加前缀 “Math”，只要在源文件最上面加上下面这行代码就可以了。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">import</span> <span class="hljs-keyword">static</span> java.lang.Math.*;<br></code></pre></td></tr></table></figure><h4 id="数据类型转换"><a href="#数据类型转换" class="headerlink" title="数据类型转换"></a>数据类型转换</h4><p><img src="/2025/05/04/hello-world/%E7%AC%AC%E4%B8%89%E7%AB%A0/IMG20251026163247.jpg" alt="IMG20251026163247"></p><p>在图 3-1 中有 6 个实线箭头，表示无信息丢失的转换；另外有 3 个虚线箭头，表示可能有精度损失的转换。</p><p>当用一个二元运算符连接两个值时（例如 n + f，n 是整数，f 是浮点数），先要将两个操作数转换为同一种类型，然后再进行计算。</p><ul><li>如果两个操作数中有一个是 double 类型，另一个操作数就会转换为 double 类型。</li><li>否则，如果其中一个操作数是 float 类型，另一个操作数将会转换为 float 类型。</li><li>否则，如果其中一个操作数是 long 类型，另一个操作数将会转换为 long 类型。</li><li>否则，两个操作数都将被转换为 int 类型。</li></ul><h4 id="强制类型转换"><a href="#强制类型转换" class="headerlink" title="强制类型转换"></a>强制类型转换</h4><p>在必要的时候，int 类型的值将会自动地转换为 double 类型。但另一方面，有时也需要将 double 类型转换成 int 类型。在 Java 中，允许进行这种数值转换，不过当然可能会丢失一些信息。这种可能损失信息的转换要通过**强制类型转换（cast）**来完成。强制类型转换的语法格式是在圆括号中指定想要转换的目标类型，后面紧跟待转换的变量名。例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">double</span> <span class="hljs-variable">x</span> <span class="hljs-operator">=</span> <span class="hljs-number">9.997</span>;<br><span class="hljs-type">int</span> <span class="hljs-variable">nx</span> <span class="hljs-operator">=</span> (<span class="hljs-type">int</span>) x;<br></code></pre></td></tr></table></figure><p>这样，变量 nx 的值为9，因为强制类型转换通过<strong>截断小数部分</strong>将浮点值转换为整型。</p><p>如果想<strong>舍入（round）</strong>(四舍五入) 一个浮点数来得到最接近的整数，可以使用 Math.round 方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">double</span> <span class="hljs-variable">x</span> <span class="hljs-operator">=</span> <span class="hljs-number">9.997</span>;<br><span class="hljs-type">int</span> <span class="hljs-variable">nx</span> <span class="hljs-operator">=</span> (<span class="hljs-type">int</span>) Math.round(x);<br></code></pre></td></tr></table></figure><p>现在，变量 nx 的值为10。调用 round 时，仍然需要使用强制类型转换（int）。原因是 round 方法的返回值是 long 类型，由于存在信息丢失的可能性，所以只有使用显式的强制类型转换才能够将一个 long 值赋给 int类 型的变量。</p><p>警告：如果试图将一个数从一种类型强制转换为另一种类型，而又超出了目标类型的表示范围，结果就会截断成一个完全不同的值。例如，（byte）300实际上会得到44。</p><h4 id="赋值"><a href="#赋值" class="headerlink" title="赋值"></a>赋值</h4><p>可以在赋值中使用二元运算符，为此有一种很方便的简写形式。例如，</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">x += <span class="hljs-number">4</span>;<br></code></pre></td></tr></table></figure><p>等价于：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">x = x + <span class="hljs-number">4</span>;<br></code></pre></td></tr></table></figure><p>(一般来说，要把运算符放在 &#x3D; 号左边，如 *&#x3D; 或 %&#x3D; )。</p><p>警告:如果运算符得到一个值，其类型与左侧操作数的类型不同，就会发生强制类型转换。例如，如果 x 是一个 int，则以下语句</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">x += <span class="hljs-number">3.5</span>;<br></code></pre></td></tr></table></figure><p>是合法的，将把 x 设置为 (int)(x+3.5)。</p><p>需要说明，在 Java 中 ，<strong>赋值是一个表达式(expression)</strong>。也就是说，它有一个值，具体来讲就是所赋的那个值。可以使用这个值完成一些操作，例如，可以把它赋给另一个变量。考虑以下语句:</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">int</span> <span class="hljs-variable">x</span> <span class="hljs-operator">=</span> <span class="hljs-number">1</span>;<br><span class="hljs-type">int</span> <span class="hljs-variable">y</span> <span class="hljs-operator">=</span> x += <span class="hljs-number">4</span>;<br></code></pre></td></tr></table></figure><p><em>x</em>+&#x3D;4 的值是 5 ，因为这是赋给 x 的值。然后将这个值赋给 y 。</p><p>在 Java 中，也提供了自增、自减运算符: n++ 将变量 n 的当前值加1，n– 则将 n 的值减1。由于这些运算符会改变变量的值，所以不能对数值本身应用这些运算符。还有一种“前缀”形式: ++n。后缀和前缀形式都会使变量值加1或减1。但用在表达式中时，二者就有区别了。前缀形式会先完成加1;而后缀形式会使用变量原来的值。在 Java 中，很少在表达式中使用 ++。</p><h4 id="关系运算符"><a href="#关系运算符" class="headerlink" title="关系运算符"></a>关系运算符</h4><p>Java 包含丰富的关系运算符。要检测相等性，可以使用两个等号&#x3D;&#x3D;。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-number">3</span> == <span class="hljs-number">7</span><br></code></pre></td></tr></table></figure><p>的值为false。</p><p>另外可以使用 !&#x3D; 检测不相等。例如，</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-number">3</span> != <span class="hljs-number">7</span><br></code></pre></td></tr></table></figure><p>的值为 true 。</p><p>最后，还有经常使用的 &lt;(小于)、&gt;(大于)、&lt;&#x3D;（小于等于）和 &gt;&#x3D;（大于等于）运算符。</p><p>Java使用 &amp;&amp; 表示逻辑 “ 与 ” 运算符，使用 || 表示逻辑 “ 或 ” 运算符。从 !&#x3D; 运算符可以想到，感叹号 ! 就是逻辑非运算符。&amp;&amp; 和 || 运算符是按照 <strong>“短路”</strong> 方式来求值的: 如果第一个操作数已经能够确定表达式的值，第二个操作数就不必计算了。而且已经计算得到第一个表达式的真值为 false，那么结果就不可能为 true。因此，第二个表达式就不必计算了。</p><p>Java提供了 conditional ? :  运算符，可以根据一个布尔表达式选择一个值。如果条件 (condition) 为 true，表达式</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">condition? expression1: expression2<br></code></pre></td></tr></table></figure><p>就计算为第一个表达式的值，否则为第二个表达式的值。</p><p>需要在两个以上的值中做出选择时，可以使用<strong>switch表达式(这是Java 14中引入的)</strong>。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">String</span> <span class="hljs-variable">seasonName</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">switch</span>(seasonCode)<br>&#123;<span class="hljs-keyword">case</span> <span class="hljs-number">0</span> -&gt; <span class="hljs-string">&quot;Spring&quot;</span>;<br><span class="hljs-keyword">case</span> <span class="hljs-number">1</span> -&gt; <span class="hljs-string">&quot;Summer&quot;</span>;<br><span class="hljs-keyword">case</span> <span class="hljs-number">2</span> -&gt; <span class="hljs-string">&quot;Fall&quot;</span>;<br><span class="hljs-keyword">case</span> <span class="hljs-number">3</span> -&gt; <span class="hljs-string">&quot;Winter&quot;</span>;<br><span class="hljs-keyword">default</span> -&gt; <span class="hljs-string">&quot;?&quot;</span>;&#125;;<br></code></pre></td></tr></table></figure><p>case 标签还可以是字符串或枚举类型常量。</p><p>注释：与所有表达式类似，switch表达式也有一个值。注意各个分支中箭头-&gt;放在值前面。</p><p>注释：在Java 14中，switch有4种形式。</p><p>可以为各个case提供多个标签，用逗号分隔：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">int</span> <span class="hljs-variable">numLetters</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">switch</span> (seasonName)<br>&#123;<br><span class="hljs-keyword">case</span> <span class="hljs-string">&quot;Spring&quot;</span>,<span class="hljs-string">&quot;Summer&quot;</span>,<span class="hljs-string">&quot;Winter&quot;</span> -&gt; <span class="hljs-number">6</span>;<br><br><span class="hljs-keyword">case</span> <span class="hljs-string">&quot;Fall&quot;</span> -&gt; <span class="hljs-number">4</span>;<br><br><span class="hljs-keyword">default</span> -&gt; -<span class="hljs-number">1</span>;<br>&#125;；<br></code></pre></td></tr></table></figure><p>switch表达式中使用枚举常量时，不需要为各个标签提供枚举名，这可以从switch值推导得出。</p><p>例如：enum Size { SMALL，MEDIUM，LARGE，EXTRA_LARGE }；</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">enum</span> <span class="hljs-title class_">Size</span> &#123; SMALL，MEDIUM， LARGE，EXTRA_LARGE &#125;;<br><span class="hljs-type">Size</span> <span class="hljs-variable">itemSize</span> <span class="hljs-operator">=</span> . . . ;<br><span class="hljs-type">String</span> <span class="hljs-variable">label</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">switch</span> (itemSize)<br>&#123;<br><span class="hljs-keyword">case</span> SMALL -&gt; <span class="hljs-string">&quot;S&quot;</span>;<span class="hljs-comment">// no need to use Size.SMALL</span><br><br><span class="hljs-keyword">case</span> MEDIUM -&gt; <span class="hljs-string">&quot;M&quot;</span>;<br><br><span class="hljs-keyword">case</span> LARGE -&gt; <span class="hljs-string">&quot;L&quot;</span>;<br><br><span class="hljs-keyword">case</span> EXTRA_LARGE -&gt; <span class="hljs-string">&quot;XL&quot;</span>;<br>&#125;；<br></code></pre></td></tr></table></figure><p>在这个例子中，完全可以省略default，因为每一个可能的值都有相应的一个case。</p><p>警告：<strong>使用整数或String操作数的switch表达式必须有一个default，因为不论操作数值是什么，这个表达式都必须生成一个值。</strong></p><p>警告：如果操作数为null，会抛出一个NullPointerException。</p><p>处理整型类型时，还有一些运算符可以直接处理组成整数的各个位。这意味着可以使用掩码技术得到一个数中的各个位。位运算符包括：</p><p>&amp; (与) | (或)  ^ (异或)  ~ (非)</p><p>这些运算符按位模式操作。</p><p>例如，如果 n 是一个整数变量，而且 n 的二进制表示中从右边数第4位为1，则</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">int</span> <span class="hljs-variable">fourthBitFromRight</span> <span class="hljs-operator">=</span> (n &amp; <span class="hljs-number">0b1000</span>) / <span class="hljs-number">0b1000</span>;<br></code></pre></td></tr></table></figure><p>会返回1，否则返回0。利用 &amp; 并结合适当的2的幂，可以屏蔽其他位，而只留下其中的某一位。</p><p>注释：应用在布尔值上时，&amp; 和 | 运算符也会得到一个布尔值。这些运算符与 &amp;&amp; 和 || 运算符很类似，不过 &amp; 和 | 运算符**不采用“短路”**方式来求值，也就是说，计算结果之前，两个操作数都需要计算。</p><p>另外，还有&lt;&lt;和&gt;&gt;运算符可以将位模式左移或右移。</p><p><strong>最后&gt;&gt;&gt;运算符会用0填充高位，这与&gt;&gt;不同，&gt;&gt;会用符号位填充高位，不存在&lt;&lt;&lt;运算符。</strong></p><p>警告：移位运算符的右操作数要完成模32运算（除非左操作数是long类型，在这种情况下需要对右操作数完成模64运算）。例如，1&lt;&lt;35的值等同于1&lt;&lt;3或8。</p><p>表3-4给出了运算符的优先级。如果不使用圆括号，就按照这里给出的运算符优先级次序进行计算。同一个级别的运算符按照从左到右的次序进行计算（但右结合运算符除外，如表中所示）。例如，由于 &amp;&amp; 的优先级比 || 的优先级高，所以表达式 a &amp;&amp; b || c 等价于 (a &amp;&amp; b) ||c</p><p>因为+&#x3D; 是右结合运算符，所以表达式 a +&#x3D; b +&#x3D; c  等价于  a +&#x3D; (b +&#x3D; c)    也就是将b&#x3D;c的结果（完成加法后b的值）加到a上。</p><p>Java不使用逗号运算符。不过，可以在for语句的第1和第3部分中使用逗号分隔表达式列表。</p><table><thead><tr><th>优先级</th><th>运算符</th><th>结合顺序</th><th>运算符类别</th></tr></thead><tbody><tr><td>1</td><td>[ ]  .   ( ) （方法调用）</td><td>从左向右</td><td>括号运算符</td></tr><tr><td>2</td><td>! 、~ 、++ 、– 、+（一元运算）、-（一元运算）、( )（强制类型转换）、new</td><td>从右向左</td><td>逻辑、正负、自增减运算符</td></tr><tr><td>3</td><td>*  、&#x2F;  、%</td><td>从左向右</td><td>算术运算符</td></tr><tr><td>4</td><td>＋（加）、－（减）</td><td>从左向右</td><td>算术运算符</td></tr><tr><td>5</td><td>&lt;&lt; 、&gt;&gt; 、&gt;&gt;&gt;</td><td>从左向右</td><td>移位运算符</td></tr><tr><td>6</td><td>&lt; 、&lt;&#x3D; 、&gt; 、&gt;&#x3D; 、instanceof</td><td>从左向右</td><td>关系运算符</td></tr><tr><td>7</td><td>&#x3D;&#x3D; 、!&#x3D;</td><td>从左向右</td><td>关系运算符</td></tr><tr><td>8</td><td>&amp;</td><td>从左向右</td><td>位逻辑运算符</td></tr><tr><td>9</td><td>^</td><td>从左向右</td><td>位逻辑运算符</td></tr><tr><td>10</td><td>|</td><td>从左向右</td><td>位逻辑运算符</td></tr><tr><td>11</td><td>&amp;&amp;</td><td>从左向右</td><td>逻辑运算符</td></tr><tr><td>12</td><td>||</td><td>从左向右</td><td>逻辑运算符</td></tr><tr><td>13</td><td>?:</td><td>从右向左</td><td>条件运算符</td></tr><tr><td>14</td><td>&#x3D;、+&#x3D;、-&#x3D;、*&#x3D;、&#x2F;&#x3D;、%&#x3D;、&amp;&#x3D;、|&#x3D;、^&#x3D;、&lt;&lt;&#x3D;、&gt;&gt;&#x3D;、&gt;&gt;&gt;&#x3D;</td><td>从右向左</td><td>赋值运算符</td></tr></tbody></table><h4 id="字符串"><a href="#字符串" class="headerlink" title="字符串"></a>字符串</h4><p>从概念上讲，Java 字符串就是 Unicode 字符序列。例如，字符串 “Java\u212B” 由 5 个 Unicode 字符 J、a、v、a 和 <em>TM</em>组成。<strong>‘’Java’’ 没有内置的字符串类型，而是标准 Java 类库中提供了一个预定义类</strong>，很自然地叫作 String。<strong>每个用双引号括起来的字符串都是 String 类的一个实例</strong>：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">String</span> <span class="hljs-variable">e</span> <span class="hljs-operator">=</span><span class="hljs-string">&quot;&quot;</span>;<span class="hljs-comment">// an empty string</span><br><span class="hljs-type">String</span> <span class="hljs-variable">greeting</span> <span class="hljs-operator">=</span> <span class="hljs-string">&quot;Hello&quot;</span>;<br></code></pre></td></tr></table></figure><p>String 类的 substring方法可以从一个较大的字符串提取出一个子串。例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">String</span> <span class="hljs-variable">greeting</span> <span class="hljs-operator">=</span> <span class="hljs-string">&quot;Hello&quot;</span>;<br><span class="hljs-type">String</span> <span class="hljs-variable">s</span> <span class="hljs-operator">=</span> greeting.substring(<span class="hljs-number">0</span>,<span class="hljs-number">3</span>);<br></code></pre></td></tr></table></figure><p>会创建一个由字符 “Hel” 组成的字符串。</p><p>注释：类似于 C 和 C++，Java 字符串中的代码单元和码点从 0 开始计数。</p><p>substring 方法的第二个参数是你不想复制的第一个位置。这里要复制位置为 0、1 和 2（从 0 到 2，包括 0 和 2）的字符。substring 会计数，这说明会从 0 开始，直到 3 为止，但不包含 3。</p><p>substring 的工作方式有一个优点：很容易计算子串的长度。字符串 s.substring(a, b) 的长度为 b-a。例如，子串 “Hel” 的长度为 3-0&#x3D;3。</p><p>与绝大多数程序设计语言一样，Java 语言允许使用 + 号连接（拼接）两个字符串。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">String</span> <span class="hljs-variable">expletive</span> <span class="hljs-operator">=</span> <span class="hljs-string">&quot;Expletive&quot;</span>;<br><span class="hljs-type">String</span> <span class="hljs-variable">PG13</span> <span class="hljs-operator">=</span> <span class="hljs-string">&quot;deleted&quot;</span>;<br><span class="hljs-type">String</span> <span class="hljs-variable">message</span> <span class="hljs-operator">=</span> expletive + PG13;<br></code></pre></td></tr></table></figure><p>上述代码将字符串 “ Expletivedeleted “ 赋给变量 message (注意，单词之间没有空格，+ 号完全按照给定的次序将两个字符串拼接起来)。</p><p>当将一个字符串与一个非字符串的值进行拼接时，后者会转换成字符串(在第5章中可以看到，任何一个Java对象都可以转换成字符串)。例如:</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">int</span> <span class="hljs-variable">age</span> <span class="hljs-operator">=</span> <span class="hljs-number">13</span>;<br>String rating= “PG” + age;<br></code></pre></td></tr></table></figure><p>将把 rating 设置为 “ PG13 ” 。</p><p>这个特性通常用在输出语句中。例如:</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">System.out.println(<span class="hljs-string">&quot;The answer is&quot;</span> + answer);<br></code></pre></td></tr></table></figure><p>这是一条合法的语句，会打印出你希望的结果(因为单词 is 后面加了一个空格，输出时也会有这个空格)。</p><p><strong>如果需要把多个字符串放在一起，用一个界定符分隔，可以使用静态join方法:</strong></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">String</span> <span class="hljs-variable">all</span> <span class="hljs-operator">=</span> String.join(<span class="hljs-string">&quot;/&quot;</span>, <span class="hljs-string">&quot;S&quot;</span>,<span class="hljs-string">&quot;M&quot;</span>, <span class="hljs-string">&quot;L&quot;</span>, <span class="hljs-string">&quot;XL&quot;</span>);<span class="hljs-comment">// all is the string“S / M / L / XL”</span><br></code></pre></td></tr></table></figure><p><strong>在Java11中，还提供了一个repeat方法</strong>:</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">String repeated=<span class="hljs-string">&quot;Java&quot;</span>.repeat(<span class="hljs-number">3</span>)；<span class="hljs-comment">// repeated is“JavaJavaJava”</span><br></code></pre></td></tr></table></figure><p>String 类没有提供任何方法来修改字符串中的某个字符。如果希望将 greeting 的内容修改为 “Help!” ，不能直接将 greeting 的最后两个位置的字符修改为 ‘p’ 和  ‘!’ 。可以提取想要保留的子串，再与希望替换的字符拼接:</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">String</span> <span class="hljs-variable">greeting</span> <span class="hljs-operator">=</span> <span class="hljs-string">&quot;Hello&quot;</span>;<br>greeting = greeting.substring(<span class="hljs-number">0</span>,<span class="hljs-number">3</span>) + <span class="hljs-string">&quot;p!&quot;</span>;<span class="hljs-comment">//创建了一个新的对象，使变量指向它，原对象仍然存在</span><br></code></pre></td></tr></table></figure><p>上面这条语句将把 greeting 变量的当前值修改为 “Help!” 。</p><p>由于<strong>不能修改 Java 字符串中的单个字符</strong>，所以在 Java 文档中<strong>将String类对象称为是不可变的(immutable)</strong>，如同数字3永远是数字3一样，字符串 “Hello” 永远包含字符H、e、l、l和o的代码单元序列。你不能修改这些值，不过，我们已经看到，可以修改字符串变量 greeting 的内容，让它指向另外一个字符串，这就如同可以让原本存放3的数值变量改成存放4一样。</p><p>通过拼接“Hello”和“p!”来生成一个新字符串的效率确实不高。但是，不可变字符串有一个很大的优点:<strong>编译器可以让字符串共享。</strong></p><p>为了弄清具体如何工作，可以想象各个字符串存放一个在公共存储池中。字符串变量指向存储池中相应的位置。如果复制一个字符串变量，原始字符串和复制的字符串共享相同的字符。</p><p>可以使用 equals 方法检测两个字符串是否相等。对于表达式：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">s.equals(t)<br></code></pre></td></tr></table></figure><p>如果字符串 s 与字符串 t 相等，则返回 true ;否则，返回 false。需要注意的是，s 与 t 可以是字符串变量，也可以是字符串字面量。例如，以下表达式是合法的：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-string">&quot;Hello&quot;</span>.equals(greeting)<br></code></pre></td></tr></table></figure><p>要想检测两个字符串是否相等，而不区分大小写，可以使用 <code>equalsIgnoreCase</code> 方法。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-string">&quot;Hello&quot;</span>.equalsIgnoreCase(<span class="hljs-string">&quot;hello&quot;</span>)<br></code></pre></td></tr></table></figure><p>不要使用 &#x3D;&#x3D; 运算符检测两个字符串是否相等! 这个运算符只能够确定两个字符串是否存放在同一个位置上。当然，如果字符串在同一个位置上，它们必然相等。但是，完全有可能将多个相等的字符串副本存放在不同的位置上。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">String</span> <span class="hljs-variable">greeting</span> <span class="hljs-operator">=</span> <span class="hljs-string">&quot;Hello&quot;</span>; <span class="hljs-comment">// initialize greeting to a string</span><br><span class="hljs-keyword">if</span> (greeting == <span class="hljs-string">&quot;Hello&quot;</span>) . . .<br><span class="hljs-comment">// probably true</span><br><span class="hljs-keyword">if</span> (greeting.substring(<span class="hljs-number">0</span>, <span class="hljs-number">3</span>) == <span class="hljs-string">&quot;Hel&quot;</span>) . . .<br><span class="hljs-comment">// probably false</span><br></code></pre></td></tr></table></figure><p>如果虚拟机总是共享相等的字符串，则可以使用 &#x3D;&#x3D; 运算符检测字符串是否相等。但实际上只有字符串字面量会共享，而 + 或 substring 等操作得到的字符串并不共享。因此，千万不要使用 &#x3D;&#x3D; 运算符测试字符串的相等，否则程序中会出现最糟糕的一种bug，这种bug可能会间歇性地随机出现。</p><p>空串 “” 是长度为0的字符串。可以调用以下代码检查一个字符串是否为空：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">if</span> (str.length() == <span class="hljs-number">0</span>)<br><br>或<br><br><span class="hljs-keyword">if</span> (str.equals(<span class="hljs-string">&quot;&quot;</span>))<br></code></pre></td></tr></table></figure><p>空串是一个Java对象，有自己的串长度（0）和内容（空）。不过，String变量还可以存放一个特殊的值，名为<code>null</code>，表示目前没有任何对象与该变量关联（关于null的更多信息请参见第4章）。要检查一个字符串是否为null，可以使用以下代码：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">if</span> (str == <span class="hljs-literal">null</span>)<br></code></pre></td></tr></table></figure><p>有时要检查一个字符串既不是 null 也不是空串，这种情况下可以使用：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">if</span> (str != <span class="hljs-literal">null</span> &amp;&amp; str.length() != <span class="hljs-number">0</span>)<br></code></pre></td></tr></table></figure><p>首先要检查 str 不为null。如果在一个 null 值上调用方法，会出现错误。</p><p>有些时候，需要由较短的字符串构建字符串，例如，按键或文件中的单词。如果采用字符串拼接的方式来达到这个目的，效率会比较低。每次拼接字符串时，都会构建一个新的 String 对象，既耗时，又浪费空间。使用 StringBuilder 类就可以避免这个问题。</p><p>如果需要用许多小字符串来构建一个字符串，可以采用以下步骤。首先，构建一个空的字符串构建器:</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">StringBuilder</span> <span class="hljs-variable">builder</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">StringBuilder</span>();<br></code></pre></td></tr></table></figure><p>当每次需要添加另外一部分时，就调用 append 方法。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java">builder.append(ch);<span class="hljs-comment">//appends a single character</span><br><span class="hljs-type">String</span> <span class="hljs-variable">appendedString</span> <span class="hljs-operator">=</span> builder.toString();<br></code></pre></td></tr></table></figure><p>字符串构建完成时，调用 toString 方法。你会得到一个 String 对象，其中包含了构建器中的字符序列。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">String</span> <span class="hljs-variable">completedString</span> <span class="hljs-operator">=</span> builder.toString();<br></code></pre></td></tr></table></figure><p>注释: StringBuffer 类的效率不如 StringBuilde 类，不过它允许采用多线程的方式添加或删除字符。如果所有字符串编辑操作都在单个线程中执行(通常都是这样)，则应当使用 StringBuilder 类。这两个类的API是一样的。</p><p>利用 Java15 新增的 **文本块(text block)**特性，可以很容易地提供跨多行的字符串字面量。文本块以 “””  开头(这是开始 “”” ），后面是一个换行符，并以另一个 “”” 结尾(这是结束 “”” )：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">String</span> <span class="hljs-variable">greeting</span> <span class="hljs-operator">=</span> <span class="hljs-string">&quot;&quot;&quot;</span><br><span class="hljs-string">Hello</span><br><span class="hljs-string">World</span><br><span class="hljs-string">&quot;&quot;&quot;</span>;<br></code></pre></td></tr></table></figure><p>文本块比相应的字符串字面量更易于读写：</p><p>“Hello\nWorld\n”</p><p>这个字符串包含两个\n：一个在Hello后面，另一个在World后面。开始 “”” 后面的换行符不作为字符串字面量的一部分。</p><p>如果不想要最后一行后面的换行符，可以让结束 “”” 紧跟在最后一个字符后面：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">String</span> <span class="hljs-variable">prompt</span> <span class="hljs-operator">=</span> <span class="hljs-string">&quot;&quot;&quot;</span><br><span class="hljs-string">Hello, my name is Hal.</span><br><span class="hljs-string">Please enter your name: &quot;&quot;&quot;</span>;<br></code></pre></td></tr></table></figure><p>文本块特别适合包含用其他语言编写的代码,如 SQL 或 HTML。可以直接将那些代码粘贴到一对三重引号之间:</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">String</span> <span class="hljs-variable">html</span> <span class="hljs-operator">=</span> <span class="hljs-string">&quot;&quot;&quot;</span><br><span class="hljs-string">&lt;div class=&quot;Warning&quot;&gt;</span><br><span class="hljs-string">    Beware of those who say &quot;Hello&quot; to the world</span><br><span class="hljs-string">&lt;/div&gt;</span><br><span class="hljs-string">&quot;&quot;&quot;</span>;<br></code></pre></td></tr></table></figure><p>需要说明的是,一般不用对引号转义。只有两种情况下需要对引号转义:</p><p>● 文本块以一个引号结尾。</p><p>● 文本块中包含三个或更多引号组成的一个序列。</p><p>遗憾的是,所有反斜线都需要转义。</p><p>常规字符串中的所有转义序列在文本块中也有同样的作用。</p><p>有一个转义序列只能在文本块中使用。行尾的 \ 会把这一行与下一行连接起来。例如:</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-string">&quot;&quot;&quot;</span><br><span class="hljs-string">Hello, my name is Hal. \</span><br><span class="hljs-string">Please enter your name: &quot;&quot;&quot;</span>;<br></code></pre></td></tr></table></figure><p>等同于: “Hello, my name is Hal. Please enter your name:”</p><p>文本块会对行结束符进行标准化, 删除末尾的空白符, 并把Windows的行结束符 ( \r\n)改为简单的换行符( \n)。假如确实需要保留末尾的空格,这种情况下可以把最后一个空格转换为一个 \s 转义序列。</p><p>文本块也可以缩进:</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">String</span> <span class="hljs-variable">html</span> <span class="hljs-operator">=</span> <span class="hljs-string">&quot;&quot;&quot;</span><br><span class="hljs-string">&lt;div class=&quot;Warning&quot;&gt;</span><br><span class="hljs-string">Beware of those who say &quot;Hello&quot; to the world</span><br><span class="hljs-string">&lt;/div&gt;</span><br><span class="hljs-string">&quot;&quot;&quot;</span>;<br></code></pre></td></tr></table></figure><p>将去除文本块中所有行的公共缩进。实际字符串为:</p><p>“&lt;div class&#x3D;\“Warning\“&gt;\n Beware of those who say \“Hello\“ to the world\n</div>\n”</p><p>注意,第一行和第三行没有缩进。</p><p>你的IDE很可能会使用制表符、空格或者制表符以及空格缩进所有文本块。</p><p>Java没有规定制表符的宽度。</p><p>空白符前缀必须与文本块中的所有行完全匹配。(文本块会找到所有非空行的<strong>最小公共缩进</strong>，然后从每行删除这个数量的空白符)</p><p>去除缩进过程中不考虑空行。(空行不影响最小缩进的计算，但会参与缩进的删除)</p><p>结束 “”” 前面的空白符很重要(结束分隔符的位置决定了<strong>要删除多少缩进</strong>)。一定要缩进到想要去除的空白符的末尾。</p><p>警告:要当心缩进文本块的公共前缀中混用制表符和空格的情况。</p><h4 id="输入与输出"><a href="#输入与输出" class="headerlink" title="输入与输出"></a>输入与输出</h4><p>前面已经看到，将输出打印到“标准输出流”（即控制台窗口）是一件非常容易的事情，只需要调用 System.out.println 。不过，读取“标准输入流” System.in 就没有那么简单了。要想读取控制台输入，首先需要构造一个与“标准输入流” System.in 关联的 Scanner 对象。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">Scanner</span> <span class="hljs-variable">in</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Scanner</span>(System.in);<br></code></pre></td></tr></table></figure><p>现在，就可以使用 Scanner 类的各种方法读取输入了。例如，nextLine 方法将读取一行输入。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java">System.out.print(<span class="hljs-string">&quot;What is your name? &quot;</span>);<br><span class="hljs-type">String</span> <span class="hljs-variable">name</span> <span class="hljs-operator">=</span> in.nextLine();<br></code></pre></td></tr></table></figure><p>使用 nextLine 方法是因为输入行中有可能包含空格。要想读取一个单词(以空白符作为分隔符)，可以调用</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">String</span> <span class="hljs-variable">firstName</span> <span class="hljs-operator">=</span> in.next();<br></code></pre></td></tr></table></figure><p>要想读取一个整数，要使用 nextInt 方法。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java">System.out.print(<span class="hljs-string">&quot;How old are you? &quot;</span>);<br><span class="hljs-type">int</span> <span class="hljs-variable">age</span> <span class="hljs-operator">=</span> in.nextInt();<br></code></pre></td></tr></table></figure><p>与此类似，nextDouble 方法可以读取下一个浮点数。</p><p>Scanner类在 java.util 包中定义。当使用的类不是定义在基本 java.lang 包中时，需要使用import指令导入相应的包。在程序的最前面添加一行代码:</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">import</span> java.util.*;<br></code></pre></td></tr></table></figure><p>注释：因为输入对所有人都可见，所以Scanner类不适用于从控制台读取密码。可以使用Console类来达到这个目的。要想读取一个密码，可以使用以下代码：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">Console</span> <span class="hljs-variable">cons</span> <span class="hljs-operator">=</span> System.console();<br><br><span class="hljs-type">String</span> <span class="hljs-variable">username</span> <span class="hljs-operator">=</span> cons.readLine(<span class="hljs-string">&quot;User name: &quot;</span>);<br><br><span class="hljs-type">char</span>[] passwd = cons.readPassword(<span class="hljs-string">&quot;Password: &quot;</span>);<br></code></pre></td></tr></table></figure><p>为安全起见，将返回的密码存放在一个字符数组中，而不是字符串中。完成对密码的处理之后，应该马上用一个填充值覆盖数组元素。使用Console对象处理输入必须一次读取一行输入，而且Console类没有提供方法来读取单个单词或数字。</p><h4 id="格式化输出"><a href="#格式化输出" class="headerlink" title="格式化输出"></a>格式化输出</h4><p>可以使用 System.out.print(x) 语句将数值 x 输出到控制台。这个命令将以 x 的类型所允许的最大非 0 位数打印 x 。例如:</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">double</span> <span class="hljs-variable">x</span> <span class="hljs-operator">=</span> <span class="hljs-number">10000.0</span> / <span class="hljs-number">3.0</span>;<br>System.out.print(x);<br></code></pre></td></tr></table></figure><p>会打印</p><p>3333.3333333333335</p><p>如果希望显示美元、美分数，这就会有问题。</p><p>这个问题可以利用 printf 方法来解决。例如，以下调用</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">System.out.printf(<span class="hljs-string">&quot;%8.2f&quot;</span>, x);<br></code></pre></td></tr></table></figure><p>打印 x 时**字段宽度(field width)**为8个字符，精度为2个字符。也就是说，结果包含一个前导的空格和7个字符，如下所示:3333.33</p><p>可以为 printf 提供多个参数，例如:</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">System.out.printf(<span class="hljs-string">&quot;Hello, %s. Next year, you&#x27;ll be %d&quot;</span>, name, age);<br></code></pre></td></tr></table></figure><p>每一个以 % 字符开头的**格式说明符(format specifiers)**都替换为相应的参数。格式说明符末尾的转换字符(conversion character)指示要格式化的数值的类型: f 表示浮点数,，s 表示字符串，d 表示十进制整数。</p><p>表 3-5：用于 printf 的转换字符</p><table><thead><tr><th>转换字符</th><th>类型</th><th>示例</th><th>转换字符</th><th>类型</th><th>示例</th></tr></thead><tbody><tr><td>d</td><td>十进制整数</td><td>159</td><td>s 或 S</td><td>字符串</td><td>Hello</td></tr><tr><td>x 或 X</td><td>十六进制整数。要想对十六进制格式化有更多控制，可以使用 HexFormat 类</td><td>9f</td><td>c 或 C</td><td>字符</td><td>H</td></tr><tr><td>o</td><td>八进制整数</td><td>237</td><td>b 或 B</td><td>布尔</td><td>true</td></tr><tr><td>f 或 F</td><td>定点浮点数</td><td>15.9</td><td>h 或 H</td><td>散列码</td><td>4262802</td></tr><tr><td>e 或 E</td><td>指数浮点数</td><td>1.59e+01</td><td>tx 或 Tx</td><td>遗留的日期时间格式化。应当改为使用 java.time 类，参见第 6 章</td><td>——</td></tr><tr><td>g 或 G</td><td>通用浮点数（e 和 f 中较短的一个）</td><td>——</td><td>%</td><td>百分号</td><td>%</td></tr><tr><td>a 或 A</td><td>十六进制浮点数</td><td>0x1.fccdp3</td><td>n</td><td>与平台有关的行为符</td><td>——</td></tr></tbody></table><p><strong>注释</strong>：可以使用 s 转换字符格式化任意的对象。如果一个任意对象实现了 Formattable 接口，会调用这个对象的 formatTo 方法。否则，会调用 toString 方法将这个对象转换为一个字符串。</p><p>另外，还可以指定控制格式化输出外观的各种标志( flag )。例如，逗号标志会增加分组分隔符。即</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">System.out.printf(<span class="hljs-string">&quot;%,.2f&quot;</span>, <span class="hljs-number">10000.0</span> / <span class="hljs-number">3.0</span>);<br></code></pre></td></tr></table></figure><p>会打印3,333.33</p><p>可以使用多个标志，例如，“ %，( .2f “  会使用分组分隔符，并将负数包围在括号内。</p><p>表 3-6：用于 printf 的标志</p><table><thead><tr><th>标志</th><th>作用</th><th>示例</th></tr></thead><tbody><tr><td>+</td><td>打印正数和负数的符号</td><td>+3333.33</td></tr><tr><td>空格</td><td>在正数前面增加一个空格</td><td>| 3333.33|</td></tr><tr><td>0</td><td>增加前导 0</td><td>003333.33</td></tr><tr><td>-</td><td>字段左对齐</td><td>|3333.33 |</td></tr><tr><td>(</td><td>将负数包围在括号内</td><td>(3333.33)</td></tr><tr><td>,</td><td>增加分组分隔符</td><td>3,333.33</td></tr><tr><td># (对于 f 格式)</td><td>总是包含一个小数点</td><td>3,333.</td></tr><tr><td># (对于 x 或 o 格式)</td><td>添加前缀 0x 或 0</td><td>0xcafe</td></tr><tr><td>$</td><td>指定要格式化的参数索引。例如，<code>%1$d %1$x</code> 将以十进制和十六进制格式打印第 1 个参数</td><td>159 9F</td></tr><tr><td>&lt;</td><td>格式化前面指定的同一个值。例如，<code>%d %&lt;x</code> 将以十进制和十六进制打印同一个数</td><td>159 9F</td></tr></tbody></table><p>可以使用静态的 <code>String.format</code> 方法创建一个格式化的字符串，而不打印输出：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">String</span> <span class="hljs-variable">message</span> <span class="hljs-operator">=</span> String.format(<span class="hljs-string">&quot;Hello, %s. Next year, you&#x27;ll be %d&quot;</span>, name, age + <span class="hljs-number">1</span>);<br></code></pre></td></tr></table></figure><p><strong>注释</strong>：在 Java 15 中，可以使用 <code>formatted</code> 方法，这样可以少敲 5 个字符：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">String</span> <span class="hljs-variable">message</span> <span class="hljs-operator">=</span> <span class="hljs-string">&quot;Hello, %s. Next year, you&#x27;ll be %d&quot;</span>.formatted(name, age + <span class="hljs-number">1</span>);<br></code></pre></td></tr></table></figure><p><img src="/2025/05/04/hello-world/%E7%AC%AC%E4%B8%89%E7%AB%A0/image-20251029150720800.png" alt="image-20251029150720800"></p><p>​<strong>图 3-6 格式说明符语法</strong></p><p><strong>注释</strong>：格式化规则是特定于本地化环境的。</p><h4 id="3-7-3-文件输入与输出"><a href="#3-7-3-文件输入与输出" class="headerlink" title="3.7.3 文件输入与输出"></a>3.7.3 文件输入与输出</h4><p>要想读取一个文件，需要构造一个 <code>Scanner</code> 对象，如下所示：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">Scanner</span> <span class="hljs-variable">in</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Scanner</span>(Path.of(<span class="hljs-string">&quot;myfile.txt&quot;</span>), StandardCharsets.UTF_8);<br></code></pre></td></tr></table></figure><p>如果文件名中包含反斜线符号，记住要在每个反斜线之前再加一个额外的反斜线转义：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-string">&quot;c:\\mydirectory\\myfile.txt&quot;</span><br></code></pre></td></tr></table></figure><p><strong>注释</strong>：读取一个文本文件时，要知道它的字符编码。如果省略字符编码，则会使用运行这个 Java 程序的机器的”默认编码”。</p><p><strong>警告</strong>：可以提供一个字符串参数来构造一个 <code>Scanner</code>，但这个 <code>Scanner</code> 会把字符串解释为数据，而不是文件名。例如，如果调用：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">Scanner</span> <span class="hljs-variable">in</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Scanner</span>(<span class="hljs-string">&quot;myfile.txt&quot;</span>); <span class="hljs-comment">// ERROR</span><br></code></pre></td></tr></table></figure><p>这个 <code>Scanner</code> 会将参数看作是包含 10 个字符（’m’, ‘y’, ‘f’ 等）的数据。</p><p>要想写入文件，需要构造一个 <code>PrintWriter</code> 对象。在构造器（constructor）中，需要提供文件名和字符编码：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">PrintWriter</span> <span class="hljs-variable">out</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">PrintWriter</span>(<span class="hljs-string">&quot;myfile.txt&quot;</span>, StandardCharsets.UTF_8);<br></code></pre></td></tr></table></figure><p>如果文件不存在，则会创建该文件。</p><p><strong>注释</strong>：当指定一个相对文件名时，例如，”myfile.txt”、”mydirectory&#x2F;myfile.txt” 或 “..&#x2F;myfile.txt”，文件将相对于启动 Java 虚拟机的那个目录放置。如果从一个命令 shell 执行以下命令启动程序：</p><figure class="highlight ebnf"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs ebnf"><span class="hljs-attribute">java MyProg</span><br></code></pre></td></tr></table></figure><p>启动目录就是命令 shell 的当前目录。不过，如果使用集成开发环境，那么启动目录将由 IDE 控制。可以使用下面的调用找到这个目录的位置：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">String</span> <span class="hljs-variable">dir</span> <span class="hljs-operator">=</span> System.getProperty(<span class="hljs-string">&quot;user.dir&quot;</span>);<br></code></pre></td></tr></table></figure><p>如果觉得文件定位太麻烦，可以考虑使用绝对路径名，例如：”c:\mydirectory\myfile.txt” 或者 “&#x2F;home&#x2F;me&#x2F;mydirectory&#x2F;myfile.txt”。</p><p>要记住一点：如果用一个不存在的文件构造一个 <code>Scanner</code>，或者用一个无法创建的文件名构造一个 <code>PrintWriter</code>，就会产生异常。为此，要用一个 <code>throws</code> 子句标记 <code>main</code> 方法，如下所示：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> <span class="hljs-keyword">throws</span> IOException<br>&#123;<br>    <span class="hljs-type">Scanner</span> <span class="hljs-variable">in</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Scanner</span>(Path.of(<span class="hljs-string">&quot;myfile.txt&quot;</span>), StandardCharsets.UTF_8);<br>&#125;<br></code></pre></td></tr></table></figure><p><strong>注释</strong>：从命令 shell 启动一个程序时，可以利用 shell 的重定向语法将任意文件关联到 <code>System.in</code> 和 <code>System.out</code>：</p><figure class="highlight stylus"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs stylus">java MyProg &lt; myfile<span class="hljs-selector-class">.txt</span> &gt; output<span class="hljs-selector-class">.txt</span><br><span class="hljs-comment">//输入重定向到myfile        输出重定向到output文件中</span><br></code></pre></td></tr></table></figure><p>这样就不必担心处理 <code>IOException</code> 异常了。</p><h4 id="3-8-控制流程"><a href="#3-8-控制流程" class="headerlink" title="3.8 控制流程"></a>3.8 控制流程</h4><p>Java 支持使用条件语句和循环结构来确定控制流程。</p><p><strong>注释</strong>：Java 中没有 <code>goto</code> 语句，但 <code>break</code> 语句可以带标签，可以利用它从嵌套循环中跳出。</p><h4 id="3-8-1-块作用域"><a href="#3-8-1-块作用域" class="headerlink" title="3.8.1 块作用域"></a>3.8.1 块作用域</h4><p><strong>块</strong>（即复合语句）由若干条 Java 语句组成，并用一对大括号括起来。块确定了变量的作用域。一个块可以嵌套在另一个块中。下面就是嵌套在 <code>main</code> 方法块中的一个块。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span><br>&#123;<br>    <span class="hljs-type">int</span> n;<br>    ...<br>    &#123;<br>        <span class="hljs-type">int</span> k;<br>    &#125; <span class="hljs-comment">// k is only defined up to here</span><br>&#125;<br></code></pre></td></tr></table></figure><p>但是，不能在嵌套的两个块中声明同名的变量。例如，下面的代码就有错误，而无法通过编译：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span><br>&#123;<br>    <span class="hljs-type">int</span> n;<br>    ...<br>    &#123;<br>        <span class="hljs-type">int</span> k;<br>        <span class="hljs-type">int</span> n; <span class="hljs-comment">// ERROR - can&#x27;t redeclare n in inner block</span><br>        ...<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><h4 id="3-8-2-条件语句"><a href="#3-8-2-条件语句" class="headerlink" title="3.8.2 条件语句"></a>3.8.2 条件语句</h4><p>在 Java 中，条件语句的形式为：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">if</span> (condition) statement<br></code></pre></td></tr></table></figure><p>这里的条件必须用小括号括起来。</p><p>在 Java 中，常常希望在某个条件为真时执行多条语句。在这种情况下，就可以使用块语句（block statement），形式如下：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java">&#123;<br>    statement1<br>    statement2<br>    . . .<br>&#125;<br></code></pre></td></tr></table></figure><p>例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">if</span> (yourSales &gt;= target)<br>&#123;<br>    performance = <span class="hljs-string">&quot;Satisfactory&quot;</span>;<br>    bonus = <span class="hljs-number">100</span>;<br>&#125;<br></code></pre></td></tr></table></figure><p>当 <code>yourSales</code> 大于或等于 <code>target</code> 时，将执行大括号中的所有语句（请参见图 3-7）。</p><p><strong>注释</strong>：使用块（有时称为复合语句）可以在 Java 程序结构中原本只能放置一条（简单）语句的地方放置多条语句。</p><p>在 Java 中，更一般的条件语句如下所示（请参见图 3-8）：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">if</span> (condition) statement1 <span class="hljs-keyword">else</span> statement2<br></code></pre></td></tr></table></figure><p>例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">if</span> (yourSales &gt;= target)<br>&#123;<br>    performance = <span class="hljs-string">&quot;Satisfactory&quot;</span>;<br>    bonus = <span class="hljs-number">100</span> + <span class="hljs-number">0.01</span> * (yourSales - target);<br>&#125;<br><span class="hljs-keyword">else</span><br>&#123;<br>    performance = <span class="hljs-string">&quot;Unsatisfactory&quot;</span>;<br>    bonus = <span class="hljs-number">0</span>;<br>&#125;<br></code></pre></td></tr></table></figure><p>其中 <code>else</code> 部分总是可选的。<code>else</code> 子句与最邻近的 <code>if</code> 构成一组。因此，在以下语句中：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">if</span> (x &lt;= <span class="hljs-number">0</span>) <span class="hljs-keyword">if</span> (x == <span class="hljs-number">0</span>) sign = <span class="hljs-number">0</span>; <span class="hljs-keyword">else</span> sign = -<span class="hljs-number">1</span>;<br></code></pre></td></tr></table></figure><p><code>else</code> 与第 2 个 <code>if</code> 配对。当然，使用大括号可以让这段代码更加清晰：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">if</span> (x &lt;= <span class="hljs-number">0</span>) &#123; <span class="hljs-keyword">if</span> (x == <span class="hljs-number">0</span>) sign = <span class="hljs-number">0</span>; <span class="hljs-keyword">else</span> sign = -<span class="hljs-number">1</span>; &#125;<br></code></pre></td></tr></table></figure><h2 id=""><a href="#" class="headerlink" title="                    "></a><img src="/2025/05/04/hello-world/%E7%AC%AC%E4%B8%89%E7%AB%A0/image-20251029152221204.png" alt="image-20251029152221204">                    <img src="/2025/05/04/hello-world/%E7%AC%AC%E4%B8%89%E7%AB%A0/image-20251029152244833.png" alt="image-20251029152244833"></h2><p>图3-7 if语句的流程图                                                                图 3-8 if&#x2F;else 语句的流程图</p><p>反复使用 <code>if...else if...</code> 很常见（如图 3-9 所示）。例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">if</span> (yourSales &gt;= <span class="hljs-number">2</span> * target)<br>&#123;<br>    performance = <span class="hljs-string">&quot;Excellent&quot;</span>;<br>    bonus = <span class="hljs-number">1000</span>;<br>&#125;<br><span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (yourSales &gt;= <span class="hljs-number">1.5</span> * target)<br>&#123;<br>    performance = <span class="hljs-string">&quot;Fine&quot;</span>;<br>    bonus = <span class="hljs-number">500</span>;<br>&#125;<br><span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (yourSales &gt;= target)<br>&#123;<br>    performance = <span class="hljs-string">&quot;Satisfactory&quot;</span>;<br>    bonus = <span class="hljs-number">100</span>;<br>&#125;<br><span class="hljs-keyword">else</span><br>&#123;<br>    System.out.println(<span class="hljs-string">&quot;You&#x27;re fired&quot;</span>);<br>&#125;<br></code></pre></td></tr></table></figure><h2 id="-1"><a href="#-1" class="headerlink" title=""></a><img src="/2025/05/04/hello-world/%E7%AC%AC%E4%B8%89%E7%AB%A0/image-20251029152937051.png" alt="image-20251029152937051"></h2><p>图3-9 if&#x2F;else if（多分支）的流程图</p><h4 id="3-8-3-循环"><a href="#3-8-3-循环" class="headerlink" title="3.8.3 循环"></a>3.8.3 循环</h4><p><code>while</code> 循环会在条件为 <code>true</code> 时执行一个语句（也可以是一个块语句）。一般形式如下：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">while</span> (condition) statement<br></code></pre></td></tr></table></figure><p>如果开始时循环条件就为 <code>false</code>，那么 <code>while</code> 循环一次也不执行（请参见图3-10）。</p><p><code>while</code> 循环语句在最前面检测循环条件。因此，循环体中的代码有可能一次都不执行。如果希望循环体至少执行一次，需要使用 do&#x2F;while 循环将检测放在最后。它的语法如下：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">do</span> statement <span class="hljs-title function_">while</span> <span class="hljs-params">(condition)</span>;<br></code></pre></td></tr></table></figure><p>这种循环先执行语句（通常是一个语句块），然后再检查循环条件。如果条件为 <code>true</code>，就重复执行语句，然后再依次检测循环条件，依此类推。</p><p>只要用户回答 “N”，循环就会重复执行（见图 3-11）。</p><p><img src="/2025/05/04/hello-world/%E7%AC%AC%E4%B8%89%E7%AB%A0/image-20251029153213520.png" alt="image-20251029153213520">                                            <img src="/2025/05/04/hello-world/%E7%AC%AC%E4%B8%89%E7%AB%A0/image-20251029153231944.png" alt="image-20251029153231944"></p><p><strong>图 3-10 while 语句的流程图</strong>                                                       <strong>图 3-11 do&#x2F;while 语句的流程图</strong></p><h4 id="3-8-4-确定性循环"><a href="#3-8-4-确定性循环" class="headerlink" title="3.8.4 确定性循环"></a>3.8.4 确定性循环</h4><p><code>for</code> 循环语句是支持迭代的一种通用结构，它由一个计数器或类似的变量控制迭代次数，每次迭代后这个变量将会更新。如图 3-12 所示，下面的循环将在屏幕上显示出打印数字 1～10：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">1</span>; i &lt;= <span class="hljs-number">10</span>; i++)<br>    System.out.println(i);<br></code></pre></td></tr></table></figure><p><code>for</code> 语句的第 1 部分通常是对计数器初始化；第 2 部分给出每次新一轮循环执行前要检测的循环条件；第 3 部分指定如何更新计数器。</p><p>尽管 Java 允许在 <code>for</code> 循环的各个部分放置任何表达式，但有一条不成立的规则：<code>for</code> 语句的 3 个部分应该对同一个计数器变量进行初始化、检测和更新。</p><p>下面这个倒计数的循环：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">10</span>; i &gt; <span class="hljs-number">0</span>; i--)<br>    System.out.println(<span class="hljs-string">&quot;Counting down . . . &quot;</span> + i);<br>System.out.println(<span class="hljs-string">&quot;Blastoff!&quot;</span>);<br></code></pre></td></tr></table></figure><p><strong>警告</strong>：在循环中，检测两个浮点数是否相等需要格外小心。下面的 for 循环：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">for</span> (<span class="hljs-type">double</span> <span class="hljs-variable">x</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>; x != <span class="hljs-number">10</span>; x += <span class="hljs-number">0.1</span>) ...<br></code></pre></td></tr></table></figure><p>可能永远不会结束。由于存在舍入误差，可能永远达不到精确的最终值。在这个例子中，因为 0.1 无法精确地用二进制表示，所以，x 将从 9.9999999999998 跳到 10.099999999999998。</p><p>在 for 语句的第 1 部分中声明一个变量之后，这个变量的作用域会扩展到这个 for 循环体的末尾。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">1</span>; i &lt;= <span class="hljs-number">10</span>; i++)<br>&#123;<br>    ...<br>&#125;<br><span class="hljs-comment">// i no longer defined here</span><br></code></pre></td></tr></table></figure><p>特别指出，如果在 for 语句内部定义一个变量，这个变量就不能在循环体之外使用。因此，如果希望在 for 循环体之外使用循环计数器的最终值，就要确保在循环体之外声明这个变量。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">int</span> i;<br><span class="hljs-keyword">for</span> (i = <span class="hljs-number">1</span>; i &lt;= <span class="hljs-number">10</span>; i++)<br>&#123;<br>    ...<br>&#125;<br><span class="hljs-comment">// i is still defined here</span><br></code></pre></td></tr></table></figure><p>另一方面，可以在不同的 for 循环中定义同名的变量：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">1</span>; i &lt;= <span class="hljs-number">10</span>; i++)<br>&#123;<br>    ...<br>&#125;<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">11</span>; i &lt;= <span class="hljs-number">20</span>; i++) <span class="hljs-comment">// OK to define another variable named i</span><br>&#123;<br>    ...<br>&#125;<br></code></pre></td></tr></table></figure><p><img src="/2025/05/04/hello-world/%E7%AC%AC%E4%B8%89%E7%AB%A0/image-20251029153845994.png" alt="image-20251029153845994"></p><p>​                                                                       <strong>图 3-12 for 语句的流程图</strong></p><p>for 循环语句只是 while 循环的一种简化形式。例如，</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">10</span>; i &gt; <span class="hljs-number">0</span>; i--)<br>    System.out.println(<span class="hljs-string">&quot;Counting down . . . &quot;</span> + i);<br></code></pre></td></tr></table></figure><p>可以重写为：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">10</span>;<br><span class="hljs-keyword">while</span> (i &gt; <span class="hljs-number">0</span>)<br>&#123;<br>    System.out.println(<span class="hljs-string">&quot;Counting down . . . &quot;</span> + i);<br>    i--;<br>&#125;<br></code></pre></td></tr></table></figure><h4 id="3-8-5-多重选择：switch-语句"><a href="#3-8-5-多重选择：switch-语句" class="headerlink" title="3.8.5 多重选择：switch 语句"></a>3.8.5 多重选择：switch 语句</h4><p>在处理同一个表达式的多个选项时，switch 语句会让这个工作变得容易，采用 Java 14 引入的形式时会更简单。</p><p>例如，如果建立一个如图 3-13 所示的菜单系统，其中包含 4 个选项，可以使用以下代码：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">Scanner</span> <span class="hljs-variable">in</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Scanner</span>(System.in);<br>System.out.print(<span class="hljs-string">&quot;Select an option (1, 2, 3, 4) &quot;</span>);<br><span class="hljs-type">int</span> <span class="hljs-variable">choice</span> <span class="hljs-operator">=</span> in.nextInt();<br><br><span class="hljs-keyword">switch</span> (choice)<br>&#123;<br>    <span class="hljs-keyword">case</span> <span class="hljs-number">1</span> -&gt; ...<br>    <span class="hljs-keyword">case</span> <span class="hljs-number">2</span> -&gt; ...<br>    <span class="hljs-keyword">case</span> <span class="hljs-number">3</span> -&gt; ...<br>    <span class="hljs-keyword">case</span> <span class="hljs-number">4</span> -&gt; ...<br>    <span class="hljs-keyword">default</span> -&gt; System.out.println(<span class="hljs-string">&quot;Bad input&quot;</span>);<br>&#125;<br></code></pre></td></tr></table></figure><p><strong>case 标签</strong>可以是：</p><ul><li>类型为 char、byte、short 或 int 的常量表达式</li><li>枚举常量</li><li>字符串字面量</li><li>多个字符串，用逗号分隔</li></ul><p>例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">String</span> <span class="hljs-variable">input</span> <span class="hljs-operator">=</span> ...;<br><span class="hljs-keyword">switch</span> (input.toLowerCase())<br>&#123;<br>    <span class="hljs-keyword">case</span> <span class="hljs-string">&quot;yes&quot;</span>, <span class="hljs-string">&quot;y&quot;</span> -&gt; ...<br>    <span class="hljs-keyword">case</span> <span class="hljs-string">&quot;no&quot;</span>, <span class="hljs-string">&quot;n&quot;</span> -&gt; ...<br>    <span class="hljs-keyword">default</span> -&gt; ...<br>&#125;<br></code></pre></td></tr></table></figure><p>switch 语句的”经典”形式如下：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">int</span> <span class="hljs-variable">choice</span> <span class="hljs-operator">=</span> ...;<br><span class="hljs-keyword">switch</span> (choice)<br>&#123;<br>    <span class="hljs-keyword">case</span> <span class="hljs-number">1</span>:<br>        ...<br>        <span class="hljs-keyword">break</span>;<br>     <span class="hljs-keyword">case</span> <span class="hljs-number">2</span>:<br>    ...<br>    <span class="hljs-keyword">break</span>;<br><span class="hljs-keyword">case</span> <span class="hljs-number">3</span>:<br>    ...<br>    <span class="hljs-keyword">break</span>;<br><span class="hljs-keyword">case</span> <span class="hljs-number">4</span>:<br>    ...<br>    <span class="hljs-keyword">break</span>;<br><span class="hljs-keyword">default</span>:<br>    <span class="hljs-comment">// bad input</span><br>    ...<br>    <span class="hljs-keyword">break</span>;<br></code></pre></td></tr></table></figure><p><img src="/%E3%80%8AJava%E6%A0%B8%E5%BF%83%E6%8A%80%E6%9C%AF%E5%8D%B7%E4%B8%80%E3%80%8B%E7%AC%AC%E4%B8%89%E7%AB%A0/image-20251029155524670.png" alt="image-20251029155524670"></p><p>​                                                               <strong>图 3-13 Switch 语句的流程图</strong></p><p>switch 语句从与选项值相匹配的 case 标签开始执行，直到遇到下一个 break 语句，或者执行到 switch 语句结束。如果没有匹配的 case 标签，则执行 default 子句（如果有 default 子句）。</p><p><strong>警告</strong>：<strong>有可能触发多个分支。如果忘记在一个分支末尾增加 break 语句，就会接着执行下一个分支！</strong></p><p>为了检测这种问题，编译代码时可以加上 <code>-Xlint:fallthrough</code> 选项，如下所示：</p><figure class="highlight cmake"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs cmake">javac -Xlint:fallthrough <span class="hljs-keyword">Test</span>.java<br></code></pre></td></tr></table></figure><p>这样一来，如果某个分支最后缺少一个 break 语句，编译器就会给出一个警告。如果你确实是想使用这种”直通式”（fallthrough）行为，可以为其外围方法加一个注解 <code>@SuppressWarnings(&quot;fallthrough&quot;)</code>。这样就不会对这个方法生成警告了。</p><p>这两种 switch 形式都是语句。switch 表达式没有”直通式”行为。</p><p>为了对称，Java 14 还引入了一个有直通行为的 switch 表达式，所以总共有 4 种不同形式的 switch。表 3-7 给出了这 4 种形式。</p><p>**在有直通行为的形式中，每个 case 以一个冒号结束。如果 case 以箭头 <code>-&gt;</code> 结束，则没有直通行为。**不能在一个 switch 语句中混合使用冒号和箭头。</p><p>注意 switch 表达式的 <code>yield</code> 关键字。与 <code>break</code> 类似，<code>yield</code> 会终止执行。但与 <code>break</code> 不同的是，<code>yield</code> 还会生成一个值，这就是表达式的值。</p><p>要在 switch 表达式的一个分支中使用语句而不想有直通行为，就必须使用大括号和 <code>yield</code>，如表中的示例所示，这个例子将为一个分支增加日志语句：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">case</span> <span class="hljs-string">&quot;Spring&quot;</span> -&gt; &#123;<br>    System.out.println(<span class="hljs-string">&quot;spring time!&quot;</span>);<br>    <span class="hljs-keyword">yield</span> <span class="hljs-number">6</span>;<br>&#125;<br></code></pre></td></tr></table></figure><p>switch 表达式的每个分支必须生成一个值。最常见的做法是，各个值跟在一个箭头 <code>-&gt;</code> 后面：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">case</span> <span class="hljs-string">&quot;Summer&quot;</span>, <span class="hljs-string">&quot;Winter&quot;</span> -&gt; <span class="hljs-number">6</span>;<br></code></pre></td></tr></table></figure><p>如果无法做到，则使用 <code>yield</code> 语句。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-comment">// 表 3-7  4 种 switch 形式</span><br><br><span class="hljs-comment">// 表达式形式 - 无直通行为</span><br><span class="hljs-type">int</span> <span class="hljs-variable">numLetters</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">switch</span> (seasonName) &#123;<br>    <span class="hljs-keyword">case</span> <span class="hljs-string">&quot;Spring&quot;</span> -&gt; &#123;<br>        System.out.println(<span class="hljs-string">&quot;spring time!&quot;</span>);<br>        <span class="hljs-keyword">yield</span> <span class="hljs-number">6</span>;<br>    &#125;<br>    <span class="hljs-keyword">case</span> <span class="hljs-string">&quot;Summer&quot;</span>, <span class="hljs-string">&quot;Winter&quot;</span> -&gt; <span class="hljs-number">6</span>;<br>    <span class="hljs-keyword">case</span> <span class="hljs-string">&quot;Fall&quot;</span> -&gt; <span class="hljs-number">4</span>;<br>    <span class="hljs-keyword">default</span> -&gt; -<span class="hljs-number">1</span>;<br>&#125;;<br><br><span class="hljs-comment">// 语句形式 - 无直通行为  </span><br><span class="hljs-keyword">switch</span> (seasonName) &#123;<br>    <span class="hljs-keyword">case</span> <span class="hljs-string">&quot;Spring&quot;</span> -&gt; &#123;<br>        System.out.println(<span class="hljs-string">&quot;spring time!&quot;</span>);<br>        numLetters = <span class="hljs-number">6</span>;<br>    &#125;<br>    <span class="hljs-keyword">case</span> <span class="hljs-string">&quot;Summer&quot;</span>, <span class="hljs-string">&quot;Winter&quot;</span> -&gt; numLetters = <span class="hljs-number">6</span>;<br>    <span class="hljs-keyword">case</span> <span class="hljs-string">&quot;Fall&quot;</span> -&gt; numLetters = <span class="hljs-number">4</span>;<br>    <span class="hljs-keyword">default</span> -&gt; numLetters = -<span class="hljs-number">1</span>;<br>&#125;<br><br><span class="hljs-comment">// 表达式形式 - 有直通行为</span><br><span class="hljs-type">int</span> <span class="hljs-variable">numLetters</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">switch</span> (seasonName) &#123;<br>    <span class="hljs-keyword">case</span> <span class="hljs-string">&quot;Spring&quot;</span>:<br>        System.out.println(<span class="hljs-string">&quot;spring time!&quot;</span>);<br>    <span class="hljs-keyword">case</span> <span class="hljs-string">&quot;Summer&quot;</span>, <span class="hljs-string">&quot;Winter&quot;</span>:<br>        <span class="hljs-keyword">yield</span> <span class="hljs-number">6</span>;<br>    <span class="hljs-keyword">case</span> <span class="hljs-string">&quot;Fall&quot;</span>:<br>        <span class="hljs-keyword">yield</span> <span class="hljs-number">4</span>;<br>    <span class="hljs-keyword">default</span>:<br>        <span class="hljs-keyword">yield</span> -<span class="hljs-number">1</span>;<br>&#125;;<br><br><span class="hljs-comment">// 语句形式 - 有直通行为</span><br><span class="hljs-keyword">switch</span> (seasonName) &#123;<br>    <span class="hljs-keyword">case</span> <span class="hljs-string">&quot;Spring&quot;</span>:<br>        System.out.println(<span class="hljs-string">&quot;spring time!&quot;</span>);<br>    <span class="hljs-keyword">case</span> <span class="hljs-string">&quot;Summer&quot;</span>, <span class="hljs-string">&quot;Winter&quot;</span>:<br>        numLetters = <span class="hljs-number">6</span>;<br>        <span class="hljs-keyword">break</span>;<br>    <span class="hljs-keyword">case</span> <span class="hljs-string">&quot;Fall&quot;</span>:<br>        numLetters = <span class="hljs-number">4</span>;<br>        <span class="hljs-keyword">break</span>;<br>    <span class="hljs-keyword">default</span>:<br>        numLetters = -<span class="hljs-number">1</span>;<br>&#125;<br></code></pre></td></tr></table></figure><p><strong>注释</strong>：完全可以在 switch 表达式的一个分支中抛出异常。例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">default</span> -&gt; <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">IllegalArgumentException</span>(<span class="hljs-string">&quot;Not a valid season&quot;</span>);<br></code></pre></td></tr></table></figure><p><strong>警告</strong>：switch 表达式的关键是生成一个值（或者产生一个异常而失败）。不允许”跳出”switch 表达式。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">default</span> -&gt; &#123; <span class="hljs-keyword">return</span> -<span class="hljs-number">1</span>; &#125; <span class="hljs-comment">// ERROR</span><br></code></pre></td></tr></table></figure><p>具体来说，不能在 switch 表达式中使用 <code>return</code>、<code>break</code> 或 <code>continue</code> 语句。</p><p>switch 表达式优于语句。如果每个分支会为一个变量赋值或方法调用计算值，则用一个表达式生成值，然后使用这个值。例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java">numLetters = <span class="hljs-keyword">switch</span> (seasonName) &#123;<br>    <span class="hljs-keyword">case</span> <span class="hljs-string">&quot;Spring&quot;</span>, <span class="hljs-string">&quot;Summer&quot;</span>, <span class="hljs-string">&quot;Winter&quot;</span> -&gt; <span class="hljs-number">6</span>;<br>    <span class="hljs-keyword">case</span> <span class="hljs-string">&quot;Fall&quot;</span> -&gt; <span class="hljs-number">4</span>;<br>    <span class="hljs-keyword">default</span> -&gt; -<span class="hljs-number">1</span>;<br>&#125;;<br></code></pre></td></tr></table></figure><p>要优于</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">switch</span> (seasonName) &#123;<br>    <span class="hljs-keyword">case</span> <span class="hljs-string">&quot;Spring&quot;</span>, <span class="hljs-string">&quot;Summer&quot;</span>, <span class="hljs-string">&quot;Winter&quot;</span> -&gt; numLetters = <span class="hljs-number">6</span>;<br>    <span class="hljs-keyword">case</span> <span class="hljs-string">&quot;Fall&quot;</span> -&gt; numLetters = <span class="hljs-number">4</span>;<br>    <span class="hljs-keyword">default</span> -&gt; numLetters = -<span class="hljs-number">1</span>;<br>&#125;<br></code></pre></td></tr></table></figure><p>只有在确实需要直通行为时，或者必须为一个 switch 表达式增加语句时，才需要使用 break 或 yield 。</p><p><strong>3.8.6 中断控制流程的语句</strong></p><p>尽管 Java 的设计者将 <code>goto</code> 仍作为一个保留字，但实际上并不打算在语言中包含 <code>goto</code>。</p><p>下面首先来看不带标签的 <code>break</code> 语句。与用于退出 <code>switch</code> 语句的 <code>break</code> 语句一样，它也可以用于退出循环。例如，</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">while</span> (years &lt;= <span class="hljs-number">100</span>)<br>&#123;<br>    balance += payment;<br>    <span class="hljs-type">double</span> <span class="hljs-variable">interest</span> <span class="hljs-operator">=</span> balance * interestRate / <span class="hljs-number">100</span>;<br>    balance += interest;<br>    <span class="hljs-keyword">if</span> (balance &gt;= goal) <span class="hljs-keyword">break</span>;<br>    years++;<br>&#125;<br></code></pre></td></tr></table></figure><p>循环开始时，如果 <code>years &gt; 100</code>，或者如果循环中间 <code>balance &gt;= goal</code>，则退出循环。当然，也可以在不使用 <code>break</code> 的情况下计算 <code>years</code> 的值，如下所示：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">while</span> (years &lt;= <span class="hljs-number">100</span> &amp;&amp; balance &lt; goal)<br>&#123;<br>    balance += payment;<br>    <span class="hljs-type">double</span> <span class="hljs-variable">interest</span> <span class="hljs-operator">=</span> balance * interestRate / <span class="hljs-number">100</span>;<br>    balance += interest;<br>    <span class="hljs-keyword">if</span> (balance &lt; goal)<br>    years++;<br>&#125;<br></code></pre></td></tr></table></figure><p>但是需要注意，在这个版本中，检测了两次 <code>balance &lt; goal</code>。为了避免重复检测，有些程序员更偏爱使用 <code>break</code> 语句。</p><p>Java 还提供了一种带标签的 <code>break</code> 语句，允许跳出多重嵌套的循环。</p><p>下面的例子展示了 <code>break</code> 语句如何工作。请注意，标签必须放在你想跳出的最外层循环之前，并且必须紧跟一个冒号。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">Scanner</span> <span class="hljs-variable">in</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Scanner</span>(System.in);<br><span class="hljs-type">int</span> n;<br>read_data:<br><span class="hljs-keyword">while</span> (...) <span class="hljs-comment">// this loop statement is tagged with the label</span><br>&#123;<br>    <span class="hljs-comment">// ...</span><br><span class="hljs-keyword">for</span> (...) <span class="hljs-comment">// this inner loop is not labeled</span><br>&#123;<br>    System.out.print(<span class="hljs-string">&quot;Enter a number &gt;= 0: &quot;</span>);<br>    n = in.nextInt();<br>    <span class="hljs-keyword">if</span> (n &lt; <span class="hljs-number">0</span>) <span class="hljs-comment">// should never happen-can&#x27;t go on</span><br>   <span class="hljs-keyword">break</span> read_data;<br>    <span class="hljs-comment">// break out of read_data loop</span><br>    ...<br>&#125;<br>&#125;<br><span class="hljs-comment">// this statement is executed immediately after the labeled break</span><br><span class="hljs-keyword">if</span> (n &lt; <span class="hljs-number">0</span>) <span class="hljs-comment">// check for bad situation</span><br>&#123;<br>    <span class="hljs-comment">// deal with bad situation</span><br>&#125;<br><span class="hljs-keyword">else</span><br>&#123;<br>    <span class="hljs-comment">// carry out normal processing</span><br>&#125;<br></code></pre></td></tr></table></figure><p>如果输入有误，执行带标签的 <code>break</code> 会跳转到带标签的语句块末尾。与任何使用 <code>break</code> 语句的代码一样，接下来需要检测循环是正常退出，还是由于 <code>break</code> 提前退出。</p><p><strong>注释：</strong> 可以将标签应用到任何语句，甚至可以应用到 <code>if</code> 语句或者块语句，如下所示：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs java">label:<br>&#123;<br>    ....<br>    <span class="hljs-keyword">if</span> (condition) <span class="hljs-keyword">break</span> label; <span class="hljs-comment">// exits block</span><br>    ...<br>&#125;<br><span class="hljs-comment">// jumps here when the break statement executes</span><br></code></pre></td></tr></table></figure><p>因此，如果确实希望使用 <code>goto</code> 语句，而且一个代码块恰好在你想要跳转到的位置之前结束，就可以使用 <code>break</code> 语句！另外需要注意，只能跳出语句块，而不能跳入语句块。</p><p>最后，还有一个 <code>continue</code> 语句。与 <code>break</code> 语句一样，它将中断正常的控制流程。<strong><code>continue</code> 语句将控制转移到最内层外围循环的首部</strong>。例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">Scanner</span> <span class="hljs-variable">in</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Scanner</span>(System.in);<br><span class="hljs-keyword">while</span> (sum &lt; goal)<br>&#123;<br>    System.out.print(<span class="hljs-string">&quot;Enter a number: &quot;</span>);<br>    n = in.nextInt();<br>    <span class="hljs-keyword">if</span> (n &lt; <span class="hljs-number">0</span>) <span class="hljs-keyword">continue</span>;<br>    sum += n; <span class="hljs-comment">// not executed if n &lt; 0</span><br>&#125;<br></code></pre></td></tr></table></figure><p>如果 <code>n &lt; 0</code>，则 <code>continue</code> 语句会越过当前循环体的剩余部分，直接跳到循环首部。</p><p>如果在 <code>for</code> 循环中使用 <code>continue</code> 语句，会跳转到 <code>for</code> 循环的”更新”部分。例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">for</span> (count = <span class="hljs-number">1</span>; count &lt;= <span class="hljs-number">100</span>; count++)<br>    &#123;<br>    System.out.print(<span class="hljs-string">&quot;Enter a number, -1 to quit: &quot;</span>);<br>    n = in.nextInt();<br>    <span class="hljs-keyword">if</span> (n &lt; <span class="hljs-number">0</span>) <span class="hljs-keyword">continue</span>;<br>    sum += n; <span class="hljs-comment">// not executed if n &lt; 0</span><br>&#125;<br></code></pre></td></tr></table></figure><p>如果 <code>n &lt; 0</code>，则 <code>continue</code> 语句将跳转到 <code>count++</code> 语句。</p><p>还有一种带标签的 <code>continue</code> 语句，将跳转到有匹配标签的循环的首部。</p><h4 id="3-9-大数"><a href="#3-9-大数" class="headerlink" title="3.9 大数"></a>3.9 大数</h4><p>如果基本的整数和浮点数精度不足以满足需求，那么可以使用 <code>java.math</code> 包中两个很有用的类：<code>BigInteger</code> 和 <code>BigDecimal</code>。这两个类可以处理包含任意长度数字序列的数值。<code>BigInteger</code> 类实现任意精度的整数运算，<code>BigDecimal</code> 实现任意精度的浮点数运算。</p><p>使用静态的 <code>valueOf</code> 方法可以将一个普通的数转换为大数：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">BigInteger</span> <span class="hljs-variable">a</span> <span class="hljs-operator">=</span> BigInteger.valueOf(<span class="hljs-number">100</span>);<br></code></pre></td></tr></table></figure><p>对于更长的数，可以使用一个带字符串参数的构造器：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">BigInteger</span> <span class="hljs-variable">realV</span> <span class="hljs-operator">=</span> BigInteger(<span class="hljs-string">&quot;222232244629420445529739893461909967206666939096499764999979600&quot;</span>);<br></code></pre></td></tr></table></figure><p>另外还有一些常量：<code>BigInteger.ZERO</code>、<code>BigInteger.ONE</code> 和 <code>BigInteger.TEN</code>，Java 9 之后还增加了 <code>BigInteger.TWO</code>。</p><p><strong>警告：</strong> 对于 <code>BigDecimal</code> 类，总是应当使用带一个字符串参数的构造器。还有一个 <code>BigDecimal(double)</code> 构造器，不过这个构造器本质上很容易产生舍入误差，例如，<code>new BigDecimal(0.1)</code> 会得到以下数位：</p><figure class="highlight apache"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs apache"><span class="hljs-attribute">0</span>.<span class="hljs-number">10000000000000055511151231257827021181583404541015625</span><br></code></pre></td></tr></table></figure><p>不能使用人们熟悉的算术运算符（如：<code>+</code> 和 <code>*</code>）来组合大数，而需要使用大数类中的 <code>add</code> 和 <code>multiply</code> 方法。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">BigInteger</span> <span class="hljs-variable">c</span> <span class="hljs-operator">=</span> a.add(b); <span class="hljs-comment">// c = a + b</span><br><span class="hljs-type">BigInteger</span> <span class="hljs-variable">d</span> <span class="hljs-operator">=</span> c.multiply(b.add(BigInteger.valueOf(<span class="hljs-number">2</span>))); <span class="hljs-comment">// d = c * (b + 2)</span><br></code></pre></td></tr></table></figure><h4 id="3-10-数组"><a href="#3-10-数组" class="headerlink" title="3.10 数组"></a>3.10 数组</h4><p>数组存储相同类型值的序列。</p><h5 id="3-10-1-声明数组"><a href="#3-10-1-声明数组" class="headerlink" title="3.10.1 声明数组"></a>3.10.1 声明数组</h5><p>数组是一种数据结构，用来存储同一类型值的集合。通过一个整型索引（index，或称下标）可以访问数组中的每一个值。例如，如果 <code>a</code> 是一个整型数组，<code>a[i]</code> 就是数组中索引为 <code>i</code> 的整数。</p><p>在声明数组变量时，需要指出数组类型（元素类型后面紧跟 <code>[]</code>）和数组变量名。例如，下面声明了整型数组 <code>a</code>：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">int</span>[] a;<br></code></pre></td></tr></table></figure><p>不过，这条语句只声明了变量 <code>a</code>，并没有将 <code>a</code> 初始化为一个真正的数组。应该使用 <code>new</code> 操作符创建数组。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">int</span>[] a = <span class="hljs-keyword">new</span> <span class="hljs-title class_">int</span>[<span class="hljs-number">100</span>]; <span class="hljs-comment">// or var a = new int[100];</span><br></code></pre></td></tr></table></figure><p>这条语句声明并初始化了一个可以存储 100 个整数的数组。</p><p>数组长度不要求是常量：<code>new int[n]</code> 会创建一个长度为 <code>n</code> 的数组。</p><p>一旦创建了数组，就不能再改变它的长度（不过，当然可以改变单个数组元素）。如果程序运行中需要经常扩展数组的大小，就应该使用另一种数据结构——数组列表（array list）。</p><p><strong>注释：</strong> 可以使用下面两种形式定义一个数组变量：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">int</span>[] a;<br></code></pre></td></tr></table></figure><p>或</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">int</span> a[];<br></code></pre></td></tr></table></figure><p>第一种风格，它可以将类型 <code>int[]</code>（整型数组）与变量名清晰地分开。</p><p>在 Java 中，提供了一种创建数组对象并同时提供初始值的简写形式。下面是一个例子：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">int</span>[] smallPrimes = &#123; <span class="hljs-number">2</span>, <span class="hljs-number">3</span>, <span class="hljs-number">5</span>, <span class="hljs-number">7</span>, <span class="hljs-number">11</span>, <span class="hljs-number">13</span> &#125;;<br></code></pre></td></tr></table></figure><p>请注意，这个语法中不需要使用 <code>new</code>，甚至不用指定长度。</p><p>最后一个值后面允许有逗号，如果你要不断为数组增加值，这样会很方便：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs java">String[] authors =<br>&#123;<br>    <span class="hljs-string">&quot;James Gosling&quot;</span>,<br>    <span class="hljs-string">&quot;Bill Joy&quot;</span>,<br>    <span class="hljs-string">&quot;Guy Steele&quot;</span>,<br>    <span class="hljs-comment">// add more names here and put a comma after each name</span><br>&#125;;<br></code></pre></td></tr></table></figure><p>还可以声明一个匿名数组（anonymous array）：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">new</span> <span class="hljs-title class_">int</span>[] &#123; <span class="hljs-number">17</span>, <span class="hljs-number">19</span>, <span class="hljs-number">23</span>, <span class="hljs-number">29</span>, <span class="hljs-number">31</span>, <span class="hljs-number">37</span> &#125;;<br></code></pre></td></tr></table></figure><p>这个表达式会分配一个新数组并填入大括号中提供的值。它会统计初始值个数，并相应地设置数组大小。可以使用这种语法重新初始化一个数组而无须创建新变量。例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">smallPrimes = <span class="hljs-keyword">new</span> <span class="hljs-title class_">int</span>[] &#123; <span class="hljs-number">17</span>, <span class="hljs-number">19</span>, <span class="hljs-number">23</span>, <span class="hljs-number">29</span>, <span class="hljs-number">31</span>, <span class="hljs-number">37</span> &#125;;<br></code></pre></td></tr></table></figure><p>这是以下语句的简写形式：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">int</span>[] anonymous = &#123; <span class="hljs-number">17</span>, <span class="hljs-number">19</span>, <span class="hljs-number">23</span>, <span class="hljs-number">29</span>, <span class="hljs-number">31</span>, <span class="hljs-number">37</span> &#125;;<br>smallPrimes = anonymous;<br></code></pre></td></tr></table></figure><p><strong>注释：</strong> 在 Java 中，允许有长度为 0 的数组。编写一个结果为数组的方法时，如果碰巧结果为空，这样一个长度为 0 的数组就很有用。可以如下构造一个长度为 0 的数组：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">new</span> <span class="hljs-title class_">elementType</span>[<span class="hljs-number">0</span>]<br></code></pre></td></tr></table></figure><p>或</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">new</span> <span class="hljs-title class_">elementType</span>[] &#123;&#125;<br></code></pre></td></tr></table></figure><p>注意，长度为 0 的数组与 <code>null</code> 并不相同。</p><h5 id="3-10-2-访问数组元素"><a href="#3-10-2-访问数组元素" class="headerlink" title="3.10.2 访问数组元素"></a>3.10.2 访问数组元素</h5><p>数组元素从 0 开始编号。最后一个合法的索引为数组长度减 1。在下面的例子中，索引值为 0～99。一旦创建了数组，就可以在数组中填入元素。例如，可以使用一个循环：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">int</span>[] a = <span class="hljs-keyword">new</span> <span class="hljs-title class_">int</span>[<span class="hljs-number">100</span>];<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>; i &lt; <span class="hljs-number">100</span>; i++)<br>    a[i] = i; <span class="hljs-comment">// fills the array with numbers 0 to 99</span><br></code></pre></td></tr></table></figure><p>创建一个数字数组时，所有元素都初始化为 0。<code>boolean</code> 数组的元素会初始化为 <code>false</code>。对象数组的元素则初始化为一个特殊值 <code>null</code>，表示这些元素（还）未存放任何对象。例如，</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">String[] names = <span class="hljs-keyword">new</span> <span class="hljs-title class_">String</span>[<span class="hljs-number">10</span>];<br></code></pre></td></tr></table></figure><p>会创建一个包含 10 个字符串的数组，（所有字符串都为 <code>null</code>）。如果希望这个数组包含空串，则必须为元素提供空串：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>; i &lt; <span class="hljs-number">10</span>; i++) names[i] = <span class="hljs-string">&quot;&quot;</span>;<br></code></pre></td></tr></table></figure><p><strong>警告：</strong> 如果创建了一个包含 100 个元素的数组，然后试图访问元素 <code>a[100]</code>（或在 0～99 之外的任何其他索引），就会出现”array index out of bounds”（数组索引越界）异常。</p><p>要想获得数组中的元素个数，可以使用 <code>array.length</code>。例如，</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>; i &lt; a.length; i++)<br>    System.out.println(a[i]);<br></code></pre></td></tr></table></figure><h5 id="3-10-3-for-each-循环"><a href="#3-10-3-for-each-循环" class="headerlink" title="3.10.3 for each 循环"></a>3.10.3 for each 循环</h5><p>Java 有一种功能很强的循环结构，可以用来依次处理数组（或者任何其他元素集合）中的每个元素，而不必考虑指定索引值。</p><p>这种增强的 <code>for</code> 循环形式如下：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">for</span> (元素类型 变量名 : 数组或集合) &#123;<br>    <span class="hljs-comment">// 循环体语句</span><br>&#125;<br></code></pre></td></tr></table></figure><p>它将给定变量设置为集合中的每一个元素，然后执行语句（statement）（当然，也可以是语句块）。<code>collection</code> 表达式必须是一个数组或者是一个实现了 <code>Iterable</code> 接口的类对象（例如 <code>ArrayList</code>）。</p><p>例如，</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> element : a)<br>    System.out.println(element);<br></code></pre></td></tr></table></figure><p>会打印数组 <code>a</code> 的每一个元素，一个元素占一行。</p><p>这个循环应该读作”循环 <code>a</code> 中的每一个元素”（for each element in a）。</p><p>当然，使用传统的 <code>for</code> 循环也可以获得同样的效果：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>; i &lt; a.length; i++)<br>    System.out.println(a[i]);<br></code></pre></td></tr></table></figure><p>但是，”for each”循环更加简洁、更不易出错。</p><p><strong>注释：</strong> “for each”循环的循环变量将会遍历数组中的每个元素，而不是索引值。</p><p><strong>提示：</strong> 有一个更容易的方法可以打印数组中的所有值，即利用 <code>Arrays</code> 类的 <code>toString</code> 方法。调用 <code>Arrays.toString(a)</code> 会返回一个包含数组元素的字符串，这些元素包围在中括号内，并用逗号分隔，例如，<code>&quot;[2,3,5,7,11,13]&quot;</code>。要想打印数组，只需要调用</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">System.out.println(Arrays.toString(a));<br></code></pre></td></tr></table></figure><h5 id="3-10-4-数组拷贝"><a href="#3-10-4-数组拷贝" class="headerlink" title="3.10.4 数组拷贝"></a>3.10.4 数组拷贝</h5><p>在 Java 中，允许将一个数组变量拷贝到另一个数组变量。这时，两个变量将引用同一个数组：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">int</span>[] luckyNumbers = smallPrimes;<br>luckyNumbers[<span class="hljs-number">5</span>] = <span class="hljs-number">12</span>; <span class="hljs-comment">// now smallPrimes[5] is also 12</span><br></code></pre></td></tr></table></figure><p>图 3-14 显示了拷贝的结果。</p><p><img src="/%E3%80%8AJava%E6%A0%B8%E5%BF%83%E6%8A%80%E6%9C%AF%E5%8D%B7%E4%B8%80%E3%80%8B%E7%AC%AC%E4%B8%89%E7%AB%A0/image-20251029231328013.png" alt="image-20251029231328013"></p><p>​                                                                       <strong>图 3-14 拷贝一个数组变量</strong></p><p>如果确实希望将一个数组的所有值拷贝到一个一个新的数组中，就要使用 Arrays 类的 copyOf 方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">int</span>[] copiedLuckyNumbers = Arrays.copyOf(luckyNumbers, luckyNumbers.length);<br></code></pre></td></tr></table></figure><p>第2个参数是新数组的长度。这个方法通常用来增加数组的大小：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">luckyNumbers = Arrays.copyOf(luckyNumbers, <span class="hljs-number">2</span> * luckyNumbers.length);<br></code></pre></td></tr></table></figure><p>如果数组元素是数值型，那么新增的元素将其入0；如果数组元素是布尔型，则填入false。相反，如果长度小于原数组的长度，则只拷贝前面的值。</p><p><strong>C++注释：</strong> Java数组与堆栈上的C++数组有很大不同，但基本上与在堆（heap）上分配的数组指针一样。也就是说，</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">int</span>[] a = <span class="hljs-keyword">new</span> <span class="hljs-title class_">int</span>[<span class="hljs-number">100</span>]; <span class="hljs-comment">// Java</span><br></code></pre></td></tr></table></figure><p>不同于</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs c++"><span class="hljs-type">int</span> a[<span class="hljs-number">100</span>]; <span class="hljs-comment">// C++</span><br></code></pre></td></tr></table></figure><p>而等同于</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs c++"><span class="hljs-type">int</span>* a = <span class="hljs-keyword">new</span> <span class="hljs-type">int</span>[<span class="hljs-number">100</span>]; <span class="hljs-comment">// C++</span><br></code></pre></td></tr></table></figure><p>Java 中的 <strong>[ ]</strong> 运算符预定义为会完成<strong>越界检查（bounds checking）</strong>。另外，没有指针运算，就意味着不能通过 a 加 1 得到数组中的下一个元素。</p><h5 id="3-10-5-命令行参数"><a href="#3-10-5-命令行参数" class="headerlink" title="3.10.5 命令行参数"></a>3.10.5 命令行参数</h5><p>每一个 Java 程序都有一个带<code>String arg[]</code>参数的 main 方法。这个参数表明 main 方法将接收一个字符串数组，也就是命令行上指定的参数。</p><p>例如，来看下面这个程序：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Message</span><br>&#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span><br>    &#123;<br>        <span class="hljs-keyword">if</span> (args.length == <span class="hljs-number">0</span> || args[<span class="hljs-number">0</span>].equals(<span class="hljs-string">&quot;-h&quot;</span>))<br>            System.out.print(<span class="hljs-string">&quot;Hello,&quot;</span>);<br>        <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (args[<span class="hljs-number">0</span>].equals(<span class="hljs-string">&quot;-g&quot;</span>))<br>            System.out.print(<span class="hljs-string">&quot;Goodbye,&quot;</span>);<br>        <span class="hljs-comment">// print the other command-line arguments</span><br>        <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">1</span>; i &lt; args.length; i++)<br>            System.out.print(<span class="hljs-string">&quot; &quot;</span> + args[i]);<br>        System.out.println(<span class="hljs-string">&quot;!&quot;</span>);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>如果如下调用这个程序：<br><code>java Message -g cruel world</code><br>args 数组将包含以下内容：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java">args[<span class="hljs-number">0</span>]: <span class="hljs-string">&quot;-g&quot;</span><br>args[<span class="hljs-number">1</span>]: <span class="hljs-string">&quot;cruel&quot;</span><br>args[<span class="hljs-number">2</span>]: <span class="hljs-string">&quot;world&quot;</span><br></code></pre></td></tr></table></figure><p>这个程序会显示下面这个消息：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">Goodbye, cruel world!<br></code></pre></td></tr></table></figure><p><strong>C++ 注释：</strong> 在 Java 程序的 main 方法中，程序名并不存储在 args 数组中。例如，从命令行如下运行一个程序时</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">java Message -h world<br></code></pre></td></tr></table></figure><p><code>args[0]</code> 是 <code>&quot;-h&quot;</code>，而不是<code> &quot;Message&quot;</code> 或 <code>&quot;java&quot;</code>。</p><h5 id="3-10-6-数组排序"><a href="#3-10-6-数组排序" class="headerlink" title="3.10.6 数组排序"></a>3.10.6 数组排序</h5><p>要想对数值型数组进行排序，可以使用 Arrays 类中的 sort 方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">int</span>[] a = <span class="hljs-keyword">new</span> <span class="hljs-title class_">int</span>[<span class="hljs-number">10000</span>];<br>. . .<br>Arrays.sort(a);<br></code></pre></td></tr></table></figure><p>这个方法使用了优化的快速排序（QuickSort）算法。快速排序算法对于大多数数据集都很高效。</p><p>要想选择一组随机的数字，首先将值 1,2,…,n 填入数组 numbers 中：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">int</span>[] numbers = <span class="hljs-keyword">new</span> <span class="hljs-title class_">int</span>[n];<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>; i &lt; numbers.length; i++)<br>    numbers[i] = i + <span class="hljs-number">1</span>;<br></code></pre></td></tr></table></figure><p>第二个数组存放抽取出来的数：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">int</span>[] result = <span class="hljs-keyword">new</span> <span class="hljs-title class_">int</span>[k];<br></code></pre></td></tr></table></figure><p>现在可以开始抽取 k 个数了。<code>Math.random</code> 方法将返回一个 0 到 1 之间（包含 0、不包含 1）的随机浮点数。用 n 乘以这个浮点数，可以得到从 0 到 n-1 之间的一个随机数。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">int</span> <span class="hljs-variable">r</span> <span class="hljs-operator">=</span> (<span class="hljs-type">int</span>) (Math.random() * n);<br></code></pre></td></tr></table></figure><p>下面将 result 的第 i 个元素设置为该索引对应的数（<code>numbers[r]</code>），最初是 r+1，但正如所看到的，numbers 数组的内容在每一次抽取之后都会发生变化。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">result[i] = numbers[r];<br></code></pre></td></tr></table></figure><p>现在，必须确保不会再次抽到那个数，因为所有抽形数字必须各不相同。因此，这里用数组中的最后一个数覆盖 number[r]，并将 n 减 1。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java">numbers[r] = numbers[n - <span class="hljs-number">1</span>];<br>n--;<br></code></pre></td></tr></table></figure><p>关键在于每次抽取的都是索引，而不是实际的值。这个索引指向一个数组，其中包含尚未抽取过的值。在抽取了 k 个数之后，可以对 result 数组进行排序，来得到更美观的输出：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java">Arrays.sort(result);<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> r : result)<br>    System.out.println(r);<br></code></pre></td></tr></table></figure><h5 id="3-10-7-多维数组"><a href="#3-10-7-多维数组" class="headerlink" title="3.10.7 多维数组"></a>3.10.7 多维数组</h5><p>多维数组使用多个索引访问数组元素，它适用于表格或其他更复杂的排列形式。</p><p>假设需要建立一个数值表格，用来显示在不同利率下投资 10 000 美元有多少收益，利息每年兑现并复投。</p><p>可以使用一个二维数组（也称为矩阵）来存储这些信息，名为 <code>balances</code>。在 Java 中，声明一个二维数组相当简单。例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">double</span>[][] balances;<br></code></pre></td></tr></table></figure><p>数组在进行初始化之前是不能使用的。在这里可以如下初始化：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">balances = <span class="hljs-keyword">new</span> <span class="hljs-title class_">double</span>[NYEARS][NRATES];<br></code></pre></td></tr></table></figure><p>其他情况下，如果知道数组元素，可以不调用 <code>new</code>，而直接使用一种简写形式对多维数组进行初始化。例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">int</span>[][] magicSquare =<br>&#123;<br>    &#123;<span class="hljs-number">16</span>, <span class="hljs-number">3</span>, <span class="hljs-number">2</span>, <span class="hljs-number">13</span>&#125;,<br>    &#123;<span class="hljs-number">5</span>, <span class="hljs-number">10</span>, <span class="hljs-number">11</span>, <span class="hljs-number">8</span>&#125;,<br>    &#123;<span class="hljs-number">9</span>, <span class="hljs-number">6</span>, <span class="hljs-number">7</span>, <span class="hljs-number">12</span>&#125;,<br>    &#123;<span class="hljs-number">4</span>, <span class="hljs-number">15</span>, <span class="hljs-number">14</span>, <span class="hljs-number">1</span>&#125;<br>&#125;;<br></code></pre></td></tr></table></figure><p>一旦初始化数组，就可以利用两对中括号访问单个元素，例如，<code>balances[i][j]</code>。</p><p><strong>注释：</strong> “for each”循环语句不会自动循环处理二维数组的所有元素。它会循环处理行，而这些行本身就是一维数组。要想访问二维数组 <code>a</code> 的所有元素，需要使用两个嵌套的循环，如下所示：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">for</span> (<span class="hljs-type">double</span>[] row : a)<br>    <span class="hljs-keyword">for</span> (<span class="hljs-type">double</span> value : row)<br>        <span class="hljs-keyword">do</span> something with value<br></code></pre></td></tr></table></figure><p><strong>提示：</strong> 要想快速地打印一个二维数组的元素列表，可以调用：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">System.out.println(Arrays.deepToString(a));<br></code></pre></td></tr></table></figure><p>输出格式为：</p><figure class="highlight inform7"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs inform7"><span class="hljs-comment">[<span class="hljs-comment">[16, 3, 2, 13]</span>, <span class="hljs-comment">[5, 10, 11, 8]</span>, <span class="hljs-comment">[9, 6, 7, 12]</span>, <span class="hljs-comment">[4, 15, 14, 1]</span>]</span><br></code></pre></td></tr></table></figure><h5 id="3-10-8-不规则数组"><a href="#3-10-8-不规则数组" class="headerlink" title="3.10.8 不规则数组"></a>3.10.8 不规则数组</h5><p>Java 实际上没有多维数组，只有一维数组。多维数组被解释为”数组的数组”。</p><p>例如，在前面的示例中，<code>balances</code> 数组实际上是一个包含 10 个元素的数组，而每个元素又是一个由 6 个浮点数组成的数组（请参见图 3-15）。</p><p><img src="/%E3%80%8AJava%E6%A0%B8%E5%BF%83%E6%8A%80%E6%9C%AF%E5%8D%B7%E4%B8%80%E3%80%8B%E7%AC%AC%E4%B8%89%E7%AB%A0/IMG20251030102018.jpg" alt="IMG20251030102018"></p><p>​                                                           <strong>图 3-15 一个二维数组</strong></p><p>表达式 <code>balances[i]</code> 指示第 <code>i</code> 个子数组，也就是表格的第 <code>i</code> 行。它本身也是一个数组，<code>balances[i][j]</code> 指示这个数组的第 <code>j</code> 个元素。</p><p>由于可以单独地访问数组的某一行，所以可以让两行交换。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">double</span>[] temp = balances[i];<br>balances[i] = balances[i + <span class="hljs-number">1</span>];<br>balances[i + <span class="hljs-number">1</span>] = temp;<br></code></pre></td></tr></table></figure><p>还可以很容易地构造一个”不规则”数组，即数组的每一行有不同的长度。下面是一个标准的示例。我们将创建一个数组，第 <code>i</code> 行第 <code>j</code> 列的元素将存放”从 <code>i</code> 个数中抽取 <code>j</code> 个数”可能的结果数。</p><figure class="highlight basic"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs basic"><span class="hljs-number">1</span><br><span class="hljs-symbol">1 </span><span class="hljs-number">1</span><br><span class="hljs-symbol">1 </span><span class="hljs-number">2</span> <span class="hljs-number">1</span><br><span class="hljs-symbol">1 </span><span class="hljs-number">3</span> <span class="hljs-number">3</span> <span class="hljs-number">1</span><br><span class="hljs-symbol">1 </span><span class="hljs-number">4</span> <span class="hljs-number">6</span> <span class="hljs-number">4</span> <span class="hljs-number">1</span><br><span class="hljs-symbol">1 </span><span class="hljs-number">5</span> <span class="hljs-number">10</span> <span class="hljs-number">10</span> <span class="hljs-number">5</span> <span class="hljs-number">1</span><br><span class="hljs-symbol">1 </span><span class="hljs-number">6</span> <span class="hljs-number">15</span> <span class="hljs-number">20</span> <span class="hljs-number">15</span> <span class="hljs-number">6</span> <span class="hljs-number">1</span><br></code></pre></td></tr></table></figure><p>由于 <code>j</code> 不可能大于 <code>i</code>，所以矩阵是三角形的。第 <code>i</code> 行有 <code>i + 1</code> 个元素（允许抽取 0 个元素，这种选择只有一种可能）。要想创建这样一个不规则的数组，首先需要分配一个数组包含这些行：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">final</span> <span class="hljs-type">int</span> <span class="hljs-variable">NMAX</span> <span class="hljs-operator">=</span> <span class="hljs-number">10</span>;<br><span class="hljs-type">int</span>[][] odds = <span class="hljs-keyword">new</span> <span class="hljs-title class_">int</span>[NMAX + <span class="hljs-number">1</span>][];<br></code></pre></td></tr></table></figure><p>接下来，分配这些行：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">n</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>; n &lt;= NMAX; n++)<br>    odds[n] = <span class="hljs-keyword">new</span> <span class="hljs-title class_">int</span>[n + <span class="hljs-number">1</span>];<br></code></pre></td></tr></table></figure><p>分配了数组之后，可以采用通常的方式访问其中的元素（前提是没有超出边界）：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">n</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>; n &lt; odds.length; n++)<br>    <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">k</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>; k &lt; odds[n].length; k++)<br>    &#123;<br>        <span class="hljs-comment">// compute lotteryOdds</span><br>        ...<br>        odds[n][k] = lotteryOdds;<br>    &#125;<br></code></pre></td></tr></table></figure>]]></content>
    
    
    <categories>
      
      <category>Java</category>
      
    </categories>
    
    
    <tags>
      
      <tag>原创</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>基础</title>
    <link href="/2025/06/14/%E5%9F%BA%E7%A1%80/"/>
    <url>/2025/06/14/%E5%9F%BA%E7%A1%80/</url>
    
    <content type="html"><![CDATA[<p>JDK &#x3D; JRE + java开发工具<br>JRE &#x3D; JVM + 核心类库</p><p>javac 编译，得到对应的 .class 字节码文件<br>java 运行，本质就是把 .class 加载到 jvm 运行</p><p>Java对各种变量、方法和类等命名时使用的字符序列称为标识符</p><p>凡是自己可以起名字的都叫标识符，<br>例如：类名，方法名，变量名，常量名等</p><p>标识符命名规则：<br>1.26个英文字母大小写    0-9    __ 或 $<br>2.数字不可以开头<br>3.不可以使用关键字和保留字，但能包含<br>4.严格区分大小写，长度无限制<br>5.不能含空格</p><p>![Pasted image 20250308153640](Pasted image 20250308153640-1768800958172-1.png)</p><p>命名规范：<br>1.包名：多单词组成时所有字母都小写<br>2.类名，接口名：多单词组成时，所有单词首字母大写<br>3.变量名，方法名：多单词组成时，第一个单词首字母小写，后续单词首字母大写<br>4.常量名：所有字母大写，多单词时各单词用下划线连接</p><p>+号的使用<br>当左右两边都是数值型时，做加法运算<br>当左右两边有一边为字符串，则做拼接运算<br>顺序从左至右</p><p><img src="/%E5%9F%BA%E7%A1%80.assets/1.png" alt="1"></p><p>![Pasted image 20250308153750](Pasted image 20250308153750-1768800986825-3.png)</p><p>![Pasted image 20250308153818](Pasted image 20250308153818-1768800999396-5.png)</p>]]></content>
    
    
    <categories>
      
      <category>Java</category>
      
    </categories>
    
    
    <tags>
      
      <tag>原创</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Hello World</title>
    <link href="/2025/05/04/hello-world/"/>
    <url>/2025/05/04/hello-world/</url>
    
    <content type="html"><![CDATA[<p>Welcome to <a href="https://hexo.io/">Hexo</a>! This is your very first post. Check <a href="https://hexo.io/docs/">documentation</a> for more info. If you get any problems when using Hexo, you can find the answer in <a href="https://hexo.io/docs/troubleshooting.html">troubleshooting</a> or you can ask me on <a href="https://github.com/hexojs/hexo/issues">GitHub</a>.</p><h2 id="Quick-Start"><a href="#Quick-Start" class="headerlink" title="Quick Start"></a>Quick Start</h2><h3 id="Create-a-new-post"><a href="#Create-a-new-post" class="headerlink" title="Create a new post"></a>Create a new post</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">$ hexo new <span class="hljs-string">&quot;My New Post&quot;</span><br></code></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/writing.html">Writing</a></p><h3 id="Run-server"><a href="#Run-server" class="headerlink" title="Run server"></a>Run server</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">$ hexo server<br></code></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/server.html">Server</a></p><h3 id="Generate-static-files"><a href="#Generate-static-files" class="headerlink" title="Generate static files"></a>Generate static files</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">$ hexo generate<br></code></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/generating.html">Generating</a></p><h3 id="Deploy-to-remote-sites"><a href="#Deploy-to-remote-sites" class="headerlink" title="Deploy to remote sites"></a>Deploy to remote sites</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">$ hexo deploy<br></code></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/one-command-deployment.html">Deployment</a></p>]]></content>
    
    
    
  </entry>
  
  
  
  
</search>
